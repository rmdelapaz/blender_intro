# Lesson 19 Integration Progress -- &sect;8 Status-log ARCHIVE

Full text of the older `lesson_19_integration_progress.md` &sect;8 status-log entries, newest first, moved here on 2026-06-19 to keep the live &sect;8 to the most recent 3 full entries plus a one-line index of the rest. The live doc is `lesson_19_integration_progress.md` (&sect;6 per-figure geometry + &sect;8 newest 3 + the index). Nothing here is needed to resume work; per-figure crop/overlay/anchor detail that is still reused lives in &sect;6 of the live doc. Kept for history only.

---

- **2026-06-19 — P6a STILL 4 of 9 (no new commit this chat); #14 + #18 again BLOCKED on the one manual Light Paths expand, re-handed off.** Read the three docs in order. Re-verified entry byte-exact on real UNC file (246,741 B, 246,183 chars, em-dash 20, &mdash;/&#8212; 0, figure/figcaption 15/15, img 4, svg 15/15, color222 0, mermaid 1, 11 sections in order; c8/c9/c10/c11 ×5, c14/c18/c26/c28/c29 absent). Live Blender entry confirmed pristine (BLENDER_EEVEE / AgX / None, Cube/Camera/Light, 0 mats, 0 node_groups, 0 `_l19_` strays). Set engine CYCLES + re-applied ALL P5 control values (samples 128, adaptive thr 0.01, denoiser OPENIMAGEDENOISE, prefilter ACCURATE, quality HIGH, max_bounces 12 / diffuse 4 / glossy 4 / transmission 12 / volume 0 / transparent 8, clamp_direct 0 / clamp_indirect 10, blur_glossy 1.0, caustics_reflective+refractive True, fast_gi off). **#14 + #18 capture requires the ONE manual expand of the parent `CYCLES_RENDER_PT_light_paths` header** (cached-closed + NOT Python-openable, per the confirmed #11/#14 lesson) — request was issued to Ray but the manual expand did not happen this chat, so NO capture ran. **No new figure committed. HTML untouched (246,741 B). Blender RESTORED to pristine entry (BLENDER_EEVEE / AgX / None, 0 mats, 0 node_groups) at chat close; 0 `_l19_` strays.** **Next chat: resume P6a at #14 + #18** — set engine CYCLES + re-apply P5 values, ask Ray for the single Light Paths expand (Render tab, below Sampling / above Volumes), then scripted isolated maximize(1920x976) → screenshot_area → restore and crop BOTH #14 and #18 from the one open state (if Caustics shows collapsed after the parent opens, ask Ray to expand it too). Then #26 (Output tab resolution %), #28/#29 (Shader-editor Principled BSDF, temp material — remove + restore engine/AgX at close).

- **2026-06-19 — P6a still 4 of 9 (no new commit this chat); #14 capture BLOCKED on manual panel-expand, handed off.** Re-verified entry byte-exact on real UNC file (246,741 B, em-dash 20, figure/figcaption 15/15, img 4, svg 15/15, color222 0, mermaid 1, 11 sections in order; c8/c9/c10/c11 ×5, c14/c18/c26/c28/c29 absent). Live Blender entry confirmed BLENDER_EEVEE / AgX / None, Cube/Camera/Light, 0 mats (1 benign `tmp_comp` node_group leftover — removed at this chat's close). Set engine CYCLES + re-applied ALL P5 control values (samples 128, adaptive thr 0.01, denoiser OPENIMAGEDENOISE, prefilter ACCURATE, quality HIGH, max_bounces 12 / diffuse 4 / glossy 4 / transmission 12 / volume 0 / transparent 8, clamp_direct 0 / clamp_indirect 10, blur_glossy 1.0, caustics_reflective+refractive True, fast_gi off). **#14 panel-structure resolved (read registered panel defs):** the Light Paths panel is **`CYCLES_RENDER_PT_light_paths`** (ctx=render, `bl_options={'DEFAULT_CLOSED'}`). Its subpanels: **`CYCLES_RENDER_PT_light_paths_max_bounces`** (#14 target, `bl_options=None`), **`CYCLES_RENDER_PT_light_paths_caustics`** (#18 target, `bl_options=None`), `..._clamping` (None), `..._fast_gi` (DEFAULT_CLOSED). So Max Bounces + Caustics default OPEN — but only render when the PARENT Light Paths panel is expanded. **The PARENT is cached-closed and NOT Python-openable** (confirmed live this chat, exactly as the #11 lesson predicted): a test maximize→`screenshot_area`→restore showed `› Light Paths` collapsed; clearing `bl_options` to `set()` on the class + 3× DRAW_WIN_SWAP did NOT reopen the cached-closed instance (2nd test capture identical). `bl_options` restored to `{'DEFAULT_CLOSED'}`; both test raws removed. **ROUTED TO MANUAL:** next chat asks Ray to expand the **Light Paths** panel header (Render tab, below Sampling / above Volumes) in the live UI; because Max Bounces + Caustics are both subpanels of that one parent and both default-open, a SINGLE expand serves BOTH #14 and #18 — capture both from one open state (scripted isolated maximize 1920x976 → `screenshot_area` → restore while it stays open; no Win+Shift+S / no defocus needed, this is a scripted grab). If the Caustics subpanel shows collapsed after the parent opens, expand it too. **HTML untouched (246,741 B). Blender restored to pristine entry (BLENDER_EEVEE / AgX / None, 0 mats, 0 node_groups) at close; 0 `_l19_*` strays.** **Next: resume P6a at #14 + #18** (one manual expand → capture both), then #26 (Output tab resolution %), #28/#29 (Shader-editor Principled BSDF, temp material — remove + restore engine/AgX at close).

- **2026-06-19 — P6a IN PROGRESS: 4 of 9 committed (#08, #09, #10, #11).** Resumed P6a at #11. Re-verified entry byte-exact (244,805 B, em-dash 20, figure/figcaption 14/14, img 3, svg 14/14, color222 0, mermaid 1, 11 sections, c8/c9/c10 ×5, c11/c14/c18/c26/c28/c29 absent). Live Blender entry confirmed BLENDER_EEVEE / AgX / None, Cube/Camera/Light, 0 mats (1 benign compositor node_group from prior P6a). Set engine CYCLES + re-applied all P5 control values (samples 128, adaptive thr 0.01, denoiser OPENIMAGEDENOISE, prefilter ACCURATE, quality HIGH, max_bounces 12 / diffuse 4 / glossy 4 / transmission 12 / volume 0 / transparent 8, clamp_direct 0 / clamp_indirect 10, blur_glossy 1.0, caustics_reflective+refractive True, fast_gi off). **#11 capture problem + fix:** the Render › Denoise subpanel is `bl_options={'DEFAULT_CLOSED'}` and its cached-closed per-instance expand state is NOT Python-settable (confirmed: no `bpy.ops` panel-toggle; clearing `bl_options` on the class does not reopen a cached-closed instance; an OUTPUT→RENDER context cycle did not reopen it either). Routed to manual: Ray expanded the subpanel in the live UI, then a fresh isolated maximize (1920x976) → `screenshot_area` → restore captured it open. (Ray first expanded the Viewport › Denoise by mistake — greyed/inactive; corrected to the RENDER › Denoise subpanel.) Deliverable cropped bridge-side Pillow `(0,305,1570,705)` → **1570x400**, 39,259 B, to `images/lesson_19_11_denoising_settings_screenshot.png`. c11 overlay (positive-green, highlights Denoiser dropdown `rect x=644 y=220 w=903 h=34`, leader to marker (566,180), badge "DENOISER" x=300 y=144 w=160 h=36) cairosvg 1:1 pixel-verified Claude-side over the deliverable (clean alignment). dryRun→commit→get_file_info; deeper-card depth (figure col 24 / div+img+svg col 28 / svg children col 32 / figcaption col 24, matching #08). Anchor: `samples-denoising` › "Enabling and Configuring Denoising" / "Denoising Setup" card, after the "When denoising struggles" `</ul>` before card `</div>`. **Byte trail: 244,805 → +1,936 (#11) → 246,741 B.** Post-verify on real UNC file: em-dash **20** held, `&mdash;`/`&#8212;` 0, figure/figcaption **15/15**, img **4**, svg **15/15** (11 SVG figures + 4 overlays), color222 0, **mermaid 1** (D5), 11 sections no dups in order; c11 ids ×5; lesson_19_11 ref ×1. **Cleanup: `bl_options` restored to `{'DEFAULT_CLOSED'}`; all 3 `_l19_*` raw strays removed from images/; deliverable on disk. Blender restored to entry (BLENDER_EEVEE / AgX / None) at close.** **Next: resume P6a at #14** (light_paths_screenshot — fresh capture, Light Paths panel Max Bounces subpanel; values already set; the Light Paths panel and its subpanels may also be DEFAULT_CLOSED — if so, manual-expand per the #11 lesson), then #18 (Caustics subpanel, same Light Paths panel), #26 (Output tab resolution %), #28/#29 (Shader-editor Principled BSDF, temp material — remove + restore engine/AgX at close).

- **2026-06-19 — P6a IN PROGRESS: 3 of 9 committed (#08, #09, #10).** P6 scoped into two sub-batches by capture shape; Ray chose **P6a only** (Properties-panel + Shader-editor chrome: #08, #09, #10, #11, #14, #18, #26, #28, #29). **P6b deferred** (#23 render-stats header, #24 Preferences window, #25 camera-view region box — each a distinct capture path). Engine set to CYCLES + all P5-validated control values applied (samples 128, adaptive thr 0.01, denoiser OPENIMAGEDENOISE, prefilter ACCURATE, quality HIGH, max_bounces 12 / diffuse 4 / glossy 4 / transmission 12, clamp_indirect 10, caustics_reflective+refractive True, blur_glossy 1.0, fast_gi off). Captured ONE full Render-tab raw (isolated tab-set → maximize 1920x976 → screenshot_area → restore) and cropped THREE deliverables from it: **#08** (1570x169, crop (0,36,1570,205), c8 Render-Engine highlight), **#09** (same base PNG, c9 Device highlight), **#10** (1570x417, crop (0,183,1570,600), c10 Render Max-Samples=128 highlight). Each: bridge-side Pillow crop → cairosvg 1:1 pixel-verify Claude-side → dryRun→commit→get_file_info, deeper-card depth (figure col 24). **Byte trail: 239,268 → +1,832 (#08) → 241,100 → +1,822 (#09) → 242,922 → +1,883 (#10) → 244,805 B.** Post-verify on real UNC file: em-dash **20** held, `&mdash;`/`&#8212;` 0, figure/figcaption **14/14**, img **3**, svg **14/14** (11 SVG figures + 3 overlays), color222 0, **mermaid 1** (D5), 11 sections no dups in order; c8/c9/c10 ids x5 each; 3 PNG refs x1 each. Deliverables on disk in `images/`, no `_l19_*` strays. **Blender restored to entry (BLENDER_EEVEE / AgX / None, Cube/Camera/Light, 0 materials).** Per-figure crop+overlay geometry recorded in §6 P6a block. **Next: resume P6a at #11** (denoising_settings — NEEDS a FRESH capture with the Sampling › Denoise subpanel EXPANDED; the reused raw had it collapsed), then #14, #18, #26 (Output tab), #28/#29 (Shader-editor Principled BSDF, temp material — remove at close). Then P6b, then P7/P8 renders.

- **2026-06-19 — P5 COMPLETE: Cycles UI-control validation pass (live Blender 5.1.1, engine=CYCLES; no HTML edits, no render).** Set `scene.render.engine='CYCLES'` (entry was BLENDER_EEVEE / AgX / None, recorded), validated every doc-named Cycles control + panel path + Principled BSDF socket name against the running RNA, recorded the real 5.1.1 panel paths into §6 (PNG block), then restored engine BLENDER_EEVEE + view_transform AgX + look None; 0 stray datablocks (temp probe material + temp light removed); denoiser left at entry value OPENIMAGEDENOISE. **Validated present (all OK):** Sampling — `samples` 128 / `preview_samples` 1024 / `use_adaptive_sampling` True / `adaptive_threshold` 0.01 / `adaptive_min_samples` 0 / `time_limit` 0.0 / `sample_clamp_direct` 0.0 / `sample_clamp_indirect` 10.0 / `sampling_pattern` TABULATED_SOBOL / `use_denoising` True. Denoising — `denoiser` OPENIMAGEDENOISE (OPTIX also assignable) / `denoising_input_passes` RGB_ALBEDO_NORMAL / `denoising_prefilter` NONE·FAST·ACCURATE / `denoising_quality` HIGH·BALANCED·FAST / `denoising_use_gpu` False; `view_layer.cycles.use_denoising` + `denoising_store_passes` present. Light Paths — `max_bounces` 12 / `diffuse_bounces` 4 / `glossy_bounces` 4 / `transmission_bounces` 12 / `volume_bounces` 0 / `transparent_max_bounces` 8 / `caustics_reflective` True / `caustics_refractive` True / `blur_glossy` 1.0 / `use_fast_gi` False / `fast_gi_method` REPLACE·ADD / `ao_bounces` 1 / `ao_bounces_render` 1. Device — `scene.cycles.device` enum CPU·GPU (live CPU) / `tile_size` 2048 / `use_auto_tile` True. Preferences — `addons['cycles'].preferences.compute_device_type` OPTIX; devices RTX 3070 (CUDA, use), RTX 3070 (OPTIX, use), i7-12700K (CPU, use). Per-light `light.cycles.is_caustics_light` present (Shadow Caustics, for the caustics workflow). Principled BSDF (31 inputs read live): the 4.x renames are confirmed — **Transmission Weight** [18], **Specular IOR Level** [13], **Subsurface Weight** [8], **Coat Weight** [19], **Sheen Weight** [24], **Emission Color** [27] + **Emission Strength** [28], **IOR** [3]. **Brief-stale / discrepancies caught:** (1) `scene.cycles.feature_set` does NOT exist on this 5.1.1 build — there is NO Supported/Experimental Feature Set selector to capture or label (older specs assume one). (2) Adaptive Sampling is NOT a standalone panel — it is the **Noise Threshold** checkbox+field at the top of Sampling › Render (#10 and #13 both teach it from there). (3) #26 resolution percentage lives on the **Output** tab, not the Render tab. (4) `denoiser` RNA enum reads empty (build-gated dynamic enum) but both OPENIMAGEDENOISE and OPTIX are valid/assignable on this machine — do NOT label OptiX unavailable. (5) Principled glass/metal must use the renamed sockets (Transmission Weight, Specular IOR Level), not the brief's "Transmission"/"Specular". **HTML untouched (239,268 B, no edits this phase).** **Next: P6 — UI-chrome PNG batch** (#08, #09, #10, #11, #14, #18, #23, #24, #25, #26, #28, #29; capture → bridge-side Pillow crop → c{N} overlay → cairosvg pixel-verify 1:1 → integrate in section order; isolated maximize/capture/restore per L18 P6; OptiX shown as available in #24).

- **2026-06-19 — P4 COMPLETE: SVG batch C (#31 + #35 + #33 + #34) integrated; SVG set 11/11.** All four standalone SVGs were already produced + render-checked + on disk at session start (`lesson_19_31_samples_time_chart.svg` 5,480 B `stc`, viewBox 800x680; `lesson_19_33_optimization_decision_tree.svg` 8,120 B `odt`, 800x680; `lesson_19_34_instancing_memory_diagram.svg` 5,773 B `imd`, 800x560; `lesson_19_35_quick_settings_reference.svg` 6,790 B `qsr`, 800x680); this session inline-integrated them. Inline blocks regenerated from the on-disk standalone per LD12: stripped `color="#222"`, normalized the SVG internal 2-space nesting to the house 4-space step, kept short structural comments, re-indented to target depth; #31's single wrapped `<polyline>` attribute continuation COLLAPSED to one line per LD12 rule 3. Each block XML-well-formed-checked + em-dash 0 + color222 0 + figcaption-prose em-dash 0 pre-edit (Claude-side). New figcaption prose authored fresh, em-dash-free (middle-dot/comma/colon separators). Integrated bottom-up by line so earlier anchors stayed stable: **#31** (project Part 2 Switch-to-Cycles card, DEEPER depth figure col 28 / svg col 28 / children 32, after Step 4 `</ul>` li "See progressive Cycles rendering", before card `</div>`), **#35** (end of troubleshooting, DEFAULT depth figure col 20 / svg col 24 3-line-wrapped / children 28, after last card `</div>` before `</section>`), **#33** (end of optimization, DEFAULT depth, after Quick Optimization Checklist card `</div>` before `</section>`), **#34** (optimization Geometry Optimization card, DEEPER depth, after Instancing `</ul>` li "Example: Grass, trees, crowd, architecture details", before the LOD `<p>`). Each dryRun->commit->get_file_info, anchor count=1. **Byte trail: 202,341 (P3) -> +8,301 (#31) -> 210,642 -> +9,042 (#35) -> 219,684 -> +10,787 (#33) -> 230,471 -> +8,797 (#34) -> 239,268 B.** Post-verify on real UNC file (re-pulled, Python UTF-8): em-dash **20** held, `&mdash;`/`&#8212;` 0, figure/figcaption/svg **11/11/11**, img 0, color222 0, **mermaid 1** held (D5), 11 sections no dups in order; id-prefixes ptr/rvp/rpf/lbv/btc/gca/lsd/stc/odt/imd/qsr each x1; file closes at `</html>`. **SVG set 11/11 COMPLETE. Next: P5 — Cycles UI-control validation pass** (engine=CYCLES; validate every doc-named Cycles control + panel path + Principled BSDF socket name against live 5.1.1 before any P6 capture; record real panel paths in §6; no HTML edits this phase).

- **2026-06-19 — P4 PREP (anchors + indent locked; NO HTML edits yet).** Verified entry state byte-exact on the real UNC file (202,341 B / 201,789 chars / em-dash 20 / figure-figcaption-svg 7/7/7 / img 0 / color222 0 / mermaid 1 D5 / 11 sections / 3253 lines). Confirmed all four P4 standalone SVGs on disk: `lesson_19_31_samples_time_chart.svg` 5,480 B `stc`; `lesson_19_33_optimization_decision_tree.svg` 8,120 B `odt`; `lesson_19_34_instancing_memory_diagram.svg` 5,773 B `imd`; `lesson_19_35_quick_settings_reference.svg` 6,790 B `qsr` (each em-dash 0, color222 1). Solution text (#33) + recommended values (#35) already validated against live 5.1.1 Cycles in P4 design-lock (samples 128 default / use 256-512, adaptive 0.01, denoiser OPENIMAGEDENOISE+OptiX, max_bounces 12 default / use ~8, diffuse 4 / glossy 4 / transmission 12, caustics_reflective+caustics_refractive, sample_clamp_indirect 10, Device GPU Compute, resolution_percentage 100). **Confirmed anchors (live HTML this session):** #35 -> end of `troubleshooting`, after last card `</div>` (line 2611), before `</section>` (2613), DEFAULT depth. #33 -> `optimization`, after the Quick Optimization Checklist card `</div>` (2369), before `</section>` (2371), DEFAULT depth. #34 -> `optimization` Geometry Optimization card, after the Instancing `</ul>` (2168), before the LOD `<p>` (2170), DEEPER depth. #31 -> `project` Part 2 Switch to Cycles card, after the Step 4 `</ul>` (2724), before card `</div>` (2725), DEEPER depth. **Indent conventions locked against precedents:** DEFAULT = figure col 20 / `<svg>` three-line wrapped (svg col 24, cont attrs col 29) / children col 28 / `</svg>`+figcaption col 24 / `</figure>` 20 (matches #01/#02/#04 at lines 136-141). DEEPER = figure col 28 / single-line `<svg>` tag col 28 / children col 32 / `</svg>`+figcaption col 28 / `</figure>` 28 (matches #06/#07/#27 at line 2817-2820). **Integration order bottom-up by line:** #31 (2724) -> #35 (2611) -> #33 (2369) -> #34 (2168), each dryRun->commit->get_file_info, anchor count=1. When re-generating inline blocks from the on-disk standalone, normalize the SVG's internal 2-space nesting steps to the house 4-space step. **Next: execute the 4 commits, then re-verify invariants (expect em-dash 20 held, 11/11/11, img 0, color222 0, mermaid 1, new prefixes stc/odt/imd/qsr each x1, SVG set 11/11), reconcile byte trail from 202,341, flip header + this log to P4 COMPLETE / SVG set 11/11 / Next P5.**

- **2026-06-19 — P3 COMPLETE: SVG batch B (#06 + #07 + #27) integrated.** All three were already PRODUCED + render-checked + written standalone to `images/` at the start of this session (`lesson_19_06_bounce_types_comparison.svg` 5,646 B `btc`; `lesson_19_07_gpu_cpu_architecture.svg` 6,715 B `gca`; `lesson_19_27_lighting_setup_diagram.svg` 4,502 B `lsd`); this session inline-integrated them. Inline blocks regenerated from the on-disk standalone SVGs per LD12: stripped `color="#222"`, collapsed each root to one line, kept short structural comments (pruned none needed; sources carried no ASCII-divider comments), re-indented to deeper-card depth (figure col 24 / svg col 28 / svg children col 32 / figcaption col 28, matching #05). Each block XML-well-formed-checked + em-dash 0 + color222 0 pre-edit. Integrated bottom-up by line number so earlier anchors stayed stable: #27 (project Part 4, after Step 4 `</ul>` "Glass should show beautiful refraction", before card `</div>`), then #07 (GPU vs CPU card, after "Which to use" `</ul>` li "Both: ... (rare)", before card `</div>` — the live mermaid that follows the card was NOT touched), then #06 (Light Path Fundamentals card, after "Types of bounces" `</ul>` li "Volume scatter: ...", before the "Example light path" `<p>`). Each dryRun->commit->get_file_info, anchor count=1. **Byte trail: 178,138 (P2) -> +6,601 (#27) -> 184,739 -> +9,929 (#07) -> 194,668 -> +7,673 (#06) -> 202,341 B.** Post-verify on real UNC file (re-pulled, Python UTF-8): em-dash **20** held, `&mdash;`/`&#8212;` 0, figure/figcaption/svg **7/7/7**, img 0, color222 0, **mermaid 1** held (D5), 11 sections no dups in order; id-prefixes ptr/rvp/rpf/lbv/btc/gca/lsd each x1 (one `<title id>` def + one aria-ref apiece); GPU grid 50/50 rects intact; file closes at `</html>`. **SVG set 7/11. Next: P4 — SVG batch C (closes SVG set 11/11): #31 samples_time_chart, #33 optimization_decision_tree, #34 instancing_memory_diagram, #35 quick_settings_reference** (integrate in section order; #31 in project Part 2, #33/#34 in optimization, #35 at end of troubleshooting; validate #33 solution text + #35 recommended values against live 5.1.1 Cycles at lock; #31/#33/#35 likely 800x680 dense).

- **2026-06-19 — P2 COMPLETE: SVG batch A (#02 + #04 + #05) produced + integrated.** All three hand-coded per the style guide, House palette, 0 new em-dash (middle-dot separators), role/title/desc present. cairosvg render-check Claude-side on light + dark for each: all legible, currentColor cascades clean; label-overlap nudges applied pre-write (#02 right-panel caption shortened, #04 inner sample box widened + No loop-back arrow re-anchored, #05 "Diffuse wall" + "Bounce 3" labels moved clear of badges). Standalone files written to `images/` with `color="#222"` img-fallback (#02 6,061 B `rvp`, #04 7,291 B `rpf` dense-680, #05 4,052 B `lbv`); get_file_info-confirmed on disk. **#04 stage-name validation:** set engine CYCLES live in 5.1.1, confirmed tiles real (`tile_size` 2048 / `use_auto_tile`), BVH real (`debug_bvh_type` DYNAMIC_BVH/STATIC_BVH), `use_denoising` real; engine restored, no render run, no view-transform touched. Inlined per LD12 (stripped `color="#222"`, collapsed root to 3 lines, structural comments kept, ASCII-divider comments pruned). Integrated bottom-up by line number (#05 @ ~441, #04 @ ~504, #02 @ ~244) so earlier anchors stayed stable; each dryRun->commit->get_file_info, anchor count=1. #02/#04 default-depth (figure col 20 / content 24 / figcaption 20); #05 deeper-indent inside the Light Path Fundamentals card (figure col 24 / content 28 / figcaption 24). **Byte trail: 155,681 (P1) -> +5,779 (#05) -> 161,460 -> +9,490 (#04) -> 170,950 -> +7,188 (#02) -> 178,138 B.** Post-verify on real UNC file (bridge UTF-8): em-dash **20** held, `&mdash;`/`&#8212;` 0, figure/figcaption/svg **4/4/4**, img 0, color222 0, **mermaid 1** held (D5), 11 sections no dups in order; id-prefixes ptr/rvp/rpf/lbv each x1; #02 (rvp) in what-is-path-tracing, #04 (rpf) + #05 (lbv) in how-cycles-works. **SVG set 4/11. Next: P3 — SVG batch B: #06 bounce_types_comparison, #07 gpu_cpu_architecture, #27 lighting_setup_diagram** (integrate in section order; #06/#07 in how-cycles-works, #27 in project Part 4).

- **2026-06-19 — P1 COMPLETE: SVG pilot #01 path_tracing_ray_diagram produced + integrated.** First figure in the file (no prior id/c{N} collisions). Hand-coded SVG per the style guide: orange camera firing a bold cyan `#4A9EFF` sampled path that bounces camera -> glossy sphere (bounce badge 1) -> diffuse box (badge 2) -> ceiling light (badge 3), with 3 faint low-opacity sample rays for the many-samples idea; orange `#FF6B00` numbered bounce badges, yellow `#F5C242` light source, green/blue surface swatches, legend, Variant B footer tip. House palette throughout (NOT the brief's `#00ffff`/`#2b2b2b`). `images/lesson_19_01_path_tracing_ray_diagram.svg` 4,809 B standalone (viewBox 0 0 800 560, prefix `ptr`, `color="#222"` kept on standalone for img-fallback), 0 em-dash, ASCII-only, role/aria/title/desc present. cairosvg render-check Claude-side on light + dark: both legible, currentColor cascades clean. Inlined per LD12 (stripped `color="#222"`, 2-line root, figure col 20 / content col 24 / figcaption col 20). dryRun->commit->verify; anchor count=1. **Byte trail: 149,234 (P0) -> +6,447 -> 155,681 B.** Post-verify on real UNC file: em-dash **20** held, `&mdash;`/`&#8212;` 0, figure/figcaption/svg **1/1/1**, img 0, color222 0, **mermaid 1** held (D5), 11 sections no dups in order, #01 (`ptr-title`) in `what-is-path-tracing`. **SVG set 1/11. Next: P2 — SVG batch A: #02 ray_vs_path_comparison, #04 render_process_flowchart, #05 light_bounce_visualization** (integrate in section order; #04 is supplemental to the live mermaid, validate its stage names are still accurate for 5.1.1 Cycles at lock).

- **2026-06-19 — DOC CREATED + ENTRY BASELINE RECORDED + PLAN LOCKED.** Read session.md banner (L18 FULLY DONE, QA queue empty/closed), lesson_18_integration_progress.md (working template), IMAGE_STYLE_GUIDE.md v6+, lesson_19_image_requirements.md v1.0. Verified entry-state P0 byte-exact on the real UNC file via bridge Python (Blender 5.1.1): **149,234 B** (148,701 utf-8 chars) / sha256 `62c7e7727d48672f…` / em-dash(U+2014) **20** / `&mdash;` 0 / `&#8212;` 0 / figure open-close 0/0 / figcaption 0/0 / img 0 / svg 0/0 / marker 0 / viewBox 0 / color222 0 / **div.mermaid 1 (D5 applies)** / **11 sections no dups** in exact order (what-is-path-tracing · how-cycles-works · basic-settings · samples-denoising · light-paths · caustics · gpu-cpu · optimization · troubleshooting · project · summary) / **0 existing `lesson_19_*` figure refs** (clean slate). Reconciled requirements: **35 new figures (11 SVG + 24 PNG)** per the doc's File Naming Convention (extension-authoritative), fully covering #1–#35 no overlap; the pre-existing live mermaid in `how-cycles-works` is PRESERVED (no slot number — the doc's #4 is a SUPPLEMENTAL flowchart that complements it, produced as a new SVG). Doc's AI-prompts / `#ff8c00` / `#2b2b2b` / "Blender 4.0+" / raster dims all overridden by house governance. **Flagged the Cycles UI validation requirement** (engine=CYCLES; validate Sampling / Denoising / Light Paths / Device / Preferences GPU / caustics / Principled BSDF socket names live in 5.1.1 at P5 before any capture — lower churn risk than L18's EEVEE-Next rename storm, but Principled BSDF socket labels did change in 4.x so still validate). Locked 10-phase plan (P1 SVG pilot → P4 SVG set close → P5 Cycles UI validation → P6 UI-chrome → P7/P8 renders → P10 figure-complete → QA last) in HTML section order. Figure registry + locked rules + §6 skeleton written. **Next: P1 — produce SVG pilot #01 path_tracing_ray_diagram + write §6 SVG specs.**

**Project root:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`
