# Lesson 16 &#183; Three-Point Lighting Setup &#8211; Integration Progress

**Module:** 04 &#183; Lighting &amp; Rendering
**HTML target:** `lesson_16_three_point_lighting_setup.html`
**Baseline (entry, verified this kickoff):** **135,522 B** &#183; PRISTINE pre-figure body (0 figure / 0 img / 0 svg / 0 figcaption / 0 overlay wrapper-div). 12 sections, 12 unique ids, no collisions, 2 live `mermaid` diagram blocks (intro three-light graph + positioning top-view graph), no `c{N}` ids present.
**Requirements doc:** `lesson_16_image_requirements.md` (v1.0, Nov 2025; 32 images enumerated with AI-generation prompts). **Reconciled to the pipeline this kickoff &#8211; see &sect;5 below.**
**Style guide:** `images/IMAGE_STYLE_GUIDE.md` v6 (locked decisions 1&#8211;21).
**Status:** **P9 DONE 2026-06-15** &#8211; `advanced-techniques` (#21/#22) FULLY CLOSED (single PRODUCTION + inline INTEGRATION chat, all-SVG). Both inline SVGs produced + integrated; HTML **196,228 &#8594; 217,022 B** (+20,794). Entry P0 re-verified byte-exact (196,228 B, em-dash 26, figure 20 / img 17 / `<svg` 20 / figcaption 20, 2 mermaid, wrapper-divs 17, `&#183;` 54, middot 3, `&#176;` 37, `&#8211;` 6, 12 ids no dups, c21&#8211;c22 absent). No Blender work (all-SVG, no scene/temps/debt). **22/25 produced / 22/25 integrated.** **Next: L16 P10** (`troubleshooting`, #23 `c23` troubleshooting_guide, 8-panel composite PNG, single chat).

> **em-dash baseline: 26** literal U+2014 (all pre-existing lesson-body prose; Python `count('\u2014')` authoritative, `grep -c` miscounts). Hold at 26, add none. middot (U+00B7) 0, `&mdash;` 0, `&#183;` 0, en-dash U+2013 0, minus U+2212 0 at entry.

> **Entry verified:** `copy_file_user_to_claude` + Python read on the real UNC path = 135,522 B; 0 `<figure>` / 0 `<img>` / 0 `<svg` / 0 `<figcaption>` / 0 `color="#222"`; 2 `class="mermaid"`; 12 `<section ... class="content-section">` ids = introduction, key-light, fill-light, rim-light, lighting-ratios, positioning, variations, different-subjects, advanced-techniques, troubleshooting, project, summary. No prior figure work exists; no `lesson_16_browser_qa_progress.md`.

---

## DECISIONS (resolved at this PLANNING lock unless flagged for a phase)

- **D1 &#8211; Three-light color coding (RESOLVED).** The requirements doc codes Key=purple `#667eea`, Fill=green `#6BCF7F`, Rim=pink `#FF6B9D`. These are **legitimate semantic CONTENT colors** (the three-light coding is this lesson's core visual language, the same status as L14 socket hues and the L04+ axis colors), so they are KEPT for the key/fill/rim glyphs in the diagrams (#1, #2, #21, #22, #25). Everything else &#8212; the 3px top accent strip, tip-callout rails, annotation leaders / markers / badges on PNG overlays, and any tie-break highlight &#8212; uses the **brand palette** per style-guide LD6/LD7 (`#FF6B00` accent, `#4ADE80` positive / `#E63946` warning overlay badges). The spec's `#ff8c00` annotation orange is NOT used; brand `#FF6B00` replaces it. The two mermaid blocks already in the body keep their existing spec-colored node fills (pre-existing content, not touched).
- **D2 &#8211; Figure count (RESOLVED): trim 32 &#8594; 25.** 7 cut, all from the requirements doc's own "Lower Priority / enrichment" tier or redundant with a kept diagram (see &sect;5 cut list). The cut set is recorded so nothing is lost; any can be reinstated later as a follow-up without disturbing the 1&#8211;25 numbering.
- **D3 &#8211; SVG/PNG split (RESOLVED).** 6 inline SVG (#1 #2 #12 #21 #22 #25), 19 PNG. Adopts the requirements doc's SVG-recommendation list intersected with the kept set (doc-recommended SVGs #1 #2 #15 #18 #19 #31 map to kept #1 #2 #21 #12 #22 #25; doc's #13 history-timeline SVG was cut).
- **D4 &#8211; Subject + render engine (RESOLVED, mirrors L15).** Suzanne (monkey head) as the standard subject for all portrait/lighting renders, gray Principled BSDF roughness ~0.4, camera ~5m back. Renders are Cycles for final-quality portrait/shadow figures (128&#8211;256 spp + denoise); color-dependent figures follow style-guide LD17 (F12 `render.render`, Standard view transform, RESTORE AgX at close) ONLY if a PRODUCTION review shows washing &#8212; not pre-forced. Three-point setup per the lesson's own Position Quick Reference (Key Area 3m 1000W 45&deg;/40&deg;; Fill Area 4m 330W opposite, shadows OFF; Rim Spot 40&deg; 1000W behind/50&deg;).
- **D5 &#8211; mermaid kept intact (RESOLVED).** Both existing `mermaid` blocks (intro three-light graph, positioning top-view graph) are KEPT unchanged. #1 (overhead diagram SVG) sits in `introduction` alongside the intro mermaid; #2 (side diagram SVG) is the vertical companion. No mermaid is replaced or edited (the L15 D5 precedent: add SVG alongside, never touch the live mermaid).
- **D6 &#8211; overlay-skip candidates (DECIDE per phase at plan-lock).** PNG renders that are self-evident or self-labeling may use the composite-overlay variant (centered per-panel badge, no highlight/leader) or skip the overlay entirely (L14 #40 / L13 #24 precedent). Likely minimal/skip: #14 rembrandt_triangle, #16 butterfly_pattern, #17 loop_pattern, #18 split_lighting_dramatic (single annotated renders &#8212; one labeled callout each, not a skip), #24 project_steps_montage (per-step labels needed). Confirm per figure at PRODUCTION; a skipped `c{N}` is simply unused (tracked via once-each PNG refs).

---

## 1. Figure registry (25 images, trimmed from 32)

Filename pattern `images/lesson_16_NN_slug.ext`. New # = 1&#8211;25 in HTML section order; (old #) = the figure's number in the v1.0 requirements doc. Type = hand-coded inline **SVG** vs Blender/composite **PNG**. Overlay column: `c{N}` = PNG annotation-overlay namespace; `{prefix}` = inline-SVG id-prefix; `&#8212;` = none planned. Section = HTML `id`. Status: all **not produced / not integrated** at kickoff.

| new# | (old#) | filename slug                       | section            | type | overlay / prefix | status |
|------|--------|-------------------------------------|--------------------|------|------------------|--------|
| 1  | (1)  | lesson_16_01_three_point_overhead_diagram.svg | introduction       | SVG | `tpo` | INTEGRATED (P1) |
| 2  | (2)  | lesson_16_02_three_point_side_diagram.svg     | introduction       | SVG | `tps` | INTEGRATED (P1) |
| 3  | (3)  | lesson_16_03_key_positions_comparison.png     | key-light          | PNG (4-panel composite) | c3 | INTEGRATED (P2) |
| 4  | (6)  | lesson_16_04_key_settings_panel.png           | key-light          | PNG (UI-chrome) | c4 | INTEGRATED (P2) |
| 5  | (20) | lesson_16_05_shadow_quality_comparison.png    | key-light          | PNG (3-panel composite) | c5 | INTEGRATED (P2) |
| 6  | (7)  | lesson_16_06_fill_mistakes_3panel.png         | fill-light         | PNG (3-panel composite) | c6 | INTEGRATED (P3) |
| 7  | (8)  | lesson_16_07_fill_settings_panel.png          | fill-light         | PNG (UI-chrome) | c7 | INTEGRATED (P3) |
| 8  | (9)  | lesson_16_08_rim_position_effect.png          | rim-light          | PNG (split setup/result) | c8 | INTEGRATED (P4) |
| 9  | (10) | lesson_16_09_rim_variations_4panel.png        | rim-light          | PNG (4-panel composite) | c9 | INTEGRATED (P4) |
| 10 | (5)  | lesson_16_10_ratio_comparison_5panel.png      | lighting-ratios    | PNG (5-panel composite) | c10 | INTEGRATED (P5) |
| 11 | (11) | lesson_16_11_highkey_lowkey_comparison.png    | lighting-ratios    | PNG (2-panel comparison) | c11 | INTEGRATED (P5) |
| 12 | (18) | lesson_16_12_ratio_calculator.svg             | lighting-ratios    | SVG | `rc` | INTEGRATED (P5) |
| 13 | (29) | lesson_16_13_height_angles_comparison.png     | positioning        | PNG (5-panel composite) | c13 | INTEGRATED (P6) |
| 14 | (4)  | lesson_16_14_rembrandt_triangle.png           | variations         | PNG (annotated render) | c14 | INTEGRATED (P7) |
| 15 | (12) | lesson_16_15_lighting_variations_showcase.png | variations         | PNG (6-panel composite) | c15 | INTEGRATED (P7) |
| 16 | (25) | lesson_16_16_butterfly_pattern.png            | variations         | PNG (annotated render) | c16 | INTEGRATED (P7) |
| 17 | (26) | lesson_16_17_loop_pattern.png                 | variations         | PNG (annotated render) | c17 | INTEGRATED (P7) |
| 18 | (27) | lesson_16_18_split_lighting_dramatic.png      | variations         | PNG (annotated render) | c18 | INTEGRATED (P7) |
| 19 | (16) | lesson_16_19_product_adaptation.png           | different-subjects | PNG (annotated render) | c19 | INTEGRATED (P8) |
| 20 | (22) | lesson_16_20_color_temperature_effects.png    | different-subjects | PNG (3-panel composite) | c20 | INTEGRATED (P8) |
| 21 | (15) | lesson_16_21_fourpoint_diagram.svg            | advanced-techniques | SVG | `fp` | INTEGRATED (P9) |
| 22 | (19) | lesson_16_22_light_type_comparison.svg        | advanced-techniques | SVG | `ltc` | INTEGRATED (P9) |
| 23 | (17) | lesson_16_23_troubleshooting_guide.png        | troubleshooting    | PNG (8-panel composite) | c23 | not produced / not integrated |
| 24 | (32) | lesson_16_24_project_steps_montage.png        | project            | PNG (6-panel composite) | c24 | not produced / not integrated |
| 25 | (31) | lesson_16_25_complete_reference_sheet.svg     | summary            | SVG | `crs` | not produced / not integrated |

**Type tally:** 6 SVG (#1 #2 #12 #21 #22 #25) &#183; 19 PNG. **Totals: 25 produced-target / 25 integrated-target.**

---

## 2. id-prefix registry (inline SVGs)

One short prefix per inline SVG, collision-checked against the live HTML (no existing inline-SVG ids; the 12 `<section>` ids are the only ids present, none collide). Header pattern per style-guide: `aria-labelledby="{prefix}-title {prefix}-desc"`, `<title id="{prefix}-title">`, `<desc id="{prefix}-desc">`, child ids `{prefix}-*`.

| prefix | figure | slug |
|--------|--------|------|
| `tpo` | #1  | three_point_overhead_diagram |
| `tps` | #2  | three_point_side_diagram |
| `rc`  | #12 | ratio_calculator |
| `fp`  | #21 | fourpoint_diagram |
| `ltc` | #22 | light_type_comparison |
| `crs` | #25 | complete_reference_sheet |

All 6 prefixes mutually distinct and distinct from the 12 section ids. (Per-lesson scoped; no cross-lesson clash concern.)

---

## 3. c{N} overlay namespace (PNG annotation overlays)

One `c{N}` namespace per PNG that carries callouts; ids namespaced `c{N}-*` (e.g. `c3-badge`, `c10-marker`) per style-guide LD10. 19 PNGs &#8594; up to 19 `c{N}` namespaces (`c3 c4 c5 c6 c7 c8 c9 c10 c11 c13 c14 c15 c16 c17 c18 c19 c20 c23 c24`). N matches the figure number for 1:1 traceability. Composite PNGs with baked-in panel dividers use the composite-overlay variant (centered per-panel badge, no highlight/leader). If an overlay is skipped (D6), its `c{N}` is unused.

---

## 4. Phase plan (HTML section order)

Sequenced by live-HTML section order (verified): introduction &#8594; key-light &#8594; fill-light &#8594; rim-light &#8594; lighting-ratios &#8594; positioning &#8594; variations &#8594; different-subjects &#8594; advanced-techniques &#8594; troubleshooting &#8594; project &#8594; summary.

**OUR conventions (not the requirements doc's 4-week grouping):**
- &lt;5 figures &#8594; single chat (PRODUCTION + INTEGRATION together).
- &#8805;5 figures &#8594; PRODUCTION sub-chat split on the capture-method boundary (hand-coded SVG / UI-chrome screenshot / viewport-render).
- &#8805;6 figures &#8594; INTEGRATION gets its OWN chat.
- plan-lock before any build; bottom-up insertion order at INTEGRATION; warn before context tightens.

| Phase | section | figures | count | type mix | plan |
|-------|---------|---------|-------|----------|------|
| **P1** | introduction | #1, #2 | 2 | 2 SVG | single chat (both inline SVG) |
| **P2** | key-light | #3, #4, #5 | 3 | 1 composite + 1 UI-chrome + 1 composite | single chat (mixed-method) |
| **P3** | fill-light | #6, #7 | 2 | 1 composite + 1 UI-chrome | single chat |
| **P4** | rim-light | #8, #9 | 2 | 1 split-composite + 1 composite | single chat |
| **P5** | lighting-ratios | #10, #11, #12 | 3 | 2 composite + 1 SVG | single chat (mixed-method) |
| **P6** | positioning | #13 | 1 | 1 composite | single chat |
| **P7** | variations | #14, #15, #16, #17, #18 | 5 | 1 showcase composite + 4 annotated renders | **&#8805;5: PRODUCTION sub-chat split** (renders share one scene; split on review-batch boundary if context tightens); INTEGRATION inline at &lt;6 unless context tightens |
| **P8** | different-subjects | #19, #20 | 2 | 1 product render + 1 composite | single chat |
| **P9** | advanced-techniques | #21, #22 | 2 | 2 SVG | single chat (both inline SVG) |
| **P10** | troubleshooting | #23 | 1 | 1 8-panel composite | single chat |
| **P11** | project | #24 | 1 | 1 6-panel composite | single chat |
| **P12** | summary | #25 | 1 | 1 SVG | single chat (inline SVG) |

12 phases, one per section. Only P7 trips the &#8805;5 split. No phase reaches &#8805;6, so no INTEGRATION-own-chat is pre-planned (break P7 INTEGRATION out only if context tightens mid-phase).

---

## 5. Requirements-doc reconciliation (32 &#8594; 25)

The v1.0 requirements doc (`lesson_16_image_requirements.md`, Nov 2025) predates the locked pipeline: it uses the spec palette, AI-generation prompts, and a 4-week "create everything" workflow. This progress doc is the source-of-truth that supersedes it for production. Reconciliation:

- **Palette:** per D1, three-light coding (purple/green/pink) kept as content color; all accent/overlay furniture re-routed to the brand palette. The doc's `#ff8c00` annotation orange &#8594; brand `#FF6B00`.
- **AI prompts:** the doc's per-image prompts remain useful as CONTENT references (what each figure should show), but production follows the BlenderMCP-render / hand-coded-SVG / UI-capture pipeline, NOT AI image generation.
- **Numbering:** renumbered 1&#8211;25 in HTML section order (the doc's numbering was placement-suggestion order). The (old #) column in &sect;1 preserves the cross-reference.
- **Dup ref dropped:** the doc references image #5 (ratio comparison) twice (in `lighting-ratios` and in a duplicate note); kept once, in `lighting-ratios` (new #10).

**CUT set (7 figures, recorded so nothing is lost &#8212; all from the doc's Lower-Priority/enrichment tier or redundant):**

| old# | slug | section | reason cut |
|------|------|---------|-----------|
| 13 | history_timeline | introduction | enrichment timeline, non-instructional (doc lower-pri) |
| 14 | setup_overhead_3d | positioning | redundant with #1 overhead SVG diagram |
| 21 | rim_mistakes_guide | rim-light | covered by the main troubleshooting guide (#23) + rim-card prose |
| 23 | camera_overlay_view | positioning | spatial understanding already carried by #1 / #2 diagrams |
| 24 | distance_softness | positioning | enrichment; overlaps #5 shadow-quality (light-size &#8594; softness) |
| 28 | multisubject_challenge | different-subjects | advanced beyond core three-point scope (doc lower-pri) |
| 30 | squint_test | lighting-ratios | specialized evaluation technique (doc lower-pri); covered in fill-light prose |

Any cut figure can be reinstated later as an appended #26+ without disturbing the 1&#8211;25 numbering.

---

## 6. Status log

**P9 DONE 2026-06-15.** `advanced-techniques` FULLY CLOSED &#8212; both figures #21 `fp` fourpoint_diagram + #22 `ltc` light_type_comparison produced + integrated (single PRODUCTION + inline INTEGRATION chat, all-SVG, same shape as P1). Entry state byte-exact: live P0 **196,228 B**, em-dash 26, figure 20 / img 17 / `<svg` 20 / figcaption 20, 2 mermaid, 12 section ids no dups, wrapper-divs 17, `&#183;` 54, middot 3, `&#176;` 37, `&#8211;` 6, c21&#8211;c22 absent, `<marker` 0, `color="#222"` 0 &#8212; matches the P8-close record exactly (safe resume). No Blender scene this phase (all-SVG; no renders, no temps, STOP rule N/A). **PRODUCTION:** both hand-coded per the v6 header pattern, root `color="#222"` KEPT in the standalone `images/` `.svg` for `<img>` fallback, `currentColor` text, three-light coding Key `#667eea` / Fill `#6BCF7F` / Rim `#FF6B9D` as semantic CONTENT color (D1), brand `#FF6B00` accent furniture, Variant-B footer tip; cairosvg light+dark render-checked each before write, both footer tips shortened pre-write to clear the x&le;760 bound (first drafts overflowed to x~796&#8211;799). #21 `fp` (viewBox **800&times;680** dense; top view + side view of four-point: standard Key/Fill/Rim plus a **background light placed behind the subject aimed at the backdrop**, blue `#4A9EFF` chosen brand-distinct from the 3 coding hues AND from brand orange furniture; side view shows the bg-light glow ellipse on the back wall; K/F/R/B glyphs both views + 4-row legend; 8,253 B). #22 `ltc` (viewBox **800&times;680** dense; 3-column Key/Fill/Rim reference, each column = colored header band + light-type glyph [Area rect / larger Area rect / Spot cone] + recommended type + TYPICAL SETTINGS + PROS [green `#4ADE80`] + CONS [red `#E63946`]; column dividers; 7,370 B). Standalone `.svg` written to `images/` via `Filesystem:write_file`, `get_file_info`-confirmed: #21 8,253 B / #22 7,370 B. **INTEGRATION (inline, <6 figs):** P0 re-verified byte-exact first; both anchors confirmed count=1 (globally unique) on a fresh copy. **#21 anchor:** inside the Four-Point Lighting card, after the "When to use four-point" `</ul>` before the card's `</div>` (requirements-doc intent "within Four-Point Lighting card"; **deeper-indent variant** &#8212; card opens at col20, so figure col24 / svg col28 / children col32 / `<g>` col36 / figcaption col24). **#22 anchor:** end of `advanced-techniques`, after the Five-Point card `</div>` before `</section>` (requirements-doc intent "#19 Light Type Comparison as supporting material"; **default-depth** &#8212; figure col20 / svg col24 / children col28 / `<g>` col32 / figcaption col20). Document order top&#8594;bottom = #21 (in card, upper) &#8594; #22 (end of section, lower); bottom-up insert #22 &#8594; #21, each `edit_file` dryRun&#8594;commit&#8594;get_file_info. Inlined per LD12 (root `color="#222"` stripped, svg root tag collapsed one-line, re-indented per depth, pruned decorative `====` comments, kept structural comments, preserved role/aria/title/desc + `fp-*`/`ltc-*` ids). Byte trail: 196,228 (P0) &#8594; #22 +9,516 &#8594; 205,744 &#8594; #21 +11,278 &#8594; **217,022 B**. POST-COMMIT INTEGRITY CLEAN (live-swept on 217,022 B): em-dash **26 held**, `&mdash;` 0, `color="#222"` 0 (stripped on inlining), figure 20&#8594;**22**, figcaption 20&#8594;**22** (balanced), img **17 UNCHANGED** (both inline, add 0), literal `<svg` 20&#8594;**22** / `</svg>` **22**, `<marker` 0, `class="mermaid"` **2** (kept), wrapper relative divs **17 UNCHANGED** (inline adds none), `&#183;` 54&#8594;**60** (+6: #21 subtitle 1 + 4 legend separators + #22 subtitle 1), literal middot U+00B7 **3** (unchanged, the 3 c6 pills), `&#176;` 37&#8594;**43** (+6: #21 legend 1 + #22 "45&#176; side, 40&#176; above" 2 + "30&#8211;50&#176;" 1 + "45&#8211;60&#176;" 1 + figcaption "30&#8211;50&#176;" 1), `&#8211;` 6&#8594;**19** (+13: range en-dashes in both SVG bodies + both figcaptions), en-dash U+2013 0, minus U+2212 0, 12 section ids no dups, **c21-/c22- ids 0** (inline SVG, no `c{N}` namespace &#8212; expected), `fp-` 4 (2 ids + 2 aria refs), `ltc-` 4 (2 ids + 2 aria refs). No BlenderMCP work this chat (all-SVG; no scene touched, no temps, no `_overlay_VERIFIED.svg` artifacts, no debt). Deliverables retained: #21 SVG 8,253 B + #22 SVG 7,370 B. **22/25 produced / 22/25 integrated.** **Next: L16 P10 PRODUCTION** (`troubleshooting`, #23 `c23` troubleshooting_guide, 8-panel composite PNG, single chat).

**P8 DONE 2026-06-15.** `different-subjects` FULLY CLOSED &#8212; both figures #19 `c19` product_adaptation + #20 `c20` color_temperature_effects produced + integrated (single PRODUCTION + inline INTEGRATION chat, <6 figs). Entry state byte-exact: live P0 **191,864 B**, em-dash 26, figure 18 / img 15 / `<svg` 18 / figcaption 18, 2 mermaid, 12 section ids no dups, wrapper-divs 15, `&#183;` 48, middot 3, `&#176;` 37, `&#8211;` 6, c19&#8211;c20 absent &#8212; matches the P7-close record exactly (safe resume). Blender base verified clean 0/0/0/0/0 at entry (EEVEE / AgX / look None / 0.05 world; P7's INTEGRATION-only close left it clean, no leftover rig &#8212; STOP rule not triggered). **SUBJECT (Ray-confirmed at plan-lock):** a cylindrical bottle/canister product primitive (body + shoulder taper + neck + rounded cap, joined, gray dielectric Principled BSDF rough 0.30 metallic 0), on a built seamless dark cyclorama sweep (floor &#8594; filleted quarter-circle &#8594; back wall, swept on X). No bust needed (P7's bust was only for named face-patterns; product + color-temp don't require a face). Three-point rig per the lesson Position Quick Reference: Key Area 45&deg;R/40&deg;up ~3m, Fill Area opposite (left) lower shadows-OFF, Rim Spot behind/above brighter (product rim stronger). Camera 70mm ~5.6m back. **D9 split by figure:** #19 = **AgX held** (neutral grey product, probe R&#8776;G&#8776;B, no wash; PRODUCTION-discovery: first render at key 600W sat the body in AgX's shoulder p50 209 / washed modeling, fix = key 600&#8594;230W + fill 180&#8594;70W + rim 900&#8594;1500W so lit side lands mid p50 177 / p10 139 with real falloff and the rim edge reads); #20 = **LD17 Standard INVOKED** (the warm/cool key tints are legitimate content color &#8212; D9 probe confirmed AgX crushes them: warm lit-side R&#8722;B only **+1.45** under AgX vs **+15.9 / &#8722;2.8 / &#8722;21.1** (warm/neutral/cool) under Standard once the key was dropped to 130W + fill 45W + rim 700W so the body sits mid ~170 where the tint reads; P5 #13-sun precedent). RESTORED AgX at close. #19 lesson_16_19 (1400&times;1600, Cycles 160spp+denoise, single annotated render, no baked labels; 1,412,984 B). #20 lesson_16_20 (4204&times;1600, 3 panels Warm/Neutral/Cool key, same product+pose+setup only key color changes, Pillow-composited with `#FF6B00` 2px dividers verified at x=1400/2802, panel centers x=700/2102/3504, no baked labels; 4,112,974 B; render tints slightly deeper than the spec hex for thumbnail legibility, pill labels carry the canonical #FFF8E7/#FFFFFF/#F0F5FF reference values). Both PNGs byte-verified + Pillow decode-verified + visually reviewed. **D6 overlays:** #19 `c19` = 3 leader callouts (Key &#183; reveals form &#8594; lit shoulder highlight x800,455; Rim &#183; edge separation &#8594; right edge x944,640; Fill &#183; shadow detail &#8594; shaded left body x520,900), neutral `#1a1a1a` 0.82 pills + `#FF6B00` leaders/markers, all pills asserted in-bounds (Rim pill clipping fixed: cx 1200&#8594;1180 w420 so x+w&le;1400); #20 `c20` = composite-overlay 3 centered pills near bottom (Warm &#183; #FFF8E7 / Neutral &#183; #FFFFFF / Cool &#183; #F0F5FF, cy=1480), no leaders. Both overlays cairosvg-composite pixel-verified 1:1 over the real PNGs before commit (LD19). Anchors both count=1 (verified globally unique on a fresh copy; all 3 different-subjects h3s &#8212; Product Lighting / Character Lighting / Environmental Integration &#8212; are distinct): #19 before `<h3>Product Lighting</h3>` (after the section intro `<p>`); **#20 before `<h3>Environmental Integration</h3>`** (Ray-confirmed doc intent &#8212; that section's prose is where light color/temperature lives: warm interior practicals, cool-blue skylight, beige bounce tint; Character Lighting is coverage/personality ratios, not color). Default-depth (figure col20, wrapper-div + img margin:0 + inlined overlay-svg viewBox=PNG px, figcaption OUTSIDE). Document order top&#8594;bottom = #19 &#8594; #20; bottom-up insert #20 &#8594; #19, each `edit_file` dryRun&#8594;commit&#8594;get_file_info. Byte trail: 191,864 (P0) &#8594; #20 +1,950 &#8594; 193,814 &#8594; #19 +2,414 &#8594; **196,228 B**. POST-COMMIT INTEGRITY CLEAN (live-swept on 196,228 B): em-dash **26 held**, `&mdash;` 0, `color="#222"` 0, figure 18&#8594;**20**, figcaption 18&#8594;**20** (balanced), img 15&#8594;**17**, literal `<svg` 18&#8594;**20** / `</svg>` **20**, `<marker` 0, `class="mermaid"` **2** (kept), wrapper relative divs 15&#8594;**17**, `&#183;` 48&#8594;**54** (+6: c19 3 pills + c20 3 pills; figcaptions use periods/parens, no middot), literal middot U+00B7 **3** (unchanged, the 3 c6 pills), `&#176;` **37** (unchanged), `&#8211;` **6** (unchanged), en-dash U+2013 0, minus U+2212 0, 12 section ids no dups, **c19-/c20- ids 0** (leader + composite pills carry no `c{N}-*` ids &#8212; expected, tracked via PNG once-each), both PNGs referenced once each. BlenderMCP: scene torn down to clean base 0/0/0/0/0 (confirmed), **AgX RESTORED** (was Standard for #20) + EEVEE + look None + 0.05 world, Windows `C:\Users\Public\L16P8` deleted (isdir False), 2 WSL `_tmp_l16p8_*` temps deleted (glob-empty verified), no `_overlay_VERIFIED.svg` artifacts (cairosvg verify ran Claude-side only), no BlenderMCP debt. Deliverables retained: #19 PNG 1,412,984 B + #20 PNG 4,112,974 B. **20/25 produced / 20/25 integrated.** **Next: L16 P9 PRODUCTION** (`advanced-techniques`, #21 `fp` fourpoint_diagram + #22 `ltc` light_type_comparison, both inline SVG, single chat with inline INTEGRATION).

**P7 DONE 2026-06-15.** `variations` FULLY CLOSED &#8212; all 5 figures #14 `c14` rembrandt_triangle + #15 `c15` lighting_variations_showcase + #16 `c16` butterfly_pattern + #17 `c17` loop_pattern + #18 `c18` split_lighting_dramatic integrated (PRODUCTION landed prior chat; this was an INTEGRATION-only chat). Entry state byte-exact: live P0 **180,844 B**, em-dash 26, figure 13 / img 10 / `<svg` 13 / figcaption 13, 2 mermaid, 12 section ids no dups, wrapper-divs 10, `&#183;` 39, middot 3, `&#176;` 26, c14&#8211;c18 absent &#8212; matches the P6-close record exactly (safe resume). All 5 finals byte-verified + Pillow decode-verified on WSL `images/`: #14 449,076 B 900&times;1100, #15 1,181,413 B 2404&times;1802, #16 450,089 B 900&times;1100, #17 460,608 B 900&times;1100, #18 377,508 B **900&times;1100** (the resume note said 900&times;900; the file on disk is authoritative at 900&times;1100, overlay viewBox set to match). **SUBJECT (resolved at PRODUCTION):** a PROCEDURAL constructed bust (all 4 asset connectors were dead ends &#8212; PolyHaven paid addon / Sketchfab key dead / Hyper3D trial out of funds / Hunyuan3D needs a local server); Ray approved the bust, all named patterns (Rembrandt triangle / butterfly under-nose / loop / split) read cleanly. **D9 AgX held** (all 5 luminance-probed neutral, no wash). **D6 overlays (plan-locked, carried forward):** #14/#16/#17 single-pattern renders each got a top-center pattern-name pill + a small bottom-left VECTOR key-position inset (head ring + nose-front marker + CAM glyph + orange key dot/ray + angle label) drawn in the c{N} overlay (no baked mini-diagram); #18 = NO inset (minimal noir, single split-line dashed callout down center x=450 + a top pill + a bottom "Shadow / light divide" pill); #15 = composite-overlay 6 centered pills, no leaders/insets, grid TL&#8594;TR top / BL&#8594;BR bottom: Standard 3-point (x400 y30) / Rembrandt (x1202 y30) / Loop (x2003 y30) / Butterfly (x400 y930) / Split (x1202 y930) / Low-Key (x2003 y930); panel cells 800&times;900 + 2px orange gaps (verified vertical dividers x=800/1602, horizontal y=900). All 5 overlays cairosvg-composite pixel-verified 1:1 over the real PNGs before commit (LD19); the #14/#16/#17 insets were rebuilt once after first verify (key dot/label collided with the "KEY POSITION" title) then re-verified clean. Anchors all count=1 (verified globally unique on a fresh copy), each figure inserted immediately before its section heading at default-depth (figure col20, wrapper-div + img margin:0 + inlined overlay-svg viewBox=PNG px, figcaption OUTSIDE): #15 before `<h3>High-Key Portrait Lighting</h3>`; #18 before `<h3>Rembrandt Lighting</h3>`; #14 before `<h3>Butterfly Lighting (Paramount Lighting)</h3>`; #16 before `<h3>Loop Lighting</h3>`; #17 before the blue &ldquo;Choosing Your Variation&rdquo; card. Document order top&#8594;bottom = #15 &#8594; #18 &#8594; #14 &#8594; #16 &#8594; #17; bottom-up insert #17 &#8594; #16 &#8594; #14 &#8594; #18 &#8594; #15, each `edit_file` dryRun&#8594;commit&#8594;get_file_info. Byte trail: 180,844 (P0) &#8594; #17 +2,168 &#8594; 183,012 &#8594; #16 +2,154 &#8594; 185,166 &#8594; #14 +2,204 &#8594; 187,370 &#8594; #18 +1,730 &#8594; 189,100 &#8594; #15 +2,764 &#8594; **191,864 B**. POST-COMMIT INTEGRITY CLEAN (live-swept on 191,864 B): em-dash **26 held**, `&mdash;` 0, `color="#222"` 0, figure 13&#8594;**18**, figcaption 13&#8594;**18** (balanced), img 10&#8594;**15**, literal `<svg` 13&#8594;**18** / `</svg>` **18**, `<marker` 0, `class="mermaid"` **2** (kept), wrapper relative divs 10&#8594;**15**, `&#183;` 39&#8594;**48** (+9: 4 overlay pills + 5 figcaptions), literal middot U+00B7 **3** (unchanged, the 3 c6 pills), `&#176;` 26&#8594;**37** (+11: 7 overlay + 4 figcaption degrees), `&#8211;` **6** (+1: c17 "30&#8211;45&#176;" + #17 figcaption "30&#8211;45&#176;"), en-dash U+2013 0, minus U+2212 0, 12 section ids no dups, **c14&#8211;c18 ids 0** (composite/inset pills carry no `c{N}-*` ids &#8212; expected, tracked via PNG once-each), all 5 PNGs referenced once each. The pre-existing Rembrandt-card em-dash ("see it&mdash;it's very specific!") preserved verbatim through the #14 anchor. No BlenderMCP work this chat (INTEGRATION-only; PRODUCTION landed prior chat, no scene touched, no temps, no `_overlay_VERIFIED.svg` artifacts written, no debt). Deliverables retained: #14 449,076 B + #15 1,181,413 B + #16 450,089 B + #17 460,608 B + #18 377,508 B. **18/25 produced / 18/25 integrated.** **Next: L16 P8 PRODUCTION** (`different-subjects`, #19 product_adaptation + #20 color_temperature_effects).

**P7 PRODUCTION IN PROGRESS &#8212; SPLIT 2026-06-15.** `variations` (#14/#15/#16/#17/#18, 5 figures) PRODUCTION started and then SPLIT to a fresh PRODUCTION chat per the &#8805;5 plan when subject-geometry trials began filling context. **0/5 produced; nothing written to `images/`; HTML UNTOUCHED at 180,844 B; 13/25 produced / 13/25 integrated unchanged.** Entry P0 re-verified byte-exact on the real UNC path (180,844 B, em-dash 26, figure 13 / img 10 / `<svg` 13 / figcaption 13, 2 mermaid, wrapper-divs 10, `&#183;` 39, middot 3, `&#176;` 26, 12 ids no dups, c14&#8211;c18 absent) and Blender base verified clean 0/0/0/0/0 (EEVEE/AgX/look None/0.05 world) at entry &#8212; matches the P6-close record exactly (safe resume). **PLAN-LOCKED this chat (carry forward):** D9 = **hold AgX** (first Rembrandt test frame luminance-probed neutral R&#8776;G&#8776;B = 99.6/99.7/99.6, lit p90 ~193&#8211;206 not clipped, shadow p10 ~32; no wash, LD17 NOT invoked, P5/P6 precedent); D6 = the #14/#16/#17 **setup-diagram insets are VECTOR drawn in the c{N} INTEGRATION overlay** (corner of the overlay, no baked mini-diagram in the PNG), **#18 gets NO inset** (minimal noir, single split-line callout); NO baked labels on any PNG (overlays at INTEGRATION). #15 = composite-overlay 6 centered pills (Standard 3-point / Rembrandt / Loop / Butterfly / Split / Low-Key), no leaders/insets. **SUBJECT DECISION (the split trigger, OPEN, blocks resume):** the named portrait patterns (#14 Rembrandt triangle, #16 butterfly under-nose, #17 loop) are human nose+cheekbone shadow phenomena. Suzanne (D4 standard subject) has no nose bridge / cheekbones / eye-sockets, so a key just lights the front fairly evenly &#8212; the triangle never forms. A primitive **sphere+nose was tried and also failed** (a smooth sphere gives a terminator gradient, not a triangle; the nose reads as a lone bump on a featureless egg). A gaussian-displacement-sculpted head (recessed eye-sockets + brow + nose bridge + cheekbones) got CLOSE (a directional portrait with a real nose-shadow began to form) but read lumpy/asymmetric &#8212; not ship-quality. **Ray's decision: use a real stylized human bust via the asset connectors.** BLOCKER: `blendermcp_use_polyhaven` / `blendermcp_use_sketchfab` / `blendermcp_use_hyper3d` / `blendermcp_use_hunyuan3d` are ALL toggled **OFF** in the BlenderMCP sidebar, and `blendermcp_sketchfab_api_key` is **empty** &#8212; the bridge rejected `search_sketchfab_models` with "Unknown command type" while disabled. These are Blender-UI settings Claude cannot flip; **Ray must enable PolyHaven (no key needed) and/or Sketchfab (needs a free API key) in the BlenderMCP sidebar before the next chat can download.** **NEXT CHAT (fresh PRODUCTION):** confirm the connector toggle(s) are ON, download a clean stylized human head/bust (target ~1.7&#8211;2.0 m largest dim, gray it to BSDF rough 0.4 to match the lesson subject), then produce all 5 P7 figures &#8212; the 4 single-pattern renders (#14 Rembrandt key 45&deg;R/45&deg;up, #16 butterfly high-frontal ~70&deg;, #17 loop key 30&#8211;45&deg;, #18 split 90&deg; side no fill) + the 6-panel #15 showcase (heaviest, 6 setups, render last). Recipe carried from this chat: Cycles 160spp + denoise, AgX, dark world (~0.02&#8211;0.05), key Area ~200&#8211;260W size ~1.0, weak fill ~16&#8211;22W shadow OFF, 85&#8211;90mm cam ~5&#8211;6m back, render to Windows tempdir then Pillow-relay to WSL, luminance-probe + visual-review each before locking. INTEGRATION inline at &lt;6 unless context tightens. **No figures, no `images/` writes, no HTML edit, no `_overlay_VERIFIED.svg`, no debt this chat.** Scene torn down to clean base 0/0/0/0/0 (EEVEE/AgX/look None/0.05 world restored), Windows `C:\Users\Public\L16P7` + WSL `_tmp_l16p7_test_rembrandt.png` relay temp deleted (isdir/exists False, no strays). **Next: L16 P7 PRODUCTION (fresh chat).**

**P6 DONE 2026-06-15.** `positioning` FULLY CLOSED &#8212; #13 `c13` height_angles_comparison produced + integrated (single PRODUCTION + INTEGRATION chat, Cycles 5-panel composite). Entry state byte-exact: live P0 **177,974 B**, em-dash 26, figure 12 / img 9 / `<svg` 12 / figcaption 12, 2 mermaid, 12 section ids no dups, wrapper-divs 9, `&#183;` 34 / middot 3, c13- 0 &#8212; matches the P5-close record exactly (safe resume). Blender base verified clean 0/0/0/0/0 at entry (P5 left it clean), EEVEE + AgX + 0.05 world. **D4 render recipe (Cycles, AgX) held; D9 LD17 Standard NOT invoked** &#8212; luminance probe neutral grey (R=G=B on the test frame), no wash; tone/shadow-progression figure, not color-marker. **PRODUCTION discovery (logged, mirrors P5):** first test render at key 600W sat the lit side in AgX's shoulder (45&deg; panel p50 202 / p90 229), compressing the 15&#8594;90 shadow steps. Fix = drop key 600W&#8594;230W + fill 120W&#8594;55W so the lit side lands mid-range (p90 ~204&#8211;208) and the under-brow/nose/chin shadow deepens distinctly across the sweep (median 188&#8594;168&#8594;102 from 15&#8594;45&#8594;90; shadow side held p10 36&#8211;54, detail not crushed). On grey-subject AgX height sweeps, render for headroom (mid-range lit side), not to a bright key. #13 lesson_16_13 (5008&times;1000, 1&times;5 Cycles strip, Suzanne gray BSDF rough 0.4, single key Area size 2.0 @ 230W constant azimuth ~20&deg; right of front, only ELEVATION varies 15/30/45/60/90&deg; at fixed slant radius 3.2, modest fill 55W, `#FF6B00` 2px dividers at x 1000/2002/3004/4006, no baked labels; `c13` 5 composite pills 15&deg;&#183;Flat / 30&deg;&#183;Flattering / 45&deg;&#183;Classic / 60&deg;&#183;Dramatic / 90&deg;&#183;Theatrical, panel centers x=500/1502/2504/3506/4508 y=905). Overlay cairosvg-pixel-verified 1:1 over the real PNG before commit (LD19): pills center under each panel, no subject collision. Anchor count=1 (verified globally unique on a fresh copy): after the &ldquo;Height and Angle Variations&rdquo; card &grave;</ul></div>&grave; (tail &ldquo;Reverses natural lighting (we expect light from above)&rdquo;, count=1) before `<h3>Adapting to Subject Position</h3>`. Default-depth (figure col20, wrapper-div + img margin:0 + inlined overlay-svg viewBox=PNG px, figcaption OUTSIDE). Single `edit_file` dryRun&#8594;commit&#8594;get_file_info. Byte trail: 177,974 (P0) &#8594; #13 +2,870 &#8594; **180,844 B**. POST-COMMIT INTEGRITY CLEAN (live-swept on 180,844 B): em-dash **26 held**, `&mdash;` 0, `color="#222"` 0, figure 12&#8594;**13**, figcaption 12&#8594;**13** (balanced), img 9&#8594;**10**, literal `<svg` 12&#8594;**13** / `</svg>` **13**, `<marker` 0, `class="mermaid"` **2** (kept), wrapper relative divs 9&#8594;**10**, `&#183;` 34&#8594;**39** (+5 c13 pills), literal middot U+00B7 **3** (unchanged, the 3 c6 pills), en-dash U+2013 0, minus U+2212 0, `&#176;` 18&#8594;**26** (+5 pills +3 figcaption), 12 section ids no dups, **c13- ids 0** (composite pills carry no `c{N}-*` ids &#8212; expected, tracked via PNG once-each), #13 PNG referenced once. BlenderMCP: scene torn down to clean base 0/0/0/0/0 (confirmed), EEVEE + AgX + look None + 0.05 world restored, Windows `C:\Users\Public\L16P6` + 9 temp PNGs (5 panels + test45 + 3 probes) deleted (isdir False), no WSL `_tmp_l16p6_*` temps this chat (Pillow composited direct to final `images/` path), no `_overlay_VERIFIED.svg` artifacts (cairosvg verify ran Claude-side only), no BlenderMCP debt. Deliverable retained: #13 PNG 1,971,871 B. **Next: L16 P7** (`variations`, #14/#15/#16/#17/#18).

**P5 DONE 2026-06-15.** `lighting-ratios` FULLY CLOSED &#8212; all 3 figures #10 `c10` ratio_comparison_5panel + #11 `c11` highkey_lowkey_comparison + #12 `rc` ratio_calculator produced + integrated (single mixed-method chat, PRODUCTION + INTEGRATION). Entry state byte-exact: live P0 **167,108 B**, em-dash 26, figure 9 / img 7 / `<svg` 9 / figcaption 9, 2 mermaid, 12 section ids no dups, c10-/c11-/rc- 0 &#8212; matches the P4-close record exactly (safe resume). Blender base verified clean 0/0/0/0/0 at entry (P4 left it clean). **D4 render recipe (Cycles, AgX) held; D9 LD17 Standard NOT invoked** &#8212; luminance probe showed perfectly neutral grey (R&#8776;G&#8776;B on all 7 frames), no wash; tone/contrast figures, not color-marker. **PRODUCTION discovery (logged):** first ratio renders were overexposed &#8212; whole subject sat at ~200&#8211;218 in AgX's shoulder where contrast compresses, so the 1:1&#8594;8:1 sweep barely moved (lit/shadow gap collapsed). Fix = drop key from 1000W to ~200W (area sizes key 2.0 / fill 3.5) so the lit side lands mid-range (~150) and the shadow side falls off progressively (lit/shadow gap widens 1:1 &#8722;9 &#8594; 8:1 +20). On grey-subject AgX ratio sweeps, render for headroom (mid-range lit side), not to a bright key. Ray reviewed the 7 raw panels and approved compositing as-is (labels carry the mid-ratio disambiguation). #10 lesson_16_10 (5008&times;1000, 1&times;5 Cycles strip, Suzanne gray BSDF rough 0.4, key fixed ~200W, fill 1.0/0.5/0.333/0.25/0.125&times; key = 1:1/2:1/3:1/4:1/8:1, `#FF6B00` 2px dividers, no baked labels; `c10` 5 composite pills 1:1&#183;Flat / 2:1&#183;Soft / 3:1&#183;Standard / 4:1&#183;Dramatic / 8:1&#183;Noir, panel centers x=500/1502/2504/3506/4508 y=900). #11 lesson_16_11 (2002&times;1000, 1&times;2 Cycles, LEFT high-key 2:1 lifted ambient world bright / RIGHT low-key 8:1 dark world strong rim; `#FF6B00` 2px divider; `c11` 2 pills High-Key&#183;2:1 / Low-Key&#183;8:1, centers x=500/1502 y=900). Both overlays cairosvg-pixel-verified 1:1 over the real PNGs before commit (LD19). #12 lesson_16_12 ratio_calculator INLINE SVG (`rc`, viewBox 800&times;560; 4 rows 2:1/3:1/4:1/8:1, full orange KEY&#183;1000W bar vs proportional blue FILL bar + right-column %, brand palette, Variant-B footer tip; light+dark cairosvg render-checked, dark-mode currentColor cascade confirmed). Anchors all count=1 (verified globally unique on a fresh copy): #10 after the Key-to-Fill Understanding card `</div>` before `<h3>Common Lighting Ratios</h3>`; #12 after the Common-Ratios card `</div>` before `<h3>Choosing the Right Ratio</h3>`; #11 after the High-Key vs Low-Key card `</div>` before the green &ldquo;Working with Ratios in Blender&rdquo; card. PNG figures default-depth (figure col20, wrapper-div + img margin:0 + inlined overlay-svg viewBox=PNG px, figcaption OUTSIDE); #12 inlined per LD12 (root `color="#222"` stripped, figure col20 / svg col24 / children col28, `rc-*` ids kept). Bottom-up insert #11 &#8594; #12 &#8594; #10, each `edit_file` dryRun&#8594;commit&#8594;get_file_info. Byte trail: 167,108 (P0) &#8594; #11 +2,102 &#8594; 169,210 &#8594; #12 +5,410 &#8594; 174,620 &#8594; #10 +3,354 &#8594; **177,974 B**. POST-COMMIT INTEGRITY CLEAN (live-swept on 177,974 B): em-dash **26 held**, `&mdash;` 0, `color="#222"` 0, figure 9&#8594;**12**, figcaption 9&#8594;**12** (balanced), img 7&#8594;**9** (#10/#11 PNG; #12 inline adds 0), literal `<svg` 9&#8594;**12** / `</svg>` **12** (#12 inline + c10/c11 overlays), `<marker` 0, `class="mermaid"` **2** (kept), wrapper relative divs 7&#8594;**9**, `&#183;` 19&#8594;**34** (+15: #10 5 pills, #11 2 pills, #12 8 = KEY/FILL bar labels + tip), literal middot U+00B7 **3** (unchanged, the 3 c6 pills), en-dash U+2013 0, minus U+2212 0, 12 section ids no dups, **c10-/c11- ids 0** (composite pills carry no `c{N}-*` ids &#8212; expected, tracked via PNG once-each), `rc-` 4 (2 ids + 2 aria refs), all 9 PNGs referenced once each. BlenderMCP: scene torn down to clean base 0/0/0/0/0 (confirmed), EEVEE + AgX restored + 0.05 world, Windows `C:\Users\Public\L16P5` deleted (isdir False), 8 WSL `_tmp_l16p5_*` review temps + review sheet deleted (glob-empty verified), no `_overlay_VERIFIED.svg` artifacts (cairosvg verify ran Claude-side only), no BlenderMCP debt. Deliverables retained: #10 PNG 2,228,271 B + #11 PNG 1,223,737 B + #12 SVG 4,035 B. **Next: L16 P6 PRODUCTION** (`positioning`, #13 height_angles_comparison).

**P4 DONE 2026-06-15.** `rim-light` FULLY CLOSED &#8212; both figures #8 `c8` rim_position_effect + #9 `c9` rim_variations_4panel produced + integrated (single PRODUCTION + INTEGRATION chat, both Cycles PNG). Entry state byte-exact: live P0 **162,524 B**, em-dash 26, figure 7 / img 5 / `<svg` 7 / figcaption 7, 2 mermaid, 12 section ids no dups, c8-/c9- 0, neither PNG referenced &#8212; matches the P3-close record exactly (safe resume). **ENTRY-STATE FLAG: scene was NOT a clean base** &#8212; a leftover `L16_P3_*` rig (Suzanne / Ground / Cam / Key / Rim / Fill + 2 materials) was still loaded from the P3 PRODUCTION chat (P3's INTEGRATION-only close logged "no scene touched" and never tore down the prior PRODUCTION rig). Per the locked STOP rule it was flagged, then cleared to a verified clean empty base 0/0/0/0/0 before building P4 fresh. **D9 LD17: #8 rendered under AgX** (neutral grey, no color, no wash &#8212; Standard not needed); **#9 rendered under Standard** (the colored blue rim panel needs Standard for full saturation; the 3 white panels rendered Standard too for 4-panel set parity, white being hue-neutral). #8 lesson_16_08 (2002&times;1000 split composite, `#FF6B00` 2px center divider at x 1000&#8211;1001: LEFT = side-view setup with an emissive orange rim-light marker + translucent beam cone grazing the back of the head; RIGHT = camera-view render with a clear bright top-edge rim glow separating the subject from the dark world). #9 lesson_16_09 (2002&times;2002 2&times;2 composite, `#FF6B00` 2px dividers: TL Subtle 50% faint edge / TR Standard 100% clear edge / BL Dramatic 200% bright wraparound / BR Colored blue rim; same Suzanne pose + front key/fill, only rim power/color changes; power spread widened to read at thumbnail scale, framing pulled to full-head to drop the floor-pool variable). Blue locked as the colored-rim hue (spec default + first in the lesson body's colored-rim list + cleanest read against grey/dark). **D6 resolved per phase:** both are composite PNGs with baked dividers &#8594; composite-overlay variant (centered `#1a1a1a` 0.82 pill per panel, no leaders): c8 = Setup / Result (panel centers x=500/1502, y=900); c9 = Subtle &#183; 50% / Standard &#183; 100% / Dramatic &#183; 200% / Colored &#183; Blue (panel centers x=500/1502 &times; y=905/1907). Both overlays cairosvg-pixel-verified 1:1 over the real PNGs before commit (LD19): pills center under each panel, no subject collision. Anchors both count=1 (verified globally unique on a fresh copy): #8 after the “What is the Rim Light?” card `</div>` before `<h3>Rim Light Positioning</h3>`; #9 after the “Rim Light Settings” card `</div>` before `<h3>Rim Light Effects and Variations</h3>`. All default-depth (figure col20 / contents col24, wrapper-div + img margin:0 + inlined overlay-SVG viewBox = PNG pixels, figcaption OUTSIDE). Bottom-up insert #9 &#8594; #8, each `edit_file` dryRun&#8594;commit&#8594;get_file_info. Byte trail: 162,524 (P0) &#8594; #9 +2,666 &#8594; 165,190 &#8594; #8 +1,918 &#8594; **167,108 B**. POST-COMMIT INTEGRITY CLEAN (live-swept on 167,108 B): em-dash **26 held**, `&mdash;` 0, `color="#222"` 0, figure 7&#8594;**9**, figcaption 7&#8594;**9** (balanced), img 5&#8594;**7**, literal `<svg` 7&#8594;**9**, `</svg>` 9, `<marker` 0, `class="mermaid"` **2** (kept), wrapper relative divs 5&#8594;**7**, `&#183;` 15&#8594;**19** (+4: c9's four pills; c8 pills carry none), literal middot U+00B7 **3** (unchanged, the 3 c6 pills), en-dash U+2013 0, minus U+2212 0, 12 section ids no dups, **c8-/c9- ids 0** (composite pills carry no `c{N}-*` ids &#8212; expected, tracked via PNG once-each), both PNGs referenced once each. BlenderMCP: scene torn down to clean base 0/0/0/0/0 (confirmed), EEVEE + AgX restored + neutral world, Windows `C:\Users\Public\L16P4` deleted (isdir False), 10 WSL `_tmp_l16p4_*` review temps deleted (glob-empty verified), no `_overlay_VERIFIED.svg` artifacts (cairosvg verify ran Claude-side only), no BlenderMCP debt. Deliverables retained: #8 PNG 1,204,728 B + #9 PNG 2,109,006 B. **Next: L16 P5 PRODUCTION** (`lighting-ratios`, #10 + #11 + #12).

**P3 DONE 2026-06-14.** `fill-light` FULLY CLOSED &#8212; both figures #6 `c6` fill_mistakes_3panel + #7 `c7` fill_settings_panel integrated (P3 PRODUCTION landed prior chat; this was an INTEGRATION-only chat). Entry state byte-exact: live P0 **158,326 B**, em-dash 26, figure 5 / img 3 / `<svg` 5 / figcaption 5, 2 mermaid, 12 section ids no dups, c6-/c7- ids 0, neither PNG referenced &#8212; matches the P2-close record exactly (safe resume). Both source PNGs byte-verified on WSL `images/`: #6 3004&times;1000 1,222,445 B (3-panel Cycles Too Much / Just Right / Too Little fill, orange 2px dividers, AgX no wash), #7 530&times;435 32,207 B (EEVEE light-DATA UI-chrome, Shadow checkbox UNCHECKED the critical callout, checkbox center (68,385) in PNG-pixel space). **D6 resolved per phase:** #6 is a composite PNG with baked `#FF6B00` 2px dividers (verified at x 1000&#8211;1001 / 2002&#8211;2003, three equal panels, centers 500/1502/2504) &#8594; composite-overlay variant (one centered `#1a1a1a` 0.82 pill per panel, no leader: Too Much Fill · Flat / Just Right · Balanced / Too Little Fill · Harsh); #7 is UI-chrome &#8594; SINGLE orange numbered marker (1) on the Shadow checkbox at (68,385) + leader up-right to a `#FF6B00` “Shadow: DISABLED” pill. Both overlays cairosvg-pixel-verified 1:1 over the real PNGs before commit (LD19): marker lands exactly on the unchecked checkbox, pills center under each panel. Anchors both count=1 (verified globally unique on a fresh copy): #6 after the “What is the Fill Light?” card `</div>` before `<h3>Fill Light Positioning</h3>` (anchor tail “Like a good film score—supports…”, the pre-existing em-dash preserved verbatim); #7 after the “Fill Light Settings” card `</div>` before `<h3>Getting Fill Light Right</h3>`. All default-depth (figure col20 / contents col24, wrapper-div + img margin:0 + inlined overlay-SVG viewBox = PNG pixels, figcaption OUTSIDE). Bottom-up insert #7 &#8594; #6, each `edit_file` dryRun&#8594;commit&#8594;get_file_info. Byte trail: 158,326 (P0) &#8594; #7 +1,904 &#8594; 160,230 &#8594; #6 +2,294 &#8594; **162,524 B**. POST-COMMIT INTEGRITY CLEAN (live-swept on 162,524 B): em-dash **26 held**, `&mdash;` 0, `color="#222"` 0, figure 5&#8594;**7**, figcaption 5&#8594;**7** (balanced), img 3&#8594;**5**, literal `<svg` 5&#8594;**7**, `</svg>` 7, `<marker` 0, `class="mermaid"` **2** (kept), wrapper relative divs 3&#8594;**5**, `&#183;` 15 (unchanged) + literal middot U+00B7 **3** (the 3 c6 pills), 12 section ids no dups, **c6-/c7- ids 0** (composite pills + single marker carry no `c{N}-*` ids &#8212; expected, tracked via PNG once-each), both PNGs referenced once each. No BlenderMCP work this chat (INTEGRATION-only; PRODUCTION landed prior chat, no scene touched, no temps, no debt). Deliverables retained: #6 PNG 1,222,445 B + #7 PNG 32,207 B. **Next: L16 P4 PRODUCTION** (`rim-light`, #8 + #9).

**P2 DONE 2026-06-14.** `key-light` FULLY CLOSED &#8212; all 3 figures #3 `c3` key_positions_comparison + #4 `c4` key_settings_panel + #5 `c5` shadow_quality_comparison produced + integrated (single mixed-method chat, PRODUCTION + INTEGRATION). Entry state byte-exact: live P0 **150,258 B**, em-dash 26, figure 2 / img 0 / `<svg` 2 / figcaption 2 (P1's #1/#2 inline SVGs), 2 mermaid, 12 section ids no dups, no `c{N}` ids &#8212; matches the P1-close record exactly. **D4 render recipe (Cycles, AgX) held; LD17 Standard NOT invoked** (luminance probe + visual review showed neutral grey, no wash on any panel). **D6 resolved per phase:** #3/#5 are composite PNGs with baked `#FF6B00` 2px dividers &#8594; composite-overlay variant (one centered pill per panel, no leader); #4 is genuine UI-chrome &#8594; numbered markers + legend. All 3 overlays cairosvg-pixel-verified 1:1 over the real PNGs before commit (LD19). #3 lesson_16_03 (1602&times;1602, 2&times;2 Cycles, Suzanne gray BSDF rough 0.4, key at 45&deg;R / 90&deg; side / frontal / 45&deg;L, only key position changes; `c3` 4 pills 45&deg;R&#183;Standard / 90&deg;&#183;Dramatic / Frontal&#183;Flat / 45&deg;L&#183;Alternate). #4 lesson_16_04 (576&times;575, UI-chrome &#8212; Properties DATA tab flipped cleanly via `space.context='DATA'` in THIS build, full-window local-tempdir `screen.screenshot` + deterministic area crop per LD20, then trimmed to the 4 teaching rows; `c4` 4 orange numbered markers Type/Power/Shape/Size + legend; all controls legible: Area highlighted, Power 1000, Shape Square, Size 3 m). #5 lesson_16_05 (2404&times;800, 1&times;3 Cycles, same key position, light SIZE 1/3/6 m showing hard&#8594;soft shadow edges, ground shadow framed; `c5` 3 pills 1 m&#183;Hard / 3 m&#183;Soft / 6 m&#183;Very soft). Anchors all count=1 (verified globally unique): #3 after the "What is the Key Light?" card `</div>` before `<h3>Key Light Positioning</h3>`; #4 after the "Key Light Settings" card `</div>` before `<h3>Common Key Light Mistakes</h3>`; #5 after the Common-Key-Light-Mistakes blockquote `</blockquote>` before `</section>`. All default-depth (figure col20 / contents col24, wrapper-div + img margin:0 + inlined overlay-SVG viewBox = PNG pixels, figcaption OUTSIDE). Bottom-up insert #5 &#8594; #4 &#8594; #3, each `edit_file` dryRun&#8594;commit&#8594;get_file_info. Byte trail: 150,258 (P0) &#8594; #5 +2,347 &#8594; 152,605 &#8594; #4 +2,974 &#8594; 155,579 &#8594; #3 +2,747 &#8594; **158,326 B**. POST-COMMIT INTEGRITY CLEAN (live-swept on 158,326 B): em-dash **26 held**, `&mdash;` 0, `color="#222"` 0, figure 2&#8594;**5**, figcaption 2&#8594;**5** (balanced), img 0&#8594;**3** (the 3 PNGs), literal `<svg` 2&#8594;**5** (P1 2 inline + 3 P2 overlays), `</svg>` 5, `<marker` 0, `class="mermaid"` **2** (kept), wrapper relative divs 0&#8594;**3**, `&#183;` 4&#8594;**15** (+11: c3 4 pills + c5 3 pills + c4 4 legend rows), `&#176;` 12&#8594;**18** (+6: c3 three + #3 figcaption three), `&#8211;` 2&#8594;**4** (+2: c4 legend ranges), literal middot/en-dash/minus 0, 12 section ids no dups, **c3-/c4-/c5- ids 0** (composite pills + numbered markers carry no `c{N}-*` ids &#8212; expected, tracked via PNG once-each), all 3 PNGs referenced once each. BlenderMCP: scene torn down to clean base 0/0/0 (confirmed via get_scene_info), EEVEE + AgX restored, Windows `C:\Users\Public\L16P2` + WSL `_tmp_l16_p2_f4_props_raw.png` deleted (ENOENT-verified), no debt. Deliverables retained: #3 PNG 1,146,525 B + #4 PNG 50,330 B + #5 PNG 842,828 B. **Next: L16 P3 PRODUCTION** (`fill-light`, #6 + #7).

**P1 DONE 2026-06-14.** `introduction` FULLY CLOSED &#8212; both figures #1 `tpo` three_point_overhead_diagram + #2 `tps` three_point_side_diagram produced + integrated (single chat, PRODUCTION + INTEGRATION, both hand-coded inline SVG). Entry state byte-exact: live P0 **135,522 B**, em-dash 26, 0 figure / 0 img / 0 svg / 0 figcaption / 0 `color="#222"`, 2 mermaid, 12 section ids no dups, no `c{N}` ids &#8212; matches the kickoff record exactly. Both SVGs default viewBox 800&times;560, three-light content colors per D1 (Key `#667eea` / Fill `#6BCF7F` / Rim `#FF6B9D`), brand `#FF6B00` top accent + tip rail + Suzanne tie-break glyph, `currentColor` text/dividers, Variant-B footer tip; cairosvg light+dark render-checked (camera/footer-tip collision in #1 fixed pre-write: camera raised y500&#8594;458, distance note relocated). Standalone `.svg` written to `images/` with root `color="#222"` KEPT for `<img>` fallback: #1 **5,364 B** / #2 **5,011 B** (both get_file_info-confirmed). Anchors count=1: #1 after the "What is Three-Point Lighting?" card `</div>` before the intro mermaid `<div class="mermaid">` (anchor extended to the unique "Predictable, repeatable" list-tail span, the bare `</div>`+mermaid pattern was count=2); #2 after the intro mermaid `</div>` before `<h3>History and Origins</h3>` (count=1). **D5 honored: both mermaid blocks KEPT intact** (#1 sits alongside the intro mermaid, not replacing it). Bottom-up insert #2 first then #1, each `edit_file` dryRun&#8594;commit&#8594;get_file_info. Inlined per LD12: root `color="#222"` stripped, multi-line `<svg>` root tag collapsed to one line, re-indented figure col20 / svg col24 / children col28 / `<g>` children col32, figcaption col20, structural comments kept, role/aria/title/desc/`tpo-*`/`tps-*` ids preserved. Byte trail: 135,522 (P0) &#8594; #2 +7,127 &#8594; 142,649 &#8594; #1 +7,609 &#8594; **150,258 B**. POST-COMMIT INTEGRITY CLEAN (live-swept on 150,258 B): em-dash **26 held**, `&mdash;` 0, `color="#222"` 0, figure 2/2, figcaption 2/2, literal `<svg` 2, `</svg>` 2, img 0 (both inline), `<marker` 0, `class="mermaid"` **2** (kept), `&#183;` 4 (2 per fig: subtitle + heading separator), `&#8211;` 2 (the two "3&#8211;5 m" notes), `&#176;` 12, middot/en-dash/minus literals 0, 12 section ids no dups, `tpo-title`/`tpo-desc`/`tps-title`/`tps-desc` each 1, no `c{N}` ids, 0 overlay wrapper-divs. No BlenderMCP work this phase (all-SVG, no renders, no scene touched, no temps). Deliverables retained: #1 SVG 5,364 B + #2 SVG 5,011 B. **Next: L16 P2 PRODUCTION** (`key-light`, #3/#4/#5).

**PLANNING DONE 2026-06-14.** Entry state verified on the real UNC path (135,522 B, em-dash 26, 0 figures / 0 img / 0 svg / 0 figcaption / 0 `color="#222"`, 2 mermaid, 12 section ids no dups, no `c{N}` ids). Read at kickoff: `session.md`, `lesson_16_three_point_lighting_setup.html` (full), `lesson_16_image_requirements.md` (full v1.0), `images/IMAGE_STYLE_GUIDE.md` v6, `lesson_15_integration_progress.md` (structural template). Authored this doc (registry / id-prefix registry / `c{N}` namespace / 12-phase plan / requirements reconciliation / Locked-rules). Reconciled the requirements doc to the pipeline (header note added; D1&#8211;D6 resolved). Figure set trimmed 32 &#8594; 25 (6 SVG / 19 PNG); 7 cut recorded in &sect;5. **0/25 produced / 0/25 integrated; HTML UNTOUCHED at 135,522 B.** No BlenderMCP work this chat (planning only; no scene touched, no temps). **Next: L16 P1 PRODUCTION** (`introduction`, #1 `tpo` overhead diagram + #2 `tps` side diagram, both hand-coded inline SVG, single chat with inline INTEGRATION).

---

## Locked rules carry forward

Inherited verbatim from the L06&#8211;L15 carry-forward blocks + `images/IMAGE_STYLE_GUIDE.md` v6 (LD1&#8211;21). All UNCHANGED:

- WSL `\\wsl$\Ubuntu\` paths only (never `\\wsl.localhost\`); `Filesystem:edit_file` dry-run &#8594; commit &#8594; `get_file_info` verify; `Filesystem:write_file` for new files / full-doc overwrites (never `create_file`); `copy_file_user_to_claude` to refresh the working HTML copy before grep/Python + **verify anchor GLOBAL uniqueness first** (anchor by unique TEXT extended to a multi-line span until count=1, never by section id).
- **em-dash baseline 26** (hold, add none; Python `count('\u2014')` authoritative). middle-dot `&#183;` entities for separation; pre-existing em/en-dashes preserved verbatim through exact anchors; content entities `&#215;` / `&#8211;` / `&#8594;` / `&#183;` fine.
- `tool_search` for deferred tools (silently). **Brand palette governs all overlays/SVG accent furniture** (NOT the spec's `#667eea`/`#6BCF7F`/`#FF6B9D`/`#ff8c00`); the three-light purple/green/pink coding is kept ONLY as semantic content color per D1.
- Inline-SVG conventions (strip root `color="#222"` on inlining, re-indent figure col20 / svg col24 / children col28 / deeper col32, collapse multi-line attrs, prune decorative comments, keep structural comments, preserve role/aria/title/desc/id); figure indentation depth from parent container; PNG overlay = wrapper-div + overlay-SVG, viewBox = PNG pixels, NO baked labels; pixel-verify every PNG overlay via cairosvg-composite (Claude-side) or PIL-composite (bridge-side) BEFORE commit (LD19).
- UI-capture craft (v6 LD14&#8211;16/LD20): modal status bar authoritative; full-window local-tempdir `screen.screenshot` then deterministic area crop (UNC write truncates to 0 bytes); Win+Shift+S overlay-freeze for transient popovers saved direct to WSL `images/`; clean-canvas viewport staging hides toolbar/N-panel/nav-gizmo.
- Color-dependent render recipe (v6 LD17) applies only if a PRODUCTION review shows washing (not pre-forced); BlenderMCP lowercase `blender:execute_blender_code` + clean-scene-manually via `bpy.data.objects.remove` + BLENDER_EEVEE (EEVEE_NEXT&#8594;EEVEE fallback) + Cycles for off-screen reflection/refraction; NEVER `read_factory_settings`; inline all render helpers per call; scene state between PRODUCTION chats is always a clean base (RESTORE at close; STOP if a leftover `_WORK` collection or hidden base objects are found).
- Pre-plan sub-chat splits for &#8805;5-figure phases + INTEGRATION-own-chat for &#8805;6; plan-lock before build; warn Ray before context tightens rather than letting automatic compaction happen.
