# Lesson 12 Image Work &middot; Integration Progress

**Lesson:** `lesson_12_uv_unwrapping_basics.html` (Module 3: Materials &amp; Texturing)
**Project root:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\`
**Image output:** `images/`
**Status:** **L12 COMPLETE &mdash; 28/28 produced / 28/28 integrated &middot; HTML 295,089 B.** All 7 figure-bearing phases done; the final P7 figures (#27/#28) integrated into `#hands-on-project`, overlays `c27`/`c28` BUILT + cairosvg-pixel-verified. Next step is the standing L12 browser-QA pass (all figures, dark + light at 1920/1024/414). **#28 island-label deviation flagged for Ray:** the produced UV layout is one large body fan + two cap discs + one handle barrel grid (not two wall fans), so the spec's four names were mapped large-disc=Bottom / small-disc=Inner Body / fan=Outer Body / barrel=Handle &mdash; a pixel-judgment call in the c26 "UV Editing tab" substitution class.
P2 reference: all 5 P2 Section-A figures (#6/#7/#8/#9/#26) integrated into `#uv-editor-interface` Section A, anchored by unique Section-A text/followers (never the duplicate section id). Overlays built + cairosvg-pixel-verified: `c6`, `c7`, `c9`; `c8` SKIPPED. **c26 substitution flagged for Ray:** the spec's "UV Editing tab" is not in the #26 crop, so the visible "Edit Mode" dropdown was labeled instead.
**Planning date:** 2026-06-05
**Style guide:** `images\IMAGE_STYLE_GUIDE.md` v6 (locked; all conventions live there, not restated here).

---

## Scope

28 figures total, sourced from `lesson_12_image_requirements.md`. Final split: **7 SVG / 21 PNG** (#3 flipped PNG&rarr;SVG at P1 PRODUCTION 2026-06-05; the SVG set is now #1, #2, #3, #11, #12, #13, #14 &mdash; matching the requirements-doc prose "7 SVG" and the SVG-Recommendations list).
Lesson 12 is interface-and-screenshot-heavy (UV Editor chrome, viewport seam captures, before/after composites), so the PNG share is large; the 7 SVGs are the pure geometry/flowchart diagrams (#1, #2, #3, #11, #12, #13, #14). This is the inverse balance from L11 (which was diagram-heavy at 14 SVG); the L11 PRODUCTION SVG craft still applies to the 7 here, and the L06/L10 UI-capture craft (status-bar-as-truth, local-tempdir screenshot carries overlays, Win+Shift+S overlay-freeze for transient popovers, deterministic area crop, `show_wire` ON for cut visibility) governs the screenshot PNGs.

PRODUCTION sub-chat split on the one &ge;6-figure phase (P2 = 6) and the borderline &ge;5 phase (P5 = 5); INTEGRATION-as-its-own-chat on the one &ge;6-figure phase (P2 = 6). P5 (5) is borderline: it rides PROD close as inline INTEGRATION unless context tightens, then breaks INTEGRATION out to its own chat (same call as L10 P4 / L11 P7).

### CRITICAL &mdash; live-HTML vs requirements-doc anchor reconciliation (read before any PRODUCTION/INTEGRATION)

The live HTML does **not** match the requirements-doc section-id list cleanly. Confirmed against the copied live file (194,981 B, 2026-06-05). The requirements doc lists 7 figure-bearing section ids; the live file has its own section structure. Reconciliation locked here so every downstream chat works from real, unique anchors and never from the requirements-doc ids alone:

- **DUPLICATE id defect: `uv-editor-interface` appears TWICE as a real `<section id>`** &mdash; line 254 (`<h2>The UV Editor Interface</h2>`, the requirements-doc home for #6/#7/#8/#9/#26) and line 1311 (`<h2>Working in the UV Editor</h2>`, a second UV-editor section the requirements doc does not enumerate). Because `id` must be unique, the requirements-doc `#uv-editor-interface` figure anchors (UV Editor Anatomy, Selection Modes in UV Editor, etc.) all resolve into **Section A (lines 254&ndash;638)**, confirmed by h3 content. **All P2 anchors are taken from Section A by unique h3/card text, NOT by the section id.** The id collision is a pre-existing lesson-body defect; figure work does NOT touch it (flag for a separate lesson-body QA fix, see Deferred). Section B (1311&ndash;1569, "Working in the UV Editor") carries NO requirements-doc figures and gets NO phase.
- **TOC ids do not match section ids.** The in-page TOC links to `#minimizing-distortion`, `#uv-layout-packing`, `#practical-unwrapping`, `#project` &mdash; **none of which exist as `id=` anchors anywhere in the file**. The real sections are `#manipulating-uv-islands`, `#optimizing-uvs`, `#hands-on-project`, plus `#common-mistakes` / `#summary` / `#whats-next`. The TOC is broken/stale (separate lesson-body defect, flag for QA; figure work does NOT touch the TOC). Map the requirements-doc placement sections to the REAL section ids per the table below.
- **Requirements-doc section id &rarr; REAL live-HTML section id mapping** (this is the binding map for phases):

  | Requirements-doc section id | Real live-HTML section id | Lines | Phase |
  |---|---|---|---|
  | `what-are-uvs` | `what-are-uvs` (match) | 104&ndash;253 | P1 |
  | `uv-editor-interface` | `uv-editor-interface` **Section A only** | 254&ndash;638 | P2 |
  | `seams-and-unwrapping` | `seams-and-unwrapping` (match) | 639&ndash;1000 | P3 |
  | `unwrapping-methods` | `unwrapping-methods` (match) | 1001&ndash;1310 | P4 |
  | `manipulating-uv-islands` | `manipulating-uv-islands` (match) | 1570&ndash;1859 | P5 |
  | `optimizing-uvs` | `optimizing-uvs` (match) | 1860&ndash;2114 | P6 |
  | `hands-on-project` | `hands-on-project` (match) | 2115&ndash;2602 | P7 |

  Six of seven match by id; only `uv-editor-interface` needs the Section-A disambiguation. Sections with NO figures and NO phase: the duplicate `uv-editor-interface` Section B (1311), `common-mistakes` (2604), `summary` (2825), `whats-next` (3246).
- **Anchor-by-text rule (locked):** every INTEGRATION insert anchors on the unique h3 / card text from the placement map, verified GLOBALLY UNIQUE on a freshly-refreshed copy at INTEGRATION time, NOT on the section id. Several placement-map h3 strings are already known non-unique on the live file (e.g. "UV Islands" 31&times;, "Pack Islands" 19&times;, "texture density" 19&times;, "Project from View" 7&times;, "Mirror UVs" 3&times;, "Strategic Seam Placement" 3&times;, "Overlapping UVs" 4&times;) &mdash; these require multi-line anchors (the L11 #7/#10 precedent: extend the anchor to a 3&ndash;8 line span until the count hits 1). Unique-on-first-check anchors include "The Problem: Mapping 2D to 3D", "UV Space: The 0 to 1 Square", "How UV Mapping Works", "Good vs. Bad Unwraps", "UV Editor Anatomy", "Selection Modes in UV Editor", "What Are Seams", "Cube/Cylinder/Sphere Seam Pattern", "UV Manipulation Tools", "Proportional Island Scaling", "UV Pinning", "Analyzing UV Distortion", "Straightening UV Islands", "Step 4", "Step 10".

### Planning decisions (LOCKED 2026-06-05)

- **D1 &mdash; no cross-phase reuse by default.** Unlike L11 (D1 #3 / D2 #26) and L10, the L12 requirements-doc placement map assigns each figure to exactly one section; there is no "insert Image #N" that appears in two sections. So the default is **produce-once / integrate-once, no reuse-by-reference**. If a PRODUCTION chat finds a natural second home for a figure (e.g. #8 UV Grid test texture or #22 texture-density echo), that becomes a reuse-by-ref decision logged at that time, ids NOT renamed (the L11 D1/D2 pattern). None pre-committed here.
- **D2 &mdash; format baseline = the requirements-doc File Naming Convention.** It assigns `.svg` to 7 figures (#1, #2, #3, #11, #12, #13, #14) and `.png` to 21. The "SVG Diagram Recommendations" list names the same 7. The split table below follows that 7/21 assignment as the locked baseline. Any in-flight type-flip is a PRODUCTION-time decision logged in the Status log.
- **D3 &mdash; the duplicate-id `uv-editor-interface` Section B and the TOC-id breakage are lesson-body defects, OUT OF SCOPE for image work.** Figure work anchors by unique text and never edits the section id or the TOC. Both are flagged in Deferred for a lesson-body QA fix.
- **D4 &mdash; #8 UV Grid Test Texture sourcing is a PRODUCTION-time call.** The requirements doc spec is a literal checker/number test texture (2048&times;2048 tileable). It is a PNG either way; route (Pillow-generated checker vs Blender's built-in UV Grid captured) decided at P2 PRODUCTION. It is self-labeling (numbers baked into the pattern ARE the content), so its overlay (`c8`) is reserved-default-SKIP.

---

## SVG-vs-PNG split (all 28)

Format column follows the requirements-doc File Naming Convention (D2). "Phase" is the figure's PRODUCTION home (primary section).

| # | Figure (slug) | Format | Phase | Notes |
|---|---------------|--------|-------|-------|
| 1 | uv_coordinate_system_diagram | **SVG** | P1 | **INTEGRATED** &middot; id-prefix `cs` &middot; 3D-cube-XYZ vs 2D-UV-square paired diagram + mapping arrows |
| 2 | uv_space_grid | **SVG** | P1 | **INTEGRATED** &middot; id-prefix `gd` &middot; 0&ndash;1 UV square, corner coords + center (0.5,0.5) |
| 3 | uv_mapping_process | **SVG** | P1 | **INTEGRATED** &middot; id-prefix `mp` &middot; 5-stage mapping flowchart (vertex&rarr;UV&rarr;texture&rarr;color&rarr;surface) &middot; **flipped PNG&rarr;SVG at P1 PRODUCTION** (pure flowchart geometry, inlines cleanly like the L11 `le` flowchart); labels live inside the `mp` SVG, so `c3` is DROPPED from the overlay namespace |
| 4 | uv_islands_example | PNG | P1 | **INTEGRATED** &middot; produced PNG has **6 islands (3&times;2)**, not the 4&ndash;5 planned &middot; overlay `c4` labels UV Island 1&ndash;6 (count taken from actual pixels) |
| 5 | good_bad_unwrap_comparison | PNG | P1 | **INTEGRATED** &middot; 2-panel good-vs-bad checker comparison (1526&times;944) &middot; overlay `c5` GOOD UNWRAP green &#10003; / BAD UNWRAP red &#10007; over upper area |
| 6 | uv_editor_interface_overview | PNG | P2 | **INTEGRATED** (787&times;972) &middot; after the UV Editor Anatomy card (Section A) &middot; overlay `c6` BUILT (multi-callout, fs15): Header / Toolbar / UV Islands / UV Grid (0&#8211;1 Space) / Sidebar (N&#8211;panel) &middot; cairosvg pixel-verified |
| 7 | uv_selection_modes | PNG | P2 | **INTEGRATED** (1696&times;972) &middot; after the Selection Modes in UV Editor card (Section A) &middot; overlay `c7` BUILT (composite-overlay, fs19): Vertex Mode (1) / Edge Mode (2) / Face Mode (3), leaders to the per-panel highlighted header toggle &middot; cairosvg pixel-verified |
| 8 | uv_grid_test_texture | PNG | P2 | **INTEGRATED** (2048&times;2048) &middot; after the Displaying Textures in UV Editor card, before the Checker Test blockquote (Section A) &middot; overlay `c8` **SKIPPED** (self-labeling numbered checker; img + figcaption only) per D4 |
| 9 | uv_stretch_overlay | PNG | P2 | **INTEGRATED** (784&times;972) &middot; anchored via the unique "Basic UV Manipulation Tools" follower (the Section-A Display-Options card "UV Editor Display Options" h3 is non-unique, count 2) &middot; overlay `c9` **BUILT** (legend, fs16): Area Stretch &mdash; Blue/Cyan low, Green moderate, Yellow high (honest to the ramp, tops at yellow, NO red) &middot; cairosvg pixel-verified |
| 10 | seam_marking_example | PNG | P3 | **INTEGRATED** (337&times;396) &middot; after the Seam Fundamentals card under "What Are Seams?" &middot; capped cylinder in Edit Mode, cyan-grey wire, top+bottom cap rims + 1 front vertical seam in red &middot; overlay `c10` BUILT (single "Marked Seams" callout, fs15, leader to the top-rim red seam) &middot; cairosvg-composited + viewed |
| 11 | seam_placement_strategy | **SVG** | P3 | **INTEGRATED** &middot; id-prefix `sp` &middot; within "Strategic Seam Placement" (after the Seam Placement Strategy card) &middot; 800&times;680 dense variant &middot; humanoid front view, good seams green-dashed + bad seams red-solid, 4 GOOD + 3 BAD pills, two-key legend &middot; 6,027 B |
| 12 | cube_seam_pattern | **SVG** | P3 | **INTEGRATED** &middot; id-prefix `cu` &middot; after the Cube Seam Pattern card &middot; 800&times;560 2-panel &middot; L: perspective cube cyan edges + 7 red seams; R: orange cross of 6 labeled squares &middot; 3,636 B |
| 13 | cylinder_seam_pattern | **SVG** | P3 | **INTEGRATED** &middot; id-prefix `cy` &middot; after the Cylinder Seam Pattern card &middot; 800&times;560 2-panel &middot; L: cyan cylinder + dashed red back seam + red cap rings; R: orange barrel rect + 2 cap circles &middot; 3,154 B |
| 14 | sphere_seam_pattern | **SVG** | P3 | **INTEGRATED** &middot; id-prefix `ss` &middot; after the Sphere Seam Pattern card &middot; 800&times;560 2-panel &middot; L: cyan wire sphere + red longitude (orange-peel) seams; R: orange peel segments full at equator, pinch markers at both poles &middot; 3,324 B |
| 15 | unwrap_methods_comparison | PNG | P4 | **INTEGRATED** (1574&times;1950) &middot; 2&times;2 grid Standard / Smart UV Project / Cube Projection / Cylinder Projection &mdash; one mug, four genuine Blender unwraps stored as 4 UV layers, each UV-editor canvas captured (clean canvas, toolbar hidden) + PIL-composited with `#FF6B00` cross-divider &middot; after the intro `<p>` under the Unwrap Method Comparison h3, before the comparison table &middot; overlay `c15` BUILT (composite-overlay, fs18, 4 panel-center pills) &middot; cairosvg-composited + viewed |
| 16 | project_from_view_example | PNG | P4 | **INTEGRATED** (1573&times;972) &middot; 2-panel &mdash; L: 3D viewport mug at 3/4 angle (SOLID, clean overlays); R: the resulting flat UVs from `project_from_view` run on that same view (UVs mirror the viewport silhouette) &mdash; both genuine captures, PIL-composited with `#FF6B00` divider &middot; after the Project from View card `</div>`, before the Cube Projection h3 &middot; overlay `c16` BUILT (Viewport View / Projected UVs pills + Project from View &#8594; connector across the divider, fs18) &middot; cairosvg-composited + viewed |
| 17 | uv_manipulation_tools | PNG | P5 | **INTEGRATED** (2364&times;924) &middot; after the UV Packing Basics card in `#manipulating-uv-islands` &middot; overlay `c17` BUILT (composite-overlay, fs27): Move (G) / Rotate (R) / Scale (S) |
| 18 | pack_islands_before_after | PNG | P5 | **INTEGRATED** (1574&times;924) &middot; after the Pack Islands Settings card &middot; overlay `c18` BUILT (fs18): BEFORE / AFTER + Pack Islands &#8594; on the divider |
| 19 | average_island_scale | PNG | P5 | **INTEGRATED** (1574&times;924, 34,604 B) &middot; after the When-to-Break-Proportionality card &middot; overlay `c19` **BUILT, NOT SKIP** (reserved-default-SKIP overridden against pixels &mdash; nothing in the PNG names the concept; fs18): Inconsistent / Normalized |
| 20 | uv_pinning_example | PNG | P5 | **INTEGRATED** (784&times;924, 17,079 B) &middot; after the Advanced Pinning Workflow card &middot; overlay `c20` BUILT (fs16 floor): Pinned [red #E63946] / Unpinned callouts + leaders &mdash; pixel scan confirmed bottom rows pinned-red / top rows orange, leaders verified on correct-colored verts |
| 21 | uv_mirroring_setup | PNG | P5 | **INTEGRATED** (1573&times;924, 205,593 B) &middot; after the UV Mirroring card, before the mermaid &middot; overlay `c21` BUILT (fs18): Symmetrical Model / Mirrored UVs + 50% Space Saved [green #4ADE80] |
| 22 | texture_density_test | PNG | P6 | **INTEGRATED** (588&times;584) &middot; after the "Achieving Consistent Texture Density" card in `#optimizing-uvs` &middot; checker mug, uniform density, no baked label &middot; overlay `c22` **BUILT** (fs18): Uniform Texture Density callout + leader to body checker &middot; cairosvg pixel-verified |
| 23 | uv_distortion_analysis | PNG | P6 | **INTEGRATED** (944&times;902) &middot; after the "Analyzing UV Distortion" card &middot; area-stretch overlay, ramp tops at yellow (NO red, same as `c9`) &middot; overlay `c23` **BUILT** (legend, fs16): Blue/Cyan low &middot; Green moderate &middot; Yellow high &middot; cairosvg pixel-verified |
| 24 | straightening_uv_islands | PNG | P6 | **INTEGRATED** (1797&times;893) &middot; grouped after #23 (the "Analyzing UV Distortion" card &mdash; reqs string "Within Straightening UV Islands" has no matching card; placed in the distortion-remedy family) &middot; 2-panel skewed&rarr;straight, divider x&asymp;897 &middot; overlay `c24` **BUILT** (fs20&ndash;22): BEFORE / AFTER + Follow Active Quads &#8594; connector across divider &middot; cairosvg pixel-verified |
| 25 | overlapping_uvs_detection | PNG | P6 | **INTEGRATED** (166&times;186, 3,069 B) &middot; after the "Overlapping UV Strategies" card &middot; small frame, orange overlap bottom-left (centroid 38,139) &middot; overlay `c25` **BUILT** (fs16 floor): Overlapping UVs Detected [warning-red] callout + leader to the orange island &middot; cairosvg pixel-verified |
| 26 | uv_workspace_layout | PNG | P2 | **INTEGRATED** (1575&times;972) &middot; after the Quick Setup for UV Work card (Section A) &middot; overlay `c26` BUILT (multi-callout, fs18): UV Editor / 3D Viewport / Edit Mode &mdash; **c26 substitution: spec's "UV Editing tab" not in crop, labeled the visible Edit Mode dropdown instead** (flagged for Ray) &middot; cairosvg pixel-verified |
| 27 | mug_project_seam_placement | PNG | P7 | **INTEGRATED** (764&times;840, 86,706 B) &middot; after Step 4 (Mark Seams), before Step 5 &middot; mug in Edit Mode, 4 red seams &middot; overlay `c27` **BUILT** (multi-callout, fs16 floor): Back Seam / Bottom Separation / Inner Rim / Handle Seam &mdash; leaders measured on the red-seam pixel clusters &middot; cairosvg pixel-verified |
| 28 | mug_project_final_uvs | PNG | P7 | **INTEGRATED** (960&times;945, 291,185 B) &middot; after Step 10 (Analyze and Optimize), before Bonus Challenges &middot; packed mug UV layout, 4 islands &middot; overlay `c28` **BUILT** (4 island labels, fs16 floor): Outer Body (left fan, centroid 281,586) / Bottom (large disc top-right, 682,263) / Handle (barrel grid mid-right, 627,587) / Inner Body (small disc lower-right, 651,789) &mdash; **island-label deviation flagged:** actual layout = 1 body fan + 2 cap discs + 1 handle barrel, not 2 wall fans; spec's 4 names mapped to the real blobs by geometry (large-disc=Bottom / small-disc=Inner Body) &middot; cairosvg pixel-verified |

**Totals:** **7 SVG (#1 `cs`, #2 `gd`, #3 `mp`, #11 `sp`, #12 `cu`, #13 `cy`, #14 `ss`) / 21 PNG.** #3 was flipped PNG&rarr;SVG at P1 PRODUCTION (2026-06-05), resolving the one open P1 call. The flip aligns the working split with the requirements-doc prose ("7 vector images") and the SVG-Recommendations list, which both counted #3 as an SVG; the File Naming Convention's `.png` for #3 is superseded by the PRODUCTION flip. `c3` is dropped from the `c{N}` overlay namespace (labels now live inside the `mp` SVG via the id-prefix registry).

&rarr; **Working split LOCKED: 7 SVG (#1 `cs`, #2 `gd`, #3 `mp`, #11 `sp`, #12 `cu`, #13 `cy`, #14 `ss`) / 21 PNG.** #3&rarr;SVG flip RESOLVED at P1 PRODUCTION 2026-06-05 (naming-convention line for #3 updated `lesson_12_03_uv_mapping_process.png` &rarr; `.svg`).

No color-dependent renders are forced by the spec the way L11 #5/#15/#30 were: the UV-editor and seam captures are UI chrome + wireframe/seam-edge captures, where the teaching colors (cyan UV edges, red seams, stretch blue/green/yellow/red) come from Blender's own theme, not from emissive-marker tuning. Treat the stretch-overlay figures (#9, #23) and any colored-seam capture as **standard captures** unless a PRODUCTION review shows a color washing out, at which point the v6 LD17 Standard-view-transform recipe applies (decide per figure at PRODUCTION).

**Sourcing note (not a format decision):** the PNGs split into (a) genuine Blender UI / viewport captures (#4, #6, #7, #9, #10, #16, #20, #22, #23, #25, #26, #27, #28 and the live-capture halves of #5/#12-context) routed through the BlenderMCP capture paths + manual Win+Shift+S for transient UI per v6 LD20, and (b) infographic / composite figures with no natural single viewport (#3 flowchart, #5 good-vs-bad, #8 test texture, #15 methods grid, #17 tools triptych, #18/#19/#24 before-after, #21 mirroring) that are Pillow-vignette overlay-host or Canva/Adobe rasters per v6 LD21. Route per figure is a PRODUCTION-time call; format (PNG) is locked here.

---

## Per-lesson id-prefix registry (inline SVG roots)

| Prefix | Figure |
|--------|--------|
| `cs` | #1 uv_coordinate_system_diagram |
| `gd` | #2 uv_space_grid |
| `mp` | #3 uv_mapping_process |
| `sp` | #11 seam_placement_strategy |
| `cu` | #12 cube_seam_pattern |
| `cy` | #13 cylinder_seam_pattern |
| `ss` | #14 sphere_seam_pattern |

7 inline-SVG prefixes (#3 flipped to SVG at P1 PRODUCTION 2026-06-05; `mp` added). No collisions with each other or with the `c{N}` overlay namespace. Per-page registry is document-scoped; no cross-lesson collision concern.

## PNG overlay `c{N}-*` namespace registry

Reserved now, built at the figure's INTEGRATION (coordinates measured against the actual cropped/produced PNG pixels per v6 LD19, never from plan estimates). Each overlay: white text on dark pills, `#FF6B00` leaders+dots, `&#183;` separators, composite-overlay fs `round(22 &times; viewBox_w / 1920)` with small-frame fs-floor 16; positive-green `#4ADE80` / warning-red `#E63946` colorway per label semantics (v6 decision 8). PNGs whose produced form already carries legible labels end up overlay-free (the L10 `c22` / L11 #17/#19/#20/#24 SKIPPED precedent) &mdash; decided at INTEGRATION.

- `c4` (#4 uv_islands_example &middot; per-island labels UV Island 1&ndash;**6**; multi-callout &mdash; produced PNG has 6 islands 3&times;2, not the planned 4&ndash;5; INTEGRATED)
- `c5` (#5 good_bad_unwrap_comparison &middot; 2 panel labels GOOD UNWRAP [positive-green] / BAD UNWRAP [warning-red] + check/X; composite-overlay variant over the panel divider)
- `c6` (#6 uv_editor_interface_overview &middot; Main UV Canvas / Toolbar / Header / UV Grid 0-1 / UV Islands / Sidebar; multi-callout, the largest by label count this lesson)
- `c7` (#7 uv_selection_modes &middot; 3 panel labels Vertex Mode (1) / Edge Mode (2) / Face Mode (3); composite-overlay variant)
- `c8` (#8 uv_grid_test_texture &middot; RESERVED, default SKIP &mdash; self-labeling numbered checker, D4)
- `c9` (#9 uv_stretch_overlay &middot; legend Blue=good / Green=ok / Yellow / Red=bad; decide at INTEGRATION whether the produced legend is self-labeling &rarr; SKIP, or needs the overlay)
- `c10` (#10 seam_marking_example &middot; single "Marked Seams" callout, white-on-dark pill, `#FF6B00` leader to the top-rim red seam; fs15 &middot; BUILT + cairosvg-composited; INTEGRATED)
- `c15` (#15 unwrap_methods_comparison &middot; 4 panel labels Standard Unwrap / Smart UV Project / Cube Projection / Cylinder Projection; composite-overlay variant, 2&times;2)
- `c16` (#16 project_from_view_example &middot; 2 labels Viewport View / Projected UVs + Project from View arrow)
- `c17` (#17 uv_manipulation_tools &middot; 3 panel labels Move (G) / Rotate (R) / Scale (S); composite-overlay variant)
- `c18` (#18 pack_islands_before_after &middot; 2 labels BEFORE / AFTER + Pack Islands arrow)
- `c19` (#19 average_island_scale &middot; 2 labels Inconsistent / Normalized; or self-labeling, decide at INTEGRATION)
- `c20` (#20 uv_pinning_example &middot; Pinned Vertices [won't move] / Unpinned Vertices [movable] callouts)
- `c21` (#21 uv_mirroring_setup &middot; 2 labels Symmetrical Model / Mirrored UVs + 50% Space Saved callout)
- `c22` (#22 texture_density_test &middot; Uniform Density callout; or self-labeling, decide at INTEGRATION)
- `c23` (#23 uv_distortion_analysis &middot; legend Blue=no distortion / Green=minimal / Yellow=some / Red=severe; or self-labeling)
- `c24` (#24 straightening_uv_islands &middot; 2 labels BEFORE / AFTER + Follow Active Quads arrow)
- `c25` (#25 overlapping_uvs_detection &middot; Overlapping UVs Detected [warning] callout on the highlighted overlap)
- `c26` (#26 uv_workspace_layout &middot; UV Editor (left) / 3D Viewport (right) / UV Editing tab; multi-callout)
- `c27` (#27 mug_project_seam_placement &middot; Back Seam / Bottom Separation / Inner Rim / Handle Seam; multi-callout, 4 labels)
- `c28` (#28 mug_project_final_uvs &middot; 4 island labels Outer Body / Inner Body / Bottom / Handle)

Reserved-if-needed (decide at INTEGRATION; default SKIP): `c8`, `c9`, `c19`, `c22`, `c23` (the self-labeling-legend candidates). `c3` is DROPPED (#3 flipped to SVG; labels live inside the `mp` SVG). The inline SVGs (#1 / #2 / #3 / #11 / #12 / #13 / #14) carry labels inside the SVG via the id-prefix registry and take NO `c{N}` overlay.

---

## Phase plan

Section-aligned to the 7 figure-bearing real HTML sections (the duplicate `uv-editor-interface` Section B, `common-mistakes`, `summary`, `whats-next` carry no figures, no phase). PRODUCTION sub-chat split on &ge;5-figure phases; INTEGRATION its own chat on &ge;6-figure phases. Phase ordering follows HTML section order (P1&rarr;P7), not figure-number order. Note figures #6/#7/#8/#9/#26 (P2) live in Section A of the duplicate-id pair, anchored by text.

| Phase | Section (real HTML id) | Figures (primary) | Count | PRODUCTION | INTEGRATION |
|-------|------------------------|-------------------|-------|------------|-------------|
| P1 | `what-are-uvs` | #1, #2, #3, #4, #5 | 5 | **2 sub-chats** &mdash; A: #1, #2 (SVG) + #3 if flipped &middot; B: #4, #5 (PNG) + #3 if PNG | inline (5 = borderline; rides PROD close, breaks out if context tightens) |
| P2 | `uv-editor-interface` (Section A) | #6, #7, #8, #9, #26 | 5 | **2 sub-chats** &mdash; A: #6, #26 (annotated chrome) &middot; B: #7, #8, #9 (panels/texture/overlay) | **own chat** (see note) |
| P3 | `seams-and-unwrapping` | #10, #11, #12, #13, #14 | 5 | **2 sub-chats** &mdash; A: #11, #12, #13, #14 (SVG) &middot; B: #10 (PNG capture) | inline (4 SVG cheap; #10 single capture) |
| P4 | `unwrapping-methods` | #15, #16 | 2 | single chat | inline |
| P5 | `manipulating-uv-islands` | #17, #18, #19, #20, #21 | 5 | **2 sub-chats** &mdash; A: #17, #18 &middot; B: #19, #20, #21 &middot; **&check; COMPLETE** | **&check; COMPLETE** (broke out to its own INTEGRATION chat) |
| P6 | `optimizing-uvs` | #22, #23, #24, #25 | 4 | single chat **&check; COMPLETE** | inline **&check; COMPLETE** |
| P7 | `hands-on-project` | #27, #28 | 2 | single chat **&check; COMPLETE** | inline **&check; COMPLETE** |

**Total figures across phases:** 5 + 5 + 5 + 2 + 5 + 4 + 2 = **28.** &check;

**P2 is the &ge;6 INTEGRATION-own-chat phase by count adjustment:** P2 carries 5 figures (#6/#7/#8/#9/#26) in its primary section. That is at the &ge;5 PRODUCTION-sub-chat threshold but below the &ge;6 INTEGRATION-own-chat threshold. **However**, P2's figures are the heaviest INTEGRATION work in the lesson (two multi-callout overlays `c6`/`c26` measured against full-window chrome + the `c7` composite + the `c8`/`c9` self-labeling decisions), and they all land in the duplicate-id Section A where every anchor needs the Section-A disambiguation. **LOCKED: P2 INTEGRATION runs as its own chat** (treat the multi-callout + duplicate-id-disambiguation load as equivalent to the &ge;6 rule, the same judgment call L10 P4 / L11 P7 made for borderline phases). P1, P3, P5 stay inline-borderline (break out only if context tightens). P4, P6, P7 are inline.

Within a phase, the inline SVGs (P1 #1/#2, P3 #11/#12/#13/#14) and self-labeling textures are cheap; the cost centers are the full-window UI captures with multi-callout overlays (#6, #26, #27) and the composite/before-after PNGs routed through Canva/Adobe or the Pillow-vignette host.

### Cross-phase reuse + produce-here / integrate-there

None pre-committed (D1). Produce-once / integrate-once for all 28. Any second-home reuse is a PRODUCTION-time decision logged in the Status log, ids NOT renamed (the L11 D1/D2 pattern). #14's id-prefix is `ss` (not `sphere`, to keep prefixes 2-char and collision-safe).

---

## Status log

- **2026-06-05 &mdash; L12 BROWSER-QA COMPLETE &mdash; VERDICT: PASS / CLEAN.** All 28 figures visual-reviewed dark + light at 1920/1024/414 against a from-scratch render harness (prior container's renders did not persist). Re-staged: copied the live HTML (295,089 B &mdash; matches source-of-truth), `styles/main.css`, and all 21 referenced PNGs to a local working dir; all 21 PNG dimensions match the registry **1:1, incl. #24 straightening_uv_islands 1797&times;893**. Throwaway QA copy built with the 4 externals neutralized (mermaid CDN `type="module"` import, `/favicon.png` link, `js/clipboard.js`, `js/course-enhancements.js`) &mdash; **real HTML untouched**. Launched real full Chromium 141 (`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, confirmed NOT headless_shell), **no route interception**; per width&times;theme: set `data-theme` on documentElement, scrolled top&rarr;bottom to trigger lazy imgs, waited `complete && naturalWidth>0` on every image, screenshotted each `<figure>` individually in DOM order. **168 shots (28&times;3&times;2), zero blank, zero naturalWidth=0.** **LAZY-LOAD MEASUREMENT FINDING RESOLVED:** the prior "naturalWidth=0 for ~16 of 21 PNGs" was NOT a route-abort handler &mdash; it was `loading="lazy"` (the 16 zero-width imgs = exactly the 16 lazy imgs; the 5 always-measured = the 5 eager #10/#22/#23/#24/#25). Fix = scroll top&rarr;bottom before measuring; with that, all 21 PNGs report true pixel dims 1:1 and zero naturalWidth=0. No real PNG load defects; measurement pass COMPLETE. **Visual-review verdict (PASS / CLEAN, no defects):** (a) all 28 render in both themes at all 3 widths &mdash; no broken images, no layout collapse, themes apply correctly; (b) **#24** overlay text (BEFORE/AFTER + Follow Active Quads &rarr;) legible at 414 (~350px downscale), both themes; (c) the **7 inline SVGs** (#1 `cs`/#2 `gd`/#3 `mp`/#11 `sp`/#12 `cu`/#13 `cy`/#14 `ss`) currentColor legible in BOTH themes (titles/labels/coords/axis text recolor correctly dark&harr;light, brand-color strokes constant); (d) the two accepted judgment calls eyeball-only confirmed reading correctly &mdash; **#28** four island labels (Bottom large disc / Outer Body fan / Handle barrel / Inner Body small disc) centered on their blobs, geometry-mapping honest to the unwrap; **#26** Edit Mode dropdown substitution points at the real visible control; (e) smallest frames hold up &mdash; **#25** (166&times;186) warning-red callout + leader legible at 414, **#10** seam-mark red rims/vertical clear. **Two cosmetic non-blocking notes (NOT figure defects, NOT fixed):** (1) the `position:sticky` "In This Lesson" TOC card overlaps figures during isolated `figure.screenshot()` captures &mdash; an artifact of per-figure screenshotting, NOT present in normal scroll reading; (2) #10's "Marked Seams" pill sits near the right margin at 414px (text still readable). **OUT OF SCOPE confirmed-present, noted not fixed (lesson-body defects):** broken TOC links `#minimizing-distortion` / `#uv-layout-packing` / `#practical-unwrapping` / `#project` (visible in the TOC card) + the duplicate `id="uv-editor-interface"` (line 254 + 1311). No HTML edits this chat (QA only; real HTML never touched &mdash; still 295,089 B). **L12 browser-QA is DONE / CLEARED (COMPLETE / CLEAN).**

- **2026-06-05 &mdash; P7 COMPLETE &mdash; L12 IMAGE WORK COMPLETE (28/28 produced / 28/28 integrated &mdash; single chat: INTEGRATION + close-out + scene RESTORE; #27/#28 produced earlier this session).** Read `session.md` + this progress doc. Verified both produced PNGs on disk (`get_file_info`): **#27** `lesson_12_27_mug_project_seam_placement.png` (764&times;840, 86,706 B) + **#28** `lesson_12_28_mug_project_final_uvs.png` (960&times;945, 291,185 B). Refreshed the working copy (`copy_file_user_to_claude`, 288,906 B confirmed pre-edit); confirmed both insertion anchors GLOBALLY UNIQUE on the fresh copy ("Step 4" h3 count=1, "Step 10" h3 count=1, plus the unique Checkpoint-2-card-close+Step-5-h3 and Final-Checkpoint-card-close+Bonus-Challenges-h3 insertion spans). Both PNG-overlay figures inserted into `#hands-on-project`: **#27** `c27` (fs16 floor) after Step 4 (Mark Seams), before Step 5 &mdash; Back Seam / Bottom Separation / Inner Rim / Handle Seam, each leader measured on the red-seam pixel clusters (back vertical body seam cx185/cy520, bottom rim cx327/cy705, inner top rim cx301/cy162, handle inner curve cx613/cy450); **#28** `c28` (fs16 floor) after Step 10 (Analyze and Optimize), before Bonus Challenges &mdash; 4 island labels measured to the actual island blobs via light-dilation labeling (Outer Body left fan 281,586 / Bottom large disc top-right 682,263 / Handle barrel grid mid-right 627,587 / Inner Body small disc lower-right 651,789). Both overlays cairosvg-composited + **viewed before commit** (all c27 leaders land on the correct red seams; all c28 labels centered on their islands, legible). **#28 island-label deviation flagged for Ray:** the produced UV layout is one large body fan + two radial cap discs + one handle barrel grid &mdash; NOT the spec's implied two wall fans (Outer Body + Inner Body as arc strips). The four spec names were mapped to the real blobs by geometry: fan=Outer Body, large disc=Bottom, small disc=Inner Body, barrel=Handle. This is a pixel-judgment call in the same class as the P2 c26 "UV Editing tab" substitution; the figure is honest to what the unwrap actually produced. One atomic `edit_file` (dry-run &rarr; commit &rarr; `get_file_info`), both inserts non-overlapping. Post-commit `get_file_info` &rarr; **295,089 B** (+6,183). Refreshed-copy integrity re-verify in `#hands-on-project`: 2 `<figure>` / 2 `</figure>` / 2 `<figcaption>` / 2 balanced overlay `<svg>`+`</svg>`; c27/c28 title+desc ids present once each; both img refs present once (section + doc-wide); doc-wide em-dash count **29&rarr;29** (0 added &mdash; figcaptions use `&#183;` + the `&#8211;` in #28's "0&#8211;1"); 0 stray root `color="#222"`; 11 section ids (duplicate `uv-editor-interface` preserved, out of scope). **L12 image work is now COMPLETE: 28/28 produced / 28/28 integrated &middot; HTML 295,089 B.**
- **SCENE RESTORE (this chat):** the open state was NON-standard per the handoff. Handled in order: (1) `mug_p7` was still in EDIT_MESH &mdash; exited to OBJECT mode; (2) one screen area had been reconfigured during capture (maximized IMAGE_EDITOR &rarr; VIEW_3D &rarr; IMAGE_EDITOR/UV) and `mug_p7.show_wire=True` &mdash; reset area/overlay/wire state; (3) removed `_L12P7_WORK` + `mug_p7` (verified no separate L12P7 mat/grid was created this session), orphan-purged; (4) deleted the WSL temps (`images/_tmp_l12_p7_27_fullwin/_27_crop/_28_fullwin/_28_crop.png` + any Windows `_tmp_l12_p7_*`); (5) re-verified the clean base (0-obj / no `_WORK` / no hidden / 0 orphans / EEVEE / 512&times;512 / AgX / world 0.88). **Scene-RESTORE detail (findings):** the open state matched the handoff exactly &mdash; `mug_p7` in EDIT_MESH, `mug_p7.show_wire=True`, one maximized IMAGE_EDITOR area, no separate L12P7 material/grid created this session, 1 orphan mesh at audit (purged). Steps 1&ndash;3 (exit EDIT&rarr;OBJECT; reset reconfigured area + overlay/wire; remove `_L12P7_WORK` + `mug_p7`, orphan-purge 2) were done last chat. Steps 4&ndash;5 done this chat: step 4 deleted the 3 still-present WSL temps (`_27_crop` / `_28_fullwin` / `_28_crop`; `_27_fullwin` was already absent) + 3 Windows `_tmp_l12_p7_*` in `%TEMP%` (`_27_crop` / `_27_fullwin` / `_28_fullwin`); step 5 re-verified the clean base via BlenderMCP (0-obj / no `_WORK` / no hidden / 0 orphans / EEVEE / 512&times;512 / AgX / world 0.88; default empty `Collection` is the normal clean base, not a leftover `_WORK`). L12 P7 RESTORE complete.
- `session.md` repointed to **L12 browser-QA** (the standing deferred follow-up: all figures dark + light at 1920/1024/414) as the next step now that figure work is done. **28/28 produced / 28/28 integrated &middot; HTML 295,089 B.**

- **2026-06-05 &mdash; P6 COMPLETE (26/28 produced / 26/28 integrated &mdash; single chat, PRODUCTION carried in from CONTINUATION-1, INTEGRATION + close-out this chat).** Read `session.md` + this progress doc; refreshed the working copy (`copy_file_user_to_claude`, 281,234 B confirmed pre-edit) and re-verified all 4 keepers on disk + dimensions (#22 588&times;584 / #23 944&times;902 / #24 1797&times;893 / #25 166&times;186, 3,069 B). All 4 P6 PNG-overlay figures inserted into `#optimizing-uvs`, each anchored on its card's unique final-`<li>`+`</div>` span (count=1 verified on the fresh copy): **#22** `c22` (fs18) after the "Achieving Consistent Texture Density" card &mdash; Uniform Texture Density callout + leader to the body checker (checker mug, no baked label, BUILDS); **#25** `c25` (fs16 floor, small 186px frame) after the "Overlapping UV Strategies" card &mdash; Overlapping UVs Detected warning-red callout + leader to the orange overlap (measured centroid 38,139); **#23** `c23` (legend fs16) after the "Analyzing UV Distortion" card &mdash; Area Stretch Blue/Cyan low &middot; Green moderate &middot; Yellow high (ramp tops at yellow, NO red, same as `c9`, honest to the pixels); **#24** `c24` (fs20&ndash;22) grouped immediately after #23 &mdash; BEFORE / AFTER + Follow Active Quads &#8594; connector across the divider (measured at x&asymp;897, left-center ~447 / right-center ~1348). **#24 OPEN DECISION resolved:** the reqs placement string "Within Straightening UV Islands" has no matching card/h3 in `#optimizing-uvs`; of the two candidates (after the Margin Trade-off card vs grouped with #23), chose **grouped after #23 (Analyzing UV Distortion)** &mdash; straightening via Follow Active Quads is a distortion-remedy, sitting directly under the card's Fixing-distortion list (Relax / re-unwrap), the stronger conceptual home; the Margin Trade-off card is about packing margins, unrelated. All 4 overlays cairosvg-composited + viewed pre-commit (every callout lands clear of mesh/UV content; leaders on the correct features). One atomic `edit_file` (dry-run &rarr; commit &rarr; `get_file_info`), 3 non-overlapping edits (#22, #25, #23+#24). Post-commit `get_file_info` &rarr; **288,906 B** (+7,672). Refreshed-copy integrity re-verify in `#optimizing-uvs`: 4 `<figure>` / 4 `</figure>` / 4 `<figcaption>` / 4 balanced overlay `<svg>`+`</svg>`; c22/c23/c24/c25 overlay ids present once each; all 4 img refs present once; doc-wide em-dash count **29&rarr;29** (0 added &mdash; figcaptions use `&#183;`, not em-dash entities; the pre-existing "UV channel" em-dash + the `&#8594;` in the Relax `<li>` anchor preserved verbatim); 0 stray root `color="#222"`; 11 section ids (duplicate `uv-editor-interface` preserved, out of scope).
- **OPEN-STATE FINDING (recorded for the scene-state audit trail):** P6 PRODUCTION (CONTINUATION-1) opened with Blender at the **factory-startup default** (Camera/Cube/Light, 1920&times;1080, world 1.0), NOT the locked clean baseline (0-obj/512&times;512/world 0.88/EEVEE/AgX). No leftover `_WORK` collection or hidden base objects were present, so this read as a **fresh Blender relaunch since the P5 close**, not a failed prior RESTORE &mdash; **no STOP triggered.** The scene was normalized to the locked baseline (0 objects / 512&times;512 / world 0.88 / EEVEE / AgX) before producing. Carry-forward note for P7: if Blender again opens at factory-startup rather than the clean baseline, treat it as a relaunch (normalize, no STOP) **only if** there is no `_WORK`/hidden-base residue; any `_L12Px_WORK` or hidden base objects still means a failed RESTORE = STOP.
- **SCENE RESTORE (this chat):** `_L12P6_WORK` was still live at chat open (PRODUCTION did not RESTORE before the chat split). At inspection the collection held **3** work objects (`mug_p6`, `straighten_strip_p6`, `overlap_obj_p6`); `distort_sphere_p6` was already absent (removed during PRODUCTION). Removed the 3 remaining work objects + `_L12P6_WORK` + `L12P6_UVGridMat` + `L12P6_UVGrid`, orphan-purged (6 data-blocks), and re-verified the clean base (0 objects / no `_WORK` / no hidden / 0 orphans / EEVEE / 512&times;512 / AgX / world 0.88). Deleted all 17 WSL `images/_tmp_l12_p6_*` temps (`_22_mask`/`_22_crop`/`_22_fullwin`, `_23_area`/`_23_area_clean`/`_23_angle`/`_23_crop`, `_24_before`/`_24_after`/`_24_after2`/`_24_after3`/`_24_before_probe`/`_24_composite`, `_25_wide`/`_25_crop`/`_25_probe`/`_25_probe2`); the Windows tempdir was already cleared at PRODUCTION.
- `session.md` repointed to **L12 P7 `#hands-on-project` (#27, #28)** &mdash; the final figure-bearing phase. **26/28 produced / 26/28 integrated &middot; HTML 288,906 B.**


- **2026-06-05 &mdash; P5 INTEGRATION COMPLETE (22/28 integrated &mdash; P5 fully complete; broke out to its own chat).** Read `session.md` + this progress doc; refreshed the working copy (`copy_file_user_to_claude`) and ran a full integrity re-verify on the fresh file (`get_file_info` 281,234 B = +12,749 from 268,485). All 5 P5 PNG-overlay figures inserted after their card `</div>` in `#manipulating-uv-islands`, each anchored on a unique emoji+h4 card-title `card-close` span (count=1): **#17** `c17` (2364&times;924, fs27) after UV Packing Basics; **#18** `c18` (1574&times;924, fs18) after Pack Islands Settings (BEFORE/AFTER + Pack Islands &#8594; on divider); **#19** `c19` (1574&times;924, fs18) after When-to-Break-Proportionality &mdash; **`c19` overlay BUILT, NOT skipped** (the reserved-default-SKIP was overridden against the actual pixels: nothing in the PNG names Inconsistent/Normalized); **#20** `c20` (784&times;924, fs16 floor) after Advanced Pinning Workflow &mdash; pixel scan confirmed bottom rows pinned-red `#E63946` / top rows orange, leaders verified landing on the correct-colored verts; **#21** `c21` (1573&times;924, fs18) after UV Mirroring before the mermaid (Symmetrical Model / Mirrored UVs + 50% Space Saved `#4ADE80`). All overlays cairosvg-composited + viewed pre-commit. Integrity re-verify on the fresh file: P5 section 5 `<figure>` / 5 `</figure>` / 5 `<figcaption>` / 5 balanced overlay `<svg>`+`</svg>`; all of c17&ndash;c21 present; each of the 5 img refs (lesson_12_17&hellip;/18&hellip;/19&hellip;/20&hellip;/21&hellip;.png) present once; doc-wide em-dash count **29&rarr;29** (0 added); 0 stray root `color="#222"`; 11 section ids (duplicate `uv-editor-interface` preserved, out of scope). No BlenderMCP this chat (INTEGRATION only; no scene state to RESTORE). `session.md` repointed to **L12 P6 `#optimizing-uvs`**. **22/28 produced / 22/28 integrated &middot; HTML 281,234 B.**

- **2026-06-05 &mdash; P5 PRODUCTION sub-chat B COMPLETE (22/28 produced &mdash; P5 PRODUCTION now complete).** Read `session.md` + this progress doc. Resumed mid-sub-chat: #19 + #20 had been PRODUCED + written to disk earlier in the sub-chat but NOT yet logged, and #21 was in progress in `_L12P5_WORK`. Verified both unlogged keepers on disk (`get_file_info`): **#19** `lesson_12_19_average_island_scale.png` (1574&times;924, 34,604 B, 2-panel inconsistent vs normalized island scale) + **#20** `lesson_12_20_uv_pinning_example.png` (784&times;924, 17,079 B, pinned-red vs unpinned vertices). Inspected the live scene against the clean-base rule: found the expected in-progress state &mdash; `mirror_model` (visible) + `uv_p5b_cube` (hidden via hide_get + hide_viewport) in `_L12P5_WORK`, `L12P5_UVGridMat`/`L12P5_UVGrid`, 1 orphan mesh `Cube.001`; this is sub-chat-B work-in-progress, NOT a failed prior RESTORE, so no STOP. **#21** `lesson_12_21_uv_mirroring_setup.png` (1573&times;924, 205,593 B): `mirror_model` is a symmetric subdivided box (98v/96f, no live mirror modifier &mdash; symmetry baked into geometry; base `UVMap` layer empty, UVs live on the evaluated mesh, one 0&ndash;1 region U[0.005,0.995] V[0.005,0.995]). Confirmed the two halves genuinely overlap in UV space before staging: 48 faces X&lt;0 + 48 faces X&gt;0 + 0 center, both halves' UV-centroid bboxes overlap on X and Y = stacked (the real mirror teaching point). **Confirmed the UV-panel approach with Ray** (selection-contrast, no UVs moved) then resolved the flagged staging gap: a naive select-all UV shot reads as a single uniformly-lit island (overlap invisible). Fix = `use_uv_select_sync=True` with only the +X half mesh-selected, so that half's UV island draws LIT (orange) directly over the identical unlit (grey) &minus;X half island in the same 0&ndash;1 region &mdash; the lit-over-unlit contrast makes the mirror overlap read while keeping the UVs a true untouched overlap. Also cleared the busy UV-Grid checker behind (set IMAGE_EDITOR image None) for a clean dark canvas matching #17/#18. LEFT panel: VIEW_3D SOLID + `show_wire`, +X half selected (symmetry plane reads), clean canvas (toolbar/N-panel/gizmo/floor/axes/cursor/text off), 3/4 PERSP. Both full-window `screen.screenshot` (no `full=` kwarg in this build) &rarr; Windows tempdir &rarr; PIL-crop (VIEW_3D 792,&hellip;,783&times;924 drop-48 header; IMAGE_EDITOR 3,&hellip;,784&times;924) &rarr; QA-viewed each before composite &rarr; 2-panel PIL composite with 6px `#FF6B00` divider. QA-viewed final: lit +X half sits exactly over the dark &minus;X half = mirror overlap unambiguous; model symmetry plane clear. Standard captures (theme colors; no LD17). No baked labels (`c19`/`c20`/`c21` ride at INTEGRATION). Deleted 18 temps (13 Windows `_tmp_l12_p5b_*` incl. leftover #19/#20/#21 shots + crops, 5 WSL `_tmp_l12_p5b_*` relays incl. 2 leftover #19 crops). Scene RESTORE: exited Edit Mode, reset `use_uv_select_sync` False + VIEW_3D overlays/gizmo/toolbar/N-panel + IMAGE_EDITOR image None, removed `mirror_model` + `uv_p5b_cube` (un-hidden then removed) + `_L12P5_WORK` + `L12P5_UVGridMat` + `L12P5_UVGrid`, orphan-purge (3 data-blocks incl. `Cube.001`) &rarr; re-verified clean base (0 objects, no `_WORK`, no hidden, 0 orphans, EEVEE/512&times;512/AgX/world 0.880). HTML untouched 268,485 B (PRODUCTION only). **P5 PRODUCTION now complete (all 5: #17/#18/#19/#20/#21).** `session.md` repointed to **L12 P5 INTEGRATION (inline-borderline, all 5 P5 figures)**. **22/28 produced / 17/28 integrated &middot; HTML 268,485 B.**

- **2026-06-05 &mdash; P5 PRODUCTION sub-chat A COMPLETE (19/28 produced).** Read `session.md` + this progress doc. Confirmed both specs + per-figure PROD routes with Ray (both Blender-genuine UV-editor captures, only the final stitch is PIL; #17 result-only panels per the #7 precedent, #18 fresh subdivided cube for a stark scatter&rarr;pack contrast) before producing. Verified clean base (0 objects, no `_WORK`, no hidden, EEVEE/512&times;512/AgX/world 0.88, 0 orphans &mdash; no STOP). Built `_L12P5_WORK`: a 2m cube + UV-Grid generated material, Smart-UV projected. **#17** `lesson_12_17_uv_manipulation_tools.png` (2364&times;924, 127,496 B): staged a clean UV-editor canvas (toolbar/N-panel/tool-header hidden, IMAGE_EDITOR x=3 w=784, window 1920&times;1046 scale 1.0), locked a fixed view (no `view_all` between transforms so the islands move against a static 0&ndash;1 grid), then applied + captured each of Move (translate +0.16/+0.09) / Rotate (30&deg; Z) / Scale (resize 0.6) with an **exact inverse revert** between each so the baseline + view never drifted. Key gotcha caught: `screen.screenshot` grabbed a stale framebuffer after the UV transform (first pass: all 3 panels byte-identical to neutral, ImageChops diff = 0) &mdash; fixed by forcing a redraw (`area.tag_redraw()` + `bpy.ops.wm.redraw_timer(type='DRAW_WIN_SWAP', iterations=1)`) before each shot; re-verified each state's diff vs neutral non-zero (move 11.0M / rotate 6.4M / scale 10.7M total-diff). Cropped the canvas region (3,87,787,1011 = 784&times;924, header strip dropped) per state &rarr; 3-panel PIL composite with 6px `#FF6B00` dividers. QA-viewed: all three operations read unambiguously against the fixed grid (offset block / 30&deg; tilt / 60% shrink); the small `<`/`>` region-toggle arrows at the top corners are consistent real-UI chrome, accepted per the P4 #15 precedent. **#18** `lesson_12_18_pack_islands_before_after.png` (1574&times;924, 85,757 B): subdivided the cube to 24 faces, low-angle Smart-UV to ~16 small islands, then **scattered** them via a bmesh UV-loop pass (per-face random offset across/beyond 0&ndash;1 + random rotation + random 0.18&ndash;0.42 scale, seed 7) for a clear inefficient BEFORE; captured; ran `uv.pack_islands(rotate=True, margin=0.02)` for the AFTER (reframed to 0&ndash;1). Both canvas-cropped + PIL 2-panel composite with `#FF6B00` divider. QA-viewed: stark scattered/rotated/tiny &rarr; packed/axis-aligned/maximized contrast; the pack `rotate=True` straightened every island (the teaching point). Both standard captures (UV/island colors are Blender's theme; no LD17 recipe forced). No baked labels (`c17`/`c18` ride at INTEGRATION). Deleted 18 temps (12 Windows full-window+intermediate shots + 6 WSL QA relays). Scene RESTORE: exited Edit Mode, reset the staged IMAGE_EDITOR toolbar/N-panel/tool-header/stretch toggles, removed `uv_tools_cube` + `_L12P5_WORK` + `L12P5_UVGridMat` + `L12P5_UVGrid`, orphan-purge (1 data-block) &rarr; re-verified clean base (0 objects, no `_WORK`, no hidden, 0 orphans, EEVEE/512&times;512/AgX/world 0.88). HTML untouched 268,485 B (PRODUCTION only). `session.md` repointed to **L12 P5 PRODUCTION sub-chat B (#19, #20, #21)**. **19/28 produced / 17/28 integrated &middot; HTML 268,485 B.**

- **2026-06-05 &mdash; P4 COMPLETE (17/28 produced / 17/28 integrated &mdash; single chat, PRODUCTION + INTEGRATION inline).** Read `session.md` + this progress doc. Confirmed both specs + per-figure PROD routes with Ray (mug mesh for #15; both routes Blender-genuine, no Pillow/Canva composite, only the final stitch is PIL) before producing. **PRODUCTION (one BlenderMCP session):** clean-base gate passed at open (0 objects, no `_WORK`, no hidden, EEVEE/512&times;512/AgX/world 0.88, 0 orphans &mdash; no STOP). Built `_L12P4_WORK`: a mug = 32-seg cylinder (open top) + torus handle joined (352 v / 672 e / 321 f), flat-shaded, UV-Grid material. **#15** `lesson_12_15_unwrap_methods_comparison.png` (1574&times;1950, 610,948 B): created 4 UV layers (`m_standard`/`m_smart`/`m_cube`/`m_cylinder`) and unwrapped the same mug four ways into them (ANGLE_BASED w/ sharp-edge seams; smart_project; cube_project; cylinder_project ALIGN_TO_OBJECT). Staged a clean UV-editor canvas (toolbar + N-panel + tool-header hidden), switched the active UV layer per method, framed UVs, full-window `screen.screenshot` &rarr; Windows tempdir (scale 1.0 verified vs 1920&times;1046) &rarr; PIL-crop the IMAGE_EDITOR region (3,39,787,1011 = 784&times;972) per method &rarr; 2&times;2 PIL composite with 6px `#FF6B00` cross-divider (TL Standard / TR Smart / BL Cube / BR Cylinder). The four methods read as genuinely distinct (scattered angle islands / packed small islands / overlapping concentric rings / single tall barrel). Note: the UV-editor vertex dots + per-panel area-header menus remain in-frame (consistent across all four, reads as real UV editor) &mdash; accepted, not re-shot. **#16** `lesson_12_16_project_from_view_example.png` (1573&times;972, 434,373 B): added a 5th UV layer `m_pfv`, staged the 3D viewport (SOLID, floor/axes/cursor/gizmo/text off, `show_wire` ON, 3/4 PERSP via view_rotation Euler 72&deg;/0/28&deg;), captured the viewport (left), ran `uv.project_from_view` from that exact view, captured the UV editor (right) &mdash; both full-window &rarr; tempdir &rarr; PIL-crop (VIEW_3D 792,39,1575,1011 = 783&times;972; IMAGE_EDITOR 3,39,787,1011 = 784&times;972) &rarr; 2-panel PIL composite with 6px `#FF6B00` divider. The projected UVs mirror the viewport silhouette exactly (same taper, rim curve, handle position) &mdash; the teaching point lands. Deleted 14 Windows temps + 6 WSL QA relays. Scene RESTORE: exited Edit Mode, reset the staged VIEW_3D + IMAGE_EDITOR toggles, removed mug + `_L12P4_WORK` + `L12P4_UVGridMat` + `L12P4_UVGrid`, orphan-purge (1 data-block) &rarr; re-verified clean base (0 objects, no `_WORK`, no hidden, 0 orphans, EEVEE/512&times;512/AgX/world 0.88). **INTEGRATION (inline):** refreshed the working copy (`copy_file_user_to_claude`, 262,768 B confirmed pre-edit), re-verified both anchors GLOBALLY UNIQUE on the fresh copy (`<h3>Project from View</h3>` count 1 &mdash; the bare "Project from View" is 7&times; so the full h3 tag was used; "Unwrap Method Comparison" count 1; the card-close+`<h3>Cube Projection</h3>` and the intro-`<p>`+`<table>` insertion anchors both count 1). Built overlays `c15` (composite-overlay, fs18, 4 panel-center pills measured against the actual composite pixels) + `c16` (Viewport View / Projected UVs pills + a Project from View &#8594; connector pill straddling the divider at mid-height, fs18/fs16), cairosvg-composited + **viewed before commit** (all pills land in the dark header bands, clear of UV content). One atomic `edit_file` (dry-run &rarr; commit &rarr; `get_file_info`), both inserts non-overlapping: **#16** after the Project from View card `</div>` (before the Cube Projection h3); **#15** after the intro `<p>` under the Unwrap Method Comparison h3 (before the comparison table). Both figures use the locked PNG-overlay pattern (wrapper-div + overlay-SVG, viewBox = PNG pixels, no baked labels), figure col20 / div col24 / img col28 / svg col28 / children col32 / figcaption col20 (matching the #26 template), real alt + figcaption, `&#8594;` numeric entity. Post-commit `get_file_info` &rarr; **268,485 B** (+5,717); refreshed copy verified: 2 `<figure>`/2 `</figure>`/2 `<figcaption>`/2 `<svg>`/2 `</svg>` in `#unwrapping-methods`; both `<img>` refs present (1 each); `c15`/`c16` ids present; #16 between the Project-from-View and Cube-Projection h3s, #15 between the intro `<p>` and the table; Section B ("Working in the UV Editor") intact; doc-wide em-dash count unchanged **29&rarr;29** (the 5 pre-existing em-dashes in the unwrapping-methods cards all below/inside the anchors, preserved verbatim; 0 added); 11 section ids (duplicate `uv-editor-interface` preserved, out of scope); 0 stray root `color="#222"`. No BlenderMCP needed during INTEGRATION (scene already RESTORED at PRODUCTION close). `session.md` repointed to **L12 P5 `#manipulating-uv-islands`**. **17/28 produced / 17/28 integrated &middot; HTML 268,485 B.**

- **2026-06-05 &mdash; P3 INTEGRATION COMPLETE (15/28 integrated &mdash; P3 fully complete).** Read `session.md` + this progress doc; refreshed the working copy (`copy_file_user_to_claude` &mdash; 236,933 B confirmed pre-edit) and re-verified every anchor GLOBALLY UNIQUE on the fresh copy. Integrated all 5 P3 figures into `#seams-and-unwrapping`, each anchored by unique card text (all 5 placement-map h3/h4 strings already count=1; no multi-line extension needed), one atomic `edit_file` (dry-run &rarr; commit &rarr; `get_file_info`), all 5 inserts non-overlapping. **#10** `seam_marking_example` PNG after the Seam Fundamentals card under "What Are Seams?" (wrapper-div + overlay-SVG, viewBox = 337&times;396 PNG pixels), overlay **`c10` BUILT** (single "Marked Seams" callout, white-on-dark pill fs15, `#FF6B00` leader to the top-rim red seam at the measured red-pixel position) + cairosvg-composited + viewed before commit; **#11 `sp`** within "Strategic Seam Placement" (after the Seam Placement Strategy card, before the Visibility Trade-off card); **#12 `cu`** after the Cube Seam Pattern card; **#13 `cy`** after the Cylinder Seam Pattern card; **#14 `ss`** after the Sphere Seam Pattern card. The 4 SVGs inlined per locked conventions (root `color="#222"` stripped &mdash; 0 remain doc-wide; root attrs collapsed to one line; re-indent figure col20 / svg col24 / children col28+; decorative `=====` comments pruned, structural kept; role/aria/title/desc/id preserved; `&#183;`/`&#8593;`/`&#8595;` entities preserved; figcaptions added). Locked conventions held: PNG-overlay wrapper-div pattern (viewBox = PNG pixels, no baked labels); no new em-dashes (pre-existing em-dashes in the Cardboard-Box / cube / sphere cards all below the anchors, preserved verbatim &mdash; doc-wide em-dash count unchanged 29&rarr;29). Post-commit `get_file_info` &rarr; **262,768 B** (+25,835); refreshed copy verified 5 `<figure>` / 5 `<figcaption>` / balanced 5 `<svg>`+5 `</svg>` in the P3 section / all 4 inline-SVG ids + `c10` ids + the #10 PNG ref present / 0 stray root `color="#222"` / 11 section ids (duplicate `uv-editor-interface` preserved, out of scope). The 7 inline SVGs carry no `c{N}`. No BlenderMCP this chat (INTEGRATION only; no scene state to RESTORE). `session.md` repointed to **L12 P4 `#unwrapping-methods`**. **15/28 produced / 15/28 integrated &middot; HTML 262,768 B.**

- **2026-06-05 &mdash; P3 PRODUCTION sub-chat B COMPLETE (15/28 produced &mdash; P3 PRODUCTION now complete).** Read `session.md` + this progress doc + `lesson_12_image_requirements.md` (#10 spec + Prompt 10) + `IMAGE_STYLE_GUIDE.md` v6 (via the carried rules). Confirmed the #10 capture spec with Ray (object = capped cylinder over character-form, no `c10` baking) before producing. Verified clean base (0 objects, no `_WORK`, no hidden, EEVEE/512&times;512/AgX/world 0.88, 0 orphans &mdash; no STOP condition). Built `_L12P3_WORK`: a 32-segment capped cylinder (`seam_cyl`, r1 / depth2, 64 v / 96 e / 34 f). Marked **65 seams** = top rim loop (32) + bottom rim loop (32) + 1 vertical side edge; the vertical was first marked dead-back (+Y) but read occluded in SOLID, so re-marked to a **front-visible edge** (&minus;Y) where it reads clearly. Staged Edit Mode + SOLID + `obj.show_wire = True` (red seams read in SOLID) + clean canvas (toolbar/N-panel/gizmos/floor/axes/cursor off) + deselected all (no selection-orange competing) + 3/4 PERSP orbit (66&deg;/40&deg;, dist 5.4). Full-window `screen.screenshot` &rarr; Windows tempdir &rarr; Pillow-relay to WSL for inspection (UNC direct-write truncates to 0). Two orbit trials before lock: 62&deg;/40&deg; (vertical seam too subtle), 72&deg;/28&deg; (top cap flattened + vertical occluded), then 66&deg;/40&deg; with the vertical moved front = keeper (both rims + front vertical all vivid red). Crop measured from the **red-seam pixel bbox** (x 1045&ndash;1322, y 372&ndash;708 = object bbox; grey/dark masks bled into chrome/viewport-bg so red was the reliable signal) + 30px margin &rarr; **`lesson_12_10_seam_marking_example.png` 337&times;396, 27,206 B**, Pillow-written to `images/`, `get_file_info`-verified non-zero. Standard capture (seam red is Blender's theme; no LD17 recipe forced). No baked labels (`c10` rides at INTEGRATION). Deleted 3 temps (1 Windows full-window shot + 2 WSL QA relays: `_tmp_l12_p3b_fullwin.png`, `_tmp_l12_p3b_crop.png`). Scene RESTORE: exited Edit Mode, reset the staged VIEW_3D overlay/region state, removed `seam_cyl` + `_L12P3_WORK`, orphan-purge &rarr; re-verified clean base (0 objects, no `_WORK`, no hidden, 0 orphans, EEVEE/512&times;512/AgX/world 0.88). HTML untouched 236,933 B (PRODUCTION only). **P3 PRODUCTION is now complete (all 5: #10/#11/#12/#13/#14).** `session.md` repointed to **L12 P3 INTEGRATION (inline)**. **15/28 produced / 10/28 integrated &middot; HTML 236,933 B.**

- **2026-06-05 &mdash; P3 PRODUCTION sub-chat A COMPLETE (14/28 produced).** Read `session.md` + this progress doc + `lesson_12_image_requirements.md` (the #11&ndash;14 specs + AI prompts) + `IMAGE_STYLE_GUIDE.md` v6. No BlenderMCP this chat (the 4 are pure-geometry inline SVGs; no scene state to verify or RESTORE &mdash; that is sub-chat B's #10). Built all 4 P3 inline SVGs, cairosvg-QA'd each in light AND dark (currentColor legibility confirmed both modes), then wrote to `images/` (standalone files keep `color="#222"` fallback; stripped only at inline) and `get_file_info`-verified non-zero. **#11 `sp` `lesson_12_11_seam_placement_strategy.svg`** (6,027 B, 800&times;680 dense): front-view humanoid in cyan, GOOD seams green-dashed on hidden areas (back/spine, under chin, underarms, inner legs) with 4 right-side pills, BAD seams red-solid on visible areas (face, chest, visible limbs) with 3 left-side pills, two-key legend (GOOD &#183; Hidden / BAD &#183; Visible), footer tip. Built as the sub-chat pilot first, QA-locked, then the three 2-panel diagrams batched. **#12 `cu` `lesson_12_12_cube_seam_pattern.svg`** (3,636 B, 800&times;560): L panel perspective cube (cyan edges) + 7 red seam edges tracing the unfold; R panel orange cross of 6 connected squares labeled Top/Front/Bottom/Back/Left/Right; `#FF6B00` 2px divider. **#13 `cy` `lesson_12_13_cylinder_seam_pattern.svg`** (3,154 B, 800&times;560): L cyan cylinder + dashed red vertical back seam + red cap rings; R orange barrel rectangle + 2 cap circles (Top/Bottom). **#14 `ss` `lesson_12_14_sphere_seam_pattern.svg`** (3,324 B, 800&times;560): L cyan wire sphere + red longitude orange-peel seams; R orange peel segments full-width at equator pinching to points at both poles, red `pinch` markers up/down, `equator: full width` label &mdash; the pole-pinch teaching point lands. All 4 use the v6 header skeleton, brand palette (cyan `#00bcd4` mesh edges, red `#ff0000` seams matching the requirements-doc seam color, `#FF6B00` UV-layout lines + divider + accents, `#4ADE80`/`#E63946` GOOD/BAD on #11), Variant-B footer tip, `&#183;` entities, no em-dashes, full role/aria/title/desc + per-file id-prefix on every id. Labels live inside each SVG; no `c{N}` overlay for any (locked: inline SVGs carry no overlay). #14's `ss` prefix (not `sphere`) confirmed in use. No HTML edits (INTEGRATION rides inline later). `session.md` repointed to **L12 P3 PRODUCTION sub-chat B (#10 capture)**. **14/28 produced / 10/28 integrated &middot; HTML 236,933 B.**

- **2026-06-05 &mdash; P2 INTEGRATION COMPLETE (10/28 integrated &mdash; P2 fully complete).** Read `session.md` + this progress doc; refreshed the working copy (`copy_file_user_to_claude`, 223,703 B confirmed pre-edit) and re-verified every anchor GLOBALLY UNIQUE on the fresh copy. Integrated all 5 P2 Section-A figures into `#uv-editor-interface` Section A, each anchored by the unique follower h3/blockquote (never the duplicate section id), one atomic `edit_file` (dry-run &rarr; commit &rarr; `get_file_info`). **#6** after the UV Editor Anatomy card (follower h3 "Navigation in UV Editor"); **#7** after the Selection Modes card (follower h3 "UV Selection Tools"); **#8** after the Displaying Textures card (follower = the unique Checker-Test blockquote), **overlay SKIPPED** (self-labeling numbered checker, img+figcaption only); **#9** after the Section-A Display-Options card &mdash; "UV Editor Display Options" h3 is non-unique (count 2: Section A line ~773 + Section B line ~1773), so anchored via the unique follower h3 "Basic UV Manipulation Tools"; **#26** after the Quick Setup for UV Work card (follower h3 "Common UV Editor Issues"). Overlays built from the carried-forward verified geometry and **cairosvg-composited + viewed before commit** (all coords land correctly on the actual PNG pixels): `c6` 5-callout fs15, `c7` 3-leader fs19 (each dot on the per-panel highlighted blue header toggle), `c9` Area-Stretch legend fs16 (Blue/Cyan low &middot; Green moderate &middot; Yellow high &mdash; honest to the ramp, tops at yellow, NO red), `c26` 3-callout fs18. **c26 substitution (flag for Ray):** the spec's "UV Editing tab" is not present in the #26 crop, so the visible "Edit Mode" dropdown was labeled instead. Locked conventions held: figure col20 / svg col24 / children col28; PNG-overlay wrapper-div pattern (viewBox = PNG pixels, no baked labels); `&#183;`/`&#8211;` entities; no new em-dashes; real alt + figcaption per figure. Post-commit `get_file_info` &rarr; **236,933 B** (+13,230). No BlenderMCP this chat (INTEGRATION only; no scene state to RESTORE). `session.md` repointed to **L12 P3 `#seams-and-unwrapping`**. **10/28 produced / 10/28 integrated &middot; HTML 236,933 B.**

- **2026-06-05 &mdash; P2 PRODUCTION sub-chat B COMPLETE (10/28 produced &mdash; P2 PRODUCTION now complete).** Read `session.md` + this progress doc. Verified clean base (0 objects, no `_WORK`, no hidden, EEVEE/512&times;512/AgX/world 0.88 &mdash; no STOP condition). Built `_L12P2_WORK`: a 2m cube + a UV-sphere, both with a Generated UV-Grid image material. **#7** `lesson_12_07_uv_selection_modes.png`: Smart-UV'd the cube, opened the UV Editing workspace (IMAGE_EDITOR x=3 w=784, window 1920&times;1046 scale 1.0), closed the N-panel for a clean canvas, framed UVs, and captured the UV editor three times cycling `tool_settings.uv_select_mode` VERTEX/EDGE/FACE (select-all UVs each) &mdash; full-window `screen.screenshot` to Windows tempdir, PIL-cropped the IMAGE_EDITOR region (3,39,784,972), trimmed each panel to the toolbar+UV-canvas width (560px), composited the three side-by-side with `#FF6B00` accent dividers &rarr; **1696&times;972, 58,936 B**. QA-viewed: the header UV-select-mode toggle (the 3-button cluster after the editor dropdown) is highlighted distinctly per panel (1st/2nd/3rd) = the unambiguous c7 anchor; in-canvas selection rendering differs (vertex dots / edge outlines / face center-dots). No baked labels (c7 composite-overlay built at INTEGRATION). **#8** `lesson_12_08_uv_grid_test_texture.png`: **PROD route call = Pillow-checker** (deterministic, exactly tileable, resolution-controlled &mdash; chosen over a Blender UV-Grid screen capture which would be screen-res + non-self-labeling) per D4. Generated bridge-side with PIL: 8&times;8 numbered checker (cells 1&ndash;64, two-tone neutral, per-cell number baked = self-labeling content), `#FF6B00` corner-registration squares + center crosshair for orientation &rarr; **2048&times;2048, 114,808 B**. `c8` stays reserved-default-SKIP. **#9** `lesson_12_09_uv_stretch_overlay.png`: built a deliberately-distorting unwrap (UV-sphere, `sphere_project` &mdash; poles bunch &rarr; strong stretch), enabled the UV editor's Display Stretch (`uv_editor.show_stretch=True`), captured both ANGLE and AREA types and **chose AREA** (fuller, more teaching-legible blue&rarr;cyan&rarr;green&rarr;yellow range; ANGLE was mostly blue/green). Standard capture (stretch colors are Blender's theme; no LD17 recipe forced). Cropped IMAGE_EDITOR region &rarr; **784&times;972, 278,962 B**. Range tops at yellow not red (honest to Blender's area-stretch ramp at this distortion); c9 self-labeling-vs-overlay decision deferred to INTEGRATION against these pixels. All 3 `get_file_info`-confirmed non-zero on disk. Deleted 19 temps (8 WSL `_tmp_l12_p2b_*` QA relays + 11 Windows tempdir shots incl. the v1 capture pass + `_crop`s). Scene RESTORE: exited Edit Mode, reset the shared IMAGE_EDITOR stretch/N-panel state, removed `_L12P2_WORK` + `uv_sel_cube` + `uv_stretch_sphere` + `L12UVGridMat` + `L12UVGrid`, orphan-purge (2 data-blocks) &rarr; re-verified clean base (0 objects, no `_WORK`, no hidden, 0 orphan meshes/mats, EEVEE/512&times;512/AgX/world 0.88). HTML untouched 223,703 B (PRODUCTION only). **P2 PRODUCTION is now complete (all 5: #6/#7/#8/#9/#26).** `session.md` repointed to **L12 P2 INTEGRATION (own chat)**. **10/28 produced / 5/28 integrated &middot; HTML 223,703 B.**

- **2026-06-05 &mdash; P2 PRODUCTION sub-chat A COMPLETE (7/28 produced).** Read `session.md` + this progress doc. Verified clean base (0 objects, no `_WORK`, no hidden, EEVEE/512&times;512/AgX/world 0.88 &mdash; no STOP condition). Built `_L12P2_WORK`: a 2m cube with a Generated UV-Grid image material, Smart-UV-projected to 6 islands (3&times;2). UV Editing workspace already active (IMAGE_EDITOR left x=3 w=784 + VIEW_3D right x=792 w=783 + PROPERTIES/OUTLINER far-right column; window 1920&times;1046, screenshot scale factor 1.0 confirmed via PIL so area units = pixels). Staged: 3D viewport MATERIAL shading + gizmos/clutter-overlays off + 3/4 PERSP orbit (so the cube reads as 3D, three faces visible); UV editor with toolbar (T) + N-panel sidebar + header all opened, UVs framed (`image.view_all fit_view`). Captured full-window to Windows tempdir (`screen.screenshot`, 981,719 B, v2 with panels open), PIL-cropped two deterministic regions (Blender bottom-left origin &rarr; PIL top-left conversion `top = winH-(y+h)`), Pillow-relayed both to WSL `images/` (Pillow `.save` writes real bytes where direct UNC `screen.screenshot` would truncate to 0). **#6 `lesson_12_06_uv_editor_interface_overview.png`** = IMAGE_EDITOR crop (0,35,787,972) &rarr; **787&times;972, 86,424 B**; QA-viewed: header menus (View/Select/Image/UV) + vertical toolbar (select/cursor/move/rotate/scale/annotate) + main canvas with 6 orange islands over the UV-grid + open N-panel (UV Vertex X/Y, Image=UV Grid) all legible &rarr; LOCKED. **#26 `lesson_12_26_uv_workspace_layout.png`** = IMAGE_EDITOR+VIEW_3D crop (0,35,1575,972), Properties/Outliner excluded &rarr; **1575&times;972, 752,875 B**; QA-viewed: UV editor (full chrome, islands) left + 3D viewport (3/4-orbit cube, UV-grid texture, Edit-Mode header) right, both headers visible &rarr; LOCKED. `get_file_info` confirmed both on-disk non-zero. Overlays NOT built here (c6/c26 are multi-callout, coords measured at P2 INTEGRATION against these pixels per v6 LD19). Deleted 4 Windows temps (2 full-window shots + 2 crops). Scene RESTORE: exited Edit Mode, removed `_L12P2_WORK` + `uv_demo_cube`, removed `UVGridDemo` material + `UVGrid` image, orphan-purge (1 data-block) &rarr; re-verified clean base (0 objects, no `_WORK`, no hidden, 0 orphans, EEVEE/512&times;512/AgX/world 0.88). HTML untouched 223,703 B (PRODUCTION only). `session.md` repointed to L12 P2 PRODUCTION sub-chat B (#7, #8, #9). **7/28 produced / 5/28 integrated &middot; HTML 223,703 B.**

- **2026-06-05 &mdash; P1 INTEGRATION COMPLETE (5/28 integrated).** Integrated all 5 P1 figures into `#what-are-uvs` on a freshly-refreshed copy (194,981 B confirmed pre-edit). Re-verified the four P1 anchor strings GLOBALLY UNIQUE (count=1 each) plus the 5 actual insertion anchors (the card/mermaid `</div>` spans) unique before commit. **#1 `cs` / #2 `gd` / #3 `mp`** inlined per locked conventions (root `color="#222"` stripped &mdash; 0 remain; root attrs collapsed to one line; re-indent figure col20 / svg col24 / children col28+; `&#183;` entity preserved in `gd` heading; figcaptions added; no new em-dashes). **#4 `c4`**: the produced PNG (748&times;890) has **6 islands in a 3&times;2 grid, not the 4&ndash;5 the plan estimated** &mdash; measured island bands by Pillow, labeled all six UV Island 1&ndash;6 (fs 16 = small-frame floor, raw formula gave 9), cairosvg-composited + viewed (all six pills centered, legible) before commit. **#5 `c5`**: PNG 1526&times;944, divider at x&asymp;759; placed GOOD UNWRAP (green &#10003;) over the left/even-checker cylinder and BAD UNWRAP (red &#10007;) over the right/distorted cylinder in the clean upper area (fs 17), cairosvg-composited + viewed before commit; &#10003;/&#10007; written as numeric entities. Bottom-up not required &mdash; all 5 committed in one atomic `edit_file` (anchors non-overlapping, each unique). Post-commit: `get_file_info` &rarr; **223,703 B** (+28,722); refreshed copy verified 5 `<figure>` / 5 `<figcaption>` / all 5 SVG ids present / 0 stray root `color="#222"` / 11 section ids (duplicate `uv-editor-interface` preserved, out of scope) / exactly 1 em/en-dash in the region (the pre-existing "Distortion Problem" em-dash, untouched). `c3` remains dropped; the 7 inline SVGs carry no `c{N}`. No BlenderMCP this chat (INTEGRATION only; no scene state to RESTORE). `session.md` repointed to L12 Phase 2 PRODUCTION sub-chat A. **5/28 produced / 5/28 integrated &middot; HTML 223,703 B.**

- **2026-06-05 &mdash; P1 PRODUCTION COMPLETE (5/28 produced).** Two PRODUCTION sub-chats. **Sub-chat A** produced #1 `cs` + #2 `gd` (inline SVG) and **resolved the one open P1 call: #3 flipped PNG&rarr;SVG** (inline SVG `mp`). Rationale: #3 is pure flowchart geometry (vertex&rarr;UV&rarr;texture&rarr;color&rarr;surface) and inlines cleanly like the L11 `le` flowchart; flipping makes the working split **7 SVG / 21 PNG**, matching the requirements-doc prose ("7 vector images") and the SVG-Recommendations list. Consequences: id-prefix registry +`mp` (7 entries); `c3` DROPPED from the `c{N}` namespace (labels live inside the `mp` SVG); naming-convention line for #3 updated `lesson_12_03_uv_mapping_process.png` &rarr; `.svg`. (Sub-chat A closed without writing its doc update; deltas carried into this close-out.) **Sub-chat B** produced #4 `lesson_12_04_uv_islands_example.png` (748&times;890, LOCKED) + #5 `lesson_12_05_good_bad_unwrap_comparison.png` (1526&times;944). This close-out: QA'd #5 (`copy_file_user_to_claude` &rarr; view &mdash; even checker good panel left, stretched/smeared checker bad panel right, divider clean &rarr; **#5 LOCKED**); deleted the two #5 temps (`_tmp_l12_05_good_panel.png`, `_tmp_l12_05_bad_panel.png`); scene RESTORE (removed `_L12P1_WORK` + `uw_cyl`, purged 2 orphans, no hidden base objects to un-hide, confirmed EEVEE / 512&times;512 / AgX / world 0.88 &mdash; clean base verified). HTML unchanged 194,981 B (no INTEGRATION yet). `session.md` repointed to L12 Phase 2 PRODUCTION sub-chat A. **5/28 produced / 0/28 integrated &middot; HTML 194,981 B.**

- **2026-06-05 &mdash; PLANNING COMPLETE.** Read `session.md` + `lesson_11_integration_progress.md` (the structure template; L11 closed at 30/30) + `lesson_12_image_requirements.md` (28-figure spec + AI prompts + per-section placement map) + `IMAGE_STYLE_GUIDE.md` v6 + `lesson_06_completion_handoff.md` (locked rules), then `copy_file_user_to_claude` on the live `lesson_12_uv_unwrapping_basics.html` (194,981 B) and grep-verified every `<section id>` anchor. **Confirmed the section structure does NOT match the requirements-doc id list cleanly** and locked the reconciliation: (1) **`uv-editor-interface` is a DUPLICATE `<section id>`** &mdash; line 254 "The UV Editor Interface" (the requirements-doc P2 home; all P2 anchors taken from this Section A by unique h3/card text) and line 1311 "Working in the UV Editor" (Section B, no figures, no phase); (2) **the TOC links to four ids that do not exist as anchors** (`#minimizing-distortion`, `#uv-layout-packing`, `#practical-unwrapping`, `#project`) &mdash; the real figure sections are `#manipulating-uv-islands` / `#optimizing-uvs` / `#hands-on-project`; both the duplicate id and the broken TOC are pre-existing lesson-body defects flagged for a separate QA fix and OUT OF SCOPE for image work (D3). Built the requirements-doc-id &rarr; real-id mapping table (6 of 7 match; only `uv-editor-interface` needs the Section-A disambiguation) and confirmed each placement-map anchor's global-uniqueness status (several known non-unique: "UV Islands" 31&times;, "Pack Islands" 19&times;, "texture density" 19&times;, "Project from View" 7&times; etc &mdash; these need multi-line anchors at INTEGRATION per the L11 #7/#10 precedent). Verified Filesystem read/write tools + `get_file_info` loaded via `tool_search` (silent). No BlenderMCP (planning only). Locked: the **6 SVG / 22 PNG** working split (File Naming Convention authoritative over the prose "7"; #3 PNG baseline with the one open #3&rarr;SVG flip call at P1 PRODUCTION; per the L11-D3 convention-is-authoritative precedent), the **7-phase** section-aligned plan (P1/P2/P3/P5 PRODUCTION sub-chat splits; **P2 INTEGRATION own chat** by the multi-callout + duplicate-id-disambiguation load; P1/P3/P5 INTEGRATION inline-borderline; P4/P6/P7 inline), **D1** (no pre-committed reuse; produce-once/integrate-once), **D2** (File Naming Convention format baseline), **D3** (duplicate-id + TOC defects out of scope), **D4** (#8 sourcing + self-labeling SKIP a PRODUCTION call), the 6-entry inline-SVG id-prefix registry (`cs`/`gd`/`sp`/`cu`/`cy`/`ss`; `+mp` if #3 flips), and the `c{N}` overlay namespace registry (`c3`&ndash;`c28` reserved; `c8`/`c9`/`c19`/`c22`/`c23` reserved-default-SKIP self-labeling candidates). `session.md` repointed to L12 Phase 1 PRODUCTION. No production done in this chat. **0/28 produced / 0/28 integrated &middot; HTML 194,981 B.**

---

## Locked rules carry forward

All UNCHANGED from L06&ndash;L11 (full list in `lesson_06_completion_handoff.md` + the L07/L08/L09/L10/L11 carry-forward blocks + `IMAGE_STYLE_GUIDE.md` v6 Locked decisions 1&ndash;21). Highlights that govern L12 specifically:

- WSL `\\wsl$\Ubuntu\` paths only (never `\\wsl.localhost\`).
- `Filesystem:edit_file` dryRun &rarr; commit &rarr; `Filesystem:get_file_info` verify per insertion; `Filesystem:write_file` for new files / full-doc overwrites (never `create_file`); `copy_file_user_to_claude` to refresh the working HTML copy before grep/sed + **verify anchor GLOBAL uniqueness first** (extra important this lesson given the many non-unique placement-map h3 strings and the duplicate `uv-editor-interface` id).
- **Anchor by unique text, never by section id** for the P2 figures (the duplicate `uv-editor-interface` id makes the id ambiguous); extend anchors to 3&ndash;8 lines until the count hits 1 (the L11 #7/#10 multi-line-anchor precedent).
- No new em-dashes (middle-dot `&#183;` entities; pre-existing em/en-dashes preserved verbatim through exact anchors; content entities `&#215;`/`&#8211;`/`&#8594;`/`&#183;` are fine). Scan the live body for pre-existing em-dashes at each INTEGRATION and preserve every one through exact anchors.
- Brand palette governs all overlays/SVGs; inline-SVG conventions (strip root `color="#222"` on inlining, re-indent figure col20 / svg col24 / children col28 / deeper col32, collapse multi-line attrs, prune decorative `=====` comments, keep structural comments, preserve role/aria/title/desc/id); figure indentation depth from parent container; PNG overlay = wrapper-div + overlay-SVG, viewBox = PNG pixels, NO baked labels.
- **UI-capture craft (v6 LD14&ndash;16, LD20) is the dominant craft this lesson** (inverse of L11): bottom-of-viewport modal status bar authoritative for key bindings; full-window local-tempdir `screen.screenshot` carries panel/popover overlays then deterministic area crop (UNC-write truncates to 0 bytes &mdash; capture to Windows tempdir then Pillow&rarr;WSL); Win+Shift+S overlay-freeze for transient popovers/pie menus saved direct to WSL `images/`; `plane.show_wire = True` before any cut/seam capture so edges read in SOLID; clean-canvas viewport staging (hide toolbar/N-panel/nav-gizmo) for clean UV-editor captures.
- **Color-dependent render recipe (v6 LD17)** applies only if a PRODUCTION review shows a teaching color washing (stretch-overlay #9/#23, colored seams) &mdash; not pre-forced; default is standard capture since the UV/seam/stretch colors come from Blender's theme, not emissive markers.
- Pixel-verify every PNG overlay before commit (v6 LD19): Pillow scan + overlay-over-PNG composite at exact viewBox coords; cairosvg 1:1 Claude-side, PIL substitute bridge-side.
- BlenderMCP lowercase `blender:execute_blender_code`; clean-scene-manually via `bpy.data.objects.remove`; BLENDER_EEVEE (EEVEE_NEXT&rarr;EEVEE fallback); NEVER `read_factory_settings`; inline all render helpers per call (namespaces do not persist); Cycles for off-screen reflection/refraction if any.
- **Scene state between PRODUCTION chats = always clean base.** Every PRODUCTION chat does RESTORE at close (removes its `_L12Px_WORK` collection, purges orphans, un-hides base objects, AgX/EEVEE/512&times;512/world 0.88). If a chat finds a leftover `_WORK` collection or hidden base objects, the prior RESTORE failed &mdash; STOP and report.
- Pre-plan sub-chat splits for &ge;5-figure phases (P1, P2, P3, P5) + INTEGRATION-own-chat for &ge;6 (P2 promoted by load); plan-lock before build (propose spec + anchors in writing first).
- Warn Ray to start a new chat before context tightens rather than letting automatic compaction happen.

---

## Deferred follow-ups (lesson-body defects surfaced during L12 planning &mdash; NOT image work)

- **Duplicate `id="uv-editor-interface"`** on two `<section>` tags (line 254 "The UV Editor Interface" + line 1311 "Working in the UV Editor"). Invalid HTML (ids must be unique); the second should be renamed (e.g. `working-in-uv-editor`) and the TOC updated. Image work anchors by text and does NOT touch this; flagged for a lesson-body QA fix.
- **Broken TOC links:** the in-page TOC links to `#minimizing-distortion`, `#uv-layout-packing`, `#practical-unwrapping`, `#project` &mdash; none exist as anchors. Real sections are `#manipulating-uv-islands`, `#optimizing-uvs`, `#hands-on-project`. TOC needs re-pointing to the real section ids. Flagged for the same lesson-body QA fix.
- **Browser-QA pass for Lesson 12** &mdash; **DONE / CLEARED 2026-06-05 (COMPLETE / CLEAN).** All 28 figures visual-reviewed dark + light at 1920/1024/414 via a from-scratch real-Chromium harness; verdict PASS, no figure defects. The lazy-load "naturalWidth=0" measurement scare was resolved as `loading="lazy"` (scroll-before-measure fixes it), not a load defect. See the Status log entry for the full verdict. The two lesson-body defects below (duplicate id + broken TOC) remain OPEN and out of scope for image work.
