# Lesson 13 Image Work &middot; Integration Progress

**Lesson:** `lesson_13_texture_painting_basics.html` (Module 3: Materials &amp; Texturing)
**Project root:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\`
**Image output:** `images/`
**Status:** **L13 P7 PRODUCTION + INTEGRATION COMPLETE &mdash; 21/30 produced / 22/30 integrated [21 distinct + 1 reuse-by-ref] &middot; HTML 291,630 B.** All four `#texture-slots` figures done + the #20 reuse-by-ref second placement: #11 `multiple_texture_slots` inline SVG (id-prefix `ts`, shader node graph 3 Image Texture &rarr; Principled BSDF &rarr; Material Output), #27 `roughness_map_example` PNG (overlay `c27` Roughness Map / Rendered + Dark=Shiny / Light=Rough), #28 `metallic_map_example` PNG (overlay `c28` Metallic Map / Rendered + White=Metal 1.0 / Black=Non-Metal 0.0), #12 `3d_2d_sync_view` PNG (overlay `c12` Image Editor / 3D Viewport + sync arrow + Same stroke) &mdash; the three PNGs all 1528&times;760, overlays viewBox 0 0 1528 760 / fs 18, pixel-verified via cairosvg-composite before commit (LD19). #20 reused-by-reference (D1: same `images/lesson_13_20_pbr_texture_types.png`, same `c20`, id NOT renamed). #11 inlined after `<h3>What Are Texture Slots?</h3>` (root `color="#222"` stripped, re-indented col24/28/32); #20-ref after the Common-texture-slots card before the mermaid; #27/#28/#12 after their respective h3s. Each `edit_file` dry-run &rarr; commit &rarr; `get_file_info`. Post-commit integrity clean (em-dash 25, 22 figure / 13 img / 22 svg open=close, `ts`/`c27`/`c28`/`c12` ids present, second `c20` present id-not-renamed, zero `color="#222"` leaked, 13 section ids no dups, all 5 anchors count=1). P8 PRODUCTION next. Plan locked: 8 SVG / 22 PNG, 10 figure-bearing phases section-aligned to the live HTML, all single-chat with inline INTEGRATION (no phase hits the &ge;5 PRODUCTION-sub-chat or &ge;6 INTEGRATION-own-chat threshold). The live HTML is **clean** &mdash; no duplicate ids, no broken TOC links (the inverse of L12); every spec placement-section id matches a real `<section id>` 1:1.
**Planning date:** 2026-06-06
**Style guide:** `images\IMAGE_STYLE_GUIDE.md` v6 (locked; all conventions LD1&ndash;21 live there, not restated here).

---

## Scope

30 figures total, sourced from `lesson_13_image_requirements.md` (v1.0). Split: **8 SVG / 22 PNG**, following the requirements-doc File Naming Convention + the SVG Diagram Recommendations list (both name the same 8: #1, #3, #4, #10, #11, #16, #17, #23). The 8 SVGs are the pure diagram / chart / infographic / node-graph figures; the 22 PNGs are Texture-Paint UI captures, brush/stroke composites, before/after comparisons, PBR-map grids, and the crate-project renders.

Lesson 13 is **UI-capture-heavy**, like L12: the Texture Paint workspace (Image Editor + 3D Viewport split), the color picker, the texture-slot dropdown, the brush cursor, stencil/mask overlays, and symmetry painting all need genuine Texture-Paint-mode chrome. The v6 **LD14&ndash;16 / LD20 UI-capture craft will dominate** (modal status-bar-as-truth, full-window local-tempdir `screen.screenshot` carries panel/popover overlays then deterministic area crop, Win+Shift+S overlay-freeze for transient popovers and brush-cursor states, clean-canvas viewport staging). The 8 SVGs reuse the L11/L12 inline-SVG PRODUCTION craft. Several PNGs are infographic / composite figures with no single natural viewport (#7, #9, #13, #18, #22, #25, the before/after pairs) routed through the Pillow-vignette overlay host (v6 LD21) or Canva/Adobe raster &mdash; route is a PRODUCTION-time call per figure.

No PRODUCTION sub-chat split and no INTEGRATION-own-chat are forced this lesson: the largest figure-bearing phase carries 4 figures (below the &ge;5 / &ge;6 thresholds). Any phase still breaks INTEGRATION (or a PRODUCTION sub-chat) out to its own chat if context tightens mid-phase (the standing L10 P4 / L11 P7 borderline call).

### CRITICAL &mdash; live-HTML vs requirements-doc anchor reconciliation (read before any PRODUCTION/INTEGRATION)

Confirmed against the copied live file (187,917 B, 2026-06-06). **Unlike L12, the L13 live HTML is clean:**

- **NO duplicate section ids.** All 13 `<section id>` values are globally unique (verified: `grep id=` uniq -d returns empty).
- **NO broken TOC links.** All 13 in-page TOC `href="#..."` targets resolve to a real `<section id>` 1:1 (13 hrefs = 13 ids, exact match). No re-point needed.
- **Spec placement-section ids match the live HTML exactly.** The requirements-doc "Image Placement Suggestions" use the same section-id names that appear on the live `<section>` tags. The mapping is identity for all 13; no Section-A disambiguation, no stale-id remap.

- **Requirements-doc section id &rarr; REAL live-HTML section id mapping** (identity map; figures listed per the placement suggestions):

  | Requirements-doc section id | Real live-HTML section id | Line | Figures (primary) | Phase |
  |---|---|---|---|---|
  | `what-is-texture-painting` | `what-is-texture-painting` (match) | 99 | #1, #20 | P1 |
  | `why-paint-textures` | `why-paint-textures` (match) | 287 | &mdash; (none) | &mdash; |
  | `texture-painting-workspace` | `texture-painting-workspace` (match) | 428 | #2, #6 | P2 |
  | `creating-textures` | `creating-textures` (match) | 665 | #10, #30 | P3 |
  | `brush-basics` | `brush-basics` (match) | 967 | #4, #3, #5, #29 | P4 |
  | `painting-techniques` | `painting-techniques` (match) | 1244 | #7, #9, #8, #21 | P5 |
  | `color-management` | `color-management` (match) | 1533 | #16, #17, #23 | P6 |
  | `texture-slots` | `texture-slots` (match) | 1771 | #11, #27, #28, #12 (+#20 reuse-by-ref) | P7 |
  | `advanced-painting` | `advanced-painting` (match) | 2025 | #15, #26, #22 | P8 |
  | `hands-on-project` | `hands-on-project` (match) | 2297 | #24, #25, #13, #14 | P9 |
  | `common-mistakes` | `common-mistakes` (match) | 2687 | #18, #19 | P10 |
  | `summary` | `summary` (match) | 2927 | &mdash; (none) | &mdash; |
  | `whats-next` | `whats-next` (match) | 3114 | &mdash; (none) | &mdash; |

  Sections with NO figures and NO phase: `why-paint-textures` (spec: text strong standalone), `summary`, `whats-next`.
- **Anchor-by-text rule (locked, carried):** every INTEGRATION insert anchors on the unique h3 / card text from the placement map, verified GLOBALLY UNIQUE on a freshly-refreshed copy at INTEGRATION time, NOT on the section id. Planning-time probe (2026-06-06) found nearly all placement-map anchor strings already count=1 on the live file. **One non-unique anchor flagged:** bare `Masking` counts 3&times; (the `#advanced-painting` `<h3>Masking</h3>` at line 2030 + `<h3>Cavity Masking</h3>` substring at 2132 + a `<li>Masking to protect areas</li>` in the summary at 3037). Anchor #15 by the exact `<h3>Masking</h3>` tag (count=1) or extend to a multi-line span (the L11 #7/#10 / L12 P2 precedent). All other probed anchors unique on first check: "Understanding the relationship", "Types of Textures You Can Paint", "Understanding the Texture Paint Workspace Layout", "Tool Shelf and Settings", "Texture Resolution Considerations", "Saving Your Texture", "Essential Brush Settings", "Brush Falloff and Shape", "Color Selection" (note: appears once as a heading but #5 AND #29 both place here &mdash; sequence the two inserts at INTEGRATION), "Basic Stroke Techniques", "Layering and Building Color", "Color Variation for Realism", "Working with Symmetry", "Color Relationships", "three properties of color", "Color Tips for Different Materials", "What Are Texture Slots", "Painting Roughness Maps", "Painting Metallic Maps", "Coordinating Multiple Maps", "Stencil Painting", "Brush Texture and Spacing", "Step 10", "Poor UVs".
- **Doc-wide em-dash baseline = 25 literal `—`** (U+2014), to be preserved verbatim through exact anchors; **no new em-dashes** (v6 LD9). Figcaptions/alt use `&#183;` + sentence breaks. The spec's own prose uses em-dashes and `❌`/`✅`/`⚠️` glyphs &mdash; those are spec guidance, NOT to be copied literally into figcaptions; overlay semantics use the locked positive-green `#4ADE80` / warning-red `#E63946` colorway (LD8) and numeric entities.
- **Existing media in the live file:** 0 `<figure>`, 0 `<img>`, 0 inline `<svg>`, **6 mermaid blocks** (kept as-is per spec; the spec explicitly says keep the existing Paint-to-Texture-Pipeline mermaid). All 30 figures are net-new inserts.

### Planning decisions (LOCKED 2026-06-06)

- **D1 &mdash; one cross-phase reuse-by-reference: #20 (PBR Texture Types).** The spec places #20 in BOTH `what-is-texture-painting` ("Types of Textures You Can Paint" card) and `texture-slots` ("Common texture slots in PBR workflow", explicitly "again or cross-reference"). **Locked: produce #20 once in P1, reuse-by-reference in P7** (second `<figure>` referencing the same `images/lesson_13_20_pbr_texture_types.png`, id NOT renamed &mdash; the L11 D1/D2 pattern). P1 is #20's PRODUCTION home; P7 carries the second placement only. All other 29 figures are produce-once / integrate-once.
- **D2 &mdash; format baseline = the requirements-doc File Naming Convention + SVG Recommendations.** Both assign `.svg` to the same 8 (#1, #3, #4, #10, #11, #16, #17, #23) and `.png` to 22. The split table below follows that 8/22 assignment as the locked baseline. Any in-flight type-flip is a PRODUCTION-time decision logged in the Status log (the L12 #3 PNG&rarr;SVG precedent).
- **D3 &mdash; the live HTML has no lesson-body defects to flag** (no duplicate id, no broken TOC). Nothing here parallels the L12 duplicate-`uv-editor-interface` / stale-TOC out-of-scope flags. Figure work anchors by unique text as always.
- **D4 &mdash; Texture-Paint-mode scene staging is a PRODUCTION-time call per figure.** Many PNGs need genuine Texture Paint workspace chrome (split Image Editor + 3D Viewport, brush cursor, texture-slot dropdown, mask/stencil overlays, symmetry). Route (live BlenderMCP capture vs Pillow-composite infographic vs Canva/Adobe raster) decided at each phase's PRODUCTION per v6 LD20/LD21. The brush-cursor and transient-popover states (F-resize, color picker, eyedropper S, slot dropdown expanded, stencil overlay) are the Win+Shift+S overlay-freeze candidates (LD16/LD20).
- **D5 &mdash; the spec's color palette differs from the brand palette; brand palette governs.** The requirements doc specifies `#ff8c00` orange, `#4CAF50` green, `#f44336` red, `#667eea` purple, `#2b2b2b` bg for AI-generated art. For inline SVGs and `c{N}` overlays, the LOCKED brand palette governs instead (LD6): `#FF6B00` brand orange, `#4ADE80` positive-green, `#E63946` warning-red, axis colors, `currentColor` text. The spec's hexes are a sourcing hint for any Canva/Adobe raster only, never for inline SVG or overlay text.

---

## SVG-vs-PNG split (all 30)

Format column follows the requirements-doc File Naming Convention + SVG Recommendations (D2). "Phase" is the figure's PRODUCTION home (primary section).

| # | Figure (slug) | Format | Phase | Notes |
|---|---------------|--------|-------|-------|
| 1 | texture_painting_pipeline | **SVG** | P1 | id-prefix `pp` &middot; horizontal 5+ stage flowchart (paint 3D &rarr; UV map &rarr; project to 2D &rarr; texture updates &rarr; material result); inlines like the L11 `le` / L12 `mp` flowcharts; labels inside the SVG, no `c{N}` |
| 2 | workspace_layout_annotated | PNG | P2 | annotated Texture Paint workspace (Image Editor L + 3D Viewport R + header + slot dropdown); full-window capture + multi-callout overlay `c2` (the heaviest UI annotation this lesson, like L12 #6/#26) |
| 3 | brush_falloff_comparison | **SVG** | P4 | id-prefix `bf` &middot; 2&times;2 falloff curves (Smooth/Sharp/Root/Constant) + gradient-circle examples; pure curve+circle geometry, SVG |
| 4 | brush_resize_interactive | **SVG** | P4 | id-prefix `br` &middot; F-key radial resize diagram (cursor circle + larger/smaller arrows + size progression); geometric, SVG |
| 5 | color_picker_annotated | PNG | P4 | annotated Blender color picker (wheel/value slider/RGB/hex/swatches); capture + multi-callout overlay `c5` |
| 6 | texture_slot_dropdown | PNG | P2 | viewport-header slot dropdown highlighted; capture (collapsed+expanded) + overlay `c6` (single highlight callout, decorative-box variant candidate) |
| 7 | stroke_techniques_guide | PNG | P5 | 6-panel stroke techniques (continuous/stipple/straight/hatch/cross-hatch/layered); composite, Pillow or Canva; overlay `c7` panel labels (or self-labeling &rarr; SKIP, decide at INTEGRATION) |
| 8 | color_variation_example | PNG | P5 | before/after flat-vs-varied color; 2-panel composite; overlay `c8` Without Variation / With Variation (warning-red / positive-green) |
| 9 | layer_workflow_stages | PNG | P5 | 3-stage base&rarr;mid&rarr;detail progression; composite; overlay `c9` Stage 1/2/3 |
| 10 | resolution_comparison_chart | **SVG** | P3 | id-prefix `rc` &middot; text-heavy resolution table (512&ndash;8192 + file sizes + use cases + 2K star); infographic, SVG |
| 11 | multiple_texture_slots | **SVG** | P7 | id-prefix `ts` &middot; shader-node diagram (3 Image Texture nodes &rarr; Principled BSDF &rarr; Material Output); node graph, SVG |
| 12 | 3d_2d_sync_view | PNG | P7 | split 3D viewport + Image Editor showing same stroke; composite; overlay `c12` 3D Viewport / Image Editor + sync arrow |
| 13 | wood_grain_technique | PNG | P9 | 3-step wood-grain (base/grain/knots); composite; overlay `c13` Step 1/2/3 |
| 14 | weathering_wear_guide | PNG | P9 | annotated crate wear-areas (corners/edges/lock/bottom/top); render + multi-callout overlay `c14` |
| 15 | mask_tool_visualization | PNG | P8 | before/after masked-vs-paintable; composite; overlay `c15` MASKED / PAINTABLE &mdash; anchor by exact `<h3>Masking</h3>` (bare "Masking" non-unique, count 3) |
| 16 | color_wheel_relationships | **SVG** | P6 | id-prefix `cw` &middot; 12-section color wheel + analogous arc + complementary line; circular geometry, SVG |
| 17 | hsv_properties_diagram | **SVG** | P6 | id-prefix `hv` &middot; 3-row Hue/Saturation/Value swatch progressions; swatch+arrow infographic, SVG |
| 18 | common_mistakes_gallery | PNG | P10 | 6-pair wrong-vs-right grid; composite; overlay `c18` per-pair wrong/right as positive-green/warning-red badges (NO literal emoji in overlay) |
| 19 | uv_quality_check | PNG | P10 | before/after poor-vs-good UVs with painted texture + UV inset; composite; overlay `c19` Stretched & Distorted [warning-red] / Clean & Even [positive-green] |
| 20 | pbr_texture_types | PNG | P1 | 5-map reference grid (Base Color/Roughness/Metallic/Normal/AO on same crate); composite or multi-render; overlay `c20` 5 map-type labels &middot; **PRODUCE in P1; reuse-by-ref in P7 (D1)** |
| 21 | symmetry_painting_demo | PNG | P5 | symmetry painting (one side painted, mirrored other side, center axis); capture; overlay `c21` paint-one-side / mirrors arrows + axis |
| 22 | brush_texture_examples | PNG | P8 | 4&times;2 brush-texture + stroke-result grid (noise/clouds/scratches/custom); composite; overlay `c22` 4 texture labels (or self-labeling &rarr; SKIP) |
| 23 | material_color_chart | **SVG** | P6 | id-prefix `mc` &middot; Wood/Metal/Stone/Fabric palettes + hex + notes; color-swatch infographic, SVG |
| 24 | crate_final_result | PNG | P9 | beauty-shot painted crate (3/4 angle, 3-point light, hand-painted look NOT photoreal); render; overlay default-SKIP (self-evident; decide at INTEGRATION) |
| 25 | crate_step_progression | PNG | P9 | 6-stage crate (model/UV/base/grain/weather/final); composite; overlay `c25` Stage 1&ndash;6 |
| 26 | stencil_painting_setup | PNG | P8 | stencil overlay on 3D model (semi-transparent stencil, brush cursor, stencil-mode UI); capture + Win+Shift+S overlay-freeze; overlay `c26` stencil-projects callout |
| 27 | roughness_map_example | PNG | P7 | grayscale roughness map + rendered result; 2-panel composite; overlay `c27` Roughness Map / Rendered + Dark=Shiny / Light=Rough |
| 28 | metallic_map_example | PNG | P7 | binary metallic mask + rendered result; 2-panel composite; overlay `c28` Metallic Map / Rendered + White=Metal / Black=Non-Metal |
| 29 | eyedropper_usage | PNG | P4 | S-key eyedropper sampling (reference + cursor + picker update); capture or composite; overlay `c29` Press S to sample / Color updates + flow arrow |
| 30 | save_texture_warning | **SVG**? | P3 | id-prefix `sw` (IF SVG) &middot; "SAVE YOUR TEXTURE!" warning infographic (asterisk-unsaved, Image&rarr;Save path, warning icon). **File-Naming-Convention assigns `.svg`; spec body Type = "Warning Infographic" + "red-orange gradient".** Treat as the v6 LD21 warning-vignette pattern at PRODUCTION: if built as an inline SVG it carries its own text (id-prefix `sw`, no `c30`); if built as a Pillow warning-vignette PNG it reserves bands for overlay `c30`. **Format flip PNG&harr;SVG is a P3 PRODUCTION-time call** (logged in Status log); baseline per File Naming Convention = SVG. |

**Totals (baseline): 8 SVG (#1 `pp`, #3 `bf`, #4 `br`, #10 `rc`, #11 `ts`, #16 `cw`, #17 `hv`, #23 `mc`) / 22 PNG.** #30 is the one open format call (File Naming Convention says `.svg`; if PRODUCTION builds it as a warning-vignette PNG host per LD21, it flips to PNG and gains overlay `c30` &mdash; resolved at P3 PRODUCTION). If #30 stays SVG, its id-prefix is `sw` and it carries no `c{N}`.

No color-dependent renders are force-required by the spec the way L11 #5/#15/#30 / the L09 markers were: the PBR-map figures (#20, #27, #28) show map textures + a rendered result where the teaching colors come from Blender's own shading, not emissive-marker tuning. Treat all captures as **standard captures** unless a PRODUCTION review shows a color washing out, at which point the v6 LD17 Standard-view-transform recipe applies (decide per figure at PRODUCTION). The roughness/metallic rendered halves (#27/#28 right panels) may use Cycles for clean reflections (the L11 off-screen-reflection precedent) &mdash; a PRODUCTION call.

---

## Per-lesson id-prefix registry (inline SVG roots)

| Prefix | Figure |
|--------|--------|
| `pp` | #1 texture_painting_pipeline |
| `bf` | #3 brush_falloff_comparison |
| `br` | #4 brush_resize_interactive |
| `rc` | #10 resolution_comparison_chart |
| `ts` | #11 multiple_texture_slots |
| `cw` | #16 color_wheel_relationships |
| `hv` | #17 hsv_properties_diagram |
| `mc` | #23 material_color_chart |
| `sw` | #30 save_texture_warning &mdash; ONLY IF built as SVG (baseline); dropped if #30 flips to a warning-vignette PNG (then overlay `c30` instead) |

8 inline-SVG prefixes at baseline (9th `sw` reserved, contingent on the #30 format call). All 2-char, no collisions with each other or with the `c{N}` overlay namespace. Per-page registry is document-scoped; no cross-lesson collision concern.

## PNG overlay `c{N}-*` namespace registry

Reserved now, built at the figure's INTEGRATION (coordinates measured against the actual cropped/produced PNG pixels per v6 LD19, never from plan estimates). Each overlay: white text on dark pills, `#FF6B00` leaders+dots, `&#183;` separators, composite-overlay fs `round(22 &times; viewBox_w / 1920)` with small-frame fs-floor 16; positive-green `#4ADE80` / warning-red `#E63946` colorway per label semantics (LD8). PNGs whose produced form already carries legible labels end up overlay-free (the L10 `c22` / L11 / L12 `c8` SKIPPED precedent) &mdash; decided at INTEGRATION.

- `c2` (#2 workspace_layout_annotated &middot; Image Editor / 3D Viewport / Texture Slot dropdown / Brush tools / Color picker / Sidebar (N); multi-callout, the largest by label count this lesson)
- `c5` (#5 color_picker_annotated &middot; Color wheel / Value slider / RGB sliders / Hex input / Swatches; multi-callout)
- `c6` (#6 texture_slot_dropdown &middot; single highlight callout on the dropdown; decorative-box variant candidate, decide at INTEGRATION)
- `c7` (#7 stroke_techniques_guide &middot; 6 panel labels; or self-labeling &rarr; SKIP)
- `c8` (#8 color_variation_example &middot; Without Variation [warning] / With Variation [positive]; composite-overlay)
- `c9` (#9 layer_workflow_stages &middot; Stage 1 / Stage 2 / Stage 3)
- `c12` (#12 3d_2d_sync_view &middot; 3D Viewport / Image Editor + sync arrow)
- `c13` (#13 wood_grain_technique &middot; Step 1 Base / Step 2 Grain / Step 3 Knots)
- `c14` (#14 weathering_wear_guide &middot; Corners / Edges / Lock area / Bottom / Top edges; multi-callout)
- `c15` (#15 mask_tool_visualization &middot; MASKED / PAINTABLE)
- `c18` (#18 common_mistakes_gallery &middot; per-pair wrong/right as `#E63946`/`#4ADE80` badges; NO literal emoji in overlay text)
- `c19` (#19 uv_quality_check &middot; Stretched & Distorted [warning] / Clean & Even [positive])
- `c20` (#20 pbr_texture_types &middot; Base Color / Roughness / Metallic / Normal / AO; 5 labels &mdash; built once in P1, the SAME overlay rides the reuse-by-ref placement in P7, D1)
- `c21` (#21 symmetry_painting_demo &middot; Paint one side &rarr; / &larr; Mirrors to other side + axis)
- `c22` (#22 brush_texture_examples &middot; Noise / Clouds / Scratches / Custom; or self-labeling &rarr; SKIP)
- `c25` (#25 crate_step_progression &middot; Stage 1&ndash;6 labels)
- `c26` (#26 stencil_painting_setup &middot; Stencil Image Projects onto Surface callout)
- `c27` (#27 roughness_map_example &middot; Roughness Map / Rendered Result + Dark=Shiny / Light=Rough)
- `c28` (#28 metallic_map_example &middot; Metallic Map / Rendered Result + White=Metal 1.0 / Black=Non-Metal 0.0)
- `c29` (#29 eyedropper_usage &middot; Press S to sample / Color updates + flow arrow)
- `c30` (#30 save_texture_warning &middot; RESERVED, ONLY IF #30 is built as a warning-vignette PNG &mdash; warning-red problem band + positive-green save-action band; DROPPED if #30 stays inline SVG with `sw`)

Reserved-if-needed (decide at INTEGRATION; default SKIP): `c7`, `c22` (self-labeling-panel candidates), `c24` (#24 beauty shot, self-evident). `c30` is contingent on the #30 format call. The inline SVGs (#1 `pp`, #3 `bf`, #4 `br`, #10 `rc`, #11 `ts`, #16 `cw`, #17 `hv`, #23 `mc`, and #30 `sw` if SVG) carry labels inside the SVG via the id-prefix registry and take NO `c{N}` overlay.

---

## Phase plan

Section-aligned to the 10 figure-bearing real HTML sections (`why-paint-textures`, `summary`, `whats-next` carry no figures, no phase). Phase ordering follows HTML section order (P1&rarr;P10), not figure-number order. **No phase reaches the &ge;5 PRODUCTION-sub-chat threshold or the &ge;6 INTEGRATION-own-chat threshold** (largest = 4), so every phase is a single PRODUCTION chat with inline INTEGRATION. Break a phase out to its own INTEGRATION (or split PRODUCTION) chat ONLY if context tightens mid-phase (the standing L10 P4 / L11 P7 borderline call).

| Phase | Section (real HTML id) | Figures (primary) | Count | PRODUCTION | INTEGRATION |
|-------|------------------------|-------------------|-------|------------|-------------|
| P1 | `what-is-texture-painting` | #1 (SVG), #20 (PNG) | 2 | single chat | inline |
| P2 | `texture-painting-workspace` | #2, #6 (PNG) | 2 | single chat | inline (the two heaviest UI-annotation overlays `c2`/`c6`; break out only if tight) |
| P3 | `creating-textures` | #10 (SVG), #30 (SVG?) | 2 | single chat (resolve the #30 SVG-vs-warning-vignette-PNG format call) | inline |
| P4 | `brush-basics` | #4 (SVG), #3 (SVG), #5 (PNG), #29 (PNG) | 4 | single chat | inline (2 SVG cheap + 2 captures) |
| P5 | `painting-techniques` | #7, #9, #8, #21 (PNG) | 4 | single chat | inline (composites + 1 symmetry capture) |
| P6 | `color-management` | #16 (SVG), #17 (SVG), #23 (SVG) | 3 | single chat | inline (all 3 SVG, cheap) |
| P7 | `texture-slots` | #11 (SVG), #27, #28, #12 (PNG) + #20 reuse-by-ref | 4 (+1 ref) | single chat | inline (#20 second placement is a copy-ref, no new produce) |
| P8 | `advanced-painting` | #15, #26, #22 (PNG) | 3 | single chat | inline (#15 anchor by exact `<h3>Masking</h3>`; #26 Win+Shift+S stencil overlay) |
| P9 | `hands-on-project` | #24, #25, #13, #14 (PNG) | 4 | single chat | inline (beauty render + 3 composites/annotated) |
| P10 | `common-mistakes` | #18, #19 (PNG) | 2 | single chat | inline |

**Total figures across phases:** 2 + 2 + 2 + 4 + 4 + 3 + 4 + 3 + 4 + 2 = **30.** &check; (#20 produced in P1, reused-by-ref in P7 = 30 distinct produced, 31 placements.)

Within a phase, the inline SVGs (P1 #1, P3 #10, P4 #3/#4, P6 #16/#17/#23, P7 #11) and self-labeling composites are cheap; the cost centers are the full-window Texture-Paint UI captures with multi-callout overlays (#2, #5, #14), the Win+Shift+S transient states (#6 dropdown, #26 stencil, #29 eyedropper, #21 symmetry, the brush cursor), and the PBR-map / before-after composites routed through Canva/Adobe or the Pillow-vignette host.

### Cross-phase reuse + produce-here / integrate-there

**One reuse-by-ref locked (D1): #20** produced in P1 (`what-is-texture-painting`), reused-by-reference in P7 (`texture-slots`), id NOT renamed, the same `c20` overlay rides both placements (the L11 D1/D2 pattern). All other 29 figures are produce-once / integrate-once. Any additional second-home reuse a PRODUCTION chat discovers is logged in the Status log at that time, ids NOT renamed.

---

## Status log

- **2026-06-06 &mdash; PLANNING COMPLETE (0/30 produced / 0/30 integrated &middot; HTML 187,917 B).** Read `session.md`, `lesson_12_integration_progress.md` (structure template + Locked-rules clone), `images/IMAGE_STYLE_GUIDE.md` v6 (LD1&ndash;21), `lesson_06_completion_handoff.md` (locked inline pattern + carry-forward rules), `lesson_13_image_requirements.md` (the 30-figure v1.0 spec), and the live `lesson_13_texture_painting_basics.html` (187,917 B, copied 2026-06-06). **Live-HTML reconciliation:** confirmed all 13 `<section id>` values via grep &mdash; all globally UNIQUE (no duplicate-id defect, the inverse of L12); all 13 in-page TOC `href="#..."` targets resolve to real ids 1:1 (no broken-TOC defect); every spec placement-section id matches a real section id (identity map, table above). 0 existing `<figure>`/`<img>`/inline `<svg>`; 6 mermaid blocks present, kept as-is. Probed all placement-map anchor strings for global uniqueness: nearly all count=1; flagged **`Masking` non-unique (count 3)** &mdash; anchor #15 by exact `<h3>Masking</h3>` or multi-line. Doc-wide em-dash baseline = **25** literal `—` (preserve verbatim; no new em-dashes). **Locked:** the 8 SVG / 22 PNG split (D2, matching the File Naming Convention + SVG Recommendations); the 10-phase section-aligned plan (P1&rarr;P10, all single-chat / inline INTEGRATION &mdash; no phase &ge;5, so no PRODUCTION-sub-chat split and no INTEGRATION-own-chat forced; break out only if context tightens); the one reuse-by-ref decision **#20** (P1 produce / P7 ref, D1); the inline-SVG id-prefix registry (8 + reserved `sw`); the `c{N}` overlay namespace registry. **Two open PRODUCTION-time calls noted, not pre-decided:** (1) **#30 format** &mdash; File Naming Convention assigns `.svg`, but the figure is a warning infographic that could host as a Pillow warning-vignette PNG per v6 LD21 (then overlay `c30`, prefix `sw` dropped); resolve at P3 PRODUCTION. (2) per-figure capture-vs-composite-vs-raster route (D4) and standard-vs-LD17 render call, decided at each phase's PRODUCTION. No PRODUCTION, no INTEGRATION, no BlenderMCP, no HTML edits this chat (PLANNING only; real HTML untouched at 187,917 B). **L13 PLANNING COMPLETE.** Next step: **L13 Phase 1 PRODUCTION** (`#what-is-texture-painting`: #1 `pp` SVG pipeline flowchart + #20 PBR-map grid PNG).

- **2026-06-06 &mdash; P1 PRODUCTION COMPLETE (2/30 produced / 0/30 integrated &middot; HTML 187,917 B, untouched).** Scene pre-flight: clean base confirmed (0 objects, no `_WORK` collection, no hidden base). **#1 `texture_painting_pipeline`** built as a standalone inline-ready SVG `images/lesson_13_01_texture_painting_pipeline.svg` (5,849 B, id-prefix `pp`, viewBox 0 0 800 360): horizontal 5-stage flowchart Paint stroke on 3D model &rarr; Blender uses UV map &rarr; Projects to 2D texture &rarr; Texture image updates &rarr; Material shows result, plus a dashed live-update loop-back and a left-rail tip. Brand palette per D5 (`#FF6B00` start/accents/arrows, `#4ADE80` result node, `currentColor` boxes/text); NOT the spec's `#667eea`/`#4CAF50`. cairosvg render-check PASS on both light (#222 fallback) and dark (stripped, page-light `currentColor`) backgrounds; labels inside SVG, no `c{N}`. Standalone keeps `color="#222"`; strip on inline. **#20 `pbr_texture_types`** produced as `images/lesson_13_20_pbr_texture_types.png` (536,032 B, 3016&times;600). **Route (D4): BlenderMCP multi-render** &mdash; one beveled wooden crate (54 polys, smart-projected UVs) with 5 procedurally-authored 512px maps (base color / roughness / metallic / normal / AO), rendered 5&times; as unlit emission readouts under **Standard view transform** (faithful grayscale + normal-blue; AgX would wash them), film-transparent, EEVEE; then Pillow-composited 1&times;5 with `#FF6B00` 4px dividers. base color = sRGB, the 4 data maps = Non-Color. **Two PRODUCTION review passes (LD17 distinctness QA):** pass 1 rejected (roughness/AO washing near-white, metallic showed only 1 bolt + invisible silhouette); fixed by moving bolts to a 3&times;3 grid (several land on every visible face), strengthening AO edge-darkening, darkening roughness wood, and lifting the metallic non-metal floor to 0.07 so the cube silhouette reads while staying clearly black. All 5 panels read distinct + correct on final review. **No baked labels** &mdash; `c20` (Base Color / Roughness / Metallic / Normal / AO, 5 labels) rides at INTEGRATION, and the SAME `c20` rides the P7 reuse-by-ref placement (D1). Scene RESTORED to clean AgX base at close (0 objects, view_transform=AgX, film_transparent=False, all `L13_*` materials/images purged); Windows tempdir + WSL `_temp_l13_20_qa.png` removed. **No HTML edits** (INTEGRATION not run this chat &mdash; #1 inline + #20 figure insert both need fresh anchor-uniqueness verification on a refreshed copy; deferred to P1 INTEGRATION). Next step: **L13 P1 INTEGRATION** (insert #1 after the `Understanding the relationship` paragraph; insert #20 in the `Types of Textures You Can Paint` card; build `c20`), then **P2 PRODUCTION** (`texture-painting-workspace`: #2, #6).

- **2026-06-06 &mdash; P1 INTEGRATION COMPLETE (2/30 produced / 2/30 integrated &middot; HTML 187,917 &rarr; 198,910 B).** Refreshed the working copy (187,917 B baseline confirmed); pre-flight on the fresh copy: 25 em-dashes, 0 existing `<figure>`/`<img>`/inline `<svg>`, both P1 anchor strings global-unique (count=1). **#1 inlined** as a `<figure>` directly after the `<p>Understanding the relationship...` paragraph in `#what-is-texture-painting` (default-depth: figure col 20, svg root col 24, children col 28+). On inline: stripped root `color="#222"`, re-indented, collapsed the `<marker>` multi-line attrs, kept structural stage comments, preserved `role`/`aria-labelledby`/`<title>`/`<desc>`/all `pp-*` ids; no `c{N}`. Commit clean; +7,977 B (195,894). **#20 inserted** as the last child of the `Types of Textures You Can Paint` card (deeper-indent: figure col 24, wrapper-div + img + overlay-svg col 28+, card opens at col 20). **Overlay `c20` built** = 5 composite-overlay labels (Base Color / Roughness / Metallic / Normal / AO), viewBox `0 0 3016 600` matching the PNG pixels 1:1, `preserveAspectRatio="xMidYMid meet"`, `pointer-events: none`, white text on dark pills (fs 35 per the composite formula, neutral map-type labels so no positive/warning colorway), one centered badge near the bottom of each of the 5 baked-divider panels (LD10 composite-overlay variant). **Pixel-verified BEFORE commit (LD19):** Pillow-scanned the actual PNG for the 4 `#FF6B00` dividers (x&asymp;601/1205/1809/2413, 5 equal ~600px panels, centers 300/904/1508/2112/2716), then cairosvg-composited the exact overlay over the PNG (Claude-side, 1:1) and view-verified all 5 badges centered + legible + correctly map-matched. Commit clean; +3,016 B (198,910). **Post-commit integrity on a re-refreshed copy:** em-dash baseline still 25; 2 `<figure>` / 1 `<img>` / 2 inline `<svg>` (figure open=close=2); `pp-*` + all 5 `c20` labels present; no duplicate section ids; both anchors still count=1. **#20's `c20` overlay is reused verbatim at the P7 reuse-by-ref placement (D1) &mdash; not produced/integrated now; id NOT renamed.** No BlenderMCP this chat. Next step: **L13 P2 PRODUCTION** (`texture-painting-workspace`: #2 workspace-layout-annotated + #6 texture-slot-dropdown; the two heaviest UI-annotation overlays `c2`/`c6`).

- **2026-06-06 &mdash; P2 PRODUCTION COMPLETE (4/30 produced / 2/30 integrated &middot; HTML 198,910 B, untouched).** Scene pre-flight: clean base confirmed (0 objects, no `_WORK`, no `L13_*` leftovers, AgX, film opaque, Blender 5.1.1). Both figures are genuine Texture-Paint-mode UI captures (route D4 = live BlenderMCP capture, LD20). **Shared scene build:** beveled wooden crate (54 polys, smart-projected UVs) + a 1024 hand-painted wood base-color image (`L13_CrateColor`, vertical grain streaks + plank seams) in a Principled material; ground plane + sun. Switched the active workspace to the built-in **Texture Paint** workspace (Image Editor L + 3D Viewport R), entered TEXTURE_PAINT mode, MATERIAL shading, paint mode=MATERIAL (so the header slot dropdown appears), T-toolbar + N-sidebar open (N-panel set to the Tool tab so Brush + Color Picker show). **CAPTURE-PATH DISCOVERY (important, carry forward):** `bpy.ops.screen.screenshot` initially returned a FULLY BLACK frame (pixel max 0, zero non-black px, regardless of redraws) &mdash; this is DISTINCT from the documented 0-byte UNC-truncation bug; the file writes at correct 1920&times;1046 dimensions but all-black because the Blender window was not front-buffer drawing (window minimized / not visible / display asleep). **Fix: bring the Blender window to the foreground + keep it visible**, after which `screen.screenshot` captured correctly (redraws went from ~0.001ms no-ops to ~20&ndash;40ms real draws; file 26KB&rarr;1.6MB; non-black px 0&rarr;~1.99M). So the LD20 full-window-tempdir path DOES work this session once the window is visible; the manual Win+Shift+S fallback was NOT needed. **#2 `workspace_layout_annotated`** saved `images/lesson_13_02_workspace_layout_annotated.png` (1,620,351 B, 1920&times;1046). Framed the crate into the open left-center of the 3D viewport by setting `region_3d.view_location` directly (the `view_pan`/`view_selected` ops through the bridge override did not shift the offset reliably; direct `view_location` + `view_distance` assignment is the deterministic lever) so it clears the N-panel (which overlays the right ~420px of the 784px viewport). Cleaned 3 viewport artifacts before final grab: hid the 3D cursor (`overlay.show_cursor=False`), the paint brush cursor ring (`tool_settings.image_paint.show_brush=False`), and the light relationship/helper line (`overlay.show_extras=False`, `show_relationship_lines=False`). Full window kept as-is (Ray's call &mdash; Properties+Outliner column retained for full-workspace context; BlenderMCP sidebar tab is visible but acceptable). All six `c2` targets present + legible: Image Editor (L, 2D wood texture), 3D Viewport (painted crate), Texture Slot dropdown (header `L13_CrateColor`), Brush tools (T-toolbar), Color picker (N-panel), Sidebar (N). NO baked labels &mdash; `c2` (6 callouts) rides at INTEGRATION. **#6 `texture_slot_dropdown`** saved `images/lesson_13_06_texture_slot_dropdown.png` (421,676 B, 1218&times;406, 3:1). **DEVIATION FROM SPEC (Ray-approved, log):** spec wanted the slot dropdown *expanded* (transient popover). A bridge screenshot cannot hold a popover open; Ray chose the scripted route over a Win+Shift+S manual snip, so #6 is the **non-transient Image-Editor-header slot selector** (`[image-icon &#9662;] L13_CrateColor` control) cropped as a 3:1 wide banner (header rows + a strip of the wood canvas for context), 2&times; LANCZOS upscaled from the 1920-wide full frame. To make the figure truthful to its teaching point (&ldquo;switch between Color / Roughness / Metallic slots&rdquo;), added Roughness + Metallic image-texture nodes to the material so the paint slot list genuinely has 3 entries (`L13_CrateColor` / `L13_CrateRough` / `L13_CrateMetal`); active slot stays 0 so the canvas stays on the wood. `c6` (single highlight callout on the `[img &#9662;] L13_CrateColor` control; decorative-box variant candidate) rides at INTEGRATION. **Two notes for INTEGRATION:** (1) #2 at 1.62 MB is above the ~500 KB web target (detail-rich full-UI screenshot; pngquant optional at INTEGRATION if Ray wants it under target); (2) verify both anchors fresh on a refreshed copy &mdash; #2 anchor `<h3>Understanding the Texture Paint Workspace Layout</h3>` (count=1, col 20, default-depth figure col 20) insert AFTER the heading; #6 insert AFTER the `Tool Shelf and Settings` card's closing `</div>` and BEFORE `<h3>Properties Sidebar</h3>` (anchor `Tool Shelf and Settings` count=1, col 20, default-depth figure col 20 &mdash; NOT inside a card). Scene RESTORED to clean base at close (0 objects, AgX, film opaque, all `L13_*` mats/images purged, orphans purged); all Windows-tempdir + WSL `_temp_*` + Claude-side staging temps removed. **No HTML edits this chat (PRODUCTION only; real HTML untouched at 198,910 B).** Next step: **L13 P2 INTEGRATION** (insert #2 + #6, build `c2` 6-callout overlay + `c6` highlight, pixel-verify both against actual PNG pixels per LD19, two `edit_file` commits, post-commit integrity), then **P3 PRODUCTION** (`creating-textures`: #10 SVG + #30, resolving the #30 SVG-vs-warning-vignette-PNG format call).

- **2026-06-06 &mdash; P2 INTEGRATION COMPLETE (4/30 produced / 4/30 integrated &middot; HTML 198,910 &rarr; 206,417 B).** Refreshed the working copy (198,910 B baseline confirmed); pre-flight on the fresh copy: 25 em-dashes, 2 existing `<figure>` / 1 `<img>` / 2 inline `<svg>` (the P1 #1 + #20), all three anchor strings global-unique (`<h3>Understanding the Texture Paint Workspace Layout</h3>` count=1, `Tool Shelf and Settings` count=1, `<h3>Properties Sidebar</h3>` count=1). PNG dimensions re-confirmed on disk: #2 1920&times;1046, #6 1218&times;406 &mdash; both match their overlay viewBoxes 1:1. **#2 `workspace_layout_annotated` inserted** as a default-depth `<figure>` (col 20; wrapper-div + img + overlay-svg col 24, svg children col 28) directly AFTER `<h3>Understanding the Texture Paint Workspace Layout</h3>`, BEFORE the `<p>When you switch to Texture Paint workspace...`. **Overlay `c2` built** = 6 callouts (Image Editor / Texture Slot dropdown / 3D Viewport / Brush tools / Color picker / Sidebar (N)), viewBox `0 0 1920 1046` matching PNG pixels 1:1, `preserveAspectRatio="xMidYMid meet"`, `pointer-events: none`, white text on `#1a1a1a` pills + `#FF6B00` leaders/dots/highlights, fs 22 (neutral UI labels, no positive/warning colorway). **#6 `texture_slot_dropdown` inserted** as a default-depth `<figure>` (col 20) AFTER the `Tool Shelf and Settings` card's closing `</div>` and BEFORE `<h3>Properties Sidebar</h3>` &mdash; NOT inside the card (anchored on the unique `</div>` + blank + `<h3>Properties Sidebar</h3>` sequence, count=1). **Overlay `c6` built** = single highlight callout (Texture Slot selector) boxing the `[img &#9662;] L13_CrateColor` header control, viewBox `0 0 1218 406` 1:1, fs 16 (small-frame). **Both overlays pixel-verified BEFORE commit (LD19):** cairosvg-composited each exact overlay over its actual PNG (Claude-side, 1:1) and view-confirmed &mdash; all 6 `c2` callouts land on their correct UI regions (Image Editor header icon, header slot dropdown, crate, T-toolbar, N-panel color wheel, N-panel right edge) and the `c6` box surrounds the slot selector exactly. Both committed via `edit_file` dry-run &rarr; commit &rarr; `get_file_info` (+5,717 B then +1,790 B). **Post-commit integrity on a re-refreshed copy:** em-dash baseline still 25; 4 `<figure>` (open=close=4) / 3 `<img>` / 4 inline `<svg>` (open=close=4); all 6 `c2-*` groups complete (6&times; each of hl/ld/mk/bg/tx = 30 ids) + 5 `c6-*` ids present; 13 section ids, no duplicates; all three anchors still count=1. **#2 left at 1.62 MB** &mdash; pngquant NOT run (Ray did not request it; optional follow-up if he wants it under the ~500 KB target). No BlenderMCP this chat. Next step: **L13 P3 PRODUCTION** (`creating-textures`: #10 `rc` resolution-comparison SVG + #30 save-texture warning, resolving the #30 SVG-vs-warning-vignette-PNG format call).

- **2026-06-06 &mdash; P3 PRODUCTION + INTEGRATION COMPLETE (6/30 produced / 6/30 integrated &middot; HTML 206,417 &rarr; 219,648 B).** Both figures for `#creating-textures` built as inline SVGs, render-verified on light + dark, written to `images/`, inlined, and committed in one chat. **#30 FORMAT CALL RESOLVED &mdash; stays inline SVG (`sw`), overlay `c30` DROPPED.** Rationale: #30 is pure warning text + iconography with no captured/rendered content to host, so the v6 LD21 Pillow-warning-vignette-PNG route adds nothing; inline SVG matches the File Naming Convention baseline (D2), themes with the page via `currentColor`, scales crisply, and needs no overlay. The `c30` reservation is now permanently dropped for L13; `sw` is the realized 9th inline-SVG prefix. **#10 `resolution_comparison_chart`** built `images/lesson_13_10_resolution_comparison_chart.svg` (id-prefix `rc`, viewBox 0 0 800 560): text-heavy 5-tier table (512 / 1K / 2K / 4K / 8K) with file sizes (~1/3/12/48/192 MB), use cases, ascending detail-bars, and a starred + `2K &#183; RECOMMENDED` highlighted row. Brand palette per D5 (`#FF6B00` accents/star/bars, `currentColor` text/rules); NOT the spec's `#ff8c00`. PRODUCTION QA caught + fixed a detail-bar / FILE-SIZE-column collision (bars capped to clear x=250 on every row). **#30 `save_texture_warning`** built `images/lesson_13_30_save_texture_warning.svg` (id-prefix `sw`, viewBox 0 0 800 420): `SAVE YOUR TEXTURE!` headline + triangle warning icon, warning-red problem band (`Chest_Color*` = UNSAVED mock header chip, matching the lesson body example), positive-green save-action band (Image &#8594; Save menu-path chips + Save As / PNG notes), dashed footer reinforcement. Brand palette per D5 (`#E63946` warning-red, `#4ADE80` positive-green, `#FF6B00` menu-path accent, `currentColor`); NOT the spec's `#f44336`/`#ff8c00`/`#ffebee`. PRODUCTION QA caught + fixed a footer-tip overflow (line shortened to fit the 800px frame). **INTEGRATION (inline, this chat):** refreshed working copy (206,417 B baseline confirmed); pre-flight clean (em-dash 25, 4 `<figure>` / 3 `<img>` / 4 `<svg>`, both anchors count=1). **#10 inlined** as a default-depth `<figure>` (col 20, svg root col 24, children col 28+) directly AFTER the resolution-guidelines card's closing `</div>` and BEFORE the `&#9889; Pro Tip` card (anchored on the `File size` + `Hardware` `<li>` tail + `</div>` + the Pro-Tip-card open, count=1). On inline: stripped root `color="#222"`, kept structural comments, preserved `role`/`aria-labelledby`/`<title>`/`<desc>`/all `rc-*` ids; added a figcaption; no `c{N}`. Commit clean; +6,877 B (213,294). **#30 inlined** as a default-depth `<figure>` (col 20) AFTER the `&#128680; IMPORTANT: Saving Textures` critical card's closing `</div>` and BEFORE `<h3>Texture File Formats</h3>` (the spec's PROMINENT PLACEMENT; anchored on the auto-save `<li>` tail + `</div>` + the `Texture File Formats` heading, count=1). Same inline treatment; `sw-*` ids preserved; figcaption added; no `c{N}`. Commit clean; +6,354 B (219,648). Both inline SVGs render-verified on the page-light background in their committed (stripped) form before close. **Post-commit integrity on a re-refreshed copy:** bytes 219,648; em-dash baseline still **25**; **6 `<figure>` (open=close=6) / 3 `<img>` / 6 inline `<svg>` (open=close=6)**; `rc-title`/`rc-desc`/`sw-title`/`sw-desc` each present once; **zero `color="#222"` leaked** into the inlined roots; 13 section ids, all unique, no dups; all three anchors (`Texture Resolution Considerations` h3, `Texture File Formats` h3, `IMPORTANT: Saving Textures` card) still count=1; **zero `c30` ids** (correctly dropped). No BlenderMCP this chat (both pure inline SVGs). Next step: **L13 P4 PRODUCTION** (`brush-basics`: #4 `br` brush-resize SVG + #3 `bf` brush-falloff SVG + #5 color-picker-annotated PNG + #29 eyedropper PNG; 2 cheap SVGs + 2 Texture-Paint UI captures).

- **2026-06-06 &mdash; P4 PRODUCTION + INTEGRATION COMPLETE (10/30 produced / 10/30 integrated &middot; HTML 219,648 &rarr; 237,577 B).** All four `#brush-basics` figures produced, integrated, and verified in one chat. Scene pre-flight: clean base confirmed (0 objects, 0 materials, no `_WORK`/`L13_*`, AgX, Blender 5.1.1). **#4 `brush_resize_interactive`** built `images/lesson_13_04_brush_resize_interactive.svg` (id-prefix `br`, viewBox 0 0 800 520): F-key radial resize diagram &mdash; live brush-cursor circle + faint concentric size-guide rings, outward (Move away / larger) + inward (Move toward center / smaller) `#FF6B00` arrows, a `Press F, then move mouse` pill, and a lower-right size-steps progression. Brand palette per D5 (`#FF6B00` + `currentColor`); NOT the spec's `#ff8c00`. cairosvg render-check PASS light + dark; labels inside, no `c{N}`. **#3 `brush_falloff_comparison`** built `images/lesson_13_03_brush_falloff_comparison.svg` (id-prefix `bf`, dense viewBox 0 0 800 680): 2&times;2 grid (Smooth / Sharp / Root / Constant), each panel a falloff-curve graph (per-type curve shape) + a radial-gradient circle showing the resulting edge hardness; grid dividers + axis-hint line + footer tip. Brand palette per D5. cairosvg render-check PASS light + dark; labels inside, no `c{N}`. **#5 `color_picker_annotated`** PNG `images/lesson_13_05_color_picker_annotated.png` (736&times;684, 102,884 B). **Route (D4): bridge N-panel capture (Ray's call over the Win+Shift+S popover).** Built the P2 shared scene (beveled crate + 1024 wood base-color image + Principled mat), Texture Paint workspace, TEXTURE_PAINT mode, MATERIAL shading; **maximized the 3D viewport (`screen.screen_full_area`)** so the N-panel Color Picker (wheel + value slider + Color/Gradient toggle) cleared the window bottom; tight-cropped the picker panel + 2&times; LANCZOS. **CAPTURE DISCOVERY (carry forward):** the Texture-Paint N-panel Color Picker wheel binds to `image_paint.unified_paint_settings.color` with `use_unified_color` defaulting ON &mdash; the wheel renders BLACK until the *unified* color is set (setting `brush.color` alone does nothing); set unified color (warm brown, HSV V&ge;0.7) to make the wheel show a real selection. Also the picker is bottom-clipped until the viewport is maximized. NO baked labels &mdash; `c5` rides at INTEGRATION. **#29 `eyedropper_usage`** PNG `images/lesson_13_29_eyedropper_usage.png` (1102&times;684, 108,304 B). **Route (D4): bridge composite HOST (Ray's call over the live S-cursor snip).** The S-key eyedropper crosshair is a live transient the bridge cannot freeze, so #29 is a Pillow-composited host: a wood-tone reference strip (grain-textured bands, the 3rd = the sampled tone) + the picker showing that sampled color, `#FF6B00` 6px divider; built directly to WSL via Blender-side Pillow. NO baked text &mdash; `c29` rides at INTEGRATION. Scene RESTORED to clean base at close (viewport un-maximized via `screen.back_to_previous`, 0 objects/materials, AgX, film opaque, all `L13_*` mats/images/palette purged, orphans purged); all Windows-tempdir + WSL `_temp_l13_p4_*` + Claude-side temps removed. **INTEGRATION (inline, this chat):** refreshed working copy (219,648 B baseline confirmed); pre-flight clean (em-dash 25, 6 `<figure>` / 3 `<img>` / 6 `<svg>`; all 3 region anchors count=1). **Placement (all default-depth, col 20):** #4 inlined AFTER the F-Key `</blockquote>` and BEFORE `<h3>Brush Falloff and Shape</h3>` (+4,866 B &rarr; 224,514); #3 inlined AFTER the Falloff card `</div>` and BEFORE `<h3>Advanced Brush Settings</h3>` (+7,333 B &rarr; 231,847); **#5 then #29 sequenced** (#5 first per the card's picker-before-eyedropper order) AFTER the Color-Selection card `</div>` and BEFORE `<h3>Brush Libraries and Presets</h3>` (+5,730 B &rarr; 237,577). On inline of the two SVGs: stripped root `color="#222"`, kept structural panel comments, preserved `role`/`aria-labelledby`/`<title>`/`<desc>`/all `br-*`+`bf-*` ids; figcaptions added. **Overlay `c5` built** = 3 neutral callouts (Color wheel / Value slider / Selected color), viewBox `0 0 736 684` 1:1, fs 16 small-frame floor, white-on-`#1a1a1a` pills + `#FF6B00` leaders/dots. **Overlay `c29` built** = Press S to sample badge over the reference strip + a highlighted sampled-band box + a `#FF6B00` flow arrow (reference &rarr; picker selection dot) + Color updates badge, viewBox `0 0 1102 684` 1:1, fs 17. **Both overlays pixel-verified BEFORE commit (LD19):** measured target coords against the actual PNG pixels (grid-overlay read, not layout reasoning), cairosvg-composited each exact overlay over its PNG (Claude-side, 1:1), view-confirmed all callouts/leaders/arrow land correctly. All four committed via `edit_file` dry-run &rarr; commit &rarr; `get_file_info`. Both inline SVGs render-verified on page-light in committed (stripped) form before close. **Post-commit integrity on a re-refreshed copy:** bytes 237,577; em-dash baseline still **25** (no new); **10 `<figure>` (open=close=10) / 5 `<img>` / 10 inline `<svg>` (open=close=10)**; `br`/`bf` + all `c5`/`c29` ids present; **zero `color="#222"` leaked**; 13 section ids, all unique, no dups; all 5 `#brush-basics` anchors (`Essential Brush Settings`, `Brush Falloff and Shape`, `Color Selection`, `Advanced Brush Settings`, `Brush Libraries and Presets`) still count=1. Next step: **L13 P5 PRODUCTION** (`painting-techniques`: #7 stroke-techniques-guide, #9 layer-workflow-stages, #8 color-variation-example, #21 symmetry-painting-demo &mdash; all PNG; composites + 1 symmetry capture; `c7` self-labeling SKIP-candidate, `c8` warning/positive colorway, `c9`/`c21` stage+axis callouts).

- **2026-06-06 &mdash; P5 PRODUCTION + INTEGRATION COMPLETE (14/30 produced / 14/30 integrated &middot; HTML 237,577 &rarr; 246,603 B).** All four `#painting-techniques` figures (all PNG) produced, overlaid, pixel-verified, integrated, and verified. PRODUCTION closed in a prior chat with the four PNGs on disk (no baked labels) and all four overlay-bearing `<figure>` blocks pre-built + figcaptioned + pixel-verified (cairosvg 1:1) and saved verbatim to `_l13_p5_blocks.html`. **#7 `stroke_techniques_guide`** (1584&times;738): 6-panel stroke composite (continuous / stipple / straight / hatching / cross-hatch / layered); **`c7` RESOLVED to BUILD, not SKIP** &mdash; 6 neutral panel-label badges (`c7-b00`&hellip;`c7-b12`), white-on-`#1a1a1a`, fs 18. #9 `stroke_techniques_guide`-sibling note: #8 was rebuilt once at PRODUCTION for genuinely subtle flat-vs-varied distinctness (read-distinctness QA passed). **#9 `layer_workflow_stages`** (1572&times;560): 3-stage base&rarr;mid&rarr;detail composite; overlay `c9` Stage 1 &#183; Base / Stage 2 &#183; Mid / Stage 3 &#183; Detail (neutral, fs 18). **#8 `color_variation_example`** (1526&times;760): before/after flat-vs-varied spheres; overlay `c8` Without Variation [`#E63946` warning-red] / With Variation [`#4ADE80` positive-green, `#0f1a14` text] per the LD8 colorway, fs 17. **#21 `symmetry_painting_demo`** (500&times;557): paint-one-side-mirrors capture; overlay `c21` = dashed white center axis + Paint one side [`#FF6B00`] badge+arrow + Mirrors here [`#1a1a1a`] badge+arrow + a two-headed mirror-flow arrow pair, fs 16 small-frame. **INTEGRATION (inline, this chat):** refreshed working copy (237,577 B baseline confirmed); pre-flight clean (em-dash 25, 10 `<figure>` / 5 `<img>` / 10 `<svg>`; all four anchors count=1). All four PNG dimensions re-confirmed on disk vs overlay viewBoxes (1:1). **Placement (all default-depth, figure col 20), each block inserted VERBATIM from `_l13_p5_blocks.html` (overlays NOT rebuilt):** #7 before `<h3>Layering and Building Color</h3>` (+2,858 B &rarr; 240,435); #9 before `<h3>Color Variation for Realism</h3>` (+1,977 B &rarr; 242,412); #8 before `<h3>Blending and Smoothing</h3>` (+1,674 B &rarr; 244,086); #21 before `<h3>Fill Tool</h3>` (+2,517 B &rarr; 246,603). Each via `edit_file` dry-run &rarr; commit &rarr; `get_file_info`. **`_l13_p5_blocks.html` DELETED** via bridge `os.remove` after the four commits (existed=True, removed=True). **Post-commit integrity on a re-refreshed copy:** bytes 246,603; em-dash baseline still **25** (no new); **14 `<figure>` (open=close=14) / 9 `<img>` / 14 inline `<svg>` (open=close=14)**; `c7` (6) / `c9` (3) / `c8` (2) / `c21` (4) ids all present; 13 section ids, all unique, no dups; all four `#painting-techniques` anchors (`Layering and Building Color`, `Color Variation for Realism`, `Blending and Smoothing`, `Fill Tool`) still count=1. No BlenderMCP scene work this chat (PRODUCTION was prior; only the `os.remove` bridge call). Next step: **L13 P6 PRODUCTION** (`color-management`: #16 `cw` color-wheel-relationships SVG + #17 `hv` hsv-properties-diagram SVG + #23 `mc` material-color-chart SVG &mdash; all three inline SVG, cheap; no captures).

- **2026-06-06 &mdash; P6 PRODUCTION + INTEGRATION COMPLETE (17/30 produced / 17/30 integrated &middot; HTML 246,603 &rarr; 272,613 B).** All three `#color-management` figures (all pure inline SVG, no captures, no BlenderMCP) produced, render-checked light + dark, written to `images/`, inlined, and verified in one chat. Brand palette per D5 throughout (`#FF6B00` accents, `currentColor` text/rules; NOT the spec's `#ff8c00`/`#4CAF50`); labels inside via the id-prefix registry, NO `c{N}` overlays. **#16 `color_wheel_relationships`** built `images/lesson_13_16_color_wheel_relationships.svg` (id-prefix `cw`, viewBox 0 0 800 640): 12-section color wheel (full-spectrum pure hues as fixed-hex content) + orange analogous arc over the blue / blue-green / green segments with tick marks + a `currentColor` complementary line (orange &harr; blue) across the wheel with a white dashed inlay and ringed endpoints + top-right `Complementary` callout with leader to the orange endpoint + a two-block stacked legend (Analogous &#183; Harmony / Complementary &#183; Contrast with example swatches). PRODUCTION QA v1 caught a stray complementary-leader pointing into empty space + a legend text overflow / swatch collision; rebuilt v2 (leader retargeted to the orange endpoint, legend restacked) &mdash; clean on both light + dark. **#17 `hsv_properties_diagram`** built `images/lesson_13_17_hsv_properties_diagram.svg` (id-prefix `hv`, viewBox 0 0 800 620): 3 rows (Hue / Saturation / Value), each = property name + sub + a wrapped left-column description + a 5-swatch progression (HSV-computed from the same base blue) + a directional arrow (shared `hv-arrow` marker) with end labels (cyan&rarr;purple / vivid&rarr;gray / dark&rarr;light) + footer tip. QA v1 had the Hue description clipping its 3rd line; rebuilt v2 with shorter descriptions that complete cleanly. **#23 `material_color_chart`** built `images/lesson_13_23_material_color_chart.svg` (id-prefix `mc`, viewBox 0 0 800 600): 2&times;2 grid (Wood / Metal / Stone / Fabric), each = orange material header + 4 swatches with monospace hex codes beneath + a note line (e.g. Wood `Never pure brown &#183; add orange and yellow tints`) + footer tip. All four palettes read true to material on light + dark. All three standalone files keep `color="#222"` for `<img>` fallback. **INTEGRATION (inline, this chat):** refreshed working copy (246,603 B baseline confirmed); pre-flight clean (em-dash 25, 14 `<figure>` / 9 `<img>` / 14 `<svg>`; all three anchors verified count=1 as multi-line spans on the fresh copy). **Placement (all default-depth, figure col 20 / svg root col 24 / children col 28+ / deeper col 32):** #17 inlined AFTER the `Essential Color Concepts` card `</div>` and BEFORE the `Color Relationships` `<div class="card">` (+8,253 B &rarr; 254,856); #16 inlined AFTER the `Color Relationships` card `</div>` and BEFORE the `Value Is King` `<blockquote>` (+8,410 B &rarr; 263,266); #23 inlined AFTER `<h3>Color Tips for Different Materials</h3>` and BEFORE the `Material-Specific Color Guidance` card (+9,347 B &rarr; 272,613). On inline of all three: stripped root `color="#222"`, re-indented, kept structural comments (`<!-- Row N -->`, `<!-- Wood -->`, etc.), preserved `role`/`aria-labelledby`/`<title>`/`<desc>`/all `cw-*`/`hv-*`/`mc-*` ids + the `hv-arrow` marker def; figcaptions added with `&#183;` separators; no `c{N}`. Each committed via `edit_file` dry-run &rarr; commit &rarr; `get_file_info`. **Post-commit integrity on a re-refreshed copy:** bytes 272,613; em-dash baseline still **25** (no new); **17 `<figure>` (open=close=17) / 9 `<img>` / 17 inline `<svg>` (open=close=17)**; `cw-title`/`cw-desc`/`hv-title`/`hv-desc`/`mc-title`/`mc-desc` each present (2 hits = aria-ref + element); **zero `color="#222"` leaked** into the inlined roots; 13 section ids, all unique, no dups; all three anchors still count=1 (`Color Relationships` and `three properties of color` now show 2/3 doc-wide, but only as new figcaption/desc prose &mdash; the edited anchors were unique multi-line spans); **zero NEW `c{N}` ids** (P6 added none; the 7 distinct `c*` prefixes in the file are all pre-existing P1&ndash;P5 overlays). No BlenderMCP this chat (all three pure inline SVG). Next step: **L13 P7 PRODUCTION** (`texture-slots`: #11 `ts` multiple-texture-slots shader-node SVG + #27 roughness-map / #28 metallic-map / #12 3d-2d-sync PNG composites + #20 reuse-by-ref second placement).

- **2026-06-06 &mdash; P7 PRODUCTION + INTEGRATION COMPLETE (21/30 produced / 22/30 integrated [21 distinct + 1 reuse-by-ref] &middot; HTML 272,613 &rarr; 291,630 B).** All four `#texture-slots` figures + the #20 reuse-by-ref second placement produced, overlaid, pixel-verified, integrated, and verified. **PRODUCTION** (prior, logged here): scene staged from the P2/P4 shared crate; all assets on disk + verified (#11 svg 6,646 / #27 png 517,077 / #28 png 503,415 / #12 png 328,198 &mdash; the three PNGs all 1528&times;760; scene RESTORED clean AgX, temps gone; HTML untouched 272,613 B). **#11 `multiple_texture_slots`** built `images/lesson_13_11_multiple_texture_slots.svg` (id-prefix `ts`, viewBox 0 0 800 560): shader node graph &mdash; 3 Image Texture nodes (Base Color `Chest_Color` sRGB / Roughness `Chest_Rough` Non-Color / Metallic `Chest_Metal` Non-Color, blue headers + colored output sockets) &rarr; orange-bordered Principled BSDF (Base Color / Roughness / Metallic inputs) &rarr; red Material Output, with `#FF6B00` noodles + a left-rail footer tip. Brand palette per D5; labels inside, no `c{N}`. **#27 `roughness_map_example`** (1528&times;760): 2-panel grayscale roughness map (orange-bordered, light/dark quadrants) + rendered wood crate; overlay `c27` = Roughness Map / Rendered headers + Dark = Shiny / Light = Rough legend. **#28 `metallic_map_example`** (1528&times;760): 2-panel binary metallic mask (white bands/corners) + rendered crate with metal bands + corner brackets; overlay `c28` = Metallic Map / Rendered + White = Metal 1.0 / Black = Non-Metal 0.0. **#12 `3d_2d_sync_view`** (1528&times;760): split Image Editor (L, red V-stroke on 2D wood) + 3D Viewport (R, same stroke on crate); overlay `c12` = Image Editor / 3D Viewport headers + a dashed `#FF6B00` sync arrow across the divider + Same stroke badge (labels match the actual on-screen layout: Image Editor LEFT, 3D Viewport RIGHT). All three overlays viewBox `0 0 1528 760` 1:1, fs 18 (16 for the small Same-stroke badge), white-on-`#1a1a1a` pills + `#FF6B00` accents. **INTEGRATION (inline, this chat):** re-refreshed working copy (272,613 B baseline confirmed); pre-flight clean (em-dash 25, 17 `<figure>` / 9 `<img>` / 17 `<svg>`, all 5 anchors count=1). **Placement (all default-depth, figure col 20):** #11 inlined AFTER `<h3>What Are Texture Slots?</h3>` and BEFORE its `Texture Slot Concept` card (+9,292 &rarr; 281,905) &mdash; stripped root `color="#222"`, re-indented (svg col 24 / children col 28 / deeper col 32), kept structural node comments, preserved `role`/`aria-labelledby`/`<title>`/`<desc>`/all `ts-*` ids, figcaption added, no `c{N}`; **#20 reuse-by-ref** inserted AFTER the `Common texture slots in PBR workflow` card `</div>` and BEFORE the mermaid `<div>` (+2,905 &rarr; 284,810) &mdash; the existing P1 #20 `<figure>` copied VERBATIM (same `images/lesson_13_20_pbr_texture_types.png`, the same `c20` overlay, **id NOT renamed**, D1), re-indented from its P1 card depth (col 24) to default-depth (col 20); #27 AFTER `<h3>Painting Roughness Maps</h3>` (+2,227 &rarr; 287,037); #28 AFTER `<h3>Painting Metallic Maps</h3>` (+2,253 &rarr; 289,290); #12 AFTER `<h3>Coordinating Multiple Maps</h3>` (+2,340 &rarr; 291,630). The three PNG overlays were **pixel-verified BEFORE commit (LD19):** cairosvg-composited each exact overlay over its actual 1528&times;760 PNG (Claude-side, 1:1) and view-confirmed &mdash; all headers/legends/arrow land on their correct panels (c27/c28 legends lifted from y=690 to y=672 after a first composite showed slight bottom-edge clipping). Each of the 5 inserts via `edit_file` dry-run &rarr; commit &rarr; `get_file_info`. **Post-commit integrity on a re-refreshed copy:** bytes 291,630; em-dash baseline still **25** (no new); **22 `<figure>` (open=close=22) / 13 `<img>` / 22 inline `<svg>` (open=close=22)** (figure +5; img +4 = #20-ref + 3 PNGs, #11 is inline SVG so no img; svg +5 = #11 + 4 overlays); `ts-title`/`ts-desc` present; `c27`/`c28`/`c12` labels + the `c12-arrow` marker (def + ref) present; **second `c20` present, id NOT renamed** (2 `lesson_13_20_pbr_texture_types.png` refs); **zero `color="#222"` leaked**; 13 section ids, all unique, no dups; all 5 `#texture-slots` anchors (`What Are Texture Slots`, `Common texture slots`, `Painting Roughness Maps`, `Painting Metallic Maps`, `Coordinating Multiple Maps`) still count=1. No BlenderMCP this chat (PRODUCTION was prior). Next step: **L13 P8 PRODUCTION** (`advanced-painting`: #15 `c15` mask-tool-visualization &mdash; anchor by the exact `<h3>Masking</h3>` (bare `Masking` non-unique, count 3) + #26 `c26` stencil-painting-setup (Win+Shift+S stencil overlay) + #22 `c22` brush-texture-examples (SKIP-candidate, self-labeling)).

---

## Locked rules carry forward

All UNCHANGED from `session.md`, `lesson_06_completion_handoff.md`, the L07&ndash;L12 progress-doc carry-forward blocks, and `images/IMAGE_STYLE_GUIDE.md` v6 (LD1&ndash;21). Not restated here &mdash; read them. In brief: WSL `\\wsl$\Ubuntu\` paths only (never `\\wsl.localhost\`); `Filesystem:edit_file` dry-run &rarr; commit &rarr; `get_file_info` verify; `Filesystem:write_file` for new files / full-doc overwrites (never `create_file`); `copy_file_user_to_claude` to refresh the working HTML copy before grep/sed + **verify anchor GLOBAL uniqueness first** (anchor by unique TEXT extended to a multi-line span until count=1, never by section id &mdash; relevant here for `Masking`); no new em-dashes (middle-dot `&#183;`; pre-existing 25 em-dashes preserved verbatim through exact anchors; content entities `&#215;`/`&#8211;`/`&#8594;`/`&#183;` fine); `tool_search` for deferred tools (silently); brand palette governs all overlays/SVGs (LD6, NOT the spec's `#ff8c00`/`#4CAF50` hexes &mdash; D5); inline-SVG conventions (strip root `color="#222"` on inlining, re-indent figure col20 / svg col24 / children col28 / deeper col32, collapse multi-line attrs, prune decorative comments, keep structural comments, preserve role/aria/title/desc/id); figure indentation depth from parent container; PNG overlay = wrapper-div + overlay-SVG, viewBox = PNG pixels, NO baked labels; **UI-capture craft dominates L13** (v6 LD14&ndash;16/LD20: modal status bar authoritative for bindings; full-window local-tempdir `screen.screenshot` carries panel/popover overlays then deterministic area crop &mdash; UNC write truncates to 0 bytes, capture to Windows tempdir then Pillow&rarr;WSL; Win+Shift+S overlay-freeze for transient popovers / brush-cursor / picker / dropdown / stencil states saved direct to WSL `images/`; clean-canvas viewport staging hides toolbar/N-panel/nav-gizmo); color-dependent render recipe v6 LD17 applies only if a PRODUCTION review shows a color washing (not pre-forced); pixel-verify every PNG overlay via cairosvg-composite (Claude-side) or PIL-composite (bridge-side) BEFORE commit (LD19); theory/infographic sourcing (Pillow-vignette host OR Canva/Adobe raster) is a PRODUCTION-time call per figure (LD21); BlenderMCP lowercase `blender:execute_blender_code` + clean-scene-manually via `bpy.data.objects.remove` + BLENDER_EEVEE (EEVEE_NEXT&rarr;EEVEE fallback) + Cycles for off-screen reflection/refraction (relevant for #27/#28 rendered halves); NEVER `read_factory_settings`; inline all render helpers per call; scene state between PRODUCTION chats is always a clean base (RESTORE at close; STOP if a leftover `_WORK` collection or hidden base objects are found); pre-plan sub-chat splits for &ge;5-figure phases + INTEGRATION-own-chat for &ge;6 (NONE this lesson by count; break out only if context tightens); plan-lock before build; warn Ray before context tightens rather than letting automatic compaction happen.
