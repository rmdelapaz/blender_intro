# Lesson 20 &#183; Camera Basics &#183; Integration Progress

**Source-of-truth pointer for L20.** Read order at every session start: `session.md` &#8594; this doc &#8594; `images/IMAGE_STYLE_GUIDE.md` (v6).

**Lesson file:** `lesson_20_camera_basics.html`
**Project root:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`
**Status:** PRODUCTION &#183; started 2026-06-20. Pilot = #1 (SVG). Figure set locked at **High+Medium 17** (not the full 30).

---

## 1. Invariant baseline (verified against HTML at session start 2026-06-20)

| Invariant | Baseline | Target after 17-figure integration |
|---|---|---|
| HTML bytes | 165,087 | grows with integration |
| figure | 0 | 17 |
| figcaption | 0 | 17 |
| img (PNG figures) | 0 | 12 |
| svg (open tags) | 0 | 5 inline SVG figures + PNG overlays (1 per annotated PNG) |
| mermaid | 1 | 1 (preserve, never touch) |
| em-dash (pre-existing) | 33 | 33 (preserve verbatim, **add none** &#8212; use `&#183;`) |
| color222 | 0 | 0 (strip on every inlined SVG root) |

**Section IDs (document order):** camera-fundamentals &#183; camera-properties &#183; focal-length &#183; positioning &#183; framing &#183; multiple-cameras &#183; camera-constraints &#183; tips-tricks &#183; project &#183; summary

Pre-existing em-dashes live in body prose (e.g. lesson-intro "creation&#8212;it", various card prose). They are NOT touched; new figcaption / alt-text uses `&#183;` / `.` / `,` / `( )` / `:` per em-dash discipline.

---

## 2. Locked figure roster (17 = High 7 + Medium 10)

Format / placement / priority pulled from `lesson_20_image_requirements.md`.

| # | Slug | Fmt | Section | Priority | State |
|---|------|-----|---------|----------|-------|
| 1 | camera_anatomy_diagram | SVG | camera-fundamentals | High | **PRODUCED** (standalone `images/lesson_20_01_camera_anatomy_diagram.svg`, 5,105 B; cairosvg render-check PASS light+dark) |
| 2 | camera_vs_viewport_comparison | PNG | camera-fundamentals | High | **PRODUCED** (`images/lesson_20_02_camera_vs_viewport.png`, 1266&#215;425, 231,980 B; 2-panel #5 convention; L=CAMERA VIEW triad rig, R=VIEWPORT 3/4 orbit w/ synthetic orange frustum+body baked in render; c2 band labels at integration) |
| 3 | properties_panel_location | PNG | camera-properties | High | pending |
| 4 | focal_length_comparison | PNG | camera-properties / focal-length | High | pending &#183; **Group C RENDER** (computed-framing focal pair, conceptual partner to #22 chart; NOT a UI snip) |
| 5 | perspective_vs_orthographic | PNG | camera-properties | Med | pending |
| 6 | wide_angle_distortion | PNG | focal-length | Med | pending |
| 7 | telephoto_compression | PNG | focal-length | Med | pending |
| 8 | camera_angle_comparison | PNG | positioning | High | **PRODUCED** (`images/lesson_20_08_camera_angle_comparison.png`, 1900&#215;425, 274,090 B; 3-panel #4/#11 convention; LOW/EYE/HIGH constant-Y; c8 band labels at integration) |
| 9 | rule_of_thirds_grid | SVG | framing | High | **PRODUCED** (`images/lesson_20_09_rule_of_thirds_grid.svg`, 3,930 B; render-check PASS) |
| 11 | shot_types_reference | PNG | camera-fundamentals | Med | pending |
| 12 | headroom_looking_room | SVG | framing | Med | **PRODUCED** (`images/lesson_20_12_headroom_looking_room.svg`, 4,692 B; render-check PASS) |
| 14 | camera_selection_active | PNG | multiple-cameras | Med | pending |
| 15 | track_to_constraint | PNG | positioning / camera-constraints | Med | pending |
| 17 | camera_organization_outliner | PNG | multiple-cameras | Med | pending |
| 22 | focal_length_chart | SVG | camera-properties / focal-length | High | **PRODUCED** (`images/lesson_20_22_focal_length_chart.svg`, 5,063 B, 800x680 dense; render-check PASS) |
| 23 | camera_movement_tools | SVG | positioning | Med | **PRODUCED** (`images/lesson_20_23_camera_movement_tools.svg`, 6,138 B; render-check PASS) |
| 30 | common_camera_mistakes | PNG | tips-tricks | Med | **PRODUCED** (`images/lesson_20_30_common_camera_mistakes.png`, 1900&#215;1080, 12,685 B; LD21 Pillow warning-vignette overlay-HOST, NO baked text; title banner + 2&#215;2 problem cards + center green-check solution card; clear bands reserved for c30 overlay) |

**SVG (5):** #1, #9, #12, #22, #23.
**PNG (12):** #2, #3, #4, #5, #6, #7, #8, #11, #14, #15, #17, #30.

Deferred (13, lower priority &#8212; not in this build): #10, #13, #16, #18, #19, #20, #21, #24, #25, #26, #27, #28, #29.

---

## 3. id-prefix registry (inline SVG)

One unique prefix per inline SVG to avoid `id` collisions on the page.

| # | Slug | id-prefix |
|---|------|-----------|
| 1 | camera_anatomy_diagram | `ca` |
| 9 | rule_of_thirds_grid | `rt` |
| 12 | headroom_looking_room | `hr` |
| 22 | focal_length_chart | `fc` |
| 23 | camera_movement_tools | `cm` |

## 4. c{N} overlay-namespace registry (PNG annotation overlays)

Each annotated PNG's overlay ids use a `c{N}-*` prefix matching the figure number.
Assigned as overlays are authored: c2, c3, c4, c5, c6, c7, c8, c11, c14, c15, c17, c30 (per the PNG list; #16/#-style overlay-free figures noted at integration if any land bare).

---

**Cheat-sheet badge variant for L20: FILLED** (solid `#FF6B00` rect, `#ffffff` text). Set by #23 (first SVG with shortcut badges). Any future L20 SVG/overlay shortcut badges use filled, not muted, to stay consistent within the lesson.

## 5. Production cadence (locked)

- Plan-lock before build (this doc); pilot SVG first, then batches of 3.
- SVG: hand-written, viewBox `0 0 800 560` (or `800 680` dense), transparent, `currentColor`, 3px `#FF6B00` top strip, system-ui stack, role/aria/title/desc. Standalone `.svg` in `images/` keeps `color="#222"`; strip it when inlining.
- PNG: per capture-path routing (LD20). Color-dependent renders use F12 `render.render` + Standard view transform (LD17); plain-grey geometry and UI-chrome are exempt. **Group C exception (corrected 2026-06-20):** the L20 Group C grey-clay renders ALSO use **F12 EEVEE `render.render` (NOT `render.opengl`)** under the locked SUN recipe (Standard / look None / exp 0; world grey 0.039 str 1.0; SUN rx66&#176; en1.75 angle10&#176;) &#8212; `render.opengl` SOLID returns a near-black empty frame for this dark-floor + grid-geometry staging. The earlier &#167;6 / LD17-exempt `render.opengl` note for Group C was WRONG. Bridge-side Pillow in-place crop for UI chrome. cairosvg overlay pixel-verify Claude-side before every `edit_file` commit (LD19).
- INTEGRATION: `Filesystem:edit_file` dryRun &#8594; commit (dryRun=false) &#8594; `get_file_info` after every commit. Default 16/20-space figure depth; 20/24/28/32 deeper-indent inside cards.
- Blender restore at chat close: pristine (EEVEE / AgX / None / exposure 0) per session.md.

---

## 6. Status log

- **2026-06-20 &#183; session start.** Read order complete (session.md &#8594; this doc created &#8594; IMAGE_STYLE_GUIDE v6). Invariant baseline captured (165,087 B; 0 figures; mermaid 1; em-dash 33; color222 0). Figure set locked High+Medium 17. Pilot #1 build starting.
- **2026-06-20 &#183; pilot #1 PRODUCED.** `lesson_20_01_camera_anatomy_diagram.svg` (prefix `ca`, viewBox 800x560, 5,105 B). Standalone keeps `color="#222"` for `<img>` fallback; will strip on inline. cairosvg render-check PASS on both light (dark text via `currentColor`) and dark (light text) backgrounds; all 5 callouts (Camera Body, Field of View, Frustum, Frame Border, Top Indicator) clear of geometry and each other; 3px `#FF6B00` top strip, Variant B footer tip, `&#183;` separator, 0 em-dash. Awaiting style sign-off before batch 1 (next SVGs: #9, #22).
- **2026-06-20 &#183; pilot signed off + SVG batch 1 PRODUCED.** Ray approved pilot, no adjustments. Built 3 more SVGs: #9 rule_of_thirds_grid (`rt`, 3,930 B), #22 focal_length_chart (`fc`, 800x680 dense, 5,063 B), #12 headroom_looking_room (`hr`, 4,692 B). All cairosvg render-checked light+dark, 0 em-dash, color222 kept for `<img>` fallback, 3px top strip + Variant B footer each. #12 rebuilt once (subject enlarged + shifted left to fill frame and make the front/back asymmetry obvious). **SVG progress: 4 of 5 PRODUCED** (#1, #9, #12, #22); **#23 camera_movement_tools still pending.** Next: SVG #23 (closes the vector set), then the 12 PNG captures (BlenderMCP / manual).
- **2026-06-20 &#183; SVG #23 PRODUCED &#183; ALL 5 SVGs DONE.** `lesson_20_23_camera_movement_tools.svg` (`cm`, 6,138 B, three-method workflow cards: Transform G/R &#183; Lock Camera to View &#183; Numeric N-panel). render-check PASS light+dark, 0 em-dash. Locked the L20 cheat-sheet badge variant = **FILLED** (orange rect / white text), recorded in &#167;5. **PRODUCTION STATE: 5/5 SVG complete (#1, #9, #12, #22, #23); 0/12 PNG.** Remaining work all PNG (BlenderMCP / manual capture) + then INTEGRATION (inline 5 SVGs, place 12 PNGs w/ overlays) + browser QA. Handoff to fresh chat for the PNG phase (needs BlenderMCP bridge up).
- **2026-06-20 &#183; PNG phase session start &#183; two routing decisions recorded.** Read order complete (session.md &#8594; this doc &#8594; IMAGE_STYLE_GUIDE v6). Plan-lock from prior chat AGREED. Two prior-chat routing decisions now written into &#167;2 roster: (1) **#4 focal_length_comparison &#8594; Group C RENDER** (computed-framing focal pair, conceptual partner to the #22 chart; NOT a UI snip). (2) **#30 common_camera_mistakes &#8594; LD21 Pillow warning-vignette overlay-host** (no baked text; clear bands reserved for the c30 overlay). **Net build groups (12 PNG total, no invariant-target change):** Group C = 8 renders (#2, #4, #5, #6, #7, #8, #11, #30); Group A = 3 UI-chrome (#3, #14, #17); Group B = 1 manual snip (#15 Track To popover, Win+Shift+S / LD16). **BUILD ORDER: Group C first, then Group A, then Group B as one manual batch.** Group C method LOCK: computed camera framing (one reusable helper solving camera distance from focal length + target subject-fills-X%-of-frame-height, reused across #5/#6/#7/#8/#11); subject = foreground hero + distinct midground + background marker (NOT equal cubes) so wide=exaggerated depth / tele=compressed stack reads; grey clay color-INDEPENDENT &#8594; `render.opengl` viewport path correct (F12 NOT needed, LD17 exempt); selective overlays (floor grid + X/Y axes ON for depth cue, cursor/text/gizmo/extras OFF, deselect all); render to Windows tempdir &#8594; copy to WSL `images\` (UNC truncation rule); cairosvg overlay pixel-verify Claude-side before every commit (LD19). Blender confirmed restored true-pristine at last close (Cube+Light+Camera, EEVEE/AgX/None/exp0, world 0.88/str1.0, 50mm, 1920/1080/100, 0 mats/0 node_groups; prior 45mm+0.05 L09 drift corrected). **Next: plan-lock the Group C subject design with Ray before any render.**
- **2026-06-20 &#183; Group C subject plan-locked + focal triad PRODUCED (#4, #6, #7 &#183; 3/8 Group C).** Plan-lock signed off by Ray. **Grid question resolved:** `render.opengl(view_context=True)` does NOT carry the viewport floor grid (only a faint axis line); switched to **real grid-line geometry** (L09 `make_grid_lines` pattern, but SOLID `render.opengl` path so grid lines use a light *base color* `(0.30,0.30,0.32)` not emission). **LOCKED STAGING SPEC (reuse for #5/#8/#11):** subject = hero slab 0.4&#215;0.4&#215;1.8 m at origin (base on floor) + midground torus (major 0.45&#215;0.7-scale ring on short post at `(1.4, 4.5, 0.7)`) + background gate (two pillars r0.12 h3.0 at `(-2.2, 8.5, 1.5)` & `(-0.2, 8.5, 1.5)` + lintel at `(-1.2, 8.5, 3.0)`); dark floor plane `L20FloorDark` base `(0.62,0.62,0.64)` at `(0,3,0)` size 40; shared grey clay `L20Clay` base `(0.55,0.55,0.55)` rough 0.7. **Camera rig:** eye-level `CAM_Z=1.5`, on &#8722;Y looking +Y. **Framing solver = STABLE GEOMETRIC-AIM (NOT feedback):** alternate (a) distance &#8592; `dist *= measured_fill/target_fill` using `world_to_camera_view` on hero top/bottom, (b) pitch &#8592; GEOMETRIC aim `rot_x = 90&#176; + atan2(hero_mid_z &#8722; CAM_Z, dist)`; ~20 iters. Target **fill 0.62**, hero auto-centers both axes. (The earlier proportional-gain pitch feedback loop ran away at 135mm &#8212; do NOT use it; the geometric-aim version is proven across 20/50/135.) **Focal triad values:** 20mm dist 2.75 m pitch 77.69&#176;; 50mm dist 7.119 m pitch 85.18&#176;; 135mm dist 19.336 m pitch 88.22&#176; (all fill 0.620, cx 0.500). Tele gate runs off frame-top &#8212; **kept by Ray** (looming reinforces compression). **Naming reconciled:** #6 `lesson_20_06_wide_angle_distortion.png` = 20mm solo (1,507,624 B); #7 `lesson_20_07_telephoto_compression.png` = 135mm solo (1,369,857 B); **#4 `lesson_20_04_focal_length_comparison.png` = 3-panel COMPOSITE** (20/50/135 L&#8594;R, 632px panels, 2px `#FF6B00` dividers, 3px orange top strip, **70px clear top label band, NO baked text** per LD21/LD10 composite-overlay variant; 1902&#215;425, 254,449 B). The 50mm render is the composite's middle panel, not a standalone deliverable. All three built via the same stable solver for material/method parity; #4 composited bridge-side with PIL from the three `_l20_F_{20,50,135}_TEMP.png` panels. **c4 overlay plan:** three labels (`20mm WIDE` / `50mm NORMAL` / `135mm TELE`) centered over each panel in the clear band at INTEGRATION; cairosvg pixel-verify before commit (LD19). **BLENDER NOW NON-PRISTINE:** staged Group C scene + 3 materials (`L20Clay`, `L20FloorDark`, `L20GridLine`) + 50 grid-line objects + hero/mid/gate live in scene; default Cube removed. LEFT IN PLACE intentionally for the next chat to reuse the rig (re-running the solver per figure). **Must restore pristine at final Group-C close.** **Strays in `images\` to clean or reuse:** `_l20_gridtest_TEMP.png`, `_l20_gridtest2_TEMP.png`, `_l20_gridtest3_TEMP.png`, `_l20_focal_{20,50,135}_TEMP.png`, `_l20_F_{20,50,135}_TEMP.png` (the `_l20_F_*` ones ARE the saved triad panels; safe to delete after #4 is confirmed in-page at integration). **REMAINING Group C (5): #2 camera_vs_viewport, #5 perspective_vs_orthographic, #8 camera_angle_comparison, #11 shot_types_reference (all reuse the locked rig/solver), then #30 warning-vignette (LD21 Pillow host, different path).** Then Group A (#3/#14/#17 UI-chrome) + Group B (#15 manual snip), then INTEGRATION, then browser QA.
- **2026-06-20 &#183; RENDER-PATH CORRECTION (must read before next Group C render) &#183; #5 attempted, NOT produced.** Reused the live rig (confirmed 59 objects / 3 mats intact, default Cube gone) + the stable geometric-aim solver (converged exact: 50mm dist 7.1187, pitch 85.182&#176;, fill 0.6200, cx 0.5000 &#183; ortho_scale 2.8930 same fill). **The prior §6 claim that Group C renders via `render.opengl` SOLID is WRONG and cost several renders.** `render.opengl` SOLID (studio light, color_type TEXTURE, theme bg 0.05) does NOT light the dark floor plane or the grid-line geometry &#8212; it returns a near-black near-empty frame (hero lost, only gate + faint horizon read). The known-good triad panels (`_l20_F_{20,50,135}_TEMP.png`, **1920&#215;1080** each, NOT 632px &#8212; the 632px figure in the #4 entry is the DOWNSCALED composite-panel width) were produced by **F12 `bpy.ops.render.render(write_still=True)` (EEVEE)** with the scene Light + floor/grid materials doing the lighting: that path reproduces the hero-dominant lit-floor-with-grid composition. **So: render Group C #2/#5/#8/#11 via F12 EEVEE, resolution 1920&#215;1080, NOT render.opengl.** Correct the §5 cadence + LD17-exempt note accordingly at next doc pass. **STILL UNRESOLVED (blocker for exact parity): the world + view-transform the triad used.** F50 measured luminance targets: floor &#8776;(114,115,117), backdrop &#8776;(55,55,55), hero &#8776;(150,151,151). A calibration render at **Standard / world 0.05** gave backdrop (63,63,63) close but floor + hero badly off and asymmetric (floor_LR +45, hero &#8722;104), i.e. the live **Light position and/or world setup differs from what the triad used** &#8212; not recoverable from this doc or the scene alone. **Next chat MUST either (a) get the triad's exact Light loc/energy + world color/strength + view_transform from Ray, or (b) re-derive by bracketing F12 renders against the three F50/F20/F135 luminance targets until matched, lock it, THEN render #2/#5/#8/#11.** Do NOT composite any panel until a 50mm F12 render matches `_l20_F_50_TEMP.png` within a tight luminance tolerance (the marginal-read / pixel-verify discipline). **Scene restored to as-found this chat:** AgX / world 0.88 str 1.0 / camera PERSP 50mm; **rig LEFT LIVE (non-pristine) for reuse** (still 59 obj / 3 mats). **All P5 diagnostic strays deleted** from both Windows tempdir and WSL `images\` (`_l20_P5_{persp,ortho,F12test,calib}_TEMP.png`); triad source panels + the 3 deliverables (#4 254,449 / #6 1,507,624 / #7 1,369,857) verified intact. Group C still **3/8**; #5 NOT produced this chat.
- **2026-06-20 &#183; RECIPE LOCKED &#183; Group C render recipe re-derived to F50 parity (bracket-match path).** Ray chose bracket-matching over hand-supplied values. Confirmed rig live (59 obj / 3 mats / Cube gone) before any render. Decomposed F50 ground truth at locked sample coords (hero (918,400), backdrop (1500,120), floor (300,720) on the 1920x1080 panel): hero (151,152,152), backdrop flat (55,55,55) uniform in every direction (= world, NOT a lit plane), floor 107 (far) rising to 115 (near) i.e. **nearly flat**. **KEY FINDING: the triad Light was a SUN, not the POINT light left live in the rig.** The as-found POINT light (loc (4.08,1.01,5.9) energy 1000) cannot make the floor flat (point-light inverse-square gives a hot-foreground gradient that does NOT match F50) and was leftover drift, not the triad rig. A SUN reproduces the dead-flat floor. **LOCKED RECIPE (reproduces F50 within tight tolerance):** EEVEE F12 `bpy.ops.render.render(write_still=True)`, **1920x1080**; `view_transform='Standard'` + `look='None'` + exposure 0; world grey **0.039** strength **1.0** (&#8594; backdrop 55); Light **SUN**, `rotation_euler` = (**66&#176;**, 0, 0), energy **1.75**, **angle 10&#176;** (soft &#8212; washes the cast shadow to match F50's near-shadowless floor; the default 0.526&#176; sun threw a too-hard torus shadow); materials at base values (L20Clay 0.55, L20FloorDark 0.62/0.62/0.64, L20GridLine 0.30/0.30/0.32, all emis 0). Camera unchanged PERSP 50mm at (0,&#8722;7.1187,1.5) pitch 85.182&#176;. **VERIFICATION:** full-frame diff of a 1920x1080 render vs `_l20_F_50_TEMP.png` = **mean abs diff 2.58**, 85% px within 5 lum, 96% within 10, only 2.5% > 20 (those are object-edge AA + the residual faint torus shadow). Sample-point match: hero 152 / backdrop 56 / floor 114 (flat near=far). Marginal-read / pixel-verify discipline satisfied &#8212; cleared to render #2/#5/#8/#11 (and to composite). **All bracket temps deleted** (`_l20_brk_*`, `_l20_sun_*` from Windows tempdir; `_l20_brk_FULL` / `_l20_sun_QFULL` / `_l20_sun_QFULL2` from WSL images). Triad source panels + 3 deliverables still intact. **Scene now: SUN-recipe live** (Light converted POINT&#8594;SUN, rx66/en1.75/angle10; world 0.039/1.0; Standard/None/exp0; camera PERSP 50mm). Rig still 59 obj / 3 mats, non-pristine, LEFT LIVE for #5 render next. **DOC FIX STILL PENDING:** &#167;5 cadence + the LD17-exempt note still say `render.opengl` / F12-not-needed for Group C &#8212; both are WRONG; correct to **F12 EEVEE Standard + SUN recipe** at next doc pass. Group C still **3/8** (#5 not yet produced; recipe now locked to produce it).
- **2026-06-20 &#183; #5 perspective_vs_orthographic PRODUCED &#183; Group C 4/8.** Rendered with the locked SUN recipe. **Framing decision (Ray-signed):** matched-hero-fill ortho (ortho_scale 2.8930) at the triad's pitched close camera blew the hero up and lost the scene, AND the pitched aim made ortho gate-pillars lean/clip badly &#8212; both failed the persp-vs-ortho read. Ray chose **level camera (0 pitch) for BOTH panels** (this figure's teaching point is projection, so a dedicated level aim distinct from the triad staging is correct). Final rig for #5 only: camera at (0,&#8722;8.5,1.5), `rotation_euler`=(90&#176;,0,0) level, lighting = locked SUN recipe unchanged. **Left = PERSP 50mm** (grid converges, depth falloff). **Right = ORTHO**, same level position/aim, `ortho_scale` = **6.12** (= persp visible width at the hero plane y=0, dist 8.5: 2&#183;8.5&#183;tan(hfov/2), hfov 39.6&#176;) so scene extent matches; parallel verticals, distant gate stays full-size (textbook ortho signature). Pillars vertical / no clip in both. **Composite (matches #4 convention exactly):** 2 panels each 632&#215;355 (LANCZOS from 1920&#215;1080), 70px clear top label band color **(36,36,38)**, 3px `#FF6B00` top strip full width, single 2px `#FF6B00` center divider; final **1266&#215;425, 144,157 B**, `images/lesson_20_05_perspective_vs_orthographic.png`. **NO baked text** &#8212; c5 overlay labels PERSPECTIVE / ORTHOGRAPHIC land in the clear band at INTEGRATION (cairosvg pixel-verify before commit, LD19). **All #5 intermediates deleted** (`_l20_P5*`, `_l20_P5L_*` from both tempdir + WSL). **Camera RESTORED to triad rig** (PERSP 50mm, (0,&#8722;7.1187,1.5), pitch 85.182&#176;) so #2/#8/#11 reuse the staging solver. Scene: SUN-recipe live, 59 obj / 3 mats, non-pristine. **REMAINING Group C (4): #2 camera_vs_viewport, #8 camera_angle_comparison, #11 shot_types_reference (reuse triad rig + solver + SUN recipe), then #30 warning-vignette (LD21 Pillow host).**
- **2026-06-20 &#183; #11 shot_types_reference PRODUCED &#183; Group C 5/8.** Rendered with the locked SUN recipe on the live triad rig (confirmed first: 59 obj / 3 mats / Cube gone; SUN rx66/en1.75/angle10, world 0.039/1.0, Standard/None/exp0, EEVEE 1920x1080, camera PERSP 50mm restored to triad). **Reused the stable geometric-aim solver at three target fills** (this figure's teaching point is framing DISTANCE, so the triad pitch staging is correct &#8212; no dedicated aim needed, Ray-signed): WIDE fill 0.35 &#8594; dist 12.6701 pitch 87.289&#176;; MEDIUM fill 0.62 &#8594; dist 7.1187 pitch 85.182&#176; (= triad 50mm exactly, solver/rig parity confirmed); CLOSE fill 0.85 &#8594; dist 5.1611 pitch 83.369&#176;. All cx 0.5000. **Recipe parity verified on the MEDIUM panel** at the locked F50 sample coords: hero (151,151,151), backdrop (56,56,56), floor (114,114,115) &#8212; dead-on the F50 targets (151/55/114), recipe held across a fresh render. **Marginal-read PASS:** hero rendered width 279 / 472 / 560 px (wide<medium<close), three distinct crops at thumbnail scale. **Composite (matches #4/#5 convention exactly):** 3 panels each 632&#215;355 (LANCZOS from 1920&#215;1080), 70px clear top label band color (36,36,38), 3px `#FF6B00` top strip full width, 2px `#FF6B00` dividers between panels; final **1900&#215;429, 252,484 B**, `images/lesson_20_11_shot_types_reference.png`. **NO baked text** &#8212; c11 overlay labels WIDE / MEDIUM / CLOSE-UP land in the clear band at INTEGRATION (cairosvg pixel-verify before commit, LD19). Eyeball QA PASS (Claude-side view): wide = airy establishing, medium = context tightening, close = hero dominant with gate clipping. **#11 intermediates still on disk** (`_l20_S11_{wide,medium,close,composite}_TEMP.png` in Windows tempdir) &#8212; delete at next render-batch cleanup. **Camera still at triad rig** (PERSP 50mm, (0,&#8722;7.1187,1.5), pitch 85.182&#176;). Scene: SUN-recipe live, 59 obj / 3 mats, non-pristine. **REMAINING Group C (3): #8 camera_angle_comparison (dedicated low/eye/high aims), #2 camera_vs_viewport (dedicated 3/4 orbit right panel showing camera object), then #30 warning-vignette (LD21 Pillow host). Plan-locked with Ray: #8 = low/eye/high constant-distance; #2 = camera-view vs free-orbit-showing-camera-object; render order #8 then #2.**
- **2026-06-20 &#183; #8 + #2 PRODUCED &#183; Group C 7/8 &#183; render batch (#8 then #2) complete.** Both rendered on the live triad rig with the locked SUN recipe (confirmed first: 59 obj / 3 mats / Cube gone; SUN rx66&#176;/en1.75/angle10&#176;, world 0.039/1.0, Standard/None/exp0, EEVEE 1920x1080). **As-found note:** camera was sitting at the #11 CLOSE-UP framing ((0,&#8722;5.161,1.5) pitch 83.369&#176;), NOT the triad rig the prior entry claimed it was restored to &#8212; harmless here since both #8 and #2 set their own cameras, but flagged for accuracy.
  &#183; **#8 camera_angle_comparison** &#8212; Ray-signed framing: **constant Y = &#8722;7.1187** (true constant subject depth, not slant distance), only CAM_Z + pitch change. LOW Z 0.4 &#8594; pitch 94.018&#176;; EYE Z 1.5 &#8594; pitch 85.182&#176; (= triad MEDIUM exactly); HIGH Z 3.2 &#8594; pitch 72.095&#176;. Aim via geometric solver `rot_x = 90&#176; + atan2(0.9 &#8722; CAM_Z, 7.1187)`. **Recipe parity verified on EYE panel** at F50 sample coords: hero 151 / backdrop 56 / floor 114 &#8212; dead-on F50 targets. Marginal-read PASS (Claude-side view): LOW looking-up/thin-floor, EYE level/balanced, HIGH looking-down/floor-dominant; hero held at constant depth+scale so only angle changes. Composite #4/#11 convention: 3 panels 632&#215;355 LANCZOS, 70px clear band (36,36,38), 3px `#FF6B00` top strip, 2px orange dividers, NO baked text &#8594; **1900&#215;425, 274,090 B**, `images/lesson_20_08_camera_angle_comparison.png`. c8 labels LOW / EYE LEVEL / HIGH at integration.
  &#183; **#2 camera_vs_viewport** &#8212; Ray-signed: Left = CAMERA VIEW (triad MEDIUM rig as-is, parity 151/56/114). Right = VIEWPORT (dedicated 3/4 orbit camera showing the camera object + frustum in-scene). **Frustum approach (Ray-signed): synthetic orange geometry baked into the render** (LD20 synthetic-cage), NOT overlay-at-integration. Built 8 orange emissive edge-rods (4 converging rays apex&#8594;far-corners + 4 far-rectangle edges, geometrically faithful 50mm cone hfov 39.6&#176;) + a small camera-body box at the triad apex (0,&#8722;7.1187,1.5)/pitch85.182&#176;; material `L20Frustum` emission. **Color tuning:** first pass emission (1.0,0.42,0.0)/str2.0 clipped to YELLOW under Standard; corrected to (1.0,0.27,0.0)/str1.0 to read as brand orange. **Orbit reframe:** first vantage (10,&#8722;11,6) put the apex off-frame (camera object not shown = teaching point lost); corrected to (12.5,&#8722;16,8.5) aim (&#8722;0.5,&#8722;1.5,1.0) lens 42mm so apex sits lower-left and the cone fans across the staging. Composite #5 convention: 2 panels 632&#215;355 LANCZOS, 70px clear band, 3px top strip, single 2px center divider, NO baked text &#8594; **1266&#215;425, 231,980 B**, `images/lesson_20_02_camera_vs_viewport.png`. c2 labels CAMERA VIEW / VIEWPORT at integration.
  &#183; **Teardown:** all #2 synthetic objects removed (8 frustum rods + C2_CamBody + C2_OrbitCam), `L20Frustum` material + orphan datablocks purged; **scene back to 59 obj / 3 mats** (L20Clay/L20FloorDark/L20GridLine), Cube gone, main Camera restored active at triad MEDIUM rig (0,&#8722;7.1187,1.5)/pitch85.182&#176;. SUN-recipe live, non-pristine, LEFT LIVE.
  &#183; **Cleanup:** deleted #8 temps (`_l20_A8_*`), #2 temps (`_l20_A2_*`), AND the pending #11 intermediates (`_l20_S11_*`) from both Windows tempdir + WSL images. **Triad source panels `_l20_F_{20,50,135}_TEMP.png` PRESERVED** (still needed until #4 confirmed in-page at integration). No `_l20_*` strays remain except those three protected panels.
  &#183; **Doc fix applied this pass:** &#167;5 cadence line corrected &#8212; Group C renders via F12 EEVEE + SUN recipe, NOT `render.opengl`. (The LD17-exempt mention in &#167;5 is now annotated; the standalone LD17 entry in IMAGE_STYLE_GUIDE is unchanged since the Group C path is an L20-local exception, noted in &#167;5.)
  &#183; **REMAINING Group C (1): #30 common_camera_mistakes** &#8212; LD21 Pillow warning-vignette overlay-host (no render, no rig; different path). After #30: **RESTORE BLENDER PRISTINE** at final Group C close, then Group A (#3/#14/#17 UI-chrome) &#8594; Group B (#15 manual snip) &#8594; INTEGRATION &#8594; browser QA.
- **2026-06-20 &#183; #30 common_camera_mistakes PRODUCED &#183; GROUP C 8/8 COMPLETE &#183; BLENDER RESTORED PRISTINE.** Built per LD21 / v6 warning-vignette-as-overlay-host. Pure bridge-side Pillow (PIL 12.2.0 inside Blender 5.1.1 Python); NO render, NO rig touched for the build. **Canvas 1900&#215;1080** (Ray-signed), dark-grey&#8594;warm-amber vertical gradient bg, 3px `#FF6B00` top strip. **Structure (all glyphs/shapes baked, ZERO baked text):** title banner (red `#E63946` left rail + warning-triangle glyph + clear title band) &#183; 2&#215;2 problem cards (each red top rail + baked red-X badge `#E63946`/white X strokes + yellow-filled warning-triangle glyph + tight clear label band) &#183; enlarged center green-check solution card (`#4ADE80` rail + baked green-check glyph + wide clear command band) &#183; yellow `#F5C242` footer rail with `#FF6B00` accent ticks. **Four problem scenarios (Ray-signed, c30 label-band text at INTEGRATION):** (1) default 50mm for everything; (2) camera clipping (subject cut by clip start/end); (3) no headroom / cramped framing; (4) forgetting to set the active camera (Ctrl+Numpad0). **Solution-card command (c30):** frame with intent &#183; pick focal length, check clipping, mind the thirds. **QA-view Claude-side BEFORE disk-write** (copied temp via `copy_file_user_to_claude`, viewed): bands tight, no dead leaders, glyphs clear, palette correct &#8212; PASS. Saved `images/lesson_20_30_common_camera_mistakes.png` (**1900&#215;1080, 12,685 B**; small byte count expected for a flat-fill overlay host). QA temps removed (Windows tempdir + WSL `_l20_30_QA_TEMP.png`). **c30 reserved band coords (for INTEGRATION overlay):** title (200,60,1790,126); problems [(98,330,902,428),(998,330,1802,428),(98,632,902,730),(998,632,1802,730)]; solution (430,834,1560,946). c30 namespace already reserved in &#167;4.
  &#183; **FINAL GROUP C CLOSE &#183; BLENDER RESTORED TRUE-PRISTINE.** Pre-restore audit confirmed non-pristine (59 obj / 3 mats `L20Clay`/`L20FloorDark`/`L20GridLine` / 0 node_groups; SUN-recipe live Standard/None/exp0 world 0.039; camera triad rig). Restore: deleted all 58 non-Camera objects (hero/mid/gate/floor + 50 grid-line objs + SUN); re-added default Cube (2m at origin) + Light (POINT energy 1000 at default loc) ; Camera &#8594; default loc/rot, PERSP 50mm; view transform &#8594; **AgX / look None / exposure 0**; EEVEE; res **1920/1080/100**; world grey **0.88** strength **1.0**; removed all 3 L20 materials + `orphans_purge` (58 datablocks deleted) &#8594; **0 materials / 0 node_groups**. **`get_scene_info` CONFIRMED:** object_count 3 (Camera + Cube + Light), materials_count 0. True default-startup pristine. **No `_l20_*` strays** except the 3 protected triad source panels `_l20_F_{20,50,135}_TEMP.png` (still needed until #4 confirmed in-page at INTEGRATION).
  &#183; **PRODUCTION STATE: Group C 8/8 DONE; all 5 SVGs done.** PRODUCED + on disk: SVGs #1/#9/#12/#22/#23; PNGs #4 composite, #5, #6, #7, #8, #2, #11, #30. **REMAINING PRODUCTION: Group A (#3/#14/#17 UI-chrome) &#8594; Group B (#15 manual snip, Win+Shift+S / LD16).** Then INTEGRATION (inline 5 SVGs strip `color="#222"`; place 12 PNGs w/ c{N} overlays: c4 3-label WIDE/NORMAL/TELE &#183; c5 PERSPECTIVE/ORTHOGRAPHIC &#183; c8 LOW/EYE LEVEL/HIGH &#183; c11 WIDE/MEDIUM/CLOSE-UP &#183; c2 CAMERA VIEW/VIEWPORT &#183; c30 title+4 problems+solution) &#8594; browser QA. **Group A/B need Blender bridge again (UI-chrome captures); #30 did not.** **L20 invariant targets unchanged:** figure/figcaption 17/17, img 12, mermaid 1 (preserve), em-dash 33, color222 0.
  &#183; **CLEANUP PENDING (unchanged):** `_l20_F_{20,50,135}_TEMP.png` ARE #4's composite source panels &#8212; delete ONLY after #4 confirmed in-page at INTEGRATION. No other `_l20_*` strays remain.
