# Lesson 15 &#183; Understanding Light Types &#8211; Integration Progress

**Module:** 04 &#183; Lighting &amp; Rendering
**HTML target:** `lesson_15_understanding_light_types.html`
**Baseline (entry, verified this kickoff):** **191,211 B** &#183; PRISTINE pre-figure body (0 figure / 0 img / 0 svg / 0 figcaption / 0 overlay wrapper-div). 12 sections, 12 unique ids, no collisions, 3 `mermaid` refs (1 live diagram block in `comparing-lights`), no `c{N}` ids present.
**Requirements doc:** `lesson_15_image_requirements.md` 64,702 B &#8211; 40 images enumerated.
**Style guide:** `images/IMAGE_STYLE_GUIDE.md` v6 (locked decisions 1&#8211;21).
**Status:** **P12 DONE 2026-06-13** (`summary` FULLY CLOSED &#8212; #38 flat-lighting mistake 2-panel + `c38` WRONG/CORRECT overlay, #39 too-many-lights mistake 2-panel + `c39` WRONG/CORRECT overlay, #40 volumetric_example Cycles render, overlay SKIPPED per D7; single-chat PRODUCTION + INTEGRATION). **L15 FIGURE-COMPLETE: 40/40 produced / 40/40 integrated.** HTML **351,720 (P0) &#8594; 356,187 B** (+4,467; all 3 figs inserted at one anchor in `summary`, after the Common-Lighting-Mistakes card, before Next-Steps card). #38/#39 = 2002&#215;1000 Pillow composites (bad LEFT / good RIGHT, `#FF6B00` 2px divider, no baked labels), Suzanne subject, Cycles, AgX; `c38`/`c39` LD8 red `#E63946` WRONG / green `#4ADE80` CORRECT composite-pills, cairosvg-pixel-verified 1:1 (LD19). #40 = Cycles spot+volumetric-fog beam (1920&#215;1080, AgX held per D9, lum mean 36 no wash), D7&#8594;overlay SKIP (self-evident beauty shot, L14 #40 precedent). Post-commit integrity CLEAN (em-dash 27 held, figure 37&#8594;40, img 24&#8594;27, literal `<svg` 35&#8594;37, `<marker` 4, figcaption 37&#8594;40, wrapper-div 22&#8594;24, `color="#222"` 0, 12 section ids no dups, mistakes-card pre-existing em-dash preserved). Blender scene torn down to clean empty base 0/0/0/0/0 + Windows/WSL temps ENOENT-verified; no BlenderMCP debt. Next: standing deferred queue (L05&#8211;L08 browser-QA, L09-F2 / L12 / L13 lesson-body fixes). Prior: **P11 DONE 2026-06-13** (`project` FULLY CLOSED &#8212; #35 + #36), 340,702 &#8594; 351,720 B.

> **ENTRY-STATE FLAG (resolve at P1 plan-lock):** the handoff/session banner cited the baseline as **191,258 B**; a fresh `copy_file_user_to_claude` + `get_file_info` on the real UNC path both read **191,211 B** (47 B lower). The body is verified pristine (0 figures, em-dash 27, 12 ids no dups), so this is a stale banner figure, not a body edit. **191,211 B is the authoritative P0 reference.** Reconcile the session banner at first commit.

> **em-dash baseline: 27** literal U+2014 (all pre-existing lesson-body prose; Python `count('\u2014')` authoritative, `grep -c` miscounts). Hold at 27, add none. middot (U+00B7) 0, `&mdash;` 0, `&#183;` 0 at entry.

---

## 1. Figure registry (40 images)

Filename pattern `images/lesson_15_NN_slug.ext`. Type = hand-coded inline **SVG** vs Blender/composite **PNG**. Overlay column: `c{N}` = PNG annotation-overlay namespace; `{prefix}` = inline-SVG id-prefix; `&#8212;` = none planned (self-evident / no callouts). Section = HTML `id`. Status: all **not produced / not integrated** at kickoff.

The requirements doc's SVG-recommendation list (13 figures: #3 #4 #5 #8 #11 #12 #15 #22 #24 #25 #26 #29 #36) is adopted as the SVG/PNG split below; everything else is PNG (render / annotated screenshot / composite). **DECISIONS** flagged inline are resolved at each phase plan-lock.

| #  | filename slug                          | section            | type | overlay / prefix | status |
|----|----------------------------------------|--------------------|------|------------------|--------|
| 1  | lesson_15_01_lighting_impact_comparison.png | why-lighting-matters | PNG (4-panel composite) | c1 | **PRODUCED / INTEGRATED (P1)** |
| 2  | lesson_15_02_hard_soft_light_comparison.png | why-lighting-matters | PNG (2-panel composite) | c2 | **PRODUCED / INTEGRATED (P1)** |
| 3  | lesson_15_03_color_temperature_scale.svg | light-fundamentals | SVG | `ct` | **PRODUCED / INTEGRATED (P2)** |
| 4  | lesson_15_04_light_units_chart.svg     | light-fundamentals | SVG | `lu` | **PRODUCED / INTEGRATED (P2)** |
| 5  | lesson_15_05_inverse_square_law.svg    | light-fundamentals | SVG | `isq` | **PRODUCED / INTEGRATED (P2)** |
| 6  | lesson_15_06_add_light_menu.png        | adding-lights      | PNG (annotated screenshot) | c6 | **PRODUCED / INTEGRATED (P3)** |
| 7  | lesson_15_07_properties_panel_overview.png | adding-lights  | PNG (annotated screenshot) | c7 | **PRODUCED / INTEGRATED (P3)** |
| 8  | lesson_15_08_point_light_diagram.svg   | point-light        | SVG | `pt` | **PRODUCED / INTEGRATED (P4)** |
| 9  | lesson_15_09_point_light_shadow.png    | point-light        | PNG (render) | c9 (skipped, D7) | **PRODUCED / INTEGRATED (P4)** |
| 10 | lesson_15_10_point_light_settings.png  | point-light        | PNG (annotated screenshot) | c10 | **PRODUCED / INTEGRATED (P4)** |
| 11 | lesson_15_11_sun_light_parallel_rays.svg | sun-light        | SVG | `sr` | **PRODUCED / INTEGRATED (P5)** |
| 12 | lesson_15_12_sun_position_independence.svg | sun-light      | SVG | `spi` | **PRODUCED / INTEGRATED (P5)** |
| 13 | lesson_15_13_sun_time_of_day.png       | sun-light          | PNG (4-panel composite) | c13 | **PRODUCED / INTEGRATED (P5)** |
| 14 | lesson_15_14_sun_light_settings.png    | sun-light          | PNG (annotated screenshot) | c14 | **PRODUCED / INTEGRATED (P5)** |
| 15 | lesson_15_15_spot_light_cone.svg       | spot-light         | SVG | `sc` | **PRODUCED / INTEGRATED (P6)** |
| 16 | lesson_15_16_spot_beam_patterns.png    | spot-light         | PNG (2x3 composite) | c16 | **PRODUCED / INTEGRATED (P6)** |
| 17 | lesson_15_17_spot_aiming_techniques.png | spot-light        | PNG (annotated screenshot) | c17 | **PRODUCED / INTEGRATED (P6)** |
| 18 | lesson_15_18_spot_light_settings.png   | spot-light         | PNG (annotated screenshot) | c18 | **PRODUCED / INTEGRATED (P6)** |
| 19 | lesson_15_19_gobo_pattern_example.png  | spot-light         | PNG (render) | c19 (skipped) | **PRODUCED / INTEGRATED (P6)** |
| 20 | lesson_15_20_area_light_shapes.png     | area-light         | PNG (4-panel composite) | c20 | **PRODUCED / INTEGRATED (P7)** |
| 21 | lesson_15_21_area_size_softness.png    | area-light         | PNG (4-panel progressive) | c21 | **PRODUCED / INTEGRATED (P7)** |
| 22 | lesson_15_22_area_direction_indicator.svg | area-light      | SVG | `ad` | **PRODUCED / INTEGRATED (P7)** |
| 23 | lesson_15_23_area_light_settings.png   | area-light         | PNG (annotated screenshot) | c23 | **PRODUCED / INTEGRATED (P7)** |
| 24 | lesson_15_24_three_point_diagram.svg   | comparing-lights   | SVG | `tp` | **PRODUCED / INTEGRATED (P9)** |
| 25 | lesson_15_25_light_types_table.svg     | comparing-lights   | SVG | `ltt` | **PRODUCED / INTEGRATED (P9)** |
| 26 | lesson_15_26_decision_tree_flowchart.svg | comparing-lights | SVG | `dt` | **PRODUCED / INTEGRATED (P9)** |
| 27 | lesson_15_27_diffuse_specular_split.png | light-properties  | PNG (3-panel composite) | c27 | **PRODUCED / INTEGRATED (P8)** |
| 28 | lesson_15_28_contact_shadows.png       | light-properties   | PNG (2-panel comparison) | c28 | **PRODUCED / INTEGRATED (P8)** |
| 29 | lesson_15_29_custom_distance_sphere.svg | light-properties  | SVG | `cd` | **PRODUCED / INTEGRATED (P8)** |
| 30 | lesson_15_30_product_photo_setup.png   | practical-scenarios | PNG (route-b: SVG diagram + Cycles render, Pillow-composited 2002&#215;1000) | c30 | **PRODUCED / INTEGRATED (P10)** |
| 31 | lesson_15_31_architectural_setup.png   | practical-scenarios | PNG (route-b: SVG diagram + Cycles render, Pillow-composited 2002&#215;1000) | c31 | **PRODUCED / INTEGRATED (P10)** |
| 32 | lesson_15_32_interior_window_setup.png | practical-scenarios | PNG (route-b: SVG diagram + Cycles render, Pillow-composited 2002&#215;1000) | c32 | **PRODUCED / INTEGRATED (P10)** |
| 33 | lesson_15_33_portrait_setup.png        | practical-scenarios | PNG (route-b: SVG diagram + Cycles render, Pillow-composited 2002&#215;1000) | c33 | **PRODUCED / INTEGRATED (P10)** |
| 34 | lesson_15_34_moody_night_setup.png     | practical-scenarios | PNG (route-b: SVG diagram + Cycles render, Pillow-composited 2002&#215;1000) | c34 | **PRODUCED / INTEGRATED (P10)** |
| 35 | lesson_15_35_project_result.png        | project            | PNG (render) | c35 | **PRODUCED / INTEGRATED (P11)** |
| 36 | lesson_15_36_light_icons_guide.svg     | project            | SVG | `li` | **PRODUCED / INTEGRATED (P11)** |
| 37 | lesson_15_37_shadow_comparison_all.png | comparing-lights   | PNG (4-panel composite) | c37 | **PRODUCED / INTEGRATED (P9)** |
| 38 | lesson_15_38_mistake_flat_lighting.png | summary            | PNG (2-panel comparison) | c38 | **PRODUCED / INTEGRATED (P12)** |
| 39 | lesson_15_39_mistake_too_many_lights.png | summary          | PNG (2-panel comparison) | c39 | **PRODUCED / INTEGRATED (P12)** |
| 40 | lesson_15_40_volumetric_example.png    | summary            | PNG (render) | c40 (skipped, D7) | **PRODUCED / INTEGRATED (P12)** |

**Type tally:** 13 SVG (#3 #4 #5 #8 #11 #12 #15 #22 #24 #25 #26 #29 #36) &#183; 27 PNG. **Totals: 40 produced-target / 40 integrated-target.**

**Note on figure #37 section:** the requirements doc places #37 (Shadow Quality Comparison All Light Types) in `comparing-lights` (after the four strengths/weaknesses cards), NOT in `summary`. Registry follows the doc. This pulls #37 into the P9 phase (`comparing-lights`), not the summary phase.

---

## 2. id-prefix registry (inline SVGs)

One short prefix per inline SVG, collision-checked against the live HTML (no existing inline-SVG ids; the 12 `<section>` ids are the only ids present, none of which collide with these). Header pattern from style-guide: `aria-labelledby="{prefix}-title {prefix}-desc"`, `<title id="{prefix}-title">`, `<desc id="{prefix}-desc">`, child ids `{prefix}-*`.

| prefix | figure | slug |
|--------|--------|------|
| `ct`  | #3  | color_temperature_scale |
| `lu`  | #4  | light_units_chart |
| `isq` | #5  | inverse_square_law |
| `pt`  | #8  | point_light_diagram |
| `sr`  | #11 | sun_light_parallel_rays |
| `spi` | #12 | sun_position_independence |
| `sc`  | #15 | spot_light_cone |
| `ad`  | #22 | area_direction_indicator |
| `tp`  | #24 | three_point_diagram |
| `ltt` | #25 | light_types_table |
| `dt`  | #26 | decision_tree_flowchart |
| `cd`  | #29 | custom_distance_sphere |
| `li`  | #36 | light_icons_guide |

All 13 prefixes mutually distinct and distinct from the 12 section ids. (`sc` = spot-cone here; no clash with the L14 `sc` socket-color prefix &#8212; prefixes are per-lesson-scoped.)

---

## 3. c{N} overlay namespace (PNG annotation overlays)

One `c{N}` namespace per PNG that carries callouts; ids namespaced `c{N}-*` (e.g. `c1-badge`, `c17-marker`) per style-guide LD10. 27 PNGs &#8594; up to 27 `c{N}` namespaces (`c1 c2 c6 c7 c9 c10 c13 c14 c16 c17 c18 c19 c20 c21 c23 c27 c28 c30 c31 c32 c33 c34 c35 c37 c38 c39 c40`). N matches the figure number for 1:1 traceability.

**Overlay-skip candidates (DECISION at phase plan-lock):** composite PNGs with baked-in panel dividers may use the composite-overlay variant (centered per-panel badge, no highlight/leader) OR skip the overlay entirely if self-labeling (the L14 #5 / #40, L13 #24 precedents). Likely skip/minimal: #9 (single self-evident shadow), #35 (project result with in-render labels possible), #40 (volumetric beauty shot). Confirm per figure at production; if an overlay is skipped, its `c{N}` is simply unused (tracked via once-each PNG refs, not namespace-id counts).

---

## 4. Phase plan (HTML section order)

Sequenced by live-HTML section order (verified): why-lighting-matters &#8594; light-fundamentals &#8594; adding-lights &#8594; point-light &#8594; sun-light &#8594; spot-light &#8594; area-light &#8594; light-properties &#8594; comparing-lights &#8594; practical-scenarios &#8594; project &#8594; summary.

**OUR conventions (not the requirements doc's 4-week grouping):**
- &lt;5 figures &#8594; single chat (PRODUCTION + INTEGRATION together).
- &#8805;5 figures &#8594; PRODUCTION sub-chat split on the capture-method boundary (hand-coded SVG / UI-chrome screenshot / viewport-render).
- &#8805;6 figures &#8594; INTEGRATION gets its OWN chat.
- plan-lock before any build; bottom-up insertion order at INTEGRATION; warn before context tightens.

| Phase | section | figures | count | type mix | plan |
|-------|---------|---------|-------|----------|------|
| **P1** | why-lighting-matters | #1, #2 | 2 | 2 PNG composite | single chat |
| **P2** | light-fundamentals | #3, #4, #5 | 3 | 3 SVG | single chat (all inline SVG) |
| **P3** | adding-lights | #6, #7 | 2 | 2 PNG UI-chrome | single chat |
| **P4** | point-light | #8, #9, #10 | 3 | 1 SVG + 1 render + 1 UI-chrome | single chat (mixed-method; split sub-chats only if context tightens) |
| **P5** | sun-light | #11, #12, #13, #14 | 4 | 2 SVG + 1 composite + 1 UI-chrome | single chat (mixed-method; watch context &#8212; near the &#8805;5 split line) |
| **P6** | spot-light | #15, #16, #17, #18, #19 | 5 | 1 SVG + 1 composite + 2 UI-chrome + 1 render | **&#8805;5: PRODUCTION sub-chat split** (A: SVG #15 + renders #16/#19 ; B: UI-chrome #17/#18) ; INTEGRATION inline at &lt;6 unless context tightens |
| **P7** | area-light | #20, #21, #22, #23 | 4 | 1 SVG + 2 composite + 1 UI-chrome | single chat (mixed-method; watch context) |
| **P8** | light-properties | #27, #28, #29 | 3 | 2 PNG composite + 1 SVG | single chat |
| **P9** | comparing-lights | #24, #25, #26, #37 | 4 | 3 SVG + 1 composite render | single chat (3 SVG inline + #37 render; #26 is the static twin of the live mermaid &#8212; keep mermaid, add SVG alongside per doc) |
| **P10** | practical-scenarios | #30, #31, #32, #33, #34 | 5 | 5 PNG diagram+render hybrids &#8212; **DEC** | **&#8805;5: PRODUCTION sub-chat split** on sourcing method once **DEC** resolved (Pillow-vignette host vs render-with-overlay vs hybrid) ; INTEGRATION inline at &lt;6 unless context tightens |
| **P11** | project | #35, #36 | 2 | 1 render + 1 SVG | single chat |
| **P12** | summary | #38, #39, #40 | 3 | 2 PNG comparison + 1 render | single chat |

**No phase reaches &#8805;6**, so no INTEGRATION-own-chat is pre-mandated. P6 and P10 (both 5) pre-plan a PRODUCTION sub-chat split; their INTEGRATION stays inline unless context tightens mid-phase (warn Ray first). All counts and the section split sum to 40.

---

## 5. DECISIONS to resolve at phase plan-lock

- **D1 (P1, #1/#2):** lighting composites &#8212; pure Blender multi-render Pillow composite (4-panel for #1, 2-panel for #2), or Pillow-vignette host? Lean: real Cycles renders of one shared test scene (Suzanne / sphere-on-plane), Pillow-composited with `#FF6B00` 2px dividers, overlays at INTEGRATION. Shadow-softness in #2 needs Cycles (area-vs-point source-size). Confirm camera/scene reuse for cross-figure consistency.
- **D2 (P2, #3 color-temperature):** the gradient bar is a content-colored gradient (warm&#8594;cool Kelvin), NOT brand palette &#8212; this is legitimate content color (like L14 socket hues / axis colors), brand orange stays the top-strip accent only. Confirm the Kelvin stop hexes at plan-lock (warm-orange&#8594;white&#8594;cool-blue); the doc's `#FF9E4A/#FFD700/#FFFFFF/#F0F8FF/#87CEEB` are loose guidance.
- **D3 (P2, #4 light-units):** 3-column infographic &#8212; brand palette governs the column accents (NOT the doc's warm-orange/yellow/cool-white). Pick 3 distinct brand roles (e.g. orange `#FF6B00` / blue `#4A9EFF` / purple `#B47EE8`). 800-wide or wider canvas (table content) &#8212; confirm viewBox (likely 800x560 or the 800x680 dense variant).
- **D4 (P5, #12 + P9 #26):** both are multi-view / flowchart SVGs that may exceed the 560 height &#8212; confirm 800x680 dense variant vs custom height at plan-lock.
- **D5 (P9, #26 decision-tree):** the requirements doc explicitly wants a static SVG twin of the EXISTING live mermaid decision tree (`comparing-lights`, line ~1980). Keep the mermaid block intact; insert #26 SVG as the static alternative per the doc's placement note. No mermaid edit. Confirm whether the SVG mirrors the mermaid's exact branch logic.
- **D6 (P10, #30&#8211;#34 scenario setups):** each is a "diagram + inset render" hybrid &#8212; resolve sourcing: (a) hand-coded SVG setup diagram + small embedded render, (b) Pillow composite of a top-down/section diagram beside a Cycles render, or (c) overlay-host vignette. These are the doc's lowest-fidelity "AI-prompt" figures; brand palette governs all diagram linework/labels. This is the biggest open block &#8212; lock the method before P10 PRODUCTION. Likely route: Blender render for the inset + hand-coded SVG diagram half, composited; overlay labels at INTEGRATION.
- **D7 (overlay-skip set):** confirm #9 / #35 / #40 overlay-skip-or-minimal at their production (see &#167;3). Self-evident renders skip overlays (L13 #24 / L14 #5/#40 precedent).
- **D8 (#37 placement):** registry + P9 follow the requirements doc placing #37 in `comparing-lights` (after the strengths/weaknesses cards), not `summary`. Confirm at P9 plan-lock.
- **D9 (color-dependent renders):** any render whose teaching point is a marker/emission/shadow color (e.g. #13 time-of-day warm/cool, #34 moody blue/orange) invokes the v6 LD17 recipe (F12 `render.render`, Standard view transform) ONLY if a PRODUCTION review shows AgX washing &#8212; not pre-forced. Plain shadow-shape renders stay on the default path.

---

## 6. Status log

### 2026-06-13 &#8212; L15 P12 DONE (`summary` FULLY CLOSED &#8212; #38 flat-lighting + #39 too-many-lights mistake comparisons + #40 volumetric beam; single-chat PRODUCTION + INTEGRATION) &#8212; **L15 FIGURE-COMPLETE 40/40**
- **40/40 produced / 40/40 integrated &#8212; `summary` section complete; LESSON 15 FIGURE-COMPLETE.** HTML **351,720 (P0) &#8594; 356,187 B** (+4,467). All 3 figs inserted at a single count=1 anchor in `summary` (mistakes-card `</div>` &#8594; Next-Steps card), document order #38 &#8594; #39 &#8594; #40.
- **Entry-state PASSED** on fresh `copy_file_user_to_claude`: **351,720 B** authoritative P0, em-dash 27, `&mdash;` 0, `color="#222"` 0, figure 37/37, img 24, literal `<svg` 35, `<marker` 4, figcaption 37/37, wrapper-div 22 (one #35 wrapper uses tight spacing &#8212; tolerant regex `position:\s*relative;\s*display:\s*inline-block` is authoritative = 22; exact-string count of 21 is the known false-low), 12 section ids no dups.
- **#40** `lesson_15_40_volumetric_example.png` (1,357,141 B WSL, 1920&#215;1080): Cycles spot light + volume-scatter fog domain (density 0.02, anisotropy 0.30), visible beam shaft on a lit sphere. Dark world. AgX held per D9 (lum mean 36, max 221 unclipped, no wash). Fog-domain horizon-seam fixed (domain base lowered below floor) on re-render. **D7 &#8594; overlay SKIPPED** (self-evident beauty shot, L14 #40 precedent) &#8212; plain figure (img + figcaption, no wrapper/overlay). `c40` unused.
- **#38** `lesson_15_38_mistake_flat_lighting.png` (1,127,610 B WSL, 2002&#215;1000): 2-panel, flat front light at camera axis (BAD, washed/dimensionless) vs 45&#176; key (GOOD, form-revealing). Suzanne subject (default orientation = faces -Y camera, no rotation; two earlier rotation attempts tumbled her &#8212; reset to identity fixed it), Cycles, AgX. Pillow composite, `#FF6B00` 2px divider, NO baked labels. Overlay `c38`: LD8 red `#E63946` WRONG (left pill) / green `#4ADE80` CORRECT (right pill), composite-pill (NO `c38-*` ids, NO leaders). cairosvg-over-PNG pixel-verified 1:1 (LD19).
- **#39** `lesson_15_39_mistake_too_many_lights.png` (1,165,556 B WSL, 2002&#215;1000): 2-panel, 6 hard saturated competing lights (BAD, conflicting colored shadows + color pollution + muddied form) vs clean 3-point key/fill/rim (GOOD). First bad pass too gentle &#8212; strengthened (harder/smaller lights, saturated RGB) for marginal-read before composite. Same subject/divider/overlay pattern as #38; `c39` WRONG/CORRECT, pixel-verified.
- **Commit:** single `edit_file` (all 3 figs share one anchor; cleaner than 3 same-point edits) dryRun &#8594; commit &#8594; `get_file_info` 356,187 B confirmed.
- **POST-COMMIT INTEGRITY SWEEP CLEAN** (live 356,187, Python-authoritative): bytes 356,187 &#183; em-dash **27 held** &#183; `&mdash;` 0 &#183; `color="#222"` 0 &#183; figure 37&#8594;**40/40** &#183; img 24&#8594;**27** (all 3 PNG) &#183; literal `<svg` 35&#8594;**37** (#38/#39 overlays; #40 none) &#183; `<marker` **4** unchanged &#183; figcaption 37&#8594;**40/40** &#183; wrapper-div 22&#8594;**24** (#38/#39; #40 plain adds 0) &#183; 12 section ids no dups &#183; #38/#39/#40 PNGs referenced once each &#183; c38/c39/c40 namespace ids **0** (composite/plain, expected) &#183; mistakes-card pre-existing em-dash (`form&#8212;they matter`) preserved.
- **Blender:** torn down to clean empty base 0/0/0/0/0 (objects/meshes/mats/lights/cams), orphan-purged, engine restored `BLENDER_EEVEE` + view-transform AgX. Windows tempdir `C:\Users\Public\L15P12` rmtree'd + 4 WSL `_tmp_l15_p12_*.png` review temps deleted, all ENOENT-verified. No BlenderMCP debt.
- **Decisions exercised:** D7 (#40 overlay skip), D9 (#40 AgX hold), LD8 (WRONG/CORRECT colorway), LD10 (PNG-overlay wrapper for #38/#39), LD19 (cairosvg pixel-verify). LD21 warning-vignette considered for #38/#39 but NOT used (real render comparisons, plain 2-panel + pills is the #27/#28 precedent).
- **NEXT:** L15 done. Standing deferred queue per session.md: L05&#8211;L08 browser-QA, L09-F2 / L12 / L13 lesson-body fixes.

### 2026-06-13 &#8212; L15 P11 DONE (`project` FULLY CLOSED &#8212; #35 project_result PNG render + `c35` 4-pill minimal overlay + #36 `li` light_icons_guide inline SVG; single-chat PRODUCTION + INTEGRATION)
- **37/40 produced / 37/40 integrated &#8212; `project` section complete.** HTML **340,702 (P0) &#8594; 351,720 B** (+11,018). Byte trail (bottom-up commits #36&#8594;#35): #36 inline SVG +8,577 &#8594; 349,279 ; #35 PNG-overlay +2,441 &#8594; 351,720 (each `get_file_info`-confirmed live).
- **Entry-state PASSED** on fresh `copy_file_user_to_claude` + `get_file_info`: **340,702 B** authoritative P0, em-dash 27, `&mdash;` 0, `color="#222"` 0, figure 35/35, img 23, literal `<svg` 33, `<marker` 4, figcaption 35/35, wrapper-div 21, 12 section ids no dups. Blender scene verified clean empty base 0/0/0 at entry (no STOP).
- **P11 PLAN-LOCK decisions (Ray-confirmed):** #35 render engine = **Cycles** (accurate soft-shadow penumbra for the Area sphere); **D7 &#8594; minimal `c35` overlay, NOT skip** &#8212; the requirements doc wants baked Point/Sun/Spot/Area labels but locked rules forbid baked labels on renders; the 4 spheres genuinely need disambiguating (unlike the self-evident #40 beauty shot), so 4 minimal pills beat a full skip. **D9 (only #35-relevant via shadow color) &#8594; AgX HELD** (luminance probe: global mean 127, max 249.9, min 1.1 &#8212; full range, no wash; LD17 Standard NOT invoked).
- **#35** `images/lesson_15_35_project_result.png` &#8212; **1,279,450 B**, 1920&#215;1080 RGB. Cycles GPU 200spp+denoise, AgX. Four light-grey spheres (radius 1, roughness 0.35) in a row on a dark-grey plane (enlarged to fill frame, no black band), each lit primarily by its own light over its sphere: **Point** (energy 900, soft-size 0.1 &#8594; hard shadow directly below), **Sun** (energy 2.6, angle 1.2&#176;, raked &#8594; parallel angled shadow), **Spot** (energy 1500, spot_size 45&#176; blend 0.25 &#8594; circular pool + cone-confined shadow), **Area** (square size 4, energy 900 &#8594; soft gradual penumbra). Camera 3/4 angle (lens 40, loc (0,-18.5,7.4), rot 71&#176;); all 4 sphere NDC centers x 0.159/0.386/0.614/0.841 y ~0.497, evenly spaced in-frame (re-framed twice: outer spheres were clipped at the first two attempts). Rendered to Windows tempdir &#8594; Pillow-relay to WSL &#8594; review (NO baked labels). **NO washing &#8594; AgX kept.** Overlay `c35` = 4 minimal composite-style pills (Point/Sun/Spot/Area, `#1a1a1a` fill-opacity 0.82, white fs26, 140&#215;46 rx23 at y=250, centered on each sphere x = 305/741/1179/1615), viewBox 0 0 1920 1080, NO `c35-*` ids (composite-pill pattern), cairosvg-rasterized at 1920&#215;1080 + alpha-composited over the real PNG (LD19, 1:1) and legibility-verified before commit &#8212; pills sit in the clear plane area above each sphere, clear of all shadows.
- **#36** `images/lesson_15_36_light_icons_guide.svg` &#8212; **6,611 B**, inline-SVG prefix `li`, viewBox 800&#215;680. 2&#215;2 reference card (dark `#1b1d22` self-contained background, brand `#FF6B00` icon accents + white rays + `#4A9EFF` emission-direction arrows): **Point** (ring + dot + 8 radiating rays = omni), **Sun** (ring + long even rays + down direction arrow), **Spot** (emitter dot + filled cone + pool ellipse), **Area** (tilted plane parallelogram + normal arrow). Each quadrant: name + subtitle + 3 `&#183;`-bullet characteristics. Footer legend explains the blue arrow. cairosvg light+dark render-checked; right-column text overflow fixed (moved x 600&#8594;560 + shortened 3 lines to fit viewBox). Self-assert: em-dash 0, `&mdash;` 0, 12 `&#183;`, 1 svg/title/desc, role+aria, 6 `li-*` ids. Standalone KEEPS `color="#222"` &#8212; STRIPPED on inlining. Brand palette governs (the spec's `#ff8c00`/`#2b2b2b` are loose guidance; the card uses its own dark fill so it reads identically on any page background).
- **Anchors (both count=1 on the fresh copy, by unique TEXT span):** #35 after the Project Goal card `</div>`, before `<h3>Step 1: Scene Setup</h3>`. #36 after the "&#127916; Prepare Your Scene" (Step 1) card `</div>`, before `<h3>Step 2: Add Point Light</h3>`. Document order #35 &#8594; #36, so **bottom-up insert = #36 first, then #35.** Each `edit_file` dryRun &#8594; commit &#8594; `get_file_info`.
- **#36 inline SVG** per LD12: root `color="#222"` STRIPPED, root tag + 2 multi-line `<path>` tags collapsed, re-indented (figure col20 / svg col24 / children col28 / `<g>` col32), structural comments kept (decorative `=====` pruned to short labels), `li-*` ids + role/aria/title/desc preserved. **#35 PNG-overlay** per LD10: wrapper-div (`position:relative; display:inline-block; max-width:100%`) + img (`display:block; max-width:100%; height:auto; margin:0; loading=lazy`) + inlined overlay-SVG (`position:absolute; inset:0; width:100%; height:100%; pointer-events:none`, viewBox = PNG px), figcaption OUTSIDE. `c35` carries NO `c{N}-*` ids (composite-pill). Figcaptions comma/colon/period, NO em-dash.
- **POST-COMMIT INTEGRITY SWEEP CLEAN** (fresh `copy_file_user_to_claude` + Python `count()` authoritative on the live **351,720 B** file): bytes 351,720 ; **em-dash 27** held (`&mdash;` 0) ; `color="#222"` 0 ; figure 35&#8594;**37/37** ; img 23&#8594;**24** (#35 PNG; #36 inline adds 0) ; literal `<svg` 33&#8594;**35** (#36 inline + c35 overlay) ; `<marker` **4** unchanged (no markers this phase) ; figcaption 35&#8594;**37/37** ; wrapper-divs 21&#8594;**22** (#35 only; #36 inline adds 0) ; 12 section ids no dups ; `li-*` 6 ; #35 PNG referenced once ; #36 inlined (0 file ref) ; pre-existing Bonus-Challenges em-dash preserved verbatim ("10m sizes&#8212;compare shadows"). (`&#183;` rose by #36's 12 + figcaptions use commas/colons not middots.)
- **Blender teardown DONE:** all `L15_P11_*` objects (Plane / 4 Spheres / Point / Sun / Spot / Area lights / Camera) + meshes/materials/light-data/camera-data removed via `bpy.data.objects.remove`, orphans purged, restored BLENDER_EEVEE (this build has no EEVEE_NEXT enum) + AgX/look None + world 0.05 str 1.0, cursor origin. Verified clean empty base 0/0/0/0/0. Windows `C:\Users\Public\L15P11` deleted (`isdir` False) ; WSL `_tmp_l15_p11_r35.png` review temp deleted (ENOENT-verified). NO `_overlay_VERIFIED.svg` artifacts (c35 + #36 checks ran Claude-side via cairosvg only). No BlenderMCP debt.
- **Next: L15 P12** (`summary`, #38 mistake_flat_lighting + #39 mistake_too_many_lights + #40 volumetric_example, 3 figs, single chat). #38/#39 are before/after comparison PNGs (warning-vignette LD21 candidate hosts &#8212; resolve at plan-lock); #40 is a volumetric beauty render (D7 overlay-skip candidate &#8212; the L14 #40 precedent). NEEDS BlenderMCP. After P12: **L15 figure-complete 40/40.**

### 2026-06-13 &#8212; L15 P10 DONE (`practical-scenarios` FULLY CLOSED &#8212; #30 product_photo_setup + #31 architectural_setup + #32 interior_window_setup + #33 portrait_setup + #34 moody_night_setup, all 5 route-b composites + `c30`&#8211;`c34` overlays; INTEGRATION-only close)
- **35/40 produced / 35/40 integrated &#8212; `practical-scenarios` section complete.** HTML **334,099 (P0) &#8594; 340,702 B** (+6,603). Byte trail (bottom-up commits #34&#8594;#33&#8594;#32&#8594;#31&#8594;#30): #34 +1,352 &#8594; 335,451 ; #33 +1,313 &#8594; 336,764 ; #32 +1,320 &#8594; 338,084 ; #31 +1,318 &#8594; 339,402 ; #30 +1,300 &#8594; 340,702 (each `get_file_info`-confirmed live).
- **STEP-1 TRANSFER RESOLVED WITHOUT REBUILD:** the P10-PRODUCTION entry flagged transfer-to-WSL as the open blocker. At this chat's start the 5 finals were ALREADY on WSL `images/` (Windows-copied between chats &#8212; each carried an NTFS `Zone.Identifier` ADS, the tell of a drag/drop copy). Verified three ways before trusting them: (a) byte-exact match to the PRODUCTION record (737,225 / 775,118 / 787,209 / 782,471 / 793,974), (b) decode-validated via Pillow (all 2002&#215;1000 RGB, non-blank, per-figure mean-luminance sensible: #32 interior brightest 94, #34 moody darkest 48), (c) visual review of all 5. No rebuild from d30&#8211;d34 SVG sources was needed; route-(0) effectively landed via the Windows copy. (The `.pngZone.Identifier` streams are not reachable as standalone files through the `os` layer over UNC [`os.path.exists` False]; harmless, left in place.)
- **Overlays `c30`&#8211;`c34`:** the route-b diagram halves are exhaustively self-labeling hand-coded brand-palette linework (title, light roles KEY/FILL/RIM/etc., power specs all baked into the SVG diagram), and there is NO consistent clear spot for a left-half caption across all five (#30/#33 have a Camera glyph dead-center-bottom of the diagram half). So `c{N}` = a SINGLE minimal composite-style caption pill **"Rendered result"** in the RENDER half, bottom-center (rect x1351 y932 w300 h52 rx26 `#1a1a1a` fill-opacity 0.82, white fs28 text x1501 y958), viewBox 0 0 2002 1000. NO `c{N}-*` ids (composite-pill pattern, uniqueness tracked via once-each PNG refs). All 5 cairosvg-rasterized at 2002&#215;1000 + alpha-composited over the real PNGs (LD19, 1:1) and legibility-checked on both the light-floor (#30) and dark-plinth (#34) cases before any commit.
- **Placements (all anchors count=1 on the fresh copy, by unique TEXT span):** each figure inserted after its scenario card's `</div>`, before the next `<h3>` (default section depth, figure col20). #30 after Scenario-1 card before `<h3>Scenario 2: Outdoor Architectural Exterior</h3>`; #31 after Scenario-2 card before `<h3>Scenario 3: Interior Room with Window Light</h3>`; #32 after Scenario-3 card before `<h3>Scenario 4: Portrait/Character Lighting</h3>`; #33 after Scenario-4 card before `<h3>Scenario 5: Moody/Cinematic Night Scene</h3>`; **#34 after the Scenario-5 CONTENT card `</div>`, before the "&#9989; Universal Lighting Setup Tips" card** (so #34 sits with Scenario 5's content, not after the universal-tips card; the practical-scenarios `</section>` follows the tips card). Bottom-up insert order #34&#8594;#33&#8594;#32&#8594;#31&#8594;#30. Each `edit_file` dryRun &#8594; commit &#8594; `get_file_info`. Figcaptions comma/period, NO em-dash.
- **PNG-overlay pattern** per LD10: wrapper-div (`position:relative; display:inline-block; max-width:100%`) + img (`display:block; max-width:100%; height:auto; margin:0; loading=lazy`) + inlined overlay-SVG (`pointer-events:none`, viewBox = PNG px), figcaption OUTSIDE.
- **POST-COMMIT INTEGRITY SWEEP CLEAN** (fresh `copy_file_user_to_claude` + Python `count()` authoritative on the live **340,702 B** file): bytes 340,702 ; **em-dash 27** held (`&mdash;` 0) ; `color="#222"` 0 ; figure 30&#8594;**35/35** ; img 18&#8594;**23** ; literal `<svg` 28&#8594;**33** (+5 overlay svgs) ; `<marker` **4** unchanged (no markers this phase) ; figcaption 30&#8594;**35/35** ; wrapper-divs 16&#8594;**21** ; 12 section ids no dups ; all 5 scenario anchors count=1 ; #30&#8211;#34 PNGs each referenced once ; 5 "Rendered result" pills ; both pre-existing section em-dashes preserved verbatim (Scenario-4 "separation&#8212;don't skip it", Scenario-5 "first try&#8212;keep refining"). (`&#183;` / `&#8211;` / `&#8722;` unchanged &#8212; figcaptions + pills use no middots/en-dashes.)
- **Blender:** no production this chat (INTEGRATION-only). `get_scene_info`-equivalent re-confirmed clean empty base 0/0/0/0/0, BLENDER_EEVEE + AgX. 5 WSL `_tmp_l15_p10_r30..r34.png` render insets deleted + ENOENT-verified (still-present = []). No `_overlay_VERIFIED.svg` artifacts (cairosvg checks ran Claude-side only). No BlenderMCP debt.
- **Next: L15 P11** (`project`, #35 project_result PNG render + #36 `li` light_icons_guide SVG, 2 figs, single chat). NEEDS BlenderMCP (#35 render). D7 overlay-skip set includes #35 (confirm skip-or-minimal at production). After P11: P12 (`summary` #38/#39/#40, 3 figs) &#8212; L15 then figure-complete 40/40.

### 2026-06-13 &#8212; L15 P10 PRODUCTION SUB-CHAT A (`practical-scenarios` #30&#8211;#34 all 5 built + verified Claude-side; D6 + D9 resolved; **TRANSFER-TO-WSL BLOCKED &#8212; finals NOT yet on `images/`**)
- **D6 RESOLVED &#8594; route (b):** each #30&#8211;#34 = hand-coded brand-palette SVG setup diagram (LEFT) + Cycles render inset (RIGHT), Pillow-composited into ONE 2002&#215;1000 PNG with a `#FF6B00` 2px divider. `c30`&#8211;`c34` label overlays deferred to INTEGRATION. (Rejected (a) inline-SVG+embedded-render byte bloat; (c) vignette-host reserved for #38/#39.) Layout diagram-LEFT/render-RIGHT for all five (top-down-diagram/portrait-render for #33).
- **D9 (only #34 moody blue/orange) RESOLVED &#8594; AgX HELD, LD17 Standard NOT invoked.** PRODUCTION review + luminance probe showed clear warm/cool separation (right practical +41 warmth vs left moon +10.5, mean 74.6 low-key, max 234 no clip); visually distinct cool/warm across the face. No washing &#8594; default AgX path kept.
- **All 5 final composites BUILT + visually verified Claude-side** (each 2002&#215;1000, diagram+render share the dark theme, orange divider clean, legible) and copied to `/mnt/user-data/outputs/`: `lesson_15_30_product_photo_setup.png` 737,225 B ; `lesson_15_31_architectural_setup.png` 775,118 B ; `lesson_15_32_interior_window_setup.png` 787,209 B ; `lesson_15_33_portrait_setup.png` 782,471 B ; `lesson_15_34_moody_night_setup.png` 793,974 B. **ZERO em-dash glyphs in all 5 diagram SVGs** (Python `count('\u2014')` = 0 each; used `&#183;` middots, fixed #34's accidental em-dash).
- **Diagram halves (Claude-side `/home/claude/p10/d30.svg`&#8211;`d34.svg`), each cairosvg light+dark verified + collision-fixed:** d30 product top-down (KEY orange front-R / FILL blue front-L 4&#215;4m / RIM purple behind / BG yellow opt / camera bottom) ; d31 architectural elevation (building+windows, SUN orange ~35&#176; raking parallel rays, HDRI dome arc blue, sky-fill band) ; d32 interior cross-section (room cutaway, WINDOW orange area / SKY FILL blue / BOUNCE yellow / practical purple) ; d33 portrait top-down (head silhouette, KEY/FILL/RIM at labeled angles, camera, key:fill ratio note) ; d34 moody night (dark `#0e0f12` bg, MOONLIGHT blue crescent dim / PRACTICAL orange strong / RIM purple / NO-FILL red dashed / high-contrast band).
- **Render insets** (Cycles GPU 160&#8211;200spp+denoise, 1000&#215;1000, AgX, render&#8594;Windows-tempdir&#8594;Pillow-relay to WSL `images\_tmp_l15_p10_r3N.png`&#8594;`copy_file_user_to_claude`&#8594;review): #30 product (cylinder+sphere-cap proxy, 4 area lights; first render washed/cap-cropped &#8594; camera pullback `(0,-8.6,2.6)` lens80 + power &#8595;~45% + deeper material `(0.42,0.07,0.08)` &#8594; clean, max248) ; #31 architectural (stepped building+window quads+Sun; first too flat &#8594; raked sun low `(62&#176;,-18&#176;,-58&#176;)` energy3.0 &#8594; bright facade + window grid + long ground shadow, cool-sky ambient) ; #32 interior (room box+sphere+table+window area+emissive sky panel; first render had broken-geometry black void &#8594; rebuilt solid left wall + bright emissive `WinPanel` strength6.0 in the gap &#8594; clean window-lit interior, soft directional shadows) ; #33 portrait (head+neck+shoulders+hair-crown proxy, 3-point key520/fill175/rim470; reframed `(0,-6.6,2.15)` lens90 + deeper skin `(0.42,0.28,0.22)` &#8594; correct 3-point modeling + rim separation) ; #34 moody (bust proxy, moonlight SUN0.45 cool, warm PRACTICAL area420, cool RIM, no fill; AgX held per D9). All 5 render PNGs resident in WSL `images\_tmp_l15_p10_r30..r34.png` (656,502 / 711,310 / 747,954 / 724,331 / 731,746 B) &#8212; **RETAINED for the final composite step.**
- **&#9888; OPEN BLOCKER &#8212; finals NOT written to WSL `images/`.** The 5 composites exist Claude-side (`/home/claude/p10/` + `/mnt/user-data/outputs/`) but are NOT yet on the real WSL `images/`. Transfer mechanics are the snag: `Filesystem:write_file` is text-only + cannot read `/home/claude`; the BlenderMCP bridge writes UNC via Pillow but cannot reach Claude-side files + has NO cairosvg; inlining ~98KB b64 per figure into a bridge `code` call is impractical/truncation-prone. b64 transfer files staged Claude-side (`/home/claude/p10/`): full-res `b64full_3N.txt` (~98KB each) + diagram-only `dl64_3N.txt` (re-rasterized `_full_3N.png` 1000px, ~98KB) + 720px `m64_3N.txt` (~68KB) + 560px `dlow64_30.txt` (~52KB).
- **NEXT-CHAT RESOLUTION (pick one, then INTEGRATE):** (1) `Filesystem:write_file` each diagram-half b64 to a WSL `.b64` text file, then ONE bridge call decodes all 5 + Pillow-composites with the resident `_tmp_l15_p10_r3N.png` renders + writes `images\lesson_15_3N_<slug>.png` ; OR (2) per-figure bridge call with the diagram-half b64 inlined (bridge decodes+composites+writes). Render PNGs already resident &#8594; bridge re-uses them. After write, `get_file_info`-verify the 5 byte counts (expect ~737K/775K/787K/782K/794K). [A test stub `_dl64_30.txt` was written to WSL then REMOVED this chat &#8212; do not look for it.]
- **Blender teardown DONE:** scene RESTORED to clean empty base (0 obj / 0 mesh / 0 mat / 0 light / 0 cam; orphans purged), BLENDER_EEVEE + AgX/look None + world `(0.05)` str 1.0, cursor origin. Windows `C:\Users\Public\L15P10` (5 render scratch) deleted + `os.path.exists` False. **WSL `_tmp_l15_p10_r30..r34.png` RETAINED** (needed to composite the finals next chat) &#8212; delete them only AFTER the finals are written + verified. No `_overlay_VERIFIED.svg` artifacts.
- **HTML UNTOUCHED at 334,099 B = safe resume P0** (PRODUCTION-only sub-chat; INTEGRATION not started). 30/40 integrated unchanged; #30&#8211;#34 produced-but-not-on-WSL (pipeline incomplete pending transfer).
- **INTEGRATION TODO (next chat, after finals land on WSL):** fresh `copy_file_user_to_claude` + re-verify 334,099 B (em-dash 27). Anchors (all confirmed count=1 this chat): the five `<h3>Scenario N: ...</h3>` &#8212; "Scenario 1: Product Photography Setup" / "Scenario 2: Outdoor Architectural Exterior" / "Scenario 3: Interior Room with Window Light" / "Scenario 4: Portrait/Character Lighting" / "Scenario 5: Moody/Cinematic Night Scene"; practical-scenarios `</section>` ends before `id="project"`; the last Scenario-5 `<li>` has a PRE-EXISTING em-dash ("never 'done' on first try&#8212;keep refining!") &#8212; PRESERVE verbatim. Bottom-up insert **#34&#8594;#33&#8594;#32&#8594;#31&#8594;#30**. Each `edit_file` dryRun&#8594;commit&#8594;`get_file_info`. Build + cairosvg-pixel-verify `c30`&#8211;`c34` overlay coords (LD19) BEFORE commit; PNG-overlay pattern (wrapper-div + img margin:0 + inlined overlay-SVG viewBox=2002&#215;1000, figcaption OUTSIDE). Post-commit sweep (em-dash hold 27, figure/img/svg/figcaption/wrapper-div balanced, `color="#222"` 0, 12 section ids no dups, anchors count=1) + doc/banner updates.
- **Next: L15 P10 transfer-resolution + INTEGRATION** (write 5 finals to WSL `images/`, then integrate #30&#8211;#34 inline at &lt;6 unless context tightens &#8212; warn Ray first). NEEDS BlenderMCP (transfer composite) + Filesystem.

### 2026-06-13 &#8212; L15 P9 DONE (`comparing-lights` FULLY CLOSED &#8212; #24 `tp` three_point_diagram inline SVG + #37 shadow_comparison_all PNG composite + `c37` 4-pill + #26 `dt` decision_tree_flowchart inline SVG + #25 `ltt` light_types_table inline SVG; mixed-method, 3 SVG inline + 1 composite render)
- **30/40 produced / 30/40 integrated &#8212; `comparing-lights` section complete.** This INTEGRATION close committed the final two figures (#26 + #25) bottom-up; #24 + #37 were committed earlier in the phase (prior sub-chat). Byte trail this close: **311,393 (P0, #24+#37 already present) &#8594; #26 inline SVG +9,763 &#8594; 321,156 &#8594; #25 inline SVG +12,943 &#8594; 334,099** (each `get_file_info`-confirmed live).
- **ENTRY-STATE RECONCILED:** the resume note projected P0 = 308,946 B, but the live file read **311,393 B** at the start of this close (+2,447). The note explicitly flagged 308,946 as an estimate and instructed re-reading the live count; **311,393 was the true P0** (post-#24+#37 state, fully balanced: figure 28/28, img 18, figcaption 28/28, literal `<svg` 26, `<marker` 3, viewBox 29, em-dash 27, `tp-` present, `c37` 0 because #37 uses the composite-pill pattern with NO `c37-*` ids). No lost or double commit; file intact (clean doctype/`</html>`, 12 section ids no dups).
- **P9 DECISIONS (locked, confirmed):** D4 #26/#25 = 800&#215;680 dense; D5 #26 KEEPS the live mermaid + adds the static SVG twin alongside (no mermaid edit); D8 #37 in `comparing-lights` (not summary); #37 AgX no-wash; #37 KEEPS the `c37` 4-pill overlay. Brand palette governs overlays/SVG; content/semantic colors (table cells green `#1F8A4C` / amber `#B8860B` / red `#C0392B`, mermaid-twin outcome fills `#FFD93D`/`#6BCF7F`/`#FF6B9D`/`#4D96FF`) are legitimate exceptions.
- **#24** `images/lesson_15_24_three_point_diagram.svg` (5,792 B, inline `tp`, viewBox 800&#215;560, 1 `<marker>` tp-arrow, 7 `&#183;`) committed earlier inside the "&#127912; Effective Multi-Light Setups" card (deeper card depth). **#37** `images/lesson_15_37_shadow_comparison_all.png` (1,897,909 B, 1802&#215;1802) committed earlier as a PNG-overlay (default depth) after the Area Light card, before `<h3>Common Lighting Combinations</h3>`; `c37` = 4-pill composite (Point/Sun/Spot/Area &#183; shadow descriptors, `#1a1a1a` fill-opacity 0.82, fs30 white, `&#183;` seps, viewBox 0 0 1802 1802), NO `c37-*` ids, cairosvg-pixel-verified 1:1 before commit (LD19 PASSED).
- **#26** `images/lesson_15_26_decision_tree_flowchart.svg` (6,891 B, inline `dt`, viewBox 800&#215;680, 1 `<marker>` dt-arrow): static twin of the live mermaid decision tree, mirroring its exact branch logic (outdoor &#8594; Sun, soft shadows &#8594; Area, focused beam &#8594; Spot, small omni &#8594; Point, default Area). Outcome fills match the mermaid (`#FFD93D`/`#6BCF7F`/`#FF6B9D`/`#4D96FF`); edge labels green/red. Inserted after the mermaid `</div>`, before `<h3>Light Type Strengths and Weaknesses</h3>` (count=1). Root `color="#222"` STRIPPED, re-indented figure col16 / svg col20 / children col24 / `<g>` col28, `dt-*` ids + `<marker>` def kept. cairosvg light+dark render-checked OK.
- **#25** `images/lesson_15_25_light_types_table.svg` (9,503 B, inline `ltt`, viewBox 800&#215;680, NO marker): 9-row scan-friendly comparison of the four light types, content-semantic cell colors (green advantage / amber neutral / red limitation), 1 `&#183;` subtitle + 2 `&#8211;` range dashes (Hard&#8211;Medium, Low&#8211;Medium). Inserted after the Quick Comparison Table card `</div>` (the "&#128202; Light Types at a Glance" `<table>`), before `<h3>Decision Tree: Which Light to Use?</h3>` (count=1). Root `color="#222"` STRIPPED, re-indented, `ltt-*` ids + `ltt-ic-*` defs kept. cairosvg light+dark render-checked OK. Document order #25 &#62; #26, so bottom-up insert = #26 first, #25 last.
- Each `edit_file` dryRun &#8594; commit &#8594; `get_file_info`. Figcaptions comma/period, NO em-dash.
- **POST-COMMIT INTEGRITY SWEEP CLEAN** (fresh `copy_file_user_to_claude` + Python `count()` authoritative on the live **334,099 B** file): bytes 334,099 ; **em-dash 27** held (`&mdash;` 0) ; `color="#222"` 0 ; figure 28&#8594;**30/30** ; img **18** (only #37 PNG; #24/#25/#26 inline add none) ; figcaption 28&#8594;**30/30** ; literal `<svg` 26&#8594;**28/28** (24 base + #24/#25/#26 inline 3 + #37 overlay 1) ; `<marker` 3&#8594;**4** (#24 tp-arrow + #26 dt-arrow + 2 base) ; **viewBox 29&#8594;32** (ENUMERATED live: 28 `<svg>` + 4 `<marker>`) ; wrapper-divs **16** (only #37; the 3 inline SVGs add none) ; **`&#183;` 44&#8594;45** (#25 subtitle +1; #26 adds 0; live recount) ; literal middot 2 ; **`&#8211;` 4&#8594;6** (#25's 2 range dashes) ; `&#8722;` 5 ; 12 section ids no dups ; `tp-`/`ltt-`/`dt-` present ; `c37-` 0 (composite-pill, NO c{N}-* ids) ; #37 PNG referenced once, #24/#25/#26 inline (0 file ref each).
- **mermaid string-count reconciliation:** raw `count('mermaid')` went 5&#8594;**6**, but the actual `<div class="mermaid">` diagram block count is UNCHANGED at 1 (live mermaid intact per D5). The 6th occurrence is the KEPT structural comment `<!-- OUTCOME boxes (colors match the live mermaid fills) -->` inside #26's inline SVG &#8212; benign, not a new diagram.
- **No Blender teardown owed** &#8212; P9 production closed in a prior chat; this was an INTEGRATION-only close. `get_scene_info` re-confirmed clean empty base (0 objects / 0 materials). No `_tmp_l15_p9_*` temps created this chat; no BlenderMCP debt. No `_overlay_VERIFIED.svg` artifacts (both inline SVGs verified Claude-side via cairosvg only).
- **Next: L15 P10** (`practical-scenarios`, #30&#8211;#34, 5 figs &#8212; **&#8805;5: PRODUCTION sub-chat split** per the locked plan; resolve **D6** scenario-setup sourcing method [hand-coded SVG diagram + inset render vs Pillow composite vs vignette host] at plan-lock). NEEDS BlenderMCP.

### 2026-06-13 &#8212; L15 P8 DONE (`light-properties` FULLY CLOSED &#8212; #27 diffuse_specular_split PNG + `c27` + #28 contact_shadows PNG + `c28` + #29 `cd` custom_distance_sphere inline SVG; PRODUCTION + INTEGRATION single chat, mixed-method) [RECONCILED THIS CLOSE]
- **Logged retroactively at the P9 close** &#8212; the P8 work landed (all 3 figures integrated, header Status line + session banner updated to P8 DONE) but the §6 Status-log entry was never written at the time; reconciled here from the header/session state and the live HTML.
- **26/40 produced / 26/40 integrated &#8212; `light-properties` section complete.** HTML **290,115 (P0) &#8594; 300,148 B**. #29 `cd` custom_distance_sphere INLINE SVG; #27 diffuse_specular_split 3-panel PNG-overlay `c27`; #28 contact_shadows 2-panel PNG-overlay `c28`; both PNG overlays cairosvg-pixel-verified over the real PNGs before commit (LD19). Post-commit integrity was CLEAN at the P8 close (em-dash 27 held, `&mdash;` 0, `color="#222"` 0, figure 26, img 17, literal `<svg` 24 [+2 `<marker>` = viewBox 26], figcaption 26, wrapper-divs 15, `&#183;` 33, middot 2, `&#8211;` 4, `&#8722;` 5, 12 section ids no dups, `cd-*` present, c27/c28 no `c{N}-*` ids = composite/numbered-pill, each fig referenced once). Blender scene torn down to clean base, temps ENOENT-verified, no BlenderMCP debt.
- **L15 lesson-body note queued (NOT figure work):** the Shadow Settings card (`light-properties`, ~line 2769) describes "Contact Shadow (Eevee only)" as a live per-light checkbox &#8212; STALE for Blender 4.2+ / EEVEE-Next (removed `use_contact_shadow`); flagged in the deferred queue for a future lesson-body pass.

### 2026-06-13 &#8212; L15 P7 DONE (`area-light` FULLY CLOSED &#8212; #20 area_light_shapes + `c20` + #21 area_size_softness + `c21` + #22 `ad` area_direction_indicator inline SVG + #23 area_light_settings UI-chrome + `c23`; PRODUCTION + INTEGRATION single chat, mixed-method)
- **23/40 produced / 23/40 integrated &#8212; `area-light` section complete.** HTML **274,874 (P0) &#8594; 290,115 B** (+15,241). Byte trail (bottom-up commits): #22 inline SVG +6,561 &#8594; 281,435 ; #21 +2,247 &#8594; 283,682 ; #23 +4,185 &#8594; 287,867 ; #20 +2,248 &#8594; 290,115 (each `get_file_info`-confirmed live).
- **Entry-state PASSED** on fresh `copy_file_user_to_claude` + `get_file_info`: **274,874 B** authoritative P0, em-dash 27, `&mdash;` 0, `color="#222"` 0, figure 19, img 12, svg 17, figcaption 19, wrapper-divs 10, `&#183;` 29, middot literal 2, `&#8211;` 4, 12 section ids no dups, `ad-`/`c20`/`c21`/`c23` + all 4 P7 file refs absent. Blender scene verified clean empty base 0/0/0 at entry (no STOP).
- **P7 PLAN-LOCK decisions (confirmed by Ray, "proceed"):** #20 glossy SPHERE (consistent with the lesson's sphere-on-plane scenes); #21 1&#215;4 horizontal strip (softness reads left-to-right, per the doc's 1920&#215;600 wide-strip spec); #22 viewBox 800&#215;560 (no 680 bump needed); #20/#21 default AgX (highlight-shape / shadow-shape teaching, not marker-color &#8212; LD17 NOT invoked, no washing seen at review); no P7-specific D-flags open (D4/D9 resolved at P5; D7 set #9/#35/#40 not in P7).
- **#22** `images/lesson_15_22_area_direction_indicator.svg` &#8212; **5,048 B**, inline-SVG prefix `ad`, viewBox 800&#215;560. Perspective view (orange area-light plane parallelogram + parallel emission arrows down local &#8722;Z to a green "Lit (in front)" sphere + a muted "Dark (behind)" sphere + "back face &#183; no emission" marker) and a side-view inset (plane edge-on, orange emission hemisphere + &#8722;Z arrow to the front, red crossed-out "no light" to the back) + conclusion band + Variant-B footer tip. cairosvg light+dark render-checked (collisions fixed: conclusion band moved to the right column clear of the "Dark (behind)" caption; rotated emission label shortened + repositioned off the lit sphere). Self-assert PASSED: em-dash 0, `&mdash;` 0, 3 `&#183;`, 5 `&#8722;` (content minus signs), 1 svg/title/desc, role+aria, `ad-title`/`ad-desc` ids. Standalone KEEPS `color="#222"` &#8212; STRIPPED on inlining.
- **#20** `images/lesson_15_20_area_light_shapes.png` &#8212; **1,360,378 B**, 1602&#215;1602. 2&#215;2 Cycles grid (Rectangle / Square top, Disk / Ellipse bottom): one glossy sphere (roughness 0.12) on a dark plane, one area light front-top-right; only the light's `shape` + dims change per panel so the specular highlight mirrors the shape. Cycles AgX 160spp+denoise, Pillow-composited with `#FF6B00` 2px dividers (GAP=2), NO baked labels. Marginal-read check PASSED: all four highlight shapes read distinctly (Rectangle vs Ellipse closest pair but crisp corners vs rounded oval distinguish them). Overlay `c20` = composite-overlay variant (4 per-panel pills Rectangle/Square/Disk/Ellipse, no leaders) &#8212; NO `c20-*` ids.
- **#21** `images/lesson_15_21_area_size_softness.png` &#8212; **1,438,122 B**, 3206&#215;800. 1&#215;4 Cycles strip: same matte sphere (roughness 0.5) casting a shadow, area light SQUARE above-front at fixed radiant power (1400 W), only `size` changes 0.5m / 2m / 5m / 10m &#8594; progressively softer shadow. Cycles AgX 160spp+denoise, Pillow-composited (GAP=2) with `#FF6B00` 2px dividers, NO baked labels. Marginal-read check PASSED (Pillow penumbra measure: deep-shadow fraction 0.774 &#8594; 0.683 &#8594; 0.340 &#8594; 0.153, mid-tone penumbra 0.047 &#8594; 0.101 &#8594; 0.419 &#8594; 0.786 &#8212; monotonic, distinct steps incl. 5m vs 10m). Overlay `c21` = composite-overlay variant (4 size pills 0.5 m / 2 m / 5 m / 10 m, no leaders) &#8212; NO `c21-*` ids.
- **#23** `images/lesson_15_23_area_light_settings.png` &#8212; **144,567 B**, 911&#215;1275. Light Data Properties panel, Area light selected (Power 1000 / Shape Rectangle / Size X 2m Y 1m, type row Area active). **MANUAL CAPTURE (Win+Shift+S panel snip direct to `images/`, Option A)** &#8212; the bridge could NOT flip the Properties editor to the light DATA tab (`space.context = 'DATA'` live-poll rejected "enum DATA not found" even inside a properties-area `temp_override` with the active Area light set; 5.1.1 context-poll resistance). Routed to the manual Win+Shift+S path per LD20 (render/bridge-unreachable UI). **NOTE: Spread is NOT a top-level Area-light field in 5.1.1 EEVEE-Next** (the panel shows Power / Exposure / Normalize, with Spread under a subpanel); the 4 visible teaching targets (Type=Area / Color / Power / Shape + Size X-Y) map to the doc's intent. Overlay `c23` = numbered-pill treatment (5 orange markers + short orange leaders: 1 Type / 2 Color / 3 Power / 4 Shape / 5 Size X-Y bracketed, + bottom dark-pill legend), cairosvg-pixel-verified 1:1 over the real 911&#215;1275 snip before commit. NO `c23-*` ids (numbered-pill pattern).
- **Anchors (all count=1 on the fresh copy, by unique TEXT span):** #20 after the "What is an Area Light?" content, at `<h3>Area Light Settings</h3>`. #23 after the "Area Light Settings" card, at `<h3>Area Light Size and Shadow Softness</h3>`. #21 after the "Area Light Size and Shadow Softness" card, at `<h3>Positioning and Aiming Area Lights</h3>`. #22 after the "Positioning and Aiming Area Lights" card, at `<h3>Practical Area Light Examples</h3>`. Document order #20 &#8594; #23 &#8594; #21 &#8594; #22, so **bottom-up insert = #22 first, then #21, #23, #20 last.** Each `edit_file` dryRun &#8594; commit &#8594; `get_file_info`.
- **#22 inline SVG** per LD12: root `color="#222"` STRIPPED, multi-line tags collapsed, re-indented (svg col20 / children col24 / `<g>` children col28), structural comments kept, `ad-*` ids preserved. **#20/#21/#23 PNG-overlay** per LD10: wrapper-div (`position: relative; display: inline-block; max-width: 100%`) + img (`margin:0`) + inlined overlay-SVG (`pointer-events:none`, viewBox = PNG px), figcaption OUTSIDE. `c20`/`c21`/`c23` carry NO `c{N}-*` id attributes (composite-pill / numbered-pill patterns &#8212; expected; uniqueness tracked via once-each refs). Figcaptions comma/period, NO em-dash.
- **POST-COMMIT INTEGRITY SWEEP CLEAN** (fresh `copy_file_user_to_claude` + Python `count()` authoritative on the live **290,115 B** file): bytes 290,115 ; **em-dash 27** held (`&mdash;` 0) ; `color="#222"` 0 ; figure 19&#8594;**23/23** ; img 12&#8594;**15** (#20/#21/#23 PNGs; #22 inline + #19-style add none) ; svg 17&#8594;**21** (literal `<svg` enumerated: 17 base + #22 inline(1) + c20/c21/c23(3); no `<marker>` traps this phase) ; figcaption 19&#8594;**23** ; overlay wrapper-divs 10&#8594;**13** (#20/#21/#23; #22 inline adds none) ; **`&#183;` 29&#8594;32** (live recount: +3 from #22's subhead &#215;2 + back-face marker; #20/#21/#23 overlays + figcaptions use commas/slashes not middots) ; literal middot 2 (unchanged) ; **`&#8211;` 4** (unchanged &#8212; #21 size pills + #23 legend use " / " slashes, not en-dashes) ; **`&#8722;` 5** (new #22 content minus signs) ; 12 section ids no dups ; `ad-*` present (4 = id + aria refs) ; `c20`/`c21`/`c23` no `c{N}-*` ids ; #20/#21/#23 PNGs each referenced once, #22 inlined (0 file ref).
- **Blender teardown DONE:** all 4 `L15_P7_*` objects (Plane/Sphere/Area/Cam) + meshes/materials/light-data/camera-data removed via `bpy.data.objects.remove` (names collected first), orphans purged (none remained), restored BLENDER_EEVEE + AgX/look None + world 0.05 str 1.0, cursor origin, re-shown toolbar/N-panel/gizmo + overlays, Properties&#8594;SCENE. Verified clean empty base 0/0/0/0/0. Windows `C:\Users\Public\L15P7` deleted (`isdir` False) ; 8 WSL `_tmp_l15_p7_*` review temps deleted (ENOENT-verified). NO `_overlay_VERIFIED.svg` artifacts written (the c20/c21/c23 + #22 checks ran Claude-side via cairosvg only, never persisted to `images/`). No BlenderMCP debt.
- **Next: L15 P8** (`light-properties`, #27 diffuse_specular_split 3-panel composite + #28 contact_shadows 2-panel comparison + #29 `cd` custom_distance_sphere SVG, 3 figs, single chat). NEEDS BlenderMCP (#27/#28 render composites). Resolve any P8-specific D-flags at plan-lock.


### 2026-06-13 &#8212; L15 P6 DONE (`spot-light` FULLY CLOSED &#8212; #15 `sc` spot_light_cone inline SVG + #16 spot_beam_patterns + `c16` + #17 spot_aiming_techniques + `c17` + #18 spot_light_settings + `c18` + #19 gobo_pattern_example plain; INTEGRATION-only close, sweep + doc updates)
- **19/40 produced / 19/40 integrated &#8212; `spot-light` section complete.** #19 gobo was committed in the prior sub-chat-B chat; #15/#16/#17/#18 (the 4 remaining spot figures) in the chat after, all 3 PNG overlays cairosvg-pixel-verified over the real PNGs before commit. Byte trail: **257,883 (true P0, #19 already present) &#8594; #17 260,358 &#8594; #16 263,561 &#8594; #18 267,921 &#8594; #15 274,874** (live, each `get_file_info`-confirmed).
- **ENTRY-STATE RECONCILED:** the resume doc carried P0 = 257,167 B, but the live file read **257,883 B** at the start of the integration chat. The 257,167 was the **PRE-#19 baseline** (P6-A PRODUCTION close, HTML untouched); the #19 commit in the prior sub-chat-B chat added **+716 B** to reach 257,883. Confirmed #19 present at entry (img 9 / figure 15 / em-dash 27, 3 spot anchors count=1), so **257,883 was the true P0** for the 4-figure integration. No lost or double commit.
- **Placements (all anchors count=1 on the fresh copy, by unique TEXT span):** #17 `spot_aiming_techniques` PNG-overlay `c17` (viewBox 1564&#215;853) inserted after the "What is a Spot Light?" content, anchored at `<h3>Spot Light Patterns and Effects</h3>`. #16 `spot_beam_patterns` PNG-overlay `c16` (2104&#215;1402, 6 composite pills) + #18 `spot_light_settings` PNG-overlay `c18` (337&#215;765, 5 numbered markers + bottom legend) both at `<h3>Aiming and Positioning Spot Lights</h3>`, with **#18 inserted ABOVE #16** (document order #18 &#62; #16). #15 `spot_light_cone` **INLINE SVG** `sc` (viewBox 800&#215;560) at `<h3>Spot Light Settings</h3>`.
- **#15 inline SVG** per LD12: root `color="#222"` STRIPPED, decorative `=====` comments pruned, `sc-*` ids kept, re-indented to default section depth. **#16/#17/#18 PNG-overlay** per LD10: wrapper-div (`position: relative; display: inline-block; max-width: 100%`) + img (`margin:0`) + inlined overlay-SVG (`pointer-events:none`, viewBox = PNG px), figcaption OUTSIDE. #19 is a PLAIN default-depth figure (img + figcaption, no wrapper, no overlay &#8212; `c19` unused/skipped). `c16`/`c17`/`c18` carry NO `c{N}-*` id attributes (composite-pill / numbered-pill patterns &#8212; expected; uniqueness tracked via once-each PNG refs).
- **POST-COMMIT INTEGRITY SWEEP CLEAN** (fresh `copy_file_user_to_claude` + Python `count()` authoritative on the live **274,874 B** file): bytes 274,874 ; **em-dash 27** held (`&mdash;` 0) ; `color="#222"` 0 ; figure 14&#8594;**19** ; img 9&#8594;**12** (#16/#17/#18 PNGs; #15 inline + #19 plain add none) ; svg 13&#8594;**17** ; figcaption 14&#8594;**19** ; overlay wrapper-divs 7&#8594;**10** (#16/#17/#18; #15 inline + #19 plain add none) ; **`&#183;` 29** (live recount) ; literal middot 2 (unchanged) ; **`&#8211;` 3&#8594;4** (the #18 legend's "0&#8211;1" range en-dash) ; 12 section ids no dups ; `sc-*` present ; `c16`/`c17`/`c18` no `c{N}-*` ids ; **c19 absent** ; #15/#16/#17/#18/#19 PNGs+SVG each referenced once.
- **svg PROJECTION CORRECTION (13&#8594;17, not the prep's projected 18):** the prep projected svg &#8594;18 on the assumption that #19 carried an overlay svg, but #19 is a **plain `<img>` with no overlay** (overlay-skipped). The real delta is base 13 + #15 inline(1) + c16/c17/c18(3) = **17**. The two extra `viewBox` hits seen on a naive grep are `<marker>` arrowhead defs nested inside the inline SVGs, NOT `<svg>` elements &#8212; literal `<svg` count is 17, internally consistent with figure 19 / img 12 / wrapper-divs 10.
- **No Blender teardown owed** &#8212; the spot scene was cleaned to a verified clean empty base at the P6-A PRODUCTION close; this was an INTEGRATION-only close. **No `_overlay_VERIFIED.svg` artifacts written** (the 3 overlays were verified Claude-side via cairosvg only, never persisted to `images/`). No BlenderMCP debt.
- **Next: L15 P7** (`area-light`, #20 area_light_shapes + #21 area_size_softness + #22 `ad` area_direction_indicator SVG + #23 area_light_settings UI-chrome, 4 figs, single chat &#8212; mixed-method, watch context). NEEDS BlenderMCP (#20/#21 render composites + #23 docked-panel capture). Resolve any open D-flags at P7 plan-lock.

### 2026-06-12 &#8212; L15 P6 Sub-chat A PRODUCTION COMPLETE (`spot-light` #15 `sc` spot_light_cone SVG + #16 spot_beam_patterns 2&#215;3 composite + #19 gobo_pattern_example render; INTEGRATION + #17/#18 deferred to Sub-chat B)
- **17/40 produced (was 14) / 14/40 integrated.** HTML **UNTOUCHED at 257,167 B** = safe resume P0. Entry-state PASSED on fresh `copy_file_user_to_claude` + `get_file_info`: **257,167 B** authoritative P0, em-dash 27, literal middot 2, `&#183;` 20, `&mdash;` 0, `color="#222"` 0, figure 14/14, img 8, svg 13/13, figcaption 14/14, overlay wrapper-divs 7, 12 section ids no dups, `sc-`/`c16-`/`c17-`/`c18-`/`c19-` namespaces absent, `spot-light` section present count=1. Blender scene verified clean empty base 0/0/0 at entry (no STOP condition).
- **P6 PLAN-LOCK decisions (confirmed by Ray, "proceed"):** #15 viewBox 800&#215;560 (cone + 3 callouts fit, no bump to 680 needed); cone gradient = legitimate CONTENT color (warm beam), brand orange = top strip + glyph + direction + tie-break markers; #16 beam pools = content color, brand palette governs the `c16` overlay pills only; #19 gobo pattern = **window-frame** (most recognizable); #16/#19 = default AgX path (beam-shape / pattern-shape teaching, not marker-color &#8212; LD17 Standard NOT invoked, no washing seen at review); #16 overlay = composite-overlay variant (6 centered per-panel pills, no leaders, baked dividers carry separation).
- **#15** `images/lesson_15_15_spot_light_cone.svg` &#8212; **5,708 B**, inline-SVG prefix `sc`, viewBox 800&#215;560. Spot glyph (orange housing + warm core) at apex (150,170) + two-region cone (outer blend `sc-blend` gradient + inner fully-lit `sc-beam` gradient, both warm content color) aimed down-right to a floor pool at y=470; orange direction arrow down the cone axis + left-rail "Direction (aim)" label; Spot Size angle arc spanning the cone at the apex + label; Blend callout (orange marker + leader to the falloff band, "soft edge: 0 = hard, 1 = feathered"); lit object inside cone (green "Lit (inside cone)") vs dark object outside ("Dark (outside)"); v6 header (orange strip / 24-700 SPOT LIGHT CONE title / 0.65 subtitle with 2 `&#183;` / divider); Variant-B left-rail footer tip. cairosvg light+dark render-checked (collisions fixed: direction label moved off the arc, arc re-spanned the cone, blend text raised clear of the dark sphere). Self-assert PASSED: em-dash 0, `&mdash;` 0, 2 `&#183;`, literal middot 0, one svg/title/desc, role+aria, `sc-title`/`sc-desc` ids. Standalone KEEPS `color="#222"` &#8212; STRIP on inlining.
- **#16** `images/lesson_15_16_spot_beam_patterns.png` &#8212; **1,191,544 B**, 2104&#215;1402. 2&#215;3 Cycles grid: **columns** = spot_size narrow 18&#176; / medium 40&#176; / wide 65&#176;; **rows** = blend hard 0.02 (top) / soft 0.8 (bottom). Wall (16m plane, dark `(0.16,0.16,0.17)` rough 0.9) lit by one warm-white spot (1500W) at y=-5.5 aiming +Y, camera at y=-12.8 lens 50 (half-extent ~4.6m so all three pools fit centered, world-origin projects to exact frame center 0.5/0.5 verified). 6 panels each 700&#215;700, Cycles AgX 160spp+denoise, Pillow-composited with `#FF6B00` 2px dividers (GAP=2, orange bleed-through), NO baked labels. Hard rows show crisp pool edges, soft rows show feathered penumbra; sizes read distinctly. Overlay `c16` (composite-overlay variant, 6 per-panel pills) deferred to INTEGRATION.
- **#19** `images/lesson_15_19_gobo_pattern_example.png` &#8212; **912,537 B**, 1600&#215;900. Cycles AgX 200spp+denoise. Corner room (back wall 14m at y=3 warm `(0.30,0.27,0.24)`, floor). Warm spot (2600W, spot_size 60&#176;, blend 0.10, shadow_soft_size 0.03 for crisp gobo edges) at (-1.2,-3.5,3.2) aimed at the back wall through a **window-frame cookie** (real geometry: outer frame + center vertical + center horizontal mullion = 2&#215;2 panes, opaque dark mat, `visible_camera=False` + `visible_diffuse=False` so only its shadow projects). Window pattern projects clearly inside a soft circular pool against a dark surround, slight perspective skew. NO baked labels; overlay `c19` deferred to INTEGRATION (likely minimal/skip-candidate &#8212; confirm at integration).
- **INTEGRATION TODO (Sub-chat B, after #17/#18 UI-chrome produced):** refresh a fresh `copy_file_user_to_claude` HTML copy + re-confirm 257,167 B baseline. Resolve anchors count=1 on the fresh copy per the requirements-doc placement: #15 after the "What is a Spot Light?" card; #18 + #16 after the "Spot Light Settings" card (doc lists #18 above #16); #17 after the "Aiming and Positioning Spot Lights" card; #19 after the "Spot Light Patterns and Effects" card. Bottom-up insert (lowest-in-document first). #15 inline SVG (STRIP `color="#222"`, re-indent, keep `sc-*` ids); #16/#17/#18/#19 PNG-overlay pattern (wrapper-div + img margin:0 + inlined overlay-SVG viewBox=PNG px, figcaption OUTSIDE), each overlay cairosvg-pixel-verified before commit. Each `edit_file` dryRun &#8594; commit &#8594; `get_file_info`. Post-commit sweep: em-dash hold 27, recount `&#183;` live, figure 14&#8594;19, img 8&#8594;12 (#16/#17/#18/#19 PNGs; #15 is inline SVG adds none), svg 13&#8594;~19 (depends on overlay-skip set), figcaption 14&#8594;19, wrapper-divs 7&#8594;~11, 12 section ids no dups, anchors count=1.
- **Blender teardown DONE** (scene staged for #16/#19 + engine on Cycles + AgX): all `L15_P6_*` objects removed via `bpy.data.objects.remove` (the #16 wall/spot/cam removed before the #19 room build; #19 GWall/GFloor/Cookie/GSpot/GCam removed at close), orphans purged (none remained), **restored BLENDER_EEVEE + AgX/look None** + world 0.05 str 1.0, cursor origin. Verified clean empty base 0/0/0/0/0 (obj/mesh/mat/light/cam), 0 `L15_P6_*` leftovers. Windows `C:\Users\Public\L15P6` deleted (`isdir` False) ; WSL `_tmp_l15_p6_test.png` deleted (ENOENT-verified both bridge-side + Filesystem-side). NO `_overlay_VERIFIED.svg` handoff artifacts written (overlays are produced at INTEGRATION, not now). Deliverables retained: #15 SVG 5,708 B + #16 PNG 1,191,544 B + #19 PNG 912,537 B. No BlenderMCP debt.
- **Next: L15 P6 Sub-chat B** (`spot-light` UI-chrome #17 spot_aiming_techniques + #18 spot_light_settings, both docked-panel/viewport captures per LD20; then INTEGRATION inline at <6 of all 5 spot figures unless context tightens &#8212; warn Ray first). NEEDS BlenderMCP (#17 viewport-with-cone capture + #18 Light Data Properties panel capture). Resolve #17/#18 overlay treatments + #19 overlay-skip-or-minimal at Sub-chat B.

### 2026-06-12 &#8212; L15 P5 DONE (`sun-light` #11 `sr` sun_light_parallel_rays SVG + #12 `spi` sun_position_independence SVG + #13 sun_time_of_day composite + `c13` + #14 sun_light_settings UI-chrome + `c14`; PRODUCTION + INTEGRATION single chat, mixed-method)
- **14/40 produced / 14/40 integrated.** HTML **237,557 &#8594; 257,167 B** (+19,610). Bottom-up commits: #14 +3,688 &#8594; 241,245 ; #13 (+ inlined `c13`) +2,704 &#8594; 243,949 ; #12 inline SVG +7,726 &#8594; 251,675 ; #11 inline SVG +5,492 &#8594; 257,167. Entry-state PASSED on fresh `copy_file_user_to_claude` + `get_file_info`: **237,557 B** authoritative P0, em-dash 27, literal middot 2, `&#183;` 15, `&mdash;` 0, `color="#222"` 0, figure 10/10, img 6, svg 9/9, figcaption 10/10, overlay wrapper-divs 5, 12 section ids no dups, `sr-`/`spi-`/`c13-`/`c14-` absent (bare `c10` = 4, incidental `#ffc107` CSS hex).
- **D4 RESOLVED:** #12 locked at **800&#215;680 dense** variant (three side-by-side mini-views needed the vertical room; 560 would crowd). **D9 RESOLVED:** #13 rendered under **LD17 Standard view transform + look None on a 0.05 world** &#8212; a PRODUCTION review of the default-AgX panels showed the warm/cool teaching tones reading muted/brown, and Standard gave crisper warm-orange / cool-purple separation. AgX RESTORED at close.
- **#11 `sun_light_parallel_rays.svg`** (4,230 B standalone, inline `sr`, viewBox 800&#215;560): brand-orange sun glyph top-left + 10 perfectly parallel arrowed rays (same dx/dy) crossing the scene + near/far objects receiving the same-angle rays; "same angle on every surface &#183; no divergence &#183; no falloff" callout; Variant-B footer tip. cairosvg light+dark checked. Standalone KEEPS `color="#222"`; STRIPPED on inlining.
- **#12 `sun_position_independence.svg`** (6,685 B standalone, inline `spi`, viewBox **800&#215;680**): three panels (Sun at left/center/right) each with the SAME ray angle + IDENTICAL shadow polygon, proving rotation (not position) drives the result; conclusion band + footer tip. cairosvg light+dark checked. Standalone KEEPS `color="#222"`; STRIPPED on inlining.
- **#13 `sun_time_of_day.png`** (1,098,287 B, 1602&#215;1202): 4 Cycles panels (morning warm low / midday bright overhead / golden-hour vivid orange / dusk cool purple), **Standard view transform**, 200 spp + denoise, same column+sphere scene, only sun angle+color changed. Pillow-composited 2&#215;2 with `#FF6B00` 2px dividers, NO baked labels. Overlay `c13` = composite-overlay variant (4 centered pills Morning/Midday/Golden Hour/Dusk, no leaders, dividers carry separation), cairosvg-pixel-verified 1:1 over the real PNG before commit. NO `c13-*` ids (composite-pill pattern).
- **#14 `sun_light_settings.png`** (337&#215;765, 56,011 B): docked Light Data Properties panel, Sun light selected, Strength 3.000 / Angle 5&#176; / warm Color swatch legible. Full-window local-tempdir `screen.screenshot` (1920&#215;1046) &#8594; deterministic area crop (PROPERTIES area x1580 y35 w337 h795, Y-flip, 30px header trim, identical geometry to P4 #10). Overlay `c14` = numbered-pill treatment (4 orange markers + short orange leaders at type-row y217 / Color y285 / Strength y337 / Angle y453 + bottom dark-pill legend + italic orange "Direction is set by rotation, not position" note), cairosvg-pixel-verified over the real crop before commit. NO `c14-*` ids (numbered-pill pattern).
- **Anchors (all count=1 on the P0 copy, by unique TEXT span):** #11 after the "What is a Sun Light?" card `</div>`, before `<h3>Sun Light Settings</h3>` ; #12 after the "Sun Light Settings" card `</div>`, before `<h3>Sun Light Direction and Time of Day</h3>` ; #13 after the "Sun Light Direction and Time of Day" card `</div>`, before `<h3>Practical Sun Light Examples</h3>` ; #14 after the "Sun Light Best Practices" card `</div>`, before the `<blockquote>` Infinite-Distance-Insight (last figure in section). Bottom-up insert = #14 first, then #13, #12, #11 last. All 4 simulated Claude-side first, then each `edit_file` dryRun &#8594; commit &#8594; `get_file_info` (241,245 &#8594; 243,949 &#8594; 251,675 &#8594; 257,167 B confirmed live).
- **#11/#12 inline SVG** per LD12: root `color="#222"` STRIPPED, re-indented figure col16 / svg col20 / children col24 / `<g>` col28, multi-line root attrs collapsed, structural comments kept, `role`/`aria-labelledby`/`<title>`/`<desc>`/`sr-*`/`spi-*` ids preserved. **#13/#14 PNG-overlay** per LD10: wrapper-div (`position: relative; display: inline-block; max-width: 100%`) + img (`margin:0`) + inlined overlay-SVG (`pointer-events:none`, viewBox = PNG px), figcaption OUTSIDE. Figcaptions comma/period, NO em-dash, NO middot.
- **Post-commit integrity CLEAN** (fresh copy, Python-authoritative): bytes 257,167, **em-dash 27** (held, added none), literal middot 2 (unchanged), **`&#183;` 20** (live-recounted: 15 base + #11 SVG 3 + #12 SVG 2 = +5; #13/#14 figcaptions+overlays use comma/slash not middot &#8212; NOT trusted from arithmetic), `&mdash;` 0, `color="#222"` 0, figure 10&#8594;**14/14**, img 6&#8594;**8**, svg 9&#8594;**13/13**, figcaption 10&#8594;**14/14**, overlay wrapper-divs 5&#8594;**7** (#13 + #14; #11/#12 inline-SVG add none), 12 section ids no dups, `sr-title`/`sr-desc`/`spi-title`/`spi-desc` each 2&#215; (aria + id), `c13-*`/`c14-*` absent (composite/numbered-pill patterns), #13 + #14 PNGs referenced once each, #11/#12 inlined (0 file refs), both pre-existing #14-zone em-dashes preserved verbatim ("sun direction&#8212;make it make sense", "same angle&#8212;they're parallel").
- **Blender teardown DONE** (scene staged + engine on Cycles + Standard view transform from #13): 4 `L15_P5_*` objects (Ground/Column/Sphere/Sun + Cam) + 2 materials removed via `bpy.data.objects.remove` (collected names first to avoid stale-RNA mid-mutation error), orphans purged (none remained), **restored BLENDER_EEVEE + AgX/look None** + world 0.05 str 1.0, cursor origin, re-shown toolbar/N-panel/gizmo + overlays, Properties&#8594;SCENE. Verified clean empty base 0/0/0/0/0, 0 `L15_P5_*` leftovers. Windows `C:\Users\Public\L15P5` deleted (`isdir` False) ; WSL `_tmp_l15_p5_review_agx.png` + `_tmp_l15_p5_review_std.png` deleted (ENOENT-verified). NO `_overlay_VERIFIED.svg` handoff artifacts written this chat (overlays verified Claude-side only via cairosvg). Deliverables retained: #11 SVG 4,230 B + #12 SVG 6,685 B + #13 PNG 1,098,287 B + #14 PNG 56,011 B. No BlenderMCP debt.
- **OWED AT NEXT CHAT START (session.md banner not yet rewritten this close):** update the session.md TOP banner lead &#8594; "Next chat: L15 P6", the "Last updated" footer, the L15 outcome line, and the mid-doc "Next active target" section to reflect P5 DONE / 257,167 B / 14-of-40. (Ran low on context at P5 close; banner rewrite deferred to the fresh chat.)
- **Next: L15 P6** (`spot-light`, #15 `sc` spot_light_cone SVG + #16 spot_beam_patterns 2&#215;3 composite + #17 spot_aiming_techniques UI-chrome + #18 spot_light_settings UI-chrome + #19 gobo_pattern_example render, **5 figs &#8594; PRODUCTION sub-chat split** per the locked plan [A: SVG #15 + renders #16/#19 ; B: UI-chrome #17/#18] ; INTEGRATION inline at &lt;6 unless context tightens). NEEDS BlenderMCP. Resolve any P6-specific decisions at plan-lock.


### 2026-06-12 &#8212; L15 P4 DONE (`point-light` #8 `pt` point_light_diagram + #9 point_light_shadow + #10 point_light_settings + overlay `c10`; INTEGRATION-only chat, production from prior chat)
- **10/40 produced / 10/40 integrated.** HTML **226,950 &#8594; 237,557 B** (+10,607). Bottom-up commits: #9 +519 &#8594; 227,469 ; #10 (+ inlined `c10` overlay) +4,524 &#8594; 231,993 ; #8 inline SVG +5,564 &#8594; 237,557. Entry-state PASSED on fresh `copy_file_user_to_claude` + `get_file_info`: **226,950 B** authoritative P0, em-dash 27, literal middot 2, `&#183;` 15, `&mdash;` 0, `color="#222"` 0, figure 7/7, img 4, svg 7/7, figcaption 7/7, overlay wrapper-divs 4, 12 section ids no dups, `pt-`/`c8-`/`c9-`/`c10-` namespaces absent (bare `c10` = 4, the incidental `#ffc107` CSS hex).
- **All 4 deliverables confirmed on the real path at exact expected sizes** before integration: #8 SVG 4,001 B (prefix `pt`, viewBox 800&#215;560, root `color="#222"` present for `<img>` fallback), #9 PNG 1,051,388 B (1920&#215;1080, Cycles default-AgX, hard shadow), #10 PNG 54,190 B (337&#215;765 Light Data Properties crop), `_c10_overlay_VERIFIED.svg` 2,850 B (viewBox 337&#215;765, 5 numbered markers + bottom legend, already carrying `position:absolute; inset:0; ... pointer-events:none`).
- **Anchors (both count=1 on the fresh copy):** #8 after the "What is a Point Light?" card `</div>`, before `<h3>Point Light Settings</h3>` ; #10 + #9 after the "Point Light Settings" card `</div>`, before `<h3>Practical Point Light Examples</h3>` (doc lists #10 above #9). Bottom-up insert = #9 first (lands lowest), then #10 at the same anchor above #9, then #8 last (highest in document). Final visual order verified: #8 &#8594; Point Light Settings &#8594; #10 &#8594; #9 &#8594; Practical Point Light Examples.
- **#9 = PLAIN default-depth figure** (figure col16 / img col20 / figcaption col20): `<img>` with `display:block; max-width:100%; height:auto; margin:0; loading=lazy`, NO wrapper-div, NO overlay (D7 overlay-skip confirmed; `c9` unused). Figcaption comma/period: "A sphere lit by a single point light, casting a sharp, hard-edged shadow on the plane below."
- **#10 = PNG-overlay figure** (LD10): wrapper-div (`position: relative; display: inline-block; max-width: 100%`) + img (`margin:0`) + inlined `_c10_overlay_VERIFIED.svg` (kept its `position:absolute; inset:0; width:100%; height:100%; pointer-events:none` + viewBox 337&#215;765), figcaption OUTSIDE. NO `c10-*` id attributes (numbered-pill pattern, expected). Figcaption: "The Light Data Properties panel for a point light, showing the type selector, color, power, radius, and custom distance settings."
- **#8 = inline SVG** per LD12: root `color="#222"` STRIPPED, re-indented figure col16 / svg col20 / children col24 / `<g>` col28, multi-line root attrs collapsed, structural comments kept (no decorative `====` present to prune), `role`/`aria-labelledby`/`<title>`/`<desc>`/`pt-*` ids preserved verbatim. Default section depth. Figcaption: "A point light emits rays equally in all directions from a single point, with intensity falling off over distance."
- Each `edit_file` dryRun &#8594; commit &#8594; `get_file_info` (227,469 &#8594; 231,993 &#8594; 237,557 B confirmed live).
- **Post-commit integrity CLEAN** (fresh copy, Python-authoritative): bytes 237,557, **em-dash 27** (held, added none), literal middot 2 (unchanged), **`&#183;` 15** (held &#8212; the 3 figcaptions use comma/period, the `c10` legend uses " / " slashes not middots, the #8 tip uses semicolons; live-recounted, not trusted from arithmetic), `&mdash;` 0, `color="#222"` 0, figure 7&#8594;**10/10**, img 4&#8594;**6**, svg 7&#8594;**9/9**, figcaption 7&#8594;**10/10**, overlay wrapper-divs 4&#8594;**5**, 12 section ids no dups, `pt-title`/`pt-desc` each 2&#215; (aria + `<title>`/`<desc>` id), `c10-*` absent (numbered-pill pattern), #9 + #10 PNGs referenced once each, #8 inlined (not referenced by name), all anchors count=1, the pre-existing intro em-dash ("simplest light type&#8212;it radiates") preserved verbatim.
- **PROJECTION CORRECTION:** the P4-PRODUCTION-COMPLETE entry projected **img 4&#8594;7**; the correct delta is **img 4&#8594;6** &#8212; #8 is an inline SVG and adds NO `<img>` (only #9 + #10 add one img each). The svg projection (7&#8594;9) already correctly credited #8's inline SVG, so 6 imgs + 9 svgs is internally consistent. All other projected deltas (figure 10, svg 9, figcaption 10, wrapper-divs 5, em-dash 27, `&#183;` 15) landed exactly.
- **Blender teardown DONE** (scene was staged + engine left on EEVEE from #10 capture): `get_scene_info` found the 4 `L15_P4_*` objects (`L15_P4_Plane`/`L15_P4_Sphere`/`L15_P4_Point`/`L15_P4_Cam`) + 2 materials &#8594; all removed via `bpy.data.objects.remove`, orphans purged (2 data-blocks deleted, recursive `orphans_purge`), restored BLENDER_EEVEE + AgX/look None + world `(0.05)` str 1.0, cursor origin, re-shown toolbar/N-panel/gizmo + overlay defaults (floor + X/Y axes + cursor), Properties tab &#8594; SCENE. Verified clean empty base 0/0/0/0/0 (obj/mesh/mat/light/cam), 0 `L15_P4_*` leftovers. Windows `C:\Users\Public\L15P4` deleted (`isdir` False) ; WSL `_tmp_l15_p4_fig9_preview.png` + `_tmp_l15_p4_fig10_preview.png` deleted (ENOENT-verified) ; `_c10_overlay_VERIFIED.svg` deleted post-commit (ENOENT-verified both bridge-side + Filesystem-side). Deliverables retained: #8 SVG 4,001 B + #9 PNG 1,051,388 B + #10 PNG 54,190 B. No BlenderMCP debt.
- **DEFERRED RECONCILIATION DONE AT CLOSE:** the session.md TOP banner still led "Next chat: L15 P3" / 217,735 B (the P3-DONE banner update was never written last chat). Both the P3 reconciliation AND the P4 close were folded into the session.md banner rewrite this close.
- **Next: L15 P5** (`sun-light`, #11 `sr` sun_light_parallel_rays SVG + #12 `spi` sun_position_independence SVG + #13 sun_time_of_day 4-panel composite + #14 sun_light_settings UI-chrome, 4 figs, single chat &#8212; mixed-method, near the &#8805;5 split line so watch context). NEEDS BlenderMCP (#13 render composite + #14 docked-panel capture). Resolve **D4** (#12 800&#215;680 dense vs custom height) + **D9** (#13 time-of-day warm/cool color-dependent render &#8212; invoke LD17 Standard ONLY if a production review shows AgX washing) at P5 plan-lock.


### 2026-06-12 &#8212; L15 P4 PRODUCTION COMPLETE (INTEGRATION-deferred to next chat)
- **9/40 produced (was 7) / 7/40 integrated.** HTML **UNTOUCHED at 226,950 B** = safe resume P0. All 3 P4 deliverables produced + verified on the real path:
  - **#8** `images/lesson_15_08_point_light_diagram.svg` &#8212; **4,001 B**, inline-SVG prefix `pt`, viewBox 800&#215;560. Omnidirectional: central orange point source (glow + 12px core) + 16 equal-length arrowed rays at 360&#176; + 3 fading falloff rings; v6 header (orange strip / 24-700 POINT LIGHT title / 0.65 subtitle / divider), annotation line y=470, Variant-B left-rail footer tip. cairosvg render-checked light + dark (both legible). Self-assert: em-dash 0, one svg/title/desc, role+aria, `pt-*` ids. Standalone KEEPS `color="#222"` &#8212; STRIP on inlining.
  - **#9** `images/lesson_15_09_point_light_shadow.png` &#8212; **1,051,388 B**, 1920&#215;1080. Cycles/OPTIX 128spp+denoise, **default AgX path** (D9 confirmed: lit ~118 / shadow ~22 / sphere ~186, no wash). Sphere hovering over plane, single warm-white POINT light (1500W, soft_size 0.05) from above-side, 3/4 cam &#8594; sharp hard shadow. **D7 CONFIRMED: overlay SKIPPED** &#8594; integrate as a PLAIN default-depth figure (img + figcaption only, NO wrapper-div, NO overlay-SVG; `c9` unused).
  - **#10** `images/lesson_15_10_point_light_settings.png` &#8212; **54,190 B**, 337&#215;765. Docked Light Data Properties panel, Point light active, params Power 300.000 / warm Color swatch / Radius 0.1m / Custom Distance ON. Captured via full-window local-tempdir `screen.screenshot` &#8594; deterministic area crop (PROPERTIES area x1580 y35 w337 h795, Y-flip, 30px header trim). **NOTE: Properties display engine switched to EEVEE for the capture** so Custom Distance shows as a top-level expanded light panel (under Cycles it hides in the collapsed Settings subpanel); UI-chrome only, does not affect #9's Cycles render. Overlay handoff written: `images/_c10_overlay_VERIFIED.svg` (2,850 B) &#8212; cairosvg-composited 1:1 over the real crop + view-verified clean.
- **`c10` overlay (VERIFIED, ready to inline):** viewBox `0 0 337 765`, neutral right-gutter numbered markers (orange `#FF6B00` r=11 circles, white digit, short orange leader to each control) + bottom dark-pill legend (`#1a1a1a` fill-opacity 0.9, rx8, x10 y636 w318 h116). 5 callouts at measured y: (1) Light-type toggle row y216 leader from x120, (2) Color y284, (3) Power y336, (4) Radius y452, (5) Custom Distance y602 leader from x230. Legend rows fs13 white, marker digits fs11. NO `c10-*` id attributes (composite-overlay numbered-pill pattern; uniqueness tracked via once-each PNG ref). em-dash 0, middot 0. Matches the P3 #7 neutral-pill + orange-leader treatment.
- **INTEGRATION TODO (next chat, bottom-up #9 &#8594; #10 &#8594; #8):** refresh a fresh `copy_file_user_to_claude` HTML copy + re-confirm 226,950 B baseline (em-dash 27, middot 2, `&#183;` 15, figure/img/svg/figcaption 7/4/7/7, wrapper-divs 4, c8/c9/c10-/pt- absent). Resolve all 3 anchors to count=1 on the fresh copy (requirements doc: #8 after the "What is a Point Light?" card; #10 + #9 after the "Point Light Settings" card, doc lists #10 above #9 &#8594; bottom-up insert #9 FIRST, then #10, then #8 LAST since #8 is highest in document). Per-figure:
  - **#9** PLAIN default-depth figure (figure col16 / img col20 / figcaption col20), `<img ... style="display:block; max-width:100%; height:auto; margin:0;" loading="lazy">`, NO wrapper-div, NO overlay. Figcaption (comma/period, NO em-dash): "A sphere lit by a single point light, casting a sharp, hard-edged shadow on the plane below."
  - **#10** PNG-overlay figure: wrapper-div (`position: relative; display: inline-block; max-width: 100%`) + img (`margin:0`) + inlined `_c10_overlay_VERIFIED.svg` (keep its `position:absolute; inset:0; ...; pointer-events:none` + viewBox 337&#215;765), figcaption OUTSIDE. Figcaption: "The Light Data Properties panel for a point light, showing the type selector, color, power, radius, and custom distance settings."
  - **#8** inline SVG per locked conventions: STRIP root `color="#222"`, re-indent figure col16 / svg col20 / children col24 / `<g>` col28, collapse multi-line attrs, prune decorative comments, keep role/aria/title/desc/`pt-*` ids. Default section depth (parent = content-section). Figcaption: "A point light emits rays equally in all directions from a single point, with intensity falling off over distance."
  - Each `edit_file` dryRun &#8594; commit &#8594; `get_file_info`.
- **POST-COMMIT SWEEP (fresh copy, Python-authoritative):** bytes; em-dash **27** (hold, add none); literal middot 2 (recount); `&#183;` 15 + new figcaption/overlay separators (recount live, don't trust arithmetic &#8212; #8/#9/#10 figcaptions use comma/period, the c10 legend uses " / " slashes not middots, so `&#183;` likely stays 15); `&mdash;` 0; `color="#222"` 0; figure 7&#8594;**10**; img 4&#8594;**7**; svg 7&#8594;**9** (7 base + #8 inline + #10 overlay; #9 has NO svg); figcaption 7&#8594;**10**; overlay wrapper-divs 4&#8594;**5** (only #10; #9 skipped, #8 is inline-SVG); 12 section ids no dups; 3 new files referenced once each; all 3 anchors count=1; `pt-*` present, `c10-*` absent (numbered-pill pattern, expected).
- **BLENDER TEARDOWN OWED (scene still staged + engine left on EEVEE from #10 capture):** `get_scene_info` &#8594; remove all `L15_P4_*` objects (`L15_P4_Cam`/`L15_P4_Plane`/`L15_P4_Point`/`L15_P4_Sphere`) via `bpy.data.objects.remove`, purge orphans (incl. `L15_P4_*` materials/meshes/light-data/camera-data), **restore BLENDER_EEVEE** (it's already EEVEE but re-confirm) + AgX/look None + world (0.05) str 1.0, cursor origin, re-show toolbar/N-panel/gizmo + overlay defaults, Properties&#8594;SCENE. Delete Windows tempdir `C:\Users\Public\L15P4` (has fig9/fig10 scratch) + WSL temps `_tmp_l15_p4_fig9_preview.png` + `_tmp_l15_p4_fig10_preview.png` (ENOENT-verify). Delete `_c10_overlay_VERIFIED.svg` post-commit (ENOENT-verify). Retain deliverable #8 SVG + #9/#10 PNGs.
- **DEFERRED RECONCILIATION OWED AT CLOSE:** the session.md TOP banner still leads "Next chat: L15 P3" / 217,735 B (the P3-DONE banner update was never written). Fold the P3 reconciliation + the P4 close into the session.md banner rewrite, and update this doc (Status log P4 DONE, header Status line, figure registry #8/#9/#10 rows &#8594; PRODUCED/INTEGRATED, new byte count).

### 2026-06-12 &#8212; L15 P4 PLAN-LOCK (`point-light`, #8 `pt` point_light_diagram SVG + #9 point_light_shadow render + #10 point_light_settings UI-chrome; single chat, mixed-method)
- **Entry-state PASSED** on fresh `copy_file_user_to_claude` + `get_file_info` (real UNC path): **226,950 B** authoritative P0, em-dash 27, literal middot 2, `&#183;` entities 15, `&mdash;` 0, `color="#222"` 0, figure 7/7, img 4, svg 7/7, figcaption 7/7, overlay wrapper-divs 4, 12 section ids no dups, `c8`/`c9`/`c10-`/`pt-` namespaces absent. (The bare string `c10` matches 4&#215; inside the existing `#ffc107` CSS hex in lesson-body cards &#8212; incidental, NOT the overlay namespace; post-commit sweep counts `c10-`.)
- **Build order:** #8 SVG pilot first, then #9 render, then #10 UI capture. INTEGRATION bottom-up (document order #8 &#8594; #9 &#8594; #10; lowest-in-document inserted first).
- **#8 `point_light_diagram.svg` (inline SVG, prefix `pt`):** omnidirectional emission &#8212; central point-light glyph + 12&#8211;16 equal-length rays radiating 360&#176;, faint concentric falloff rings behind. Brand orange `#FF6B00` central source + tie-break; ray/label text `currentColor`; one Variant-B left-rail footer tip. viewBox `800&#215;560` default (content is light; bump to `800&#215;680` only if labels collide, confirm at build). Standalone `.svg` to `images/` keeps `color="#222"`; STRIP on inlining.
- **#9 `point_light_shadow.png` (Cycles render):** sphere hovering over plane, single point light above/side &#8594; sharp hard-edged shadow; neutral grey materials, 3/4 camera; render-to-Windows-tempdir &#8594; Pillow-relay to WSL (UNC direct-write truncates). **D7 RESOLVED &#8594; overlay SKIPPED** (single self-evident hard shadow, the L13 #24 / L14 #5/#40 precedent): plain default-depth figure (img + figcaption only, NO wrapper-div, NO overlay-SVG), `c9` stays unused. **D9 RESOLVED &#8594; default render path (AgX), NOT pre-forced** (teaching point is shadow SHAPE, not a marker color); invoke LD17 Standard recipe ONLY if a production review shows AgX washing shadow contrast.
- **#10 `point_light_settings.png` (docked-panel UI-chrome, overlay `c10`):** Light Data Properties panel with a Point light selected; full-window local-tempdir `screen.screenshot` &#8594; deterministic area crop &#8594; Pillow-relay (LD20). Overlay `c10` boxes on Color, Power, Radius, Custom Distance (neutral brand pills + `#FF6B00` leaders, matching the P3 #7 treatment, NOT the spec's literal red/orange/yellow/green). cairosvg-pixel-verify over the real PNG before commit (LD19).
- **Anchors (resolve count=1 on a fresh copy at integration):** requirements doc places #8 after the "What is a Point Light?" card; #10 + #9 after the "Point Light Settings" card (doc lists #10 above #9). Bottom-up insert = #9 first, then #10, then #8 last (#8 is highest in document). Each `edit_file` dryRun &#8594; commit &#8594; `get_file_info`.
- **Post-commit sweep deltas (projected, since #9 overlay skipped):** figure 7&#8594;10, img 4&#8594;7, svg 7&#8594;9 (7 base + #8 inline + #10 overlay; #9 has NO svg), figcaption 7&#8594;10, overlay wrapper-divs 4&#8594;5 (only #10 adds one; #9 skipped, #8 is inline-SVG not a PNG-overlay). em-dash hold 27, `&#183;` recount live, `&mdash;` 0, `color="#222"` 0, 12 section ids no dups, new PNGs/SVG referenced once each, anchors count=1, `pt-*`/`c10-*` namespaces present.
- **BlenderMCP teardown owed at close** (#9 render + #10 capture): remove all `L15_P4_*` objects, purge orphans, restore EEVEE + AgX/look None + world 0.05 str 1.0, cursor origin, re-show toolbar/N-panel/gizmo + overlay defaults, Properties&#8594;SCENE; delete Windows tempdir `C:\Users\Public\L15P4` + WSL temps (ENOENT-verify); delete `_overlay_VERIFIED.svg` handoff artifacts post-commit; retain deliverable PNGs/SVG.
- **Deferred reconciliation owed at close:** the session.md TOP banner still leads "Next chat: L15 P3" / 217,735 B (the P3-DONE banner update was not written last chat). Fold the P3 reconciliation + P4 close into the banner rewrite.

### 2026-06-12 &#8212; L15 P3 DONE (`adding-lights`, #6 `add_light_menu` + #7 `properties_panel_overview` + overlays `c6`/`c7`; INTEGRATION-only chat, production from a prior chat)
- **7/40 produced / 7/40 integrated.** HTML **217,735 &#8594; 226,950 B** (+9,215). Bottom-up commits: #7 `c7` +4,245 &#8594; 221,980 ; #6 `c6` +4,970 &#8594; 226,950. Entry-state PASSED on fresh `copy_file_user_to_claude` + `get_file_info`: **217,735 B** authoritative P0, em-dash 27, literal middot 2, `&mdash;` 0, `color="#222"` 0, figure 5/5, img 2, svg 5/5, figcaption 5/5, wrapper-divs 2, 12 section ids no dups, c6/c7 namespaces absent, both new PNGs unreferenced.
- **Production was completed in a prior chat** (this was an INTEGRATION-only chat). The 4 pre-produced + verified assets confirmed present on the real path with exact expected sizes: `lesson_15_06_add_light_menu.png` 212,314 B (1572&#215;877, synthetic clean-viewport host + reconstructed Shift+A&#8594;Light menu) ; `lesson_15_07_properties_panel_overview.png` 55,647 B (337&#215;795, docked Light Data Properties panel) ; `_c6_overlay_VERIFIED.svg` 3,030 B ; `_c7_overlay_VERIFIED.svg` 2,715 B.
- **DECISIONS (P3 plan-lock, from prior chat):** #6 = synthetic overlay-over-clean-viewport (route 1) ; #7 overlay = neutral brand pills + `#FF6B00` leaders (NOT literal red/yellow/green) ; #6 placement INSIDE the Creating Light Objects card after the add-light steps' `</ol>`.
- **Belt-and-suspenders re-verify (Claude-side):** both overlays re-composited 1:1 via cairosvg over the real PNGs (viewBox = PNG px) and eyeballed clean before commit. #6: Shift+A pill points at the Add header, the styled Add dropdown has Light highlighted, the Light fly-out shows all four types + characteristics, over the clean-viewport host cube. #7: boxes land on the Type toggle-row, Color &amp; Power, Shadow, and Custom Distance; numbered legend at the bottom. Coords were final from the handoff; both register correctly.
- **INTEGRATION (bottom-up #7 &#8594; #6, per LD10 wrapper-div + img margin:0 + inlined overlay-SVG pointer-events:none, viewBox=PNG px, figcaption OUTSIDE):**
  - **#7** at section-level depth (figure col16 / img col20 / svg col20 / `<g>` col24): inserted after the Light Data Properties card's `</div>` (the card whose `<h4>` is `⚙️ Light Data Properties Panel`), before the green `✅ Light Setup Workflow` card. Anchor on the `</div>` + green-card open + its `<h4>` (count=1).
  - **#6** at deeper indent (figure col20 / img col24 / svg col24 / `<g>` col28): inserted INSIDE the `🔦 Adding Your First Light` card, after the `</ol>` ending the add-light steps (the `<ol>` containing the `Add light:` `<li>` with code `Shift+A &#8594; Light &#8594; [Choose type]`), before `<p><strong>Quick tips:</strong></p>`. Anchor `</ol>` + blank + Quick-tips `<p>` (count=1).
  - Each `edit_file` dryRun &#8594; commit &#8594; `get_file_info`. Overlay SVGs kept their `style="position:absolute; inset:0; ... pointer-events:none"` as written; wrapper-div style `position: relative; display: inline-block; max-width: 100%`.
- **Figcaptions (comma/period, NO em-dash):** #6 "The Add menu (Shift+A) with the Light submenu expanded, showing Blender's four light types and their characteristics." ; #7 "The Light Data Properties panel, where light type, color, power, shadow, and custom distance are set."
- **Post-commit integrity CLEAN** (fresh copy, Python-authoritative): bytes 226,950, **em-dash 27** (held, added none), **literal middot 2** (unchanged), **`&#183;` entities 15** (7 base P1/P2 + 8 new = #6 overlay 4 + #7 overlay 4 in the legends; both figcaptions 0; LIVE-RECOUNTED, not trusted from handoff arithmetic), `&mdash;` 0, `color="#222"` 0, figure 5&#8594;**7/7**, img 2&#8594;**4**, svg 5&#8594;**7/7**, figcaption 5&#8594;**7**, overlay wrapper-divs 2&#8594;**4**, 12 section ids no dups, c6-title/c6-desc/c7-title/c7-desc each present 2&#215; (aria ref + `<title>`/`<desc>` id), both new PNGs referenced once each, all anchor landmarks (Light Setup Workflow / Quick tips / Light Data Properties Panel / Adding Your First Light) count=1. Pre-existing em-dash in the section ("never &lsquo;done&rsquo; on first try&#8212;keep refining!") untouched.
- **Blender teardown DONE** (the production scene was left staged): `get_scene_info` found `L15_P3_HostCube` / `L15_P3_HostLight` / `L15_P3_HostCam` &#8594; all 3 removed via `bpy.data.objects.remove`, orphans purged (3 data-blocks deleted, 0 orphans remaining), restored BLENDER_EEVEE + AgX/look None + world `(0.05)` str 1.0, cursor origin, re-shown toolbar/N-panel/gizmo + overlay defaults; Properties tab set to SCENE (OBJECT/MATERIAL unavailable with an empty scene). Verified clean empty base 0/0/0/0/0/0 (obj/mesh/light/cam/mat/img). Windows `C:\Users\Public\L15P3` deleted (`isdir` False) ; WSL `_tmp_l15_p3_fig6_preview.png` + `_tmp_l15_p3_fig7_preview.png` deleted (ENOENT-verified) ; the two `_c6`/`_c7_overlay_VERIFIED.svg` handoff artifacts deleted post-commit (ENOENT-verified). Deliverable PNGs `lesson_15_06_*` + `lesson_15_07_*` retained. No BlenderMCP debt.
- **Next: L15 P4** (`point-light`, #8 `pt` point_light_diagram SVG + #9 point_light_shadow render + #10 point_light_settings UI-chrome screenshot, 3 figs, single chat &#8212; mixed-method; split sub-chats only if context tightens). NEEDS BlenderMCP (#9 render + #10 docked-panel capture). Resolve any P4-specific decisions at plan-lock; D7 (#9 overlay-skip-or-minimal) and D9 (color-dependent render) are the relevant flags.

### 2026-06-12 &#8212; L15 P2 DONE (`light-fundamentals`, #3 `ct` + #4 `lu` + #5 `isq`; all hand-coded inline SVG; PRODUCTION + INTEGRATION single chat)
- **5/40 produced / 5/40 integrated.** HTML **195,982 &#8594; 217,735 B** (+21,753). Bottom-up commits: #3 `ct` +7,094 &#8594; 203,076 ; #5 `isq` +5,221 &#8594; 208,297 ; #4 `lu` +9,438 &#8594; 217,735. Entry-state PASSED on fresh copy + `get_file_info`: 195,982 B authoritative P0, em-dash 27, middot 2, figure 2/2, img 2, svg 2/2, figcaption 2/2, 12 section ids no dups, `ct`/`lu`/`isq` namespaces absent.
- **Standalone `.svg` written to `images/`** (root `color="#222"` kept for `<img>` fallback; `get_file_info`-confirmed): `lesson_15_03_color_temperature_scale.svg` 5,444 B ; `lesson_15_04_light_units_chart.svg` 7,598 B ; `lesson_15_05_inverse_square_law.svg` 4,510 B. Each render-checked light + dark via cairosvg (white-bg `color="#222"` fallback / `#e8e8e8`-on-`#1e1e1e` dark); text-overflow + warm-end label-collision fixed before write; self-assert PASSED (em-dash 0, one svg/title/desc, namespaces, role/aria).
- **D2 resolved:** #3 Kelvin gradient is legitimate CONTENT color (warm `#FF8E3C` &#8594; white `#FFFFFF` @6500K default, emphasized orange &#8594; cool `#7FB0FF`), 8 stops; brand orange `#FF6B00` = 3px top strip + 6500K emphasis only. viewBox 800&#215;560.
- **D3 resolved:** #4 three-column infographic, brand-palette column accents Watts `#FF6B00` / Lumens `#4A9EFF` / Candelas `#B47EE8` (orange/blue/purple, Ray-confirmed). viewBox 800&#215;680 dense.
- **#5** inverse-square diagram: light source + expanding cone, planes at 1d/2d/3d showing 100%/25%/11% and 1&#215;/4&#215;/9&#215; area, brand orange focal element. viewBox 800&#215;560.
- **Anchors (all count=1, bottom-up #3 &#8594; #5 &#8594; #4):** #3 after Light Color card `</div>` before `<h3>Light Visibility and Rendering</h3>` ; #5 after Light Falloff card before `<h3>Light Color</h3>` ; #4 after Light Units card before `<h3>Light Falloff</h3>`. Default-depth indentation (figure col16 / svg col20 / children col24 / `<g>` children col28). Each `edit_file` dryRun &#8594; commit &#8594; `get_file_info`.
- **Post-commit integrity CLEAN** (fresh copy, Python-authoritative): bytes 217,735, **em-dash 27** (held, added none), **middot 2** (unchanged P1 c2 pills; new figures use `&#183;` ENTITIES not literal), `&mdash;` 0, `color="#222"` 0, figure 5/5, img 2, svg 5/5, figcaption 5/5, 12 section ids no dups, all 6 new namespaces present 2&#215; (aria ref + `<title>` id), all 3 anchors count=1. Both pre-existing lesson-body em-dashes near anchors preserved verbatim ("Don't overdo it&#8212;subtle", "starting points&#8212;your scene scale"). Figcaptions written with comma/period (no em-dash).
- **No BlenderMCP debt** this phase (all-SVG, no renders). No scene teardown required.
- **Next: L15 P3** (`adding-lights`, #6 `add_light_menu` + #7 `properties_panel_overview`, 2 PNG annotated UI-chrome screenshots, single chat). P3 NEEDS BlenderMCP (docked-panel UI-chrome captures per LD20: full-window local-tempdir `screen.screenshot` + deterministic area crop) &#8212; unlike P2. Overlays `c6`/`c7`. Resolve any P3-specific decisions at plan-lock.

### 2026-06-12 &#8212; L15 P1 DONE (`why-lighting-matters`, #1 + #2 + overlays `c1`/`c2`; PRODUCTION + INTEGRATION single chat)
- **2/40 produced / 2/40 integrated.** HTML **191,211 &#8594; 195,982 B** (+4,771; #2 +1,956 &#8594; 193,167, then #1 +2,815 &#8594; 195,982). Entry-state flag RESOLVED: fresh `copy_file_user_to_claude` + `get_file_info` confirmed **191,211 B** authoritative P0 (the 191,258 banner figure was stale by 47 B; body pristine, em-dash 27); reconciled in `session.md` this close.
- **Scene (D1 lean path, locked):** ONE shared Cycles scene reused across both figures &#8212; Suzanne (subsurf 2, smooth) on a 20m grey ground plane (base `(0.18,0.18,0.19)` rough 0.85), neutral mid-grey Suzanne material (`(0.45,0.45,0.46)` rough 0.55), fixed 50mm camera at `(0,-6,2.4)` aimed at `(0,0,1.1)`, black 0-strength world. Cycles GPU/OPTIX, 128 spp + denoise, 800&times;800 per panel, **AgX** view transform. Only lighting changed between renders.
- **#1** `lesson_15_01_lighting_impact_comparison.png` (1602&times;1602, ~1.0 MB): 4 Cycles renders Pillow-composited 2&times;2 with `#FF6B00` 2px dividers, NO baked labels. Panels: No light (black, subject_mean 0.1) / Poor (single hard frontal point, power 900 radius 0.02, flat+harsh, 92.8) / Good (area key 700/size1.5 + area fill 180/size2.0, dimensional, 118.4) / Excellent (3-point soft: big key 600/size3.0 + soft fill 220/size3.5 + rim 900/size1.6, 133.3). Luminance climbs cleanly, subj_max 241 (not blown) &#8594; **D9 resolved: AgX did NOT wash, LD17 Standard NOT invoked.**
- **#2** `lesson_15_02_hard_soft_light_comparison.png` (1602&times;800, ~626 KB): 2 Cycles renders side-by-side, `#FF6B00` 2px divider, NO baked labels. Same area light at fixed loc `(-3.2,-3.0,4.3)` / power 700; only `size` differs &#8212; Hard `size=0.12` (sharp shadow, mean 71.8) / Soft `size=5.0` (broad penumbra, mean 71.7). Source-size-drives-softness confirmed in Cycles; brightness-matched.
- **Overlays (composite-overlay variant: 1 centered brand-neutral pill per panel near bottom edge, no highlight/leader; baked dividers carry separation):** `c1` 4 pills (No Lighting / Poor Lighting / Good Lighting / Excellent Lighting), `c2` 2 pills (Hard Light &#183; Small Source / Soft Light &#183; Large Source, **middot** not em-dash). Pill style: `#1a1a1a` fill-opacity 0.82, rx-pill, fs 30 weight 600 white text, centered. Panel centers x=400/1202; #1 row pills cy=730 (top) / 1532 (bottom), #2 cy=730. **Both cairosvg-pixel-verified 1:1 over the real PNGs before commit** (viewBox = PNG px). NO `c{N}-*` id attributes (composite-overlay pill pattern; uniqueness tracked via once-each PNG refs).
- **INTEGRATION:** both default-depth figures (parent = content-section, not a card) &#8212; figure col16 / div+img+svg+figcaption col20 / `<g>`+children col24/28. Wrapper-div (`position: relative; display: inline-block; max-width: 100%`) + img (`margin: 0`) + inlined overlay-SVG (`pointer-events: none`, viewBox=PNG px), figcaption OUTSIDE. Inserted BOTTOM-UP (#2 first after the Hard vs. Soft Light card `</div>` before `<h3>Light and Mood</h3>`; #1 second after the Power of Light card `</div>` before the Photography Analogy `<blockquote>`). `edit_file` dryRun &#8594; commit &#8594; `get_file_info` each; both anchors count=1 at insert (HTML untouched between entry-copy and commits, so anchors stable).
- **Post-commit integrity CLEAN** (fresh copy, Python-authoritative): bytes 195,982, **em-dash 27** (held, added none), **middot 2** (the two c2 pill middots), `&mdash;` 0, `&#183;` 0, `color="#222"` 0, figure 2/2, img 2, svg 2/2, figcaption 2/2, wrapper-divs 2, 12 section ids no dups, both PNGs referenced once each, all 4 placement landmarks (Photography Analogy / Light and Mood / Power of Light / Hard vs. Soft Light) count=1. Figcaptions written with sentence-break/comma/period (no em-dash).
- **Blender teardown DONE:** scene RESTORED to clean empty base (0 obj / 0 mesh / 0 mat / 0 light; orphans purged), EEVEE + AgX + world `(0.05)` str 1.0 restored, cursor origin. Windows `C:\Users\Public\L15P1` (8 renders + 2 previews) deleted + `os.path.isdir` False. 2 WSL review-preview temps (`_tmp_l15_p1_preview_fig1/2.png`) deleted + ENOENT-verified. No BlenderMCP debt.
- **Next: L15 P2** (`light-fundamentals`, #3 `ct` color-temperature-scale + #4 `lu` light-units-chart + #5 `isq` inverse-square-law, all hand-coded inline SVG, single chat). Resolve **D2** (Kelvin gradient content-color stops) + **D3** (light-units 3-col accents, brand roles, viewBox) at P2 plan-lock.

### 2026-06-12 &#8212; L15 PLANNING KICKOFF (this entry)
- Read in order: `session.md` (banner + Next-active-target L15 + Locked rules), `lesson_15_image_requirements.md` (full, 40 images), `images/IMAGE_STYLE_GUIDE.md` v6 (LD 1&#8211;21).
- Entry-state verified on a FRESH `copy_file_user_to_claude` copy + `get_file_info` on the real UNC path: **HTML = 191,211 B** (banner said 191,258; **47 B discrepancy flagged** &#8212; body is pristine, so the banner figure is stale; 191,211 is authoritative P0). Body confirmed pristine: 0 figure / 0 img / 0 svg / 0 figcaption / 0 overlay wrapper-div. 12 `<section>` ids, all unique, no collisions. 3 `mermaid` refs (1 live diagram block at ~line 1980 in `comparing-lights`). No `c{N}` ids present. **em-dash 27** (U+2014, all pre-existing prose; Python-authoritative), middot 0, `&mdash;` 0, `&#183;` 0.
- Wrote this `lesson_15_integration_progress.md` (figure registry + id-prefix registry + c{N} namespace + 12-phase plan in HTML section order + this Status log + Locked-rules block), mirroring the L13/L14 progress-doc structure. NO figure production this chat (planning only).
- Plan: **40 figures, 13 SVG / 27 PNG**, 12 phases P1&#8594;P12 in section order. No phase &#8805;6, so no INTEGRATION-own-chat pre-mandated; P6 (spot, 5) and P10 (scenarios, 5) pre-plan a PRODUCTION sub-chat split. 9 DECISIONS (D1&#8211;D9) flagged for resolution at each phase plan-lock; the big one is **D6** (scenario-setup sourcing method, P10).
- **Next: L15 P1 plan-lock + PRODUCTION** (`why-lighting-matters`, #1 + #2). Read at chat start: `session.md`, this doc (Status log newest + phase plan + Locked-rules), `lesson_15_image_requirements.md`, `images/IMAGE_STYLE_GUIDE.md` v6.

---

## 7. Locked rules (carry forward from L09 / L14)

UNCHANGED from the L14 / L09 progress-doc Locked-rules blocks and `session.md`:

- **Paths:** WSL `\\wsl$\Ubuntu\` only (never `\\wsl.localhost\`). The Claude-side bash `/mnt/wsl/...` mount is a SEPARATE near-empty FS, NOT the project &#8212; never read baselines or commit there. Confirm every write landed by re-reading the real path's byte count.
- **File ops:** `Filesystem:write_file` for new files / full-doc overwrites (never `create_file`); `copy_file_user_to_claude` to refresh the working HTML copy before grep/sed; `tool_search` for deferred Filesystem tools (silently).
- **Editing:** anchor by unique TEXT span extended until count=1 (never by section id); `Filesystem:edit_file` dryRun &#8594; commit &#8594; `get_file_info` after every commit. Anchor 2&#8211;3 lines default, extend to 4&#8211;8 for ambiguous spots. Bottom-up insertion order within a phase.
- **em-dash discipline:** no new em-dashes; baseline **27** held. Separators `&#183;`; content entities `&#8211;` / `&#8594;` / `&#215;` / `&#8217;` fine. Pre-existing em-dashes preserved verbatim through exact anchors. Python `read().count('\u2014')` authoritative (grep miscounts multibyte).
- **Brand palette governs** all overlays/SVGs (`#FF6B00` / `#4A9EFF` / `#B47EE8` / `#FF7EA8` / `#4ADE80` / `#F5C242` / `#E63946` / badge text `#0f1a14` on green, `#ffffff` on red). The requirements doc's `#2b2b2b` / `#ff8c00` / `#00ffff` + its "AI-prompt" raster language are LOOSE guidance only &#8212; reconcile at each plan-lock. Content colors (Kelvin gradient #3, axis tints) are legitimate exceptions, like L14 socket hues. Every course SVG is HAND-CODED (never Canva/AI raster).
- **Inline-SVG conventions:** strip root `color="#222"` on inlining; re-indent figure col20 / svg col24 / children col28 / deeper col32; collapse multi-line attrs; prune decorative `====` comments, keep structural ones; preserve every `role` / `aria-labelledby` / `<title>` / `<desc>` / `id` verbatim. Standalone `.svg` in `images/` keeps `color="#222"` for `<img>` fallback.
- **PNG overlay pattern:** wrapper-div (`position:relative; display:inline-block; max-width:100%`) + `<img style="display:block; max-width:100%; height:auto; margin:0">` + inlined overlay-SVG (`position:absolute; inset:0; width:100%; height:100%; pointer-events:none`, viewBox = PNG pixels), figcaption OUTSIDE. NO baked labels. Overlay ids `c{N}-*`. **Pixel-verify every PNG overlay via cairosvg-over-PNG composite (Claude-side, 1:1 since viewBox=PNG px) BEFORE commit** (LD19); PIL-composite substitute when the check runs bridge-side (cairosvg unavailable inside Blender 5.1 Python on Windows).
- **Figure indentation depth** from the parent container (default 16/20-space; 20/24/28/32 deeper-indent for figures inside col-16 cards).
- **BlenderMCP (render phases):** lowercase `blender:execute_blender_code`; clean scene manually via `bpy.data.objects.remove` (NEVER `read_factory_settings`); inline all render helpers per call (namespaces do not persist); BLENDER_EEVEE (EEVEE_NEXT&#8594;EEVEE fallback) + Cycles for off-screen reflection/refraction and accurate shadow-softness; grid-floor = real emissive line geometry via `make_grid_lines` (viewport grid does not render). Render-to-Windows-tempdir-then-Pillow-relay to WSL `images/` (UNC direct-write truncates to 0 bytes). **color-dependent render recipe (v6 LD17): F12 `render.render` NOT `render.opengl`, view_transform Standard / look None, on a 0.05 world** &#8212; invoke ONLY if a PRODUCTION review shows AgX washing (not pre-forced); RESTORE AgX + base values at chat close. Capture-path routing (LD20): render-unreachable overlays &#8594; synthetic-cage/overlay-over-render from recorded `world_to_camera_view` pixels; docked-panel UI chrome &#8594; full-window local-tempdir `screen.screenshot` + deterministic area crop; transient popovers &#8594; Win+Shift+S overlay-freeze direct to WSL `images/`; clean ortho/reference &#8594; hide toolbar/N-panel/nav-gizmo first. Modal capture (LD14): status bar authoritative for bindings; frame must show live operator readout + modal hints. SOLID-shading readability (LD15): `show_wire` ON / `Alt+Z` X-ray for cut captures. **RESTORE scene to clean empty base at every PRODUCTION close** (STOP if a leftover `_WORK` collection or hidden base objects are found).
- **Warning-vignette figures (LD21):** Pillow-composited vignette PNG with NO baked text, clear bands reserved for the `c{N}` INTEGRATION overlay (warning-red problems / positive-green solution). Not a render, not a Canva raster. (Candidate host for some P10 scenario diagrams &#8212; see D6.)
- **Theory/infographic sourcing (LD21 family)** is a PRODUCTION-time call per figure (Pillow-vignette host vs hand-coded SVG vs render-composite).
- **Workflow:** plan-lock before any build (propose spec + design plan in writing, locked here); 1 pilot then batches of 3 for SVG-heavy phases; one SVG per file; warn Ray before context tightens rather than letting auto-compaction happen.

---

## Deferred queue (behind L15, per session.md)

L05&#8211;L08 browser-QA (L08 parked) &#183; L07 #20/#21 held flags &#183; L11 1024-width sweep &#183; lesson-body pass for L09-F2 (414 `#summary` table overflow 25px ~line 5714) + L12 (dup `id="uv-editor-interface"` + broken TOC) + L13 (414 table overflow `#what-is-texture-painting`). All itemized in `session.md` Standing-deferred.

**L15 lesson-body note (NOT figure work, queued during P8):** the Shadow Settings card (`light-properties`, ~line 2769) describes **"Contact Shadow (Eevee only)"** as a live per-light checkbox enabled via `Light Properties &#8594; Shadow &#8594; Contact Shadows`. This is **STALE for Blender 4.2+ / EEVEE-Next**, which REMOVED the `use_contact_shadow` feature (replaced by ray-traced shadows / Fast-GI). Body text needs updating in a future lesson-body pass &#8212; figure work does not touch it.
