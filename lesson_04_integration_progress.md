# Lesson 04 HTML Integration: Progress

**Current state:** COMPLETE 2026-05-19. All 14 Lesson 04 images produced or captured (10 SVGs + 4 PNGs) and inlined into `lesson_04_basic_object_manipulation.html` across 6 phases. 14 of 14 integrated. HTML grew from 106.0 KB (108,546 bytes) to 229.2 KB (234,730 bytes); total integration delta +123.2 KB (+126,184 bytes). Lesson close-out documented in `lesson_04_completion_handoff.md`.

Running narrative log for Lesson 04 image production and integration. Status section bullets run newest at top, oldest at bottom; each bullet corresponds to one chat in the per-phase cadence (production-then-integration from Phase 4 onward; Phases 1, 2, and 3 combined production plus integration in single chats since they predated the cadence lock-in).

The intermediate per-phase handoff files from Phases 1, 2, and 3 (six files total) were moved to `cleanup/` on 2026-05-20; they are superseded by this doc plus `lesson_04_completion_handoff.md`. See `cleanup/README.md` for the inventory. Phases 4, 5, and 6 did not generate dedicated handoff files (they used `session.md` overwrite instead).

## Status

- Phase 6 integration chat (this chat). Inlined PNG #13 and SVG #14 into `lesson_04_basic_object_manipulation.html`, closing Lesson 04 image integration at 14 of 14. PNG #13 `lesson_04_13_completed_scene.png` inserted in the `#manipulation-project` section between the Project Goal info card's closing `</div>` and the `<h3>Step 1: Setup and Cleanup</h3>` as the visual project target before the seven-step build begins. Plain `<figure>` / `<img>` / `<figcaption>` pattern (no annotation overlay): the rendered table scene reads as the target on its own, unlike the annotated #8, #9, and #12 captures from Phases 4 and 5 which all needed orange callouts to point at specific elements. Alt text describes the underlying scene content (three-quarter perspective Eevee render, neutral gray ground plane, square table with four short legs, four primitive shapes arranged on the tabletop, soft directional lighting from upper right, default neutral gray materials throughout); figcaption complements the project-target framing: "The finished table scene that this section's seven steps build, one operation at a time. A 2-meter table top sits on four short legs, with a sphere, cone, torus, and cylinder placed on top. Match this layout in your own scene and you've covered every manipulation tool the lesson teaches." SVG #14 `lesson_04_14_keyboard_shortcuts.svg` (id prefix `ks`) inserted in the `#summary` section between the "Your Manipulation Cheat Sheet" card's closing `</div>` and the `<h3>Common Questions at This Stage</h3>`, mirroring the Lesson 02 #18 and Lesson 03 #18 placement pattern (tabular cheat sheet first, visual reference card after). `color="#222"` attribute stripped from the inlined root `<svg>` tag (the standalone `.svg` file in `images/` retains that attribute for `<img>` fallback); content re-indented to 20-space content depth per the locked inline pattern (figure and /figure at column 16, svg open and close plus figcaption at column 20, svg-attribute continuations at column 25, SVG body at column 22, multi-line rect attribute continuations inside the footer tip block at column 28, `<tspan>` content inside `<text>` at column 24). Every `role`, `aria-labelledby`, `<title>`, `<desc>`, and `id` preserved verbatim with the `ks-` prefix carried through unchanged; no collision with the existing `ss/st/tg/am/gl/ra/so/dc/op` id prefixes already on the page (the three inline-stacked PNG annotation overlay namespaces `c8/c9/c12` also stay distinct since they live inside the wrapper-div PNG figures). Both inserts via `Filesystem:edit_file` with `dryRun=true` first then commit. The em-dash inside the M2 anchor source paragraph ("Keep this reference handy—these shortcuts form the core of 3D workflow!") preserved verbatim through the edit. SVG #14 figcaption (em-dash-free, complementing the tabular cheat sheet above the figure): "The same shortcuts in card form. Orange badges call out every key combo at a glance: selection on the left top, then move, rotate, scale below it; duplication and the snap menu on the right top, then the apply-and-reset shortcuts at the bottom. Pin or print this when you're getting started." HTML size deltas: M1 PNG #13 added 944 bytes (220793 to 221737), slightly above the projected 400 bytes since the alt text ran on the longer side describing the scene composition; M2 SVG #14 added 12993 bytes (221737 to 234730), slightly above the projected 9.5 KB since the 20-space content depth re-indentation expanded each of the roughly 100 SVG body lines by about 20 bytes on top of the inline figcaption text. Total Phase 6 INTEGRATION growth: 13937 bytes (13.6 KB) over the two inserts. Final HTML 234730 bytes (229.2 KB), up from 220793 bytes (215.6 KB) at Phase 6 INTEGRATION chat start. Placement table rows #13 and #14 flipped from `captured; not integrated` and `produced; not integrated` respectively to `integrated`; phase plan table Phase 6 row flipped from `2 of 2 produced or captured, 0 of 2 integrated` to `2 of 2 produced or captured, 2 of 2 integrated`. Lesson 04 image work now closed: 14 of 14 images produced, 14 of 14 inlined, with the lesson joining Lessons 01, 02, and 03 in the complete tier of the Site-Wide Feature row. No new em-dashes introduced anywhere in the new content. Both inserts verified on disk via `Filesystem:get_file_info` after each commit (221737 bytes after M1, 234730 bytes after M2).
- Phase 6 production chat (this chat). Captured PNG #13 and authored SVG #14, writing both to `images/`. PNG #13 `lesson_04_13_completed_scene.png` at 501641 bytes (490 KB), 1280x720 Eevee render via the BlenderMCP bridge under the addon-safe pattern. Scene state verified on chat entry via `blender:get_scene_info` showing the table-scene build from a prior session matching `session.md`'s spec exactly: 6m ground plane at z=0 (plane primitive scaled), 2m square table top at z=0.75 (cube primitive scaled to 1.0 by 1.0 by 0.05), four corner legs at (plus or minus 0.9, plus or minus 0.9, 0.35) sized 0.1 by 0.1 by 0.7 meters, four target objects on the table top (UV sphere at (-0.55, -0.55, 1.0) dimensioned 0.4 by 0.4 by 0.4, cone at (0.55, -0.55, 1.05) dimensioned 0.4 by 0.4 by 0.5, torus at (0.55, 0.55, 0.85) dimensioned 0.5 by 0.5 by 0.1 lying flat on the table top, cylinder at (-0.55, 0.55, 1.0) dimensioned 0.3 by 0.3 by 0.4), point light at (3, 3, 6), camera at (4.5, -4.5, 3.5) for the three-quarter perspective angle, two default materials present. Render engine attempted as `BLENDER_EEVEE_NEXT` first per `session.md` spec; the Blender install in this session exposed only `('BLENDER_EEVEE', 'BLENDER_WORKBENCH', 'CYCLES')`, so fell back to `BLENDER_EEVEE` as documented in `lesson_04_image_prompts.md` ("If [BLENDER_EEVEE_NEXT] fails, try 'BLENDER_EEVEE' (older engine name) or 'CYCLES'"). Rendered via `bpy.ops.render.render(write_still=True)` directly to `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\images\lesson_04_13_completed_scene.png`. Resulting PNG size 490 KB sits comfortably inside the 200 KB to 1 MB target band specified in `session.md`. Modified timestamp on the file refreshed to 2026-05-19 20:13:30 PDT after the render. LOCKED RULE from 2026-05-19 carried through: no `bpy.ops.wm.read_factory_settings(use_empty=False)` calls anywhere; the render operates on the existing scene state without resetting. SVG #14 `lesson_04_14_keyboard_shortcuts.svg` at 10062 bytes (9.8 KB), id prefix `ks`, two-column cheat-sheet card adapting the Lesson 02 #18 (`lesson_02_18_keyboard_shortcuts_reference.svg`) plus Lesson 03 #18 (`lesson_03_18_navigation_shortcuts_reference.svg`) reference-card pattern. 800x560 viewBox per `session.md` spec, tighter than the 800x680 canvas used by Lessons 02 and 03 cheat sheets and matching the rest of Lesson 04's diagram height; the tighter canvas required compacting TRANSFORMATIONS from a planned 4 rows down to 3 to fit, with the local-axis nuance (press axis key twice for local) folded into the footer tip at y=506. Four category groupings stacked two-per-column: LEFT column has SELECTION (7 rows: A, Alt + A, B, C, Ctrl + drag, Click, Shift + Click) above TRANSFORMATIONS (3 compacted rows: G / R / S move-rotate-scale, +X/Y/Z constrain to global axis, +number for exact value); RIGHT column has DUPLICATION & CURSOR (4 rows: Shift + D, Alt + D, Shift + S, Shift + Right Click) above ADVANCED (3 rows: Ctrl + A apply transforms, Alt + G / R / S reset transforms, N toggle sidebar). Footer tip reads `Tip: Press the axis key twice for local axes. G X moves along world X, G X X moves along the object's own X.` Key-combo badges rendered as Blender-orange `#FF6B00` solid-fill rounded rects (rx=4, height 22) with white bold (font-weight 700) 12pt text per `session.md` spec, a deliberate variation from the muted `currentColor` `fill-opacity=0.08` badge pattern used in Lessons 02 #18 and Lesson 03 #18 (both of which use weight-600 currentColor text on a faintly tinted background). The orange-fill choice ties into Lesson 04's broader operation-badge idiom established in #10 (Shift + D, Alt + D badges) and #5 (S X 2, S Z 3, S Y .5 badges) where the same orange-on-white styling spotlights individual shortcuts on diagrams; here it is applied uniformly across all 17 keyboard rows for cheat-sheet consistency. Description text in `currentColor` at standard 13pt, plus separators between badge halves at `currentColor opacity=0.5`, category headers `#FF6B00` weight 700 letter-spacing 1.5, footer tip rect with orange-tinted fill (`#FF6B00` opacity 0.10) and orange-tinted stroke (`#FF6B00` opacity 0.4). Standalone `.svg` file retains `color="#222"` on the root `<svg>` tag for `<img>` fallback rendering; Phase 6 INTEGRATION will strip that attribute on inline per the locked pattern. Full accessibility tags: `role="img"` plus `aria-labelledby="ks-title ks-desc"` pointing at `<title id="ks-title">` ("Keyboard Shortcuts: Object Manipulation") and `<desc id="ks-desc">` ("A reference card of essential object-manipulation shortcuts for Blender 5.1, organized into four categories: selection, transformations, duplication and cursor, and advanced controls."). Id prefix `ks` matches Lesson 02 #18's prefix exactly; reuse is safe per the id-prefix registry note since Lesson 02 SVGs and Lesson 04 SVGs never share a page. Single in-chat correction on the file: a typo on the `Shift + Right Click` row's description text element placed at y=288 instead of y=268 (which would have pushed the description below the badge baseline rather than inline with it) was caught immediately and corrected via `Filesystem:edit_file` `dryRun=true` then commit; SVG re-verified on disk at 10062 bytes after the fix. No new em-dashes anywhere in the new content. Both #13 and #14 verified on disk via `Filesystem:get_file_info` after write. Available section advanced from 12 of 14 to 14 of 14 with new PNG entry for #13 (501641 bytes, 490 KB, 1280x720) and new SVG entry for #14 (10062 bytes, 9.8 KB, id prefix `ks`); Missing section reduced from 2 of 14 to 0 of 14 (full set now on disk). Placement table row #13 flipped from `not captured; not integrated` to `captured; not integrated`; row #14 stays at `produced; not integrated` as the planning chat's anticipatory population predicted (the inconsistency between the row status and the disk state resolved naturally now that Phase 6 production landed the SVG). Phase plan Phase 6 row flipped from `0 of 2 produced or captured, 0 of 2 integrated` to `2 of 2 produced or captured, 0 of 2 integrated`. No HTML edits this chat per the production-then-integration cadence locked in `session.md`; Phase 6 INTEGRATION is the next chat: inline PNG #13 as a plain `<figure>` / `<img>` / `<figcaption>` insert in the manipulation-project section between the Project Goal info card's closing `</div>` and the `<h3>Step 1: Setup and Cleanup</h3>` (no overlay needed; the rendered scene reads as the visual project target on its own), inline SVG #14 in the summary section between the Your Manipulation Cheat Sheet card's closing `</div>` and the `<h3>Common Questions at This Stage</h3>` with `color="#222"` stripped from the root `<svg>` and content re-indented to 20-space content depth per the locked inline SVG pattern.
- Phase 5 integration chat (this chat). Inlined all three Phase 5 assets into `lesson_04_basic_object_manipulation.html`. SVG #10 `lesson_04_10_duplication_comparison.svg` (id prefix `dc`) inserted in the `#duplication` section between the "Linked Duplicate" card's closing `</div>` and the opening of the "When to Use Linked Duplication" info card; SVG #11 `lesson_04_11_origin_point.svg` (id prefix `op`) inserted in the `#origin-transform` section between the "Origin Point Functions" card's closing `</div>` and the opening of the "Why Origins Matter" info card; PNG #12 `lesson_04_12_transform_properties.png` inserted in the `#origin-transform` section between the "Transform Properties" card's closing `</div>` and the opening of the "Try It Now: Precise Positioning" card. All three inserts via `Filesystem:edit_file` with `dryRun=true` first then commit. For #10 and #11, `color="#222"` attribute stripped from each inlined root `<svg>` tag (the standalone `.svg` files in `images/` retain that attribute for `<img>` fallback); content re-indented to 20-space content depth per the locked inline pattern (figure and /figure at column 16, svg open and close plus figcaption at column 20, svg-attribute continuations at column 25, SVG body at column 22, `<g>` children at column 24, rect and path attribute continuations inside `<g>` at column 30); every `role`, `aria-labelledby`, `<title>`, `<desc>`, and `id` preserved verbatim with `dc-` and `op-` prefixes carried through unchanged so no collision with the existing `ss/st/tg/am/gl/ra/so/c8/c9` id prefixes already on the page. For #12, a `c12-` namespaced annotation overlay was authored alongside the figure following the locked Phase 4 inline-stacked overlay pattern (`<figure>` wraps a `position: relative; display: inline-block; max-width: 100%` div containing the `<img>` and an absolutely-positioned overlay `<svg>` with `pointer-events: none`, viewBox `0 0 1920 1046` matching the PNG pixel dimensions exactly, namespaced `c12-` id prefix for title and desc). Overlay decision at integration time landed on the minimal #9-style pattern rather than the multi-callout #8-style: a single dashed orange `#FF6B00` rectangle at `x=915 y=80 w=290 h=460` with `stroke-width=6` and `stroke-dasharray="24 14"` traces the N-panel Item tab Transform section, plus a filled orange tab-style pill at `x=915 y=24 w=480 h=50` with white 30px bold "N-Panel: Transform Section" text sitting above the rectangle's top edge. No individual arrowed field callouts for Location, Rotation, Scale, Dimensions, since the N-panel itself already labels each field clearly at lesson display width. Overlay geometry was previewed via PIL on the actual 1920x1046 PNG and on a downscaled 800px lesson-display-width version to confirm legibility before HTML commit per the Phase 4 cadence. The img element carries inline `style="display: block; max-width: 100%; height: auto; margin: 0;"` to override the global `img { margin: var(--spacing-md) auto; }` rule so the wrapper div sizes cleanly to the image content. Alt text on the #12 `<img>` describes the underlying screenshot content (Blender 5.1 viewport in Object Mode, default cube with transformed Location 2.5/-1/1.5 m, Rotation 30/0/45 degrees XYZ Euler, Scale 1.500/1.000/0.750, Dimensions 3/2/1.5 m, navigation gizmo and sidebar tabs visible); the overlay SVG `<title>` and `<desc>` cover the annotation specifically so assistive tech receives both. All three figcaptions em-dash-free, conversational, pointing the reader at what to notice. SVG #10 figcaption: "Two side by side panels. On the left, Shift + D copies the cube wholesale: two separate meshes, edit one and the other stays untouched. On the right, Alt + D shares one mesh between both cubes; the small chain link between them is the visual cue that an Edit Mode change to either will ripple through to its partner." SVG #11 figcaption: "A single cube with the small orange dot at its center marking the object origin. The three labeled cards around it spell out what that one point does: the pivot for R, the center for S, and the X, Y, Z values the N-panel reports for Location. Move the origin and every transform reroutes through the new point." PNG #12 figcaption: "The N-panel Item tab with the default cube's Location set to (2.5, -1.0, 1.5), Rotation to thirty degrees on X and forty-five on Z, and Scale stretched to 1.5 on X and squashed to 0.75 on Z. Dimensions report the actual size the cube takes up in the scene after those scales apply, in Blender units." No new em-dashes introduced anywhere in the new content. Source-prose em-dashes inside the M2 anchor for #11 (the "Why Origins Matter" paragraph reads "hinge is in the middle—it would swing all wrong! Origins are like hinges—they determine") preserved verbatim through the edit. HTML size deltas per insert: M1 #10 added 11091 bytes (197567 to 208658); M2 #11 added 9596 bytes (208658 to 218254); M3 #12 with overlay added 2539 bytes (218254 to 220793). Total Phase 5 integration growth: 23226 bytes (22.7 KB) over the three inserts. Final HTML 220793 bytes (215.6 KB), up from 197567 bytes (192.9 KB) at Phase 5 integration chat start. Placement table rows for #10, #11, and #12 flipped from `produced; not integrated`, `produced; not integrated`, and `captured; not integrated` respectively to `integrated`; phase plan table Phase 5 row flipped to "3 of 3 produced or captured, 3 of 3 integrated". `status.md` updated (Site-Wide Feature row Lesson 04 entry to 12 of 14 produced 12 of 14 integrated; Image Integration Status row Lesson 04 cell to 12 of 14 with appended Phase 5 paragraph; Module 1 Lesson File Status row Lesson 04 file size refreshed to 215.6 KB; Other Documentation entry for lesson_04_integration_progress.md refreshed). `session.md` overwritten to CLOSED CLEAN pointing at Phase 6: PNG #13 `lesson_04_13_completed_scene.png` (finished table-scene Eevee Next render via BlenderMCP, scene built programmatically per the addon-safe pattern: delete the default cube manually then add ground plane plus table top plus four legs plus sphere plus cone plus torus plus cylinder) and SVG #14 `lesson_04_14_keyboard_shortcuts.svg` (id prefix `ks`, two-column cheat sheet adapting the Lesson 02 #18 plus Lesson 03 #18 reference-card pattern).
- Phase 5 production chat (this chat). Authored both Phase 5 SVGs and captured the Phase 5 PNG, writing all three to `images/`. `lesson_04_10_duplication_comparison.svg` at 8315 bytes (8.1 KB), id prefix `dc`, two-panel comparison adapting the Lesson 04 #5 paired-panel pattern with 340x260 panels at translate(40, 160) and translate(420, 160). Left panel labeled Independent Copy shows the Shift + D shortcut: original cube at panel-local front-face (30, 105) and duplicate cube at (210, 105) drawn identically to convey equal-but-separate, with an orange `#FF6B00` translation arrow from (105, 130) to (200, 130) between them. Right panel labeled Linked Copy shows the Alt + D shortcut with the same cube layout and arrow, plus a horizontal chain-link icon below the arrow midpoint (two interlocking orange ellipses at panel-local cx=140 and cx=158, both cy=170, rx=11, ry=7) and a small italic 'shared mesh data' label at y=190. Operation badges rendered as Blender-orange `#FF6B00` rounded rects at panel-local (130, 42) width 80 height 22 with white 13pt bold text (Shift + D on left, Alt + D on right). Shared inset at translate(60, 438) carries the INDEPENDENT VS LINKED one-line takeaway: 'Shift + D copies the mesh data too, so each cube has its own geometry. Alt + D shares one mesh between both, so edits in Edit Mode show up on every linked instance.' Footer tip at y=506: 'Tip: Reach for Alt + D when you need lots of the same geometry, rivets, leaves, fence posts. One Edit Mode tweak updates every copy at once.' File size 8.1 KB sits just under the 9 to 12 KB target band, accepted since the compact paired-panel composition rendered more efficiently than the predicted byte estimate while still covering every design element from the M1 sketch. `lesson_04_11_origin_point.svg` at 7106 bytes (6.9 KB), id prefix `op`, single-panel hero composition adapting the Lesson 03 #1 big-three-operations pattern (one central visual with three supporting annotation cards). Hero cube at front-face (350, 270) to (450, 370) with depth offset (30, 30) using the same fill-then-outline pattern as #5 and #10. Origin dot at the geometric center (415, 305) drawn as a three-layer stack: outer halo `#FF6B00` opacity 0.20 r=11, solid orange disc r=7, white center dot r=2.5; an italic orange 'origin' label tag sits at (427, 322). Three annotation cards 200x50 each at translate(300, 155) Rotation pivot above, translate(80, 385) Scale center lower-left, translate(520, 385) Location reference lower-right. Each card carries a short label, one line of subtext, and a custom 24x24 icon at the card right edge: rotation card has an arc-with-arrowhead and a center dot, scale card has four outward arrows from a center dot, location card has X red `#E63946`, Y green `#4ADE80`, Z blue `#4A9EFF` axes radiating from a center dot using the locked axis palette. Three dashed connector lines run from the inner edge of each card to the origin dot at (415, 305) in `currentColor` opacity 0.30, drawn before the cube so the cube outline and origin dot sit on top. Italic takeaway at y=470: 'Move the origin and rotation, scale, and location all reroute through the new point.' Footer tip at y=506: 'Tip: The origin sits at the center of every transform. If a rotation looks off axis or a scale stretches the wrong way, check the origin first.' File size 6.9 KB sits just under the 8 to 11 KB target band, accepted given the simpler single-hero composition relative to the multi-panel SVGs. Both SVGs verified on disk via `Filesystem:get_file_info`. Locked house style followed verbatim: 800x560 viewBox, 3px Blender-orange top strip at y=0, `currentColor` labels and connector lines, footer tip callout in `#FF6B00`, full accessibility tags (`role="img"` plus `aria-labelledby` pointing at matching `<title>` and `<desc>`), `color="#222"` retained on each standalone root for `<img>` fallback. Id prefix continuity confirmed: `dc` and `op` do not collide with the existing `ss/st/tg/am/gl/ra/so` prefixes already on the page; #11's `op` does match the Lesson 03 #3 orbit_operation prefix, but Lesson 03 and Lesson 04 SVGs never share a page so the reuse is safe per the id-prefix registry note. `lesson_04_12_transform_properties.png` at 453308 bytes (442.7 KB), N-panel Item tab capture via the BlenderMCP bridge under the addon-safe pattern documented in `lesson_04_image_prompts.md`. Scene setup: default cube's `location = (2.5, -1.0, 1.5)`, `rotation_euler = (radians(30), 0, radians(45))`, `scale = (1.5, 1.0, 0.75)` so the N-panel Item tab fields show meaningfully non-default values; cube re-selected and made active; N-panel toggled open via `space.show_region_ui = True`. Captured via `bpy.ops.screen.screenshot(filepath="//images/lesson_04_12_transform_properties.png", full=False)`. The full-window screenshot captures the N-panel layout alongside the adjacent viewport showing the transformed cube; a post-crop step during integration can tighten the frame onto the N-panel if needed. Annotation overlay (orange outline rectangle around the Transform section plus arrowed callouts for Location, Rotation, Scale, Dimensions) deferred to the Phase 5 integration chat where it can ride as an inline-stacked SVG overlay per the locked Phase 4 integration pattern (wrapper div with `position: relative; display: inline-block`, overlay SVG with `position: absolute` and `pointer-events: none`, namespaced id prefix `c12-`). LOCKED RULE from 2026-05-19 carried through: no `bpy.ops.wm.read_factory_settings(use_empty=False)` calls anywhere in the Phase 5 capture code; the cube transform setup operates on the existing default scene without resetting. PNG verified on disk via `Filesystem:get_file_info`. Available section advanced from 9 of 14 to 12 of 14 with new SVG entries for #10 and #11 and new PNG entry for #12; Missing section reduced from 5 of 14 to 2 of 14 (only #13 and #14 remain). Placement table row #12 flipped from `not captured; not integrated` to `captured; not integrated`; rows for #10 and #11 already sat at `produced; not integrated` from the planning chat's anticipatory population, so they did not need updating. Phase plan Phase 5 row flipped to `3 of 3 produced or captured, 0 of 3 integrated`. No HTML edits this chat per the production-then-integration cadence locked in `session.md`; Phase 5 integration is the next chat: inline #10 and #11 with strip-`color="#222"` and 20-space content depth per the locked inline SVG pattern, inline #12 with the locked PNG figure pattern (plain `<figure>` plus `<img>` plus `<figcaption>` with descriptive alt text), plus optional `c12-` annotation overlay if the captured screenshot's contents need additional callouts to read clearly. No new em-dashes introduced anywhere in the new content.
- Phase 4 integration chat (this chat). Inlined both Phase 4 PNGs into `lesson_04_basic_object_manipulation.html` and authored inline SVG annotation overlays for each. PNG #8 `lesson_04_08_3d_cursor.png` inserted in the `#3d-cursor` section between the Moving the 3D Cursor card's closing `</div>` and the opening of the Try It Now: Position the Cursor card; PNG #9 `lesson_04_09_snap_menu.png` inserted in the same section between the Shift + S Snap Menu card's closing `</div>` and the opening of the Try It Now: Snap Menu Magic card. Both via `Filesystem:edit_file` with `dryRun=true` first then commit. M1 inserted #8 as a plain `<figure>` first (no annotations) to verify the anchor and lock the base size; M2 restructured that figure to wrap the img and an overlay SVG in a `position: relative; display: inline-block` div, with the SVG carrying `position: absolute; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none`. M3 inserted #9 with its overlay in a single combined edit (no plain-figure intermediate, since the #9 overlay is simpler: just a dashed bounding rect and a title pill). Inline-stacked SVG chosen over standalone overlay files in `images/`: keeps annotations co-located with the figure they belong to, no new image files in `images/`, no CSS edits to `styles/main.css`, and the locked figure wrapper still reads as a single `<figure>` block with one `<figcaption>` underneath. Marker id `c8-arrow` namespaced to avoid collisions with the existing `ss/st/tg/am/gl/ra/so` SVG id prefixes already on the page; #9 needs no marker (just a dashed rect and a title pill, no arrows). #8 carries three orange `#FF6B00` callouts: a `3D Cursor` label pill upper-right with an arrow into the red and white cursor target ring, a `New object spawns at cursor` label pill lower-right with an arrow into the UV sphere, and a dashed circle at radius 55 around the cursor center plus a `pivot reference` label pill lower-left with an arrow up-right to the dashed circle. All three #8 callouts laid out so no arrow crosses the cube selection outline; the dashed circle is tight enough around cursor and sphere to not touch the cube. #9 carries one orange callout: a dashed rectangle at `x=60 y=25 w=770 h=405` in the 882x450 viewBox encompassing all eight pie wedges, plus a `Shift + S Snap Menu` title pill at `x=76 y=9 w=250 h=32` sitting tab-style on the upper-left of the rectangle's top edge so it does not collide with the top `Selection to Cursor` pie pill. Geometry for both overlays previewed with PIL on the actual PNGs before HTML commit; #8 iterated from a v1 layout that crossed the cube body with the pivot arrow to a v3 layout that routes that arrow up from lower-left around the cube; #9 iterated from a centered title (which collided with the top pie pill) to a left-corner tab title. Alt text on each `<img>` describes the underlying screenshot content (cursor target, sphere, cube outline, grid, axis lines for #8; the eight pie wedges with numeric shortcuts for #9); the overlay SVG `<title>` and `<desc>` cover the annotations specifically so assistive tech receives both. Figcaptions em-dash-free, conversational, pointing at what to notice (#8: the spawn-at-cursor rule; #9: the left-vs-right cursor/selection split plus the keypad-shortcut numbers on the right of each pill). The img element carries inline `style="display: block; max-width: 100%; height: auto; margin: 0;"` to override the global `img { margin: var(--spacing-md) auto; }` rule so the wrapper div sizes cleanly to the image content; the wrapper div's `display: inline-block` combined with the figure's existing `text-align: center` keeps the figure centered. HTML size deltas: M1 (plain #8) added 806 bytes (191576 to 192382); M2 (#8 overlay restructure) added 2780 bytes (192382 to 195162); M3 (#9 with overlay) added 2405 bytes (195162 to 197567). Total Phase 4 integration growth: 5991 bytes (5.9 KB) over the three edits. Final HTML 197567 bytes (192.9 KB), up from 191576 bytes (187.1 KB) at Phase 4 integration chat start. Placement table rows for #8 and #9 flipped from `captured; not integrated` to `integrated`; phase plan table Phase 4 row flipped to `2 of 2 captured, 2 of 2 integrated`. `status.md` Image Production and Integration row for Lesson 04 updated to reflect 9 of 14 integrated and new file size 192.9 KB. `session.md` overwritten to CLOSED CLEAN pointing at Phase 5: SVGs #10 `lesson_04_10_duplication_comparison.svg` (id prefix `dc`) and #11 `lesson_04_11_origin_point.svg` (id prefix `op`), plus PNG #12 `lesson_04_12_transform_properties.png` (N-panel Item tab capture under the addon-safe pattern documented in `lesson_04_image_prompts.md`).
- Phase 4 production chat (this chat). Captured both Phase 4 PNGs and wrote them to `images/`. #8 `lesson_04_08_3d_cursor.png` at 783526 bytes (765.2 KB), 1280x720 OpenGL viewport render via the BlenderMCP bridge. Setup: 3D cursor moved to world coordinates (2.5, -1.5, 0.8), UV sphere with radius 0.6 added at the cursor location to demonstrate "new objects spawn at cursor", default cube reselected and made active to anchor the orange selection outline. View positioned via FRONT axis then orbit-up 0.6 radians plus orbit-right 0.4 radians per the prompts-doc recipe (view came out as User Orthographic; accepted for the base capture since annotation overlays during integration will provide the explanatory callouts). #9 `lesson_04_09_snap_menu.png` at 112585 bytes (110.0 KB), 882x450 manual capture via Win+Shift+S over the Shift+S snap pie menu, with the default cube visible behind the menu carrying the orange selection outline. All eight pie wedges legible (1 Cursor to World Origin, 2 Cursor to Selected, 3 Cursor to Active, 4 Cursor to Grid, 6 Selection to Grid, 7 Selection to Cursor Offset, 8 Selection to Cursor, 9 Selection to Active). 882px width is below the rough 1024px HiDPI target but the legibility acceptance gate is met cleanly. Both PNGs verified on disk via `Filesystem:get_file_info`. Annotation overlays (orange callouts arrowing at the 3D cursor target, the spawned sphere, and the pivot reference for #8; outlining the pie menu and labeling the menu title for #9) deferred to the Phase 4 integration chat where they can ride alongside the figure inserts as SVG overlays without modifying the base PNGs. Phase 4 production chat also patched `lesson_04_image_prompts.md` (20678 to 22146 bytes) to drop `bpy.ops.wm.read_factory_settings(use_empty=False)` from all four PNG state-setup snippets and added a new Addon safety note documenting why: the call reloads Blender's startup file which forcibly unregisters all addons including BlenderMCP, killing the bridge socket mid-execution; the chat hit this failure mode twice before the addon-safe pattern was identified, forcing two Blender restarts. The patched snippets assume a fresh Blender session (default Cube plus Light plus Camera) and operate on the existing scene without resetting it. For #13's deeper cleanup needs the snippet still deletes the default cube manually, just without the factory reset wrapper. No HTML edits this chat per the production-then-integration cadence locked in `session.md`; Phase 4 integration is the next chat.
- Phase 3 integration chat (this chat). Inlined both Phase 3 SVGs into `lesson_04_basic_object_manipulation.html`. SVG #7 `lesson_04_07_scale_operations.svg` (id prefix `so`) inserted in the Uniform vs. Non-Uniform Scaling subsection between the Axis-Constrained Scaling card's closing `</div>` and the opening of the Try It Now: Stretch and Squash card; SVG #5 `lesson_04_05_global_local_axes.svg` (id prefix `gl`) inserted in the Global vs. Local Axes subsection between the Global vs Local Orientation card's closing `</div>` and the opening of the Try It Now: Global vs Local card. Both inserts via `Filesystem:edit_file` with `dryRun=true` first then commit; `color="#222"` attribute stripped from each inlined root `<svg>` tag (the standalone `.svg` files in `images/` retain that attribute for `<img>` fallback); content re-indented to 20-space content depth per the locked inline pattern (figure and /figure at column 16, svg open and close plus figcaption at column 20, svg-attribute continuations at column 25, SVG body at column 22, `<g>` children at column 24, nested `<g>` children at column 26, rect and path attribute continuations inside `<g>` at column 30); every `role`, `aria-labelledby`, `<title>`, `<desc>`, and `id` preserved verbatim with `so-` and `gl-` prefixes carried through unchanged so no collision with the existing `ss-`, `st-`, `tg-`, `am-`, `ra-` ids on the page. New figcaptions written em-dash-free, conversational, pointing the reader at what each figure shows. SVG #7 figcaption: "Six cubes show how scale behaves. The top row keeps every dimension equal: the same factor on all three axes, so the cube stays cube-shaped. The bottom row constrains the change to a single axis, and the colored edges or inward arrows mark which dimension is moving." SVG #5 figcaption: "Two cubes side by side. On the left the gnomon arrows stay aligned with the world, so G X always moves along world X. On the right the cube has been rotated thirty degrees; pressing the axis key twice, G X X, locks the move to the object's own X, and the arrows rotate with the cube. The faint dashed lines under the rotated gnomon show where world X, Y, and Z still point." No new em-dashes introduced anywhere in the new content; no source-prose em-dashes were inside either of the two anchors used this round so no preservation case arose. HTML size deltas per insert: #7 added 17254 bytes (162912 to 180166); #5 added 11410 bytes (180166 to 191576). Total Phase 3 integration growth: 28664 bytes (28.0 KB) over the two inserts. Final HTML 191576 bytes (187.1 KB), up from 162912 bytes (159.1 KB) at Phase 3 integration chat start. Placement table rows for #7 and #5 flipped from `produced; handoff_written; not integrated` to `integrated`; phase plan table Phase 3 row flipped to "2 of 2 produced, 2 of 2 integrated". `status.md` Image Production and Integration row for Lesson 04 updated to reflect 7 of 14 integrated and new file size 187.1 KB. `session.md` overwritten to CLOSED CLEAN pointing at Phase 4: PNG captures for #8 `lesson_04_08_3d_cursor.png` and #9 `lesson_04_09_snap_menu.png`, driven by `lesson_04_image_prompts.md`. Phase 4 is the next phase: two Blender 5.1 viewport screenshots for the `#3d-cursor` section.
- Protocol retrofit meta chat (this chat). Adopted `INTERRUPTION_RECOVERY.md` minimum viable subset (pillar 2.2 resume pointer plus pillar 2.4 intermediate states plus pillar 2.1 save-at-milestone-boundaries, per section 6.5 minimum viable adoption). Authored `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\session.md` (2884 bytes) as the new pillar-2.2 resume pointer at project root; overwrite-every-update semantics, first file read in every new chat. Added a Status legend above the placement table covering the four states currently in use (`not captured; not integrated`, `produced; not integrated`, `produced; handoff_written; not integrated`, `integrated`); the third state is new and codifies the window between Phase production close and Phase integration close. Flipped placement-table rows for #7 `lesson_04_07_scale_operations.svg` and #5 `lesson_04_05_global_local_axes.svg` from `produced; not integrated` to `produced; handoff_written; not integrated`, since `lesson_04_integration_phase3_integration_handoff.md` (9733 bytes) is already on disk. No image production this chat. No HTML edits. HTML unchanged at 162912 bytes (159.1 KB). Adoption also recorded in `status.md` Image Integration Status section.
- Phase 3 production chat (this chat). Authored both Phase 3 SVGs and wrote them to `images/`. `lesson_04_07_scale_operations.svg` at 12848 bytes (12.5 KB), id prefix `so`, six-panel 2x3 grid covering Original, Uniform 2x, and Uniform 0.5x across the top row and Stretch X, Stretch Z, and Compress Y across the bottom row. Top-row panels at y=155 with height 160; bottom-row panels at y=325. Each panel renders a small isometric cube with a labeled operation badge: gray muted `1x` for the original, brand-orange `S 2`, `S .5`, `S X 2`, `S Z 3`, `S Y .5` for the others. Stretch X tints the three X-aligned cube edges with `#E63946` at stroke-width 2.4 to mark the stretched dimension; Stretch Z does the same with `#4A9EFF` on the three Z-aligned edges; Compress Y uses two inward green `#4ADE80` arrows plus a dashed top-face ghost showing the original depth, since the depth edges in the compressed cube are too short (around 3 px) to read clearly with the edge-tint pattern used by the X and Z variants. Uniform 0.5x panel adds a dashed 30x30 ghost outline so the size delta against the original reads at a glance. Footer tip at y=506 reads: "Tip: S scales uniformly. Add X, Y, or Z to constrain to one axis. Type a number for an exact factor, like S Z 3." File size sits slightly above the 9 to 12 KB target band, accepted given the six-panel grid complexity. `lesson_04_05_global_local_axes.svg` at 8597 bytes (8.4 KB), id prefix `gl`, two-panel comparison adapting the Lesson 03 paired-panel pattern (#12 perspective_depth plus #13 orthographic_parallels). Left panel at x=40 width 340 covers Global Axes (World Space, default); right panel at x=420 width 340 covers Local Axes (Object Space). Each panel centers a cube with a gnomon emerging from the right side of the cube in the Blender-viewport idiom, axis arrows colored per the locked palette: X red `#E63946`, Y green `#4ADE80`, Z blue `#4A9EFF`. Global panel renders cube and gnomon upright; local panel wraps the cube and gnomon in `<g transform="rotate(-30 170 130)">` to show the axes rotating with the object, with a faint dashed world-axis reference at opacity 0.20 underneath the rotated group plus a tiny italic "world ref." label so the contrast between rotated local axes and the still-world reference is visible. Axis labels at the rotated arrowhead endpoints were positioned outside the rotated group so they stay upright and readable rather than rotating with the gnomon. Shared inset at y=438 width 680 height 52 carries the one-line WORLD VS OBJECT takeaway. Footer tip at y=506: "Tip: Press the axis key twice to switch to local. G X moves along world X, G X X moves along the object's own X." Both SVGs verified on disk via `Filesystem:get_file_info`. Locked house style followed verbatim: 800x560 viewBox, 3px Blender-orange top strip at y=0, `currentColor` labels and connector lines, footer tip callout in `#FF6B00`, full accessibility tags (`role="img"` plus `aria-labelledby` pointing at matching `<title>` and `<desc>`), `color="#222"` retained on each standalone root for `<img>` fallback. Axis palette continuity confirmed against the Phase 2 SVGs: X = `#E63946`, Y = `#4ADE80`, Z = `#4A9EFF`; Blender-orange `#FF6B00` retained for top strip, key-combo badges, footer tip, and subhead labels. No new em-dashes introduced anywhere in the new content. No HTML edits made this chat; Phase 3 integration deferred to a dedicated chat. The `images/IMAGE_STYLE_GUIDE.md` palette revision to fold `#E63946` in is still deferred. Phase 3 integration handoff written at `lesson_04_integration_phase3_integration_handoff.md` (9733 bytes, 9.5 KB) covering placement anchors (#7 in `#scale` after the Axis-Constrained Scaling card before the Try It Now: Stretch and Squash card; #5 in `#axis-constraints` after the Global vs Local Orientation card before the Try It Now: Global vs Local card), figcaption drafts em-dash-free for both SVGs, the locked workflow (`Filesystem:edit_file` dry-run-then-commit, strip `color="#222"` on inline, re-indent to 20-space content depth, preserve all id prefixes), and a verification checklist.
- Phase 2 integration chat. Inlined all three move-grab and rotate SVGs into `lesson_04_basic_object_manipulation.html`. SVG #3 `lesson_04_03_transform_gizmo.svg` (id prefix `tg`) inserted in the Understanding the Move Gizmo subsection between the four-element `<ul>` (Red arrow, Green arrow, Blue arrow, White circle) and the paragraph beginning `You can press and drag these arrows`; SVG #4 `lesson_04_04_axis_movement.svg` (id prefix `am`) inserted in the Moving Along Specific Axes subsection between the Axis-Constrained Movement card's closing `</div>` and the opening of the Try It Now: Constrained Movement card; SVG #6 `lesson_04_06_rotation_axes.svg` (id prefix `ra`) inserted in the Understanding Rotation Axes subsection between the closing `</div>` of the rotation-axes mermaid block and the `<h3>Precise Rotation with Numbers</h3>`. All three inserts via `Filesystem:edit_file` with `dryRun=true` first then commit; `color="#222"` attribute stripped from each inlined root `<svg>` tag (the standalone `.svg` files in `images/` retain that attribute for `<img>` fallback); content re-indented to 20-space content depth per the locked inline pattern (figure and /figure at column 16, svg open and close plus figcaption at column 20, svg-attribute continuations at column 25, SVG body at column 22, `<g>` children at column 24, rect and path attribute continuations inside `<g>` at column 30); every `role`, `aria-labelledby`, `<title>`, `<desc>`, and `id` preserved verbatim. New figcaptions written em-dash-free, conversational, pointing the reader at what each figure shows. Notable figcaption decision: for #6 the original handoff-draft language said "orange arc" describing the rotation arcs, but the actual SVG draws per-axis colored arcs (red for X, green for Y, blue for Z), so the figcaption was revised to "colored arc" for accuracy. No new em-dashes introduced in any new content. No source-prose em-dashes were inside the three anchors used this round, so no preservation case arose. HTML size deltas per insert: #3 added 9142 bytes (128694 to 137836); #4 added 12627 bytes (137836 to 150463); #6 added 12449 bytes (150463 to 162912). Total Phase 2 integration growth: 34218 bytes (33.4 KB) over the three inserts. Final HTML 162912 bytes (159.1 KB), up from 128694 bytes (125.7 KB) at Phase 2 chat start. Placement table rows for #3, #4, and #6 flipped from "produced; not integrated" to "integrated"; phase plan table Phase 2 row flipped to "3 of 3 produced, 3 of 3 integrated". `status.md` Image Integration Status row for Lesson 04 updated to reflect 5 of 14 integrated and new file size 159.1 KB. Phase 3 production for SVGs #7 scale_operations (scale section) and #5 global_local_axes (axis-constraints section) deferred to the next chat; both must carry forward the new X-axis red `#E63946` to stay consistent with the Phase 2 SVGs, and `images/IMAGE_STYLE_GUIDE.md` palette revision to fold `#E63946` in is still deferred to a dedicated chat.
- Phase 2 production chat. Authored all three move-grab and rotate SVGs and wrote them to `images/`. `lesson_04_03_transform_gizmo.svg` at 7164 bytes (7.0 KB), id prefix `tg`, single-panel hero with the Blender move gizmo on a center-stage cube plus four annotation callouts naming the X arrow, Y arrow, Z arrow, and the white free-movement circle. `lesson_04_04_axis_movement.svg` at 9533 bytes (9.3 KB), id prefix `am`, three-panel triptych for G X, G Y, G Z showing a ghosted starting cube, a translation arrow, and the displaced cube in each panel with the matching per-axis color motif and operation badges. `lesson_04_06_rotation_axes.svg` at 9351 bytes (9.1 KB), id prefix `ra`, three-panel triptych for R X, R Y, R Z showing a rod through the cube along each axis with a curved orange rotation arc; panel descriptions read "Pitch (around X)", "Yaw (around Y)", "Roll (around Z)" verbatim per the handoff spec (Yaw and Roll are swapped relative to standard aviation convention; the handoff label text is preserved as-is and can be revisited in a later pass if Ray wants to retune the wording). All three verified on disk via `Filesystem:get_file_info`. Locked house style followed verbatim: 800x560 viewBox, 3px Blender-orange top strip, `currentColor` labels and connector lines, footer tip callout in `#FF6B00`, full accessibility tags (`role="img"` plus `aria-labelledby` pointing at matching `<title>` and `<desc>`), `color="#222"` retained on each standalone root for `<img>` fallback. Axis-color decision locked across all three SVGs: X = `#E63946` (new red; fold into `images/IMAGE_STYLE_GUIDE.md` palette at next revision), Y = `#4ADE80` (existing resources-green), Z = `#4A9EFF` (existing course-blue); Blender-orange `#FF6B00` retained for the top strip, key-combo badges, footer tip, rotation arcs in #6, and subhead labels. No new em-dashes introduced anywhere in the new content. No HTML edits made this chat; Phase 2 integration deferred to a dedicated chat. Phase 2 integration handoff written at `lesson_04_integration_phase2_integration_handoff.md`.
- Phase 1 integration chat. Inlined both selection-basics SVGs into `lesson_04_basic_object_manipulation.html`. SVG #1 `lesson_04_01_selection_states.svg` (id prefix `ss`) inserted in the Understanding Selection States subsection between the outline-states `<ul>` and the active-object `<p>`; SVG #2 `lesson_04_02_selection_tools.svg` (id prefix `st`) inserted in the Advanced Selection Techniques subsection between the More Selection Tools card and the selection-methods mermaid block. Both inserts via `Filesystem:edit_file` with `dryRun=true` first then commit; `color="#222"` attribute stripped from each inlined root `<svg>` tag (the standalone `.svg` files in `images/` retain that attribute for `<img>` fallback); content re-indented to 20-space content depth per the locked inline pattern; new figcaptions written em-dash-free, conversational, pointing the reader at what each figure shows. The pre-existing em-dash in the source paragraph ("you selected—it's the one") was preserved verbatim through the anchor; no new em-dashes introduced in any new content. HTML grew from 106.0 KB (108546 bytes) at chat start to 125.7 KB (128694 bytes) at chat end, plus 20.1 KB across both inserts. Placement table rows for #1 and #2 flipped from "produced; not integrated" to "integrated"; phase plan table Phase 1 row flipped to "2 of 2 produced, 2 of 2 integrated". `status.md` Image Integration Status row for Lesson 04 updated to reflect 2 of 14 integrated and new file size 125.7 KB. Phase 2 handoff written at `lesson_04_integration_phase2_handoff.md`. Image production for Phase 2 (SVGs #3 transform_gizmo, #4 axis_movement, #6 rotation_axes covering the move-grab and rotate sections) deferred to the next chat to keep context budget clean.
- Phase 1 production chat. Authored both selection-basics SVGs and wrote them to `images/`. `lesson_04_01_selection_states.svg` at 5578 bytes (5.4 KB), id prefix `ss`, trio comparison of three cubes showing active, selected-not-active, and unselected outline states with a tip explaining the active-vs-selected distinction. `lesson_04_02_selection_tools.svg` at 9540 bytes (9.3 KB), id prefix `st`, three-panel triptych for Box Select (dashed rectangle, B key badge), Circle Select (orange brush, C key badge), and Lasso Select (freeform curve, Ctrl + drag badge). Both verified on disk via `Filesystem:get_file_info`. Locked house style followed verbatim: 800x560 viewBox, 3px Blender-orange top strip, `currentColor` labels, footer tip callout, full accessibility tags (`role="img"` plus `aria-labelledby` pointing at matching `<title>` and `<desc>`), `color="#222"` retained on the root for `<img>` fallback. No new em-dashes introduced anywhere in the new content. No HTML edits made this chat; Phase 1 integration deferred to a dedicated chat to keep context budget clean. Phase 2 handoff written at `lesson_04_integration_phase2_handoff.md`.
- Planning chat (this chat). Read `lesson_04_image_requirements.md`, the full `lesson_04_basic_object_manipulation.html` (106.0 KB at chat start), `lesson_03_completion_handoff.md` for the locked patterns carried forward, `lesson_03_integration_progress.md` for structural reference, and `images/IMAGE_STYLE_GUIDE.md` for SVG house style. Confirmed `images/` contains no `lesson_04_*` assets at chat start (only stray `_tmp_*` and `_wave*_temp` directories alongside the produced lesson_01, lesson_02, and lesson_03 assets). Decided a 6-phase plan mapping cleanly to the lesson's 10 HTML sections, with the 14 images split as 10 inline SVGs plus 4 PNG Blender screenshots. The SVG-heavy split is deliberate: most Lesson 04 images are diagrammatic in nature (selection-state outlines, transform gizmos, axis-constraint motion diagrams, rotation rods, scale comparison grids, global-vs-local axis frames, duplication concept, origin point illustration, keyboard cheat sheet) and convert cleanly to the locked house style; only #8 3D cursor placement, #9 Shift+S snap pie menu, #12 N-panel transform sidebar, and #13 finished table-scene render genuinely require a live Blender 5.1 viewport capture. Placement anchors mapped for all 14 images, with each row of the placement table pointing at a paragraph-or-card anchor above and an h3-or-card anchor below to give `Filesystem:edit_file` a unique 2 to 3 line match. Companion prompts doc `lesson_04_image_prompts.md` also written this chat, documenting Blender 5.1 capture instructions plus Adobe Firefly and Canva AI fallback prompts for the 4 PNGs. Both planning docs confirmed on disk via `Filesystem:get_file_info`: `lesson_04_integration_progress.md` at 22353 bytes (21.8 KB), `lesson_04_image_prompts.md` at 20678 bytes (20.2 KB). No Phase 1 SVG production was started this chat; the chat ended after the two planning docs so the budget stays clean for a dedicated Phase 1 production chat. Phase 1 handoff written at `lesson_04_integration_phase1_handoff.md`. HTML still unchanged at 106.0 KB. No HTML edits made.

## Images available and missing

Available in `images/` (14 of 14):

SVGs (10 of 10):

- #1 `lesson_04_01_selection_states.svg` (5578 bytes, id prefix `ss`)
- #2 `lesson_04_02_selection_tools.svg` (9540 bytes, id prefix `st`)
- #3 `lesson_04_03_transform_gizmo.svg` (7164 bytes, id prefix `tg`)
- #4 `lesson_04_04_axis_movement.svg` (9533 bytes, id prefix `am`)
- #5 `lesson_04_05_global_local_axes.svg` (8597 bytes, id prefix `gl`)
- #6 `lesson_04_06_rotation_axes.svg` (9351 bytes, id prefix `ra`)
- #7 `lesson_04_07_scale_operations.svg` (12848 bytes, id prefix `so`)
- #10 `lesson_04_10_duplication_comparison.svg` (8315 bytes, id prefix `dc`)
- #11 `lesson_04_11_origin_point.svg` (7106 bytes, id prefix `op`)
- #14 `lesson_04_14_keyboard_shortcuts.svg` (10062 bytes, id prefix `ks`)

PNGs (4 of 4):

- #8 `lesson_04_08_3d_cursor.png` (783526 bytes, 765.2 KB, 1280x720)
- #9 `lesson_04_09_snap_menu.png` (112585 bytes, 110.0 KB, 882x450)
- #12 `lesson_04_12_transform_properties.png` (453308 bytes, 442.7 KB)
- #13 `lesson_04_13_completed_scene.png` (501641 bytes, 490 KB, 1280x720)

Missing from production (0 of 14): all 14 images have been produced or captured.

Full capture instructions and AI-generation fallback prompts for all 4 PNGs live in `lesson_04_image_prompts.md`.

## Locked approach (identical to Lessons 01, 02, and 03)

- **Wrapper:** plain `<figure>` with `<figcaption>` for every image. No class. `styles/main.css` already styles both elements directly.
- **PNGs:** `<img src="images/..." alt="..." loading="lazy">` inside the figure with descriptive alt text.
- **SVGs:** inlined verbatim inside the figure, re-indented to 20-space content depth. **Critical: strip the `color="#222"` attribute from the inline `<svg>` root tag.** That attribute is the `<img>`-embedded fallback (so a standalone SVG has some color); when inlined, it locks the SVG's `color` to `#222` and prevents `currentColor` from inheriting the page text color, breaking dark mode. The `.svg` source files in `images/` keep the attribute for standalone viewing; only the inline copy in HTML drops it.
- **Tools:** `Filesystem:edit_file` only for existing files. Always `dryRun=true` first to confirm anchors match, then commit. Verify with `Filesystem:get_file_info` after every commit. For new files (the SVGs in `images/`, this progress doc, the prompts file), use `Filesystem:write_file`. NEVER use the built-in `create_file` (writes to Claude's container, not the user filesystem at `\\wsl$\Ubuntu\` paths).
- **Anchors:** use 2 to 3 lines for `oldText` (the paragraph or h3 immediately above and the next element below the insertion point). Make sure the anchor is unique within the file.
- **Path prefix:** `\\wsl$\Ubuntu\` always. Never `\\wsl.localhost\`.
- **No new em-dashes** in any new content (captions, alt text, this doc, the prompts doc). Preserve em-dashes that exist in source content (existing paragraphs, blockquotes, the SVG body text from prior lessons). Note: Lesson 04 source HTML contains many em-dashes in the existing prose (for example, "Move (Grab)—Positioning Objects"); those are preserved verbatim through every edit anchor.
- **Per-chat archive discipline:** when the chat is winding down or context is getting heavy, warn Ray rather than letting automatic compaction happen.
- **AI prompts for missing raster images:** all 4 PNGs that this lesson needs are documented in `lesson_04_image_prompts.md`. Real Blender 5.1 screenshots are the strongly preferred sourcing route for every one of them; AI generation (Adobe Firefly via the Adobe for creativity MCP, or Canva AI image generator) is a fallback only.
- **Version currency:** Blender 5.1 is the project target (current stable as of March 2026). Audit Lesson 04 prose for version-specific references when integration runs. The HTML in its current form references "Blender 4.0+" in the Box Select card; that should be retargeted to Blender 5.1 (or made version-generic) at the relevant integration phase, since press-and-drag box select has been default since well before 5.1.

## Phase plan

Phases are scoped to one or two HTML sections at a time, matching the Lesson 03 cadence. Each phase places that section's images and updates this progress doc when complete.

| Phase | Section(s) | Images | Inline SVGs | Status |
|---|---|---|---|---|
| 1 | selection-basics | #1, #2 | both | 2 of 2 produced, 2 of 2 integrated |
| 2 | move-grab, rotate | #3, #4, #6 | all 3 | 3 of 3 produced, 3 of 3 integrated |
| 3 | scale, axis-constraints | #7, #5 | both | 2 of 2 produced, 2 of 2 integrated |
| 4 | 3d-cursor | #8, #9 | none (both PNG) | 2 of 2 captured, 2 of 2 integrated |
| 5 | duplication, origin-transform | #10, #11, #12 | #10, #11 | 3 of 3 produced or captured, 3 of 3 integrated |
| 6 | manipulation-project, summary | #13, #14 | #14 only | 2 of 2 produced or captured, 2 of 2 integrated |

Phase order is document order in the HTML: selection-basics, move-grab, rotate, scale, axis-constraints, 3d-cursor, duplication, origin-transform, manipulation-project, summary. Production order within each phase should match document order so the placement table reads top to bottom when complete.

Production-vs-capture cadence:

- SVG phases (Phases 1, 2, 3, 5, 6 partial): one SVG per chat is the ideal cadence per the locked rules. Two SVGs per chat is workable when the diagrams are simple variants of an existing pattern (for example, #6 rotation axes adapts the Lesson 03 motion-diagram trio pattern; #14 keyboard shortcuts adapts the Lesson 02 #18 and Lesson 03 #18 two-column reference card pattern).
- PNG phases (Phase 4, plus #12 in Phase 5 and #13 in Phase 6): one or two captures per chat depending on Blender state setup overhead. Capture instructions in `lesson_04_image_prompts.md` keep state setup self-contained per image.

## Placement table (all 14 images)

Placements are derived from the HTML section structure plus the requirements doc's "Image Placement Suggestions in HTML" section. Each row gives the section ID, an anchor pair (the paragraph or h3 above the insertion point and the element immediately below it), and the current placement status.

**Status legend** (per `INTERRUPTION_RECOVERY.md` pillar 2.4 intermediate-states discipline):

- `not captured; not integrated`: a PNG that requires live Blender capture (or AI fallback) and has not yet been produced.
- `produced; not integrated`: an SVG that is on disk in `images/` with no Phase-integration handoff doc written yet.
- `produced; handoff_written; not integrated`: an SVG that is on disk in `images/` and whose Phase-integration handoff doc exists at project root, but the HTML edit has not yet landed. New state, added per the protocol retrofit; it covers the window between Phase production close and Phase integration close.
- `captured; not integrated`: a PNG that is on disk in `images/` after live Blender capture (or manual capture, or AI generation), with no inline insertion yet. PNG analogue of `produced; not integrated`. Used during the window between Phase production close and Phase integration close. PNG phases have no separate handoff-written intermediate since `lesson_04_image_prompts.md` serves as the de-facto Phase handoff for all four PNGs.
- `integrated`: image (SVG or PNG) is inlined into the lesson HTML and verified on disk via `Filesystem:get_file_info`.

| # | File | Section | Insertion point | Status |
|---|---|---|---|---|
| 1 | `lesson_04_01_selection_states.svg` | selection-basics | In "Understanding Selection States". After the `<ul>` listing the four outline states (Orange, Light orange or yellow, No outline, Dark outline), before the paragraph beginning `The "active" object is the last one`. | integrated |
| 2 | `lesson_04_02_selection_tools.svg` | selection-basics | In "Advanced Selection Techniques". After the "More Selection Tools" card's closing `</div>` (which introduces lasso plus other tools), before the mermaid block showing the selection-methods tree. The figure summarizes box, circle, and lasso visually as the trio is wrapped up. | integrated |
| 3 | `lesson_04_03_transform_gizmo.svg` | move-grab | In "Understanding the Move Gizmo". After the `<ul>` listing Red arrow, Green arrow, Blue arrow, White circle, before the paragraph beginning `You can press and drag these arrows`. | integrated |
| 4 | `lesson_04_04_axis_movement.svg` | move-grab | In "Moving Along Specific Axes". After the "Axis-Constrained Movement" card's closing `</div>` (which lists G then X, G then Y, G then Z), before the "Try It Now: Constrained Movement" card. | integrated |
| 6 | `lesson_04_06_rotation_axes.svg` | rotate | In "Understanding Rotation Axes". After the existing mermaid block that branches X, Y, Z with rod descriptions, before the `<h3>Precise Rotation with Numbers</h3>`. The mermaid covers the conceptual branches; this SVG adds the actual visual rotation rods with motion arcs. | integrated |
| 7 | `lesson_04_07_scale_operations.svg` | scale | In "Uniform vs. Non-Uniform Scaling". After the "Axis-Constrained Scaling" card's closing `</div>` (which lists S then X, S then Y, S then Z), before the "Try It Now: Stretch and Squash" card. | integrated |
| 5 | `lesson_04_05_global_local_axes.svg` | axis-constraints | In "Global vs. Local Axes". After the "Global vs Local Orientation" card's closing `</div>`, before the "Try It Now: Global vs Local" card. | integrated |
| 8 | `lesson_04_08_3d_cursor.png` | 3d-cursor | In "What Is the 3D Cursor?". After the "Moving the 3D Cursor" card's closing `</div>` (lists Shift plus Right Click, Shift plus S, manual positioning), before the "Try It Now: Position the Cursor" card. | integrated |
| 9 | `lesson_04_09_snap_menu.png` | 3d-cursor | In "The Snap Menu (Shift + S)". After the "Shift + S Snap Menu" card's closing `</div>` (lists all snap options), before the "Try It Now: Snap Menu Magic" card. | integrated |
| 10 | `lesson_04_10_duplication_comparison.svg` | duplication | In "Linked Duplication". After the "Linked Duplicate" card's closing `</div>` (which lists key differences from Shift plus D), before the "When to Use Linked Duplication" info card. | integrated |
| 11 | `lesson_04_11_origin_point.svg` | origin-transform | In "What Is an Object Origin?". After the "Origin Point Functions" card's closing `</div>` (lists Rotation pivot, Scale center, Position reference, Snap target, Parent connection), before the "Why Origins Matter" info card. | integrated |
| 12 | `lesson_04_12_transform_properties.png` | origin-transform | In "Transform Properties Panel". After the "Transform Properties" card's closing `</div>` (describes Location, Rotation, Scale, Dimensions fields), before the "Try It Now: Precise Positioning" card. | integrated |
| 13 | `lesson_04_13_completed_scene.png` | manipulation-project | After the "Project Goal" info card's closing `</div>` (which lists the four scene elements), before the `<h3>Step 1: Setup and Cleanup</h3>`. The figure sits as the visual target reference before the step-by-step build begins. | integrated |
| 14 | `lesson_04_14_keyboard_shortcuts.svg` | summary | In "Essential Manipulation Shortcuts Reference". After the "Your Manipulation Cheat Sheet" card's closing `</div>` (which holds the four reference tables: Selection, Transformations, Duplication and Cursor, Advanced), before the `<h3>Common Questions at This Stage</h3>`. Mirrors the Lesson 02 #18 and Lesson 03 #18 placement pattern: tabular cheat sheet first, visual reference card after. | integrated |

Phase order in document order: 1 (selection-basics), 2 (move-grab then rotate), 3 (scale then axis-constraints), 4 (3d-cursor), 5 (duplication then origin-transform), 6 (manipulation-project then summary). Note that the placement table groups Phase 5 with #10 and #11 above #12 because document order in the HTML places duplication before origin-transform; #5 axis-constraints sits below #7 scale in the table because the axis-constraints section sits after the scale section in the HTML.

## Caption and alt-text guidance

- **Alt text:** describe the image content factually for screen readers. PNG screenshots get full descriptive alt (what is visible: window panels, viewport contents, annotations, highlighted regions). Inline SVGs keep their own `<title>` and `<desc>`, so no additional alt is needed; the figcaption is the visible label.
- **Figcaption:** one or two sentences, conversational, points the reader at what to notice or how to use the image. Should not just repeat the image title.
- **No new em-dashes** in any caption or alt text written in this lesson. Use a comma, a colon, or a sentence break instead. Em-dashes in pre-existing source HTML are preserved verbatim through the edit anchors (Lesson 04 prose uses em-dashes liberally; that is not a problem, just do not add new ones inside new figcaptions or new alt text).
- **House style continuity:** match the caption tone Lesson 03 established. Conversational, present-tense, draws the reader's eye. Examples to model: Lesson 03 #14 navigation_workflow figcaption pointed at the dashed side-note callouts; Lesson 03 #18 navigation_shortcuts_reference figcaption positioned the SVG as a visual companion to the tabular cheat sheet.

## Example: the wrapper patterns

PNG figure (as used throughout Lessons 01, 02, and 03):

```html
<figure>
    <img src="images/lesson_04_NN_slug.png" alt="..." loading="lazy">
    <figcaption>Caption text.</figcaption>
</figure>
```

Inline SVG figure (preserve the SVG's `role`, `aria-labelledby`, `<title>`, `<desc>` exactly; re-indent the outer `<svg>` to 20-space depth and inner lines proportionally; **DROP the `color="#222"` attribute when inlining**):

```html
<figure>
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="..." role="img" aria-labelledby="..." font-family="...">
      <title id="...-title">...</title>
      <desc id="...-desc">...</desc>
      ... SVG body ...
    </svg>
    <figcaption>Caption text.</figcaption>
</figure>
```

Indentation when inlined: `<figure>` and `</figure>` at column 16, `<svg>`, `</svg>`, `<img>`, `<figcaption>` at column 20, multi-line svg attribute continuations at column 25, SVG body at column 22, `<g>` children at column 24. Strip `color="#222"` from the root `<svg>` tag; if it sits on its own continuation line, drop the line entirely and reattach the closing `>` to the previous attribute line.

## SVG id-prefix registry (Lesson 04, unique per file)

Following the Lesson 03 convention of unique id prefixes per inlined SVG to avoid `<title>` and `<desc>` id collisions when several are on the same page:

| # | File | id prefix |
|---|---|---|
| 1 | `lesson_04_01_selection_states.svg` | `ss` |
| 2 | `lesson_04_02_selection_tools.svg` | `st` |
| 3 | `lesson_04_03_transform_gizmo.svg` | `tg` |
| 4 | `lesson_04_04_axis_movement.svg` | `am` |
| 5 | `lesson_04_05_global_local_axes.svg` | `gl` |
| 6 | `lesson_04_06_rotation_axes.svg` | `ra` |
| 7 | `lesson_04_07_scale_operations.svg` | `so` |
| 10 | `lesson_04_10_duplication_comparison.svg` | `dc` |
| 11 | `lesson_04_11_origin_point.svg` | `op` |
| 14 | `lesson_04_14_keyboard_shortcuts.svg` | `ks` |

Each prefix is two letters. None collide with the Lesson 03 prefixes (`op` was used in Lesson 03 for orbit_operation, but Lesson 03 SVGs never share a page with Lesson 04 SVGs, so the reuse is safe). If a future cross-lesson summary page ever inlines SVGs from multiple lessons, revisit the registry.

## SVG patterns to reuse from Lesson 03

These Lesson 04 SVGs adapt patterns proven in Lesson 03:

- **#6 rotation_axes**: adapts the Lesson 03 motion-diagram trio pattern (#3 orbit, #4 pan, #5 zoom). Three side-by-side panels showing rotation around X, Y, and Z, each with an operation badge near top, central scene visual (a rod through a cube with a rotation arc), and a small inset showing the keyboard shortcut. House style: 800px width, 3px Blender-orange top strip, `currentColor` labels, brand orange `#FF6B00` for the rotation arcs, footer tip callout.
- **#4 axis_movement**: same three-panel structure as #6 but with translation arrows instead of rotation arcs.
- **#7 scale_operations**: 2x3 grid pattern from the requirements doc. Top row: original cube, uniform-up 2x, uniform-down 0.5x. Bottom row: stretched X (2x), stretched Z (3x), compressed Y (0.5x). Grid background for reference.
- **#14 keyboard_shortcuts**: directly modeled on `lesson_02_18_keyboard_shortcuts_reference.svg` and `lesson_03_18_navigation_shortcuts_reference.svg`. Two-column reference card. Four category groupings to mirror the existing tabular cheat sheet: Selection, Transformations (G, R, S plus axis constraints), Duplication and Cursor, Advanced (Apply scale, reset transforms, sidebar).
- **#5 global_local_axes**: side-by-side comparison adapting the Lesson 03 #12 (perspective_depth) and #13 (orthographic_parallels) authored-as-a-pair pattern. Left panel: cube in default orientation with global X-Y-Z axes labeled, captioned "Global Axes: World Space". Right panel: same cube rotated 45 degrees with local axes rotating with it, captioned "Local Axes: Object Space".
- **#1 selection_states**: three identical cubes side by side with three different outline states (bright orange, light orange, no outline), each labeled. Adapts the comparison-trio composition.
- **#2 selection_tools**: three-panel composite showing box select rectangle, circle select brush, and lasso select freeform path. Same triptych layout.
- **#3 transform_gizmo**: detailed close-up of the Blender gizmo on a single cube with annotated arrows. Single-panel layout, like Lesson 02 #2 areas_editors_concept.
- **#10 duplication_comparison**: two-column comparison. Left: Shift plus D showing independent copies. Right: Alt plus D showing linked copies with chain-link visual indicators.
- **#11 origin_point**: single-panel illustration of a cube with the orange origin dot, plus three small annotation diagrams around it (rotation pivot, scale center, location reference). Adapts the Lesson 03 #1 (big_three_operations) composition: one hero visual with supporting callouts.

## Standing rules (carry forward from Lesson 03 completion handoff)

- `\\wsl$\Ubuntu\` paths only, never `\\wsl.localhost\`.
- No new em-dashes anywhere in new content; preserve pre-existing em-dashes verbatim. Lesson 04 source HTML uses em-dashes heavily in section h2 titles (for example "Move (Grab): Positioning Objects"... actually those use colons, but section bodies use em-dashes generously). Edit anchors will preserve them.
- Style guide canonical: `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\images\IMAGE_STYLE_GUIDE.md`.
- One phase per chat is the ideal cadence for integration; one image per chat is the ideal cadence for SVG production, with two-per-chat acceptable when the second is a variant of the same pattern.
- Warn Ray before context gets tight rather than letting automatic compaction happen.
- Propose spec plus design plan before generating new images; lock plan in writing first, then build. (This doc plus the prompts doc are that plan.)
- Per-chat archive discipline for intermediate captures (PNG work only; SVGs have no intermediates).
- Always `tool_search` for deferred tools rather than inheriting "tool X is unavailable" assumptions from a compaction summary.
