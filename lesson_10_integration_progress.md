# Lesson 10 Image Work &middot; Integration Progress

**Lesson:** `lesson_10_shader_editor_introduction.html` (Module 3: Materials &amp; Texturing)
**Project root:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\`
**Image output:** `images/`
**Status:** **L10 COMPLETE &mdash; 30/30 produced / 30/30 integrated.** All 8 phases done. HTML `lesson_10_shader_editor_introduction.html` 313,707 B, `<figure>` balance 33/33, literal em-dash 81, zero `&mdash;` / `color="#222"`. Remaining L10 work: none (the standing browser-QA pass is a deferred follow-up, tracked in `session.md`).
**Planning date:** 2026-06-02
**Style guide:** `images\IMAGE_STYLE_GUIDE.md` v6 (locked; all conventions live there, not restated here).

---

## Scope

30 figures total, sourced from `lesson_10_image_requirements.md`. Final split: **6 SVG / 24 PNG**.
8 HTML sections drive 8 phases. PRODUCTION sub-chat splits on the two &ge;5-figure phases (P4, P6); INTEGRATION-as-its-own-chat on the one &ge;6-figure phase (P6).

### Planning decisions (LOCKED 2026-06-02)

- **D1 &mdash; #24 Metal Base Colors format:** SVG. Stylized radial-gradient swatch discs stand in for the spec's "small rendered spheres"; keeps the figure in the resolution-independent, page-inheriting chart-reference family with #5 / #25. No photoreal metal-sphere render.
- **D2 &mdash; #10 Material Personality Types phase home:** P4 (`#principled-bsdf`, its "Quick Material Recipes" home). The requirements-doc `#basic-material-properties` mention is a 2nd-reference placement only (reused by reference at P5 INTEGRATION if the body calls for it), NOT a re-render. Same pattern as L09 #26.
- **D3 &mdash; #14 Material Library Organization:** produced ONCE in P6 (`#creating-simple-materials`). The `#project` Step-3 mention is a reuse-by-reference at P8 INTEGRATION, not a second render.
- **#30** is rendered once in P8 and placed twice within `#project` (intro goal image + closing success showcase).

---

## SVG-vs-PNG split (all 30)

| # | Figure (slug) | Format | Phase | Notes |
|---|---------------|--------|-------|-------|
| 1 | material_behavior_comparison | PNG | P1 | Photoreal 5-sphere PBR render &middot; color-dependent recipe |
| 2 | shader_editor_overview | PNG | P2 | Annotated real screenshot &middot; overlay `c2` |
| 3 | shader_editor_anatomy | PNG | P2 | Annotated real screenshot &middot; overlay `c3` |
| 4 | default_node_setup | PNG | P2 | Real Shader Editor screenshot |
| 5 | node_socket_colors | **SVG** | P3 | id-prefix `sk` &middot; sockets as colored circles + text |
| 6 | node_directionality | **SVG** | P3 | id-prefix `nd` &middot; node box + in/out arrows |
| 7 | principled_bsdf_sections | PNG | P4 (A) | Annotated real node screenshot &middot; overlay `c7` |
| 8 | metallic_comparison | PNG | P4 (B) | Side-by-side PBR render &middot; color-dependent |
| 9 | roughness_scale | PNG | P4 (B) | Multi-sphere PBR render |
| 10 | material_personality_types | PNG | P4 (B) | Multi-sphere PBR render &middot; reused-by-ref P5 |
| 11 | viewport_shading_modes | PNG | P7 | 4-panel real-mode composite |
| 12 | material_preview_settings | PNG | P7 | Annotated real screenshot &middot; overlay `c12` |
| 13 | hdri_environments | PNG | P7 | Chrome-sphere PBR render set &middot; color-dependent |
| 14 | material_library_organization | PNG | P6 (B) | 3D viewport render &middot; reused-by-ref P8 |
| 15 | chrome_result | PNG | P6 (A) | Photoreal PBR render &middot; color-dependent |
| 16 | matte_glossy_comparison | PNG | P6 (A) | Side-by-side PBR render |
| 17 | clear_glass | PNG | P6 (A) | Refraction PBR render |
| 18 | frosted_clear_comparison | PNG | P6 (B) | Side-by-side PBR render |
| 19 | emissive_glow | PNG | P6 (B) | Rendered-mode lighting &middot; color-dependent |
| 20 | car_paint_clearcoat | PNG | P6 (B) | Photoreal PBR render |
| 21 | node_connection_demo | PNG | P2 | Annotated real screenshot &middot; overlay `c21` |
| 22 | z_key_pie_menu | PNG | P7 | Transient pie menu &middot; Win+Shift+S overlay-freeze capture |
| 23 | workflow_decision_tree | **SVG** | P5 | id-prefix `dt` &middot; flowchart boxes/diamonds/lines |
| 24 | metal_base_colors | **SVG** | P5 | id-prefix `mb` &middot; swatch discs (D1); hex values at full opacity |
| 25 | ior_reference_chart | **SVG** | P5 | id-prefix `ir` &middot; text + simple refraction-ray graphics |
| 26 | append_interface | PNG | P8 | Real file-browser screenshot &middot; overlay `c26` |
| 27 | material_slots_panel | PNG | P8 | Annotated real screenshot &middot; overlay `c27` |
| 28 | advanced_parameters | PNG | P4 (A) | Annotated real node screenshot &middot; overlay `c28` |
| 29 | real_world_reference | PNG | P1 | Real-world photo collage |
| 30 | project_completion_gallery | PNG | P8 | Portfolio PBR render &middot; placed 2x in `#project` |

**Totals:** 6 SVG (#5, #6, #23, #24, #25 + #24 promoted from doc's 5) / 24 PNG.
Color-dependent renders (F12 `render.render` + Standard view transform, per v6 Locked decision 17): #1, #8, #13, #15, #19 (and any sphere render where marker / emissive color carries the message).

---

## Per-lesson id-prefix registry (inline SVG roots)

| Prefix | Figure |
|--------|--------|
| `sk` | #5 node_socket_colors |
| `nd` | #6 node_directionality |
| `dt` | #23 workflow_decision_tree |
| `mb` | #24 metal_base_colors |
| `ir` | #25 ior_reference_chart |

## PNG overlay `c{N}-*` namespace registry

`c1` (#1 material_behavior_comparison &middot; 5 centered material-name pills, neutral dark colorway), `c2` (#2 shader_editor_overview &middot; 4 region labels on lifted `#1a1a1f`/0.9 bands, orange leaders+dots, fs 22), `c3` (#3 shader_editor_anatomy &middot; 5 component labels on lifted `#2a2a33`/0.92 bands, orange leaders+dots, fs 16; Material Output label right-anchored inside the right margin), `c21` (#21 node_connection_demo &middot; 3 numbered orange step badges, white text, fs 16; socket-target rings + noodle-midpoint dot), `c7` (#7 principled_bsdf_sections &middot; 2 group brackets built at P4 INTEGRATION: green ESSENTIAL square-bracket around the top-level sockets Base Color&rarr;Normal [node-left x408, y92&ndash;270] + orange ADVANCED bracket around the collapsed panel-header stack Diffuse&rarr;Thin Film [y280&ndash;500], each with a left-side label pill + short leader; labels shortened to ESSENTIAL / ADVANCED after a first pass overflowed the cramped left margin), `c12`, `c28` (#28 advanced_parameters &middot; 3 orange use-case badges built at P4 INTEGRATION, fs 16 small-frame floor, leaders+dots landing at the header text-ends: Subsurface &middot; skin, wax [row y121] / Coat &middot; car paint [y257] / Sheen &middot; fabric, velvet [y303]; tags point at the collapsed panel headers per the kickoff, NOT sub-rows), `c8` (#8 metallic_comparison &middot; **2** metallic-value pills [`Metallic 0.0 &middot; Non-metal` / `Metallic 1.0 &middot; Metal`], built at P4 INTEGRATION; the render + requirements doc #8 are a 2-sphere side-by-side [0.0 vs 1.0], so the PROD-B "3 pills" reservation was a miscount &mdash; the table's 0.5 "physically incorrect" value is text-only, never rendered), `c9` (#9 roughness_scale &middot; 6 roughness-value pills, built at P4 INTEGRATION), `c10` (#10 material_personality_types &middot; 6 material-name+param two-line pills, built at P4 INTEGRATION), `c29` (#29 real_world_reference &middot; title-band overlay + 8 cell note-band labels, white text on the reserved dark bands), `c15` (#15 chrome_result &middot; 1 caption pill `Chrome &middot; Metallic 1.0 &middot; Roughness 0.05`, reserved at P6 PROD-A, built at P6 INTEGRATION), `c16` (#16 matte_glossy_comparison &middot; 2 below-sphere label pills `Matte &middot; Roughness 0.9` [left, red] / `Glossy &middot; Roughness 0.2` [right, blue] + thin vertical white divider between the two spheres, reserved at P6 PROD-A, built at P6 INTEGRATION), `c17` (#17 clear_glass &middot; 1 caption pill `Clear Glass &middot; Transmission 1.0 &middot; IOR 1.45`, reserved at P6 PROD-A, built at P6 INTEGRATION), `c18` (#18 frosted_clear_comparison &middot; 2 below-sphere label pills `Clear &middot; Roughness 0.0` [left] / `Frosted &middot; Roughness 0.4` [right] + thin vertical white divider between the two spheres, reserved at P6 PROD-B, built at P6 INTEGRATION), `c19` (#19 emissive_glow &middot; caption/callout pills decided at P6 INTEGRATION [e.g. `Emission &middot; RGB 0.3/0.6/1.0 &middot; Strength 10.0` on the panel + an optional blue-light + falloff call on the lit sphere], reserved at P6 PROD-B, built at P6 INTEGRATION), `c20` (#20 car_paint_clearcoat &middot; 2 callouts `Base layer reflection` / `Clearcoat highlights` per the requirements-doc optional annotations, reserved at P6 PROD-B, built at P6 INTEGRATION), `c14` (#14 material_library_organization &middot; 12 cell labels for the 3&times;4 grid [R1 Chrome / Gold / Brushed Steel / Copper &middot; R2 Red Glossy / Blue Matte / Clear Glass / Frosted Glass &middot; R3 Emissive Blue / Car Paint / Rubber / Wood], reserved at P6 PROD-B, built at P6 INTEGRATION).
Composite-overlay PNGs (#11 4-panel, #13 HDRI grid) get centered per-panel badges only if labels are added at INTEGRATION; reserve `c11`, `c13` if so. **Built at P7 INTEGRATION:** `c11` (#11 viewport_shading_modes &middot; 4 quadrant mode-labels Solid [TL] / Material Preview [TR] / Rendered [BL] / Wireframe [BR], centered below each sphere group on `#1a1a1f`/0.9 pills, fs 26), `c13` (#13 hdri_environments &middot; 4 quadrant HDRI-name labels Studio [TL] / Forest [TR] / Interior [BL] / Night [BR], near each quadrant top, fs 30), `c12` (#12 material_preview_settings &middot; 5 orange leader+dot callouts to Lighting / HDRI / Rotation / Strength / Blur on `#1a1a1f`/0.9 pills in the right margin, fs 16 small-frame floor). **`c22` SKIPPED** (#22 z_key_pie_menu &middot; captured pie-menu segment text Rendered/Wireframe/Solid/Material Preview already legible per the registry's "only if not legible" rule &mdash; integrated as a clean screenshot, no overlay). **Built at P8 INTEGRATION:** `c26` (#26 append_interface &middot; 5 numbered Append-workflow step callouts &mdash; navigate to library / open .blend / select Material folder / choose materials / click Append &mdash; on `#1a1a1f`/0.9 pills, orange leaders+dots; pixel-verified 1:1 on the cropped 2275&times;1420 PNG), `c27` (#27 material_slots_panel &middot; 5 callouts &mdash; Active material slot / Add slot / Remove slot / Material preview sphere / Link Object-vs-Data &mdash; small-frame overlay at fs16 floor on `#1a1a1f`/0.9 pills, orange leaders+dots; pixel-verified 1:1 on the cropped 721&times;1166 PNG).

---

## Phase plan

Section-aligned to the 8 HTML sections. PRODUCTION sub-chat split on &ge;5-figure phases; INTEGRATION its own chat on &ge;6-figure phases (v6 Workflow conventions + the carry-forward locked rule).

| Phase | Section (HTML id) | Figures | Count | PRODUCTION | INTEGRATION |
|-------|-------------------|---------|-------|------------|-------------|
| P1 | `#what-are-materials` | #1, #29 | 2 | single chat | inline (this chat) |
| P2 | `#shader-editor-interface` | #2, #3, #4, #21 | 4 | single chat | inline |
| P3 | `#understanding-nodes` | #5, #6 | 2 | single chat | inline (both SVG) |
| P4 | `#principled-bsdf` | #7, #28, #8, #9, #10 | 5 | **2 sub-chats** &mdash; A: #7, #28 (screenshots) &middot; B: #8, #9, #10 (renders) | own chat (5 = borderline; rides PROD-B close unless context tightens, then breaks out) |
| P5 | `#basic-material-properties` | #23, #24, #25 | 3 | single chat (3 SVG) | inline |
| P6 | `#creating-simple-materials` | #15, #16, #17, #18, #19, #20, #14 | 7 | **2 sub-chats** &mdash; A: #15, #16, #17 &middot; B: #18, #19, #20, #14 | **own chat (&ge;6)** |
| P7 | `#material-preview` | #11, #12, #13, #22 | 4 | single chat | inline |
| P8 | `#project` | #26, #27, #30 | 3 | single chat | inline |

**Phase ordering** follows HTML section order (P1&rarr;P8), not figure-number order. Within a phase, screenshots and SVGs are cheap; color-dependent renders are the cost center (P4-B, P6-A, P6-B, P1, P7).

### Cross-phase reuse (produce once, place by reference)

- **#10** &mdash; produced P4-B; reused-by-reference at P5 INTEGRATION (DONE: 2nd-placed in `#basic-material-properties` after the Material Personality Types table card, distinct 2nd-ref figcaption, `c10` overlay reused verbatim, no re-render).
- **#14** &mdash; produced P6-B; reused-by-reference at P8 INTEGRATION (`#project` Step 3).
- **#30** &mdash; produced P8; placed twice within `#project` (goal + showcase).

---

## Status log

- **2026-06-03 &mdash; Browser-QA pass COMPLETE / CLEAN (all L10 figures, 6 configs, no fixes).** Docs-first close-out; no HTML edits owed (HTML untouched, all invariants preserved). Both legs passed. **Structural QA &mdash; clean:** invariants intact (`<figure>` 33/33 &middot; em-dash 81 &middot; zero `&mdash;` / `color="#222"`); all 21 overlay viewBoxes == their PNG pixel dims; overlay wrapper markup correct on every PNG; overlay-tracking clean at 1920 / 1024 / 414 &times; dark+light. **Visual render leg &mdash; clean** (playwright + chromium, all 25 L10 PNGs staged Claude-side, loaded at 1920 / 1024 / 414 &times; dark+light): 28 figure slots (25 PNGs incl. #10 / #14 / #30 double-placements) all load, none clipped, worst aspect drift 0.016%; all 24 overlay SVGs align 0px to their img rect; all 6 inline SVGs (#5 / #6 / #23 / #24 / #25) flip `currentColor` correctly in both modes (dark &rarr; rgb(212,212,212), light &rarr; rgb(51,65,85)), zero retained `color` attrs. No fixes found; no invariant changes. L10 remains COMPLETE 30/30; the standing browser-QA pass is now cleared.

- **2026-06-03 &mdash; Phase 8 INTEGRATION COMPLETE (#27 committed; L10 DONE 30/30).** The last work in L10. Verified `edit_file` + `get_file_info` at session start (read tools + `edit_file` on the 1st `tool_search` pass; `get_file_info` on a 2nd pass). No BlenderMCP, no render, no crop owed (#26/#27 crops + c26/c27 overlays were pixel-verified in the prior chat; 4 of the 5 `#project` inserts &mdash; #30 goal, #14 reuse, #26, #30 showcase &mdash; were already committed bottom-up). Refreshed the working HTML copy via `copy_file_user_to_claude`; confirmed start state on the live file: 310,041 B, `<figure>` 32/32, em-dash 81, zero `&mdash;` / `color="#222"`, and the #27 anchor (`</blockquote>` + blank + `<h3>Your First Look at Nodes</h3>`) unique (count=1) in `#shader-editor-interface` at line ~528. **#27 committed** via `edit_file` (dryRun &rarr; commit &rarr; `get_file_info`): the col-20 figure block (matching the section's line-3471-style col-20 pattern) inserted immediately before that `<h3>`, the cropped 721&times;1166 PNG + the c27 5-callout small-frame overlay (fs16 floor, white text on `#1a1a1f`/0.9 pills, orange leaders+dots) carried verbatim from the prior chat &mdash; not rebuilt. **Final integrity check on the live file:** all 5 new refs present once each and in document order within their sections &mdash; `#project` reads #30 goal &rarr; #14 &rarr; #26 &rarr; #30 showcase (#14 also appears once in its original P6 home in `#creating-simple-materials`, the verbatim D3 reuse); #27 the sole ref in `#shader-editor-interface`; `<figure>` balance **33/33**; em-dash still **81**; zero `&mdash;` / `color="#222"`. HTML **313,707 B** (within the ~313,8xx estimate). No scene / render state touched &mdash; no restore owed. **L10 COMPLETE: 30/30 produced / 30/30 integrated.** Deferred: the L10 browser-QA pass (all 30 figures, dark+light, 1920/1024/414) &mdash; tracked in `session.md`. Cleanup done: the P7/P8 raw snips (`_p8_26_raw.png`, `_p8_27_raw.png`, `_p7_12_raw.png`, `_p7_22_raw.png`) + the `_l10p2_*` staging PNGs were moved out of the working `images\` dir into `images\_archive\` (cropped finals committed; outside the figure set, not blocking).

- **2026-06-03 &mdash; Phase 8 PRODUCTION PARTIAL (#30 produced + restored; #26 + #27 pending Ray captures).** Single chat (P8 = 3 figures, INTEGRATION planned inline once #26/#27 land). Verified `edit_file` + `get_file_info` at session start (read tools + `edit_file` on the 1st `tool_search` pass; `write_file` not needed this chat); BlenderMCP (`execute_blender_code` / `get_scene_info` / `get_viewport_screenshot`) surfaced on the 2nd pass. Confirmed clean base before any build: 15 objects (9 rig + 6 `M_*`), EEVEE / 512&times;512 / AgX / None / 0 / 1 / world 0.88 str 1.0 / cam `Cam`, zero `hide_render`, no `_WORK` collection &mdash; prior RESTORE was clean. **Ray's call:** render #30 first, captures #26/#27 after. Layout = 3&times;4 grid matching #14's look (Ray-approved); engine Cycles (Ray-approved, matches #14/#15/#17/#19). **#30** `images\lesson_10_30_project_completion_gallery.png` (1920&times;1080, 2,023,918 B): the 10 project materials (Chrome / Brushed Steel / Gold / Red Glossy Plastic / Black Rubber / Clear Glass / Frosted Glass / Cyan Emissive / Red Car Paint / Wood) on a 3&times;4 grid (4 cols &times; 3 rows, last 2 cells empty), built fresh in isolated `_L10P8_WORK` (base objects `hide_render`-hidden, dedicated `_p8_sph_00..09` spheres + `_p8_floor` dark plane + 3-point area rig + `_p8_cam`), **Cycles** 256spp + denoise, **Standard** view transform + look None per LD17 (cyan emissive carries color), world neutral-dark `(0.14,0.14,0.16)` str 1.0 so chrome/glass have reflection content per the Prompt-30 "studio environment" brief, F12 `render.render(write_still=True)` not opengl. **Camera framing took 3 passes:** v1 (loc 0,-11.5,6.8 / 52mm) too low + close, grid read as a jumbled cluster; v2 (0,-13.5,11.5 / 70mm) better spacing but over-zoomed, edge spheres cut off; v3 LOCKED (loc 0,-20,13 / 50mm, elevated 3/4 looking at origin) &mdash; all 10 sphere centers verified inside frame with margin via `world_to_camera_view`, clean grid lanes, depth reads. Grid spacing widened to dx/dy 3.1 (from 2.6) so spheres don't overlap. NO baked labels (`c30` caption/labels decided at INTEGRATION). Verified on disk via `get_file_info` + dims re-confirmed Claude-side (1920&times;1080 RGBA, clean decode). **RESTORE done + verified** against the opening base: all `_p8_*` objects/meshes/mats/cameras/lights removed, `_L10P8_WORK` collection removed, orphans purged (`Deleted 22 data-block(s)`), base objects un-hidden, EEVEE / 512&times;512 @100 / AgX / None / 0 / 1 / film False / world (0.88,0.88,0.88) str 1.0 / cam `Cam` / 15 objects / 6 `M_*` / zero hidden / no `_WORK` &mdash; confirmed clean. The `_p8_30_preview.png` temp was written to `images/` and deleted at close. **HTML NOT touched this chat** (PRODUCTION only; INTEGRATION deferred until #26/#27 land) &mdash; 298,542 B unchanged. **#26 + #27 NOT produced:** both are real-UI screenshots needing Ray at the keyboard per LD20 (Win+Shift+S to `images\`) &mdash; #26 the File&rarr;Append browser navigated into a `.blend`'s Material folder (material list + path breadcrumb + Append button in frame), #27 the Material Properties panel on an object with 2+ material slots (active slot + `+`/`-` slot buttons + Link Object/Data toggle in frame). **29 of 30 produced / 27 of 30 integrated.** **Pending P8 (the last L10 work):** Ray drops `_p8_26_raw.png` + `_p8_27_raw.png` into `images\` &rarr; crop/verify those two (WSL-side Pillow one-shot, `/mnt` can't reach the WSL tree) &rarr; build `c26`/`c27` overlays, pixel-verify 1:1 &rarr; P8 INTEGRATION (5 inserts into `#project`: #30 goal-image at intro, #14 reused-by-ref at Step 3 per D3, #26 in "How to Use Your Material Library", #30 again as success showcase after the completion checklist, #27 at the Material Slots explanation). On INTEGRATION complete, L10 is DONE 30/30 &mdash; close out the docs.

- **2026-06-03 &mdash; Phase 7 COMPLETE (#12 + #22 cropped, then #11/#12/#13/#22 INTEGRATED into `#material-preview`).** Continuation of P7 (PRODUCTION of #13 + #11 was the prior chat; #12 + #22 were the keyboard-interactive captures owed to Ray). Verified `edit_file` + `get_file_info` at session start (each on its own `tool_search` pass; `write_file` surfaced only on a later pass &mdash; the known intermittent write-tool lag, not blocking since INTEGRATION uses `edit_file`). No BlenderMCP (no re-capture, no render owed). **#12 + #22 capture/crop:** Ray captured both popovers via Win+Shift+S into `images\` as `_p7_12_raw.png` (603&times;1053) + `_p7_22_raw.png` (978&times;634). Confirmed #12 is the correct popover: this build folds all viewport-shading settings into one "Viewport Shading" popover whose contents depend on the active mode &mdash; because Material Preview was active, it shows the spec'd Lighting / HDRI sphere picker / Rotation / Strength / World Opacity / Blur (the Wireframe Color / Render Pass / Compositor rows below are this build's extras, trimmed out at crop). Cropped on the WSL side via Pillow (Claude's `/mnt` is read-only + can't reach the WSL tree; `convert`/ImageMagick absent; WSL python lacked PIL &mdash; installed `Pillow` without `--break-system-packages` since that WSL pip predates the flag): **#12** `images\lesson_10_12_material_preview_settings.png` (580&times;610, 77,710 B, crop box 8,18,588,628 tight to popover, bottom trimmed at the Blur row) + **#22** `images\lesson_10_22_z_key_pie_menu.png` (934&times;590, 435,593 B, crop box 28,30,962,620 trimming dead background, all 4 segment labels + center wheel kept). Both verified on disk via `get_file_info` + dims re-confirmed Claude-side. NO baked labels. **Overlays built at INTEGRATION, pixel-verified 1:1 via cairosvg-composite-over-PNG (LD19) measuring on the actual PNG pixels BEFORE commit:** `c11` (dividers measured at x1281/y409 &rarr; 1280&times;408 quadrants; 4 mode-labels centered below each sphere group, fs 26), `c13` (midpoint dividers &rarr; 802&times;802 quadrants; 4 HDRI-name labels near quadrant tops, fs 30), `c12` (5 orange leader+dot callouts to Lighting [y115] / HDRI sphere [y320] / Rotation row [y432] / Strength slider [y480] / Blur row [y580], pills stacked in the right margin, fs 16 small-frame floor; Strength + Blur pill positions nudged in a 2nd pass to clear the World Opacity row + the 0.50 value text). **`c22` skipped** (segment text already legible). All overlays: white text on `#1a1a1f`/0.9 pills, `#FF6B00` leaders+dots; rebuilt to the file's exact figure style (flat `<rect rx>`+`<text text-anchor=middle font-weight=700 fill=#fff>`, no root `color=#222`, no root width/height). **INTEGRATION inline**, all 4 into `section id="material-preview"`, built bottom-up by document position (#22 &rarr; #13 &rarr; #12 &rarr; #11) so earlier anchors stayed stable; dryRun &rarr; commit &rarr; `get_file_info` per insert; `<figure>` at col-16 matching the section's existing figures. **Anchors (confirmed on disk before each insert):** #11 after the "Four Viewport Shading Modes" table card `</div>` (before the `Z opens a radial menu` `<p>`); #12 after the "Key Material Preview Settings" card `</div>` (before the HDRI-Explained blockquote); #13 after the HDRI-Explained blockquote `</blockquote>` (before the `Rendered Mode` `<h3>`); #22 after the "Shading Mode Quick Switch" card `</div>` (before the Pro-Workflow blockquote). **Placement note:** #12 lands after the settings *card* (not the bare "Material Preview Settings" paragraph) and #22 after the quick-switch *card* (not the bare heading) &mdash; matches the established L10 figure-after-content pattern; Ray approved. HTML byte progression: 289,179 &rarr; 290,185 (#22) &rarr; 292,592 (#13) &rarr; 296,082 (#12) &rarr; **298,542** (#11). **Final integrity check on the live file:** all 4 refs present exactly once + in document order inside the section (#11 &rarr; #12 &rarr; #13 &rarr; #22); `<figure>`/`</figure>` balanced **28/28**; literal em-dash count unchanged at **81** (pre-existing section em-dashes &mdash; "crucial for material creation&mdash;each", "Range Image&mdash;a", "studio HDRI&mdash;perfect", "neutral studio lighting&mdash;default", "render&mdash;the full power" &mdash; all preserved verbatim through exact anchors); zero `&mdash;` entities; zero `color="#222"` leaks. No scene / render state touched (INTEGRATION-only) &mdash; no restore owed. Registered `c11`/`c12`/`c13` built + `c22` skipped in the overlay registry. **28 of 30 produced / 27 of 30 integrated.** One harmless leftover noted in the prior chat: `%TEMP%\_p7_11_solid_full.png` (WinError 32, outside project tree) &mdash; ignore. **Remaining L10: P8 only** (`#project`: #26, #27, #30 &mdash; the last phase; #14 reused-by-ref at P8 Step 3 per D3; #30 placed 2&times;).

- **2026-06-03 &mdash; Phase 7 PRODUCTION PARTIAL (#13 + #11 produced; #12 + #22 pending).** Single chat (P7 = 4 figures, INTEGRATION planned inline). Verified `edit_file` + `write_file` + `get_file_info` at session start (each surfaced on its own `tool_search` pass &mdash; read tools first, then `write_file`/`move_file`, then `edit_file`); BlenderMCP (`execute_blender_code` / `get_scene_info` / `get_viewport_screenshot`) surfaced on the 2nd pass. Confirmed clean base scene before any render: 15 objects (9 rig + 6 `M_*`), EEVEE / 512&times;512 / AgX / None / 0 / 1 / world 0.88 str 1.0 / cam `Cam`, no `_WORK` collection, zero `hide_render` &mdash; prior RESTORE was clean. **#13 sourcing decision (Ray-approved):** use 4 Blender-bundled `datafiles/studiolights/world/*.exr` HDRIs; enumerated 8 present (city/courtyard/forest/interior/night/studio/sunrise/sunset) and the spec's exact four are all present &mdash; used **studio.exr / forest.exr / interior.exr / night.exr** 1:1, labeled Studio/Forest/Interior/Night. **#13** `images\lesson_10_13_hdri_environments.png` (1604&times;1604, 2,430,368 B): chrome sphere (Base Color 0.85, Metallic 1.0, Rough 0.05) rendered once per HDRI in isolated `_L10P7_WORK` (base objects `hide_render`-hidden, dedicated `_p7_chrome` sphere + `_p7_cam` at (0,-4.2,0) looking +Y), **Cycles** 200spp + denoise (chrome reflects off-screen HDRI &rarr; EEVEE screen-space insufficient, per the carried EEVEE-Next finding), **Standard** view transform per LD17, each panel 1000&times;1000 then PIL-stitched 2&times;2 (downscaled to 800px cells + 4px white dividers, order studio TL / forest TR / interior BL / night BR) on the Blender side into `images/`. Visually verified studio (studio lights+curtains in mirror) + forest (trees/sky/path) distinct; interior warm-tan + night dark-warm confirmed by corner pixel samples. NO baked labels. **RESTORE done + verified** against the opening base: `_L10P7_WORK` + `_p7_*` objects/meshes/mats/cams removed, orphan `.exr` images purged, base objects un-hidden, EEVEE / 512&times;512 @100 / AgX/None/0/1 / film False / world (0.88,0.88,0.88) str 1.0 / cam `Cam` / 15 objects / no WORK coll / zero hidden &mdash; confirmed clean. The four `_p7_13_*.png` temp renders were written to `images/` (not `%TEMP%`) and deleted at close. **#11** `images\lesson_10_11_viewport_shading_modes.png` (2564&times;820, 1,134,757 B): base scene's 6 `M_*` spheres framed through cam `Cam`, captured in all 4 viewport shading modes (Solid / Material Preview / Rendered / Wireframe) via full-window `bpy.ops.screen.screenshot` then PIL-crop to the VIEW_3D WINDOW region (x295 w1280; blender-y 523..931 after dropping the 76px header+tool-header strip &rarr; PIL crop top=115 bottom=523, 1280&times;408 per panel) + 2&times;2 stitch (4px dividers, Solid TL / MatPreview TR / Rendered BL / Wireframe BR). **Capture finding:** `screen.screenshot` grabs a STALE framebuffer when `sp.shading.type` is changed within the same script &mdash; the first 4-mode pass came out pixel-identical; fixed by forcing `area.tag_redraw()` + `bpy.ops.wm.redraw_timer(type='DRAW_WIN_SWAP', iterations=2)` between each mode switch, then capturing. Overlays/gizmo turned off for clean panels (faint collapsed side-panel `‹` arrows remain at far edges, negligible, sit under the figure frame), then restored (overlays on / gizmo on / RENDERED / PERSP). NO baked mode labels. UI-capture only, no render-state debt for #11. **One leftover temp:** `%TEMP%\_p7_11_solid_full.png` could not be deleted (WinError 32, file lock) &mdash; harmless, outside the project tree, OS-cleared; the other 3 mode temps were removed. **HTML NOT touched this chat** (PRODUCTION only, INTEGRATION deferred) &mdash; 289,179 B unchanged. **#12 + #22 NOT produced:** both are transient popovers (#12 Material-Preview-shading dropdown; #22 Z pie menu) requiring keyboard-interactive Win+Shift+S capture (LD20) &mdash; Ray at the keyboard, cannot be done by Claude solo. **24 of 30 produced / 23 of 30 integrated.** **Pending P7:** Ray captures #12 + #22 raw snips &rarr; crop/verify those two &rarr; P7 INTEGRATION (all 4 into `#material-preview`: build `c11`/`c12`/`c13`/`c22` overlays, pixel-verify 1:1, insert). Then P8 (last phase).

- **2026-06-03 &mdash; Phase 6 INTEGRATION COMPLETE (#15/#16/#17/#18/#19/#20 into `#creating-simple-materials`).** Its own chat (&ge;6-figure rule). RETRY of the prior kickoff chat that verified everything but ran out of turns before the first commit (0 of 6 had been committed; file was untouched at 278,255 B). Verified `edit_file` + `get_file_info` at session start (no BlenderMCP needed &mdash; all six overlays are Claude-side SVG over the existing PNGs, no render). Refreshed the working HTML copy via `copy_file_user_to_claude` and re-confirmed start state on the live file before the first insert: 278,255 B, `<figure>` balance 18/18, em-dash 81, all six anchor `<h3>`s unique (count=1), all six new refs absent, #14 already present. All six PNG dims re-confirmed on disk Claude-side (#15 1200&times;1200, #16/#18/#19/#20 1600&times;900, #17 1200&times;900). Rebuilt all six overlays this chat (prior chat's `/home/claude/l10/` artifacts don't persist) and pixel-verified each 1:1 via cairosvg-composite-over-PNG per LD19 BEFORE any commit, measuring subject centers on the actual PNGs rather than trusting kickoff estimates: #16 red cx318 / blue cx1271 (kickoff 323/1271); #18 clear-left cx~305 / frosted-right cx~1255 (pills kept at kickoff 333/1278, well within the large sphere bodies); #17 sphere frame-centered cx600; #15 chrome sphere cx~575 (caption centered there, not frame-center 600); #20 single sphere cx814 (kickoff 812); #19 sphere lit-rim brightest at x705,y457 so the leader endpoint was set to x715,y445 (vs the kickoff's x720,y420 estimate). **c19 spec adjustment (Ray-approved):** the full emission string at fs16 (~470px) does not fit the solid blue panel (~258px wide), so the emission caption was split to 3 lines (`Emission` / `RGB 0.3/0.6/1.0` / `Strength 10.0`) in a 232px pill centered on the panel at x281 &mdash; fully contained, same overflow&rarr;multi-line pattern as c1/c29; the glow callout + orange leader to the lit rim is unchanged. All overlays: white text on `#1a1a1f`/0.9 pills, `&#183;` separators, `#FF6B00` leaders+dots, fs 18 (c19/c20 callout pills at small-frame floor 16). Built all six `<figure>` blocks at #14's exact depth (col-16 `<figure>` / col-20 `<div>`+`<figcaption>` / col-24 `<img>`+`<svg>` / col-28 SVG body), matching #14, NOT the section's col-20 `<h3>`s; SVG roots carry no `color="#222"` and no width/height (cairosvg width/height was added only for the offline composite check). Integrated bottom-up by document position (#20 &rarr; #19 &rarr; #18 &rarr; #17 &rarr; #16 &rarr; #15) so earlier anchors stayed stable, each insert immediately before its anchor `<h3>`; dryRun &rarr; commit &rarr; `get_file_info` per insert. Anchors (material numbering shifted vs placement-map labels, exact `<h3>`s confirmed correct): #15 before `Material 2: Matte Plastic`; #16 before `Material 4: Clear Glass`; #17 before `Material 5: Frosted Glass`; #18 before `Material 6: Glowing Emissive Surface`; #19 before `Material 7: Brushed Metal`; #20 before `Organizing Your Material Library`. HTML byte progression: 278,255 &rarr; 280,388 (#20) &rarr; 282,704 (#19) &rarr; 284,581 (#18) &rarr; 285,945 (#17) &rarr; 287,828 (#16) &rarr; **289,179** (#15). Final integrity check on the live file: all 7 figure refs (#14/#15/#16/#17/#18/#19/#20) present exactly once and in document order inside the section (#15 &rarr; #16 &rarr; #17 &rarr; #18 &rarr; #19 &rarr; #20 &rarr; #14, all within `#creating-simple-materials` lines 2402&ndash;2985, #14 last before `#material-preview` exactly where it already lived); `<figure>`/`</figure>` balanced **24/24**; em-dash count still **81** (zero new; pre-existing section prose em-dashes &mdash; e.g. "steel&mdash;metal", "glass is frosted&mdash;still", "light&mdash;perfect", "different&mdash;a matte", "transparent&mdash;a realistic", "golden hue&mdash;that's" &mdash; all preserved verbatim through exact anchors); zero `color="#222"` leaks; 60 `&#183;` entities across the new figures. No scene / render state touched (INTEGRATION-only) &mdash; no restore owed. Per D3, #14 is reused-by-reference at P8 INTEGRATION (`#project` Step 3), no second render &mdash; so this chat advances the integrated count by 6 (#15&ndash;#20) to **23/30**. **Remaining L10: P7 + P8.**

- **2026-06-03 &mdash; Phase 6 PROD-B COMPLETE (#18, #19, #20, #14 produced).** Sub-chat B of the P6 PRODUCTION split (`#creating-simple-materials`; the lesson's only &ge;6-figure phase). Verified `edit_file` + `get_file_info` at session start; BlenderMCP (`execute_blender_code` / `get_scene_info`) surfaced on the 2nd `tool_search` pass. **Interrupted-prior-run recovery:** scene was found mid-#20 (NOT clean base) &mdash; base objects merely `hide_render`-hidden rather than removed, so this was an interrupted PROD-B, not a PROD-A restore failure; #18 / #19 / #20 finals already on disk, #14 missing. Confirmed via `get_file_info` + full decode + dims + visual check before any build. **#18** `images\lesson_10_18_frosted_clear_comparison.png` (1600&times;900) kept from the interrupted run after on-disk verification (correct dims, clean decode, visual check). **#20** `images\lesson_10_20_car_paint_clearcoat.png` (1600&times;900) likewise kept after verification. **#19** `images\lesson_10_19_emissive_glow.png` (1600&times;900) RE-RENDERED this round &mdash; the prior version clipped the emissive panel to white; fixed by dropping panel emission to non-clipping strength 0.92 and driving scene illumination via a separate blue area+point light, so the panel reads blue [sampled ~RGB 143,196,245] and the sphere catches a clear blue glow with falloff (Cycles 200spp + denoise, Standard view transform). **#14** `images\lesson_10_14_material_library_organization.png` (1920&times;1080) built fresh: 12 spheres, 3&times;4 grid, all spec materials, slight elevated angle, even studio light, dark grey bg (Cycles 220spp + denoise). All four verified on disk via `get_file_info` + dims. **Engine choices:** Cycles for #14 (chrome / glass cells), #18 (refraction), #19 (falloff), #20 (clearcoat), per the carried-forward EEVEE-Next off-screen-reflection finding. NO baked labels (overlays `c18` / `c19` / `c20` / `c14` reserved now, built at P6 INTEGRATION). **RESTORE done + verified** against the opening `get_scene_info`: all `_l10p6b_*` temps deleted, `_L10P6B_WORK` removed, orphans purged, the 15 base objects un-hidden + render-visible, EEVEE / 512&times;512 / AgX / None / 0 / 1 / world 0.88 str 1.0 / cam `Cam` &mdash; confirmed clean. The two pre-existing `_l10p2_*` Phase-2 staging files were left untouched (not mine). #14 is reused-by-reference at P8 INTEGRATION (`#project` Step 3) per D3 &mdash; no second render. **22 of 30 produced / 16 of 30 integrated.** Do NOT integrate this chat. **Pending:** P6 INTEGRATION (its own chat, &ge;6-figure rule: #15/#16/#17/#18/#19/#20/#14 into `#creating-simple-materials`), then P7 + P8.

- **2026-06-03 &mdash; Phase 6 PROD-A COMPLETE (#15, #16, #17 produced).** Sub-chat A of the P6 PRODUCTION split (the lesson's only &ge;6-figure phase; `#creating-simple-materials`). Recovered cleanly from an interrupt: on-disk start state confirmed at P5 INTEGRATION COMPLETE (16/30, no P6 artifacts on disk, no restore owed) before any build. Verified `edit_file` + `get_file_info` at session start; BlenderMCP (`execute_blender_code` / `get_scene_info` / `get_viewport_screenshot`) surfaced on the 2nd `tool_search` pass. Scene found at the documented base (EEVEE, 512&times;512, AgX/None/0/1, world 0.88 str 1.0) with the inherited P4 studio rig + `M_*` source spheres present; built each shot fresh in an isolated `_L10P6A_WORK` collection (base objects hidden via `hide_render`, never deleted) so material values were exactly the locked specs. All three plan-locked in writing before build (sphere chosen for #17 per Ray). All produced + verified on disk via `get_file_info`, NO baked labels (overlays `c15`/`c16`/`c17` built at P6 INTEGRATION): **#15** `images\lesson_10_15_chrome_result.png` (1200&times;1200, 1,423,821 B, **Cycles** 200spp + denoise; Base Color #D9D9D9, Metallic 1.0, Rough 0.05; studio + 5 colored accent reflectors so the mirror has structured content; F12 `render.render` + Standard view transform per LD17; Cycles not EEVEE because chrome reflects off-screen geometry per the PROD-B engine finding); **#16** `images\lesson_10_16_matte_glossy_comparison.png` (1600&times;900, 1,431,832 B, **EEVEE-Next** 128 TAA; matte red #8B0000 R0.9 [left] vs glossy blue #1E90FF R0.2 [right], both Metallic 0.0; strong directional key; EEVEE was sufficient &mdash; glossy highlight is an in-frame light reflection, no off-screen reflectors, so no Cycles fallback needed; Standard view transform); **#17** `images\lesson_10_17_clear_glass.png` (1200&times;900, 986,438 B, **Cycles** 220spp + denoise; Transmission 1.0, Rough 0.0, IOR 1.45; b/w checker emission backdrop + small red color-object behind glass; Cycles mandatory per the PROD-B finding [EEVEE-Next screen-space raytracing won't refract an off-screen backdrop] &mdash; confirmed: refraction visibly bends/inverts the checker through the sphere; Standard view transform). Each previewed at low samples + reframed before the final commit (sphere framing/headroom tuned for the below-sphere label pills + center divider that #16's `c16` adds at INTEGRATION). Reserved `c15`/`c16`/`c17` in the overlay `c{N}` registry. **RESTORE done at close** (renders had left the scene on Standard / Cycles / 0.05 world / non-base res): `_L10P6A_WORK` collection + all its objects removed, orphan data purged, base objects un-hidden (`hide_render=False`), view_transform back to AgX + look None + exposure 0 + gamma 1, world `(0.88,0.88,0.88)` strength 1.0, engine BLENDER_EEVEE, res 512&times;512 @ 100%, film_transparent False, scene camera back to `Cam`; verified the 15 base objects all present + render-visible against the opening `get_scene_info`. All three preview temps removed from the project `images/` dir (rendered there, not Windows `%TEMP%`, because `%TEMP%` is outside the allowed Filesystem dirs &mdash; the two pre-existing `_l10p2_*` Phase-2 staging files are not mine to touch). **18 of 30 produced / 16 of 30 integrated.** Do NOT integrate this chat. **Pending:** P6 PROD-B (#18 / #19 / #20 / #14 &mdash; #14 produced once here, reused-by-ref at P8 per D3), then P6 INTEGRATION (its own chat, &ge;6-figure rule: #15/#16/#17/#18/#19/#20/#14 into `#creating-simple-materials`).

- **2026-06-03 &mdash; Phase 5 PRODUCTION + INTEGRATION COMPLETE (#23, #24, #25 inline SVG + #10 2nd-ref) into `#basic-material-properties`.** PRODUCTION of the three SVGs (`dt`/`mb`/`ir`) was done in a prior sub-chat (built to v6 viewBoxes, cairosvg-verified 1:1 on light + dark, standalone files keep `color="#222"`); this chat finished INTEGRATION. Verified `edit_file` + `get_file_info` at session start (BlenderMCP not needed &mdash; all inline SVG / reused PNG, no render). #25 `ir_reference_chart` was already inlined + committed at the prior chat's close (progress doc + session.md were stale at 13/13; true on-disk start state was 14 produced / 14 integrated). Three inserts this chat, bottom-up by document position (#24 &rarr; #10 &rarr; #23), dryRun &rarr; commit &rarr; `get_file_info` each, root `color="#222"` stripped on the two inlined SVGs (#24, #23), gradient stops collapsed one-per-line in #24's `<defs>`: **#24** `metal_base_colors` inline SVG (viewBox 800&times;560, 4 radialGradient swatch discs) after the Base Color Best Practices card `</div>`, before the Pure Black/White Trap card &mdash; newText preserved the trap card `<div>` + its `<h4>⚠️ The Pure Black/White Trap</h4>` intact (the prior-chat dry-run had dropped that `<h4>`; fixed here, was never committed so disk was clean); HTML 255,987 &rarr; 261,788 B. **#10** 2nd-ref by D2 &mdash; reused the verbatim P4 #10 `<figure>` (img `lesson_10_10_material_personality_types.png` + full `c10` overlay SVG viewBox 1600&times;1200, 6 two-line pills, ids intentionally reused, NOT renamed) after the Material Personality Types table card `</div>` before the "🎯 The Real-World Test" `<blockquote>`, with a distinct 2nd-ref figcaption per the L09 #26 pattern; no re-render; 261,788 &rarr; 266,392 B. **#23** `workflow_decision_tree` inline SVG (viewBox 800&times;680) after the "This simple decision tree handles 80%..." `<p>` before the "Understanding Material 'Personality'" `<h3>`, complementing (not replacing) the existing inline mermaid decision-tree card; 266,392 &rarr; **273,203** B. All three at default 16/20 figure depth. Final integrity check on the live file: all 4 new figure refs present + in document order inside the section (#23 dt &rarr; #10 2nd-ref &rarr; #24 mb &rarr; #25 ir); `<figure>`/`</figure>` balanced **17/17**; zero `color="#222"` leaked into inlined SVGs; em-dash count unchanged at 78 (zero new; pre-existing section em-dashes "every parameter—it's", "More Than Just Paint", "—even fresh snow" preserved verbatim through exact anchors); all new SVG/figcaption text uses `&#183;` separators (#25's two `&#8211;` en-dashes are content IOR-range entities, fine). No scene / render state touched (INTEGRATION-only) &mdash; no restore owed. **Pending:** P6 (`#creating-simple-materials`: #15/#16/#17/#18/#19/#20/#14 &mdash; the &ge;6-figure phase; PRODUCTION 2 sub-chats [A: #15/#16/#17, B: #18/#19/#20/#14], INTEGRATION its own chat; #14 reused-by-ref at P8).

- **2026-06-03 &mdash; Phase 4 INTEGRATION COMPLETE (#7, #28, #8, #9, #10 into `#principled-bsdf`).** Own chat (rode after PROD-B). Verified `edit_file` + `get_file_info` + BlenderMCP (`execute_blender_code`, 2nd tool_search pass) at session start; INTEGRATION used only `edit_file` (overlays are Claude-side SVG over the existing PNGs, no new render). All five on-disk PNG dimensions re-confirmed Claude-side and match spec exactly (#7 1900&times;644, #28 644&times;448, #8 1600&times;900, #9 2000&times;700, #10 1600&times;1200). Built `c7` / `c28` / `c8` / `c9` / `c10` measuring callout coords against the ACTUAL PNG pixels (not kickoff estimates), then pixel-verified each 1:1 via cairosvg-composite-over-PNG on Claude's side BEFORE commit per LD19: #9 6 sphere centers x314/587/860/1133/1408/1683 bottom~y392 (pills y408); #8 2 sphere centers x317/1282 (pills y770 on the lower sphere body, spheres run to frame edges); #10 grid cols x390/790/1190 rows y-center~350/770 (pills y470 row1 / y900 row2, columns land under all 6 incl. the 2 glass spheres); #7 node-left x408 with essential sockets y92&ndash;270 + collapsed panels y280&ndash;500; #28 header rows Diffuse&rarr;Thin Film, tags on Subsurface y121 / Coat y257 / Sheen y303. **c8 = 2 pills, NOT 3** (see registry note: the render + requirements-doc #8 are a 2-sphere 0.0-vs-1.0 side-by-side; the PROD-B "3 pills" reservation was a miscount; the 0.5 table value is text-only). **c7 labels shortened to ESSENTIAL / ADVANCED** after a first pass with longer text overflowed the node's cramped left margin (caught in the pre-commit cairosvg check). Integrated bottom-up by document position (#28 highest line first, then #10, #9, #8, #7) so earlier anchors stayed stable; dryRun &rarr; commit &rarr; `get_file_info` per insert. All five at default 16/20 figure depth directly in `section id="principled-bsdf"`: **#7** after the "The Principled BSDF Interface" `<p>` before the Structure card &middot; **#8** after the Metallic Physics-Rule `<blockquote>` before the Roughness `<h3>` &middot; **#9** after the "most important parameter" `<p>` before the Transmission `<h3>` &middot; **#10** after the Basic Material Formulas card before the Advanced Parameters `<h3>` &middot; **#28** after the advanced-params card before the "We'll explore these" `<p>`. HTML byte progression: 231,671 &rarr; 234,343 (#28) &rarr; 238,908 (#10) &rarr; 241,928 (#9) &rarr; 243,680 (#8) &rarr; **246,065** (#7), +14,394 total. Final integrity check on the live file: all 5 image refs present (1 each) and in document order inside the section; `<figure>`/`</figure>` balanced 13/13; pre-existing literal em-dashes in the section prose (e.g. "Swiss Army knife of shaders\u2014it", "sounds like\u2014the main color") preserved verbatim; zero new em-dashes (all new figcaption/alt prose uses sentence breaks, all new SVG label text uses `&#183;`). #10 is reused-by-reference at P5 INTEGRATION per D2 (produced once at P4-B; no re-render). **No scene / render state touched this chat** (INTEGRATION-only, no BlenderMCP render) &mdash; no restore owed. **Pending:** P5 INTEGRATION (`#basic-material-properties`: #23, #24, #25 + the #10 2nd-ref by D2).

- **2026-06-03 &mdash; Phase 4 PROD-B COMPLETE (#8, #9, #10 produced).** Sub-chat B of the P4 PRODUCTION split (color-dependent / PBR sphere renders; value/material labels are the `c8`/`c9`/`c10` overlays built at INTEGRATION). Verified `edit_file` + `get_file_info` + BlenderMCP (`execute_blender_code`, 2nd tool_search pass) at session start. All three produced + verified on disk via `get_file_info`, NO baked labels: **#8** `images\lesson_10_08_metallic_comparison.png` (1600&times;900, 866,709 B, **EEVEE-Next**); **#9** `images\lesson_10_09_roughness_scale.png` (2000&times;700, 755,837 B, **Cycles**); **#10** `images\lesson_10_10_material_personality_types.png` (1600&times;1200, 1,268,000 B, **Cycles**). Reserved `c8` / `c9` / `c10` in the overlay `c{N}` registry (c8 = 3 metallic-value pills; c9 = 6 roughness-value pills; c10 = 6 material-name+param pills). **Engine finding:** EEVEE-Next screen-space raytracing only reflects / refracts on-screen geometry, so #9's off-screen reflectors and #10's glass refraction came out wrong under EEVEE and were re-rendered in **Cycles**; #8 stayed **EEVEE** because its reflectors are in-frame. #10 is reused-by-reference at P5 INTEGRATION per D2 (produced once here). **RESTORE done at close** (the scene had been left on Standard / 0.05 world / Cycles by the renders): view_transform back to AgX + look None + exposure 0.0 + gamma 1.0; world `(0.88,0.88,0.88)` strength 1.0; engine BLENDER_EEVEE, res 512&times;512 @ 100%, film_transparent False; L09 base marker values (L09Orange / L09Cyan / L09GridLine) set defensively &mdash; all three confirmed ABSENT from this scene's data (untouched this chat, as expected, so nothing to restore); six working temps deleted from Windows `%TEMP%` (`_l10p4b_08_render.png`, `_l10p4b_09_render.png`, `_l10p4b_09_cycles.png`, `_l10p4b_09_final.png`, `_l10p4b_10_render.png`, `_l10p4b_10_final.png`). **Pending:** P4 INTEGRATION (#7, #28, #8, #9, #10 into `#principled-bsdf`).

- **2026-06-03 &mdash; Phase 4 PROD-A COMPLETE (#7, #28 produced).** Sub-chat A of the P4 PRODUCTION split (annotated real Principled BSDF node screenshots; overlays `c7` + `c28` built at INTEGRATION). Verified `edit_file` + `get_file_info` + BlenderMCP (`execute_blender_code` / `get_viewport_screenshot` / `get_scene_info`, 2nd tool_search pass) at session start. Built a clean single-Principled-BSDF material (`L10_BSDF_demo`) on the Cube, captured via the v6 LD20 docked-panel path: full-window `bpy.ops.screen.screenshot` &rarr; local Windows tempdir &rarr; `PIL.Image.open` &rarr; deterministic crop to the live NODE_EDITOR `area.x/y/w/h` (bottom-left&rarr;top-left flip) &rarr; drop 64px header strip &rarr; Pillow-save to WSL `images/`; breadcrumb overlay hidden via `space.overlay.show_context_path=False` to clear the node-header collision (restored at close). **Running Blender is 5.1.1**, where the Principled BSDF advanced params live inside collapsible UI sub-panels (Diffuse / Subsurface / Specular / Transmission / Coat / Sheen / Emission / Thin Film) whose open/collapse state is NOT a writable Python property (no `panel`/`expand` RNA prop; no working `bpy.ops.node` expand op; linking a panel socket does NOT auto-expand &mdash; verified). Per Ray's call, **#28 re-scoped (option 2)** to a focused close-up of the collapsed panel-header stack rather than expanded sub-rows; `c28` use-case tags point at the panel headers at INTEGRATION. **#7** `lesson_10_07_principled_bsdf_sections.png` (1900&times;644, 88,819 B): full BSDF node in context with Material Output, showing all top-level essential sockets (Base Color, Metallic, Roughness, IOR, Alpha, Normal) + the 8 collapsed panel headers; captured at the 2849&times;1538 window. **#28** `lesson_10_28_advanced_parameters.png` (644&times;448, 54,702 B): tight native crop of the panel-header stack (Normal + Diffuse&rarr;Thin Film) with the node's left/bottom borders, then 2&times; LANCZOS upscale (flat UI, crisp); captured at the 1920&times;1046 window (Blender window was resized between the two captures &mdash; each crop used live `area` geometry so both are valid). Both land in the flat-UI size band (38&ndash;650 KB), below the photoreal-render band, correct. NO baked labels on either (all callouts are the `c{N}` overlay built at INTEGRATION, which re-measures pixel rows per LD19). Both verified on disk via `get_file_info`. Node graph restored (Material Output back to x360); working temp PNGs (`_l10p4_node_crop_preview.png`, `_l10p4_full_for28.png`, `_l10p4_28_crop_preview.png`) deleted. **UI-capture-only chat: no AgX / world / material-color render state touched &mdash; no render-state restore owed.** `c7` / `c28` were already pre-listed in the overlay registry (no registry change). **Pending for later sub-chats:** PROD-B (#8 / #9 / #10 color-dependent PBR sphere renders) and P4 INTEGRATION (#7, #28, #8, #9, #10 into `#principled-bsdf`).

- **2026-06-03 &mdash; Phase 3 PRODUCTION + INTEGRATION COMPLETE (#5, #6).** Single chat, INTEGRATION inline (2-figure phase, both inline SVG per the locked SVG-vs-PNG split). Verified `edit_file` + `get_file_info` at session start; BlenderMCP not needed (both figures hand-written inline SVG, no capture / no color-dependent render); `write_file` surfaced on a 2nd tool_search pass and was used to write the two standalone `.svg` files. Built both as default-560-viewBox SVGs to spec, render-checked 1:1 via cairosvg on Claude's side on BOTH light (`#ffffff`) and dark (`#1a1a1f`) backgrounds BEFORE commit. **#5** `lesson_10_05_node_socket_colors.svg` (id-prefix `sk`, 3,509 B): 4 socket-data-type rows (green `#4ADE80` Shader / yellow `#facc15` Color / gray `#a1a1a1` Value / purple `#c084fc` Vector) as colored circles + full-opacity type label + 0.6-opacity description + right-anchored hex chip, divider lines between rows, Variant-B left-rail footer tip. Socket colors are fixed hex (data-type colors, not `currentColor`); each row is labeled so color is reinforcement not sole carrier (WCAG). **#6** `lesson_10_06_node_directionality.svg` (id-prefix `nd`, 4,774 B): generic node box (`currentColor` 0.06 fill + 0.55-opacity stroke, orange header band, "Generic Node" / "Processing happens here"), 3 colored input sockets (yellow/gray/purple) on the left with arrows pointing INTO the node under an "Inputs" label, 2 colored output sockets (green/yellow) on the right with arrows pointing OUT under an "Outputs" label, a bold orange `#FF6B00` left-to-right flow arrow beneath with "Data flows left to right" caption, Variant-B footer tip. Both standalone files keep the `color="#222"` `<img>`-fallback attribute; both inlined copies have it stripped per the inlining checklist. INTEGRATION inline at default 16/20 depth directly in `section id="understanding-nodes"`, built bottom-up by document position (committed #5 first so #6's higher anchor stayed stable), dryRun &rarr; commit &rarr; `get_file_info` per insertion: **#5** after the Node Categories table card `</div>` before the "Don't Memorize Yet" `<blockquote>` &rarr; HTML 225,580 B; **#6** after the "What Is a Node?" `<p>` before the Kitchen Analogy `<blockquote>` &rarr; 231,671 B. Pre-existing em-dash in the #6 anchor `<p>` ("assembly line&mdash;each") preserved verbatim; no new em-dashes (middle-dot `&#183;` throughout new SVG text + figcaptions). Registered nothing in the `c{N}` overlay registry (SVGs use the `sk`/`nd` id-prefix registry, already pre-listed). No scene / render state touched (no BlenderMCP this phase) &mdash; no restore needed.

- **2026-06-02 &mdash; Phase 2 PRODUCTION + INTEGRATION COMPLETE (#2, #3, #4, #21).** Single chat, INTEGRATION inline (4-figure phase). Verified `edit_file` + `get_file_info` + BlenderMCP (`execute_blender_code`, 2nd tool_search pass) at session start; `write_file` not needed (no new doc files; `edit_file` carried all four inserts). #4 (default_node_setup) was produced + integrated in the prior chat (650&times;364, 38,685 B). This chat produced #2 / #3 / #21 from the two staged full-window sources (`_l10p2_fullwin3_stage.png` 1920&times;1046 panned, `_l10p2_fullwin_stage.png` 1920&times;1046 un-panned) via bridge-side PIL crops to the locked boxes, written straight to `images/`: **#2** `lesson_10_02_shader_editor_overview.png` = full un-panned 4-region window (1920&times;1046, 650,262 B); **#3** `lesson_10_03_shader_editor_anatomy.png` crop (295,528,1575,1000) &rarr; 1280&times;472 (61,787 B); **#21** `lesson_10_21_node_connection_demo.png` crop (735,558,1385,990) &rarr; 650&times;432 (39,866 B). All UI captures landed 38&ndash;650 KB (flat-UI band, below the 200&ndash;800 KB photoreal-render band, correct). Built all three overlays, pixel-verified 1:1 via cairosvg-composite-over-PNG on Claude's side BEFORE commit (`pip install cairosvg --break-system-packages`). Measured node/socket pixels on the actual crops rather than trusting the kickoff estimates: c3 BSDF node x469&ndash;728 / header top y108, Material Output x895&ndash;1050 / header top y108, BSDF out socket (727,143), Surface in socket (899,168), noodle mid ~(810,150); c21 BSDF out (287,112), Surface in (459,138), noodle mid ~(373,125). **c2:** 4 region labels (3D Viewport, Shader Editor, Properties Panel, Workspace tabs) on lifted `#1a1a1f`/0.9 bands, orange `#FF6B00` leaders+dots, fs 22, white text, middle-dot separators. **c3:** 5 component labels on lifted `#2a2a33`/0.92 bands (lighter than the `#1a1a1f` canvas so the pills read as intentional bands &mdash; FIX 2), orange leaders+dots, fs 16 small-frame floor, white text; the Material Output label is right-anchored (text-anchor=middle at x1106, band x943 w327, right edge 1270) so it no longer overflows the right edge (FIX 1). **c21:** 3 numbered orange step badges + leaders (1 &rarr; BSDF out socket ring, 2 &rarr; noodle-mid dot, 3 &rarr; Surface in socket ring), white text fs 16. Registered c2 / c3 / c21 in the overlay registry. INTEGRATION inline, dryRun &rarr; commit &rarr; `get_file_info` per insertion: #2 after the "...material workshop!..." `<p>` before `<h3>Opening the Shader Editor</h3>` (col-20 default depth) &rarr; HTML 213,007 B; #3 after the "Once you're in the Shader Editor..." `<p>` before the "Shader Editor Components" card (col-20) &rarr; 217,484 B; #21 inside the "Node Interaction Basics" card after its `</table>` before the card-closing `</div>` (col-24 deeper-indent, anchored through the unique "Preview Node Output" row) &rarr; 220,717 B. No em-dashes in any new alt/figcaption text (middle-dot `&#183;` separators). Scene state: UI-capture only, no AgX toggle / material / world edits touched this chat &mdash; no render-state restore needed. NOTE: prior header read "2 of 30" but #4 was already done in the prior chat (3/30 at this chat's start); header corrected to 6/30 here.

- **2026-06-02 &mdash; Phase 1 INTEGRATION COMPLETE (#1 + #29).** Verified `edit_file` + `get_file_info` + BlenderMCP at session start (`write_file` still did NOT surface across two tool_search passes; INTEGRATION needed only `edit_file`, so not blocking). Both PNGs confirmed on disk via `get_file_info` (#1 755,630 B 1920x600; #29 823,901 B 1920x1080) and copied Claude-side for pixel-verify. Registered `c1` + `c29` in the overlay registry. Built both overlays, verified 1:1 via cairosvg-composite-over-PNG on Claude's side BEFORE commit. **c1:** 5 neutral dark pills (`#1a1a1f` fill-opacity 0.9, white 22px text) centered under each sphere at u = 0.151 / 0.328 / 0.500 / 0.672 / 0.849, badge band y&approx;505-549 (sphere bottoms measured y&approx;480-499, bright ~248 floor below confirmed the dark-pill need). **c29:** title overlay "Real-World Material Analysis" (40px) in the top band; 8 two-line cell labels (28px name + 22px params, middle-dot separators) landing in the reserved dark note-bands (measured ~30 grey, orange rule at top edge y&approx;482-484). Single-line params overflowed the 456px cells, so split to two lines per cell. Used requirements-doc material names (Chrome / Brushed Aluminum / Glossy Plastic / Matte Rubber / Clear Glass / Frosted Glass / Polished Wood / Concrete), NOT render slugs. Both figures land at default 16/20 depth directly in `section id="what-are-materials"`: #1 after the "...how light should interact with a surface." `<p>`, before the Photography Studio Analogy `<blockquote>`; #29 after the "Think of it this way: a material is the recipe..." `<p>`, before `<h3>Real-World Material Examples</h3>`. NOTE: the #29 anchor `<p>` carries `<strong>` tags (`a <strong>material</strong> is the recipe...`) that the kickoff's plain-text quote omitted; matched the on-disk markup. Pre-existing em-dash in the blockquote ("lighting&mdash;a chrome sphere") preserved verbatim through the #1 anchor. `dryRun` &rarr; commit &rarr; `get_file_info` per insertion; HTML post-commit 203,002 B (after #1) then 208,228 B (after #29).
  - **EEVEE-Next raytracing build notes:** none recorded this chat &mdash; both Phase 1 PNGs were produced last session; this was an INTEGRATION-only chat. Any EEVEE-Next raytracing notes from PRODUCTION live in the prior PRODUCTION chat's record, not regenerated here.
- **2026-06-02 &mdash; PLANNING COMPLETE.** Read session.md + lesson_10_image_requirements.md + HTML head (8 sections confirmed: `#what-are-materials`, `#shader-editor-interface`, `#understanding-nodes`, `#principled-bsdf`, `#basic-material-properties`, `#creating-simple-materials`, `#material-preview`, `#project`) + IMAGE_STYLE_GUIDE.md v6. Verified write-side Filesystem tools (`edit_file` / `write_file`) loaded + BlenderMCP reachable (`execute_blender_code` + `get_viewport_screenshot`) at session start. Locked the 6 SVG / 24 PNG split, the 8-phase section-aligned plan (P4 + P6 PRODUCTION sub-chat splits; P6 INTEGRATION own chat), D1 / D2 / D3, the `sk`/`nd`/`dt`/`mb`/`ir` inline-SVG prefix registry, and the `c{N}` overlay namespace registry. session.md repointed to Phase 1 PRODUCTION. No production done in this chat.

---

## Locked rules carry forward

All UNCHANGED from L06&ndash;L09 (full list in `lesson_06_completion_handoff.md` + the L07/L08/L09 carry-forward blocks + IMAGE_STYLE_GUIDE.md v6 Locked decisions 1&ndash;21). Highlights that govern L10 specifically:

- WSL `\\wsl$\Ubuntu\` paths only (never `\\wsl.localhost\`).
- `Filesystem:edit_file` dryRun &rarr; commit &rarr; `Filesystem:get_file_info` verify per insertion; `Filesystem:write_file` for new files / full-doc overwrites (never `create_file`); `copy_file_user_to_claude` to refresh the working HTML copy before grep/sed + verify anchor uniqueness first.
- No new em-dashes (middle-dot `&#183;` entities; pre-existing em/en-dashes preserved verbatim through exact anchors).
- Brand palette governs all overlays/SVGs; inline-SVG conventions (strip root `color="#222"`, re-indent, collapse multi-line attrs, prune decorative comments, preserve role/aria/title/desc/id); figure indentation depth from parent container; PNG overlay = wrapper-div + overlay-SVG, viewBox = PNG pixels, no baked callouts.
- **Color-dependent render recipe (v6 LD17):** F12 `bpy.ops.render.render(write_still=True)` NOT `render.opengl`; `view_transform='Standard'` + `look='None'` on a 0.05 world; locked marker values; RESTORE AgX + base values at chat close. Applies to #1, #8, #13, #15, #19 and any color-carrying sphere render.
- BlenderMCP lowercase `blender:execute_blender_code`; clean-scene-manually via `bpy.data.objects.remove`; BLENDER_EEVEE; NEVER `read_factory_settings`; inline all render helpers per call (namespaces do not persist).
- Capture-path routing (v6 LD20): synthetic-cage/overlay-over-render for render-API-unreachable overlays; full-window local-tempdir `screen.screenshot` + deterministic area crop for docked-panel UI chrome; Win+Shift+S overlay-freeze for transient popovers / pie menus (#22).
- Pixel-verify every PNG overlay before commit (v6 LD19): Pillow scan + overlay-over-PNG composite at exact viewBox coords; cairosvg 1:1 check on Claude's side, PIL substitute bridge-side.
- NO baked labels; pre-plan sub-chat splits for &ge;5-figure phases + INTEGRATION-own-chat for &ge;6; plan-lock before build (propose spec + anchors in writing first).
- **Scene state between PRODUCTION chats = always clean base, never inherited mid-flight.** Every PRODUCTION chat does RESTORE at close (removes its `_L10P6x_WORK` collection, purges orphans, un-hides base objects, AgX/EEVEE/512&times;512/world 0.88). So the next chat's expected `get_scene_info` is: EEVEE, 512&times;512, AgX/None/0/1, world 0.88 str 1.0, the 15 base objects (Floor/Cam/KeyL/FillL/RimL/Soft/AccA/AccB/Backdrop + the 6 `M_*` source spheres) present & render-visible, NO `_WORK` collection. The `_WORK`-collection / `hide_render` / isolated-build approach is a **technique each chat re-creates from scratch**, NOT state it inherits &mdash; reading a prior Status entry's build description is not a signal that that scene still exists. If a chat instead finds a leftover `_WORK` collection or hidden base objects, the prior RESTORE failed &mdash; STOP and report, do not build over it. All P6 figures (incl. #14) are produced from scratch in their stated sub-chat; none is ever "mid-production" carried across chats.
- Warn Ray to start a new chat before context tightens rather than letting automatic compaction happen.
