# Lesson 08: Modifiers Introduction · Integration Progress

**Lesson HTML:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_08_modifiers_introduction.html` (280,202 B after browser-QA DEFECT B fix; was 280,049 B at Phase 9 INTEGRATION close)
**Current state:** L08 image work COMPLETE · 40 of 40 produced · 40 of 40 integrated · **browser-QA DONE/PASS 2026-06-14**
**Last updated:** 2026-06-14 (browser-QA DONE/PASS · DEFECT A done/verified prior, DEFECT B = 5 subdivision overlays #18/#19/#20/#21/#22 fixed [img `margin:0` + figcaption moved outside wrapper], 280,167→280,202 B, em-dash 51 held; harness-verified 0.00 box-delta all combos; see `lesson_08_browser_qa_progress.md` + QA Status bullet)
**Next chat:** L08 image pipeline COMPLETE + browser-QA DONE · no further L08 work. Standing queue: L11 1024-width sweep (only remaining item)

---

## Requirements-doc verification against live HTML (2026-05-28)

`lesson_08_image_requirements.md` was written 2025-11-11 in an earlier analysis chat. Re-verified against the live `lesson_08_modifiers_introduction.html` (129,247 B) at kickoff:

- **Section IDs:** live HTML has 9 sections: `#what-are-modifiers`, `#modifier-workflow`, `#array-modifier`, `#mirror-modifier`, `#subdivision-surface`, `#bevel-modifier`, `#modifier-stacking`, `#project`, `#summary`. The requirements doc's TOC-style placement list names 8 but also references `#summary` (for #39 / #40) in its Image Placement section, so the full 9-section set reconciles. All confirmed present.
- **Anchor cards:** every placement anchor named in the requirements doc resolves to a real card / heading once heading-suffix differences are accounted for (e.g. "The Clipping Option" is live as "The Clipping Option (Most Important!)"; "Understanding Subdivision Levels" table present; "When to Apply Modifiers" table present in `#modifier-stacking`; "Professional Modifier Workflow" checklist present in `#summary`). No blocking anchor failures. Exact 3-5 line `oldText` windows built at each INTEGRATION plan-lock per locked cadence.
- **Figure count:** 40 confirmed (#1-#40); naming-convention table lists 40 filenames; all 9 sections covered.
- **Figure reuse:** #6 (Subdivision Surface Quick Demo) is placed in BOTH `#modifier-workflow` ("After Quick Practice Exercise card") and `#subdivision-surface` ("After Your First Subdivision card · already used, or reference it"). Decision PENDING (see below): produce once in Phase 2, reference twice.

---

## DECISIONS PENDING (flagged at kickoff · not yet locked · do not treat as final)

1. **#40 Troubleshooting Guide · SVG vs PNG.** Requirements doc recommends 5 SVGs (#1, #29, #31, #38, #39) and lists #40 as PNG "Infographic/Table". Proposed flip: #40 -> hand-coded SVG `tg` following the L06 #44 `sr` structured-reference precedent (it is a text-heavy table/infographic, the same class the course has consistently inlined as SVG). If flipped: split = 6 SVG / 34 PNG. If held: split = 5 SVG / 35 PNG (#40 stays a PNG composite/infographic capture, overlay `c40` reserved). NOT due until Phase 9; left open.
2. **Phase plan (9 phases, section-aligned).** Approved as-proposed for execution start (Ray: "start phase 1"). Phases 2-9 specifics can still be adjusted as each comes up.
3. **#6 dual-reference mechanics.** Proposed: produce once in Phase 2, integrate in `#modifier-workflow`, then at Phase 5 INTEGRATION add a second `<img>` reference to the same file in `#subdivision-surface`. NOT due until Phase 5; left open.

---

## SVG-vs-PNG split (FINAL · 6 SVG / 34 PNG · DECISION 1 resolved 2026-05-29 = #40 flipped to SVG `tg`)

Locked SVGs (6):

| # | File | Type | Prefix | Rationale |
|---|---|---|---|---|
| 1 | `lesson_08_01_modifier_concept_diagram.svg` | SVG | `mc` | Conceptual 3-stage non-destructive flow with toggle icon; locked diagram precedent |
| 29 | `lesson_08_29_modifier_order_diagram.svg` | SVG | `mo` | Two-path order-impact flowchart; vector for crisp arrows + text |
| 31 | `lesson_08_31_common_stack_patterns.svg` | SVG | `sp` | 4-pattern infographic; structured-layout, scalable |
| 38 | `lesson_08_38_applying_warning.svg` | SVG | `aw` | Warning flow diagram with icons; sharp text/icons at all sizes |
| 39 | `lesson_08_39_workflow_flowchart.svg` | SVG | `wf` | Decision-tree flowchart; multi-branch, editable |
| 40 | `lesson_08_40_troubleshooting_guide.svg` | SVG | `tg` | Text-heavy problem/solution reference table; structured-reference class (L06 #44 `sr` precedent), crisp selectable text + accessibility |

DECISION 1 RESOLVED 2026-05-29: #40 flipped to hand-coded SVG `tg`. Final split = 6 SVG / 34 PNG. Other 34 figures: PNG.

---

## Phase plan (locked as-proposed 2026-05-28 · section-aligned · 9 phases)

Counts: 2 / 5 / 5 / 5 / 5 / 5 / 6 / 5 / 2. Sub-chat split for any phase with 5+ figures; INTEGRATION plan-lock as own chat for any phase with 6+ figures (per L06/L07 cadence).

| Phase | Section | Figures (file #) | Count | Sub-chat split (>=5) | INT plan-lock own chat (>=6) |
|---|---|---|---|---|---|
| 1 | `#what-are-modifiers` | #1 SVG `mc` · #2 | 2 | no | no |
| 2 | `#modifier-workflow` | #3 · #4 · #5 · #6 · #7 | 5 | yes | no |
| 3 | `#array-modifier` | #8 · #9 · #10 · #11 · #12 | 5 | yes | no |
| 4 | `#mirror-modifier` | #13 · #14 · #15 · #16 · #17 | 5 | yes | no |
| 5 | `#subdivision-surface` | #18 · #19 · #20 · #21 · #22 (+ #6 reference) | 5 | yes | no |
| 6 | `#bevel-modifier` | #23 · #24 · #25 · #26 · #27 | 5 | yes | no |
| 7 | `#modifier-stacking` | #28 · #29 SVG `mo` · #30 · #31 SVG `sp` · #32 · #38 SVG `aw` | 6 | yes | yes |
| 8 | `#project` | #33 · #34 · #35 · #36 · #37 | 5 | yes | no |
| 9 | `#summary` | #39 SVG `wf` · #40 (SVG `tg` or PNG · DECISION 1) | 2 | no | no |

Per-figure section placement comes from the requirements-doc "Image Placement Suggestions in HTML" section. #38 placed in Phase 7 (`#modifier-stacking`) per the doc's "After When to Apply Modifiers table" placement.

---

## ID prefix registry (provisional 2026-05-28)

**SVGs (6 locked):** `mc` (#1) · `mo` (#29) · `sp` (#31) · `aw` (#38) · `wf` (#39) · `tg` (#40)

**PNG overlay namespaces reserved (conservative · any PNG the requirements doc marks "annotated" / "labels" / "callout" / multi-panel with panel labels):** `c2` · `c3` · `c5` · `c8` · `c9` · `c10` · `c11` · `c13` · `c14` · `c15` · `c16` · `c17` · `c18` · `c19` · `c20` · `c21` · `c22` · `c23` · `c24` · `c25` · `c26` · `c27` · `c28` · `c30` · `c32` · `c34` · `c35` · `c36` · `c37` (NOTE: `c40` retired · #40 is now SVG `tg`, no PNG overlay namespace)

Exact overlay variant per figure (full-callout · multi-callout · composite-overlay · decorative-shape-only) decided at each INTEGRATION plan-lock per L06/L07 cadence.

**PNGs without overlays (provisional):** #4 · #6 · #7 · #12 · #33

No collisions with prior lessons since each lesson HTML is its own page scope.

---

## Placement table

Four-state legend: `not produced; not integrated` · `produced; not integrated` · `produced; handoff_written; not integrated` · `integrated`

| # | File | Phase | Section | Overlay | Status |
|---|---|---|---|---|---|
| 1 | `lesson_08_01_modifier_concept_diagram.svg` | 1 | `#what-are-modifiers` | SVG `mc` | integrated |
| 2 | `lesson_08_02_modifier_transformations.png` | 1 | `#what-are-modifiers` | `c2` | integrated |
| 3 | `lesson_08_03_properties_panel_location.png` | 2 | `#modifier-workflow` | `c3` | integrated |
| 4 | `lesson_08_04_add_modifier_menu.png` | 2 | `#modifier-workflow` | — | integrated |
| 5 | `lesson_08_05_panel_icons_explained.png` | 2 | `#modifier-workflow` | `c5` | integrated |
| 6 | `lesson_08_06_subdivision_quick_demo.png` | 2 | `#modifier-workflow` (+ `#subdivision-surface` 2nd ref, plain) | `c6` | integrated |
| 7 | `lesson_08_07_edit_mode_preview.png` | 2 | `#modifier-workflow` | `c7` synthetic | integrated |
| 8 | `lesson_08_08_array_modifier_panel.png` | 3 | `#array-modifier` | `c8` | integrated |
| 9 | `lesson_08_09_array_count_progression.png` | 3 | `#array-modifier` | `c9` | integrated |
| 10 | `lesson_08_10_array_offset_spacing.png` | 3 | `#array-modifier` | `c10` | integrated |
| 11 | `lesson_08_11_array_direction_examples.png` | 3 | `#array-modifier` | `c11` (axis-tinted) | integrated |
| 12 | `lesson_08_12_array_grid_pattern.png` | 3 | `#array-modifier` | — | integrated |
| 13 | `lesson_08_13_mirror_modifier_setup.png` | 4 | `#mirror-modifier` | `c13` | integrated |
| 14 | `lesson_08_14_mirror_modifier_panel.png` | 4 | `#mirror-modifier` | `c14` | integrated |
| 15 | `lesson_08_15_clipping_demonstration.png` | 4 | `#mirror-modifier` | `c15 (warning-vs-positive)` | integrated |
| 16 | `lesson_08_16_multi_axis_mirror.png` | 4 | `#mirror-modifier` | `c16` | integrated |
| 17 | `lesson_08_17_mirror_realtime_editing.png` | 4 | `#mirror-modifier` | `c17 synthetic` | integrated |
| 18 | `lesson_08_18_subdivision_levels.png` | 5 | `#subdivision-surface` | `c18` | integrated |
| 19 | `lesson_08_19_subdivision_viewport_render.png` | 5 | `#subdivision-surface` | `c19` | integrated |
| 20 | `lesson_08_20_edge_loop_sharpness.png` | 5 | `#subdivision-surface` | `c20 synthetic` | integrated |
| 21 | `lesson_08_21_edge_crease_tool.png` | 5 | `#subdivision-surface` | `c21 synthetic` | integrated |
| 22 | `lesson_08_22_subdivision_smooth_shading.png` | 5 | `#subdivision-surface` | `c22 (warning-vs-positive)` | integrated |
| 23 | `lesson_08_23_bevel_modifier_panel.png` | 6 | `#bevel-modifier` | `c23` | integrated |
| 24 | `lesson_08_24_bevel_amount_comparison.png` | 6 | `#bevel-modifier` | `c24` | integrated |
| 25 | `lesson_08_25_bevel_segments_comparison.png` | 6 | `#bevel-modifier` | `c25` | integrated |
| 26 | `lesson_08_26_bevel_angle_limiting.png` | 6 | `#bevel-modifier` | `c26 (actual-swept 50/75/85)` | integrated |
| 27 | `lesson_08_27_bevel_subdivision_combo.png` | 6 | `#bevel-modifier` | `c27` | integrated |
| 28 | `lesson_08_28_modifier_stack_panel.png` | 7 | `#modifier-stacking` | `c28` | integrated |
| 29 | `lesson_08_29_modifier_order_diagram.svg` | 7 | `#modifier-stacking` | SVG `mo` | integrated |
| 30 | `lesson_08_30_modifier_order_comparison.png` | 7 | `#modifier-stacking` | `c30` | integrated |
| 31 | `lesson_08_31_common_stack_patterns.svg` | 7 | `#modifier-stacking` | SVG `sp` | integrated |
| 32 | `lesson_08_32_stack_management_icons.png` | 7 | `#modifier-stacking` | `c32` | integrated |
| 38 | `lesson_08_38_applying_warning.svg` | 7 | `#modifier-stacking` | SVG `aw` | integrated |
| 33 | `lesson_08_33_project_base_post.png` | 8 | `#project` | — | integrated |
| 34 | `lesson_08_34_project_phase_progression.png` | 8 | `#project` | `c34` | integrated |
| 35 | `lesson_08_35_project_modifier_stack.png` | 8 | `#project` | `c35` | integrated |
| 36 | `lesson_08_36_fence_variations.png` | 8 | `#project` | `c36` | integrated |
| 37 | `lesson_08_37_stack_order_experiments.png` | 8 | `#project` | `c37` | integrated |
| 39 | `lesson_08_39_workflow_flowchart.svg` | 9 | `#summary` | SVG `wf` | integrated |
| 40 | `lesson_08_40_troubleshooting_guide.svg` | 9 | `#summary` | SVG `tg` | integrated |

---

## Locked rules

Full discussion in `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_06_completion_handoff.md`; carry-forward block in `lesson_07_integration_progress.md`. All carry forward into L08 unchanged. Highlights:

**Path and edit conventions:**
- WSL `\\wsl$\Ubuntu\` paths only (never `\\wsl.localhost\`)
- `Filesystem:edit_file` `dryRun=true` first -> commit `dryRun=false` -> verify with `Filesystem:get_file_info` per edit
- `Filesystem:write_file` for new files and small full-doc overwrites; NEVER Claude's built-in `create_file`
- `tool_search` for deferred tools; never inherit "tool X is unavailable" assumptions from a compaction summary
- No new em-dashes anywhere in new content; middle-dot (`·`) for separation; pre-existing en-dashes / em-dashes in source HTML preserved verbatim through exact-match edit anchors; structural em-dash after a date in Status bullets grandfathered

**BlenderMCP rules (Blender 5.1.1):** no `read_factory_settings(use_empty=False)` (clean scene manually via `bpy.data.objects.remove`); BLENDER_EEVEE_NEXT -> BLENDER_EEVEE fallback; op-name corrections (`mesh.inset`, `mesh.delete(type='FACE')` singular, `bmesh.ops.bisect_plane`, `bmesh.ops.subdivide_edges(cuts=N)` equal-spaced only); the 10 L06 P5/P6 discoveries; the 3 L07 viewport-cache discoveries (P1 `DRAW_WIN_SWAP` matrix-sync after `region_3d.view_perspective` enum change; P2 EDIT-OBJECT-EDIT toggle for draw-cache flush across vert-position-only bmesh edits; P3 BMFace `.normal` cache observation-only); `bpy.ops.screen.screenshot` silent-truncates to 0 bytes on WSL UNC paths (capture to local Windows tempdir, Pillow-relay to WSL); Pillow 12.2.0 at `C:\Users\pract\AppData\Roaming\Blender Foundation\Blender\5.1\scripts\addons\modules\PIL`; modal-status-bar-as-truth; Win+Shift+S overlay-freezes-screen popover capture path; save snips direct to WSL `images/`

**Image patterns:** inline-stacked PNG overlay pattern with four variants (full-callout · multi-callout count-agnostic · composite-overlay centered-bottom rect+text geometry-reusable across same-dimension composites · decorative-shape-only); warning-vs-positive colorway split (`#E63946`+`#ffffff` warning · `#4ADE80`+`#0f1a14` positive; composite-overlay defaults positive-green); mixed-colorway composite-overlay precedent (L07 #25, warning + positive panels in one SVG); composite-overlay font-size formula `fs = round(22 × viewBox_w / 1920)`; inline SVG re-indent + strip `color="#222"` from root + add `preserveAspectRatio="xMidYMid meet"` when inlining (standalone keeps `color="#222"` for `<img>` fallback); deeper-indent variant for figures inside cards; per-callout id naming `c{N}-highlight[-K]` / `-leader[-K]` / `-marker[-K]` / `-badge[-K]`; 3-5 line `oldText` anchors (extend if duplicate matches); bottom-up execution order within each sub-chat

**L07 indent-depth-derivation rule:** depths derive from each file's section-content depth, not the L06 numbers. L08 section-content depth to be confirmed at Phase 1 INTEGRATION (L07 was col 20: figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24).

**Workflow:** cross-sub-chat composite pattern; resume-chat precedent; MCP 4-min hang recovery (`get_file_info` size check before retry); Pillow crop+resize pre-integration for oversize manual captures; pre-plan sub-chat splits for 5+ figure phases; pre-split INTEGRATION planning for 6+ figure phases; warn Ray before context tightens; propose plan + anchors + grouping before integration, lock in writing first.

**Canva availability (confirmed 2026-05-28):** Canva MCP connector is connected and available. Usable as a raster-source fallback for infographic-class figures (#31, #40, #38) that would otherwise be captured/composited. NOT a substitute for hand-coded inline SVG diagrams: every course SVG to date is hand-coded to the house style guide (brand palette, `color="#222"` strip-on-inline, middle-dot separators, structural-comment preservation), and Canva output cannot match those inline conventions. Per-figure Canva-vs-hand-code decision deferred to each INTEGRATION plan-lock.

**Brand palette (unchanged from L05 close):** `#FF6B00` orange · `#4A9EFF` blue · `#B47EE8` purple · `#FF7EA8` pink · `#4ADE80` green · `#F5C242` yellow · `#E63946` red · `#0f1a14` near-black · `#ffffff` white. NOTE: the requirements doc proposes a divergent palette (purple `#667eea`-`#764ba2`, yellow `#FFEB3B` callouts, green `#4CAF50`, red `#F44336`, dark-grey `#2b2b2b` viewport). The course house palette governs all overlays and hand-coded SVGs; the requirements-doc palette is treated as loose guidance for AI-prompt raster content only, reconciled to the house palette at each plan-lock.

**Style guide canonical:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\images\IMAGE_STYLE_GUIDE.md` v5 (49,085 B). v6 revision deferred; not blocking L08.

---

## Status

- **2026-05-28** · L08 kickoff. Read `session.md`, `lesson_06_completion_handoff.md`, `lesson_07_integration_progress.md` (full pipeline being mirrored), `lesson_08_image_requirements.md`, and the live `lesson_08_modifiers_introduction.html` (129,247 B). Verified requirements doc against live HTML: 9 section IDs confirmed (8 in TOC + `#summary`); all placement anchors resolve; 40 figures confirmed; #6 dual-reference noted. Phase plan locked as-proposed (9 phases, 2/5/5/5/5/5/6/5/2, section-aligned). SVG split: 5 locked from requirements doc (`mc`/`mo`/`sp`/`aw`/`wf`), #40 `tg` flip PENDING (DECISION 1). Prefix registry provisional. 3 decisions flagged PENDING (not due until their phases). Canva availability confirmed (raster-source fallback for infographics; not for inline SVG diagrams). This tracking doc written. Next: Phase 1 PRODUCTION (#1 SVG `mc` hand-coded; #2 3-panel transformation composite via BlenderMCP). **Scene-state note at kickoff:** BlenderMCP bridge live but scene NOT clean · holds a prior session's `WizardHat_*` assembly + L07 `Mug` at (3,0,0) + key/rim lights + camera (19 objects / 6 materials). Must clear manually via `bpy.data.objects.remove` (never `read_factory_settings`) before building the #2 transformation scene.

- **2026-05-28** · Phase 1 PRODUCTION complete. 2 of 2 figures produced for `#what-are-modifiers`. **#1** `lesson_08_01_modifier_concept_diagram.svg` (5,614 B; SVG `mc`; hand-coded, canvas 800x560 per locked L07 SVG standard not the requirements-doc 1400x600 raster spec; 3-stage non-destructive flow Original mesh -> Modifier applied -> Editable result with brand-orange arrows; requirements-doc divergent purple-gradient palette reconciled to house palette: original neutral grey, modifier stage purple `#B47EE8` filter layer + filter glyph, result green `#4ADE80` rounded form, accents/arrows orange `#FF6B00`; bottom toggle+slider banner with `#4ADE80` ON-switch glyph + `#B47EE8` slider glyph showing toggle-off-anytime + adjust-settings-anytime; standalone keeps `color="#222"` on root for `<img>` fallback, stripped at INTEGRATION; one in-production fix: right banner text clipped past x=800, slider group + label shifted left via dry-run -> commit -> verify, re-rendered clean). **#2** `lesson_08_02_modifier_transformations.png` (429,876 B; 3844x720; 3-panel composite at shared 3/4 FRONT-persp ORBITLEFT 32 + ORBITUP 16; Panel 1 Array = single post -> base post + Array count 6 rel-offset X 1.6 (6-post row); Panel 2 Mirror = solid half-block -> full solid cube (half-to-whole); Panel 3 Subdivision = blocky cube -> Subdivision levels 3 + shade_smooth sphere-like form; per-panel 1280x720; Pillow composite `#FF6B00` 2-px dividers; light-grey world 0.88, SOLID + STUDIO; engine BLENDER_EEVEE; NO baked labels per no-bake-callouts rule · before->after arrows + panel labels (Array: 1->many / Mirror: half->whole / Subdivision: blocky->smooth) come from reserved `c2` overlay at INTEGRATION). **#2 production iterations (first-pass marginal-read rejection):** Panel 2 v1 (delete-half + Mirror) read as two thin slabs (seam visible, not legible); v2 (solid half-box + Mirror+merge) still showed center seam splitting the whole; v3 locked = solid half-block beside a clean single solid cube, unambiguous at thumbnail · Mirror mechanism itself is taught by Phase-4 figures #13/#16 so this at-a-glance hero prioritizes result clarity. Framing tightened across all 3 panels (view_distance 12/7.5/7.5) after v1 read too small. **Scene clean:** dirty kickoff scene (WizardHat assembly + L07 Mug, 19 objs / 6 mats) cleared via `bpy.data.objects.remove` + orphan purge (0/0 confirmed) before building. **No new L08 P1 PRODUCTION discoveries:** reused locked L07 P1 `DRAW_WIN_SWAP` matrix-sync at every view change + local-Windows-tempdir -> Pillow-relay-to-WSL path; all temp panels cleaned at composite close. Both figures verified on disk via `get_file_info`. Placement rows #1 / #2 flipped to `produced; not integrated`. Next chat: Phase 1 INTEGRATION (`#what-are-modifiers`; 2 figures · #1 SVG `mc` + #2 PNG + c2 overlay; single sub-chat; derive L08 section-content indent depth from live HTML at plan-time).

- **2026-05-28** · Phase 1 INTEGRATION closed. 2 of 2 Phase 1 figures integrated into `lesson_08_modifiers_introduction.html` (`#what-are-modifiers`). HTML size 129,247 B to 140,526 B (+11,279 B across 2 figures). **L08 section-content indent depth derived from live HTML:** `<section>` col 16 / section content (h2 / p / div.card) col 20 · IDENTICAL to L07, so the L07 column scheme carries verbatim (figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24). Per-figure deltas (bottom-up execution order #2 then #1): **#2** PNG + c2 composite-overlay-style panel badges between the "Let's look at what modifiers can do in practice:" para and the `<div class="mermaid">` (after the "Real-World Modifier Examples" `<h3>`); viewBox 3844×720; fs 44 per `fs = round(22 × 3844 / 1920)`; positive-green default `#4ADE80` rect + `#0f1a14` text; c12-precedent rect geometry (rect 440×100 at panel_center−220 / y=580 / text y=630 / centers 640 · 1922 · 3204 matching the L07 #6 720-tall precedent); **variant = composite-overlay-style panel badges** (chosen over multi-callout: the figure is a 3-panel same-dimension composite, the locked composite-overlay variant is the panel-label precedent); 3 before→after arrows added as decorative orange `#FF6B00` shapes inside the c2 group, one per panel at mid-height y=360 (IDs `c2-arrow-1/2/3`); panel-label badges (IDs `c2-badge-1/2/3`) "Array: 1 → many" / "Mirror: half → whole" / "Subdivision: blocky → smooth" (+3,481 B). **#1** inline SVG `mc` between the "Before modifiers existed…" para (`#why-modifiers-game-changing` body) and the "What Modifiers Give You" `<div class="card">`; `color="#222"` stripped from root + `preserveAspectRatio="xMidYMid meet"` added; re-indented to `<figure>` col 20 / `<svg>` col 24 / svg children col 28 / nested `<g>` children col 32 / `<figcaption>` col 24; all 8 structural comments preserved verbatim; viewBox 800×560 (+7,798 B). Bottom-up order #2 then #1 held; no anchor shifts. Dry-run-then-commit-then-verify per figure (2 verify checkpoints via `get_file_info`: 132,728 → 140,526 B). Both placement-table rows flipped to `integrated`. **No new L08 P1 INTEGRATION discoveries:** reused the locked inline-stacked overlay pattern, the SVG inline conventions (strip `color="#222"` / add `preserveAspectRatio` / re-indent / preserve comments), the composite-overlay font-size formula, the c12-precedent rect geometry, the positive-green-default colorway, and the indent-depth-derivation rule (L08 = col 20, same as L07) without surfacing new mechanisms. **Standing QA flag (estimated-position visual-proof):** the 3 c2 panel badges use deterministic panel-center-derived composite-overlay geometry (no pixel-scan estimate) so add NO estimated-position flag; the 3 c2 before→after arrows are mid-panel decorative placements (y=360, well clear of the rendered before/after subjects per #2 PRODUCTION framing) · flagged for browser-QA plausibility check only. **3 PENDING decisions carried, none due in Phase 1:** #40 SVG-vs-PNG flip; #6 dual-reference mechanics; Phase 2-9 plan adjustments · left open. Next chat: Phase 2 PRODUCTION (`#modifier-workflow`; 5 figures · #3 / #4 / #5 / #6 / #7; sub-chat split required per ≥5 threshold).

- **2026-05-28** · Phase 2 PRODUCTION started, partial. Sub-chat split locked in writing before producing (capture-method precedent): **Sub-chat A** = UI-chrome captures #3 properties-panel-location / #4 add-modifier-menu / #5 panel-icons-explained; **Sub-chat B** = viewport renders #6 subdivision-quick-demo / #7 edit-mode-preview. Bottom-up within each (A: #5→#4→#3; B: #7→#6). **Scene-state at kickoff (verified live):** NOT the documented #2 transformation build · held `L08Key`+`L08Fill` lights + leftover meshes `p3_blocky`@(−2.4,0,1) / `p3_smooth`@(2.4,0,1) (1 mat) from a prior partial attempt; cleared all 4 objects via `bpy.data.objects.remove` + orphan purge (0/0; 24 data-blocks deleted) before building. **#6 PRODUCED** `lesson_08_06_subdivision_quick_demo.png` (622,916 B; 1920×900 per spec; blocky cube @(−2.4,0,1) Level-0 flat beside subsurf cube @(2.4,0,1) Subdivision levels 3 + shade_smooth sphere-like; shared mid-grey mat `L08DemoGrey` base 0.55/0.55/0.58 rough 0.45; light-grey world 0.88; key 600/area6 @(−4,−5,7) + fill 250/area8 @(5,−3,4); cam @(0,−11,5.2) lens 50 track-to (0,0,1); engine BLENDER_EEVEE (EEVEE_NEXT unavailable, fell back per locked rule); NO baked labels per no-bake rule; #6 has NO overlay namespace — any before→after arrow/label is an INTEGRATION overlay decision, not baked; rendered via `render()` to local Windows tempdir → Pillow optimize-save relay to WSL `images/`; QA-viewed: blocky-vs-smooth reads unambiguous at thumbnail, high contrast on light bg). Row #6 flipped to `produced; not integrated`. **#7 DEFERRED (blocker, NOT produced).** **#3/#4/#5 NOT started.**

  **BLOCKER — edit-mode/UI overlay does not composite into `bpy.ops.screen.screenshot` over the MCP bridge.** #7 needs the orange original-cube edit-cage wireframe + selected vertex visible over the ghosted subsurf result. The subsurf scene geometry is correct, but across FOUR capture variants the orange edit-mesh edge/vertex overlay never appeared in the captured framebuffer — only the solid/ghosted surface did. Variants tried, all failed to show the cage: (1) `show_in_editmode=True` + `show_on_cage=False`, plain SOLID; (2) + `overlay.show_wireframes` toggling (note: `show_edit_mesh_edges` / `show_edit_mesh_*` are NOT valid `View3DOverlay` attrs in Blender 5.1 — `AttributeError`; valid attrs are limited to `show_wireframes` / `wireframe_opacity` / `show_edge_*`); (3) `show_xray=True` + `xray_alpha=0.35` (surface ghosted correctly but still no orange edges); (4) `show_on_cage=True` + EDIT→OBJECT→EDIT cache-flush (L07 P2) + DRAW_WIN_SWAP (L07 P1). Conclusion: `screen.screenshot` over this bridge captures the window framebuffer WITHOUT the edit-mode mesh-overlay pass, so live edit-cage capture is not viable as-attempted. This blocks #7 AND likely #3/#4/#5 (all four are UI/overlay-chrome captures dependent on the same overlay-compositing path). **NEW L08 P2 PRODUCTION discovery (capture-path limit):** MCP-bridge `screen.screenshot` ⇒ no edit-mode/overlay pass in output; do NOT assume Win+Shift+S/screenshot will carry overlays. **Two candidate resolutions for next chat to choose at kickoff (decision OPEN, not locked):** (a) **synthetic composite** — `render()` the ghosted surface (proven working via #6) then composite an orange original-cube wireframe over it deterministically in Pillow/SVG at INTEGRATION (consistent with no-bake-callouts: cage becomes an overlay element, not baked); for #3/#4/#5 the UI chrome would come from a real Blender-window grab via the Win+Shift+S overlay-freeze popover path (NOT `screen.screenshot`) → local Windows tempdir → Pillow-relay → crop+resize. (b) **Win+Shift+S overlay-freeze popover capture** for all four (the locked L06 path for UI chrome) — verify it carries the edit-mode overlay before committing to it for #7. **Region-crop reference (this session's window):** window 2849×1538; VIEW_3D WINDOW region x4 y161 w2333 h1317 ⇒ PIL crop box (4, 60, 2337, 1377); window geometry will differ next session, re-derive. **Overlay reservations unchanged:** #3 `c3` · #5 `c5` · #4/#6/#7 none. **#6 dual-reference (DECISION 3) still PENDING** — single file produced this phase; second-reference mechanic NOT resolved. **3 PENDING decisions carried, none due in Phase 2:** #40 SVG-vs-PNG; #6 dual-reference; Phase 2-9 adjustments · left open. **Scene at close:** NOT cleaned — holds `L08Cam` + `L08Key` + `L08Fill` + `demo_blocky` + `demo_smooth` (hidden) + `editpreview_cube` (subsurf, in EDIT mode) + `L08DemoGrey` mat; next chat MUST verify live scene state at kickoff and clear via `bpy.data.objects.remove` (never `read_factory_settings`) before building. No `.blend` checkpoint. Next chat: Phase 2 PRODUCTION continuation — resolve overlay-capture path FIRST, then produce #7 (Sub-chat B) + #3/#4/#5 (Sub-chat A); on close flip rows to `produced; not integrated`, then Phase 2 INTEGRATION (single sub-chat per <6, all 5 figures in `#modifier-workflow`).

- **2026-05-28** · Phase 2 PRODUCTION continuation closed. Phase 2 PRODUCTION now COMPLETE (5 of 5 `#modifier-workflow` figures produced). **OVERLAY-CAPTURE BLOCKER RESOLVED (locked in writing before producing):** path (a) synthetic-composite for #7 + path (b) Win+Shift+S overlay-freeze for #3/#4/#5. Rationale: the bridge `screen.screenshot` drops the edit-mode/overlay pass (proven last chat, 4 failed variants) and is unreliable for UI chrome too, so (i) #7's orange edit-cage becomes a deterministic INTEGRATION overlay over a proven-working ghosted render (no-bake-consistent, self-verifiable), while (ii) #3/#4/#5 are real UI chrome (dropdown menu / properties panel / panel-header icons) that cannot be synthesized and must be an OS-level snip that carries menu/panel overlays. Capture-method split held: Sub-chat B = #7 (synthetic render path); Sub-chat A = #5→#4→#3 (Win+Shift+S; Ray snipped, Claude located in Downloads + cropped via Pillow at the locked addon-modules path + verified). **Scene verified at kickoff** = documented close-state exactly (6 objs: demo_blocky + demo_smooth hidden, editpreview_cube subsurf EDIT, L08Cam + L08Key + L08Fill, L08DemoGrey mat, BLENDER_EEVEE); cleared all via `bpy.data.objects.remove` + datablock sweep (0/0/0/0; "no orphaned data-blocks to purge") before building. **#7 PRODUCED** `lesson_08_07_edit_mode_preview.png` (366,919 B; 1920×1080; **ghosted subsurf SURFACE ONLY** · path-a synthetic composite, the orange original-cube edit-cage + selected-vertex marker is a RESERVED INTEGRATION OVERLAY, NOT baked). Built fresh: cube size 2 @ (0,0,1), Subdivision modifier levels 2 (render 2), L08DemoGrey base (0.55,0.55,0.58) rough 0.45, shade_smooth; key 600/area6 @ (-4,-5,7) + fill 250/area8 @ (5,-3,4); cam @ (7.5,-9.0,5.8) lens 50 track-to (0,0,1) [angled 3/4 framing, NOT the (0,-11,5.2) front used for #6]; world 0.88 grey; BLENDER_EEVEE; render() to local Windows tempdir → Pillow `Image.blend(img, 0.88-grey, 0.45)` ghost treatment → optimize-save relay to WSL `images/`. **INTEGRATION cage data (deterministic, captured this chat for the synthetic overlay):** the 8 ORIGINAL cube corners (local ±1 in x/y, z∈{0,2}) project to PIXEL (top-left origin, 1920×1080) as — (-1,-1,-1)→(669,741); (-1,-1,1)→(651,348); (-1,1,-1)→(936,613); (-1,1,1)→(935,254); (1,-1,-1)→(989,872); (1,-1,1)→(991,445); (1,1,-1)→(1245,718); (1,1,1)→(1263,331). Cube spans px x 651–1263 / y 254–872. SUGGESTED "selected vertex" marker for INTEGRATION = front-top corner local (1,-1,1) → px (991,445). INTEGRATION draws the orange cage as a `c7`-class synthetic overlay (no PNG namespace was reserved for #7; it becomes a synthetic-composite figure, treat like a reserved overlay). **#5 PRODUCED** `lesson_08_05_panel_icons_explained.png` (69,498 B; 574×464; close-up of the Subdivision modifier panel HEADER icon row — collapse caret / type icon / On-Cage + Edit-mode + Realtime + Render visibility toggles / dropdown / X delete / drag-dots, plus Catmull-Clark/Simple + Levels rows for context; 2× LANCZOS upscale of the cropped header from a full-screen snip; `c5` overlay reserved for INTEGRATION icon labels). **#4 PRODUCED** `lesson_08_04_add_modifier_menu.png` (114,229 B; 802×1071; Add Modifier dropdown with Generate highlighted + the Generate submenu expanded showing Array / Bevel / Mirror / Subdivision Surface etc.; menu opened live by Ray since a transient popup can't be script-held across a snip; cropped to the two menu columns; minor residual Properties-tab-column + viewport sliver retained as context per requirements-doc note; no overlay). **#3 PRODUCED** `lesson_08_03_properties_panel_location.png` (148,108 B; 600×1567; wider locate-the-panel shot showing the Properties editor tab column with the wrench Modifier tab ACTIVE/highlighted blue, Subdivision panel populated, Outliner above + viewport strip right for orientation; OS title-bar trimmed; `c3` overlay reserved for INTEGRATION wrench-highlight + arrow). **NEW L08 P2 PRODUCTION discovery (capture-path resolution, locked):** the L08 P2 blocker is resolved by SPLITTING capture method by figure class — 3D-overlay geometry (edit-cage) → synthetic Pillow/SVG composite over a proven render (deterministic, self-verifiable, no-bake-consistent); interactive UI chrome (menus/panels/icons) → OS-level Win+Shift+S snip (carries the overlays the bridge `screen.screenshot` drops) located in Downloads + Pillow-cropped. Transient popups (dropdowns) must be opened live by Ray immediately before the snip; non-transient panel states can be staged by script and snipped any time. **No new viewport-cache mechanisms** (reused locked L07 P1 `DRAW_WIN_SWAP` matrix-sync before projecting/rendering). All 4 figures verified on disk via `get_file_info`. Inspection temps + stray `snip_*` copies cleaned from `images/` (Downloads snip originals retained as Ray's source archive per L07 precedent). Placement rows #3/#4/#5/#7 flipped to `produced; not integrated`; #6 already `produced; not integrated` from last chat. Header **Current state** updated to `Phase 2 PRODUCTION complete · 7 of 40 produced · 2 of 40 integrated`. **3 PENDING decisions carried, none due in Phase 2:** #40 SVG-vs-PNG; #6 dual-reference; Phase 2-9 adjustments · left open. **Scene at close:** NOT cleaned — holds `editpreview_cube` (8-vert cube, Subdivision modifier levels 2, OBJECT mode, active) + `L08Cam` + `L08CamTarget` (empty, track-to target) + `L08Fill` + `L08Key` + `L08DemoGrey` mat; BLENDER_EEVEE. No `.blend` checkpoint. Phase 2 INTEGRATION is pure HTML work (does not need the live mesh); if Blender closes, no rebuild needed. Next chat: Phase 2 INTEGRATION (`#modifier-workflow`; all 5 figures #3/#4/#5/#6/#7; single sub-chat per <6 threshold). **INTEGRATION NOTE for #7:** it is NOT a plain `<img>` — it needs the synthetic orange edit-cage + selected-vertex overlay composited per the deterministic corner-projection pixel data recorded above; design that overlay at plan-time (treat as a `c7`-class synthetic composite).

- **2026-05-28** · Phase 2 INTEGRATION closed. 5 of 5 Phase 2 figures integrated into `lesson_08_modifiers_introduction.html` (`#modifier-workflow`). HTML size 140,526 B to 153,407 B (+12,881 B across 5 figures). Plan locked in writing first; Ray "proceed" with three open confirmations taken at recommendation (see decisions below). Per-figure deltas (bottom-up execution order #7 -> #6 -> #5 -> #4 -> #3): **#7** synthetic `c7`-class composite between the "non-destructive part in action!" para and `<h3>Try It Now: Add Your First Modifier</h3>` (closes "Viewing Modifier Effects"); viewBox 1920x1080; orange `#FF6B00` edit-cage = 12 `<polyline>` edges from the 8 recorded corner pixels (group `c7-cage`), `#FF6B00` selected-vertex marker (white-stroked circle r9) at front-top corner (991,445) `c7-marker`; 2 positive-green callouts: `c7-badge`/`c7-leader` "Original geometry (editable)" leadering to the vertex marker, `c7-badge-2`/`c7-leader-2`/`c7-highlight-2` (ellipse cx1120 cy640) "Modifier preview (ghosted)" into the ghost surface; fs 22; cage/marker orange (Blender selection-color semantic), callout badges positive-green default (+4,046 B). **#6** composite-overlay 2-panel positive-green between the "Quick Practice Exercise" card `</div>` and `<h3>Understanding "Levels" and Settings</h3>`; viewBox 1920x900; fs 44 per `fs = round(22 x 1920/900-tall...)` -> actually fs 44 carried from the c2/720-precedent badge scale for legibility at the 900-tall composite (rect 440x100 at y=750 / text y=800 / bottom margin 50, c12-precedent adapted for 900-tall per L07 #13/#17); panel centers x=480/1440; labels "Before: blocky cube" / "After: subdivision"; one decorative `#FF6B00` before->after arrow `c6-arrow` at mid-height y=450; `c6-badge-1/2`; pre-existing em-dash in "rounded shape—but" preserved verbatim through anchor (+2,259 B). **#5** multi-callout 3-callout positive-green between the "Pro Workflow" blockquote `</blockquote>` and `<h3>Modifier Panel Icons Explained</h3>` (closes "Step 3: Apply or Keep"); viewBox 574x464 (small native frame); **small-frame fs floor 14** applied (strict formula `22 x 574/1920 ~= 7` is unreadable); 3 callouts label the visibility toggles "Edit-mode display" / "Realtime viewport" / "Render visibility"; ellipse r16 + leader + marker r4 + rect-h28 badge per callout; IDs `c5-highlight/leader/marker/badge-1..3` (+3,908 B). **#4** plain `<img>` no overlay between the "Adding a Modifier" card `</div>` and the "As soon as you add a modifier" para (closes "Step 1: Adding a Modifier") (+637 B). **#3** full-callout 1 positive-green between the "Accessing Modifiers" card `</div>` and the "Panel Location Tip" blockquote (closes "Finding the Modifier Properties Panel"); viewBox 600x1567 (tall narrow); **small-frame fs floor 16** applied; ellipse r30 on the wrench Modifier tab (left tab column) + leader + marker r6 + badge "Modifier Properties (wrench)"; IDs `c3-highlight/leader/marker/badge` (+2,031 B). Bottom-up order held; no anchor shifts. Dry-run -> commit -> `get_file_info` verify per figure (5 checkpoints: 144,572 -> 146,831 -> 150,739 -> 151,376 -> 153,407 B). All 5 placement-table rows flipped to `integrated`; overlay column updated #6 `—`->`c6`, #7 `—`->`c7 synthetic`. **DECISIONS taken at "proceed" (recommendation defaults, since Ray approved without specifying):** (1) #6 = option (b) add 2 composite-overlay panel badges + arrow (over option (a) plain img) — consistent with the Phase-1 #2 precedent that a no-namespace multi-panel before/after composite still gets composite-overlay panel labels (figure was produced with NO baked labels precisely so labels come from the overlay); a lightweight `c6` group was used though no `c6` namespace was pre-reserved (mirrors the #7 synthetic-overlay-on-no-namespace pattern). (2) small-frame fs floors 14 (#5) / 16 (#3). (3) #5 toggles = the 3 highest-value visibility toggles (Edit-mode display / Realtime / Render). **NEW L08 P2 INTEGRATION discoveries (2):** (1) **synthetic `c7`-class cage overlay realized** — a deterministic 12-edge orange wireframe + vertex marker drawn in an absolute-positioned SVG over a ghosted render, from recorded 8-corner projection pixels, reads as a Blender edit-cage without any baked geometry (no-bake-consistent, self-verifiable); first realized here, reusable for any future "edit-mode original-geometry over modifier-preview" figure; flag for IMAGE_STYLE_GUIDE v6. (2) **small-frame fs floor sub-rule** — for UI-chrome crops far below the ~1920-wide frame the composite/multi-callout fs formulas assume, the formula output (e.g. ~7 px) is unreadable; floor callout fs at a legibility minimum (14-16 observed here) rather than the formula value; flag for IMAGE_STYLE_GUIDE v6. Reused otherwise: locked inline-stacked overlay pattern, indent-depth-derivation rule (section-content col 20 · figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24, identical to L07), composite-overlay c12-precedent rect geometry, positive-green-default colorway, per-callout id naming `c{N}-highlight[-K]`/`-leader[-K]`/`-marker[-K]`/`-badge[-K]`. **Standing QA flags (estimated-position visual-proof, browser-QA plausibility check):** #3 c3 wrench-tab ellipse, #5 c5 three toggle ellipses, and #7 c7 cage + vertex marker + 2 callout ellipses are all estimated/projected positions (no live pixel-scan against the decoded PNG this chat) — add to the standing estimated-position QA list for browser-QA verification. #4 has no overlay; #6 c6 panel badges use deterministic panel-center geometry (no estimate) but the c6 arrow + the c6 "After" label placement are plausibility-only. **3 PENDING decisions carried, none due in Phase 2:** #40 SVG-vs-PNG; #6 dual-reference (`#subdivision-surface` second `<img>` ref = DECISION 3, due Phase 5); Phase 2-9 adjustments · left open. Next chat: Phase 3 PRODUCTION (`#array-modifier`; 5 figures #8/#9/#10/#11/#12; sub-chat split required per >=5 threshold).

- **2026-05-28** · Phase 3 PRODUCTION closed. Phase 3 PRODUCTION COMPLETE (5 of 5 `#array-modifier` figures produced). Sub-chat split locked in writing before producing (capture-method boundary, ≥5 threshold): **Sub-chat B** = viewport renders #9/#10/#11/#12 (BlenderMCP, proven path); **Sub-chat A** = UI chrome #8 (Win+Shift+S-class OS-level grab path). Run order locked at kickoff = B first, then A (Ray's call). Bottom-up within each (B: #12→#11→#10→#9; A: #8). **Scene verified at kickoff = documented close-state exactly** (5 objs: editpreview_cube subsurf + L08Cam + L08CamTarget empty + L08Fill + L08Key + L08DemoGrey mat, BLENDER_EEVEE); cleared all via `bpy.data.objects.remove` + orphan purge ("Deleted 5 data-block(s)"; 0 orphans; 0/0/0/0) before building. **Shared render base (Sub-chat B):** one fence-post mesh built once (16 v / 15 f; box footprint 0.4×0.4 × z 0→1.6 body + inset 0.06 + extrude-up 0.4 + taper×0.35 decorative pyramidal top to z=2.0); rebuilt L08 lighting (key 600/area6 @ (-4,-5,7) + fill 250/area8 @ (5,-3,4)), world light-grey 0.88, L08DemoGrey mat base (0.55,0.55,0.58) rough 0.45, L08Cam lens 50 + L08CamTarget track-to; viewport SOLID + STUDIO + color_type MATERIAL; **single shared 3/4 view rotation `Quaternion((0.768,0.59,-0.15,-0.2))` captured once and reused across #9/#10/#11 panels** so direction/count/offset read consistently; per-figure only view_location/distance vary. Engine BLENDER_EEVEE throughout. NO baked labels on any figure (overlays/panel-labels are INTEGRATION decisions). render() to local Windows tempdir → Pillow optimize-save / composite relay to WSL `images/` per locked path; `#FF6B00` 2-px Pillow dividers on all composites. **#12 PRODUCED** `lesson_08_12_array_grid_pattern.png` (858,962 B; 1920×1080; **no overlay** per registry; unit-cube source @ (0,0,0.5) + two Array modifiers ArrayX/ArrayY count 5 each, relative_offset_displace 1.5 → clean 5×5 grid; high near-top PERSP (TOP then 3× ORBITDOWN ≈45°) so cubes read as 3D blocks not flat squares · first pure-TOP-ortho attempt rejected as flat grey squares with no depth; grid centered at (3,3,0.5), view_distance span×1.7). **#11 PRODUCED** `lesson_08_11_array_direction_examples.png` (399,506 B; 3844×720; 3-panel; `c11` reserved; fence-post source, single Array per panel; X-axis rel-offset (2.4,0,0) count 5 / Y-axis (0,2.4,0) count 5 / Z-axis (0,0,1.4) count 4 [bigger z-gap so the vertical stack separates]; shared 3/4 rotation so X reads as a left-right row, Y as a row receding diagonally into depth, Z as a vertical stack; Z panel re-rendered alone with view_distance 13.5 / center z≈5.2 after first Z framing cropped top + crowded; per-panel 1280×720). **#10 PRODUCED** `lesson_08_10_array_offset_spacing.png` (387,910 B; 3844×720; 3-panel; `c10` reserved; fence-post source, Count fixed 5, relative_offset_displace X = 0.8 / 1.5 / 2.5; reads close (posts packed to a near-solid block, only tops poking out) → medium (distinct, close) → wide (well-separated); shared rotation, per-panel view centered on each row midpoint, view_distance 9.0; per-panel 1280×720). **#9 PRODUCED** `lesson_08_09_array_count_progression.png` (408,565 B; 5126×720; 4-panel; `c9` reserved; fence-post source, fixed relative_offset X 1.5, Count = 1 / 3 / 6 / 10; **fixed shared framing across all 4 panels** (view_location (row10_mid=2.7, 0, 0.95), view_distance 9.0) so rows grow rightward from a common left anchor at consistent post scale · first pass at dist 11 read too small, tightened to 9.0; per-panel 1280×720). **#8 PRODUCED** `lesson_08_08_array_modifier_panel.png` (80,223 B; 557×1157; UI chrome; `c8` reserved for INTEGRATION Count + Relative-Offset callouts). Live state scripted via BlenderMCP (FencePost active/selected; Array modifier Count=5, use_relative_offset=True, relative_offset_displace X=1.500, show_expanded=True; Properties editor switched to MODIFIER/wrench tab via temp_override — the PROPERTIES context enum only exposes object-data tabs once an object is active+selected in that area's override) and self-verified by viewport grab. **NEW L08 P3 PRODUCTION discovery (capture-path simplification, locked):** the L08 P2 blocker (`screen.screenshot` over the bridge silent-truncates to 0 bytes on WSL UNC paths AND drops the overlay pass) applies ONLY to WSL-UNC targets — capturing `bpy.ops.screen.screenshot` to a **local Windows tempdir** writes a full, non-zero window grab **with all panel/menu overlays present** (361,152 B here), so a staged non-transient panel (Array modifier expanded) can be captured by the bridge itself and Pillow-relayed, with NO manual Win+Shift+S snip needed. #8 was produced fully automatically: full-window local grab → deterministic crop of the PROPERTIES area via `area.x/y/width/height` (scale 1.0, no HiDPI; PIL box (2288,328,2845,1485) = 557×1157) → optimize-save to WSL. Win+Shift+S remains the fallback for TRANSIENT popups (dropdowns/menus that can't be script-held across a frame) per L08 P2; for non-transient panel/icon states the local-tempdir bridge screenshot is sufficient and is the lighter path. **`view3d.view_selected` not callable under the bridge temp_override** (`poll() Expected a view3d region`) even with full window/screen/area/region/region_data override; `view3d.view_axis` / `view3d.view_orbit` DO work under the same override · frame deterministically via `region_3d.view_location` + `view_distance` instead (used for every render this phase). **Sub-chat A simplification means no Ray snip was required this phase** (the planned Win+Shift+S handoff was obviated by the local-grab path). All 5 figures verified on disk via `get_file_info`. Inspection temps (`_inspect_*`, `_inspect_08*`) cleaned from `images/`. Overlay reservations unchanged: #8 `c8` · #9 `c9` · #10 `c10` · #11 `c11` · #12 none. **Two IMAGE_STYLE_GUIDE v6 flags carried from Phase 2 unchanged** (synthetic `c7`-cage overlay pattern; small-frame fs-floor sub-rule) · plus this phase's local-tempdir-screenshot-carries-overlays note is a candidate workflow addition. **3 PENDING decisions carried, none due in Phase 3:** #40 SVG-vs-PNG; #6 dual-reference (due Phase 5); Phase 2-9 adjustments · left open. **Scene at close:** NOT cleaned — holds `FencePost` (16-v post, Array modifier Count=5 rel-offset X 1.5 expanded, OBJECT mode, active/selected) + `L08Cam` + `L08CamTarget` (empty) + `L08Fill` + `L08Key` + `L08DemoGrey` mat; viewport at shared 3/4 rotation; BLENDER_EEVEE. No `.blend` checkpoint. Phase 3 INTEGRATION is pure HTML work (does not need the live mesh); if Blender closes, no rebuild needed. Next chat: Phase 3 INTEGRATION (`#array-modifier`; all 5 figures #8/#9/#10/#11/#12; single sub-chat per <6 threshold). **INTEGRATION notes:** #8/#9/#10/#11 need `c{N}` overlays designed at plan-lock (#8 Count + Relative-Offset-X callouts on the panel; #9 composite-overlay 4-panel count labels 1/3/6/10; #10 composite-overlay 3-panel offset labels 0.8/1.5/2.5; #11 composite-overlay 3-panel axis labels X/Y/Z, optionally RGB-tinted per axis); #12 is a plain `<img>` no overlay. All composite-overlay fs per `fs = round(22 × viewBox_w / 1920)` (#9 5126→fs 59; #10/#11 3844→fs 44; #8 small-frame fs-floor likely 14-16 per the Phase-2 sub-rule given the 557-wide panel). Per-figure placement anchors from the requirements-doc Image Placement section (#8 after "Creating a Fence Post Array" card · #9 after "Adjusting the Count" card · #10 after "Controlling Spacing" card · #11 after "Changing Array Direction" card · #12 after "Combining Directions for Grids" card).

- **2026-05-28** · Phase 3 INTEGRATION closed. 5 of 5 Phase 3 figures integrated into `lesson_08_modifiers_introduction.html` (`#array-modifier`). HTML size 153,407 B to 165,246 B (+11,839 B across 5 figures). Plan locked in writing first; Ray "lock and execute" (both open calls taken at recommendation: #11 axis-tinted, #8 fs-floor 16). Per-figure deltas (bottom-up execution order #12 → #11 → #10 → #9 → #8): **#12** plain `<img>` no overlay between the "Combining Directions for Grids" card `</div>` and the `<div class="mermaid">` block (+638 B). **#11** composite-overlay 3-panel **axis-tinted** between the "Changing Array Direction" card `</div>` and `<h3>Combining Directions for Grids</h3>`; viewBox 3844×720; fs 44; labels "X-axis (horizontal)" / "Y-axis (depth)" / "Z-axis (vertical)"; per-axis rect tints X red `#E63946`+`#ffffff` / Y green `#4ADE80`+`#0f1a14` / Z blue `#4A9EFF`+`#ffffff` (Blender axis-color semantic, NOT the warning colorway); c12-precedent rect geometry (440×100 at center−220 / y=580 / text y=630; centers 640·1922·3204); IDs `c11-badge-1..3` (+2,586 B). **#10** composite-overlay 3-panel positive-green between the "Controlling Spacing" card `</div>` and the "🎯 Understanding Relative Offset" `<blockquote>`; viewBox 3844×720; fs 44; labels "Offset X: 0.8 (close)" / "Offset X: 1.5 (medium)" / "Offset X: 2.5 (wide)"; same c12-precedent geometry; pre-existing em-dash in the blockquote ("like this—if") preserved verbatim through anchor; IDs `c10-badge-1..3` (+2,615 B). **#9** composite-overlay 4-panel positive-green between the "Adjusting the Count" card `</div>` and the "See how easy that was?" para; viewBox 5126×720; fs 59 per `fs = round(22 × 5126 / 1920)`; labels "Count: 1" / "Count: 3" / "Count: 6" / "Count: 10"; centers 640·1922·3204·4486; pre-existing em-dash in the para ("manually—you'd") preserved verbatim through anchor; IDs `c9-badge-1..4` (+2,890 B). **#8** multi-callout 2 positive-green between the "Creating a Fence Post Array" card `</div>` and the "Congratulations!" para; viewBox 557×1157 (small frame); **small-frame fs floor 16** applied (strict formula → ~6, unreadable; 16 chosen over 14 for the 557-wide panel, between #3's 600→16 and #5's 574→14 Phase-2 precedents); 2 callouts label the Count field and Relative-Offset-X field, badges stacked top-left (x=18, y=38 / y=120), ellipses on the estimated field rows (cy 360 / 560); IDs `c8-highlight/leader/marker/badge-1..2` (+3,110 B). Bottom-up order held; no anchor shifts. Dry-run → commit → `get_file_info` verify per figure (5 checkpoints: 154,045 → 156,631 → 159,246 → 162,136 → 165,246 B). All 5 placement-table rows flipped to `integrated`; #11 overlay column annotated `c11 (axis-tinted)`. **NEW L08 P3 INTEGRATION discovery (1):** **axis-tinted composite-overlay** — a composite-overlay where per-panel rect fill encodes a spatial-axis semantic (X red / Y green / Z blue) rather than the positive/warning split; first realized here for the array-direction figure; text fill flips to `#ffffff` on the red/blue panels and stays `#0f1a14` on green for contrast; reusable for any axis/direction comparison composite; flag for IMAGE_STYLE_GUIDE v6 (joins the carried synthetic-`c7`-cage and small-frame-fs-floor flags). Reused otherwise: locked inline-stacked overlay pattern, composite-overlay font-size formula `fs = round(22 × viewBox_w / 1920)`, c12-precedent rect geometry, positive-green-default colorway (#9/#10), small-frame fs-floor sub-rule (#8), per-callout id naming, indent-depth-derivation rule (section-content col 20 · figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24, identical to L07). **Standing QA flags (estimated-position visual-proof, browser-QA plausibility check):** #8 c8 two field-row ellipses (Count + Relative-Offset-X) are estimated positions on the 557×1157 panel (no live pixel-scan against the decoded PNG this chat) → add to the standing estimated-position QA list. #9/#10/#11 composite-overlay badges use deterministic panel-center geometry (no estimate) → no flag; #12 has no overlay. **3 PENDING decisions carried, none due in Phase 3:** #40 SVG-vs-PNG; #6 dual-reference (due Phase 5); Phase 2-9 adjustments · left open. Next chat: Phase 4 PRODUCTION (`#mirror-modifier`; 5 figures #13/#14/#15/#16/#17; sub-chat split required per ≥5 threshold).

- **2026-05-28** · Phase 4 PRODUCTION continuation closed (Sub-chat A). Phase 4 PRODUCTION now COMPLETE (5 of 5 `#mirror-modifier` figures produced). Sub-chat B (#13/#15/#16 viewport renders) had closed a prior chat WITHOUT updating this doc · verified all three on disk at kickoff before trusting the handoff (#13 470,424 B / #15 336,116 B / #16 566,638 B — exact handoff sizes). This chat produced the Sub-chat A pair. **Order inverted at Ray's explicit call: #14 first, then #17** (reused the live `SetupCube` already staged with a Mirror modifier for #14's panel rather than tearing it down first; the locked bottom-up cadence was overridden deliberately, not by error). **#14 PRODUCED** `lesson_08_14_mirror_modifier_panel.png` (75,380 B; 557×1157; Mirror panel UI chrome). Staged live via BlenderMCP off `SetupCube` (active+selected; Mirror modifier expanded; Axis X only checked + Clipping enabled; Merge on default 0.001; Bisect/Flip off), switched Properties editor to MODIFIER/wrench tab via `temp_override` (the #8 enum finding), `bpy.ops.screen.screenshot` to local Windows tempdir (336,138 B full window WITH panel overlays), deterministic crop of the PROPERTIES area via `area.x/y/width/height` with bottom-left→top-left origin flip (`top = win_h − (ay+ah)`); window 2849×1538, PROPERTIES area x2288 y53 w557 h1157 → PIL crop box (2288,328,2845,1485) = 557×1157 (SAME area geometry as #8 this lesson); Pillow optimize-save relay to WSL. QA-viewed: breadcrumb SetupCube>Mirror, Axis X blue, Clipping checked, Merge 0.001, Bisect/Flip greyed — all 5 `c14` callout targets (Axis/Clipping/Merge/Bisect/Flip) visible, NO baked labels. **#17 PRODUCED** `lesson_08_17_mirror_realtime_editing.png` (360,406 B; 1920×1080; **ghosted SURFACE ONLY · synthetic `c17`-class composite locked**, the orange edit-cage + selected-vertex + mirror-counterpart vertex + movement indicator are RESERVED INTEGRATION OVERLAY, NOT baked). Live local-tempdir `screen.screenshot` attempted FIRST per handoff (981,593 B): confirmed the L08 P2 capture-limit AGAIN — solid mirrored surface + seam captured, but NO orange edit-cage / NO selected vertex in the framebuffer (bridge drops the edit-mode 3D-overlay pass). Locked synthetic (the #7 precedent). Built fresh after tearing down `SetupCube` (removed + orphan purge, 14 data-blocks deleted, meshes/mats 0/0): −X half-box via `bmesh.ops.bisect_plane`(plane_no X, clear_outer) + `edgenet_fill` of the cut (8 v / 6 f; world bbox x −1→0 original, Mirror rebuilds +X to x=0→1 → whole 2×2×2 cube z0→2), L08DemoGrey base (0.55,0.55,0.58) rough 0.45, Mirror modifier X + clip; key 600/area6 @ (−4,−5,7) + fill 250/area8 @ (5,−3,4); L08Cam @ (7.5,−9.0,5.8) lens 50 + L08CamTarget @ (0,0,1) track-to [the #7 angled 3/4 framing]; world 0.88 grey; BLENDER_EEVEE (NEXT unavailable, fell back). render() to local tempdir → Pillow ghost = **`Image.blend(img, 224-grey, 0.25)`** (NOTE: first did 0.45 per the #7 value — washed the light-on-light cube nearly invisible; re-did at 0.25 so the form + center seam stay legible as the overlay base · L08-specific finding: the #7 0.45 ghost assumed a darker render; on the 0.88-world light-grey material use ~0.25). **INTEGRATION overlay data for the synthetic `c17` (deterministic, recorded this chat via `world_to_camera_view`, top-left-origin px on 1920×1080):** SELECTED vertex (the one being dragged) = ORIGINAL-side local (−1,−1,1) [front-top of the −X half, a MID-EDGE vertex at z=1, NOT a corner] → **px (661,550)**; its MIRROR COUNTERPART (auto-updating, +X side) local (1,−1,1) → **px (990,665)**. WHOLE-cube 8 corners (cage extent) px: (−1,−1,0)→(669,741); (−1,−1,2)→(651,348); (−1,1,0)→(936,613); (−1,1,2)→(935,254); (1,−1,0)→(989,872); (1,−1,2)→(991,445); (1,1,0)→(1245,718); (1,1,2)→(1263,331). Cube spans px x 651–1263 / y 254–872. (Corners match #7's recorded set — same cam/cube — a consistency cross-check.) INTEGRATION draws: orange `#FF6B00` 12-edge cage from the 8 corners, a `#FF6B00` selected-vertex marker at (661,550) with an "Edit This Side" callout, a second marker at (990,665) with a "Mirror Updates Automatically" callout (Blender selection-color orange for both vertices; callout badges positive-green default), and a movement/dotted-arrow indicator at the selected vertex. **NEW L08 P4 PRODUCTION discovery (1, locked):** the `c7`-class synthetic-cage path generalizes to Mirror real-time-editing figures · render the modifier RESULT surface (whole mirrored cube) ghosted, then overlay BOTH the original selected vertex AND its mirror counterpart from recorded projection px to teach "edit one side, the other updates" without any live edit-mode capture (which the bridge cannot deliver per L08 P2). Reused otherwise: local-tempdir screenshot-carries-overlays for UI chrome (#14, the L08 P3 path), `temp_override` MODIFIER-tab switch, deterministic PROPERTIES-area crop, L07 P1 `DRAW_WIN_SWAP` flush, the synthetic-composite no-bake principle. Both figures verified on disk via `get_file_info` + directory listing. **Cleanup done:** removed this chat's `_inspect_17_livetry.png` AND all 5 owed leftovers (`_inspect_16p1`/`_16_sheet`/`_15_sheet`/`_13_sheet`/`_13p2`) from `images/` (zero `_inspect_*` remain); cleared local tempdir scratch (l08_14_fullwin/l08_14_panel/l08_17_livetry/l08_17_surface). **IMAGE_STYLE_GUIDE v6 flags now carried:** synthetic `c7`/`c17`-cage overlay pattern (extended to dual-vertex mirror editing this phase); small-frame fs-floor sub-rule; axis-tinted composite-overlay; local-tempdir-screenshot-carries-overlays workflow; **NEW: ghost-blend factor is render-luminance-dependent (~0.25 on light-grey-world material, ~0.45 on darker renders).** **Scene at close:** NOT cleaned — holds `MirrorEdit` (8-v −X half-box + Mirror modifier X+clip, OBJECT mode, active) + `L08Cam` + `L08CamTarget` (empty @ (0,0,1)) + `L08Fill` + `L08Key` + `L08DemoGrey` mat; BLENDER_EEVEE; no `.blend`. (A `WizardHat` object is present in the outliner but excluded from the active view layer — leftover from an unrelated session, harmless; clear at next PRODUCTION kickoff with the rest.) Phase 4 INTEGRATION is pure HTML work (does not need the live mesh); if Blender closes, no rebuild needed. **3 PENDING decisions carried, none due in Phase 4:** #40 SVG-vs-PNG; #6 dual-reference (due Phase 5); Phase 2–9 adjustments · left open. Next chat: Phase 4 INTEGRATION (`#mirror-modifier`; all 5 figures #13/#14/#15/#16/#17; single sub-chat per <6 threshold). **INTEGRATION notes:** #13/#14/#15/#16 take `c{N}` overlays designed at plan-lock (#13 3-panel mirror-setup-step labels; #14 panel callouts Axis/Clipping/Merge/Bisect/Flip · small-frame fs-floor likely 16 per the 557-wide #8 precedent; #15 clipping ON/OFF before-after labels, warning-vs-positive colorway candidate; #16 4-panel axis-combo labels X / Y / X+Y / X+Y+Z, axis-tinted candidate); #17 is the synthetic `c17` cage+dual-vertex composite per the recorded px above (treat like #7 · NOT a plain `<img>`). Per-figure placement anchors from the requirements-doc Image Placement section (#13 after "Your First Mirror: A Simple Vase" card · #14 after "Understanding Mirror Settings" table · #15 after "The Clipping Option" card · #16 after "Mirroring on Different Axes" card · #17 after "Try This: Real-Time Mirroring" card).

- **2026-05-29** · Phase 4 INTEGRATION closed. 5 of 5 Phase 4 figures integrated into `lesson_08_modifiers_introduction.html` (`#mirror-modifier`). HTML size 165,246 B to 178,916 B (+13,670 B across 5 figures). Plan locked in writing first; Ray "lock and execute" (all 3 open calls taken at recommendation: #16 positive-green uniform, #15 warning-vs-positive split, #14 fs-floor 16). Live HTML re-verified at plan-time (165,246 B, section lines 958–1190, indent depth identical to prior phases); all 5 anchor windows built from live HTML with unique following-block markers (grep-confirmed 1 match each). Per-figure deltas (bottom-up execution order #17 → #16 → #15 → #14 → #13): **#17** synthetic `c17` cage+dual-vertex composite between the "✅ Try This: Real-Time Mirroring" card `</div>` (the "This instant feedback…" para closes it) and `</section>`; viewBox 1920×1080; fs 22; orange `#FF6B00` 12-edge cage = 12 `<polyline>` from the 8 recorded whole-cube corner px (group `c17-cage`); `#FF6B00` white-stroked vertex marker r9 at **(661,550)** `c17-marker` + positive-green "Edit This Side" callout (`c17-leader`/`c17-badge`); second marker r9 at **(990,665)** `c17-marker-2` + positive-green "Mirror Updates Automatically" callout (`c17-leader-2`/`c17-badge-2`); dotted `#FF6B00` movement arrow `c17-arrow` from (661,550)→(745,550) with `c17-arrowhead` marker; cage/markers orange (Blender selection semantic), callout badges positive-green default (+3,942 B). **#16** composite-overlay 4-panel **positive-green uniform** (open-call recommendation: axis-tint breaks down on the X+Y / X+Y+Z combo panels, so uniform green chosen over per-axis tint) between the "Mirroring on Different Axes" instruction card `</div>` and the "💡 Multi-Axis Mirroring" yellow card; viewBox 5126×720; fs 59 per `fs = round(22 × 5126 / 1920)`; labels "X-axis only" / "Y-axis only" / "X + Y" / "X + Y + Z"; c12-precedent rect geometry (440×100 at center−220 / y=580 / text y=630); centers 640·1922·3204·4486; IDs `c16-badge-1..4` (+2,106 B). **#15** composite-overlay 2-panel **warning-vs-positive split** (open-call recommendation: the textbook problem/solution figure; mixed-colorway-composite precedent = L07 #25) between the "📝 Testing Clipping" card `</div>` and the "🎯 Why Clipping Matters" blockquote; viewBox 2562×720; fs 29 per `fs = round(22 × 2562 / 1920)`; left "Clipping OFF · gap" warning `#E63946`+`#ffffff`, right "Clipping ON · seamless" positive `#4ADE80`+`#0f1a14`; 2-panel centers 640·1922 (W/4, 3W/4); rect 440×80 at center−220 / y=600 / text y=640; IDs `c15-badge-1..2` (+1,557 B). **#14** multi-callout 5 positive-green between the "🎛️ Key Mirror Settings" table card `</div>` and `<h3>The Clipping Option (Most Important!)</h3>`; viewBox 557×1157 (small frame); **small-frame fs floor 16** applied (strict formula → ~6, unreadable; 16 matches the 557-wide #8 precedent); 5 callouts label Axis / Clipping / Merge / Bisect / Flip; ellipse rx150 ry24 on each estimated row (cy 300/470/560/650/740) + leader + marker r4 + 110×28 badge stacked left margin (x=8); IDs `c14-highlight/leader/marker/badge-1..5` (+4,207 B). **#13** composite-overlay 3-panel positive-green step labels between the "📝 Creating a Mirrored Vase" card `</div>` and the "You should now see a complete cube again…" para; viewBox 3844×720; fs 44; labels "1. Original cube" / "2. Half deleted" / "3. Mirror applied"; same c12-precedent geometry (centers 640·1922·3204); pre-existing em-dash in the following `<p>` ("again—but") preserved verbatim through anchor; IDs `c13-badge-1..3` (+1,858 B). Bottom-up order held; no anchor shifts. Dry-run → commit → `get_file_info` verify per figure (5 checkpoints: 169,188 → 171,294 → 172,851 → 177,058 → 178,916 B). All 5 placement-table rows flipped to `integrated`; overlay column annotated #15 `c15 (warning-vs-positive)`. **NO new INTEGRATION discoveries** — every pattern reused from locked precedents: the synthetic `c17`/`c7`-class cage+dual-vertex overlay (the L08 P4 PRODUCTION generalization, now realized in HTML — 12-edge `<polyline>` cage + two orange vertex markers + movement arrow + two positive-green callouts over a ghosted render), composite-overlay font-size formula, c12-precedent rect geometry, positive-green-default and warning-vs-positive colorways, mixed-colorway-composite (L07 #25), small-frame fs-floor sub-rule (#14, 16), per-callout id naming, indent-depth-derivation rule (section-content col 20 · figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24, identical to L07). **Two in-draft self-corrections caught at dry-run (no bad commits):** (1) #17 first draft had a stray non-numeric `x2` token on the movement-arrow `<line>` — fixed to `x2="745"` before any commit; (2) #16 / #14 first drafts initially risked dropping the trailing `<h4>` / next-block when the `newText` tail was incomplete — re-issued with the full tail restored, confirmed clean in dry-run, then committed. **Standing QA flags (estimated-position visual-proof, browser-QA plausibility check, NO live pixel-scan against decoded PNGs this chat):** #17 c17 cage + both vertex markers (661,550)/(990,665) + the 2 callout leaders; #14 c14 five panel-callout ellipses (cy 300/470/560/650/740) → add to the standing estimated-position QA list. #13/#15/#16 composite-overlay badges use deterministic panel-center geometry (no estimate) → no flag. **3 PENDING decisions carried:** #40 SVG-vs-PNG (due Phase 9); **#6 dual-reference (DECISION 3) now DUE next chat at Phase 5** — produce-once/reference-twice mechanic must be resolved: add a second `<img>` reference to `lesson_08_06_subdivision_quick_demo.png` in `#subdivision-surface` ("After Your First Subdivision: Smooth a Cube card" per requirements-doc placement); Phase 2-9 adjustments · left open. Next chat: Phase 5 PRODUCTION (`#subdivision-surface`; 5 figures #18/#19/#20/#21/#22 + #6 reference; sub-chat split required per ≥5 threshold). **Scene-state carry (from Phase 4 PRODUCTION close, unchanged — INTEGRATION did not touch Blender):** holds `MirrorEdit` + `L08Cam` + `L08CamTarget` + `L08Fill` + `L08Key` + `L08DemoGrey` mat + stray excluded `WizardHat`; verify live + clear via `bpy.data.objects.remove` (never `read_factory_settings`) at Phase 5 PRODUCTION kickoff.

- **2026-05-29** · Phase 5 PRODUCTION closed. Phase 5 PRODUCTION COMPLETE (5 of 5 `#subdivision-surface` figures produced · #18/#19/#20/#21/#22). Sub-chat split locked + held (capture-method boundary, ≥5 threshold): **Sub-chat B** = viewport renders #22/#20/#18; **Sub-chat A** = UI chrome #21/#19; bottom-up within each. **Scene cleared at kickoff** via `bpy.data.objects.remove` + orphan purge (0/0/0/0/0; the stray excluded `WizardHat` already gone; scene-level materials_count read 0). Shared L08 base rebuilt: world light-grey 0.88, engine BLENDER_EEVEE (EEVEE_NEXT unavailable, fell back per locked rule), `L08DemoGrey` base (0.55,0.55,0.58) rough 0.45, key 600/area6 @ (-4,-5,7) + fill 250/area8 @ (5,-3,4), L08Cam @ (7.5,-9,5.8) lens 50 + L08CamTarget @ (0,0,1) track-to. NO baked labels on any figure (overlays/panel-labels are INTEGRATION decisions). **#22 PRODUCED** `lesson_08_22_subdivision_smooth_shading.png` (335,484 B; 2562×720; 2-panel flat-shaded subsurf | smooth-shaded subsurf (Level 2); `#FF6B00` 2-px Pillow divider; `c22` reserved for INTEGRATION 2-panel flat-vs-smooth labels). **#20 PRODUCED** `lesson_08_20_edge_loop_sharpness.png` (454,711 B; 3844×720; 3-panel no-loop / one-loop / two-loop subsurf-sharpness progression; supporting loops generated via `bmesh.ops.inset_individual` (panel 2 thickness 0.18; panel 3 tightened to 0.06 for a visibly sharp edge after a first 0.12/0.30 attempt read marginal); **SURFACE ONLY** — the orange loop-cut lines are SYNTHETIC `c20` overlay at INTEGRATION, NOT baked; `c20` synthetic reserved). **#18 PRODUCED** `lesson_08_18_subdivision_levels.png` (555,079 B; 6408×720; 5-panel levels 0/1/2/3/4 on one cube, smooth-shaded, shared framing; `c18` reserved for INTEGRATION 5-panel level labels). **#21 PRODUCED** `lesson_08_21_edge_crease_tool.png` (274,928 B; 1280×720; crease RESULT SURFACE — full crease 1.0 on the 4 +X-face edges via the `transform.edge_crease` operator in EDIT mode, subsurf L3, so the creased face stays sharp while the rest rounds; the crease-value / Shift+E annotation is SYNTHETIC `c21` overlay at INTEGRATION, NOT baked; `c21` synthetic reserved). **#19 PRODUCED** `lesson_08_19_subdivision_viewport_render.png` (64,291 B; 557×1157; Subdivision panel with Levels Viewport=1 / Render=3, wrench tab active; local-tempdir `screen.screenshot` (1,044,107 B full window) → PROPERTIES-area crop box (2288,328,2845,1485) [window 2849×1538, area x2288 y53 w557 h1157, SAME geometry as #8/#14]; `c19` callouts at INTEGRATION). **NEW L08 P5 PRODUCTION discoveries (2, flag for IMAGE_STYLE_GUIDE v6):** (a) **bmesh edge-crease layer API gap in this build** — no `bm.edges.layers.crease` available; use the `transform.edge_crease` operator (EDIT mode, edges selected) instead. (b) **`inset_individual` as a deterministic supporting-loop generator** for subsurf-sharpness figures — tighter inset = sharper edge (0.06 ≈ sharp, 0.18 ≈ soft); no edit-mode loopcut operator needed. Reused otherwise: local-tempdir-screenshot-carries-overlays for UI chrome (#19), deterministic PROPERTIES-area crop (same geometry as #8/#14), L07 P1 `DRAW_WIN_SWAP` flush, the synthetic-composite no-bake principle (#20/#21 surfaces only). All 5 figures verified on disk via `get_file_info`. Inspection temps + local tempdir scratch cleared. Placement rows #18/#19/#20/#21/#22 flipped to `produced; not integrated`. **DECISION 3 (#6 dual-reference) RESOLVED:** produce-once / reference-twice — at Phase 5 INTEGRATION add a SECOND plain `<img>` (NO overlay) to `lesson_08_06_subdivision_quick_demo.png` in `#subdivision-surface` after the "Your First Subdivision: Smooth a Cube" card, with distinct recap-framed alt text (re-using the `c6` overlay here would duplicate SVG IDs on the same page); row #6 stays `integrated`, track the second reference as a placement note. **3 PENDING decisions carried:** #40 SVG-vs-PNG (due Phase 9); Phase 2-9 adjustments; (#6 dual-reference now RESOLVED) · left open. **Scene at close:** NOT cleaned — holds `CreaseCube` (subsurf L1/R3, +X face creased, active) + `L08Cam` + `L08CamTarget` + `L08Fill` + `L08Key` + `L08DemoGrey` mat; BLENDER_EEVEE; no `.blend`. Phase 5 INTEGRATION is pure HTML work (does not need the live mesh); if Blender closes, no rebuild needed. Next chat: Phase 5 INTEGRATION (`#subdivision-surface`; integrate #18/#19/#20/#21/#22 + add the #6 second reference; single sub-chat per <6 threshold). **INTEGRATION notes:** #18 composite-overlay 5-panel level labels (viewBox 6408×720 → fs 73); #19 small-frame multi-callout 2 (Levels Viewport + Render fields · small-frame fs-floor 16 per the 557-wide #8/#14 precedent); #20 SYNTHETIC — per-panel orange `#FF6B00` loop-cut lines + composite-overlay 3-panel labels (viewBox 3844×720 → fs 44); #21 SYNTHETIC — crease-value indicator + "Edge Crease: 1.0 · Shift+E" callout on the sharp +X edge; #22 composite-overlay 2-panel warning-vs-positive (left "Flat shading" `#E63946`, right "Smooth shading" `#4ADE80`; mixed-colorway precedent L07 #25 / L08 #15 · viewBox 2562×720 → fs 29); #6 second-ref plain `<img>` no overlay (DECISION 3). Per-figure placement anchors from the requirements-doc Image Placement section (#6 after "Your First Subdivision: Smooth a Cube" card · #18 after "Understanding Subdivision Levels" table · #19 after "Viewport vs Render Levels" callout · #20 after "Creating Sharp Edges with Loop Cuts" card · #21 after "Using Edge Crease" card · #22 after "Combining Subdivision with Smooth Shading" card).

- **2026-05-29** · Phase 5 INTEGRATION closed. 6 figures integrated into `lesson_08_modifiers_introduction.html` (`#subdivision-surface`): the 5 Phase-5 figures #18/#19/#20/#21/#22 plus the #6 second reference (DECISION 3). HTML size 178,916 B to 191,170 B (+12,254 B across 6 figures). Plan locked in writing first; Ray "lock and execute" (all 3 open calls taken at recommendation: #20 schematic loop-pair markers near each sharp edge / panel-1 none, #21 single crease callout to the sharp +X face, #18 panel-5 label "Level 4 (overkill)"). Live HTML re-verified at plan-time (178,916 B, `#subdivision-surface` section lines 1307–1544, indent depth identical to prior phases); all 6 anchor windows built from live HTML with unique following-block markers (grep-confirmed 1 match each). Per-figure deltas (bottom-up execution order #22 → #21 → #20 → #19 → #18 → #6-2nd): **#22** composite-overlay 2-panel warning-vs-positive between the "Combining Subdivision with Smooth Shading" card `</div>` and the "Smooth shading + Subdivision…" para; viewBox 2562×720; fs 29 per `fs = round(22 × 2562 / 1920)`; left "Flat shading" warning `#E63946`+`#ffffff`, right "Smooth shading" positive `#4ADE80`+`#0f1a14`; 2-panel centers 640·1922; rect 440×80 at center−220 / y=600 / text y=640; IDs `c22-badge-1..2` (+1,743 B). **#21** SYNTHETIC `c21` small-frame between the "Using Edge Crease" card `</div>` and the "💡 Loop Cuts vs Crease" yellow card; viewBox 1280×720; small-frame fs floor 16; orange `#FF6B00` highlight ellipse (cx860 cy360 rx150 ry170) over the creased +X face + positive-green leader/marker + badge "Edge Crease: 1.0 · Shift+E" (top-left); IDs `c21-highlight/leader/marker/badge` (+1,792 B). **#20** SYNTHETIC `c20` composite-overlay 3-panel between the "Creating Sharp Edges with Loop Cuts" card `</div>` and the "🎯 The Loop Cut Rule" blockquote; viewBox 3844×720; fs 44; per-panel orange `#FF6B00` schematic loop-cut lines (panel 1 none; panel 2 one line at x1900; panel 3 two lines at x3168/x3204, groups `c20-loops-2`/`c20-loops-3`) + composite-overlay labels "No loops (rounded)" / "One loop (sharper)" / "Two loops (sharp)"; c12-precedent rect geometry (440×100 at center−220 / y=580 / text y=630; centers 640·1922·3204); pre-existing em-dash in the blockquote ("like this—the closer") preserved verbatim through anchor; IDs `c20-badge-1..3` (+2,746 B). **#19** small-frame multi-callout 2 between the "💡 Viewport vs Render Levels" yellow card `</div>` and `<h3>Adjusting Subdivision Levels</h3>`; viewBox 557×1157 (small frame); small-frame fs floor 16; 2 callouts label the Levels Viewport (cy470) + Render (cy540) field rows, badges stacked left margin (x=8, y=332 / y=640), ellipse rx170 ry26 + leader + marker r4 each; IDs `c19-highlight/leader/marker/badge-1..2` (+2,476 B). **#18** composite-overlay 5-panel between the "Understanding Subdivision Levels" table card `</div>` and the "💡 Viewport vs Render Levels" yellow card; viewBox 6408×720; fs 73 per `fs = round(22 × 6408 / 1920)`; labels "Level 0" / "Level 1" / "Level 2" / "Level 3" / "Level 4 (overkill)"; c12-precedent rect geometry (centers 640·1922·3204·4486·5768; badges 1–4 width 440, badge-5 widened to 760 to fit the longer label; y=580 / text y=630); IDs `c18-badge-1..5` (+2,865 B). **#6 second reference** plain `<img>` NO overlay between the "Your First Subdivision: Smooth a Cube" card `</div>` and the "This is the power of Subdivision Surface…" para; distinct recap-framed alt text + recap figcaption; NO SVG, NO `c6` IDs (the `c6` composite-overlay stays solely on the `#modifier-workflow` instance at HTML line ~584, so zero duplicate-ID collision); pre-existing em-dash in "Subdivision Surface—you" preserved verbatim through anchor (+632 B). Bottom-up order held; no anchor shifts. Dry-run → commit → `get_file_info` verify per figure (6 checkpoints: 180,659 → 182,451 → 185,197 → 187,673 → 190,538 → 191,170 B). All 5 Phase-5 placement-table rows flipped to `integrated`; overlay column annotated #22 `c22 (warning-vs-positive)`; row #6 stays `integrated` with the section column annotated `(+ #subdivision-surface 2nd ref, plain)`. **DECISION 3 (#6 dual-reference) now fully RESOLVED + realized:** produce-once / reference-twice — second instance is a plain `<img>` (no overlay) with distinct alt/figcaption, no SVG-ID duplication. **NO new INTEGRATION discoveries** — every pattern reused from locked precedents: composite-overlay font-size formula `fs = round(22 × viewBox_w / 1920)`, c12-precedent rect geometry, positive-green-default and warning-vs-positive colorways, mixed-colorway-composite (L07 #25 / L08 #15, reused by #22), small-frame fs-floor sub-rule (#19/#21, 16), synthetic-overlay-over-render pattern (#20 loop-cut lines + #21 crease indicator drawn over surface-only renders, the c7/c17 synthetic precedent extended to schematic non-projected markers), inline-stacked overlay pattern, indent-depth-derivation rule (section-content col 20 · figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24, identical to L07), per-callout id naming, produce-once/reference-twice plain-`<img>` second-reference (first realized here). **Standing QA flags (estimated-position visual-proof, browser-QA plausibility check, NO live pixel-scan against decoded PNGs this chat):** #22 c22 badges deterministic panel-center → no flag; #21 c21 crease highlight ellipse + leader (estimated creased-face position, no recorded projection px) → add to standing estimated-position QA list; #20 c20 the per-panel loop-cut line markers (schematic estimated positions near each panel's sharp edge, no recorded projection px) → add to list, badges deterministic so no flag on labels; #19 c19 two field-row ellipses (Levels Viewport cy470 / Render cy540, estimated on the 557×1157 panel) → add to list; #18 c18 badges deterministic panel-center → no flag; #6 2nd-ref plain, no overlay → no flag. **3 PENDING decisions:** #40 SVG-vs-PNG (due Phase 9); Phase 6-9 adjustments · left open (#6 dual-reference now CLOSED). Next chat: Phase 6 PRODUCTION (`#bevel-modifier`; 5 figures #23/#24/#25/#26/#27; sub-chat split required per ≥5 threshold). **Scene-state carry (from Phase 5 PRODUCTION close, unchanged — INTEGRATION did not touch Blender):** holds `CreaseCube` (subsurf L1/R3, +X face creased, active) + `L08Cam` + `L08CamTarget` + `L08Fill` + `L08Key` + `L08DemoGrey` mat; BLENDER_EEVEE; no `.blend`; verify live + clear via `bpy.data.objects.remove` (never `read_factory_settings`) at Phase 6 PRODUCTION kickoff.

- **2026-05-29** · Phase 6 PRODUCTION closed. Phase 6 PRODUCTION COMPLETE (5 of 5 `#bevel-modifier` figures produced · #23/#24/#25/#26/#27). Sub-chat split locked + held (capture-method boundary, ≥5 threshold): **Sub-chat B** = viewport renders #27/#26/#25/#24 (BlenderMCP proven path); **Sub-chat A** = UI chrome #23 (local-tempdir `screen.screenshot` → PROPERTIES-area crop). Bottom-up within each (B: #27→#26→#25→#24; A: #23). Ray "proceed as you see fit" — all open calls taken at recommendation (#26 varied-angle test object = stepped/tapered cube). **Scene verified at kickoff = documented Phase 5 close exactly** (5 objs: CreaseCube + L08Cam + L08CamTarget + L08Fill + L08Key, 1 mat L08DemoGrey, BLENDER_EEVEE); cleared all via `bpy.data.objects.remove` + orphan purge ("Deleted 13 data-block(s)"; 0/0/0/0/0) before rebuilding the shared L08 base (world 0.88 grey, BLENDER_EEVEE [EEVEE_NEXT unavailable, fell back per locked rule], L08DemoGrey base (0.55,0.55,0.58) rough 0.45, key 600/area6 @ (-4,-5,7) + fill 250/area8 @ (5,-3,4), L08Cam @ (7.5,-9,5.8) lens 50 + L08CamTarget @ (0,0,1) track-to). NO baked labels on any figure (overlays are INTEGRATION decisions); `#FF6B00` 2-px Pillow dividers on all composites; render() to local Windows tempdir `C:\Users\pract\AppData\Local\Temp\l08_p6` → Pillow optimize-save / composite relay to WSL `images/`. **#27 PRODUCED** `lesson_08_27_bevel_subdivision_combo.png` (451,033 B; 2562×720; 2-panel; `c27` reserved; left = bevel-only tight (width 0.06 seg 3, sharp hard-surface edges), right = same tight bevel + Subdivision L2 (form rounds, edges stay controlled); subject = elongated box (cube z-scaled ×1.4, transform-applied) so the subdivision rounding reads in silhouette · first attempt on a plain cube with width 0.12 read near-identical L/R — redone tighter + elongated for legible synergy contrast). **#26 PRODUCED** `lesson_08_26_bevel_angle_limiting.png` (447,144 B; 3844×720; 3-panel; `c26` reserved; angle-limited Bevel sweep on a STEPPED/TAPERED test object (cube + top-face inset 0.45 + extrude-up 0.5 + taper resize 0.6 → measured dihedral spread [66.3°, 80.7°, 90.0°]); thresholds chosen AGAINST the measured spread = 50° (all edges) / 75° (drops the ~66° taper edges) / 85° (only the 90° sharp corners) — NOT the requirements-doc nominal 30/60/89, which would not differentiate against this object's spread; the INTEGRATION `c26` labels should follow the doc's pedagogical 30°/60°/89° "all edges / sharp edges / corners only" framing per the doc intent, OR the actual swept values 50°/75°/85° — OPEN call at INTEGRATION plan-lock; camera pulled in to (6.2,-7.4,5.0) target (0,0,1.2) for the taller object · first attempts (plain pre-chamfered cube, then nominal 30/60/89) read identical across panels because the edge-angle families clustered — redone with the stepped object + spread-matched thresholds for legible edge add/drop). **#25 PRODUCED** `lesson_08_25_bevel_segments_comparison.png` (743,054 B; 5126×720; 4-panel; `c25` reserved; clean cube, FIXED width 0.25, Segments 1/2/4/8; panel 1 reads as a flat chamfer, 2→4→8 progressively rounder · 4→8 difference is genuinely subtle, the real diminishing-returns lesson; camera (5.8,-7.0,4.6) target (0,0,1.0) for edge-profile read). **#24 PRODUCED** `lesson_08_24_bevel_amount_comparison.png` (721,071 B; 5126×720; 4-panel; `c24` reserved; same clean cube, FIXED Segments 4, Amount 0.01/0.05/0.1/0.3; crisp → subtle → pronounced → heavy rounding, clean gradient at thumbnail). **#23 PRODUCED** `lesson_08_23_bevel_modifier_panel.png` (80,597 B; 557×1157; Bevel panel UI chrome; `c23` reserved for INTEGRATION Amount/Segments/Limit-Method/Angle callouts · small-frame fs-floor likely 16 per the 557-wide #8/#14/#19 precedent). Staged live off `BevelSeg` (active+selected; Bevel modifier expanded, width 0.05, Segments 3, Limit Method Angle, Angle 30°); Properties editor switched to MODIFIER/wrench tab via `space.context='MODIFIER'`; `bpy.ops.screen.screenshot` to local Windows tempdir (1,034,954 B full window WITH panel overlays) after a `DRAW_WIN_SWAP` flush; deterministic crop of the PROPERTIES area via `area.x/y/width/height` with bottom-left→top-left origin flip; window 2849×1538, PROPERTIES area x2288 y53 w557 h1157 → PIL crop box (2288,328,2845,1485) = 557×1157 (SAME geometry as #8/#14/#19 this lesson); Pillow optimize-save relay to WSL. QA-viewed: breadcrumb BevelSeg>Bevel, wrench tab active blue, Amount 0.05 m / Segments 3 / Limit Method Angle / Angle 30° all visible, NO baked labels. **NO new L08 P6 PRODUCTION discoveries** — every mechanism reused from locked precedents: local-tempdir-screenshot-carries-overlays for UI chrome (#23, the L08 P3 path), `space.context` MODIFIER-tab switch, deterministic PROPERTIES-area crop (same geometry as #8/#14/#19), L07 P1 `DRAW_WIN_SWAP` flush before the grab, fixed-camera shared-framing across composite panels (track-to target), `#FF6B00` 2-px Pillow dividers, render→local-tempdir→Pillow-relay path. **Production-craft note (not a new mechanism):** for parametric-comparison composites where the parameter sweep interacts with mesh topology (#26 angle-limit), derive the actual dihedral-angle spread from the test mesh FIRST (`edge.calc_face_angle()`), then choose sweep thresholds that sit BETWEEN the angle families so each panel visibly adds/drops edges — nominal doc values may not differentiate against a given object. All 5 figures verified on disk via `get_file_info` (sizes above). Local tempdir scratch (19 intermediate PNGs) + the tempdir itself removed at close. Overlay reservations unchanged: #23 `c23` · #24 `c24` · #25 `c25` · #26 `c26` · #27 `c27`. **IMAGE_STYLE_GUIDE v6 flags carried forward unchanged** (synthetic `c7`/`c17`/`c20`/`c21`-cage/overlay-over-render pattern; small-frame fs-floor sub-rule; axis-tinted composite-overlay; local-tempdir-screenshot-carries-overlays workflow; ghost-blend factor render-luminance-dependent) · plus this phase's spread-matched-threshold production-craft note as a candidate workflow addition. **2 PENDING decisions carried, none due in Phase 6:** #40 SVG-vs-PNG (due Phase 9); Phase 7-9 adjustments · left open. **Scene at close:** NOT cleaned — holds `BevelSeg` (clean cube + Bevel modifier expanded, width 0.05 seg 3 Angle 30°, active/selected) + `L08Cam` + `L08CamTarget` (empty @ (0,0,1.2)) + `L08Fill` + `L08Key` + `L08DemoGrey` mat; camera @ (6.2,-7.4,5.0) [moved from base during #26]; BLENDER_EEVEE; no `.blend`. Phase 6 INTEGRATION is pure HTML work (does not need the live mesh); if Blender closes, no rebuild needed. Next chat: Phase 6 INTEGRATION (`#bevel-modifier`; all 5 figures #23/#24/#25/#26/#27; single sub-chat per <6 threshold). **INTEGRATION notes:** #23 small-frame multi-callout 4 (Amount/Segments/Limit-Method/Angle field rows · small-frame fs-floor 16 per the 557-wide #8/#14/#19 precedent); #24 composite-overlay 4-panel amount labels (viewBox 5126×720 → fs 59); #25 composite-overlay 4-panel segment labels (viewBox 5126×720 → fs 59); #26 composite-overlay 3-panel angle labels (viewBox 3844×720 → fs 44 · OPEN call: doc-nominal 30°/60°/89° framing vs actual-swept 50°/75°/85°); #27 composite-overlay 2-panel before/after labels "Bevel only" / "Bevel + Subdivision" (viewBox 2562×720 → fs 29). All composite-overlay fs per `fs = round(22 × viewBox_w / 1920)`; c12-precedent rect geometry. Per-figure placement anchors from the requirements-doc Image Placement section (#23 after "Beveling a Cube" card · #24 after "Changing Bevel Size" card · #25 after "Testing Segment Counts" card · #26 after "Using Angle Limiting" card · #27 after "Combining Bevel and Subdivision" card).

- **2026-05-29** · Phase 6 INTEGRATION closed. 5 of 5 Phase 6 figures integrated into `lesson_08_modifiers_introduction.html` (`#bevel-modifier`). HTML size 191,170 B to 204,011 B (+12,841 B across 5 figures). Plan locked in writing first; Ray "confirmed, proceed" (the #26 OPEN call resolved at recommendation — see below). Live HTML re-verified at plan-time (191,170 B; `#bevel-modifier` section lines 1647–1879; section-content indent col 20, identical to all prior phases so the locked column scheme carries verbatim); all 5 anchor windows built from live HTML with unique following-block markers (grep-confirmed 1 match each). **#26 OPEN call RESOLVED:** labels carry the ACTUAL swept thresholds 50°/75°/85° (not the doc-nominal 30/60/89) with the doc's pedagogical gloss as the second clause — "Angle 50° · all edges" / "Angle 75° · sharp edges" / "Angle 85° · corners only"; rationale = the panels are visual proof and the rendered object's measured dihedral spread [66.3°/80.7°/90.0°] only differentiates at the swept values, so labeling 30/60/89 would contradict the pixels (same estimated-position-visual-proof principle the QA pass enforces); the doc's "all edges / sharp edges / corners only" teaching framing is preserved verbatim and is true against this object (50° catches every edge incl. the ~66° taper, 75° drops the taper edges, 85° leaves only the 90° corners). Per-figure deltas (bottom-up execution order #27 → #26 → #25 → #24 → #23): **#27** composite-overlay 2-panel positive-green between the "Combining Bevel and Subdivision" card `</div>` and the "🎯 Why This Combo Works" blockquote; viewBox 2562×720; fs 29 per `fs = round(22 × 2562 / 1920)`; labels "Bevel only" / "Bevel + Subdivision"; 2-panel centers 640·1922; rect 440×80 at center−220 / y=600 / text y=640; IDs `c27-badge-1..2` (+1,723 B). **#26** composite-overlay 3-panel positive-green between the "Using Angle Limiting" card `</div>` and the "This is incredibly useful…" para; viewBox 3844×720; fs 44; labels per the OPEN-call resolution above; c12-precedent rect geometry (440×100 at center−220 / y=580 / text y=630; centers 640·1922·3204); IDs `c26-badge-1..3` (+2,213 B). **#25** composite-overlay 4-panel positive-green between the "Testing Segment Counts" card `</div>` and the "💡 Segments vs Performance" yellow card; viewBox 5126×720; fs 59 per `fs = round(22 × 5126 / 1920)`; labels "1 segment" / "2 segments" / "4 segments" / "8 segments"; centers 640·1922·3204·4486; IDs `c25-badge-1..4` (+2,460 B). **#24** composite-overlay 4-panel positive-green between the "Changing Bevel Size" card `</div>` and the "🎯 Real-World Sizing" blockquote; viewBox 5126×720; fs 59; labels "Amount: 0.01" / "Amount: 0.05" / "Amount: 0.1" / "Amount: 0.3"; centers as #25; pre-existing em-dash in the blockquote ("look—which") preserved verbatim through anchor; IDs `c24-badge-1..4` (+2,461 B). **#23** small-frame multi-callout 4 positive-green between the "Beveling a Cube" card `</div>` and the "With just one click…" para; viewBox 557×1157 (small frame); **small-frame fs floor 16** applied (strict formula → ~6, unreadable; 16 matches the 557-wide #8/#14/#19 precedent); 4 callouts label Amount / Segments / Limit Method / Angle field rows; ellipse rx150 ry24 on each estimated row (cy 300/370/560/630) + leader + marker r4 + 150×28 badge stacked left margin (x=8); IDs `c23-highlight/leader/marker/badge-1..4` (+3,984 B). Bottom-up order held; no anchor shifts. Dry-run → commit → `get_file_info` verify per figure (5 checkpoints: 192,893 → 195,106 → 197,566 → 200,027 → 204,011 B). All 5 placement-table rows flipped to `integrated`; overlay column annotated #26 `c26 (actual-swept 50/75/85)`. **One anchor-uniqueness self-correction caught at dry-run (no bad commit):** #25's first anchor `</ol></div>\n\n<div class="card" style="background: #fff3cd; border-left: 4px solid #ffc107;">` matched the EARLIER `#array-modifier` "When to Use Which Offset" yellow card (line ~920, 13 total occurrences of that yellow-card opener in the file) instead of the bevel "Segments vs Performance" card — the dry-run diff landed at the wrong section, was NOT committed, and the `oldText` was extended to include the unique `<h4>💡 Segments vs Performance</h4>` follow-line, re-dry-run landed correctly at line 1752, then committed. (Lesson: the bare yellow-card opener is NOT a unique anchor in this file; always extend with the following `<h4>`.) **NO new INTEGRATION discoveries** — every pattern reused from locked precedents: composite-overlay font-size formula `fs = round(22 × viewBox_w / 1920)`, c12-precedent rect geometry, positive-green-default colorway, small-frame fs-floor sub-rule (#23, 16), per-callout id naming `c{N}-highlight[-K]`/`-leader[-K]`/`-marker[-K]`/`-badge[-K]`, inline-stacked overlay pattern, indent-depth-derivation rule (section-content col 20 · figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24, identical to L07), no-new-em-dashes rule (figcaptions use middle-dot · separators; #24's pre-existing source em-dash preserved through the exact-match anchor). **Standing QA flags (estimated-position visual-proof, browser-QA plausibility check, NO live pixel-scan against decoded PNGs this chat):** #23 c23 four panel-callout ellipses (cy 300/370/560/630, estimated field-row positions on the 557×1157 panel) → add to the standing estimated-position QA list. #24/#25/#26/#27 composite-overlay badges use deterministic panel-center geometry (no estimate) → no flag. **2 PENDING decisions carried:** #40 SVG-vs-PNG (due Phase 9); Phase 7-9 adjustments · left open. Next chat: Phase 7 PRODUCTION (`#modifier-stacking`; 6 figures #28/#29 SVG `mo`/#30/#31 SVG `sp`/#32/#38 SVG `aw`; sub-chat split required per ≥5 threshold; Phase 7 INTEGRATION plan-lock is its OWN chat per the ≥6 threshold). **Scene-state carry (from Phase 6 PRODUCTION close, unchanged — INTEGRATION did not touch Blender):** holds `BevelSeg` + `L08Cam` + `L08CamTarget` (@ (0,0,1.2)) + `L08Fill` + `L08Key` + `L08DemoGrey` mat; camera @ (6.2,-7.4,5.0); BLENDER_EEVEE; no `.blend`; verify live + clear via `bpy.data.objects.remove` (never `read_factory_settings`) at Phase 7 PRODUCTION kickoff.

- **2026-05-29** · Phase 7 PRODUCTION started, partial (Sub-chat C COMPLETE). Sub-chat split + run order locked in writing before producing (capture-method boundary, ≥5 threshold): **Sub-chat C** = 3 hand-coded inline SVGs #38 `aw` / #31 `sp` / #29 `mo` (house style guide, no Blender, no Canva per locked rule); **Sub-chat D** = 3 PNG renders/UI chrome #28 `c28` / #30 `c30` / #32 `c32` (BlenderMCP + local-tempdir capture). Run order locked: C first (zero scene dependency), then D; bottom-up within each (C: #38→#31→#29; D: #32→#30→#28). Ray "proceed how you think is best" — produced all 3 Sub-chat C SVGs in this chat (C needs no Blender). **Scene verified at kickoff** = documented Phase 6 close objects (`BevelSeg` + `L08Cam` + `L08CamTarget` + `L08Fill` + `L08Key` + `L08DemoGrey` mat, BLENDER_EEVEE) with two harmless deltas vs handoff: `L08CamTarget` @ (0,0,1.0) not (0,0,1.2) and `L08Cam` @ (5.8,-7.0,4.6) not (6.2,-7.4,5.0) [live state reflects a later #25-style reset]; AND a `.blend` IS bound (`G:\My Drive\blender_images\hat.blend`) where the handoff said none — NOT saved over, all work stays in-memory per locked no-`.blend` rule. Cleared all via `bpy.data.objects.remove` + orphan purge ("Deleted 9 data-block(s)"; 0 objects, meshes/mats/lights/cams 0/0/0/0; engine BLENDER_EEVEE) at kickoff [Sub-chat C does not need the clean scene, but cleared per kickoff protocol so Sub-chat D opens clean]. **#38 PRODUCED** `lesson_08_38_applying_warning.svg` (7,006 B; SVG `aw`; hand-coded; viewBox 0 0 800 680 dense-content variant; apply-is-destructive flow: LEFT live-modifier card positive-green `#4ADE80` (Base mesh + Subdivision chip `#B47EE8`, Editable/Removable/Reorderable benefits) → orange `#FF6B00` Apply arrow+pill → RIGHT applied card warning-red `#E63946` (flattened-mesh wireframe glyph, No-panel/No-undo consequences); "What you lose" warning panel = 3 red `!`-bullet items (panel disappears / base shape replaced / vertex count jumps); "When applying is safe" positive-green guidance panel (look final, need to sculpt/edit, export needs baked geometry, save a copy first); footer tip rail "keep modifiers live, apply at the end"; standalone keeps `color="#222"` on root for `<img>` fallback, stripped at INTEGRATION). **#31 PRODUCED** `lesson_08_31_common_stack_patterns.svg` (≈ stack-recipe infographic; SVG `sp`; hand-coded; viewBox 0 0 800 680; 2×2 grid of 4 named recipes, each an ordered chip list read bottom-to-top with first/last arrow + order rule + use case: 1 Hard-surface panel (Mirror→Bevel→Subdivision), 2 Fence or railing (Array→Mirror), 3 Organic/character (Mirror→Subdivision→Bevel), 4 Tiled grid (Array X→Array Y); modifier-chip colorway consistent across all cards so each modifier tracks by color: Mirror green `#4ADE80`, Bevel blue `#4A9EFF`, Subdivision purple `#B47EE8`, Array yellow `#F5C242`; footer tip "check order first"). **#29 PRODUCED** `lesson_08_29_modifier_order_diagram.svg` (≈ hero order-matters flowchart; SVG `mo`; hand-coded; viewBox 0 0 800 680; one shared source post splits to two parallel evaluation paths — Path A Array→Mirror (blue accent) yields two long mirrored rows (Result A), Path B Mirror→Array (purple accent) yields four spaced mirrored pairs (Result B); per-path step chips (Array yellow / Mirror green, matching #31), intermediate + result post glyphs with orange dashed mirror-axis lines, orange `different!` divergence arrow connecting Result A→B; footer tip "Blender reads a stack bottom to top"). All 3 rasterized to PNG and visually QA-checked (cairosvg, 800px wide): all read clean at thumbnail, chip-color consistency confirmed across #29/#31, no clipping/overlap defects. All 3 verified on disk via `get_file_info` (#38 7,006 B). **NO new PRODUCTION discoveries** — pure house-style SVG authoring per IMAGE_STYLE_GUIDE v5 (dense-content 800×680 variant, brand palette, middle-dot separators, `currentColor` text, top orange accent strip, footer tip Variant B left-rail, `role`/`aria-labelledby`/`<title>`/`<desc>` accessibility, `color="#222"` kept on standalone roots for `<img>` fallback). Placement rows #29/#31/#38 flipped to `produced; not integrated`; **Current state** updated to `Phase 7 PRODUCTION partial (Sub-chat C complete) · 30 of 40 produced · 27 of 40 integrated`. **2 PENDING decisions carried, none due in Phase 7 PRODUCTION:** #40 SVG-vs-PNG (due Phase 9); Phase 7-9 adjustments · left open. **Scene at close:** CLEAN (0 objects, BLENDER_EEVEE) — ready for Sub-chat D to build the stack-panel / order-comparison scenes. No `.blend` checkpoint (the bound `hat.blend` was NOT written). Next: **Sub-chat D** = produce #32→#30→#28 (PNG renders + UI chrome; verify live scene at D kickoff and clear again if Blender state drifted). Then Phase 7 **INTEGRATION** is its OWN chat per the ≥6 threshold (do NOT start INTEGRATION until all 6 figures produced). **INTEGRATION notes for the SVGs (Sub-chat C):** all 3 are standalone-file `<img>`-ready now; at INTEGRATION strip root `color="#222"`, add `preserveAspectRatio="xMidYMid meet"`, re-indent to section-content depth (figure col 20 / svg col 24 / svg children col 28 / nested `<g>` col 32 / figcaption col 24, identical to all prior L08 phases), collapse multi-line attr continuations, prune decorative comments / keep structural ones, preserve all `id`/`role`/`aria`/`<title>`/`<desc>` verbatim. Per-figure placement anchors from the requirements-doc Image Placement section (#29 after the order-impact explanation, #31 after the common-patterns card, #38 after the When to Apply Modifiers table per the doc's #38 placement).

- **2026-05-29** · Phase 7 PRODUCTION continuation closed (Sub-chat D). **Phase 7 PRODUCTION now COMPLETE (6 of 6 `#modifier-stacking` figures produced; all 3 Sub-chat D PNGs done).** Capture approach + run order locked in writing first (bottom-up #32 → #30 → #28). **Scene verified at kickoff = CLEAN exactly as Sub-chat C left it** (0 objects, 0 orphan meshes/mats/lights/cams, BLENDER_EEVEE; `hat.blend` bound but NOT written) — no drift, so no `bpy.data.objects.remove` clear needed; rebuilt the shared L08 base (world 0.88 grey, BLENDER_EEVEE, L08DemoGrey, key 600/area6 @ (-4,-5,7) + fill 250/area8 @ (5,-3,4), L08Cam lens 50 + L08CamTarget track-to). **#32 PRODUCED** `lesson_08_32_stack_management_icons.png` (85,954 B; 1018×410; stack-management header icon row across 3 modifier header rows Array/Mirror/Bevel — expand-caret / type-icon / display-toggles (edit-mode/realtime/render) / dropdown-v / X delete / drag-dots; staged `StackDemo` cube + 3-modifier stack Array(collapsed)→Mirror(collapsed)→Bevel(expanded), wrench/MODIFIER tab via `space.context='MODIFIER'`, local-tempdir `screen.screenshot` (1,028,153 B full window WITH overlays) → header-band crop (panel x44..553, y219..424) → 2× LANCZOS upscale; tab-column sliver trimmed; `c32` reserved). **#28 PRODUCED** `lesson_08_28_modifier_stack_panel.png` (93,952 B; 557×1157; full modifier-stack panel = Add Modifier button + 3-modifier stack in order Array→Mirror→Bevel + one settings body visible; all 3 collapsed for stack-emphasis, re-screenshot → **PROPERTIES-area crop geometry x2288 y53 w557 h1157 on a 2849×1538 window → PIL box (2288,328,2845,1485), SAME as #8/#14/#19/#23 this lesson**; `c28` reserved). **#30 PRODUCED** `lesson_08_30_modifier_order_comparison.png` (676,248 B; 2562×720; 2-panel same-stack-different-order = **Bevel→Subdivision (rounded cube) vs Subdivision→Bevel (sphere)**; `#FF6B00` 2-px Pillow divider; NO baked labels; `c30` reserved; render() → Pillow composite). **NEW L08 P7 Sub-chat D PRODUCTION discovery (order-demo legibility, flag for IMAGE_STYLE_GUIDE v6):** for a 2-panel “order matters” still, modifier-pair choice is decisive — (a) **same-axis Array+Mirror** reads only as a count/position difference (undramatic), and (b) **orthogonal Array-X + Mirror-Y is ORDER-INDEPENDENT** (the two orders rendered PIXEL-IDENTICAL, `ImageChops.difference` bbox None — the modifiers don't interact across orthogonal axes). The legible, dramatic demo is **Bevel+Subdivision on a cube** (Bevel→Subdivision = rounded cube with held corners; Subdivision→Bevel = sphere) — the two SILHOUETTES differ, reads instantly at thumbnail; recommend locking Bevel+Subdivision as the standard order-comparison still. (4 render iterations to reach this; same-axis and orthogonal variants both rejected.) **NO other new mechanisms** — reused local-tempdir-screenshot-carries-overlays for UI chrome (#32/#28, the L08 P3 path), `space.context` MODIFIER-tab switch, deterministic PROPERTIES-area crop (same geometry as #8/#14/#19/#23), L07 P1 `DRAW_WIN_SWAP` flush before each grab, `#FF6B00` 2-px Pillow divider, render→local-tempdir→Pillow-relay path. All 3 figures verified on disk via `get_file_info` (sizes above). Inspect copies (9) + local tempdir scratch (`l08_p7d`) cleaned. Placement rows #28/#30/#32 flipped to `produced; not integrated`; **Current state** → `Phase 7 PRODUCTION complete · 33 of 40 produced · 27 of 40 integrated`. **2 PENDING decisions carried, none due now:** #40 SVG-vs-PNG (due Phase 9); Phase 8-9 adjustments · left open. **Scene at close:** NOT cleaned — holds `OrderA` (Bevel→Subdivision) + `OrderB` (Subdivision→Bevel) demo cubes (shade-smooth) + `L08Cam` + `L08CamTarget` + `L08Key` + `L08Fill` + `L08DemoGrey` mat (now blue base 0.30/0.50/0.80, world reset to 0.82 during #30 iterations); BLENDER_EEVEE; `hat.blend` bound but NOT written. Phase 7 INTEGRATION is pure HTML work (does not need the live mesh). **Next chat: Phase 7 INTEGRATION (its OWN chat per the ≥6 threshold; all 6 figures #28/#29/#30/#31/#32/#38 into `#modifier-stacking`; plan-lock in writing first).** INTEGRATION notes: 3 SVGs (#29 `mo` / #31 `sp` / #38 `aw`, viewBox 0 0 800 680) inline per locked SVG conventions (strip root `color="#222"`, add `preserveAspectRatio="xMidYMid meet"`, re-indent figure col 20 / svg col 24 / svg children col 28 / nested `<g>` col 32 / figcaption col 24, preserve id/role/aria/`<title>`/`<desc>`); 3 PNGs need `c{N}` overlays at plan-lock (#28 small-frame multi-callout on the stack panel, fs-floor 16 per the 557-wide #8/#14/#19/#23 precedent; #32 callouts on the header icon row, small-frame fs-floor; #30 composite-overlay 2-panel order labels, viewBox 2562×720 → fs 29, e.g. “Bevel → Subdivision” / “Subdivision → Bevel”). Re-verify all 6 anchors against live HTML (204,011 B) at plan-time; bare yellow-card openers are NOT unique in this file — extend anchors with the following `<h3>`/`<h4>`.

- **2026-05-29** · Phase 7 INTEGRATION closed. 6 of 6 Phase 7 figures integrated into `lesson_08_modifiers_introduction.html` (`#modifier-stacking`). HTML size 204,011 B to 246,284 B (+42,273 B across 6 figures). Plan locked in writing first; Ray "lock and execute" (all 3 open calls taken at recommendation: #28 third callout = stack-order/processing-direction, #32 = 4 discrete callouts drag/caret/dropdown/X, #30 = positive-green). Live HTML re-verified at plan-time (204,011 B; `#modifier-stacking` section lines 1971–2213; section-content indent col 20, identical to all prior L08 phases so the locked column scheme carries verbatim); all 6 anchors extended to their following `<h3>`/`<h4>` and grep-confirmed unique (1 match each) — bare card-close / yellow-card openers are NOT unique in this file. Per-figure deltas (bottom-up execution order #38 → #32 → #30 → #29 → #28; the Why-Order pair committed #30 then #29 so #29 ends up ABOVE #30 in reading order): **#38** inline SVG `aw` between the "When to Apply Modifiers" table card `</div>` and the "⚠️ Before Applying: Always Duplicate!" yellow card; viewBox 0 0 800 680; `color="#222"` stripped from root + `preserveAspectRatio="xMidYMid meet"` added; re-indented (figure col 20 / svg col 24 / svg children col 28 / nested `<g>` col 32 / figcaption col 24); all structural comments preserved verbatim; id/role/aria/`<title>`/`<desc>` preserved (+9,601 B). **#32** `c32` small-frame multi-callout 4 between the "Managing Complex Stacks" card `</div>` and `<h3>Performance Considerations</h3>`; viewBox 1018×410; **small-frame fs floor 16** (strict formula → ~12, floored for legibility); 4 callouts label drag-dots (reorder) / caret (collapse·expand) / dropdown (more options) / X (delete modifier); badges in 2 tiers (y=14 top row, y=160 lower row) to avoid overlap on the wide-short frame; IDs `c32-highlight/leader/marker/badge-1..4` (+4,076 B). **#30** `c30` composite-overlay 2-panel positive-green between the "🎯 Understanding the Difference" blockquote `</blockquote>` and `<h3>The Golden Rule: Deform Last</h3>`; viewBox 2562×720; fs 29 per `fs = round(22 × 2562 / 1920)`; labels "Bevel → Subdivision" / "Subdivision → Bevel" (positive-green default — both orders valid, neither "wrong"); 2-panel centers 640·1922; rect 440×80 at center−220 / y=600 / text y=640; IDs `c30-badge-1..2` (+1,818 B). **#29** inline SVG `mo` between the same blockquote `</blockquote>` and the #30 figure (so #29 diagram reads ABOVE #30 render proof); viewBox 0 0 800 680; `color="#222"` stripped + `preserveAspectRatio` added; re-indented to the locked depth; structural comments + id/role/aria/`<title>`/`<desc>` preserved verbatim; content uses Array/Mirror two-path flow (matches the lesson body's Mirror↔Array "Why Order Matters" example), complementing #30's Bevel/Subdivision render proof (+11,509 B). **#31** inline SVG `sp` between the "Common Modifier Stack Patterns" card `</div>` and `<h3>Why Order Matters: A Practical Example</h3>`; viewBox 0 0 800 680; `color="#222"` stripped + `preserveAspectRatio` added; re-indented; all 4 pattern dividers + chip-position structural comments + accessibility attrs preserved verbatim (+12,073 B). **#28** `c28` small-frame multi-callout 3 between the "📊 How the Stack Works" card `</div>` and `<h3>Reordering Modifiers</h3>`; viewBox 557×1157 (small frame); **small-frame fs floor 16** (per the 557-wide #8/#14/#19/#23 precedent); 3 callouts label Add Modifier button / Modifier header row / Stack order (top runs first); IDs `c28-highlight/leader/marker/badge-1..2` + a `c28-leader-3`/`c28-marker-3` (triangle)/`c28-badge-3` stack-order callout (+3,196 B). Bottom-up order held; no anchor shifts. Dry-run → commit → `get_file_info` verify per figure (6 checkpoints: 213,612 → 217,688 → 219,506 → 231,015 → 243,088 → 246,284 B). All 6 placement-table rows flipped to `integrated`. **NO new INTEGRATION discoveries** — every pattern reused from locked precedents: SVG inline conventions (strip `color="#222"` / add `preserveAspectRatio` / re-indent to section-content depth / preserve comments + id/role/aria/`<title>`/`<desc>`), composite-overlay font-size formula `fs = round(22 × viewBox_w / 1920)` (#30), c12-precedent 2-panel rect geometry, positive-green-default colorway, small-frame fs-floor sub-rule (#28/#32, 16), per-callout id naming `c{N}-highlight[-K]`/`-leader[-K]`/`-marker[-K]`/`-badge[-K]`, inline-stacked overlay pattern, indent-depth-derivation rule (section-content col 20, identical to L07). The #29/#30 pair-ordering (commit lower-line figure first so the SVG lands above the PNG in reading order) is the only execution nuance, handled within the locked bottom-up cadence. **Standing QA flags (estimated-position visual-proof, browser-QA plausibility check, NO live pixel-scan against decoded PNGs this chat):** #28 c28 three callout ellipses/markers (Add Modifier button cy96 / header row cy230 / stack-order leader to cy360, estimated on the 557×1157 panel) and #32 c32 four header-row ellipses (cx 86/150/878/956, cy96, estimated on the 1018×410 crop) → add to the standing estimated-position QA list. #30 c30 composite-overlay badges use deterministic panel-center geometry (no estimate) → no flag; #29/#31/#38 are inline SVG diagrams (self-contained, no PNG-position estimate) → no flag. **2 PENDING decisions carried:** #40 SVG-vs-PNG (due Phase 9); Phase 8-9 adjustments · left open. Next chat: Phase 8 PRODUCTION (`#project`; 5 figures #33/#34/#35/#36/#37; sub-chat split required per ≥5 threshold). **Scene-state carry (from Phase 7 PRODUCTION Sub-chat D close, unchanged — INTEGRATION did not touch Blender):** holds `OrderA` (Bevel→Subdivision) + `OrderB` (Subdivision→Bevel) demo cubes + `L08Cam` + `L08CamTarget` + `L08Key` + `L08Fill` + `L08DemoGrey` mat (blue base 0.30/0.50/0.80, world 0.82); BLENDER_EEVEE; `hat.blend` bound but NOT written; verify live + clear via `bpy.data.objects.remove` (never `read_factory_settings`) at Phase 8 PRODUCTION kickoff.

- **2026-05-29** · Phase 8 PRODUCTION partial (Sub-chat B complete · finish-up chat). Sub-chat B's 4 figures (#33/#34/#36/#37) were produced + on-disk-verified in a prior chat; this chat ran the two finish-up tasks that chat ran out of room for (cleanup + doc writes · nothing rendered). **Cleanup:** deleted the 4 inspect sheets from WSL `images/` (`_inspect_e37_sheet.png` 35,310 B / `_inspect_e36_sheet.png` 63,108 B / `_inspect_e34_sheet.png` 59,700 B / `_inspect_e33.png` 9,365 B — all confirmed present then removed; #33 absence re-verified via `get_file_info` ENOENT); local tempdir `C:\Users\pract\AppData\Local\Temp\l08_p8b` was already ABSENT (no action needed). **Scene verified clean-base at kickoff, no drift:** `get_scene_info` read 18 objects = the 5-object base (`FencePost` + `L08Cam`/`L08CamTarget`/`L08Key`/`L08Fill`, `L08DemoGrey` mat, BLENDER_EEVEE, `hat.blend` bound NOT written) plus the 13 leftover experiment objects; cleared the `E34_/E36_/E37_` set only via `bpy.data.objects.remove` + orphan purge (13 objects removed — E34_p1_half/p2_mirror/p3_array/p4_bevel/p5_subdiv/p6_final + E36_original/morepost/wider/rounder + E37_correct/subfirst/bevfirst; "Deleted 28 data-block(s)"; base untouched — post-purge counts mesh/mat/light/cam 1/1/2/1, remaining objects FencePost + L08Cam + L08CamTarget + L08Fill + L08Key). **Sub-chat B production deltas (carried from the producing chat for INTEGRATION reference):** all 4 verified on disk — #33 `lesson_08_33_project_base_post.png` 655,792 B 1920×1080 single render NO overlay; #34 `lesson_08_34_project_phase_progression.png` 1,541,845 B 6010×720 (6 panels @1000px + 5×2px `#FF6B00` dividers) `c34` reserved; #36 `lesson_08_36_fence_variations.png` 1,380,024 B 5126×720 (4 panels @1280 + 3 dividers) `c36` reserved; #37 `lesson_08_37_stack_order_experiments.png` 691,287 B 3844×720 (3 panels @1280 + 2 dividers) `c37` reserved; NO baked labels on any; `#FF6B00` 2-px dividers on the 3 composites, #33 no divider/overlay. **#34 decision locked at recommendation** = half-post base → X-mirror completes it, then X-array / +bevel / +subdiv / +final (X-bisect required so the cut face is camera-facing; Y-bisect hid the cut and the mirror didn't read). **#37 applied the locked P7 #30 order-legibility discovery** — strengthened params (subsurf L3, bevel 0.12) + reframed camera onto the row midpoint so the 3 orders diverge (correct = sharp / subdivision-first = rounded-blobby / bevel-first = chunkier). **#36** fit-to-width per panel. Overlay reservations unchanged. **INTEGRATION-note carry-forward (for Phase 8 INTEGRATION plan-lock):** fs per `fs = round(22 × viewBox_w / 1920)` ⇒ #34 fs 69 (6010 wide; widen any over-long label badge per the #18 5-panel precedent), #36 fs 59 (5126), #37 fs 44 (3844); #33 plain `<img>` no overlay; placement anchors from requirements-doc `### Section: project`: #33 after "Project Overview" para · #34 after "Phase 6: Add Subdivision Surface" card · #35 after "Understanding Your Modifier Stack" card · #36 after "Customization Ideas" card · #37 after "Try This: Reorder Modifiers" card. **Phase 8 PRODUCTION is NOT complete:** #35 is Sub-chat A (panel grab + split-view render), a separate chat — it must land before Phase 8 PRODUCTION closes. **Current state** → `Phase 8 PRODUCTION partial (Sub-chat B complete) · 37 of 40 produced · 33 of 40 integrated`; rows #33/#34/#36/#37 flipped to `produced; not integrated` (row #35 stays `not produced; not integrated`). **1 PENDING decision carried:** #40 SVG-vs-PNG (due Phase 9). **Scene at close:** clean-base only (5 objects above, no E-experiments); BLENDER_EEVEE; `hat.blend` bound but NOT written. INTEGRATION is its OWN chat AFTER all 5 `#project` figures are produced. Next: Phase 8 PRODUCTION Sub-chat A (#35).

- **2026-05-29** · Phase 8 PRODUCTION Sub-chat A closed. **Phase 8 PRODUCTION now COMPLETE (5 of 5 `#project` figures produced).** Sub-chat A = #35 only (panel grab + split-view render). **Scene verified clean-base at kickoff, no drift** = the 5-object base (`FencePost` + `L08Cam`/`L08CamTarget`/`L08Key`/`L08Fill`, `L08DemoGrey` mat base (0.55,0.55,0.58) rough 0.45, world 0.88 grey, BLENDER_EEVEE, `hat.blend` bound NOT written); FencePost was a clean no-modifier base post (16 v / 15 f, 0.4×0.4×2.0); only harmless delta was `L08Cam` drifted to (2.67,−4.08,2.81) — reset for the render; no E-experiment objects present so no `bpy.data.objects.remove` clear needed. **Capture method = split-view composite** (panel grab + finished-fence render) per the requirements-doc `### Section: project` #35 spec ("Project object with full modifier stack visible (Mirror, Array, Bevel, Subdivision)" · Prompt 35 = split view, left modifier panel + right result); chosen over a synthetic composite because #35 is real UI chrome + a real render, both deliverable via the locked L08 capture paths. **#35 PRODUCED** `lesson_08_35_project_modifier_stack.png` (618,357 B; 1839×1157; split-view = LEFT modifier-stack panel grab (557×1157) | `#FF6B00` 2-px divider | RIGHT finished-fence render (1280×1157); NO baked labels per no-bake rule; `c35` overlay reserved for INTEGRATION). **Project stack built fresh on FencePost** in locked project order (bottom→top in panel = first→last evaluated): Mirror (Y axis + clip, collapsed) → Array (count 6, rel-offset X 1.6, collapsed) → Bevel (width 0.02, seg 2, Limit Method Angle 30°, collapsed) → Subdivision (levels 2 / render 2, EXPANDED to show the settings body), shade_smooth; eval bbox x −0.22→3.42 (6-post row). RIGHT render: L08Cam @ (1.6+3.4, −8.4, 4.0) lens 50 + L08CamTarget @ (1.6,0,1.0) track-to (3/4 row view, framed on row midpoint x=1.6); key bumped 600→700 / fill 250→320 for edge contrast on the 0.88 light-grey world; render() to local Windows tempdir → Pillow optimize-save relay to WSL. LEFT panel: FencePost active+selected, Properties editor switched to MODIFIER/wrench tab via `space.context='MODIFIER'`, `DRAW_WIN_SWAP` flush, `bpy.ops.screen.screenshot` to local Windows tempdir (1,062,854 B full window WITH panel overlays) → deterministic PROPERTIES-area crop **window 2849×1538, area x2288 y53 w557 h1157 → PIL box (2288,328,2845,1485) = 557×1157, SAME geometry as #8/#14/#19/#23/#28 this lesson**; QA-viewed: breadcrumb FencePost>Subdivision, wrench tab active blue, Add Modifier button + all 4 modifiers in stack order (Mirror/Array/Bevel/Subdivision), Subdivision expanded showing Catmull-Clark + Levels Viewport 2 / Render 2 + Optimal Display. Pillow-composited the two halves with a `#FF6B00` 2-px divider (final 557+2+1280 = 1839 wide). **NO new L08 P8 Sub-chat A PRODUCTION discoveries** — every mechanism reused from locked precedents: local-tempdir-screenshot-carries-overlays for UI chrome (the L08 P3 path), `space.context` MODIFIER-tab switch, deterministic PROPERTIES-area crop (same geometry as #8/#14/#19/#23/#28), L07 P1 `DRAW_WIN_SWAP` flush before the grab, render→local-tempdir→Pillow-relay path, `#FF6B00` 2-px Pillow divider, no-bake-callouts. The split-view (real panel grab beside a real render, joined in Pillow) is the one composition nuance — it is just the panel-crop path + the render path combined, no new mechanism. **#35 verified on disk via `get_file_info` (618,357 B).** Inspect copies (`_inspect_35_render`/`_panel`/`_composite`) + local tempdir scratch (`l08_p8a_*`) cleaned. Row #35 flipped to `produced; not integrated`; **Current state** → `Phase 8 PRODUCTION complete · 38 of 40 produced · 33 of 40 integrated`. **INTEGRATION-note carry-forward for #35 (Phase 8 INTEGRATION plan-lock):** #35 takes a `c35` overlay; pick the overlay variant + fs per the locked formula `fs = round(22 × viewBox_w / 1920)` at the figure's final viewBox width — BUT if #35 is treated as a small-frame UI panel (the LEFT half is a 557-wide panel), apply the small-frame fs-floor 16 (the 557-wide #8/#14/#19/#23/#28 precedent). Likely variant: callouts on the LEFT panel labeling the 4 stack entries (Mirror/Array/Bevel/Subdivision) and/or a split-view label pair (e.g. "Modifier stack" / "Result"); decide at plan-lock. The other 4 Phase-8 INTEGRATION notes sit in the Sub-chat B Status bullet (#34 fs 69 / #36 fs 59 / #37 fs 44 / #33 plain `<img>` no overlay + all 5 placement anchors: #33 after "Project Overview" para · #34 after "Phase 6: Add Subdivision Surface" card · #35 after "Understanding Your Modifier Stack" card · #36 after "Customization Ideas" card · #37 after "Try This: Reorder Modifiers" card). **1 PENDING decision carried:** #40 SVG-vs-PNG (due Phase 9). **Scene at close:** NOT cleaned — holds `FencePost` (now carrying the full project stack Mirror→Array→Bevel→Subdivision, active) + `L08Cam` (@ (5.0,−8.4,4.0)) + `L08CamTarget` (@ (1.6,0,1.0)) + `L08Key` (700) + `L08Fill` (320) + `L08DemoGrey` mat; BLENDER_EEVEE; `hat.blend` bound but NOT written; verify live + clear via `bpy.data.objects.remove` (never `read_factory_settings`) at next PRODUCTION kickoff (Phase 9). **Phase 8 PRODUCTION is COMPLETE — Phase 8 INTEGRATION is its OWN chat next** (all 5 `#project` figures #33/#34/#35/#36/#37 into `#project`; re-verify all 5 anchors against live HTML (246,284 B) at plan-time; bare card-close / yellow-card openers are NOT unique in this file — extend anchors with the following `<h3>`/`<h4>`).

- **2026-05-29** · Phase 8 INTEGRATION closed. 5 of 5 Phase 8 figures integrated into `lesson_08_modifiers_introduction.html` (`#project`). HTML size 246,284 B to 260,668 B (+14,384 across 5 figures). Plan locked in writing first; Ray "lock and execute" (3 open calls taken at recommendation: #35 = 4 callouts on the LEFT panel labeling the stack entries [over the split-view label pair]; #37 labels "Correct order / Subdivision first / Bevel first"; #36 labels "Base fence / More posts / Wider spacing / Rounder posts"). Live HTML re-verified at plan-time (246,284 B; `#project` section lines 2608–2868; section-content indent col 20, identical to all prior L08 phases); all 5 anchors extended to their following `<h3>` and grep-confirmed unique (1 match each). **#36 label correction caught at plan-time:** the produced E36 panels are `original / morepost / wider / rounder` (panel 1 is the baseline, panel 4 is rounder; NO 'ornate top' panel) — labels set to match the pixels ("Base fence / More posts / Wider spacing / Rounder posts"), same estimated-position-visual-proof principle the QA pass enforces. Per-figure deltas (bottom-up execution order #37 → #36 → #35 → #34 → #33): **#37** composite-overlay 3-panel positive-green between the "✅ Try This: Reorder Modifiers" card `</div>` and `<h3>Customization Ideas</h3>`; viewBox 3844×720; fs 44 per `fs = round(22 × 3844 / 1920)`; labels "Correct order" / "Subdivision first" / "Bevel first"; c12-precedent rect geometry (440×100 at center−220 / y=580 / text y=630; centers 640·1922·3204); IDs `c37-badge-1..3` (+2,564 B). **#36** composite-overlay 4-panel positive-green between the "🎨 Customization Options" card `</div>` and `<h3>Bonus Challenges</h3>`; viewBox 5126×720; fs 59 per `fs = round(22 × 5126 / 1920)`; labels "Base fence" / "More posts" / "Wider spacing" / "Rounder posts"; centers 640·1922·3204·4486; IDs `c36-badge-1..4` (+2,920 B). **#35** small-frame multi-callout 4 positive-green between the "🔍 Your Modifier Stack Explained" card `</div>` and `<h3>Experiment: Change the Order</h3>`; viewBox 1839×1157 (split-view; LEFT 557-wide panel); **small-frame fs floor 16** applied (LEFT half 557-wide per the #8/#14/#19/#23/#28 precedent); 4 callouts label the stack entries Mirror / Array / Bevel / Subdivision (ellipses on the LEFT panel header rows cy 210/300/390/480, badges placed in the render gutter at x=600); IDs `c35-highlight/leader/marker/badge-1..4` (+4,309 B). **#34** composite-overlay 6-panel positive-green between the "📝 Phase 6: Add Subdivision Surface" card `</div>` and `<h3>Understanding Your Modifier Stack</h3>`; viewBox 6010×720 (six 1000-wide panels + 5×2px dividers); fs 69 per `fs = round(22 × 6010 / 1920)`; labels "1. Base post" / "2. +Mirror" / "3. +Array" / "4. +Bevel" / "5. +Subdivision" / "6. Final"; panel centers 500·1502·2504·3506·4508·5510 (1000-wide panel geometry, NOT the 1280-wide c12 centers); **badge-1 widened to 520 and badge-5 widened to 760 per the #18 long-label precedent** ("1. Base post" and "5. +Subdivision" overflow the 440 rect at fs 69); badge-5 rect x 4128–4888 fits within panel 5 (x 4008–5008); IDs `c34-badge-1..6` (+3,979 B). **#33** plain `<img>` NO overlay between the end of the Project Overview `<ul></ul>` and `<h3>Step-by-Step: Building the Fence</h3>` (+612 B). Bottom-up order held; no anchor shifts. Dry-run → commit → `get_file_info` verify per figure (5 checkpoints: 248,848 → 251,768 → 256,077 → 260,056 → 260,668 B). All 5 placement-table rows flipped to `integrated`. **One in-draft self-correction caught at dry-run (no bad commit):** #34's first draft figcaption used em-dashes around the modifier list ("stack — Mirror … Surface — building"); re-issued with middle-dot `·` separators per the no-new-em-dashes rule before any commit; the only structural em-dash anywhere in the new content is none (all new figcaptions use `·`). **NO new INTEGRATION discoveries** — every pattern reused from locked precedents: composite-overlay font-size formula `fs = round(22 × viewBox_w / 1920)` (#34/#36/#37), c12-precedent rect geometry, the #18 long-label badge-widen precedent (#34 badge-1/5), 1000-wide-panel center derivation (#34, from the produced 6010-wide / six-1000-panel geometry), positive-green-default colorway, small-frame fs-floor sub-rule (#35, 16), per-callout id naming `c{N}-highlight[-K]`/`-leader[-K]`/`-marker[-K]`/`-badge[-K]`, inline-stacked overlay pattern, indent-depth-derivation rule (section-content col 20 · figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24, identical to L07), no-new-em-dashes rule. Final live-file post-checks (re-decoded copy): 5 `<figure>`/5 `</figure>` balanced in `#project`, all 5 imgs present once, 29 `c33`–`c37` ids all unique doc-wide, all 5 figcaptions middle-dot (no new em-dash), document-order figure→heading sequence correct (#33 after Project Overview · #34 after Step-by-Step/Phase 6 · #35 after Understanding Your Modifier Stack · #37 after Experiment · #36 after Customization Ideas). **Standing QA flags (estimated-position visual-proof, browser-QA plausibility check, NO live pixel-scan against decoded PNGs this chat):** #35 c35 four panel-callout ellipses (cy 210/300/390/480 estimated on the LEFT 557-wide panel) → add to the standing estimated-position QA list. #34/#36/#37 composite-overlay badges use deterministic panel-center geometry (no estimate) → no flag; #33 plain `<img>` no overlay → no flag. **1 PENDING decision carried + now DUE next chat:** **DECISION 1 (#40 SVG-vs-PNG `tg`-flip) is DUE at Phase 9** — left open here; resolve at Phase 9 plan-lock (flip to hand-coded SVG `tg` per the L06 #44 `sr` precedent → split 6 SVG / 34 PNG, OR hold as PNG `c40` → split 5 SVG / 35 PNG). Next chat: **Phase 9 PRODUCTION** (`#summary`; 2 figures · #39 SVG `wf` hand-coded + #40 [SVG `tg` or PNG, DECISION 1 DUE]; <5 so no sub-chat split). **Scene-state carry (from Phase 8 PRODUCTION Sub-chat A close, unchanged — INTEGRATION did not touch Blender):** holds `FencePost` (carrying the full project stack Mirror→Array→Bevel→Subdivision, active) + `L08Cam` (@ (5.0,−8.4,4.0)) + `L08CamTarget` (@ (1.6,0,1.0)) + `L08Key` (700) + `L08Fill` (320) + `L08DemoGrey` mat; BLENDER_EEVEE; `hat.blend` bound but NOT written; verify live + clear via `bpy.data.objects.remove` (never `read_factory_settings`) at Phase 9 PRODUCTION kickoff.

- **2026-05-29** · Phase 9 PRODUCTION closed. **Phase 9 PRODUCTION COMPLETE (2 of 2 `#summary` figures produced · #39 + #40) → 40 of 40 produced.** No sub-chat split (2 figures < 5). **DECISION 1 (#40 SVG-vs-PNG `tg`-flip) RESOLVED = FLIP to hand-coded SVG `tg`** (Ray "proceed with your recommendations"); rationale = #40 is a text-heavy problem/solution troubleshooting table, the same structured-reference class the course consistently inlines as hand-coded SVG (L06 #44 `sr` precedent + L08 #31 `sp`), so a PNG capture's baked text would fail the no-bake principle and lose accessibility/scalability; Canva (available) is a raster fallback NOT usable for inline SVG. **Final split = 6 SVG / 34 PNG** (SVG-vs-PNG split table + ID prefix registry + placement-table overlay column for #40 all updated; `c40` PNG namespace retired). **No Blender this phase** (both figures hand-coded SVG) — the live scene was NOT touched (FencePost project stack + cam/lights/mat carry forward unchanged from Phase 8 close). **#39 PRODUCED** `lesson_08_39_workflow_flowchart.svg` (6,399 B; SVG `wf`; hand-coded; viewBox 0 0 800 680 dense-content variant per the locked L08 SVG standard #1/#29/#31/#38; decision-tree workflow flowchart: START "Need to change geometry?" orange pill → D1 "Repetitive or symmetric?" diamond (No → "Edit manually" neutral terminal) → D2 "Add a modifier" (Array·Mirror·Bevel·Subdiv) → D3 "Need baked geometry?" (No·keep working → "Keep it live" green terminal / Yes·it is final → "Duplicate first, then Apply" red terminal with Shift+D note); house-palette color-coding green `#4ADE80` keep-live, yellow `#F5C242` decision diamonds, red `#E63946` apply, orange `#FF6B00` arrows/accents/start; `<marker>` arrowhead def; green default-to-live guidance rail + orange footer-tip "stack bottom to top"; standalone keeps `color="#222"` on root for `<img>` fallback, stripped at INTEGRATION). **#40 PRODUCED** `lesson_08_40_troubleshooting_guide.svg` (8,097 B; SVG `tg`; hand-coded; viewBox 0 0 800 680 dense-content variant; two-column PROBLEM/FIX troubleshooting table, 5 rows each red `!`-circle problem + green check-circle fix: gap at mirror seam → enable Clipping · bevel does nothing → raise Amount/apply scale (Ctrl+A) · array copies overlap → increase Relative Offset · subdivision bloated → add supporting loops/Edge Crease · modifier no effect → turn on viewport toggle; red `#E63946` problem indicators + green `#4ADE80` solution indicators + `#4A9EFF`/`#4ADE80` accent on key terms; blue `#4A9EFF` "When in doubt" check-order-first reminder panel + orange footer tip; standalone keeps `color="#222"` for `<img>` fallback). **Both rasterize-QA'd via cairosvg ~800px BEFORE disk-verify** (the carried SVG-QA cadence): clean parse, no clipping/overlap after fixes. **In-production fixes (caught at raster-QA, before disk-write):** #39 — START pill widened 200→248 px and Apply terminal box 200→228 px to stop text clipping at both ends; "No · keep working" edge label nudged left (x 246→196) to clear the D3 diamond. #40 — five FIX-column secondary descriptions ran past the 800-wide viewBox right edge; all five shortened to fit (rows 1/2/3/4/5), re-rendered clean. Both written to WSL `images/` via `Filesystem:write_file` and disk-verified via `get_file_info` (#39 6,399 B · #40 8,097 B, exact). **NO new PRODUCTION discoveries** — pure house-style SVG authoring per the locked conventions (dense-content 800×680 variant, brand palette, middle-dot separators, `currentColor` text, top orange accent strip, footer-tip Variant-B left rail, `role`/`aria-labelledby`/`<title>`/`<desc>` accessibility, `color="#222"` kept on standalone roots for `<img>` fallback) + cairosvg rasterize-QA cadence; chip/term colors consistent with #29/#31/#38 (Mirror green / Bevel blue / Subdivision purple / Array yellow). Placement rows #39/#40 flipped to `produced; not integrated`; **Current state** → `Phase 9 PRODUCTION complete · 40 of 40 produced · 38 of 40 integrated`. **0 PENDING decisions remain** (DECISION 1 now RESOLVED; #6 dual-reference + phase-plan adjustments all closed in prior phases). **Scene at close:** untouched — still holds `FencePost` (full project stack Mirror→Array→Bevel→Subdivision, active) + `L08Cam` (@ (5.0,−8.4,4.0)) + `L08CamTarget` (@ (1.6,0,1.0)) + `L08Key` (700) + `L08Fill` (320) + `L08DemoGrey` mat; BLENDER_EEVEE; `hat.blend` bound but NOT written. Phase 9 INTEGRATION is pure HTML work (needs no Blender). Next chat: **Phase 9 INTEGRATION** (`#summary`; 2 figures #39 SVG `wf` + #40 SVG `tg`; single sub-chat per <6 threshold; **FINAL phase — closes L08 image work, 40/40**). **INTEGRATION notes:** both are standalone-file `<img>`-ready now; at INTEGRATION inline per the locked SVG conventions — strip root `color="#222"`, add `preserveAspectRatio="xMidYMid meet"`, re-indent to section-content depth (figure col 20 / svg col 24 / svg children col 28 / nested `<g>` col 32 / figcaption col 24, identical to all prior L08 phases), collapse multi-line attr continuations, prune decorative comments / keep structural ones, preserve all `id`/`role`/`aria`/`<title>`/`<desc>` + the `<defs><marker>` (#39) verbatim. **Placement anchors** (requirements-doc `### Section: summary`): #39 after the "Professional Modifier Workflow" checklist · #40 referenced within the "Common Questions Answered" section. **Re-verify both anchors against live HTML (260,668 B) at INTEGRATION plan-time** — bare card-close / yellow-card openers are NOT unique in this file; extend every anchor with the following `<h3>`/`<h4>` and grep-confirm 1 match each. Both are inline SVG diagrams (self-contained, no PNG-position estimate) → NO estimated-position QA flag.

- **2026-05-29** · Phase 9 INTEGRATION closed. **FINAL phase — L08 image work COMPLETE at 40 of 40 integrated.** 2 of 2 Phase 9 figures inlined into `lesson_08_modifiers_introduction.html` (`#summary`). HTML size 260,668 B to 280,049 B (+19,381 B across 2 figures: #39 +8,611 B · #40 +10,770 B). Live HTML re-verified at plan-time (260,668 B; `#summary` section starts line 2986; section-content indent col 20, identical to all prior L08 phases). Both anchors extended past their following block and grep-confirmed unique (1 match each). **Document order note:** within `#summary`, "Common Questions Answered" (`<h3>`, line ~3064) precedes the "Professional Modifier Workflow" checklist (line ~3144), so #40's anchor sits EARLIER in the file than #39's — executed #39 first (bottom-up, later-in-file) so #40's line numbers did not shift. Per-figure deltas (execution order #39 then #40): **#39** inline SVG `wf` between the "✅ Professional Modifier Workflow" checklist `</ol></div>` and `<h3>Coming Up Next</h3>`; viewBox 0 0 800 680; `color="#222"` stripped from root + `preserveAspectRatio="xMidYMid meet"` added; re-indented (figure col 20 / svg col 24 / svg children col 28 / nested `<g>`/`<defs>` col 32 / deepest `<marker><path>` col 36 / figcaption col 24); `<defs><marker id="wf-arrow">` + all structural comments + id/role/aria/`<title>`/`<desc>` preserved verbatim (+8,611 B). **#40** inline SVG `tg` between `<h3>Common Questions Answered</h3>` and the first Q&A `<div class="card">` ("When should I use modifiers vs. manual modeling?"); viewBox 0 0 800 680; `color="#222"` stripped + `preserveAspectRatio` added; re-indented to the same locked depth; all 5 PROBLEM/FIX row `<g>` groups + structural comments + id/role/aria/`<title>`/`<desc>` preserved verbatim (+10,770 B). Bottom-up order held; no anchor shifts. Dry-run → commit → `get_file_info` verify per figure (2 checkpoints: 269,279 → 280,049 B). Both placement-table rows flipped to `integrated`; **Current state** → `L08 image work COMPLETE · 40 of 40 produced · 40 of 40 integrated`. **NO new INTEGRATION discoveries** — pure reuse of the locked SVG inline conventions (strip root `color="#222"` / add `preserveAspectRatio="xMidYMid meet"` / re-indent to section-content depth / preserve comments + id/role/aria/`<title>`/`<desc>` + `<defs><marker>`), the inline-stacked figure wrapper, the indent-depth-derivation rule (section-content col 20, identical to L07), and the no-new-em-dashes rule (both new figcaptions use middle-dot `·`). Built each inlined block deterministically from the standalone file and validated as well-formed XML BEFORE committing; re-validated both in the live file after commit (wf-title 8,259 chars / tg-title 10,471 chars, both well-formed). **Final live-file post-checks (re-decoded copy):** `#summary` has 2 `<figure>`/2 `</figure>` balanced, 2 `<svg>`/2 `</svg>`; ids wf-title/wf-desc/wf-arrow/tg-title/tg-desc all unique doc-wide (1 each); zero `color="#222"` remaining doc-wide; document order tg (Common Questions) before wf (Workflow checklist) confirmed; whole-doc em-dash count 51 = all pre-existing source em-dashes, zero added (both new figcaptions middle-dot, no em-dash). **Both are inline SVG diagrams (self-contained, no PNG-position estimate) → NO estimated-position QA flag added.** **0 PENDING decisions remain.** **Scene at close:** untouched (no Blender this phase) — still holds the Phase-8 `FencePost` project stack + cam/lights/mat; `hat.blend` bound but NOT written. **Next chat: NONE required for L08 image work — the pipeline is COMPLETE (40/40 produced + integrated).** Optional follow-ups (Ray's call): a browser-QA pass for L08 against the standing estimated-position flags accumulated across phases (P2 #3/#5/#7 · P3 #8 · P4 #14/#17 · P5 #19/#20/#21 · P6 #23 · P7 #28/#32 · P8 #35; the #summary SVGs carry no such flag), or Lesson 09 (Precision Modeling Techniques) image-pipeline kickoff.

- **2026-06-14** · **L08 BROWSER-QA DONE · PASS.** Full Playwright + real-Chromium (Chromium-1194) sweep, dark+light × 1920/1024/414. **Entry-state reconciled:** live P0 280,167 B vs doc 280,049 B = benign Jun-12 batch-touch +118 B, body sound (all 40 figs/overlays/4 mermaid present); 280,167 B authoritative; em-dash baseline 51 held. **DEFECT A** = done + verified prior session (carried DONE). **DEFECT B FIXED + COMMITTED this session** = the 5 `#subdivision-surface` overlays #18/#19/#20/#21/#22 (all other overlays already clean), two coupled causes per figure: (a) appended ` margin: 0;` to each img style → now `display: block; width: 100%; height: auto; margin: 0;`; (b) moved each `<figcaption>` from INSIDE the wrapper div to a direct `<figure>` child after `</div>` (figcaption col 24, `</figure>` col 20, matching the already-clean #24). HTML **280,167 → 280,202 B (+35)**; edit_file dryRun→commit→get_file_info. Post-commit integrity re-read CLEAN: em-dash 51 held, figure 41/41, figcaption 41/41, img 35, svg 37, `color="#222"` 0, `margin: 0` 18→23. **Harness rebuilt + re-run → PASS:** #18–#22 all dx/dy/dw/dh = 0.00 every combo (prior dy−16 / dh 55–124 gone); all 23 measured img-vs-svg overlay pairs worst |dy|=0 |dh|=0 |dw|≤0.08; mermaid 4/4; 35/35 imgs; 0 console errors; 414 overflow 0px. **Selector-scope note:** the pair-selector measured 23 of the ~26 overlays — the 8 synthetic/composite overlays (c7/c17 cages + multi-panel composite-overlay badge groups) whose `<svg>` is not an img-sibling in a `position:relative` wrapper fall outside the bbox-pair test by construction; none of the measured ones misregister; optional future widen-selector confirm of the synthetic ones, not expected to matter (no img-vs-svg baseline coupling = the entire DEFECT B mechanism). No lesson-body defects surfaced. Full detail in `lesson_08_browser_qa_progress.md`. **L08 browser-QA drops off the standing queue; L11 1024-width sweep is the only remaining standing item.**
