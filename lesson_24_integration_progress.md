# Lesson 24 &#183; Animation Fundamentals &#183; Integration Progress

**Source of truth for L24 production + integration.** Mirrors the L23 progress-doc structure. Read order in a fresh chat: `session.md` &#8594; this doc &#167;6 to END &#8594; `images/IMAGE_STYLE_GUIDE.md` v6.

**Lesson file:** `lesson_24_animation_fundamentals.html` (confirmed against live HTML + `curriculum_overview.md` Module 6 lesson 24 "Animation Fundamentals"). **This opens Module 6 (Introduction to Animation)** &#8212; first lesson of a new module.
**Requirements doc:** `lesson_24_image_requirements.md` (generic AI-prompt spec &#8212; use ONLY for figure list, purpose, section placement, naming convention. Its palette `#2b2b2b`/`#ff8c00`/`#00bfff`, the "AI Image Generation Prompts", Blender-4.0 UI references, and the 4-week production plan do NOT apply. IMAGE_STYLE_GUIDE v6 + locked render recipes + `#FF6B00` brand palette + Blender 5.1.1 reality OVERRIDE it.)

**STATUS: ROSTER PLAN-LOCKED (2026-06-22).** Roster = High + Medium = 24 figures (Lower 14 deferred). **BUILD IN THIS CHAT = 24 figures: 19 inline SVG + 5 PNG.** No Cycles project renders in this lesson (the bouncing-ball project is taught via diagrams/reference sheets, not photoreal stills). Q1&#8211;Q4 all resolved (see &#167;2). NOT yet produced (live HTML still figure 0 / svg 0). Blender carried in TRUE-PRISTINE from L23 close (L23 integration was HTML-only). NEXT: pilot SVG, then SVG batches of 3, then PNG capture phases.

---

## &#167;1 &#183; Invariant baseline (LIVE HTML, UTF-8-aware Python count, NOT shell grep &#8212; locale unset so grep miscounts)

Captured at L24 OPEN against `lesson_24_animation_fundamentals.html` as it exists pre-integration. Re-verify at EVERY checkpoint; integration is HTML-only unless a render/capture phase touches Blender (then verify pristine before+after per standing rules).

| Invariant | Baseline (pre-integration) | Notes |
|-----------|----------------------------|-------|
| bytes (raw) | **128,477** | `len(file.read())` bytes on disk |
| chars (UTF-8) | 127,936 | `len(text)` after utf-8 decode |
| figure | **0** | none present pre-integration |
| figcaption | **0** | |
| img | **0** | |
| svg | **0** | |
| mermaid | **0** | NO `<div class="mermaid">` diagrams in this lesson. The 4 string-hits of "mermaid" are all in the `<head>` JS import/init (`mermaid.esm.min.mjs` + `mermaid.initialize`). There is no rendered mermaid diagram to preserve; target stays 0. |
| em-dash U+2014 | **58** | ALL literal chars in existing prose. PRESERVE verbatim, ADD NONE. Use `&#183;` / `.` / `,` / `( )` / `:`. Re-verify count at every checkpoint. |
| color222 | **0** | `color="#222"` 0. Req-doc `#2b2b2b` also absent &#8212; confirms locked pipeline governs the file before any figure lands. |

**Section IDs (document order, 10 total):**
`what-is-animation` &#183; `twelve-principles` &#183; `timing-and-spacing` &#183; `weight-and-physics` &#183; `squash-and-stretch` &#183; `anticipation` &#183; `arcs` &#183; `bouncing-ball` &#183; `summary` &#183; `final-encouragement`

- All 9 content section IDs referenced in the requirements doc's Placement Suggestions EXIST and match in order against the live HTML (`what-is-animation`, `twelve-principles`, `timing-and-spacing`, `weight-and-physics`, `squash-and-stretch`, `anticipation`, `arcs`, `bouncing-ball`, `summary`).
- Tail section beyond the placement map: `final-encouragement` (standard lesson tail; no figures proposed for it).

**TARGET invariants at integration close (LOCKED for THIS chat's build set = 24 figures, 14 Lower deferred):**
figure = **24** / figcaption = **24** / img = **5** / svg = **24** / mermaid = **0** (unchanged) / em-dash U+2014 = **58** (preserved) / color222 = 0.

> **svg-count derivation:** `<svg>` tags at close = inline-diagram SVGs (**19**) + PNG-overlay SVGs (one per PNG = **5**) = **24**. img = 5 (one per PNG figure). figcaption = 24 (one per figure). Always re-derive from actuals at each integration checkpoint; never trust a single carried number.

> **Full-roster note:** the 14 Lower-tier figures are deferred (not a separate later sub-chat like L23's project renders &#8212; they are simply out of scope unless Ray reopens them). If reopened, update this target.

---

## &#167;2 &#183; Locked roster (PLAN-LOCKED 2026-06-22)

**Tier source:** requirements doc Production Priority (High 10 / Medium 14 / Lower 14).
**Precedent:** L20 + L21 + L22 + L23 built **High + Medium**, deferred Lower. Same here.

**Q-resolutions (all locked):**
- **Q1 (tier scope):** Build **High + Medium = 24 figures**. Lower 14 deferred.
- **Q2 (SVG/PNG split):** This lesson is overwhelmingly conceptual. **19 inline SVG + 5 PNG.** Only genuine Blender captures are #13 (motion-path viewport), #22 (S&S methods UI), #27 (Graph Editor arcs), #29 (project setup), #32 (Graph Editor bounce curve). All concept/motion/timing/reference figures are SVG. #28 (ball reference sheet) and #30 (animation stages) are SVG, not composited PNG (positions + numbers + arcs, no real render).
- **Q3 (motion-as-diagram):** Teach motion as DIAGRAMS (ghost positions + spacing dots + arc paths + frame-number labels), never photoreal stills. A static render of a moving ball teaches nothing; the position-over-time / spacing relationship is what teaches. Applies to all spacing / arc / squash-stretch-cycle / weight / follow-through / overlap figures.
- **Q4 (Graph Editor + UI captures):** #27 + #32 are Graph-Editor framing &#8212; NOT bridge-drivable. Ray sets up manually + Win+Shift+S overlay-freeze, with Claude guidance (L22/L23 Q6 precedent). #13 / #22 / #29 are scriptable UI-chrome (full-window local-tempdir `screen.screenshot` + deterministic area crop). Verify each 5.1.1 panel layout LIVE before capture (req-doc "Blender 4.0" is stale).

**THIS-CHAT BUILD SET = 24 figures = 19 inline SVG + 5 PNG. DEFERRED (Lower tier) = 14.**

### Roster (24 build-now) &#8212; FINAL types

| # | slug | tier | type | section | build phase |
|---|------|------|------|---------|-------------|
| 1 | principles_overview | High | SVG | twelve-principles (section start) | SVG |
| 3 | linear_vs_natural | Med | SVG | timing-and-spacing / twelve-principles (Slow In/Out) | SVG |
| 5 | volume_conservation | High | SVG | squash-and-stretch / twelve-principles (S&S) | SVG |
| 6 | material_comparison | Med | SVG | squash-and-stretch / twelve-principles (S&S) | SVG |
| 7 | anticipation_sequence | Med | SVG | anticipation / twelve-principles (Anticipation) | SVG |
| 10 | slow_in_out_spacing | High | SVG | timing-and-spacing / twelve-principles (Slow In/Out) | SVG |
| 11 | spacing_patterns | High | SVG | timing-and-spacing | SVG |
| 12 | arc_types | Med | SVG | arcs / twelve-principles (Arcs) | SVG |
| 13 | motion_path_blender | Med | PNG (UI-chrome viewport) | arcs (Checking Arcs in Blender) | PNG-UI |
| 14 | arcs_comparison | High | SVG | arcs / twelve-principles (Arcs) | SVG |
| 15 | weight_timing | High | SVG | timing-and-spacing / weight-and-physics | SVG |
| 16 | bounce_weight_comparison | Med | SVG | weight-and-physics | SVG |
| 17 | gravity_parabolic | Med | SVG | weight-and-physics | SVG |
| 19 | squash_stretch_timing | High | SVG | squash-and-stretch | SVG |
| 20 | volume_math | Med | SVG | squash-and-stretch (Common Mistakes, technical ref) | SVG |
| 22 | blender_methods | Med | PNG (4-panel UI composite) | squash-and-stretch (S&S in Blender) | PNG-composite |
| 27 | graph_editor_arcs | Med | PNG (Graph Editor, manual Q4) | arcs (Creating Arcs Manually) | PNG-Graph |
| 28 | ball_reference_sheet | High | SVG | bouncing-ball (Project Overview) | SVG |
| 29 | project_setup | Med | PNG (UI-chrome viewport) | bouncing-ball (Setup) | PNG-UI |
| 30 | animation_stages | Med | SVG | bouncing-ball (Blocking) | SVG |
| 31 | bounce_height_decay | High | SVG | bouncing-ball (Blocking) | SVG |
| 32 | graph_bounce_curve | Med | PNG (Graph Editor, manual Q4) | bouncing-ball (Refining) | PNG-Graph |
| 36 | timing_reference | Med | SVG | timing-and-spacing (Timing Reference Guidelines) | SVG |
| 37 | workflow_diagram | Med | SVG | what-is-animation / summary | SVG |
| 35 | principles_reference | High | SVG | twelve-principles (Principles Working Together) / summary | SVG |

**Build-now SVG (19):** #1, #3, #5, #6, #7, #10, #11, #12, #14, #15, #16, #17, #19, #20, #28, #30, #31, #35, #36, #37
> (count check: that list is 20 entries &#8212; see correction note below)

**Build-now PNG (5):** #13, #22, #27, #29, #32
**Deferred Lower tier (14):** #2, #4, #8, #9, #18, #21, #23, #24, #25, #26, #33, #34, #38

> **CORRECTION / verify-at-build:** SVG build-now set is **#1, #3, #5, #6, #7, #10, #11, #12, #14, #15, #16, #17, #19, #20, #28, #30, #31, #35, #36, #37 = 20 slugs**, but the locked SVG total is **19** and total roster is **24**. Re-derive before pilot: High(10) = #1,#5,#10,#11,#14,#15,#19,#28,#31,#35. Medium(14) = #3,#6,#7,#12,#13,#16,#17,#22,#27,#29,#30,#32,#36,#37. PNG = #13,#22,#27,#29,#32 (5). SVG = the remaining 19 = #1,#3,#5,#6,#7,#10,#11,#12,#14,#15,#16,#17,#19,#20,#28,#30,#31,#35,#36,#37 minus one. **#20 (volume_math) is Lower-tier in the req-doc, NOT Medium** &#8212; it is therefore DEFERRED, which makes the SVG set exactly 19 and resolves the count. Lower deferred (14) = #2,#4,#8,#9,#18,#20,#21,#23,#24,#25,#26,#33,#34,#38. **This corrected partition is the locked one; the table row for #20 above is struck (deferred).**

**LOCKED partition (authoritative):**
- **SVG build-now (19):** #1, #3, #5, #6, #7, #10, #11, #12, #14, #15, #16, #17, #19, #28, #30, #31, #35, #36, #37
- **PNG build-now (5):** #13, #22, #27, #29, #32
- **Deferred Lower (14):** #2, #4, #8, #9, #18, #20, #21, #23, #24, #25, #26, #33, #34, #38

### Motion-diagram craft notes (Q3 &#8212; applies to all spacing/arc/cycle/weight figures)
Per L21/L22/L23 LD carried in style guide: stage ghost start/end positions, draw the path as arrow/curve, show spacing dots where acceleration is the teaching point, label frame numbers. Do NOT attempt to render motion as a photographic still. Side-view (ortho) for bounce trajectories and arcs; stacked horizontal tracks for spacing-pattern comparisons; sequential panels for squash-stretch cycle and anticipation; multi-track timeline for follow-through/overlap.

### PNG capture-route notes (Q4)
- UI-chrome (#13 motion-path viewport + Object Properties Motion Paths panel; #22 four-method S&S composite; #29 bouncing-ball project setup viewport+timeline): full-window local-tempdir `screen.screenshot` + deterministic area crop; verify 5.1.1 panel layout live first. #22 is a 4-panel composite (scale / lattice / shape keys / armature) &#8212; Pillow composite from 4 captures.
- Graph Editor (#27 arc refinement, #32 bounce Z-location curve): NOT bridge-drivable. Ray sets up framing/panel-expand manually, Win+Shift+S overlay-freeze, save direct to WSL `images/`, with Claude guidance.
- All PNG figures: cairosvg pixel-verify Claude-side before EVERY overlay commit; verify Blender pristine before+after any bridge work.

### Batching cadence (style guide Workflow conventions)
1 pilot SVG &#8594; batches of 3 for SVGs. Plan-lock each asset's spec + design plan in this doc (or its archive) BEFORE generating. PNG phases grouped by scene/panel context.

---

## &#167;3 &#183; id-prefix registry (inline-SVG `id` namespaces)

Per style guide: unique `{id}` prefix per inline SVG so multiple SVGs coexist on one page without `id` collisions. 19 build-now SVGs:

| # | slug | id-prefix | status |
|---|------|-----------|--------|
| 1 | principles_overview | `po` | LOCKED |
| 3 | linear_vs_natural | `ln` | LOCKED |
| 5 | volume_conservation | `vc` | LOCKED |
| 6 | material_comparison | `mc` | LOCKED |
| 7 | anticipation_sequence | `as` | LOCKED |
| 10 | slow_in_out_spacing | `si` | LOCKED |
| 11 | spacing_patterns | `sp` | LOCKED |
| 12 | arc_types | `at` | LOCKED |
| 14 | arcs_comparison | `ac` | LOCKED |
| 15 | weight_timing | `wt` | LOCKED |
| 16 | bounce_weight_comparison | `bw` | LOCKED |
| 17 | gravity_parabolic | `gp` | LOCKED |
| 19 | squash_stretch_timing | `st` | LOCKED |
| 28 | ball_reference_sheet | `rs` | LOCKED |
| 30 | animation_stages | `ag` | LOCKED |
| 31 | bounce_height_decay | `bd` | LOCKED |
| 35 | principles_reference | `pr` | LOCKED |
| 36 | timing_reference | `tr` | LOCKED |
| 37 | workflow_diagram | `wf` | LOCKED |

### `c{N}` overlay-id registry (PNG callout namespaces)
5 build-now PNGs; per style guide each PNG's overlay ids namespaced `c{N}-*`:

| # | slug | overlay prefix | status |
|---|------|----------------|--------|
| 13 | motion_path_blender | `c13-` | reserved |
| 22 | blender_methods | `c22-` | reserved |
| 27 | graph_editor_arcs | `c27-` | reserved |
| 29 | project_setup | `c29-` | reserved |
| 32 | graph_bounce_curve | `c32-` | reserved |

---

## &#167;4 &#183; Production status log

| Phase | Scope | Status |
|-------|-------|--------|
| OPEN | doc created, baseline captured, roster plan-locked, id-prefixes assigned | DONE 2026-06-22 |
| SVG pilot | #1 principles_overview (`po`) | DONE 2026-06-22 (8,755 B, light+dark QA pass) |
| SVG batches | batch 1 done (#10, #11, #3); batch 2 done (#5, #19, #6); batch 3 done (#12, #14, #15); batch 4 done (#16, #17, #31); batch 5 done (#28, #30, #7); batch 6 done (#35, #36, #37) &#8212; 0 SVGs remain, SVG production CLOSED 19/19 | DONE 2026-06-22 |
| PNG-UI | #13, #29 (scriptable viewport UI-chrome) | DONE 2026-06-22 (both captured + on-disk verified; Blender restored TRUE-PRISTINE + re-verified) |
| PNG-composite | #22 (4-panel S&S methods) | DONE 2026-06-22 (vertical 4-stack 1291x2936, 525,726 B; Blender restored TRUE-PRISTINE + re-verified) |
| PNG-Graph | #27, #32 (manual Graph Editor, Q4) | pending |
| INTEGRATION | inline 19 SVG + place 5 PNG w/ overlays | pending |
| BROWSER QA | narrow 380px + wide 900px, headless Chromium/Playwright | pending |
| CLOSE | bump session.md (only when integrated + QA-PASS) | pending |

---

## &#167;5 &#183; Locked rules (carry-forward, cross-reference IMAGE_STYLE_GUIDE v6)

- `\\wsl$\` paths only (never `\\wsl.localhost\`). Project root `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`.
- 0 new em-dashes (U+2014). Use `&#183;` / `.` / `,` / `( )` / `:`. Baseline = 58; preserve verbatim; re-verify at every checkpoint.
- `Filesystem:write_file` for new files / full overwrites (never `create_file`).
- `Filesystem:edit_file` dryRun&#8594;commit&#8594;re-copy + UTF-8-aware re-count per edit.
- cairosvg 2.9.0 + PIL 12.1.1 Claude-side; pixel-verify before EVERY PNG-overlay commit.
- Playwright headless Chromium Claude-side for browser QA (launch `--no-sandbox --disable-gpu --disable-dev-shm-usage`).
- Color-dependent render recipe (style guide LD17) applies only if a figure needs marker color &#8212; this lesson has no Cycles renders, so N/A unless a Lower-tier figure is reopened.
- Bump `session.md` ONLY when the lesson fully closes (integrated + QA-PASS).
- Warn Ray to start a fresh chat before auto-compaction.

---

## &#167;6 &#183; Newest-first work notes

*(Append newest entries at the TOP of this section as work proceeds. Empty at OPEN.)*

- **2026-06-22 PNG-COMPOSITE PHASE (#22 blender_methods &#183; 4-method S&amp;S vertical 4-stack):** Built per the &#167;7 plan-lock. Phase opened with Blender re-verified TRUE-PRISTINE; live 5.1.1 `Layout` geometry confirmed matching the &#167;7 record (VIEW_3D 3,112,1291,895 / PROPERTIES 1299,35,618,792 / DOPESHEET-TIMELINE 3,35,1291,72 / OUTLINER top-right; benign `temp` IMAGE_EDITOR window present). Four panels staged + captured top-to-bottom M1&#8594;M2&#8594;M3&#8594;M4, each full-window `screen.screenshot` to Windows tempdir &#8594; bridge-side Pillow crop (VIEW_3D area, bottom-left&#8594;top-left flip). **Header trim = 82px** (clears global header + select-tool row + Options tab; the initial 26px and 64px trims left toolbar/Options chrome and were rejected at QA). Clean chrome per panel (SOLID, `shading.show_object_outline` orange outline, toolbar/N-panel/nav-gizmo hidden, cursor/text/stats off). Gentle 3/4 ORTHO framing (Euler pitch ~68&#176; / yaw ~12&#176;) so the ground plane reads as a surface, not the edge-on red line that pure FRONT ortho produced. **P1 Manual scaling:** UV sphere keyframed neutral&#8594;squash (scale 1.3/1.3/0.7, parked on squash frame 10), sits on ground, orange outline (1291x813). **P2 Lattice modifier:** sphere + 3x3x3 Lattice cage + Lattice modifier, top/mid control points pulled down+out for asymmetric squash. **Critical: a plain selected lattice does NOT draw its cage in SOLID viewport** &#8212; had to enter the lattice EDIT mode (`lattice.select_all SELECT`, `show_in_front=True`) so the orange control-point cage renders as the method's defining visual (1291x813). **P3 Shape keys:** sphere with Basis + Squash shape keys, Squash driven to value 1.0; two-region composite = viewport (deformed mesh) + PROPERTIES on the **`DATA`** tab cropped tight to the Shape Keys block (Basis + Squash + **1.000 slider** visible, the teaching point), scaled + joined with a 24px gutter (2135x813). **P4 Armature:** sphere skinned to a 2-bone armature (Lower+Upper, `ARMATURE_AUTO` weights), Upper bone pose-scaled (1.35/0.55/1.35) + bent 14&#176; to squash the mesh, `show_in_front=True` so both octahedral bones read through the mesh (1291x813). Pillow vertical-composite: all panels scaled to common width **1291** (P3 491h after downscale), 2px `#FF6B00` dividers between panels &#8594; **`images/lesson_24_22_blender_methods.png` = 1291x2936, 525,726 B** (accept band OK). `get_file_info` verified + Claude-side viewed: all four panels read distinctly at thumbnail. **Overlay viewBox = 1291x2936**; `c22-` callouts (4 centered method-name badges `METHOD 1 &#183; SCALE` / `METHOD 2 &#183; LATTICE` / `METHOD 3 &#183; SHAPE KEYS` / `METHOD 4 &#183; ARMATURE`, positive-green `#4ADE80` / `#0f1a14` text, no highlights/leaders per composite-overlay variant) authored at INTEGRATION, cairosvg pixel-verified before commit. After: Blender RESTORED to TRUE-PRISTINE + re-verified (full field set matches baseline: 3 objs Camera+Cube+Light, 1 mesh / 0 mats / 0 node_groups / 0 actions / 0 armatures / 0 lattices / 0 empties / 0 orphans, EEVEE/AgX/None/exp0, res 1920x1080@100, frame 1&#8211;250 @ 1, world 0.88/1.0, POINT 1000 soft 0.25, cam lens 50 / DoF off / blades 0, Cube unhidden, viewport chrome restored). All `_temp_l24_22_*` scratch PNGs (4 panels + p3_vp + p3_pr) + Windows-tempdir `_raw` files removed via bridge `os.remove`. **3 of 5 PNGs done.** **NEXT = #27 graph_editor_arcs + #32 graph_bounce_curve (manual Graph Editor, Q4: Ray drives framing + Win+Shift+S overlay-freeze, NOT bridge-drivable; Claude guidance only). c22/c27/c32 reserved.** Then INTEGRATION (inline 19 SVG strip color="#222" + place 5 PNG w/ overlays), browser QA 380/900px, close.
- **2026-06-22 PNG-UI PHASE (#13 motion_path_blender + #29 project_setup &#183; scriptable viewport UI-chrome):** Both PNGs captured, cropped bridge-side (Pillow from Windows-tempdir full-window `screen.screenshot`, never direct-to-UNC), and on-disk verified via `get_file_info`. Phase opened with Blender verified TRUE-PRISTINE; 5.1.1 UI verified LIVE (Motion Paths is `obj.animation_visualization.motion_path`, ops `paths_calculate`/`update`/`clear` present; working screen `Layout` 1920x1046). **`lesson_24_13_motion_path_blender.png`** = 1920x982, 171,434 B. Staged a UV sphere keyframed into a single clean parabola (13 keys, X -4&#8594;+4, Z apex 3.4), frame range 1&#8211;48, `paths_calculate(display_type='RANGE', range='SCENE')` &#8594; 48-point motion path; **critical: the viewport-overlay toggle `space.overlay.show_motion_paths` defaults OFF and must be set True or the path data exists but never draws** (the calculated path is invisible until this overlay is on). FRONT ortho, ball parked at apex (frame 24) so it sits ON the visible arc, deterministic framing via `r3d.view_location=(0,0,2)` + `view_distance=11` ORTHO (view_selected zooms to the 0.6m ball, too tight; hand-set the ortho distance instead). Motion Paths **panel-expand is NOT bridge-drivable** (no RNA for sub-panel expand state in 5.1.1, confirmed) &#8212; Ray expanded it manually (one click), then a single full-window grab captured BOTH the viewport arc and the expanded panel side-by-side (live layout already docks VIEW_3D left / PROPERTIES right). **5.1.1 label note:** once a path exists the panel button reads **Update Path / Update All Paths**, NOT the stale req-doc "Calculate" (the `Calculate...` label only shows pre-calculation); panel also exposes Paths Type=In Range, Frame Range 1&#8211;48, Display (Frame Numbers/Keyframes/Lines), Before/After red/green color legend. Single combined crop chosen (reads well at thumbnail; 2-up composite not needed). Crop (0,30,1920,1012). Overlay viewBox = **1920x982**, `c13-` callouts (3) authored at INTEGRATION. **`lesson_24_29_project_setup.png`** = 1006x1010, 270,180 B. Staged per the LESSON-BODY spec (verified against live HTML, NOT the stale req-doc): UV sphere 2u diameter at Z=10, ground Plane scaled S&#8594;20 (40m), End frame 96, frame 1. **Capture trap hit + fixed: after scale-apply the sphere mesh was offset ~3.4u from the object origin** (object `location.z=10` but world bbox sat at z 12.4&#8211;14.4, off-frame); `origin_set(type='ORIGIN_GEOMETRY')` then re-place to z=10 fixed it (bbox z 9.0&#8211;11.0). Framing verified by projecting world points via `location_3d_to_region_2d` BEFORE capture (ball top y=818 / ground y=138 both inside region h=895) rather than guessing distance. Final view = gentle 3/4 PERSP (Euler pitch 72&#176; / yaw 28&#176;, dist 34) so the 40m ground plane reads as a receding surface, not an edge-on line (pure FRONT ortho rendered the plane as a thin line). Cropped to VIEW_3D + bottom TIMELINE strip only, Properties panel excluded (crop 0,30,1006,1040). Overlay viewBox = **1006x1010**, `c29-` callouts authored at INTEGRATION. After both: Blender RESTORED to TRUE-PRISTINE and re-verified (full field set matches baseline: 3 objs Camera+Cube+Light, 1 mesh / 0 mats / 0 actions / 0 orphans, EEVEE/AgX/None/exp0, res 1920x1080@100, frame 1&#8211;250, world 0.88/1.0, POINT 1000 soft 0.25, cam lens 50 / DoF off / blades 0, Cube unhidden); staged sphere/plane meshes + `L24_BallAction` purged, camera + viewport overlays restored. All `_temp_l24_*` scratch PNGs removed via bridge `os.remove`. **2 of 5 PNGs done.** **NEXT = the remaining 3 PNG: #22 blender_methods (4-panel S&S composite: scale / lattice / shape keys / armature, scriptable UI-chrome per panel + Pillow composite) THEN #27 graph_editor_arcs + #32 graph_bounce_curve (manual Graph Editor, Q4: Ray sets up framing + Win+Shift+S overlay-freeze with Claude guidance, NOT bridge-drivable). Plan-lock each in &#167;7 before capturing. Verify Blender TRUE-PRISTINE before+after any bridge work.** Then INTEGRATION (inline 19 SVG + place 5 PNG w/ overlays), browser QA 380/900px, close.
- **2026-06-22 SVG BATCH 6 (FINAL SVG batch &#183; reference-tables + workflow):** `#35 principles_reference` (`pr`, 5,948 B, 800x680 dense), `#36 timing_reference` (`tr`, 5,461 B, 800x680 dense), `#37 workflow_diagram` (`wf`, 6,316 B, 800x560) written + on-disk verified (XML well-formed, 0 U+2014, `color="#222"` fallback present, ids namespaced `pr-`/`tr-`/`wf-`; on-disk bytes match sources exactly via `get_file_info`). All cairosvg light+dark QA pass, no clip, all labels legible, no fixes needed on first render. Specs were plan-locked in &#167;7 before generation. Non-chromatic carriers held: `pr` = brand-orange numbered chips (1&#8211;12, canonical Disney order) + principle NAMEs + per-cell one-line descriptions + the 3x4 grid order itself (color decorative only, color-blind safe, no category color-coding); `tr` = band NAME pills (VERY FAST / FAST / MEDIUM / SLOW / VERY SLOW, fastest-at-top) + numeric frame ranges + example-action text + proportional duration-bar LENGTHS drawn in currentColor (NOT a red-to-blue hue gradient, rejected per color-blind rule) + top-to-bottom escalation order; `wf` = brand-orange numbered chips (1&#8211;5) + stage NAMEs (PLAN / BLOCK / REFINE / POLISH / REVIEW) + two-line descriptors + forward arrows between cards + a dashed labeled `iterate` return arc looping REVIEW back to PLAN + left-to-right order. `wf` marker id `wf-arrow` is namespaced (no cross-SVG collision). **16 of 19 SVGs were done; now 19 of 19 &#8212; SVG PRODUCTION COMPLETE.** **NEXT (fresh chat) = PNG capture phases (5 PNG): #13 motion_path_blender + #29 project_setup (scriptable UI-chrome viewport, full-window local-tempdir `screen.screenshot` + deterministic area crop, verify 5.1.1 panel layout live first); #22 blender_methods (4-panel S&S composite: scale / lattice / shape keys / armature, Pillow composite from 4 captures); #27 graph_editor_arcs + #32 graph_bounce_curve (manual Graph Editor, Q4: Ray sets up framing + Win+Shift+S overlay-freeze with Claude guidance, NOT bridge-drivable). Verify Blender TRUE-PRISTINE before+after any bridge work. cairosvg pixel-verify Claude-side before EVERY PNG-overlay commit.**
- **2026-06-22 SVG BATCH 5 (bouncing-ball / reference cluster):** `#28 ball_reference_sheet` (`rs`, 6,577 B, 800x680 dense), `#30 animation_stages` (`ag`, 6,713 B, 800x560), `#7 anticipation_sequence` (`as`, 6,735 B, 800x560) written + on-disk verified (XML well-formed, 0 U+2014, `color="#222"` fallback present, ids namespaced `rs-`/`ag-`/`as-`; final on-disk bytes confirmed). All cairosvg light+dark QA pass, no clip, all labels legible. Specs were plan-locked in &#167;7 before generation. Post-render fixes: (`rs`) every quadratic arch control-y recomputed to `2*apex-300` so the f24 + f36 apex ghost balls actually land ON the dashed curve (initial control-ys left them floating above the arc, same midpoint-vs-control trap as batch 4 `bw`); dead zero-opacity height-guide line removed. (`ag`) full rewrite after first render: all three panels' arc control-ys recomputed to `2*peak-300` and every in-between dot / stretch-ellipse / apex ball placed at TRUE on-curve Bezier coordinates (computed Claude-side) so nothing floats; then the panel-3 `ease` label dropped to clear a collision with the panel-3 subhead at the apex. (`as`) clean on first render, no fixes. Non-chromatic carriers held: `rs` = labeled decaying-arc trajectory shape + per-row NAME pills (FRAME RANGE / KEY APEXES / CONTACTS / SQUASH-STRETCH / TIMING / ARC RULE) + numeric frame/height values + apex height-guides; `ag` = stage NAME pills (BLOCKING / SPACING / POLISH) + brand-orange numbered stage chips + descriptor lines + the visibly increasing dot-density and ball-deformation across the three panels; `as` = stage NAME pills (ANTICIPATION / ACTION / FOLLOW-THROUGH) + the three directional arrows (back / long-forward / short-overshoot-curve) + ghost rest/settle outlines + motion-spacing dots + frame-span labels + panel order. **13 of 19 SVGs were done; now 16 of 19.** **NEXT (fresh chat) = SVG batch 6 (final SVG batch): the remaining 3 = #35 principles_reference `pr`, #36 timing_reference `tr`, #37 workflow_diagram `wf`.** Then 0 SVGs remain, then the 5 PNG phases (#13/#29 scriptable UI-chrome viewport, #22 4-panel S&S composite, #27/#32 manual Graph Editor).
- **2026-06-22 SVG BATCH 4 (gravity / weight / bounce-physics cluster):** `#16 bounce_weight_comparison` (`bw`, 5,574 B, 800x560), `#17 gravity_parabolic` (`gp`, 4,327 B, 800x560), `#31 bounce_height_decay` (`bd`, 3,403 B, 800x560) written + on-disk verified (XML well-formed, 0 U+2014, `color="#222"` fallback present, ids namespaced `bw-`/`gp-`/`bd-`; on-disk bytes match sources exactly). All cairosvg light+dark QA pass, no clip, all labels legible. Specs were plan-locked in &#167;7 before generation. Two pre-disk fixes on `bw`: arc `Q` control-points lowered to `2*apex-ground` so each dashed arc apex actually reaches its ghost dot (initial control-y left the dots floating above the arcs), and the two bottom captions de-collided (LIGHT caption centered x=225, HEAVY centered x=560). Footer tips on `bw` + `gp` trimmed to fit within the 760 content width. `gp` dots were aligned to the TRUE on-curve Bezier values (x is linear in t since control-x=400 is the midpoint, so even-x = even-time; on-curve cy = 450/314/222/176 symmetric) rather than a separate math-parabola. `bd` apex dots sit exactly on the curve via control-y = `2*apex-ground`; apex heights 100/70/49/34% with dashed height-guides + frame-contact labels f1/f14/f24/f32/f38. Non-chromatic carriers held: `bw` = LIGHT/HEAVY name pills + ball glyphs + tall-slow-decay vs low-fast-decay arc-series shape + apex-height ticks + per-panel hang-time captions; `gp` = even-time ghost dots + frame labels + the vertical-spacing gradient + slowing/speeding-up pills + apex pill + tight/wide spacing brackets; `bd` = per-apex %-labels + dashed height-guides + frame-contact labels + the visibly decaying arc series. **NEXT (fresh chat) = SVG batch 5: pick 3 from the remaining 6 (#7 anticipation_sequence `as`, #28 ball_reference_sheet `rs`, #30 animation_stages `ag`, #35 principles_reference `pr`, #36 timing_reference `tr`, #37 workflow_diagram `wf`).** Then 3 SVGs remain after that, then the 5 PNG phases.
- **2026-06-22 SVG BATCH 3 (arcs cluster):** `#12 arc_types` (`at`, 800x560), `#14 arcs_comparison` (`ac`, 800x560), `#15 weight_timing` (`wt`, 800x680 dense) written + on-disk verified (XML well-formed, 0 U+2014, `color="#222"` fallback present, ids namespaced `at-`/`ac-`/`wt-`). All cairosvg light+dark QA pass, no clip, all labels legible. Two polish fixes applied post-render: `at` apex ghost dot moved onto the parabola (cy 183&#8594;207, label 173&#8594;189); `ac` MECHANICAL pill shifted right (x 230&#8594;248) to clear the section label. Non-chromatic carriers held: `at` uses type-name labels + descriptor pills + start/mid/end ghost dots + end arrows; `ac` uses NATURAL/MECHANICAL name pills + check/cross glyphs + curved-vs-straight path shapes (green/red reinforces only, per #3 precedent); `wt` uses LIGHT/HEAVY pills + balloon/anvil glyphs + frame-number labels + the dot-spacing pattern (wide-even vs bunched-then-widening) + impact ground line. Specs were plan-locked in &#167;7 before generation. **NEXT (fresh chat) = SVG batch 4: pick 3 from the remaining 9 (#7 anticipation_sequence `as`, #16 bounce_weight_comparison `bw`, #17 gravity_parabolic `gp`, #28 ball_reference_sheet `rs`, #30 animation_stages `ag`, #31 bounce_height_decay `bd`, #35 principles_reference `pr`, #36 timing_reference `tr`, #37 workflow_diagram `wf`).** Then 3 SVGs remain after that, then the 5 PNG phases.
- **2026-06-22 SVG BATCH 2 (squash-and-stretch cluster):** `#5 volume_conservation` (`vc`, 5,309 B, 800x560), `#19 squash_stretch_timing` (`st`, 5,797 B, 800x680 dense), `#6 material_comparison` (`mc`, 4,367 B, 800x560) written + on-disk verified (XML well-formed, 0 U+2014, `color="#222"` fallback present, ids namespaced `vc-`/`st-`/`mc-`). All cairosvg light+dark QA pass, no clip, all labels legible. Non-chromatic carriers held: `vc` uses dashed equal-volume frames + width/height tick measures + `100% volume` pills; `st` uses per-position state pills (NEUTRAL/STRETCH/SQUASH) + frame numbers along a dashed bounce arc with a ground line; `mc` uses material name + squash-% labels + a dashed undeformed-silhouette outline behind each ball + a soft-to-rigid axis. Specs were plan-locked in &#167;7 before generation. **NEXT (fresh chat) = SVG batch 3: pick 3 from the remaining 12 (#7 anticipation_sequence `as`, #12 arc_types `at`, #14 arcs_comparison `ac`, #15 weight_timing `wt`, #16 bounce_weight_comparison `bw`, #17 gravity_parabolic `gp`, #28 ball_reference_sheet `rs`, #30 animation_stages `ag`, #31 bounce_height_decay `bd`, #35 principles_reference `pr`, #36 timing_reference `tr`, #37 workflow_diagram `wf`).** Then the 5 PNG phases.
- **2026-06-22 SVG BATCH 1:** `#10 slow_in_out_spacing` (`si`, 4,188 B), `#11 spacing_patterns` (`sp`, 5,769 B), `#3 linear_vs_natural` (`ln`, 4,045 B) written + on-disk verified (XML OK, 0 em-dash, `color="#222"` fallback, ids namespaced). All cairosvg light+dark QA pass. #11 needed one fix (Natural badge moved above highlight box to clear the track dots). #3 uses semantic green/red with cross+check glyphs as non-color carriers. **NEXT (fresh chat) = SVG batch 2: #5 volume_conservation (`vc`), #19 squash_stretch_timing (`st`), #6 material_comparison (`mc`)** &#8212; the squash-and-stretch concept cluster. Then 12 SVGs remain (#7,#12,#14,#15,#16,#17,#28,#30,#31,#35,#36,#37), then 5 PNG phases.
- **2026-06-22 PILOT:** `lesson_24_01_principles_overview.svg` written + on-disk verified (8,755 B, XML well-formed, 0 em-dash, `color="#222"` fallback present, `po-` ids). cairosvg light+dark renders both legible, no clip. Dense 800x680 variant, 3x4 grid, brand-orange numbered chips (tie-break), Variant-B footer rail. Style pilot CONFIRMED. NEXT: SVG batch 1 of 3.
- **2026-06-22 OPEN:** Doc created mirroring L23 structure. Baseline captured UTF-8-aware (bytes 128,477 / chars 127,936 / figure 0 / svg 0 / mermaid 0 / em-dash 58 / color222 0). 10 section IDs confirmed against live HTML. Roster plan-locked: High+Medium 24 (19 SVG + 5 PNG), 14 Lower deferred. No Cycles renders this lesson. id-prefixes + c{N} registries assigned. Blender TRUE-PRISTINE carried from L23 (HTML-only integration). NEXT: pick pilot SVG, plan-lock its spec, generate.

---

## &#167;7 &#183; Phase detail / render recipes

*(Populated per phase as production proceeds. PNG capture recipes land here when the PNG phases open. No Blender render recipes needed at OPEN &#8212; this lesson's only bridge work is UI-chrome captures, #13/#22/#29, plus manual Graph-Editor snips #27/#32.)*

### PNG-COMPOSITE PHASE plan-lock (#22 blender_methods &#183; 4-method S&amp;S vertical 4-stack) &#8212; locked 2026-06-22, capture after this lock

**Pre-flight (done at phase open):** Blender re-verified TRUE-PRISTINE against the session.md baseline (3 objs Camera+Cube+Light, 1 mesh / 0 mats / 0 node_groups / 0 actions / 0 empties / 0 orphans, EEVEE/AgX/None/exp0, res 1920x1080@100, frame 1&#8211;250 @ frame 1, world 0.88/1.0, POINT 1000 factory loc, cam lens 50 / DoF off / blades 0, Cube unhidden). **5.1.1 UI verified LIVE** (req-doc "Blender 4.0" stale): working screen `Layout` win 1920x1046 = VIEW_3D (3,112,1291,895) left, PROPERTIES (1299,35,618,792) + OUTLINER top-right, DOPESHEET in TIMELINE mode (3,35,1291,72) bottom. All four method ops/enums confirmed present: M1 scale-keyframe (no special op); M2 `object.add(type='LATTICE')` + `LATTICE` modifier enum; M3 `object.shape_key_add` + Shape Keys live on the **`DATA`** Properties tab (green-triangle object-data tab, NOT a separate Shape-Keys tab); M4 `object.armature_add` + `ARMATURE` modifier enum. Benign leftover `temp` IMAGE_EDITOR window present; capture grabs the `Layout` window explicitly.

**Source of the 4 methods = LESSON BODY** (squash-and-stretch &#8594; "Squash and Stretch in Blender &#183; Technical Implementation"), NOT the req-doc. Verbatim: **M1 Manual scaling** (simple objects: keyframe Scale at neutral / squash / neutral, e.g. f1 (1,1,1) &#183; f10 (1.3,1.3,0.7) &#183; f15 (1,1,1)); **M2 Lattice modifier** (complex objects: Add Lattice cage, add Lattice modifier, animate lattice points; asymmetric squash possible); **M3 Shape keys** (characters: create squash/stretch shape keys, animate value 0&#8594;1); **M4 Armature/bones** (advanced: rig with bones, scale bones to deform).

**Figure type / layout (Ray-locked = option B):** **vertical 4-stack**, one method per panel, top-to-bottom M1&#8594;M2&#8594;M3&#8594;M4. Each panel captured full VIEW_3D width so the deforming mesh + the method's defining UI read large at narrow 380px (rejected 2x2 grid: panels too small at 380px). Pillow-composite the 4 cropped panels into one tall PNG with `#FF6B00` 2px horizontal dividers between panels (Phase-8 composite-overlay variant: baked dividers carry panel separation, so overlays drop highlights/leaders and use one centered badge per panel). Section squash-and-stretch ("Squash and Stretch in Blender" card; deeper-indent figure variant if the card opens at col 16). overlay prefix `c22-`.

**Per-panel staging (SOLID shaded, orange selected-object outline, clean chrome &#8212; hide N-panel/toolbar/nav-gizmo per Phase-8 clean-canvas, restore after):**
- **P1 Manual scaling:** UV sphere on a ground plane, keyframed neutral&#8594;squash (Scale z 0.7 at impact, x/y 1.3), parked on the squashed frame so the deform reads; viewport only (the method IS the scale transform, no Properties panel needed). Reads as "keyframed scale."
- **P2 Lattice modifier:** UV sphere + a Lattice object as cage + Lattice modifier on the sphere; lattice control points pulled to squash the mesh (asymmetric). Capture VIEW_3D showing the orange lattice cage deforming the sphere, AND set PROPERTIES to `MODIFIER` context so the Lattice modifier panel shows (two-region crop OR viewport-only with the cage as the carrier &#8212; decide at capture which reads better at stack width; default viewport-only with visible cage since the stack is column-width).
- **P3 Shape keys:** UV sphere with Basis + a `Squash` shape key driven to value ~1.0 (deformed); PROPERTIES set to `DATA` tab so the Shape Keys list (Basis + Squash + value slider) shows. Two-region crop (viewport deformed mesh + Shape Keys panel) preferred here since the value slider is the teaching point.
- **P4 Armature:** UV sphere skinned to a 2-bone armature (Armature modifier, auto-weights) + bones drawn In Front; one bone scaled/posed to deform the mesh. Viewport showing bones + deformed mesh.

**Capture routing (LD20):** per panel, full-window `bpy.ops.screen.screenshot` to a LOCAL WINDOWS TEMPDIR (carries panel + overlay layers a scoped grab would drop), then bridge-side Pillow deterministic crop using live `area.x/y/w/h` with bottom-left&#8594;top-left origin flip (`x &#8594; x+w`; `y = WIN_H-(area.y+area.h) &#8594; WIN_H-area.y`), header strip trimmed. NEVER write `screen.screenshot` direct to WSL UNC (truncates to 0 bytes). PIL 12.x bridge-side. Each panel &#8594; `_temp_l24_22_p{N}.png` (handoff/scratch). Final Pillow vertical-composite (equal panel widths; scale panels to a common width before stacking; 2px `#FF6B00` dividers) &#8594; `images/lesson_24_22_blender_methods.png`. Record final composite px dims (become overlay viewBox).

**`c22-` overlay callouts (4, authored at INTEGRATION over recorded composite px):** one centered method-name badge per panel near its bottom edge: `METHOD 1 &#183; SCALE` / `METHOD 2 &#183; LATTICE` / `METHOD 3 &#183; SHAPE KEYS` / `METHOD 4 &#183; ARMATURE`. Positive-green `#4ADE80` badge, `#0f1a14` text (informational, not warning). No highlight ellipses / leaders (baked dividers do the separation). cairosvg pixel-verify Claude-side over the composite PNG before the `edit_file` commit.

**Pristine (breaks during capture, restored after):** staging ADDS datablocks (mesh sphere(s) + plane + lattice + armature + action + shape keys + auto-weight vertex groups + materials if any). Standing rule: verified pristine BEFORE (done); after the phase RESTORE + re-verify the full TRUE-PRISTINE field set (delete all staged objects, purge meshes/lattices/armatures/actions/shape-keys, orphan-purge `do_local_ids=False`, restore hidden viewport regions + Cube-only scene; AgX/world/light/cam untouched since OpenGL-free UI capture only). Remove all `_temp_l24_22_*` scratch PNGs via bridge `os.remove`.

> PNG-COMPOSITE build order: stage+capture P1 &#8594; P2 &#8594; P3 &#8594; P4 (each: stage &#8594; full-window screenshot to Windows tempdir &#8594; bridge-side Pillow crop to `_temp_l24_22_p{N}.png` &#8594; `get_file_info` size-check) &#8594; Pillow vertical-composite &#8594; `images/lesson_24_22_blender_methods.png` &#8594; `get_file_info` (accept band ~200&#8211;800 KB) &#8594; `copy_file_user_to_claude` + Claude-side view to confirm the stack reads at thumbnail. Record composite px dims. Then RESTORE pristine + re-verify, remove temps, append a PNG-COMPOSITE entry at the top of &#167;6, flip the &#167;4 PNG-composite row to DONE. Overlay authored at INTEGRATION, cairosvg-pixel-verified before commit. Doc em-dash baseline stays literal-U+2014 = 0 (all dashes here are `&#8211;`/`&#8212;` entities or `&#183;` separators &#8212; re-verify after commit). After #22: the manual Graph-Editor pair #27 + #32 (Q4, Ray-driven Win+Shift+S) remains; then INTEGRATION.

### SVG BATCH 6 plan-lock (FINAL SVG batch &#183; reference-tables + workflow) &#8212; locked 2026-06-22, build after this lock

**Selection rationale:** the last 3 of the 19 SVGs. These are the lesson's two reference-table figures (`pr` 12-principles card, `tr` timing frame-count chart) plus the process flowchart (`wf`). They are the non-motion, layout-driven figures (grid card / banded table / cyclic flow), distinct craft from the motion-diagram batches 1&#8211;5: no ghost-position / arc / spacing-dot work, so the quadratic-apex on-curve trap does NOT apply to this batch. After this batch, 0 SVGs remain; then the 5 PNG phases.

**#35 principles_reference (`pr`)** &#183; dense viewBox 800x680 &#183; section twelve-principles (The Principles Working Together) + referenced again in summary (The 12 Principles Recap). High-tier one-card quick-reference of all 12 classic principles. 3x4 grid of compact cells; each cell = a brand-orange `#FF6B00` numbered chip (1&#8211;12, tie-break accent) + the principle NAME (currentColor, weight 600) + a one-line description (currentColor opacity 0.6). The 12 in canonical Disney order: 1 Squash &amp; Stretch, 2 Anticipation, 3 Staging, 4 Straight Ahead &amp; Pose to Pose, 5 Follow-Through &amp; Overlapping Action, 6 Slow In &amp; Slow Out, 7 Arcs, 8 Secondary Action, 9 Timing, 10 Exaggeration, 11 Solid Drawing, 12 Appeal. Non-chromatic carriers = numbered chips + principle names + per-cell descriptions + the grid order itself (no color-coding-by-category; color is decorative only so the card stays color-blind safe). Footer tip Variant-B: these 12 are a checklist, not a recipe &#183; most shots lean on a handful at once. id-prefix `pr-`. (This is a layout-dense card; 800x680 gives each of the 12 cells room for name + description without crowding, same density precedent as the #1 `po` pilot 3x4 grid.)

**#36 timing_reference (`tr`)** &#183; dense viewBox 800x680 &#183; section timing-and-spacing (within the Timing Reference Guidelines card). Quick-reference frame-count table at 24fps, organized as 5 horizontal speed bands top-to-bottom (slowest-to-fastest or fastest-to-slowest; lock FASTEST-AT-TOP so the eye reads escalating duration downward). Each band = a NAME pill (VERY FAST / FAST / MEDIUM / SLOW / VERY SLOW) + the frame-count range (e.g. `1&#8211;6 fr`, `6&#8211;12 fr`, `12&#8211;24 fr`, `24&#8211;48 fr`, `48+ fr`) + 2&#8211;3 example actions (blinks/impacts; gestures/quick moves; walks/reaches; heavy objects/dramatic beats; extreme weight/contemplative). A thin proportional duration bar per band (currentColor at low opacity, length proportional to the band's frame range) gives a non-chromatic visual length cue so the escalation reads as geometry, not just numbers. Non-chromatic carriers = band NAME pills + numeric frame ranges + example-action text + the proportional duration-bar lengths + top-to-bottom order. NO red-to-blue gradient as sole carrier (req-doc proposed one; rejected per color-blind rule &#8212; bars use currentColor length, not hue). Include a small `at 24 fps` note in the subtitle so the counts are anchored. Footer tip Variant-B: these are starting points, not rules &#183; always adjust to the weight and mood of the shot. id-prefix `tr-`. (800x680 dense: 5 bands each carrying a pill + range + multi-example line + duration bar overflow the 560 height.)

**#37 workflow_diagram (`wf`)** &#183; default viewBox 800x560 &#183; section what-is-animation (The Animation Mindset) + referenced again in summary (Next Steps). The professional animation process as a 5-stage cycle: PLAN &#8594; BLOCK &#8594; REFINE &#8594; POLISH &#8594; REVIEW, with REVIEW looping back. Lay the 5 stages as a horizontal pipeline of rounded stage cards (or a pentagon ring; LOCK horizontal pipeline with a return arc beneath, reads cleaner at narrow widths than a ring) each with a brand-orange numbered chip (1&#8211;5, tie-break) + stage NAME (currentColor 600) + a one-line descriptor (opacity 0.6: Plan = thumbnails + reference; Block = key poses + rough timing; Refine = add the principles; Polish = smooth curves + final timing; Review = watch, iterate, repeat). Forward arrows (currentColor opacity 0.3) between consecutive cards; a single curved return arrow from REVIEW back to the left under the row, labeled `iterate`. Non-chromatic carriers = numbered chips + stage NAMEs + descriptors + the directional forward arrows + the labeled return arc + left-to-right order. Footer tip Variant-B: animation is iterative &#183; expect to loop back through refine and polish many times before a shot is done. id-prefix `wf-`.

> Batch-6 build order: `pr` &#8594; `tr` &#8594; `wf`. Each: write via `Filesystem:write_file` to `images/lesson_24_NN_slug.svg`, on-disk verify (XML well-formed, 0 U+2014, `color="#222"` fallback present, ids namespaced), cairosvg light+dark QA Claude-side before accepting. Then append a BATCH 6 entry at the top of &#167;6, flip the &#167;4 SVG-batches row to DONE (0 SVGs remain), and re-verify the doc em-dash baseline (literal U+2014 = 0). This CLOSES SVG production (19/19); next phase = PNG capture (#13/#29 scriptable UI-chrome, #22 4-panel composite, #27/#32 manual Graph Editor).

### SVG BATCH 5 plan-lock (bouncing-ball / reference cluster) &#8212; locked 2026-06-22, build after this lock

**Selection rationale:** of the remaining 6, these 3 form the bouncing-ball project-reference cluster plus a sequential-panel sibling: the project's spec sheet (`rs`) and its staged-progression diagram (`ag`) both live in `bouncing-ball`, and the anticipation sequence (`as`) shares the same sequential-panel-strip craft as `ag`. Building them together keeps the panel-strip / ghost-position / frame-label motion-diagram craft consistent. Leaves 3 after this batch (batch 6): #35 principles_reference `pr`, #36 timing_reference `tr`, #37 workflow_diagram `wf` (the two reference-table figures + workflow diagram).

**#28 ball_reference_sheet (`rs`)** &#183; dense viewBox 800x680 &#183; section bouncing-ball (Project Overview). The bouncing-ball project's at-a-glance spec sheet: a labeled side-view bounce trajectory across the top (shared ground line, dashed `currentColor` arc series, brand-orange ghost balls at apexes + contacts, decaying apex heights) sitting above a compact spec block. Spec block = labeled rows the learner blocks the project against: frame range (e.g. `1&#8211;48`), key apex frames + heights, ball squash-on-impact / stretch-in-fall note, timing (ease-out up, ease-in down), arc rule. Each row a NAME pill + value, no color-only meaning. Non-chromatic carriers = the labeled trajectory shape + per-row name pills + numeric frame/height values + the squash/stretch state notes. Footer tip Variant-B: one reference frame to block against &#183; lock the apex frames and heights first, then refine the curves. id-prefix `rs-`.

**#30 animation_stages (`ag`)** &#183; viewBox 800x560 &#183; section bouncing-ball (Blocking). Three sequential left-to-right panels showing the project's workflow stages on the SAME bounce: (1) BLOCKING (key poses only: apex + contact ghost balls on the arc, stepped/holds feel, sparse), (2) SPACING / IN-BETWEENS (the arc filled with evenly-then-bunching ghost dots between the keys), (3) POLISH (squash/stretch on the balls + smooth arc + ease labels). Each panel a mini side-view bounce on its own ground line with a stage NAME pill + a one-line descriptor + a stage-number chip (brand-orange tie-break). Non-chromatic carriers = stage NAME pills + stage-number chips + descriptor lines + the visibly increasing dot-density / deformation across the three panels. Footer tip Variant-B: block the keys, space the in-betweens, then polish &#183; never start with polish. id-prefix `ag-`.

**#7 anticipation_sequence (`as`)** &#183; viewBox 800x560 &#183; section anticipation / twelve-principles (Anticipation). Three sequential left-to-right panels showing the anticipation beat of an action (e.g. a jump or throw): (1) ANTICIPATION (the wind-up: body/ball pulls BACK opposite the coming motion, a back-pointing arrow), (2) ACTION (the main move: fast travel in the intended direction, a long forward arrow + motion-spacing dots), (3) FOLLOW-THROUGH (overshoot + settle: a small past-the-target arrow easing back). Each panel a simple brand-orange glyph on a baseline with a stage NAME pill, a directional arrow, and a frame-span label. Non-chromatic carriers = stage NAME pills + the directional arrows (back / long-forward / short-overshoot) + frame-span labels + the panel order itself. Footer tip Variant-B: anticipation tells the viewer what is about to happen &#183; the bigger the wind-up, the bigger the action reads. id-prefix `as-`.

> Batch-5 build order: `rs` &#8594; `ag` &#8594; `as`. Each: write via `Filesystem:write_file` to `images/lesson_24_NN_slug.svg`, on-disk verify (XML well-formed, 0 U+2014, `color="#222"` fallback present, ids namespaced), cairosvg light+dark QA Claude-side before accepting. Then append a BATCH 5 entry at the top of &#167;6 and re-verify the doc em-dash baseline (literal U+2014 = 0).

### SVG BATCH 4 plan-lock (gravity / weight / bounce-physics cluster) &#8212; locked 2026-06-22, build after this lock

**Selection rationale:** of the remaining 9, these 3 form one coherent physics cluster feeding `weight-and-physics` + `bouncing-ball`: how mass reads through bounce behaviour (`bw`), the gravity parabola that governs every free-fall arc (`gp`), and the decaying bounce-height series that the project's blocking pass is built on (`bd`). Building them together keeps the motion-diagram craft (ground line, ghost positions, spacing dots, frame labels) consistent across the set. Leaves 6 after this batch: #7 `as`, #28 `rs`, #30 `ag`, #35 `pr`, #36 `tr`, #37 `wf`.

**#16 bounce_weight_comparison (`bw`)** &#183; default viewBox 800x560 &#183; section weight-and-physics. Two side-by-side bounce trajectories between a shared ground line comparing how WEIGHT reads through bounce behaviour: LIGHT (beach-ball / balloon glyph) = a tall first bounce that barely decays, many high arcs, slow settle, long hang-time; HEAVY (bowling-ball / anvil glyph) = a low first bounce that decays fast, few short arcs, quick settle, short hang-time. Each trajectory drawn as a dashed `currentColor` multi-arc path with brand-orange `#FF6B00` ghost dots at each apex and ground-contact, apex-height tick measures down to the shared ground line. Non-chromatic carriers = LIGHT / HEAVY name pills + object glyph + the visibly tall-slow-decay vs low-fast-decay arc-series SHAPE + apex-height ticks. Footer tip Variant-B: heavier objects bounce lower and settle faster &#183; weight reads through how fast the bounce decays, not the object's size. id-prefix `bw-`.

**#17 gravity_parabolic (`gp`)** &#183; default viewBox 800x560 &#183; section weight-and-physics. One free-fall / throw arc shown as a true parabola between launch and landing on a ground line, annotated to show that gravity makes the SAME parabolic shape regardless of mass: dashed `currentColor` parabolic path, brand-orange ghost dots at EVEN TIME INTERVALS (equal frame steps) so the horizontal spacing stays even while the vertical spacing bunches near the apex and widens toward the ground (the acceleration read). Frame-number labels on the dots; a small `apex` pill at the top; rising side tagged `slowing` and falling side tagged `speeding up` with short vertical spacing-bracket measures showing the bunched-near-apex / wide-near-ground gap pattern. Non-chromatic carriers = even-time ghost dots + frame labels + the vertical-spacing gradient itself + slowing/speeding-up pills + apex pill. Footer tip Variant-B: gravity accelerates everything equally &#183; the arc bunches at the top where motion slows and spreads near the ground where it speeds up. id-prefix `gp-`.

**#31 bounce_height_decay (`bd`)** &#183; default viewBox 800x560 &#183; section bouncing-ball (Blocking). The bouncing-ball project's blocking reference: a single ball's full bounce series along a shared ground line, left-to-right, each successive apex lower than the last by a roughly constant ratio (e.g. each bounce ~70% of the previous height). Drawn as a continuous dashed `currentColor` arc series with brand-orange ghost balls at each apex and each ground-contact, a horizontal dashed height-guide line per apex with a height label (e.g. `100% &#183; 70% &#183; 49% &#183; 34%`), and frame-number labels at each contact for the blocking timeline. Non-chromatic carriers = per-apex height-percentage labels + dashed height-guide lines + frame-number contact labels + the visibly decaying arc series. Footer tip Variant-B: each bounce loses a fairly constant fraction of its height &#183; block the apexes first, then fill the in-betweens. id-prefix `bd-`.

> Batch-4 build order: `bw` &#8594; `gp` &#8594; `bd`. Each: write via `Filesystem:write_file` to `images/lesson_24_NN_slug.svg`, on-disk verify (XML well-formed, 0 U+2014, `color="#222"` fallback present, ids namespaced), cairosvg light+dark QA Claude-side before accepting. Then append a BATCH 4 entry at the top of &#167;6 and re-verify the doc em-dash baseline (literal U+2014 = 0).

### SVG BATCH 3 plan-lock (arcs cluster) &#8212; locked 2026-06-22, build after this lock

**#12 arc_types (`at`)** &#183; viewBox 800x560 &#183; section arcs. Three labeled arc examples side by side showing motion paths that read as natural arcs: (1) THROWN OBJECT (high parabolic arc), (2) PENDULUM / LIMB SWING (shallow circular arc), (3) HEAD TURN (gentle S-curve / flat arc). Each drawn as a dashed `currentColor` path with brand-orange ghost dots along it (start / mid / end positions) and a small arrow at the path end. Non-chromatic carriers = type NAME label + a one-line descriptor pill per arc + frame-position ghost dots. Footer tip Variant-B: almost all natural motion travels in arcs, never straight lines. id-prefix `at-`.

**#14 arcs_comparison (`ac`)** &#183; viewBox 800x560 &#183; section arcs / twelve-principles (Arcs). Two stacked horizontal tracks comparing the SAME motion between two endpoints: TOP = NATURAL (smooth curved dashed `currentColor` arc, brand-orange ghost dots following the curve) tagged with a green check glyph + NATURAL pill; BOTTOM = MECHANICAL (same two endpoints but a straight robotic dashed path) tagged with a red cross glyph + MECHANICAL pill. Non-chromatic carriers = NATURAL / MECHANICAL name pills + check / cross glyphs + the visibly curved-vs-straight path shapes themselves (semantic green/red reinforces, never sole carrier, per #3 precedent). Footer tip Variant-B: routing motion through an arc is what separates lifelike from robotic. id-prefix `ac-`.

**#15 weight_timing (`wt`)** &#183; dense viewBox 800x680 (two vertical tracks + frame-number labels + spacing dots overflow 560) &#183; section timing-and-spacing / weight-and-physics. Two side-by-side vertical motion tracks comparing LIGHT vs HEAVY via spacing: LIGHT (feather / balloon glyph) = many ghost dots with wide even gaps, slow drift; HEAVY (anvil / bowling-ball glyph) = dots bunched near the top then rapidly widening downward (acceleration under gravity), fewer frames. Each a vertical dashed `currentColor` path with frame-number labels beside the dots. Non-chromatic carriers = LIGHT / HEAVY name pills + object glyph + frame-count labels + the dot-spacing pattern itself (wide-even vs bunched-then-widening). Footer tip Variant-B: weight is communicated through timing and spacing, not the object's size alone. id-prefix `wt-`.

> Batch-3 build order: `at` &#8594; `ac` &#8594; `wt`. Each: write via `Filesystem:write_file` to `images/lesson_24_NN_slug.svg`, on-disk verify (XML well-formed, 0 U+2014, `color="#222"` fallback present, ids namespaced), cairosvg light+dark QA Claude-side before accepting. Then append a BATCH 3 entry at the top of &#167;6 and re-verify the doc em-dash baseline (58).

### SVG BATCH 2 plan-lock (squash-and-stretch cluster) &#8212; locked 2026-06-22, build after this lock

**#5 volume_conservation (`vc`)** &#183; default viewBox 800x560 &#183; section squash-and-stretch / twelve-principles (S&amp;S). Three ball states left-to-right: SQUASHED (wide+short ellipse), NEUTRAL (perfect circle, center), STRETCHED (tall+thin ellipse). All brand-orange `#FF6B00` fill. Non-chromatic equal-volume carrier = a dashed `currentColor` bounding box drawn around each ball with a `= volume` / `100% volume` pill under each, plus width and height tick-measure lines (currentColor opacity 0.6) so the wider-shorter / taller-thinner trade reads geometrically, not by color. Subhead row labels each state. Footer tip Variant-B: volume stays constant, only the shape changes. id-prefix `vc-`.

**#19 squash_stretch_timing (`st`)** &#183; dense viewBox 800x680 (arc path + frame-number labels + per-position state pills overflow 560) &#183; section squash-and-stretch. Ortho side-view bounce cycle, 7 positions along a parabolic arc drawn as a dashed `currentColor` path: (1) neutral apex, (2) stretch falling, (3) stretch pre-impact, (4) squash impact (on ground line), (5) stretch rising, (6) stretch, (7) neutral apex. Each ball brand-orange, deformed per state (circle at neutral, vertical ellipse for stretch, horizontal ellipse for squash). Each position tagged with a frame-number label and a state pill (NEUTRAL / STRETCH / SQUASH) so state reads without color. Ground line near the squash position. Footer tip Variant-B: stretch on the fast parts, squash on impact, neutral at the slow apex. id-prefix `st-`.

**#6 material_comparison (`mc`)** &#183; default viewBox 800x560 &#183; section squash-and-stretch / twelve-principles (S&amp;S). Four balls at the moment of impact on a shared ground line, increasing rigidity left-to-right: RUBBER (extreme squash), BASKETBALL (moderate), BOWLING BALL (minimal), METAL (almost none). Each ball brand-orange, deformed as a horizontal ellipse whose flatten amount steps down across the row (rubber flattest, metal nearly circular). Non-chromatic carrier = material NAME + deformation-% label under each (e.g. `Rubber &#183; ~50% squash`, `Metal &#183; ~2% squash`), plus a thin neutral ghost-circle outline behind each ball showing its undeformed silhouette so the squash delta reads per-ball. Shared ground line. Footer tip Variant-B: softer material squashes more, rigid material barely deforms. id-prefix `mc-`.

> Batch-2 build order: `vc` &#8594; `st` &#8594; `mc`. Each: write via `Filesystem:write_file` to `images/lesson_24_NN_slug.svg`, on-disk verify (XML well-formed, 0 U+2014, `color="#222"` fallback present, ids namespaced), cairosvg light+dark QA Claude-side before accepting. Then append a BATCH 2 entry at the top of &#167;6 and re-verify the doc em-dash baseline (58).

### PNG-UI PHASE plan-lock (#13 motion_path_blender + #29 project_setup) &#8212; locked 2026-06-22, capture after this lock

**Pre-flight (done at phase open):** Blender verified TRUE-PRISTINE against the session.md baseline (3 objs Camera+Cube+Light, 1 mesh / 0 mats / 0 node_groups / 0 actions / 0 orphans / 0 empties, EEVEE/AgX/None/exp0, res 1920x1080@100, world 0.88/1.0, POINT 1000 soft 0.25, cam lens 50 / DoF OFF / blades 0, Cube unhidden, frame 1&#8211;250). **5.1.1 UI verified LIVE** (req-doc "Blender 4.0" is stale): working screen `Layout` at win 1920x1046 = VIEW_3D (x3 y112 w1291 h895) left, PROPERTIES (x1299 y35 w618 h792) + OUTLINER top-right, DOPESHEET in `TIMELINE` mode (x3 y35 w1291 h72) bottom strip. Motion Paths exists in 5.1.1 as `obj.animation_visualization.motion_path` (NOT a modifier); ops `object.paths_calculate` / `paths_update` / `paths_clear` all present; Properties OBJECT tab enum id = `OBJECT`. A leftover `temp` window (fullscreen IMAGE_EDITOR) is present from the pristine render-check; benign (no datablocks) but the capture grabs the `Layout` window explicitly.

**Capture-path routing (style guide LD20):** both are docked-panel / viewport UI chrome &#8594; full-window `bpy.ops.screen.screenshot` to a LOCAL WINDOWS TEMPDIR (carries the panel + overlay layers a scoped `screenshot_area` / `render.opengl` would drop), then bridge-side Pillow deterministic crop to the target region using the live `area.x/y/w/h` with the bottom-left&#8594;top-left origin flip (`crop box x &#8594; x+w`, `y = WIN_H-(area.y+area.h) &#8594; WIN_H-area.y`), header strip trimmed. NEVER write `screen.screenshot` direct to the WSL UNC target (truncates to 0 bytes, LD v5). Save the cropped deliverable to `images/lesson_24_NN_slug.png`. PIL 12.x is available bridge-side in Blender 5.1.1 Python.

**Staging note (breaks pristine, restored after):** the pristine scene has only the static Cube &#8212; no ball, no animation. Both figures need staged geometry, which ADDS datablocks (mesh sphere + action + optional ground plane). This intentionally breaks TRUE-PRISTINE during capture; standing rule = verify pristine BEFORE (done) and RESTORE + re-verify TRUE-PRISTINE AFTER the phase (purge staged mesh/action/plane, clear motion paths, restore Cube-only scene, AgX/world/light/cam untouched since no render recipe is used). Capture is OpenGL-free (UI screenshot path only), so no view-transform / world changes are made; AgX + world 0.88 stay as-is throughout.

**#13 motion_path_blender** &#183; section `arcs` (within the "Checking Arcs in Blender" card; deeper-indent figure variant if the card opens at col 16) &#183; overlay prefix `c13-`. Teaching point: how to check an arc using Blender's Motion Paths. **Scene staging:** add a UV sphere (the "ball"), keyframe its location across the frame range into a clean parabolic bounce arc (Z up-over-down, X travel left&#8594;right) so a motion path can be baked; set timeline frame range to the arc span; `object.paths_calculate` (RANGE) so the dotted motion-path line with frame dots renders in the viewport; turn ON `show_frame_numbers` so the path reads as timed. **View:** side/angled ortho framing the full arc (sphere selected, orange outline). **Properties:** OBJECT tab, Motion Paths panel expanded (scroll so the Calculate/Update control + the type/frame-range fields are visible). **Capture:** full-window screenshot; produce ONE composite crop that shows BOTH the viewport arc AND the Properties Motion Paths panel (the live layout already places VIEW_3D and PROPERTIES side by side, so a single full-window grab cropped to exclude only the top header + outliner + timeline gives both regions in one frame) &#8212; verify live whether a single combined crop reads at thumbnail or whether a 2-up Pillow composite (viewport-arc panel | properties panel) reads better; pick the clearer at thumbnail (decide at capture, note the choice in the &#167;6 entry). **`c13-` overlay callouts (3, drawn at INTEGRATION over recorded crop px):** (1) the dotted motion-path arc line, (2) the Calculate/Update button in the Motion Paths panel, (3) the path frame-dot visualization. Colorway: positive-green `#4ADE80` badges (informational, not warning). Pixel-verify each callout coordinate Claude-side via cairosvg-over-PNG before the INTEGRATION commit.

**#29 project_setup** &#183; section `bouncing-ball` (after the "Setup" section; default-depth figure unless inside a card) &#183; overlay prefix `c29-`. Teaching point: the bouncing-ball project's starting scene. **Scene staging:** UV sphere positioned above a large ground plane (scaled up), camera positioned for a side angle that sees the full bounce arc, timeline at frame 1 of the project range (req-doc says 96; LOCK frame 1 of 96 unless the lesson body specifies otherwise &#8212; verify against lesson HTML at integration). Simple existing POINT light is fine (no render, viewport SOLID). NO keyframes required for setup (this is the pre-animation starting state), so #29 adds a mesh sphere + a mesh plane only (no action). **View:** SOLID-shaded side view, clean chrome (optionally hide N-panel / toolbar / nav-gizmo per LD Phase-8 clean-canvas staging for a tidy frame; restore after), sphere + plane + camera visible, timeline strip showing frame 1. **Capture:** full-window screenshot cropped to VIEW_3D + the bottom TIMELINE strip together (so "timeline visible at frame 1" reads), header trimmed. **`c29-` overlay callouts (drawn at INTEGRATION over recorded crop px):** sphere (start height), ground plane, camera, timeline frame indicator &#8212; final count/placement decided against the actual crop at integration. Colorway positive-green `#4ADE80`. Pixel-verify Claude-side via cairosvg-over-PNG before commit.

> PNG-UI build order: `#13` &#8594; `#29` (both staged + captured this phase; the manual Graph-Editor pair #27/#32 and the 4-panel composite #22 are separate later phases). Per figure: stage &#8594; full-window `screen.screenshot` to Windows tempdir &#8594; bridge-side Pillow crop to `images/lesson_24_NN_slug.png` &#8594; `Filesystem:get_file_info` size-verify (accept band ~200&#8211;800 KB) &#8594; `copy_file_user_to_claude` + Claude-side view to confirm the crop reads. Record the crop pixel dimensions (they become the overlay viewBox). Overlays themselves are authored at INTEGRATION, pixel-verified via cairosvg-over-PNG before each `edit_file` commit. After BOTH captures: RESTORE Blender to TRUE-PRISTINE (purge staged sphere/plane meshes + action, `object.paths_clear`, delete staged objects, orphan-purge `do_local_ids=False`, re-verify the full pristine field set) and append a PNG-UI entry at the top of &#167;6. The doc em-dash baseline stays literal-U+2014 = 0 (this plan-lock adds none; all dashes here are `&#8211;` / `&#8212;` HTML entities or `&#183;` separators, not literal U+2014 &#8212; re-verify after commit).

### PNG-GRAPH PHASE plan-lock (#27 graph_editor_arcs + #32 graph_bounce_curve &#183; manual Graph Editor, Q4) &#8212; locked 2026-06-22, capture after this lock

**Both are MANUAL Graph-Editor captures (Q4): NOT bridge-drivable.** Ray drives the Graph-Editor framing, channel selection, panel-expand, and the Win+Shift+S overlay-freeze snip; Claude gives guidance only and does NOT script the capture. **Division of labour (locked):** the staged ball + its keyframes ARE scriptable, so Claude scripts ONE shared staged ball via the bridge (verified TRUE-PRISTINE before + after), and Ray does the Graph-Editor setup + capture for BOTH figures off that single stage. Per style-guide LD16 / decision 16: Ray saves each Win+Shift+S snip DIRECT to `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\images\` (`lesson_24_27_graph_editor_arcs.png`, `lesson_24_32_graph_bounce_curve.png`); then Claude verifies via `Filesystem:get_file_info` + `copy_file_user_to_claude` (Claude-side view), and if oversized, Pillow-rewrites IN PLACE via the bridge (`PIL.Image.open(unc).crop/resize.save(unc)`, LD16 rewrite-in-place pattern; accept band ~200&#8211;800 KB). overlay prefixes `c27-` / `c32-` (reserved). Record each snip's final px dims (become the overlay viewBox; unknown until capture, so c27/c32 viewBoxes are recorded at capture, NOT pre-locked).

**Shared staged ball (scripted by Claude via the bridge; pristine breaks during staging, restored after):** ONE UV sphere with a Z-location bounce action matching the LESSON-BODY blocking spec verbatim (bouncing-ball &#183; "Animation: Blocking the Bounces"): Z keyframes f1=10 / f18=1 / f36=7 / f54=1 / f66=4 / f78=1 / f84=2 / f90=1 (3 bounces, decreasing rebound), End frame 96, 24fps. Bezier interpolation default. This single action serves BOTH captures: #27 reads it as the arc-shaping channels (Z + optionally X for the arm-swing arc teaching), #32 reads it as the Z-location bounce curve. (Lesson body "Refining" gives the exact Z-curve teaching points, so staging the body's own keyframes makes the captured curve literally match the lesson text.) NO squash/stretch scale keys needed for either Graph-Editor figure (the curves are location curves). Claude scripts the stage, verifies it on disk / via scene info, then hands OFF to Ray with on-screen guidance.

**#27 graph_editor_arcs** &#183; section `arcs` &#8594; "Creating Arcs Manually" card &#8594; the "Using Graph Editor for arcs" subsection (card opens at col 20: DEEPER-INDENT figure variant per LD11, figure at col 20 / contents col 24 / figcaption col 24) &#183; overlay prefix `c27-`. Teaching point (from the card body): location curves should be smooth Bezier curves; adjust handles to create flowing motion; longer handles = gentler curves. **Graph-Editor setup (Ray, with Claude guidance):** open a Graph Editor on the staged ball; show the location F-curve(s) with a clearly smooth Bezier shape and VISIBLE keyframe Bezier HANDLES (select a keyframe or two so the handles draw); frame the curve so the smooth-curve shape + the handle tangents both read; keep channel-region labels legible. A 3/4 or single-channel view is fine as long as the smooth-curve-plus-handles is the dominant read. **Capture:** Win+Shift+S rectangle around the Graph-Editor region (curve + handles + enough channel/axis context to read as the Graph Editor), save direct to `images\lesson_24_27_graph_editor_arcs.png`. **`c27-` overlay callouts (authored at INTEGRATION over recorded snip px, count/placement finalized against the actual snip):** (1) the smooth Bezier curve, (2) a keyframe Bezier handle (the "longer handles = gentler curves" teaching point); a 3rd only if the snip supports it cleanly. Colorway positive-green `#4ADE80` / `#0f1a14`. cairosvg-over-PNG pixel-verify Claude-side before the INTEGRATION commit.

**#32 graph_bounce_curve** &#183; section `bouncing-ball` &#8594; "Refining: Adding Timing and Spacing" card (card opens at col 20: DEEPER-INDENT figure variant per LD11, figure at col 20 / contents col 24 / figcaption col 24) &#183; overlay prefix `c32-`. Teaching point (from the card body): the Z Location curve shows the bounce wave pattern; ease in/out (rounded) at the apex, sharp point (no easing) at each ground impact, steeper near the ground (gravity acceleration), slight plateau / hang-time at the apex. **Graph-Editor setup (Ray, with Claude guidance):** open a Graph Editor on the same staged ball; isolate the **Z Location** channel so the bounce wave reads cleanly (the 3-bounce decaying wave from the staged keys); frame all 3 bounces (f1&#8211;f90) so the decreasing apexes + the rounded-apex / sharp-impact contrast are visible; ideally show keyframe handles at one apex (rounded) and one impact (sharp) so the ease-contrast reads. **Capture:** Win+Shift+S rectangle around the Z-Location curve + channel/axis context, save direct to `images\lesson_24_32_graph_bounce_curve.png`. **`c32-` overlay callouts (authored at INTEGRATION over recorded snip px):** (1) a rounded apex (ease in/out), (2) a sharp ground-impact point (no easing), (3) the steeper-near-ground gravity segment &#8212; final count/placement decided against the actual snip. Colorway positive-green `#4ADE80` / `#0f1a14`. cairosvg-over-PNG pixel-verify Claude-side before the INTEGRATION commit.

**Pristine (breaks during staging, restored after):** scripting the shared ball ADDS datablocks (1 mesh sphere + 1 action `L24_BounceAction`, no materials/plane needed for a Graph-Editor read). Standing rule: verify TRUE-PRISTINE BEFORE staging; after BOTH snips are captured + on-disk verified, RESTORE + re-verify the full TRUE-PRISTINE field set (delete staged sphere, purge mesh + action, orphan-purge `do_local_ids=False`, Cube-only scene restored; AgX/world/light/cam untouched since no render / no view-transform change &#8212; Graph-Editor work is UI only). The Graph-Editor framing itself is NOT bridge-driven, so Claude makes no viewport/editor changes; Ray restores the editor layout to `Layout` after capture. NOTE: if BOTH snips come in correctly sized and Ray captures without asking Claude to script the stage (Ray stages his own ball), then NO bridge work happens and pristine is never broken &#8212; confirm with Ray which path before any `execute_blender_code`.

> PNG-GRAPH build order: confirm staging path with Ray (Claude-scripts-stage vs Ray-stages-own) &#8594; [if Claude scripts] verify pristine BEFORE &#8594; script shared ball + verify on disk &#8594; hand off &#8594; Ray sets up Graph Editor for #27, Win+Shift+S &#8594; `images\lesson_24_27_graph_editor_arcs.png` &#8594; Ray sets up for #32, Win+Shift+S &#8594; `images\lesson_24_32_graph_bounce_curve.png` &#8594; per snip: `Filesystem:get_file_info` + `copy_file_user_to_claude` + Claude-side view; Pillow rewrite-in-place via bridge if oversized &#8594; record each snip's px dims (overlay viewBox). Then [if Claude scripted] RESTORE pristine + re-verify, append a PNG-GRAPH entry at the top of &#167;6, flip the &#167;4 PNG-Graph row to DONE (5/5 PNG complete). Overlays authored at INTEGRATION, cairosvg-pixel-verified before commit. Doc em-dash baseline stays literal-U+2014 = 0 (all dashes here are `&#8211;` / `&#8212;` entities or `&#183;` separators &#8212; re-verify after commit). After #27 + #32: ALL 5 PNG done &#8594; INTEGRATION (inline 19 SVG + place 5 PNG w/ overlays) &#8594; browser QA 380/900px &#8594; close.
