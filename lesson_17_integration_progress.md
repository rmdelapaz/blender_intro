# Lesson 17 — HDRI and World Lighting · Integration Progress

**File:** `lesson_17_hdri_and_world_lighting.html`
**Module:** 04 — Lighting & Rendering (the lesson after L16 Three-Point Lighting)
**Status:** **L17 DONE — browser-QA PASS/CLEAN 2026-06-18.** Full Playwright + real-Chromium sweep (dark+light × 1920/1024/414): 22/22 overlay pairs register exact (|dx|=|dy|=|dh|=0.00, worst |dw|=0.16px sub-pixel at 414), 22/22 imgs load every combo, 0px 414 overflow, 0 console errors, mermaid 0/0 (no live blocks). No defects → **real HTML untouched at 274,929 B, em-dash 19 held.** See `lesson_17_browser_qa_progress.md`. **L17 is figure-complete (30/30) + QA-clean = fully DONE.** Standing browser-QA queue EMPTY (L05–L17 all cleared).
**(prior) L17 FIGURE-COMPLETE 30/30 — P8 #22 separate_lighting_nodes `c22` + P10 #29 project_setup_reference `c29` INTEGRATED** · 30/30 produced / 30/30 integrated · 2026-06-16 · HTML 274,929 B · both genuine Blender node/UI captures (bridge full-window screenshot→Windows tempdir→Pillow crop→WSL).
**(prior) P9 DONE — troubleshooting #25 `tf` troubleshooting_flowchart INLINE SVG INTEGRATED** · 28/30 produced / 28/30 integrated · 2026-06-16 · HTML 265,457 B
**(prior) P7 DONE — finding-hdris FULLY CLOSED (2/2 figs); #20 `c20` + #21 `fo` INTEGRATED** · 27/30 · 2026-06-16 · HTML 256,116 B
**(prior) P6 DONE — combining-lights FULLY CLOSED (3/3 figs); #17 `hc` / #18 `c18` / #19 `th` INTEGRATED** · 25/30 produced / 25/30 integrated · 2026-06-16 · HTML 240,669 B
**(prior) P5 DONE — hdri-types FULLY CLOSED (4/4 figs); #13/#14/#15/#16 INTEGRATED** · 22/30 produced / 22/30 integrated · 2026-06-15 · HTML 225,661 B
**(prior) P5 PRODUCTION COMPLETE** · 22/30 produced / 18/30 integrated · #13/#14/#15/#16 rendered/composited 2026-06-15 · HTML 215,086 B
**(prior) P4 DONE — adjusting-hdri FULLY CLOSED (4/4 figs)** · 18/30 produced / 18/30 integrated · #11/#9/#12/#23 INTEGRATED 2026-06-15 · HTML 215,086 B
**Entry-state P0 (byte-exact, verified on the real UNC path 2026-06-15):**

- **Bytes:** 139,279 (Python `len(bytes)` == `get_file_info` size, matched)
- **em-dash baseline:** **19** (Python `read().count('\u2014')` authoritative; all pre-existing lesson-body prose). `&mdash;` 0. **Keep at 19, add none.**
- **Body is pristine / figure-free:** `<figure>` 0 / `</figure>` 0 / `<figcaption>` 0 / `</figcaption>` 0 / `<img>` 0 / literal `<svg` 0 / `</svg>` 0 / `<marker>` 0 / `viewBox` 0 / wrapper-divs 0 / `color="#222"` 0.
- **Mermaid:** **0 live diagram blocks** (`<div class="mermaid">` 0). The 4 `mermaid` string hits are all in the `<head>` (the CDN ESM import + `mermaid.initialize`), NOT body diagrams. **D5 (keep-mermaid) does NOT apply to L17 — there is nothing to preserve.**
- **Entities present at P0:** all of `&#183;` / `&middot;` / `&#176;` / `&deg;` / `&ndash;` / `&#8211;` / `&#215;` / en-dash U+2013 / minus U+2212 / middot U+00B7 literal == **0**. Clean slate for entity tracking.
- **Sections:** 11, **no dup ids**, document order:
  `what-is-hdri` · `world-lighting-basics` · `setting-up-hdri` · `adjusting-hdri` · `hdri-types` · `combining-lights` · `finding-hdris` · `advanced-techniques` · `troubleshooting` · `project` · `summary`.

---

## Source-of-truth docs (read at every chat start)

1. `session.md` (top banner = newest)
2. this file — `lesson_17_integration_progress.md` (Status log newest + locked plan + Locked-rules)
3. `lesson_17_image_requirements.md` (enumerates 30 AI-gen prompts; **see Requirements reconciliation below — counts/palette NOT taken at face value**)
4. `images/IMAGE_STYLE_GUIDE.md` v6 (LD1–21)

---

## Requirements reconciliation (do NOT take the requirements doc at face value)

The requirements doc (`lesson_17_image_requirements.md`, v1.0, dated Nov 11 2025) enumerates **30 images** with detailed **AI-generation prompts**. Reconciliation to this pipeline:

- **Figure count = 30**, numbered #1–#30 in the requirements doc. This pipeline keeps that same #1–#30 numbering (the requirements doc's numbering is already 1..30 with no gaps, so no renumber needed — unlike some earlier lessons). Image-slug filenames follow the requirements doc's own `File Naming Convention` block verbatim (`lesson_17_NN_slug.ext`).
- **Palette OVERRIDE:** the requirements doc specifies `#ff8c00` highlight orange, `#ffd700` wire-yellow, `#4CAF50`/`#f44336` semantic greens/reds, `#667eea`/`#764ba2` annotation blues/purples, and "Blender 4.0" UI. **All superseded.** Per Locked-rules: **brand palette governs** — brand orange `#FF6B00` (NOT `#ff8c00`), overlay positive green `#4ADE80` / `#0f1a14` text, overlay warning red `#E63946` / `#ffffff` text, axis red `#E63946` / green `#4ADE80` / blue `#4A9EFF`, three-light content coding Key `#667eea` / Fill `#6BCF7F` / Rim `#FF6B9D` (the L16 D1 values). **Blender version is 5.1.1**, not 4.0 — UI-chrome captures reflect the running build.
- **"AI-generation prompts" are NOT the pipeline.** The course does not AI-generate figures; SVGs are hand-coded per v6, PNGs are BlenderMCP renders / UI-chrome captures / Pillow composites with `c{N}` overlays added at INTEGRATION. The prompts are read only as **content briefs** (what each figure must show), not as production instructions. Dimensions in the prompts are advisory; actual render/composite dims are a PRODUCTION call.
- **SVG/PNG split** follows the requirements doc's own "SVG Diagram Recommendations" (6 SVG): **#2, #4, #17, #21, #25, #28**. The other 24 are PNG (renders, UI-chrome captures, Pillow composites). This split is adopted (see registry).
- **Placement** follows the requirements doc's "Image Placement Suggestions" section-by-section table, which references the real section ids (verified against P0 — all 11 ids match). Phase plan below is in HTML section order per that table.
- **Note — requirements doc lists #2/#4 dims like 1600x900 / 1920x1080 for SVGs:** ignored. SVGs use the v6 canvas (`0 0 800 560` default or `0 0 800 680` dense), not the prompt's raster dims.

---

## Figure registry (30 figures · 6 SVG / 24 PNG)

Legend: **SVG** = hand-coded inline SVG (v6 header pattern, `currentColor`, brand palette, standalone `.svg` in `images/` with root `color="#222"` kept for `<img>` fallback, STRIPPED on inlining). **PNG** = BlenderMCP render / UI-chrome capture / Pillow composite + `c{N}` overlay at INTEGRATION (NO baked labels; pixel-verify every overlay per LD19). **PNG-plain** = self-evident beauty/comparison shot, overlay likely SKIPPED (D7 call at PRODUCTION).

| # | slug | type | id-prefix / c{N} | section (phase) | content brief (from requirements) |
|---|------|------|------------------|-----------------|-----------------------------------|
| 1 | hdr_ldr_comparison | PNG | `c1` | what-is-hdri (P1) | LDR (blown highlights/crushed blacks) vs HDR (detail both ends), value scales 8-bit vs 32-bit |
| 2 | dynamic_range_spectrum | **SVG** | `dr` | what-is-hdri (P1) | two gradient bars: LDR 8-bit 0/128/255 vs HDR 32-bit 0.0/1.0/2.0/5.0/10.0+; sun/bulb/ambient/shadow value icons |
| 3 | hdri_sphere_concept | **SVG** | `sp` | what-is-hdri (P1) | spherical env map around object, light-ray arrows inward (IBL concept). **D-A RESOLVED → SVG. PRODUCED + on disk (6,472 B).** |
| 4 | equirectangular_projection | **SVG** | `eq` | what-is-hdri (P1) | flat 2:1 equirect map + lat/long grid + small sphere showing wrap |
| 5 | world_properties_panel | PNG (UI) | `c5` | world-lighting-basics (P2) | Properties panel, World icon highlighted, Surface/Background controls |
| 6 | default_world_shader | PNG (UI) | `c6` | world-lighting-basics (P2) | Shader Editor World mode: default Background → World Output |
| 7 | hdri_setup_workflow | PNG | `c7` | setting-up-hdri (P3) | 4-panel progression: empty → +Env Texture → load HDRI → connected |
| 8 | complete_node_network | PNG (UI) | `c8` | setting-up-hdri (P3) | Env Texture → Background → World Output, labeled/color-coded |
| 10 | viewport_shading_icons | PNG (UI) | `c10` | setting-up-hdri (P3) | viewport top-right 4 shading icons, Material Preview + Rendered highlighted |
| 26 | viewport_mode_comparison | PNG | `c26` | setting-up-hdri (P3) ✓INTEGRATED | same HDRI scene in Solid / Material Preview / Rendered |
| 27 | node_connection_closeup | PNG (UI) | `c27` | setting-up-hdri (P3) | zoomed Color out → Color in socket connection |
| 28 | file_formats_guide | **SVG** | `ff` | setting-up-hdri (P3) | .hdr/.exr (check) vs .jpg/.png (X); LD8 colorway split |
| 24 | quality_comparison | PNG | `c24` | setting-up-hdri (P3) ✓INTEGRATED | 2K vs 4K vs 8K reflection close-ups |
| 11 | strength_comparison | PNG | `c11` | adjusting-hdri (P4) | same scene strength 0.5 / 1.0 / 2.5 (3-panel) |
| 9 | rotation_node_setup | PNG (UI) | `c9` | adjusting-hdri (P4) | Tex Coord → Mapping → Env Texture; Z-rotation highlighted |
| 12 | rotation_effect | PNG | `c12` | adjusting-hdri (P4) | same object HDRI rotated 0/90/180/270 (2x2) |
| 23 | color_grading_nodes | PNG (UI) | `c23` | adjusting-hdri (P4) | HSV / ColorRamp / RGB Curves between Env Texture and Background (3-panel) |
| 13 | hdri_types_grid | PNG | `c13` | hdri-types (P5) ✓INTEGRATED | 6-panel: clear sky / cloudy / sunset / studio / interior / night-urban |
| 14 | outdoor_examples | PNG | `c14` | hdri-types (P5) ✓INTEGRATED | 4-panel: clear-sky / cloudy / sunset / night on same object |
| 15 | studio_examples | PNG | `c15` | hdri-types (P5) ✓INTEGRATED | 3-panel: three-point studio / softbox grid / single key + ambient |
| 16 | interior_examples | PNG | `c16` | hdri-types (P5) ✓INTEGRATED | 3-panel: window / artificial / mixed |
| 17 | hybrid_concept_diagram | **SVG** | `hc` | combining-lights (P6) | Venn: HDRI (ambient/reflections) + Manual (key/fill/rim) = best results |
| 18 | hdri_key_light_setup | PNG | `c18` | combining-lights (P6) | split: render result + node/light setup (low-strength HDRI + Area key) |
| 19 | three_point_hdri_diagram | PNG | `c19` | combining-lights (P6) | top-down: subtle HDRI sphere + Key/Fill/Rim cones labeled. **Likely SVG-diagram — D-B** |
| 20 | poly_haven_navigation | PNG (UI) | `c20` | finding-hdris (P7) | Poly Haven site, search/categories/grid/card/download highlighted |
| 21 | file_organization | **SVG** | `fo` | finding-hdris (P7) | folder tree: Outdoor/Studio/Interior/Creative subfolders |
| 22 | separate_lighting_nodes | PNG (UI) | `c22` | advanced-techniques (P8) | Light Path → Mix Shader, 2 Backgrounds (camera low / lighting high) |
| 25 | troubleshooting_flowchart | **SVG** | `tf` | troubleshooting (P9) | decision tree "HDRI not working?" → checks → green solution boxes |
| 29 | project_setup_reference | PNG (UI) | `c29` | project (P10) | project scene: object + ground + camera 45° + HDRI bg + "default light deleted" |
| 30 | before_after_comparison | PNG | `c30` | what-is-hdri (P1) | 2-panel: default gray world vs outdoor HDRI on same glossy object |

**Counts:** 30 figures. SVG = **7** (`dr` #2, `eq` #4, **`sp` #3**, `ff` #28, `hc` #17, `fo` #21, `tf` #25). PNG = **23**. (D-A resolved #3 → SVG.) Of the PNG: **UI-chrome captures = 11** (#5,#6,#8,#9,#10,#20,#22,#23,#27,#29 + the workflow #7 which is a node-panel composite); **renders/composites = ~13** (#1,#3,#11,#12,#13,#14,#15,#16,#18,#19,#24,#26,#30).

> **Note on #3 and #19:** both are tagged in the requirements as "3D diagram" and could go either route — a BlenderMCP render OR a hand-coded SVG diagram. Logged as **D-A (#3)** and **D-B (#19)** below; resolve at their PRODUCTION phase. If rendered, they are PNG + `c{N}`; if hand-coded, they convert to SVG (and the SVG count rises to 7 or 8). Registry currently lists them PNG as the default assumption.

---

## id-prefix registry (inline SVGs)

Unique `{id}` prefix per inline SVG so they coexist on one page without id collisions (v6 header pattern, ids namespaced `{prefix}-title` / `{prefix}-desc` + any internal ids):

| prefix | figure | viewBox |
|--------|--------|---------|
| `dr` | #2 dynamic_range_spectrum | 800×560 (or 680 if icon row crowds) |
| `eq` | #4 equirectangular_projection | 800×680 dense (map + sphere + arrows) |
| `ff` | #28 file_formats_guide | 800×560 |
| `hc` | #17 hybrid_concept_diagram | 800×680 dense (Venn + 2 example tiles) |
| `fo` | #21 file_organization | 800×680 dense (4 top-level + 13 subfolders) |
| `tf` | #25 troubleshooting_flowchart | 800×680 dense (decision tree) |
| `sp` | #3 sphere_concept — **D-A RESOLVED → SVG, PRODUCED** | 800×680 |
| (`th`) | #19 three_point_hdri — RESERVED if D-B resolves to SVG | 800×680 |

## c{N} overlay namespace (PNG overlays)

Each PNG overlay's ids namespaced `c{N}-*` (e.g. `c7-marker`, `c8-badge`). Composite-overlay / numbered-pill / centered-pill variants carry NO `c{N}-*` ids (uniqueness tracked via the PNG being referenced once each, not by id count) — same convention as L14/L15/L16. Active c{N}: c1, c5, c6, c7, c8, c9, c10, c11, c12, c13, c14, c15, c16, c18, c19, c20, c22, c23, c24, c26, c27, c29, c30. (SVG figures `dr`/`eq`/`sp`/`ff`/`hc`/`fo`/`tf` carry NO c{N}. **c3 RETIRED — #3 is now SVG.**)

---

## Phase plan (HTML section order — P1 → P10; summary has no figures)

Bottom-up insertion within each phase; anchors re-verified count=1 on a fresh copy at INTEGRATION time by unique multi-line TEXT (never by section id). `edit_file` dryRun → commit → `get_file_info` each.

| Phase | section | figs | count | SVG/PNG | sub-chat policy |
|-------|---------|------|-------|---------|-----------------|
| **P1** | what-is-hdri | #1, #2, #30, #3, #4 | **5** | **3 SVG (#2,#4,#3) DONE** + 2 PNG (#1,#30) | **SVGs all produced this chat.** Renders #1,#30 → fresh PRODUCTION sub-chat. INTEGRATION inline (all 5) once renders land. |
| **P2** | world-lighting-basics | #5, #6 | 2 | 2 PNG (UI) | single chat (UI-chrome, both World/Shader panel captures) |
| **P3** | setting-up-hdri | #7, #8, #10, #26, #27, #28, #24 | **7** | 1 SVG (#28) + 6 PNG | **≥6 → PRODUCTION sub-chat split (A/B) + its OWN INTEGRATION chat.** Heavy UI-chrome load (#8,#10,#27 captures + #7 workflow composite) + 2 renders (#26,#24) + 1 SVG (#28). |
| **P4** | adjusting-hdri | #11, #9, #12, #23 | 4 | 4 PNG | single chat; #9/#23 UI-chrome, #11/#12 renders. Pre-plan PRODUCTION sub-chat if the 2 renders + 2 captures crowd one chat. |
| **P5** | hdri-types | #13, #14, #15, #16 | 4 | 4 PNG (all renders/composites) | single PRODUCTION chat (4 multi-panel HDRI renders share one world-node scene); INTEGRATION inline or own chat per context. Heaviest render phase. |
| **P6** | combining-lights | #17, #18, #19 | 3 | 1 SVG (#17) + 2 PNG (#18 render-composite, #19 diagram/render) | single chat; resolve **D-B (#19 route)** at PRODUCTION |
| **P7** | finding-hdris | #20, #21 | 2 | 1 SVG (#21) + 1 PNG (#20 UI) | single chat |
| **P8** | advanced-techniques | #22 | 1 | 1 PNG (UI, complex node net) | single chat |
| **P9** | troubleshooting | #25 | 1 | 1 SVG (#25) | single chat (all-SVG) |
| **P10** | project | #29 | 1 | 1 PNG (UI) | single chat |

**Phases flagged for sub-chat splits:** P1 (5 → PRODUCTION split), **P3 (7 → PRODUCTION A/B + own INTEGRATION chat)**. P5 (4 renders) is the heaviest single-scene render phase — pre-plan a PRODUCTION sub-chat if it crowds. All other phases single-chat / inline.

**summary** section: requirements doc places no figures there. Unless Ray wants a closing reference card (e.g. an HDRI-workflow cheat-sheet, the L16 `crs` precedent), summary stays figure-free. Flagged as **D-G**.

---

## DECISIONS (open unless marked resolved)

- **D-A — #3 HDRI sphere-wrapping concept: render or hand-coded SVG? RESOLVED 2026-06-15 → hand-coded SVG (Ray-confirmed).** Produced this chat as `images/lesson_17_03_hdri_sphere_concept.svg` (prefix `sp`, viewBox 800×680, 6,472 B): translucent env-sphere shell + sun, central shaded object, inward light-ray arrows color-coded warm `#FF8A33` (sun) / cool `#4A9EFF` (sky) / dim `#8A8F98` (shadowed ground) + legend; cairosvg light+dark render-checked, both clean. The inward-ray IBL annotation reads clearer as a diagram than a fiddly translucent-sphere render. c3 RETIRED.
- **D-B — #19 three-point + HDRI top-down diagram: render or SVG?** Same family as L16 #1/#2 (`tpo`/`tps` top + side three-point diagrams, which were hand-coded SVG) and L16 #21 `fp` four-point. A top-down hybrid diagram with subtle HDRI ring + Key/Fill/Rim cones is a natural **hand-coded SVG** (prefix `th`, three-light D1 coding). **Lean SVG.** Resolve at P6 PRODUCTION.
- **D-C — HDRI source assets (.hdr / .exr). RESOLVED 2026-06-15 → use `download_polyhaven_asset` CC0 downloads (Ray-confirmed).** HDRI lighting renders (#1,#3,#11,#12,#13,#14,#15,#16,#18,#24,#26,#30) need real environment textures. **Plan at PLANNING (this doc):** the project already has BlenderMCP + Poly Haven download path (`blender:download_polyhaven_asset` is in the deferred tool list). Options per figure: (a) `download_polyhaven_asset` to pull a small set of CC0 HDRIs (clear sky, cloudy, sunset, studio, interior, night) for the type/strength/rotation figures; (b) for figures where only the *effect* matters (strength, rotation), one HDRI reused across panels is enough; (c) procedurally-built world (Sky Texture node, gradient) where a literal photographic HDRI is not essential. **PRODUCTION owns the per-figure choice; PLANNING locks the principle: prefer one or two CC0 Poly Haven HDRIs reused across the comparison phases for subject/lighting parity, supplemented by a Sky Texture node where a synthetic sky suffices.** STOP and ask Ray if a download path is unavailable at PRODUCTION.
- **D-D — color-dependent render recipe (LD17 Standard view transform) is DEFAULT-LIKELY here, per figure.** Unlike L16 (where AgX held for most neutral-grey three-point renders), L17's teaching point *is* color: warm sunset, cool clear sky, neon/urban, color-temperature contrast. AgX will likely wash these warm/cool environment tones (the L15 #13 sun-time-of-day + L16 #20 color-temperature precedents both invoked Standard). **Default expectation: invoke LD17 Standard + look None for the type/strength/outdoor/interior/before-after color figures (#1,#11,#13,#14,#15,#16,#30), hold AgX only where a neutral-grey probe shows no wash. RESTORE AgX at every PRODUCTION close.** Confirm per figure via luminance/neutral probe at PRODUCTION (do not pre-force). **RESOLVED at P5 2026-06-15 → Standard + look None for all four P5 figs** (AgX-vs-Standard probe showed AgX compresses R-B on every color-critical env — sunset/clearsky/interior; the chrome mirrors the sky so AgX mutes the tint). AgX restored at teardown. (P4 also resolved Standard for #11/#12.)
- **D-E — clean scene base for the world-node pipeline (HDRI-specific).** Renders here lean on **world/environment-texture lighting** (Environment Texture → Background → World Output), NOT the three-point rig. The clean scene base for L17 PRODUCTION = a single subject primitive (glossy sphere or a simple product, per requirements' "consistent test object") on a matte/neutral ground, camera at ~45°, **no manual lights** (the world node carries the lighting), default world reset (grey 0.05–0.88 depending on recipe) at close. **STOP at PRODUCTION close if a leftover three-point rig OR a leftover Environment-Texture world-node is found — restore to the documented clean base + AgX + default Background world.** For the hybrid phase (P6 #18/#19) the scene legitimately *adds* manual lights to the world — tear those down at close too.
- **D-F — subject object for comparison parity.** Requirements call for "consistent test object" across comparisons (glossy/chrome sphere is the simplest; a "modern product/sculpture" for the example renders). **Lean: a chrome/glossy sphere on a matte ground for the reflection-bearing comparisons (#11,#12,#13,#24,#26,#30) so HDRI reflections read; a slightly more shaped primitive (the L16 procedural bust or a simple product solid) only if Ray wants the example renders #14/#15/#16/#18 to look more "product".** Resolve at P5 PRODUCTION; confirm with Ray. **RESOLVED at P5 2026-06-15 → chrome sphere for ALL four P5 figs** (Ray-confirmed; metallic 1.0 rough 0.08 base 0.55, matte 0.32 ground, 50mm cam @ ~45°, world-node lighting only). No product solid used.
- **D-G — summary closing figure?** Requirements place none. Optional HDRI-workflow cheat-sheet card (the L16 `crs` complete-reference-sheet precedent) if Ray wants a capstone. **Default: none.** Ray's call.
- **D-H — #7 setup workflow (4-panel) production route.** Four progressive Shader-Editor states (empty → +Env Tex → load HDRI → connected). Either four UI-chrome captures Pillow-composited, OR a hand-built node-graph composite. **Lean: four local-tempdir full-window `screen.screenshot` captures of the World Shader Editor at each state (per LD20 docked-panel routing), Pillow-composited 2×2 with `#FF6B00` 2px dividers, NO baked step labels (the `c7` overlay carries the 4 step pills).** Resolve at P3 PRODUCTION.
- **D-I — #24 quality_comparison HDRI choice. RESOLVED 2026-06-15 → `brown_photostudio_02` (detail-rich CC0 studio HDRI).** The persisted soft `belfast_sunset_puresky` made 2K/4K/8K chrome reflections visually indistinguishable (would teach the OPPOSITE of "resolution matters"), so a detail-rich CC0 studio HDRI `brown_photostudio_02` (hard high-frequency content: triangular light fixtures, window mullion grid, hex wall tiling) was pulled via `download_polyhaven_asset` and persisted at `images\hdri\brown_photostudio_02_{2k,4k,8k}.hdr`. Separation confirmed by Laplacian-variance before compositing (2K 74.8 / 4K 95.1 / 8K 111.8, monotone). #24 = `brown_photostudio_02`; the belfast HDRI remains the parity HDRI for the other comparison phases.

---

## Locked-rules carry-forward (UNCHANGED from L05–L16; full list in `lesson_06_completion_handoff.md` + each lesson's progress-doc Locked-rules block + `IMAGE_STYLE_GUIDE.md` v6 LD1–21)

- **Paths:** WSL `\\wsl$\Ubuntu\...` ONLY (never `\\wsl.localhost\`). Real project root `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`. Reachable by Filesystem tool AND by Blender-side Python (`open(r"\\wsl$\Ubuntu\...")` for the large-file bridge). The Claude-side bash `/mnt/wsl/...` mount is a SEPARATE near-empty filesystem — NEVER read baselines or commit there. Confirm every write by re-reading the real path's byte count.
- **Edit cadence:** `Filesystem:edit_file` dryRun=true → commit dryRun=false → `Filesystem:get_file_info` verify, after every commit. `Filesystem:write_file` for new files / full-doc overwrites (NEVER `create_file`). `copy_file_user_to_claude` to refresh the working HTML copy before grep/sed, and **verify anchor GLOBAL uniqueness first by unique multi-line TEXT extended until count=1 — never by section id.**
- **em-dash discipline:** no NEW em-dashes (U+2014). New separators use middle-dot `&#183;` entity. Pre-existing em/en-dashes preserved verbatim through exact anchors. Content entities `&#215;` / `&#8211;` / `&#8594;` / `&#183;` / `&#176;` fine. **L17 baseline = 19; Python `count('\u2014')` authoritative (grep miscounts).**
- **Brand palette governs** all overlays/SVG furniture (NOT the requirements doc's `#ff8c00`/`#4CAF50`/`#667eea`/`#ffd700`). Brand orange `#FF6B00`; overlay positive green `#4ADE80`/`#0f1a14`, warning red `#E63946`/`#ffffff`; axis X `#E63946` / Y `#4ADE80` / Z `#4A9EFF`; three-light Key `#667eea` / Fill `#6BCF7F` / Rim `#FF6B9D` (D1).
- **Inline-SVG conventions (LD12):** standalone `.svg` to `images/` keeps root `color="#222"` for `<img>` fallback; STRIP it on inlining; re-indent figure col20 / svg col24 / children col28 / deeper col32 (or col16/20/24/28 default-depth, col20/24/28/32 deeper-indent inside cards); collapse multi-line attrs; prune decorative `====` comments; keep structural comments; preserve every `role`/`aria-labelledby`/`<title>`/`<desc>`/`id` verbatim. v6 header pattern (3px `#FF6B00` top strip, title y=56 fs24/700, subtitle y=84 fs14 op0.65, divider y=108, first body subhead y=138; footer tip Variant-B left rail y=506/528 default or y=641/663 dense).
- **PNG-overlay pattern (LD10):** wrapper-div `position: relative; display: inline-block; max-width: 100%` + img `display:block; max-width:100%; height:auto; margin:0` `loading="lazy"` + inlined overlay-svg `viewBox = PNG pixels` `preserveAspectRatio="xMidYMid meet"` `inset:0 width:100% height:100% pointer-events:none`; figcaption OUTSIDE the wrapper div. NO baked labels on PNGs. Overlay ids `c{N}-*` (composite/numbered/centered-pill variants carry none).
- **Pixel-verify every PNG overlay BEFORE commit (LD19):** cairosvg-composite (Claude-side, 1:1 since viewBox==PNG px) or PIL-composite (bridge-side). No `_overlay_VERIFIED.svg` artifact left behind.
- **BlenderMCP:** lowercase `blender:execute_blender_code`. Clean-scene-manually via `bpy.data.objects.remove`; BLENDER_EEVEE (EEVEE_NEXT→EEVEE fallback) for viewport, **Cycles for HDRI/reflection/refraction renders** (HDRI lighting needs ray-traced bounces — Cycles is the default here, unlike many L16 EEVEE figures). NEVER `read_factory_settings`. Inline all render helpers per call (namespaces don't persist). **Scene state between PRODUCTION chats is always a clean base (RESTORE at close; STOP if a leftover world-node rig or hidden base objects are found).** F12 `render.render(write_still=True)` + Standard view-transform per LD17 only when a PRODUCTION render shows color washing (HIGHLY likely here — D-D).
- **UI-capture craft (LD14–16, LD20):** modal status bar authoritative for bindings; full-window local-tempdir `screen.screenshot` then deterministic area crop (UNC direct-write truncates to 0 bytes — capture to Windows tempdir then Pillow→WSL); Win+Shift+S overlay-freeze for transient popovers/dropdowns/pickers saved direct to WSL `images/`; clean-canvas viewport staging hides toolbar/N-panel/nav-gizmo. **`space.context` flip to switch Properties/Shader-Editor tab before the grab; `space.shader_type = 'WORLD'` to put the Shader Editor in World mode for the node-network captures (#6,#8,#9,#22,#23,#27).**
- **Sub-chat policy:** pre-plan PRODUCTION sub-chat split for ≥5-figure phases + own INTEGRATION chat for ≥6. Plan-lock before build. **Warn Ray before context tightens** rather than letting automatic compaction happen.

---

## Status log (newest first)

**L17 FIGURE-COMPLETE 30/30 — #22 + #29 INTEGRATED, P8 + P10 CLOSED 2026-06-16** — the last 2 figures landed this chat; **L17 now 30/30 produced / 30/30 integrated, HTML 274,929 B.** Entry P0 re-verified byte-exact on the real file (265,457 B, em-dash 19, figure/figcaption 28/28, img 20, svg 28/28, marker 5, wrapper-divs 20, color222 0, `&#183;` 60, `&#8226;` 7, `&#8211;` 12, `&#176;` 9, `&#215;` 1, `&#8594;` 2, `&#8805;` 1, c22/c29 absent, 11 ids no dups, 0 live mermaid). Blender clean base confirmed at entry (0/0/0/0/0 + AgX + look None + EEVEE + grey-0.05 Background→World Output). **Both are genuine Blender node/UI captures** — bridge full-window `screen.screenshot` → Windows tempdir → Pillow crop → WSL relay (LD14/16/20). AgX HELD throughout (UI captures, no color-critical render — D-D not invoked, no view-transform restore owed).

**#22 separate_lighting_nodes PRODUCTION:** built the World node network (`space.shader_type='WORLD'` on area3 Shader Editor): Texture Coordinate → Mapping → Environment Texture (belfast_sunset_puresky_2k reloaded) → splits to two Background nodes — **Lighting (high) Strength 3.000** + **Camera/Background (low) Strength 0.300** — → **Mix Shader** (Light Path **Is Camera Ray** → Factor) → World Output. Hid N-panel/toolbar, `node.view_all` to frame, full-window screenshot → crop region → tight content crop. `images/lesson_17_22_separate_lighting_nodes.png` **985×550, 72,506 B** (get_file_info + decode-verified RGB; breadcrumb shows Scene›World confirming WORLD mode; both strength values + Light Path Is-Camera-Ray + Mix Shader all legible, no UI bleed). **`c22` overlay** = 4 numbered callouts (1 Light Path · Is Camera Ray / 2 Lighting · high strength / 3 Background · low strength / 4 Mix Shader combines both), numbered-pill (`#FF6B00` badges + leaders, `#1a1a1a` 0.82 pills + white text, NO `c22-*` ids), viewBox 0 0 985 550; cairosvg-pixel-verified 1:1 over the real on-disk PNG (LD19) + visually reviewed. **INTEGRATION:** anchor count=1 = before `<h3>Separate Lighting and Background</h3>` (after the section intro `<p>`, per requirements placement). Default-depth LD10 (wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=985×550 preserveAspectRatio xMidYMid meet, figcaption OUTSIDE). edit_file dryRun→commit→get_file_info. Byte trail: 265,457 (P0) → #22 +4,788 → 270,245 B.

**#29 project_setup_reference PRODUCTION:** restored area3 to VIEW_3D, reset world to a simple HDRI chain (Environment Texture(belfast) → Background str 1.0 → World Output), built the project test scene — Suzanne (shade-smooth, grey BSDF rough 0.35, rot Z 20°) on a matte 0.32 ground plane (size 30), camera at (5,-5,3.2) lens 50 aimed at (0,0,1.1) ≈ 45°, **NO manual lights** (HDRI carries lighting). Viewport RENDERED + camera view, chrome hidden, full-window screenshot → crop → header-trim (drop top menu strip, keep "Camera Perspective" label). `images/lesson_17_29_project_setup_reference.png` **1333×788, 869,872 B** (get_file_info + decode-verified RGB; shows test object on ground, camera framing marquee, HDRI sky bg, grid floor + X/Y axes, no lights). **`c29` overlay** = 4 numbered callouts (1 Test object / 2 Ground plane / 3 Camera at ~45° / 4 HDRI background · no manual lights), numbered-pill (NO `c29-*` ids), viewBox 0 0 1333 788; cairosvg-pixel-verified 1:1 over the real on-disk PNG (LD19) + visually reviewed. **INTEGRATION:** anchor count=1 = before `<h3>Part 1: Scene Setup</h3>` (after the Project-Goals card `</div>`). Default-depth LD10. edit_file dryRun→commit→get_file_info. Byte trail: 270,245 → #29 +4,684 → **274,929 B** (get_file_info-confirmed).

**POST-COMMIT INTEGRITY SWEEP CLEAN (real file 274,929 B):** em-dash **19 HELD**, `&mdash;` 0, color222 0, figure/figcaption 28→**30** balanced, img 20→**22**, literal `<svg`/`</svg>` 28→**30**, `<marker` **5 unchanged** (PNG overlays add none), wrapper-divs 20→**22**, c22 PNG once / c29 PNG once, c22-/c29- ids **0** (numbered-pill, expected), `&#183;` 60→**66** (+6: c22/c29 pills + figcaptions), `&#176;` 9→**11** (+2: c29 "~45°" pill + figcaption), `&#8226;` 7 / `&#8211;` 12 / `&#215;` 1 / `&#8594;` 2 / `&#8805;` 1 unchanged, literal middot/en-dash/minus 0, 11 ids no dups, 0 live mermaid. **TEARDOWN:** scene reset to clean base 0/0/0/0/0 + AgX + look None + EEVEE + grey-0.05 Background→World Output (Suzanne/Ground/Cam + 2 mats + HDRI image datablock removed; persisted .hdr on disk RETAINED), orphans purge (none), area3 chrome re-shown. Temps purged + ENOENT-verified: Windows `C:\Users\Public\L17P8` + `C:\Users\Public\L17P10` isdir False, WSL `_tmp_l17p8_*` + `_tmp_l17p10_*` glob empty, no `_overlay_VERIFIED.svg`. No BlenderMCP debt. Registry rows #22/#29 → INTEGRATED; §6 P8/P10 tables added. **P8 advanced-techniques + P10 project CLOSED. L17 FIGURE-COMPLETE 30/30. Next: browser-QA pass.**

**L17 P9 DONE — #25 troubleshooting_flowchart INTEGRATED, troubleshooting CLOSED 2026-06-16** — all-SVG hand-coded phase. **L17 now 28/30 produced / 28/30 integrated, HTML 265,457 B.** Entry P0 re-verified byte-exact on the real file (256,116 B, em-dash 19, figure/figcaption 27/27, img 20, svg 27/27, marker 4, wrapper-divs 20, `&#183;` 57, `&#8226;` 4, `&#8211;` 12, `&#176;` 9, `&#215;` 1, `&#8594;` 2, color222 0, tf-/c25 absent, 11 ids no dups, 0 live mermaid). No Blender scene/render/teardown owed. Of the 3 remaining phases, P9 is the only all-SVG one (P8 #22 + P10 #29 are Blender node/UI captures) — did P9 here as the cleanest fit. **PRODUCTION:** decision-tree SVG viewBox `0 0 800 680`, id-prefix `tf` (tf-title/tf-desc + `tf-arrow` marker). Distills the troubleshooting section's 7 problems: orange start "HDRI not working?" → blue diamond "Is the HDRI visible in render?" — NO branch → dark-scene fix box (Use World not Object / Environment Texture node / Strength ≥ 1.0) → green FIX box; YES → blue diamond "Colors / look wrong?" → 3 green fixes (Washed out→Standard view transform / Wrong angle→Mapping node rotate Z / Grainy→samples + Denoise). Legend + Variant-B footer tip. 0 em-dashes (`&#183;`×2 / `&#8226;`×3 / `&#8805;`×1 NEW). cairosvg light+dark clean (currentColor cascades both modes). Standalone `images/lesson_17_25_troubleshooting_flowchart.svg` **7,212 B** (root color222 KEPT). **INTEGRATION:** inlined per LD12 (root color222 STRIPPED, figure col20 / svg col24 / children col28 / `<g>` col32, tf-arrow marker + defs + tf- ids + role/aria/title/desc preserved, figcaption col20), anchor count=1 = before `<h3>HDRI Not Showing/Scene Too Dark</h3>` (beginning of `troubleshooting`, after intro `<p>`). edit_file dryRun→commit→get_file_info. Byte trail: 256,116 (P0) → +9,341 → **265,457 B**. **POST-COMMIT SWEEP CLEAN:** em-dash **19 HELD**, figure/figcaption 27→**28**, img **20 unchanged**, `<svg`/`</svg>` 27→**28**, `<marker` 4→**5** (tf-arrow; inline-SVG-with-marker, unlike PNG-overlay phases where markers stay 4), wrapper-divs **20 unchanged**, `&#183;` 57→**60**, `&#8226;` 4→**7**, `&#8805;` 0→**1**, `&#8211;`/`&#176;`/`&#215;`/`&#8594;` unchanged, literal middot/en-dash/minus 0, tf- 12 (arrow id once + ref 7×), c25 token 0, 11 ids no dups, 0 live mermaid, color222 0. No teardown/temps/debt. Registry row #25 → INTEGRATED; §6 P9 entry added. **Next: 2 figs to L17-complete — P8 #22 separate_lighting_nodes `c22` + P10 #29 project_setup_reference `c29`, both genuine Blender node/UI captures.**

**L17 P7 DONE — #20 poly_haven_navigation INTEGRATED, finding-hdris FULLY CLOSED 2026-06-16** — the last P7 fig landed this chat (#21 `fo` landed prior). **P7 figure-complete 2/2, L17 now 27/30 produced / 27/30 integrated, HTML 256,116 B.** Entry P0 re-verified byte-exact on the real file (251,622 B, em-dash 19, figure/figcaption 26/26, img 19, svg 26/26, marker 4, wrapper-divs 19, `&#183;` 56, `&#8211;` 12, `&#8226;` 4, `&#176;` 9, `&#215;` 1, `&#8594;` 2, color222 0, c20 absent, fo- 18, hc-/th-/ff- 4 each, 11 ids no dups, 0 live mermaid; Premium-anchor count=1). Blender clean base confirmed (get_scene_info 0 obj / 0 mat); #20 is UI-chrome, no render scene owed. **BLOCKER RESOLVED via route (b) — probed FIRST per the kickoff note:** the Windows Blender Python `urllib` CAN reach Poly Haven's public API/CDN (api.polyhaven.com/assets + /files HTTP 200; CDN thumbnails real `image/png`) — the bridge network is separate from Claude-side bash's `403 host_not_allowed`. So NO browser screenshot/pixel-relay needed: fetched real CDN imagery + real metadata bridge-side and composited a faithful annotated nav reference (PIL 12.2.0 + Segoe UI). **PRODUCTION:** `images/lesson_17_20_poly_haven_navigation.png` **2200×1180, 953,973 B** (get_file_info + decode-verified RGB; the PIL-to-UNC write confirmed intact via the separate Filesystem-tool process, not truncated). Faithful Poly Haven HDRIs page: top nav (HDRIs active), LEFT sidebar category filters with REAL counts (All 972 / Outdoor 686 / Skies 293 / Indoor 295 / Studio 96 / Sunrise-Sunset 217 / Night 59 / Nature 519 / Urban 487) + Browse-by-category thumbnail row; CENTER toolbar (search + "972 results" + Sort: Popular) + 3×2 HDRI grid of 6 real CC0 CDN thumbnails (Kloofendal 48d / Lilienstein / The Sky Is On Fire / Brown Photostudio 02 / Studio Small 09 / Dikhololo Night, CC0 pills); RIGHT asset-page panel for Kloppenheim 06 (real CDN preview + 360° badge + breadcrumb + CC0 License + Resolution 1K/2K/4K/8K/16K [4K active] + Format HDR/EXR [EXR active] + Download "4K · EXR · 19.77 MB" = the REAL 4K-EXR size 19,767,459 B from the API). NO baked labels. **`c20` overlay** = 5 numbered callouts (1 Search bar / 2 Category filters / 3 HDRI grid / 4 Individual card / 5 Multiple resolutions), numbered-pill style (`#FF6B00` badges + leaders, `#1a1a1a` 0.82 pills + white 600-wt text, NO `c20-*` ids), viewBox 0 0 2200 1180; cairosvg-pixel-verified 1:1 over the real on-disk PNG (LD19) + visually reviewed (callout 5 re-aimed twice to land on the resolution selector + fit within canvas). **INTEGRATION:** anchor count=1 = before `<h3>Premium HDRI Resources</h3>` (figure AFTER the Free-HDRI-Resources card `</div>`, BEFORE the Premium h3). Default-depth LD10 wrapper-div + img margin:0 loading=lazy + inlined overlay-svg (viewBox=2200×1180, preserveAspectRatio xMidYMid meet, inset:0 width:100% height:100% pointer-events:none), figcaption OUTSIDE. Single edit_file dryRun→commit→get_file_info. Byte trail: 251,622 (P0) → #20 +4,494 → **256,116 B** (get_file_info-confirmed). **POST-COMMIT INTEGRITY SWEEP CLEAN (real file 256,116 B):** em-dash **19 HELD**, `&mdash;` 0, color222 0, figure/figcaption 26→**27** balanced, img 19→**20**, literal `<svg`/`</svg>` 26→**27**, `<marker` **4** (c20 adds none), wrapper-divs 19→**20**, c20 PNG once, c20- ids **0** (numbered-pill), `&#183;` 56→**57** (+1 figcaption), `&#8226;` 4 / `&#8211;` 12 / `&#176;` 9 / `&#215;` 1 / `&#8594;` 2 unchanged, literal middot/en-dash/minus 0, fo-/hc-/th-/ff- unchanged (18/4/4/4), 11 ids no dups, 0 live mermaid. Pre-existing `finding-hdris` em-dash ("Pro tip—Create HDRI collection", between the #20/#21 anchors) preserved verbatim. **TEARDOWN:** no Blender scene work (network/PIL only), scene still 0/0; Windows `C:\Users\Public\L17P7` rmtree'd (isdir False), WSL `_tmp_l17p7_*` removed (glob empty), no `_overlay_VERIFIED.svg`, deliverable intact on disk. No BlenderMCP debt. Registry row #20 → INTEGRATED; §6 P7 table updated (#20 row + P7 CLOSED). **Next: P8 advanced-techniques** #22 separate_lighting_nodes PNG-UI `c22` (Light Path→Mix Shader, 2 Backgrounds [camera low / lighting high]; before `<h3>` per requirements placement) · P9 troubleshooting #25 `tf` SVG · P10 project #29 `c29` PNG-UI; summary figure-free; L17 total = 30.

**L17 P7 PARTIAL — #21 `fo` file_organization SVG INTEGRATED; #20 OWED 2026-06-16** — `finding-hdris` 1 of 2 figs landed this chat (all-SVG; #20 Poly Haven PNG blocked on a browser→WSL pixel relay, deferred to a fresh chat). **L17 now 26/30 produced / 26/30 integrated, HTML 251,622 B.** Entry P0 re-verified byte-exact on a fresh copy (240,669 B, em-dash 19, figure/figcaption 25/25, img 19, svg 25/25, marker 4, wrapper-divs 19, `&#183;` 53, `&#176;` 9, `&#215;` 1, `&#8594;` 2, `&#8211;` 2, hc-/th-/ff- 4 each, c{N}- 0, fo-/c20 absent, 11 ids no dups, 0 live mermaid; `finding-hdris` figure-FREE at entry). Blender clean base confirmed at entry (get_scene_info 0 obj / 0 mat) — all-SVG phase, **no Blender work, no teardown owed.** **#21 `fo` PRODUCTION:** hand-coded inline SVG viewBox `0 0 800 680` dense, id-prefix `fo` (fo-title/fo-desc + one `fo-fold` reusable mini-folder glyph via `<use>`). Content mirrors the section's `<pre>` tree EXACTLY: root HDRIs/ → 4 category folders color-coded (Outdoor blue `#4A9EFF` / Studio green `#6BCF7F` / Interior pink `#FF6B9D` / Creative purple `#B47EE8`), each with its real subfolders + file-count hints (`&#8211;` ranges); right-side "Why organize this way?" legend (`&#8226;` bullets) + example filename; brand `#FF6B00` header strip + folder icons + Variant-B footer (rail y650 / text y663). **Vector folder glyphs, NO emoji** (cairosvg lacks emoji font — the requirements-doc 📁/☀️ emoji were dropped for renderer-independent `<use>` glyphs). Standalone `images/lesson_17_21_file_organization.svg` **7,888 B** written via `Filesystem:write_file`, get_file_info-confirmed, root `color="#222"` KEPT for `<img>` fallback. cairosvg light+dark render-checked + visually reviewed (both clean, dark-mode currentColor cascade confirmed, content within 800×680, no overflow/collision). **em-dash fixed pre-write:** the `<desc>` originally carried 2 literal em-dashes ("four category folders — Outdoor…Creative — each") — both rewritten to colon/comma so the inline fragment adds 0 em-dashes (baseline-19 protected). **#21 INTEGRATION:** anchor count=1 on a fresh copy = the green "Starting HDRI Collection Recommendation" card opener (`<div class="card" style="background: #e8f5e9; border-left: 4px solid #4CAF50;">` + its `<h4>✅ …</h4>`), i.e. the figure lands AFTER the File Organization card `</div>` and BEFORE the green card. Default-depth inline per LD12: root `color="#222"` STRIPPED, re-indent figure col20 / svg col24 / children col28 / `<g>` col32 / nested-`<g>` col36, `<defs>`+`fo-fold` kept, structural comments kept + decorative pruned (none), `fo-*` + role/aria/title/desc preserved, figcaption col24 OUTSIDE-but-inside-figure (`Recommended HDRI library folder structure &#183; group by category, then by lighting type for fast browsing`). Single `edit_file` dryRun→commit→get_file_info. Byte trail: 240,669 (P0) → #21 +10,953 → **251,622 B** (get_file_info-confirmed). **POST-COMMIT INTEGRITY SWEEP CLEAN (fresh 251,622 B):** em-dash **19 HELD**, `&mdash;` 0, color222 0, figure/figcaption 25→**26** balanced, img **19** unchanged (inline adds 0), literal `<svg`/`</svg>` 25→**26**, `<marker` **4** (fo adds none), wrapper-divs **19** unchanged, `&#183;` 53→**56** (+3: fo subtitle + footer-tip + figcaption), `&#8211;` 2→**12** (+10 fo file-count ranges), `&#8226;` 0→**4** (fo legend bullets, NEW entity this lesson — harmless), `&#176;` 9 / `&#215;` 1 / `&#8594;` 2 unchanged, literal middot/en-dash/minus 0, `fo-` **18**, c20 absent, hc-/th-/ff- 4 each, 11 ids no dups, 0 live mermaid. Pre-existing `finding-hdris` em-dash ("Pro tip—Create HDRI collection", sits BETWEEN the #20 and #21 anchors) preserved verbatim. Registry row #21 → INTEGRATED. **P7 still OPEN: #20 poly_haven_navigation PNG remains to produce + integrate (own chat, needs browser+bridge).** Detail in §6 below.

**#20 BLOCKER + ROUTE (for the fresh chat):** #20 is a Poly Haven website UI-chrome capture. The captures themselves were obtained cleanly this chat via `Claude in Chrome` (browse page polyhaven.com/hdris + a free CC0 asset page polyhaven.com/a/kloppenheim_06 showing the 4K/EXR/Download bar; the "No more ads!" promo banner can be dismissed via its × and stays dismissed for the session). **The blocker is moving the browser-extension screenshot pixels to the WSL project:** `save_to_disk:true` returns the image to the model but writes NO host path the Blender bridge can read (checked Windows Downloads/Temp/Pictures — nothing); the Blender bridge reaches WSL+Windows-temp but not the web; Claude-side bash is firewalled from polyhaven.com (`403 host_not_allowed`); in-page `toDataURL` is CORS-tainted by the CDN imagery AND got safety-filtered. **Recommended route for #20:** (a) in the fresh chat, capture the browse page + asset page via Claude-in-Chrome (dismiss the promo banner first), then (b) get the pixels to WSL via a *download* the Blender bridge can see — OR rebuild a faithful annotated composite bridge-side from Poly Haven's public CDN thumbnails IF the Windows Blender Python `urllib` can fetch them (untested; the bash 403 is Claude-side only, the bridge network is separate). Then build `c20` (5 numbered callouts: search bar / category filters [sidebar+thumbnail row] / HDRI grid / individual card / download-multiple-resolutions), cairosvg-pixel-verify 1:1 over the real PNG (LD19), integrate at the **#20 anchor = before `<h3>Premium HDRI Resources</h3>`** (count=1, verified this chat), default-depth LD10 wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=PNG px, figcaption OUTSIDE. Post-commit sweep: em-dash 19, img 19→20, `<svg` 26→27, figure 26→27, wrapper-divs 19→20, c20 PNG once, fo-/hc-/th-/ff- unchanged. After #20: P7 closes (27/30), then P8 advanced-techniques #22 PNG UI · P9 troubleshooting #25 SVG `tf` · P10 project #29 PNG UI; summary stays figure-free; L17 total = 30.


**L17 P5 DONE — hdri-types FULLY CLOSED, all 4 figs integrated 2026-06-15** — #13/#14/#15/#16 INTEGRATED this chat (INTEGRATION-only; PRODUCTION landed prior chat). **P5 figure-complete 4/4, L17 now 22/30 produced / 22/30 integrated, HTML 225,661 B.** Entry P0 re-verified byte-exact on a fresh copy (215,086 B, em-dash 19, figure/figcaption 18/18, img 14, svg 18/18, marker 4, wrapper-divs 14, ff- 4, c{N}- 0, 11 ids no dups, 0 live mermaid, `&#183;` 34, `&#176;` 7, `&#215;` 1, `&#8594;` 2; `hdri-types` figure-FREE at entry). All 4 PNGs get_file_info + decode-verified MATCH on disk (#13 2704×1802 3,465,360 / #14 3606×900 2,339,836 / #15 2704×900 1,618,456 / #16 2704×900 1,523,234). **4 overlays built** (centered-pill, brand `#1a1a1a` 0.82 + white 600-wt text, NO `c{N}-*` ids; pills near panel bottom y=810 top row / y=1712 #13 bottom row, mirroring P4 c11/c12 style), cairosvg-rendered at exact PNG px + composited over the REAL PNGs + visually reviewed 1:1 (LD19): c13 6 type pills Clear sky/Cloudy/Sunset · Studio/Interior/Night at x=450/1352/2254 y=810 & y=1712 fs40 (bbox 316..2352 × 778..1744); c14 4 pills Clear sky/Cloudy/Sunset/Night x=450/1352/2254/3506 y=810 fs40 (bbox 316..3592 × 778..842); c15 3 pills Three-point/Softbox grid/Single key + ambient x=450/1352/2254 y=810 fs40 (bbox 292..2520 × 778..842 — widest pill clears the 2704 right edge); c16 3 pills Window/Artificial/Mixed x=450/1352/2254 y=810 fs40 (bbox 352..2340 × 778..842). Visually confirmed each composite: pills centered under each chrome sphere, white-on-dark legible over every background, orange `#FF6B00` 2px dividers intact, all environments distinct. **Insertion bottom-up #16 → #15 → #14 → #13,** each anchor count=1 on a FRESH copy by unique multi-line TEXT (#14/#15/#16 by bare `<h3>` heading text; #13 by the intro-`<p>`+Outdoor-`<h3>` pair), `edit_file` dryRun→commit→get_file_info each. Anchors (in `hdri-types`, doc-order placement #13 types-grid first then #14 outdoor / #15 studio / #16 interior): #13 after the intro `<p>` before `<h3>Outdoor/Natural HDRIs</h3>`; #14 before `<h3>Studio HDRIs</h3>`; #15 before `<h3>Interior HDRIs</h3>`; #16 before `<h3>Creative/Artistic HDRIs</h3>`. Doc order in `hdri-types` now: intro → #13 → Outdoor → #14 → Studio → #15 → Interior → #16 → Creative. All DEFAULT-DEPTH (figure col20 / div col24 / img+svg col28 / svg-children col32 / figcaption col20), LD10 wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=PNG px preserveAspectRatio xMidYMid meet, figcaption OUTSIDE. Byte trail: 215,086 (P0) → #16 +2,181 → 217,267 → #15 +2,191 → 219,458 → #14 +2,625 → 222,083 → #13 +3,578 → **225,661 B** (each get_file_info-confirmed). **POST-COMMIT INTEGRITY SWEEP CLEAN (fresh 225,661 B copy):** em-dash **19 held**, `&mdash;` 0, color222 0, figure 18→**22**, figcaption 18→**22** (balanced), img 14→**18**, literal `<svg` 18→**22** / `</svg>` 22, wrapper-divs 14→**18**, loading=lazy 18, `<marker` **4** (overlays add none), c13/c14/c15/c16 PNGs once each, c{N}- ids **0**, ff- ids 4, 11 section ids no dups, 0 live mermaid, `&#183;` 34→**38** (+4 figcaptions; pills plain text, no entities), `&#176;` 7 / `&#215;` 1 / `&#8594;` 2 unchanged, no en-dash/minus/literal-middot leak. No NEW em-dashes (figcaptions use `&#183;` separators + `&rsquo;` apostrophe). **No Blender teardown owed** (INTEGRATION-only; scene torn down clean at P5 PRODUCTION close — 0/0/0/0/0 + EEVEE + AgX + look None + grey-0.05 world, no HDRI datablocks). No temps/artifacts this chat (overlay SVGs built Claude-side only, no `_overlay_VERIFIED.svg`). Persisted HDRIs retained (belfast 2k/4k/8k + brown_photostudio_02 2k/4k/8k + 6 new 2k). Registry rows #13/#14/#15/#16 → INTEGRATED + new §6 P5 table. **Next: P6 combining-lights** (#17 hybrid_concept SVG `hc` + #18 hdri_key_light_setup PNG render-composite + #19 three_point_hdri — resolve **D-B** route, leans hand-coded SVG `th` per the L16 tpo/tps/fp precedent; single chat; D-D color-render probe if #18 renders warm/cool env tones).

**L17 P5 PRODUCTION COMPLETE — hdri-types 4 figs rendered + composited 2026-06-15** — `hdri-types` PRODUCTION done; INTEGRATION owed (own chat). **HTML UNTOUCHED at 215,086 B.** Entry clean base re-verified (0/0/0/0/0 + EEVEE + AgX + look None + exposure 0 + grey-0.05 Background→World Output, no HDRI datablocks; STOP rule passed). **D-C RESOLVED — 4 new CC0 Poly Haven HDRIs downloaded + persisted 2k** (Ray-confirmed: download the needed set, derive sub-types): clear-sky `kloppenheim_06_2k.hdr` (5,830,386 B), cloudy/overcast `kloofendal_overcast_2k.hdr` (6,286,056 B), night `dikhololo_night_2k.hdr` (7,050,455 B), interior `lebombo_2k.hdr` (6,010,815 B). **Plus 2 clean studio HDRIs added mid-phase** (the persisted `brown_photostudio_02` reads as a lived-in room — plants/dreamcatcher — not a photo studio, and 3 rotations of it were near-identical; swapped for dedicated studio HDRIs): `photo_studio_01_2k.hdr` (6,375,901 B) + `studio_small_09_2k.hdr` (6,312,947 B). All 6 new files `get_file_info`-confirmed on disk in `images\hdri\`; each persisted via Blender-side `shutil.copyfile` from the addon's `C:\Users\pract\AppData\Local\Temp\tmp*.hdr` then the datablock repointed to the project copy (the addon's tmp lives in a cleanable Windows temp). On-disk HDRI inventory now **8**: belfast_sunset_puresky + brown_photostudio_02 (2k/4k/8k each, retained) + the 6 new 2k. **D-F RESOLVED — chrome sphere all four** (Ray-confirmed): Principled metallic 1.0 rough 0.08 base 0.55 @ (0,0,1), matte 0.32 ground size 30, 50mm cam (4.2,-4.2,2.6)→(0,0,1), Cycles GPU 160spp+denoise, 900×900 panels, world TexCoord→Mapping→EnvTexture→Background→Output, NO manual lights. **D-D RESOLVED for P5 → Standard view transform + look None** (probe AgX vs Standard on the 3 color-critical envs: AgX compresses R-B separation every time — sunset −19.8→−11.8, clearsky −31.1→−24.5, interior +21.8→+18.3; the chrome mirrors the sky directly so AgX mutes the environment tint = the wash D-D predicts, L15 #13 / L16 #20 precedent). **AgX RESTORED at teardown.** **Per-env Background strength** set from a 220px/32spp luminance probe so each panel lands readable: clearsky 0.85 z0 / cloudy 0.65 z0 / sunset 0.70 **z50** (warm glow into view, P1 precedent) / night **1.40** z0 (lifted so the dark env reads without losing night character) / studio_threepoint photo_studio_01 0.9 z0 / studio_softbox studio_small_09 0.9 z0 / studio_singlekey photo_studio_01 0.7 z200 / interior_window lebombo 1.1 z0 / interior_artificial lebombo 1.2 z180 / interior_mixed lebombo 1.1 z90 (studio + interior sub-types DERIVED from one HDRI each via rotation/strength — the *reflection pattern* + brightness differ, which is the teaching point). Contact-sheet visually reviewed twice (first studio trio from brown_photostudio_02 rejected for room-look + near-identical rotations → re-rendered against the clean studio HDRIs; final studio trio reads as distinct three-point/softbox/single-key setups). **Deliverables (all Pillow-composited, `#FF6B00` 2px dividers, NO baked labels — overlays c{N} at INTEGRATION; get_file_info + decode-verified RGB):** #13 `images/lesson_17_13_hdri_types_grid.png` **2704×1802, 3,465,360 B** (2×3 grid: clearsky/cloudy/sunset top · studio_threepoint/interior_window/night bottom; dividers x=900/1802, y=900); #14 `images/lesson_17_14_outdoor_examples.png` **3606×900, 2,339,836 B** (1×4 clearsky/cloudy/sunset/night; dividers x=900/1802/2704); #15 `images/lesson_17_15_studio_examples.png` **2704×900, 1,618,456 B** (1×3 three-point/softbox/single-key; dividers x=900/1802); #16 `images/lesson_17_16_interior_examples.png` **2704×900, 1,523,234 B** (1×3 window/artificial/mixed; dividers x=900/1802). Panel centers: #13 row1 x=450/1352/2254 y=450, row2 same x y=1352; #14 x=450/1352/2254/3506 y=450; #15/#16 x=450/1352/2254 y=450 (use at INTEGRATION for centered pills). **BLENDER TORN DOWN CLEAN + CONFIRMED:** all P5 objects/materials/meshes/cameras removed, all HDRI image datablocks removed (persisted .hdr on disk RETAINED), world reset to grey-0.05 Background→World Output, orphans purge (none), restored BLENDER_EEVEE + AgX + look None + exposure 0; verified 0 obj/0 mat/0 mesh/0 lights/0 cams, images just Render Result/Viewer Node. Temps purged + ENOENT-verified: Windows `C:\Users\Public\L17P5` isdir False; all addon `C:\Users\pract\AppData\Local\Temp\tmp*.hdr` removed (glob empty); WSL `_tmp_l17p5_*` (contact/studio/review sheets) removed (glob empty); no `_overlay_VERIFIED.svg`. No BlenderMCP debt. **22/30 produced / 18/30 integrated** (4 PRODUCED-not-integrated). Registry rows #13/#14/#15/#16 → PRODUCED; D-C/D-D/D-F marked RESOLVED below. **Next: P5 INTEGRATION** — build overlays c13 (6 type pills) / c14 (4 outdoor pills) / c15 (3 studio pills) / c16 (3 interior pills), cairosvg-pixel-verify 1:1 over the real PNGs (LD19), insert in `hdri-types` (re-verify anchors count=1 on a fresh copy), em-dash stays 19, post-commit integrity sweep.

**L17 P4 DONE — adjusting-hdri FULLY CLOSED, all 4 figs integrated 2026-06-15** — #11/#9/#12/#23 INTEGRATED this chat (INTEGRATION-only; PRODUCTION landed prior chat). **P4 figure-complete 4/4, L17 now 18/30 produced / 18/30 integrated, HTML 215,086 B.** Entry P0 re-verified byte-exact on a fresh copy (204,841 B, em-dash 19, figure/figcaption 14/14, img 10, svg 14/14, marker 4, wrapper-divs 10, ff- 4, c{N}- 0, 11 ids no dups, 0 live mermaid, `&#183;` 30, `&#176;` 3, `&#215;` 1, `&#8594;` 2). All 4 PNGs decode-verified MATCH on disk (#11 2704×900 1,574,084 / #12 1802×1802 2,036,424 / #9 2489×729 164,411 / #23 3784×411 145,035). **4 overlays REBUILT from the recorded verified coords** (container reset wiped the prior `/home/claude/l17/*.svg`), cairosvg-rendered at exact PNG px + composited over the REAL PNGs + visually re-confirmed 1:1 (LD19): c11 3 pills "Strength &#183; 0.5/1.0/2.5" centers x=450/1352/2254 y=812 fs40 (bbox x296..2407 y782..841); c12 4 angle pills 0&#176;/90&#176;/180&#176;/270&#176; at (450,812)/(1352,812)/(450,1714)/(1352,1714) fs46 (bbox x344..1457 y778..1747); c9 orange `#FF6B00` sw4 highlight rect x536..749 y483..530 around the Mapping Rotation-Z=45&#176; row + leader (sw3 + dot r5 at 642,530) curving to an ORANGE-STROKED pill (`#1a1a1a` 0.92 + `#FF6B00` sw2.5 fs28) "Z &#183; rotates HDRI" centered (275,605) — the orange stroke is REQUIRED so it reads on the dark node canvas (bbox x90..750 y481..632); c23 3 node pills HSV/ColorRamp/RGB Curves centers x=630/1892/3153 y=382 fs24 (bbox x572..3246 y363..400, kept y≤403 for the 411px-tall image). All composite-pill/centered-pill style brand `#1a1a1a`, white 600-wt text, NO `c{N}-*` ids. Visually reviewed each composite (pills centered per panel; c11 brightness ramp reads behind pills; c12 angle pills clear the orange quadrant dividers; c23 each pill sits under its node group; c9 highlight frames the Z row precisely, leader + orange pill legible on dark). **Insertion bottom-up #23 → #12 → #9 → #11,** each anchor count=1 on a FRESH copy by unique bare-`<h3>` TEXT, `edit_file` dryRun→commit→get_file_info each. Anchors (in `adjusting-hdri`, doc order Strength→Rotation→Color Adjustment→Background Visibility Control): #11 before `<h3>Rotation</h3>`; #9 + #12 both before `<h3>Color Adjustment</h3>` (#12 inserted FIRST so #9 lands ABOVE it = correct doc order #9>#12); #23 before `<h3>Background Visibility Control</h3>`. Doc order in `adjusting-hdri` now (Strength prose) → #11 → Rotation → #9 → #12 → Color Adjustment → #23 → Background Visibility Control. All DEFAULT-DEPTH (figure col20 / div col24 / img+svg col28 / svg-children col32 / figcaption col20), LD10 wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=PNG px preserveAspectRatio xMidYMid meet, figcaption OUTSIDE. Byte trail: 204,841 (P0) → #23 +2,520 → 207,361 → #12 +3,019 → 210,380 → #9 +2,069 → 212,449 → #11 +2,637 → **215,086 B** (each get_file_info-confirmed). **POST-COMMIT INTEGRITY SWEEP CLEAN (fresh 215,086 B copy):** em-dash **19 held**, `&mdash;` 0, color222 0, figure 14→**18**, figcaption 14→**18** (balanced), img 10→**14**, literal `<svg` 14→**18** / `</svg>` 18, wrapper-divs 10→**14**, `<marker` **4** (overlays add none), c11/c9/c12/c23 PNGs once each, c{N}- ids **0**, ff- ids 4, 11 section ids no dups, 0 live mermaid, `&#176;` 3→**7** (+4 c12 angle pills), `&#183;` 30→**34** (+4: c11 3 pills + c9 1 pill; c12/c23 use none), `&#215;` 1 / `&#8594;` 2 unchanged. Pre-existing closing-checklist em-dash ("Start with strength and rotation—these have the biggest impact", below the #23 anchor) preserved verbatim. **No Blender teardown owed** (INTEGRATION-only; scene torn down clean at P4 PRODUCTION close — 0/0/0/0/0 + EEVEE + AgX + grey-0.05 world, no HDRI datablocks). No temps/artifacts this chat (overlay SVGs built Claude-side only, no `_overlay_VERIFIED.svg`). Persisted HDRIs retained (belfast 2k/4k/8k + brown_photostudio_02 2k/4k/8k). Registry rows #11/#9/#12/#23 → INTEGRATED + new §6 P4 table. **Next: P5 hdri-types** (#13 6-panel grid + #14 4-panel outdoor + #15 3-panel studio + #16 3-panel interior — 4 multi-panel HDRI renders, the heaviest render phase; pre-plan a PRODUCTION sub-chat; D-D color-render Standard likely per figure; D-F subject = chrome/glossy sphere for reflection parity; multiple CC0 Poly Haven HDRIs needed — D-C download path).

**L17 P4 PRODUCTION COMPLETE — all 4 figs rendered/captured 2026-06-15** — `adjusting-hdri` PRODUCTION done; INTEGRATION owed (fresh chat). **HTML UNTOUCHED at 204,841 B.** Entry clean base confirmed (0/0/0/0/0 + EEVEE + AgX + grey-0.05 world, no HDRI datablocks). **D-D RESOLVED for P4 → Standard view transform + look None for both render figs #11/#12** (probe: Standard gives wider strength steps 0.5→119 / 1.0→161 / 2.5→225 = +41/+64 vs AgX 117/152/190 = +35/+39; rotation reads via monotonic sphere L-R gradient Z90 +27 → Z0 +4 → Z180 0 → Z270 −22). **belfast_sunset_puresky_2k reused across #11/#12 panels** for parity (Ray-confirmed; brown_photostudio_02 was #24-only). Shared scene: chrome sphere (Principled metallic 1.0 rough 0.08 base 0.55) @ (0,0,1), matte 0.32 ground size 30, 50mm cam (4.2,-4.2,2.6)→(0,0,1), Cycles GPU 160spp+denoise, world TexCoord→Mapping→EnvTexture(belfast 2k)→Background→Output. **Deliverables (all get_file_info-confirmed, NO baked labels):** #11 `images/lesson_17_11_strength_comparison.png` **2704×900, 1,574,084 B** (3-panel 0.5/1.0/2.5, mapping Z=0, `#FF6B00` 2px dividers x=900/1802, panel centers x=450/1352/2254); #12 `images/lesson_17_12_rotation_effect.png` **1802×1802, 2,036,424 B** (2×2 TL0°/TR90°/BL180°/BR270°, dividers x=900/y=900, centers 450/1352); #9 `images/lesson_17_09_rotation_node_setup.png` **2489×729, 164,411 B** (World Shader Editor chain TexCoord→Mapping→EnvTexture(belfast, Equirectangular)→Background→World Output, Mapping **Rotation Z=45° legible** = highlight target, visually verified clean); #23 `images/lesson_17_23_color_grading_nodes.png` **3784×411, 145,035 B** (3-panel: Env→HSV→Bg / Env→ColorRamp→Bg / Env→RGB-Curves(S-curve)→Bg, each →World Output, `#FF6B00` 2px dividers, visually verified no UI bleed). **UI-capture note:** the window DPI changed mid-session — #9 screenshot was 3840×2109 (2×), #23 screenshots 1920×1046 (1×); crop bounds derived live from `area.x/y/w/h` per capture (node-editor region 1× = x[3..1336] y_PIL[39..915]; 2× = x[6..2672] y_PIL[78..1830]). **BLENDER TORN DOWN CLEAN + CONFIRMED:** area3 NODE_EDITOR→VIEW_3D (regions re-shown), all L17_P4 objects/materials removed, world reset to grey-0.05 Background→World Output, HDRI image datablock removed (persisted .hdr on disk RETAINED), orphans purged (3 data-blocks); verified 0 obj/0 mat/0 mesh/0 lights/0 cameras, EEVEE + AgX + look None + exposure 0, areas PROPERTIES/OUTLINER/DOPESHEET_EDITOR/VIEW_3D. Temps purged + ENOENT-verified: Windows `C:\Users\Public\L17P4` isdir False; WSL `_tmp_l17p4_*` glob empty; no `_overlay_VERIFIED.svg`. No BlenderMCP debt. Persisted HDRIs retained (belfast 2k/4k/8k + brown_photostudio_02 2k/4k/8k). **14/30 produced + 4 PRODUCED-not-integrated = 18/30 produced / 14/30 integrated.** **Doc cleanup also done this chat:** D-I one-liner added to DECISIONS, registry rows #24/#26 marked ✓INTEGRATED, §6 P3-final table added. **Next: P4 INTEGRATION** (build overlays c11/c9/c12/c23 + insert bottom-up #23→#12→#9→#11; anchors below).

**L17 P3 DONE — #24 + #26 INTEGRATED, setting-up-hdri FULLY CLOSED 2026-06-15** — the final 2 P3 figs produced + integrated; **P3 figure-complete 14/14, L17 now 14/30 produced / 14/30 integrated, HTML 204,841 B.** Entry P0 199,636 B re-verified byte-exact (em-dash 19, figure/figcaption 12, img 8, svg 12, wrapper-divs 8, marker 4, ff- 4, c{N}- 0, 11 ids no dups, 0 live mermaid). Scene clean base confirmed at entry (0/0/0/0/0 + EEVEE + AgX + grey-0.05 world, no HDRI datablocks). **D-I RESOLVED:** the persisted soft `belfast_sunset_puresky` made 2K/4K/8K chrome reflections visually indistinguishable (would teach the OPPOSITE of "resolution matters"), so pulled a detail-rich CC0 studio HDRI **`brown_photostudio_02`** via `download_polyhaven_asset` (hard high-frequency content: triangular light fixtures, window mullion grid, hex wall tiling, string lights) and persisted all three resolutions to `images\hdri\brown_photostudio_02_{2k,4k,8k}.hdr` (6,492,863 / 25,433,817 / 98,137,639 B = 6.19/24.26/93.59 MB). **#24 quality_comparison re-rendered:** chrome sphere (Principled metallic 1.0 rough 0.08 base 0.55) @ (0,0,1), matte 0.32 ground size 30, close-up cam (2.7,-2.7,2.0) lens 85 aimed (0,0,1.05), 900&times;900, **Cycles GPU 400spp DENOISE OFF**, Standard view transform + look None, mapping Z=0 / bg str 1.0. Window-mullion hard-edge crop region (src x250 y240 360-square &rarr; 800px panels). **Separation CONFIRMED BEFORE compositing** per D-I (Laplacian-variance sharpness 2K **74.8** / 4K **95.1** / 8K **111.8**, monotone increasing; 3-panel preview visually distinct — mullion edges soft→mushy at 2K, crisp at 8K). Composite 2404&times;800, `#FF6B00` 2px dividers x=800-802/1602-1604, panel centers x=400/1202/2004, NO baked labels = `images/lesson_17_24_quality_comparison.png` **1,135,472 B**. **#26** already produced prior (1,255,914 B, 2404&times;800, Solid / Material Preview / Rendered of the belfast-lit chrome sphere; visually reviewed clean). **Overlays `c24`** (2K&#183;2048px / 4K&#183;4096px / 8K&#183;8192px, panel centers x=400/1202/2004 y=740) **+ `c26`** (Solid / Material Preview / Rendered, same centers) = composite centered-pill (brand `#1a1a1a` 0.82, white text, `&#183;` middots, NO `c{N}-*` ids), both **cairosvg-pixel-verified 1:1** over the real PNGs (LD19, viewBox=2404&times;800) + visually reviewed; c24 bbox 209..2195 / c26 bbox 321..2114, both y 711-769 in-bounds. **Insertion bottom-up #24 then #26,** each anchor count=1 on a fresh copy by unique multi-line TEXT, `edit_file` dryRun&rarr;commit&rarr;get_file_info each. #24 anchor: after the #28 `ff` figure `</figure>` (figcaption "HDRI file formats at a glance…") before the Setup-Philosophy `<blockquote>` (pre-existing em-dash "intentionally simple—add one node" PRESERVED). #26 anchor: after the Testing-Your-HDRI-Setup card `</div>` ABOVE the existing #27 figure (`lesson_17_27_node_connection_closeup`). **Doc order in setting-up-hdri now #7&rarr;#8&rarr;#10&rarr;#26&rarr;#27&rarr;#28&rarr;#24.** Both DEFAULT-DEPTH (figure col20 / div col24 / img+svg col28 / svg-children col32 / figcaption col20), LD10 wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=PNG px preserveAspectRatio xMidYMid meet, figcaption OUTSIDE. Byte trail: 199,636 (P0) &rarr; #24 +2,608 &rarr; 202,244 &rarr; #26 +2,597 &rarr; **204,841 B** (each get_file_info-confirmed). **POST-COMMIT INTEGRITY SWEEP CLEAN (fresh 204,841 B copy):** em-dash **19 held**, `&mdash;` 0, color222 0, figure 12&rarr;**14**, figcaption 12&rarr;**14** (balanced), img 8&rarr;**10**, literal `<svg` 12&rarr;**14** / `</svg>` 14, wrapper-divs 8&rarr;**10**, `<marker` **4** (overlays add none), c24/c26 PNGs once each, c{N}- ids **0**, ff- ids 4, 11 section ids no dups, 0 live mermaid, `&#183;` 27&rarr;**30** (+3 c24 middots), `&#8594;` 2 / `&#176;` 3 / `&#215;` 1, blockquote pre-existing em-dash preserved. **BLENDER TORN DOWN CLEAN + CONFIRMED:** 0 obj / 0 mat, EEVEE + AgX + look None + exposure 0, grey-0.05 Background&rarr;World Output (env chain stripped), HDRI image datablocks removed from .blend (persisted .hdr on disk RETAINED), orphans purged. Temps purged + ENOENT-verified: Windows `C:\Users\Public\L17P3` isdir False; WSL `images\_tmp_l17p3_preview` removed + `_tmp_l17p3*` glob empty; no `_overlay_VERIFIED.svg` artifacts. No BlenderMCP debt. Persisted HDRIs retained: belfast 2k/4k/8k + brown_photostudio_02 2k/4k/8k. Registry rows #24/#26 → INTEGRATED; D-I updated to RESOLVED below; new §6 P3-final table. **Next: P4 adjusting-hdri** (#11 strength + #12 rotation-effect renders, #9 rotation-node + #23 color-grading UI-chrome; D-D color-render probe per figure; single chat, or PRODUCTION sub-chat if the 2 renders + 2 captures crowd).

**L17 P3 INTEGRATION PARTIAL — 5 of 7 figs (#28/#27/#10/#8/#7) DONE 2026-06-15** — `setting-up-hdri` INTEGRATION of the 5 Sub-chat-A figures complete; **12/30 produced / 12/30 integrated** (P3 not yet closed — #26/#24 still owed). Entry P0 re-verified byte-exact on a fresh copy (182,621 B, em-dash 19, figure/figcaption/svg 7, img 4, marker 4, wrapper-divs 4, 11 ids no dups, 0 live mermaid). All 5 deliverables re-confirmed byte-exact on disk via get_file_info (#28 6,063 / #10 29,241 / #8 50,948 / #27 12,547 / #7 207,637). **4 overlays (c7/c8/c10/c27) REBUILT from recorded coords + cairosvg-pixel-verified 1:1 over the real PNGs (LD19)** — all content-bbox in-bounds, visually reviewed: c7 4 step pills h64 fs32 centered per panel (Step1/2/3/4, `&#8594;` arrow + `&#183;` middots); c8 3 node-column pills (HDRI image/Brightness control/Final output) + 1 Color-data pill `#FFE066` dot; c10 2 green ring rects framing Material Preview + Rendered icons + 2 green-on-dark pills; c27 2 green ring circles on the Color sockets + 1 "Yellow = Color data" pill `#FFE066` dot. Composite/centered-pill + ring variant, brand `#1a1a1a` 0.82, NO `c{N}-*` ids. **#28 `ff` inlined per LD12** (root `color="#222"` STRIPPED, decorative `====` comments pruned, footer multi-line `<text>` collapsed, ff-title/ff-desc + role/aria kept; NO defs/markers; cairosvg light+dark render-checked clean). **Insertion bottom-up #28 → #27 → #10 → (#7+#8 block),** each anchor re-verified count=1 on a fresh copy by unique multi-line TEXT, `edit_file` dryRun→commit→get_file_info each. Anchors: #28 after Supported-Formats card `</div>` before the Setup-Philosophy blockquote (pre-existing em-dash "intentionally simple—add one node" PRESERVED verbatim through the anchor); #27 after Verifying-It-Works card `</div>` before `<h3>HDRI File Formats</h3>`; #10 after Quick-Setup-Summary box `</div>` before `<h3>Testing Your HDRI Setup</h3>`; #7+#8 both after the Step-by-Step Loading card `</div>` before the green Quick-Setup-Summary box (one anchor, #7 above #8). All DEFAULT-DEPTH (figure col20 / div col24 / img+svg col28 continuations col33 / svg-children col32 / figcaption col20). PNG figs = LD10 wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=PNG px preserveAspectRatio xMidYMid meet, figcaption OUTSIDE; #28 inline SVG (figure col20 / svg col24 / children col28). Byte trail: 182,621 (P0) → #28 +7,713 → 190,334 → #27 +1,799 → 192,133 → #10 +2,097 → 194,230 → #7+#8 +5,406 → **199,636 B** (each get_file_info-confirmed). **POST-COMMIT INTEGRITY SWEEP CLEAN (live sweep on a fresh 199,636 B copy):** em-dash **19 held**, `&mdash;` 0, color222 0, figure 7→**12**, figcaption 7→**12** (balanced), img 4→**8** (#7/#8/#10/#27; #28 inline adds 0), literal `<svg` 7→**12** / `</svg>` 12 (4 overlays + #28 inline), wrapper-divs 4→**8**, `<marker` **4** (these overlays add none), c7/c8/c10/c27 PNGs once each, c{N}- ids **0**, ff- ids **4**, 11 section ids no dups, 0 live mermaid, entity sanity `&#183;` 27 / `&#8594;` **2** (+1 from c7 Step4) / `&#176;` 3 / `&#215;` 1, blockquote pre-existing em-dash preserved. No BlenderMCP work this chat (the 5 figs were already produced + scene torn down clean at Sub-chat A close; Blender base remains clean 0/0/0/0/0 + AgX, HDRI `.hdr` retained at `images\hdri\belfast_sunset_puresky_2k.hdr`, no temps/debt). Registry rows #28/#27/#10/#8/#7 → INTEGRATED + new §6 P3-partial table. **Next: produce + integrate #26 + #24 to close P3.**

**L17 P3 Sub-chat A PRODUCTION 2026-06-15** — `setting-up-hdri` PRODUCTION mid-flight (INTEGRATION owed). **5 figs produced this Sub-chat A, all NO baked labels** (overlays c{N} at INTEGRATION): #28 `images/lesson_17_28_file_formats_guide.svg` SVG **6,063 B** / #10 `images/lesson_17_10_viewport_shading_icons.png` PNG **29,241 B 476×156** / #8 `images/lesson_17_08_complete_node_network.png` PNG **50,948 B 1260×352** / #27 `images/lesson_17_27_node_connection_closeup.png` PNG **12,547 B 605×145** / #7 `images/lesson_17_07_hdri_setup_workflow.png` PNG **207,637 B 2668×1754** (2×2 P1→P4 composite, `#FF6B00` 2px dividers). All 5 re-confirmed byte-exact on disk via `get_file_info`. **HTML UNTOUCHED at 182,621 B** (PRODUCTION only). **12/30 produced / 7/30 integrated.** Registry rows #28/#10/#8/#27/#7 → **PRODUCED** (not yet integrated). **Blender torn down clean + CONFIRMED:** window-0 NODE_EDITOR→VIEW_3D + chrome restored, Properties→SCENE; 0 obj / 0 mat, HDRI image datablock removed from .blend (the persisted `.hdr` on disk RETAINED at `images\hdri\belfast_sunset_puresky_2k.hdr`), default grey-0.05 Background→World Output world, AgX, EEVEE, 4 areas (PROPERTIES/OUTLINER/DOPESHEET_EDITOR/VIEW_3D). **Temps purged + verified:** Windows `C:\Users\Public\L17P3` rmtree'd (isdir False); WSL `images\_tmp_l17p3_*` glob empty; no `_overlay_VERIFIED.svg` artifacts. No BlenderMCP/temp debt. **Next: P3 INTEGRATION** (overlays c28/c10/c8/c27/c7 + inline #28/#10/#8/#27/#7), then remaining P3 figs #26/#24.

**L17 P2 DONE 2026-06-15** — `world-lighting-basics` FULLY CLOSED, both P2 figures integrated this chat. **7/30 produced / 7/30 integrated.** Entry P0 = the P1 close (175,358 B, em-dash 19, figure/figcaption/svg 5, img 2, wrapper-divs 2, marker 4, 11 ids no dups, 0 live mermaid), re-verified byte-exact on a fresh copy. **Deliverables (both UI-chrome PNGs, re-confirmed byte-exact on disk):** #6 `images/lesson_17_06_default_world_shader.png` = **44,015 B, 1273×312** (clean 2-node shader-editor reference: Background node + green color noodle + World Output node, even ~25px margins, no clipping, NO baked labels) — this is the recropped FINAL; the prior 20,304 B crop trusted a bad x990 right-edge estimate and was overwritten (true node-body right edge is x1276; source full2 was 1920×1046). #5 `images/lesson_17_05_world_properties_panel.png` = **33,629 B, 584×426** (Properties → World tab, unchanged from its earlier crop). **Overlays `c5`/`c6` built + cairosvg-pixel-verified 1:1 over the real PNGs (LD19) + visually reviewed before commit:** centered/composite-pill style, brand `#1a1a1a` 0.82 + `#FF6B00` accents, NO `c5-*`/`c6-*` ids (composite furniture). **Insertion: bottom-up #6 then #5,** each anchor re-verified count=1 on a fresh copy, `edit_file` dryRun→commit→get_file_info each. Byte trail: 175,358 (P0) → #6 +3,005 → 178,363 → #5 +4,258 → **182,621 B**. POST-COMMIT INTEGRITY SWEEP CLEAN (live sweep on a fresh 182,621 B copy): em-dash **19 held**, `&mdash;` 0, color222 0, figure 5→**7**, figcaption 5→**7** (balanced), img 2→**4** (#5+#6 PNGs), literal `<svg` 5→**7** / `</svg>` **7**, wrapper-divs 2→**4**, `<marker` **4** (overlays add none), c5/c6 PNGs once each, c5-/c6- ids 0, 11 section ids no dups, 0 live mermaid. The pre-existing `system—the foundation` em-dash (well before the #5 anchor) preserved verbatim. **BLENDER TEARDOWN:** window-0 NODE_EDITOR area → VIEW_3D, toolbar/N-panel/header + overlays + nav-gizmo all re-shown, Properties context WORLD→SCENE; the stray temp IMAGE_EDITOR window-1 (1850×1033) closed (1 window now: PROPERTIES/OUTLINER/DOPESHEET_EDITOR/VIEW_3D); AgX confirmed (untouched); clean base re-confirmed 0 obj / 0 mat + default grey (0.05) Background→World Output world with `use_nodes`. **TEMPS PURGED + ENOENT-verified:** Windows `C:\Users\Public\L17P2` rmtree'd (had contained f5_world_props_full / f6_world_shader_full / f6_world_shader_full2 / the new f6_crop_final), isdir False; WSL `images/_tmp_l17p2_*` 5 files removed (f5_full / f5_crop / f6_full / f6_full2 / f6_crop), glob empty; no `_overlay_VERIFIED.svg` artifacts (none were created). No BlenderMCP debt. Progress-doc registry rows #5/#6 → INTEGRATED + new §6 P2 table + header Status line updated. **Next: L17 P3** (`setting-up-hdri`).

**L17 P1 DONE 2026-06-15** — `what-is-hdri` FULLY CLOSED, all 5 P1 figures integrated (single INTEGRATION chat; PRODUCTION landed prior chats). **5/30 produced / 5/30 integrated.** Entry P0 byte-exact 139,279 B verified on a fresh copy (em-dash 19, body figure-free: figure/img/svg/figcaption/wrapper-divs all 0, color222 0, 11 ids no dups, 0 live mermaid). All 5 deliverables re-confirmed byte-exact on disk (#2 7,945 / #3 6,472 / #4 6,611 / #1 1,120,454 / #30 1,175,106) + both PNGs decode 2002×1000 RGB. **Overlays built + cairosvg-pixel-verified 1:1 over the real PNGs (LD19) + visually reviewed before commit:** `c1` = LDR·8-bit (left, top-center x360 pill) + HDR·32-bit (right, x1356) + two bottom value-scale pills (8-bit scale "0·128·255 (clips to white, detail lost)" / 32-bit scale "0.0·1.0·2.0·5.0·10.0+ (detail held)"), composite-pill brand `#1a1a1a` 0.82 + `#FF6B00` accent dots, NO `c1-*` ids, content bbox 120..1882 × 46..954 in-bounds; `c30` = Before·grey world (left x330) + After·HDRI lit (right x1330), composite-pill, NO `c30-*` ids, bbox 330..1672 × 46..108. Divider baked at x=1000 (orange `#FF6B00` 2px) in both PNGs. **Insertion: bottom-up #4 → #3 → #30 → #1+#2-block, each anchor re-verified count=1 on a fresh copy by unique multi-line TEXT, `edit_file` dryRun→commit→get_file_info.** Anchors: #4 after "Types of HDRI Maps" card `</div>` before `<h3>When to Use HDRI</h3>`; #3 after the "HDRI Revolution" `</blockquote>` before `<h3>Types of HDRI Maps</h3>` (the How-HDRIs-Work card `</div>` is followed by the blockquote — #3 lands AFTER the blockquote); #30 after "HDR vs. LDR Images" card `</div>` before `<h3>How HDRIs Work as Lighting</h3>`; #1+#2 both before `<h3>HDR vs. LDR Images</h3>` (doc order #1 above #2, inserted as one block). **SVGs #2 `dr` / #3 `sp` / #4 `eq` inlined per LD12:** root `color="#222"` STRIPPED, re-indent figure col20 / svg col24 / svg children col28 (root-tag continuation attrs at col28), footer multi-line `<text>` collapsed to one line, decorative `====` comments pruned (none present), structural comments + `dr-*`/`sp-*`/`eq-*` + role/aria/title/desc ids kept; **#4 `eq` KEPT BOTH `<defs>` blocks** (the `eq-arrow` marker is forward-referenced from the 2nd defs — valid, cairosvg-clean); #3 `sp` kept all 3 `sp-arrow-*` markers. All 3 inlined fragments cairosvg render-checked clean post-transform. **#1 / #30 PNG-overlay per LD10:** wrapper-div (`position:relative; display:inline-block; max-width:100%`) + img (`margin:0`, `loading=lazy`) + inlined overlay-svg (viewBox `0 0 2002 1000`, `preserveAspectRatio=xMidYMid meet`, `pointer-events:none`), figcaption OUTSIDE. All 5 DEFAULT-DEPTH (figure col20 / svg+img col24 ... / figcaption col20). Byte trail: 139,279 (P0) → #4 +9,768 → 149,047 → #3 +9,135 → 158,182 → #30 +2,300 → 160,482 → #1+#2 +14,876 → **175,358 B** (each get_file_info-confirmed). **POST-COMMIT INTEGRITY CLEAN (live-swept on fresh copy, 175,358 B):** em-dash **19 held**, `&mdash;` 0, color222 0, figure 0→**5**, `</figure>` 5, figcaption 0→**5** (balanced), img 0→**2** (#1+#30; the 3 inline SVGs add none), literal `<svg` 0→**5** / `</svg>` 5 (3 inline + c1 + c30 overlay), wrapper-divs 0→**2** (#1+#30 only), `<marker` 0→**4** (sp 3 + eq 1), 11 section ids no dups, c1/c30 PNGs once each, c1-/c30- ids **0** (composite-pill), 0 live mermaid (unchanged), entity content `&#183;` 17 / `&#176;` 3 / `&#215;` 1 / `&#8594;` 1, en-dash U+2013 0 / minus U+2212 0 / literal middot U+00B7 0. Two pre-existing em-dashes preserved verbatim through anchors ("sunglasses—limited range" in Understanding-HDRI card list; "everything—now a single environment map" in HDRI-Revolution blockquote). No BlenderMCP work this chat (INTEGRATION-only; P1 PRODUCTION torn down clean prior chat — no scene/temps/teardown owed). Deliverables retained. **5/30 produced / 5/30 integrated — P1 what-is-hdri complete.** Detail in §6 below.

**L17 P1 INTEGRATION-PREP 2026-06-15** — verified the 5 deliverables on disk + the 4 insertion anchors count=1 on the live HTML (139,279 B, em-dash 19, body still figure-free: figure/img/svg all 0), so the INTEGRATION chat can go straight to overlay-build + insert. **Doc order in `what-is-hdri` (top→bottom): #1, #2 (after "Understanding HDRI" card) → #30 (after "HDR vs. LDR Images" card) → #3 (after the "HDRI Revolution" blockquote) → #4 (after "Types of HDRI Maps" card).** Bottom-up insert order = #4 → #3 → #30 → #2 → #1 (re-verify each anchor count=1 on a FRESH copy at insert time — they shift as inserts land). **Anchors confirmed count=1 (live, pre-insert):** #1+#2 anchor = `</ul>`+`</div>`+blank+`<h3>HDR vs. LDR Images</h3>` (both #1 and #2 land at this one point, doc order #1 above #2 — so insert #2 first then #1, OR insert as one block #1-then-#2); #30 anchor = `</div>`+blank+`<h3>How HDRIs Work as Lighting</h3>`; **#3 anchor = `</blockquote>`+blank+`<h3>Types of HDRI Maps</h3>` (NOTE: the "How HDRIs Work" card `</div>` is followed by a `<blockquote>` BEFORE the next h3 — #3 goes AFTER that blockquote, not right after the card div);** #4 anchor = `</div>`+blank+`<h3>When to Use HDRI</h3>` (all anchors at 24-space `</div>`/`</blockquote>`/`</ul>` indent, 20-space `<h3>`). **Pre-existing em-dashes near anchors to preserve verbatim (part of held-19):** "sunglasses—limited range" (Understanding-HDRI card list), "everything—now a single environment map" (HDRI-Revolution blockquote). **All 5 figures are DEFAULT-DEPTH (between cards at section level): figure col20 / img+overlay-svg+inlined-svg-root col24 / svg children col28 / figcaption col20.** **SVGs #2 `dr` (7,945 B) / #3 `sp` (6,472 B) / #4 `eq` (6,611 B)** inline per LD12 — strip root `color="#222"`, re-indent, collapse multi-line attrs, prune `====` comments, keep `dr-*`/`sp-*`/`eq-*` + role/aria/title/desc ids. **NOTE on #4 `eq`:** it has TWO `<defs>` blocks (the `eq-arrow` marker is defined in a 2nd `<defs>` AFTER it is referenced — valid SVG forward-ref, cairosvg-confirmed clean; keep both `<defs>` on inlining). #2 `dr` has a `dr-ldr`/`dr-hdr` linearGradient defs block; #3 `sp` has 3 `sp-arrow-*` markers + 3 radial gradients. **PNGs #1 (1,120,454 B, 2002×1000) / #30 (1,175,106 B, 2002×1000)** PNG-overlay pattern (LD10) — build `c1`/`c30` overlays, viewBox `0 0 2002 1000`, then cairosvg-pixel-verify over the real PNGs (LD19) BEFORE commit. **c1 content:** LDR label (left panel, ~x500) + HDR label (right panel, ~x1500) + the 8-bit/32-bit value-scale annotation (the brief's value scales — small scale glyphs/text, composite-pill or labeled style, brand `#1a1a1a` pills / `#FF6B00` accents, NO `c1-*` ids if composite-pill). **c30 content:** Before label (left, grey-world panel) + After label (right, HDRI panel), composite-pill. Divider is baked at x=1000 in both PNGs (orange `#FF6B00` 2px). **Post-commit integrity sweep targets:** em-dash 19 (add none), `&mdash;` 0, color222 0, figure 0→5, img 0→2 (#1+#30), literal `<svg` 0→5 (3 inline + c1 + c30 overlay), figcaption 0→5, wrapper-divs 0→2 (#1+#30 only; the 3 inline SVGs add none), 11 section ids no dups, c1/c30 PNGs once each, `dr-`/`sp-`/`eq-` prefixes present, no `color="#222"` leak. Byte trail starts 139,279.

**L17 P1 PRODUCTION COMPLETE — #1 + #30 RENDERED 2026-06-15** — the 2 Cycles renders are done + visually reviewed + on WSL `images/`; **P1 now 5/30 produced / 0/30 integrated**, HTML still UNTOUCHED at 139,279 B (INTEGRATION deferred to a fresh chat — all 5 P1 figures inline together). **CORRECTED SCENE LOCKED (RENDER-PREP fixes all applied + re-probed):** chrome sphere (Principled metallic **1.0** rough **0.08**, base 0.55) @ (0,0,1) — the reflective fix so it MIRRORS the sunset; matte 0.32 ground size 30; 50mm cam (4.2,-4.2,2.6)→(0,0,1); Cycles GPU 160spp+denoise; film opaque. World = Environment Texture (`images/hdri/belfast_sunset_puresky_2k.hdr`, reloaded, NOT re-downloaded) → Mapping **Z=50°** → Background **strength 0.45** → World Output. **Mapping-Z fix:** direct-pixel-sampled the HDRI (sun azimuth ~231°, warmest ~218°), then empirically swept Z 0–330° — Z=50° lands the warm sunset glow in the sphere reflection (sphere R-B +3, mid-range ~187) while the framed bg sky stays cooler; **strength fix:** 0.45 tames clipping to 1.4% (just the sun disk, correct). **D-D RESOLVED → Standard view transform** (re-probed AgX vs Standard on the CORRECTED scene: AgX visibly mutes/desaturates the golden equatorial sunset reflection to pale warm-grey; Standard keeps it saturated + punchy — the whole teaching point; L15 #13 / L16 #20 precedent). AgX RESTORED at close. **#30 before_after_comparison** = 2-panel 2002×1000, same sphere/cam both panels: LEFT before = default grey **0.05** Background world str 1.0 (env chain disconnected) → flat dull near-black sphere (mean 43, reads as boring default lighting); RIGHT after = the HDRI (locked Z=50/str0.45) → vivid warm sunset reflection + soft shadow. `#FF6B00` 2px divider at x=1000. **#1 hdr_ldr_comparison** = 2-panel 2002×1000, same sphere: LEFT LDR = the balanced HDR render run through a hard 8-bit clip S-curve (black point 95 / white point 165, then ~5-bit posterize) → crushed black shadows + blown highlight band + posterized sky + heavy color cast (the 8-bit failure); RIGHT HDR = balanced Standard render, full detail BOTH ends (warm sunset reflection + cloud detail + graded lower sphere + soft shadow). `#FF6B00` 2px divider at x=1000. **NO baked labels on either** (c1 carries LDR/HDR + 8-bit/32-bit scales; c30 carries before/after — both at INTEGRATION). Deliverables `get_file_info`-confirmed: `images/lesson_17_01_hdr_ldr_comparison.png` **1,120,454 B** (2002×1000) + `images/lesson_17_30_before_after_comparison.png` **1,175,106 B** (2002×1000). **Blender torn down to clean base at close:** 0 objects / 0 materials, EEVEE + AgX + look None + exposure 0, default grey-0.05 Background world (Env Texture removed), HDRI image datablock removed from .blend (persisted .hdr on disk RETAINED), orphans purged (3 data-blocks). Temps cleaned + verified: Windows `C:\Users\pract\AppData\Local\Temp\L17P1` rmtree'd (isdir False), 6 WSL `_tmp_l17p1_*` PNGs removed (glob empty). No `_overlay_VERIFIED.svg` artifacts (overlays come at INTEGRATION). No BlenderMCP debt. **Next: P1 INTEGRATION** — refresh fresh HTML copy, re-verify all 5 anchors count=1 by unique multi-line TEXT, bottom-up insert per the requirements placement table; SVGs #2/#3/#4 inline per LD12 (strip root `color="#222"`), #1/#30 PNG-overlay pattern with c1/c30 built + cairosvg-pixel-verified (LD19) before commit; em-dash stays 19, post-commit integrity sweep.

**L17 P1 RENDER-PREP 2026-06-15** — HDRI sourced + scene prototyped + D-D probed; renders #1/#30 NOT yet final (deferred to fresh sub-chat). **HDRI sourced (D-C):** `belfast_sunset_puresky` (Poly Haven's most-downloaded HDRI, partly-cloudy sunset pure-sky) downloaded 2k hdr via `download_polyhaven_asset`, then **persisted into the project** at `images/hdri/belfast_sunset_puresky_2k.hdr` (4,563,204 B, 2048×1024, `get_file_info`-confirmed) so later phases reload deterministically (the addon's original `tmp_*.hdr` lived in a Windows temp that could be cleaned). The Poly Haven addon auto-builds the full world chain Texture Coordinate → Mapping → Environment Texture → Background → World Output. **Scene prototyped (D-E/D-F):** glossy grey sphere (base 0.5, metallic 0, rough 0.15) on a matte 0.32 ground, 50mm camera at (4.2,−4.2,2.6) aimed at (0,0,1), Cycles GPU 128spp+denoise, no manual lights (world carries lighting), film NOT transparent (HDRI visible in bg). **D-D probe result — NEITHER AgX NOR Standard acceptable yet, BUT root cause is scene design, not just view transform:** at 800×450/64spp, AgX sphere washed near-white (sky RGB 156/155/167 R−B −11, sphere 207/197/195) and Standard sphere clipped paler-still (sky 168/164/185 R−B −17, sphere 255/251/247). **Two scene-design fixes the render sub-chat MUST apply before final:** (1) **rotate the world Mapping node Z** so the warm sunset glow sits behind/beside the sphere from camera view — current framing faces the pale/cool part of the pure-sky, so the sunset character barely reads; (2) **make the sphere more reflective** (chrome-ish, rough ~0.05–0.1, or metallic) so it actually MIRRORS the sunset (the whole point of the HDRI figures) instead of averaging to pale grey. After those fixes, re-probe AgX vs Standard — expectation per D-D still leans **Standard** for the warm sunset (it read marginally warmer here), but decide on the corrected scene. The sphere brightness also needs taming (likely lower world/background strength or expose-down) so the subject lands mid-range, not clipped. **Scene torn down to clean base at close:** 0 objects / 0 materials, EEVEE + AgX + look None, default grey-0.05 Background world (Env Texture removed), HDRI image datablock removed from .blend (the persisted .hdr file on disk RETAINED), orphans purged, Windows `C:\Users\pract\AppData\Local\Temp\L17P1` rmtree'd, 2 WSL `_tmp_l17p1_probe_*` PNGs removed (all ENOENT-verified). No BlenderMCP debt. **HTML UNTOUCHED at 139,279 B.** Still **3/30 produced** (the 3 SVGs) **/ 0/30 integrated** — #1/#30 remain to render.

**L17 P1 PRODUCTION PARTIAL — 3 SVGs DONE 2026-06-15** — all three P1 inline-SVG figures produced, cairosvg light+dark render-checked, written to WSL `images/` + `get_file_info`-confirmed byte-exact. **D-A + D-C resolved this chat (Ray-confirmed): #3 → hand-coded SVG; HDRI sourcing → `download_polyhaven_asset` CC0.** Deliverables: **#2 `dr`** `lesson_17_02_dynamic_range_spectrum.svg` 7,945 B (viewBox 800×680; LDR 8-bit bar 0/128/255 + clamp wall vs HDR 32-bit bar 0.0/1.0/2.0/5.0/10.0+ with orange over-1.0 glow; 4 value chips shadow 0.1 / ambient 1.0 / bulb 2.0 / sun 10+); **#4 `eq`** `lesson_17_04_equirectangular_projection.svg` 6,611 B (viewBox 800×680; 2:1 panoramic map + sun/clouds/horizon + lat/long grid + pole-stretch callouts, projection arrows down to a gridded wrap-sphere; footer shortened pre-write to clear x≤760); **#3 `sp`** `lesson_17_03_hdri_sphere_concept.svg` 6,472 B (viewBox 800×680; translucent env-sphere + sun, central shaded object, inward IBL rays warm `#FF8A33`/cool `#4A9EFF`/dim `#8A8F98` + legend; unused `context-stroke` marker pruned pre-write). All three: v6 header (3px `#FF6B00` strip, title fs24/700, subtitle fs14 op0.65, divider y108), `currentColor` text, brand palette, root `color="#222"` KEPT for `<img>` fallback (STRIP at inlining per LD12), Variant-B footer tip at dense y641/663, `&#183;` middle-dot separators (no em-dashes), `&#176;`/`&#215;`/`&#8594;` content entities. **HTML UNTOUCHED at 139,279 B** (PRODUCTION only; P1 INTEGRATION deferred until renders #1/#30 land — all 5 P1 figures inline together). No BlenderMCP work this chat (all-SVG). Progress-doc registry + counts (SVG 6→7 / PNG 24→23) + id-prefix registry + c{N} namespace (c3 retired) + P1 phase row + D-A/D-C updated. **3/30 produced / 0/30 integrated.**

**L17 PLANNING DONE 2026-06-15** — entry-state verified byte-exact on the real UNC path (139,279 B, em-dash 19, 0 figures, 11 sections no dups, 0 live mermaid blocks). All four source docs read in full (`session.md`, this doc, `lesson_17_image_requirements.md`, `IMAGE_STYLE_GUIDE.md` v6). Requirements reconciled: 30 figures #1–#30 (no renumber needed), 6 SVG (#2,#4,#17,#21,#25,#28) / 24 PNG; requirements palette + Blender-4.0 framing OVERRIDDEN (brand palette + Blender 5.1.1 govern); AI-gen prompts read as content briefs only. Figure registry, id-prefix registry, c{N} namespace, 10-phase plan (HTML section order, summary figure-free), 8 DECISIONS (D-A..D-H; biggest open = D-C HDRI .hdr/.exr sourcing + D-D color-dependent-render-default + D-E world-node clean base), Locked-rules carry-forward all authored this chat. P1 (5) flagged for PRODUCTION sub-chat split; **P3 (7) flagged for PRODUCTION A/B + own INTEGRATION chat**. **Plan-lock PENDING Ray's confirmation before any figure production.** No HTML touched (stays 139,279 B / em-dash 19). No BlenderMCP work (PLANNING only).

---

## §6 — Per-figure integration detail (populated as phases close)

### P10 — project (#29 `c29`, P10 CLOSED 2026-06-16) · 270,245 → 274,929 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 29 | project_setup_reference | PNG-overlay (UI) | `c29` (4 numbered callouts, no ids) | `<h3>Part 1: Scene Setup</h3>` (after Project-Goals card `</div>`) | +4,684 |

Genuine Blender VIEW_3D capture. World = simple HDRI chain (Environment Texture(belfast_sunset_puresky_2k) → Background str 1.0 → World Output). Scene: Suzanne (shade-smooth, grey BSDF rough 0.35, rot Z 20°) on matte 0.32 ground (size 30), camera (5,-5,3.2) lens 50 aimed (0,0,1.1) ≈ 45°, NO manual lights. Viewport RENDERED + camera view, chrome hidden, full-window `screen.screenshot` → Windows tempdir → Pillow crop → header-trim (keep "Camera Perspective" label) → WSL. `images/lesson_17_29_project_setup_reference.png` **1333×788, 869,872 B** (decode-verified RGB). `c29` = 4 numbered callouts (1 Test object / 2 Ground plane / 3 Camera at ~45° / 4 HDRI background · no manual lights), numbered-pill (`#FF6B00` badges + leaders, `#1a1a1a` 0.82 pills + white text, NO `c29-*` ids), viewBox 0 0 1333 788, cairosvg-pixel-verified 1:1 + visually reviewed. Default-depth LD10 (wrapper-div + img margin:0 loading=lazy + inlined overlay-svg, figcaption OUTSIDE). figcaption: `Project scene setup &#183; a test object on a ground plane, framed by the camera at about 45&#176;, lit entirely by the HDRI environment with the default light deleted.` AgX HELD (UI capture). Teardown: scene reset to clean base 0/0/0/0/0 + AgX + grey-0.05 world; Windows `C:\Users\Public\L17P10` + WSL `_tmp_l17p10_*` purged + ENOENT-verified. Registry row #29 → INTEGRATED. **P10 project CLOSED.**

### P8 — advanced-techniques (#22 `c22`, P8 CLOSED 2026-06-16) · 265,457 → 270,245 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 22 | separate_lighting_nodes | PNG-overlay (UI, World Shader Editor) | `c22` (4 numbered callouts, no ids) | `<h3>Separate Lighting and Background</h3>` (after section intro `<p>`) | +4,788 |

Genuine Blender World-mode Shader Editor capture (`space.shader_type='WORLD'` on area3). Node network: Texture Coordinate → Mapping → Environment Texture (belfast_sunset_puresky_2k reloaded) → two Background nodes [**Lighting (high) Strength 3.000** + **Camera/Background (low) Strength 0.300**] → **Mix Shader** (Light Path **Is Camera Ray** → Factor) → World Output. N-panel/toolbar hidden, `node.view_all` framed, full-window `screen.screenshot` → Windows tempdir → Pillow crop → tight content crop → WSL. `images/lesson_17_22_separate_lighting_nodes.png` **985×550, 72,506 B** (decode-verified RGB; breadcrumb Scene›World confirms WORLD mode; both strengths + Light Path Is-Camera-Ray + Mix Shader legible, no UI bleed). `c22` = 4 numbered callouts (1 Light Path · Is Camera Ray / 2 Lighting · high strength / 3 Background · low strength / 4 Mix Shader combines both), numbered-pill (NO `c22-*` ids), viewBox 0 0 985 550, cairosvg-pixel-verified 1:1 + visually reviewed. Default-depth LD10. figcaption: `Separate lighting and background strength &#183; the Light Path node's Is Camera Ray output drives a Mix Shader so the visible background can stay dim while the scene lighting stays bright.` AgX HELD (UI capture). Teardown deferred to the combined #29 close (single chat). Windows `C:\Users\Public\L17P8` + WSL `_tmp_l17p8_*` purged + ENOENT-verified. Registry row #22 → INTEGRATED. **P8 advanced-techniques CLOSED.**

### P9 — troubleshooting (#25 `tf`, P9 CLOSED 2026-06-16) · 256,116 → 265,457 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 25 | troubleshooting_flowchart | inline SVG | `tf` (+ `tf-arrow` marker) | `<h3>HDRI Not Showing/Scene Too Dark</h3>` | +9,341 |

All-SVG phase — no Blender, no render, no teardown. Hand-coded decision-tree SVG viewBox `0 0 800 680`, id-prefix `tf` (tf-title/tf-desc + a `tf-arrow` marker). Distills the section's 7 problems into a flow: orange start "HDRI not working?" → blue diamond "Is the HDRI visible in render?" — NO (left) → dark-scene fix-box (Use World not Object / Environment Texture node / Strength ≥ 1.0) → green FIX "connect to World Output, raise Strength"; YES → blue diamond "Colors / look wrong?" → 3 green fix boxes (Washed out→Standard view transform / Wrong angle→Mapping node rotate Z / Grainy→raise samples + Denoise). Legend strip + Variant-B footer tip. Palette `#FF6B00` start+strip / `#4A9EFF` decisions / `#4ADE80`+`#0f1a14` fixes / `#E63946` NO / `#8A8F98` arrows; body `currentColor`. 0 em-dashes (separators `&#183;` ×2, bullets `&#8226;` ×3, `&#8805;` ×1 NEW harmless). cairosvg light+dark render-checked clean. Standalone `images/lesson_17_25_troubleshooting_flowchart.svg` **7,212 B** (root `color="#222"` KEPT for `<img>` fallback), get_file_info-confirmed. Inlined per LD12: root `color="#222"` STRIPPED, figure col20 / svg col24 / children col28 / `<g>` col32, `tf-arrow` marker + `<defs>` + tf- ids + role/aria/title/desc preserved, figcaption col20 (`HDRI troubleshooting decision tree &#183; check visibility first, then colors, rotation, and grain`). Anchor count=1 = before the first problem h3 (figure at the beginning of `troubleshooting`, after intro `<p>`). edit_file dryRun→commit→get_file_info. Byte trail: 256,116 (P0) → +9,341 → **265,457 B**. Post-commit sweep CLEAN: em-dash **19 held**, `&mdash;` 0, color222 0, figure/figcaption 27→**28** balanced, img **20 unchanged** (inline adds 0), `<svg`/`</svg>` 27→**28**, `<marker` 4→**5** (tf-arrow adds 1 — NOTE: inline SVG with a marker, unlike PNG-overlay phases), wrapper-divs **20 unchanged**, `&#183;` 57→**60** (+3), `&#8226;` 4→**7** (+3), `&#8805;` 0→**1** (NEW), `&#8211;` 12 / `&#176;` 9 / `&#215;` 1 / `&#8594;` 2 unchanged, literal middot/en-dash/minus 0, tf- present (12; tf-arrow id once + ref 7×), c25 token 0 (inline, no PNG), 11 ids no dups, 0 live mermaid. No teardown owed, no temps, no `_overlay_VERIFIED.svg`, no BlenderMCP debt. Registry row #25 → INTEGRATED. **P9 troubleshooting CLOSED. L17 28/30 produced / 28/30 integrated.**

### P7 — finding-hdris (#20 + #21, P7 CLOSED 2026-06-16) · 240,669 → 256,116 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 21 | file_organization | inline SVG | `fo` | green "Starting HDRI Collection Recommendation" card (after File Organization card) | +10,953 |
| 20 | poly_haven_navigation | PNG-overlay (UI) | `c20` (5 numbered callouts, no ids) | `<h3>Premium HDRI Resources</h3>` | +4,494 |

Doc-order in `finding-hdris`: Free HDRI Resources card → **#20** (before Premium HDRI Resources) → Premium → Choosing → Testing → File Organization card → **#21** → green Starting-Collection card. Both figs now landed; P7 CLOSED 2/2.

**#20 poly_haven_navigation (this chat):** the prior chat's browser→WSL pixel-relay blocker was sidestepped — **route (b) probed FIRST and CONFIRMED LIVE**: the Windows Blender Python `urllib` reaches Poly Haven's public API/CDN (api.polyhaven.com/assets + /files HTTP 200; cdn.polyhaven.com/asset_img/thumbs/<slug>.png real `image/png`), separate from Claude-side bash's `403 host_not_allowed`. So NO browser screenshot needed — fetched real CDN imagery + real metadata bridge-side (PIL 12.2.0 + Segoe UI) and composited a faithful annotated nav reference. `images/lesson_17_20_poly_haven_navigation.png` **2200×1180, 953,973 B** (get_file_info + decode-verified RGB on the real path): top nav (HDRIs active), LEFT sidebar category filters with REAL counts (All 972 / Outdoor 686 / Skies 293 / Indoor 295 / Studio 96 / Sunrise-Sunset 217 / Night 59 / Nature 519 / Urban 487) + Browse-by-category thumbnail row; CENTER toolbar (search + "972 results" + Sort: Popular) + 3×2 HDRI grid of 6 real CC0 CDN thumbnails (Kloofendal 48d / Lilienstein / The Sky Is On Fire / Brown Photostudio 02 / Studio Small 09 / Dikhololo Night, CC0 pills); RIGHT asset-page panel for Kloppenheim 06 (real CDN preview + 360° badge + breadcrumb + CC0 License + Resolution 1K/2K/4K/8K/16K [4K active] + Format HDR/EXR [EXR active] + Download "4K · EXR · 19.77 MB" = the REAL 4K-EXR size 19,767,459 B). NO baked labels. **`c20`** = 5 numbered callouts (1 Search bar / 2 Category filters / 3 HDRI grid / 4 Individual card / 5 Multiple resolutions), numbered-pill (`#FF6B00` badges + leaders, `#1a1a1a` 0.82 pills + white text, NO `c20-*` ids), viewBox 0 0 2200 1180; cairosvg-pixel-verified 1:1 over the real on-disk PNG (LD19) + visually reviewed (callout 5 re-aimed twice). Default-depth LD10 (wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=2200×1180 preserveAspectRatio xMidYMid meet, figcaption OUTSIDE). Anchor count=1 = before `<h3>Premium HDRI Resources</h3>`. Byte trail: 251,622 → +4,494 → **256,116 B**. Post-commit sweep CLEAN: em-dash **19 held**, figure/figcaption 26→27, img 19→20, `<svg`/`</svg>` 26→27, marker 4, wrapper-divs 19→20, c20 PNG once, c20- ids 0, `&#183;` 56→57, `&#8226;` 4 / `&#8211;` 12 / `&#176;` 9 / `&#215;` 1 / `&#8594;` 2 unchanged, fo-/hc-/th-/ff- unchanged, 11 ids no dups, color222 0, 0 live mermaid. Pre-existing `Pro tip—Create` em-dash preserved. No Blender scene work (network/PIL only, scene stayed clean 0/0); Windows `C:\Users\Public\L17P7` + WSL `_tmp_l17p7_*` purged + ENOENT-verified, no `_overlay_VERIFIED.svg`. Registry row #20 → INTEGRATED.

**#21 file_organization (prior chat):** inline SVG per LD12 (root `color="#222"` stripped, figure col20 / svg col24 / children col28 / `<g>` col32 / nested-`<g>` col36, `<defs>`+`fo-fold` `<use>` glyph kept, `fo-*`+role/aria/title/desc preserved, figcaption col24). Standalone `images/lesson_17_21_file_organization.svg` **7,888 B** (root color222 KEPT). viewBox 800×680 dense, mirrors the section `<pre>` tree (Outdoor/Studio/Interior/Creative + real subfolders + file-count ranges), color-coded category folders, Variant-B footer. cairosvg light+dark checked. em-dash 0 (the `<desc>` em-dashes were rewritten pre-write). Byte trail: 240,669 → +10,953 → 251,622 B. **P7 finding-hdris FULLY CLOSED, 2/2 figs. L17 27/30 produced / 27/30 integrated.**

### P6 — combining-lights (#17 + #18 + #19, P6 CLOSED 2026-06-16) · 225,661 → 240,669 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 17 | hybrid_concept_diagram | inline SVG | `hc` | `<h3>Why Combine HDRI and Manual Lights?</h3>` | +6,699 |
| 18 | hdri_key_light_setup | PNG-overlay | `c18` (2 centered pills, no ids) | `<h3>Common Hybrid Lighting Setups</h3>` | +1,893 |
| 19 | three_point_hdri_diagram | inline SVG | `th` (D-B RESOLVED → SVG) | `<h3>Common Use Cases</h3>` | +6,416 |

**D-B RESOLVED → #19 hand-coded SVG `th`** (Ray-confirmed at PRODUCTION; c19 RETIRED, SVG count 7→8). **D-F for #18 → glossy dielectric sphere** (Ray-confirmed; metallic 0 / rough 0.30 / base 0.42 — the Area key reads as a visible highlight, unlike full chrome which just mirrors the HDRI). **D-D for #18 → Standard view transform** (warm belfast sunset env washes the warm reflection band under AgX; AgX restored at teardown). Doc order in `combining-lights` now: intro `<p>` → **#17** → Why Combine → Best of Both Worlds card → **#18** → Common Hybrid Setups → Workflow → 70/30 card → Matching Lights → **#19** → Common Use Cases. Inserted bottom-up #19 → #18 → #17, each anchor count=1 on a fresh copy by unique bare-`<h3>` text. #17/#19 inline per LD12 (root `color="#222"` stripped, figure col20 / svg col24 / children col28 / `<g>` col32, structural comments kept, decorative `====` pruned, `hc-*`/`th-*` + role/aria/title/desc ids preserved, footer `<text><tspan>` collapsed one-line). #18 PNG-overlay per LD10 (wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=1400×2002 preserveAspectRatio xMidYMid meet, figcaption OUTSIDE).

**#18 PRODUCTION:** glossy dielectric sphere (metallic 0 rough 0.30 base 0.42) @ (0,0,1), matte 0.32 ground size 30, 50mm cam (4.2,-4.2,2.6)→(0,0,1), Cycles GPU 200spp+denoise, **Standard + look None**. World env chain TexCoord→Mapping(Z=50°)→EnvTexture(belfast_sunset_puresky_2k)→Background(**strength 0.4**)→World Output. ADDED Area key light (energy 650, size 3.0, color near-neutral (1.0,0.98,0.95)) at (-3,-3,4.2) aimed at subject. Split composite (Pillow): render-half 1320×1320 on top + orange `#FF6B00` 2px divider + 96px CLEAR BAND (for the setup pill) + node-half 1320×464 below (World Shader Editor capture, full-window screenshot to Windows tempdir → deterministic area crop → tight node-panel crop, Strength 0.400 + Mapping Z 50° both legible), on neutral `#1a1a1a` backing. Final `images/lesson_17_18_hdri_key_light_setup.png` **1400×2002, 1,095,713 B**, NO baked labels. **c18 overlay** = 2 centered composite-pills (`#1a1a1a` 0.82 + white 600-wt text, NO `c18-*` ids): "Result &#183; HDRI + Area key" cy=110 fs42 + "Setup &#183; low HDRI (0.4) + Area key" cy=1450 fs40 (in the clear band y1402..1498). cairosvg-pixel-verified 1:1 over the REAL on-disk PNG (LD19), both bbox in-bounds, no content overlap.

**#17 `hc`** standalone `images/lesson_17_17_hybrid_concept_diagram.svg` **5,171 B** (viewBox 800×680: HDRI∩Manual Venn — HDRI orange circle [ambient/reflections/environment/quick realism] + Manual blue circle [Key #667eea / Fill #6BCF7F / Rim #FF6B9D / drama·control] + BEST RESULTS overlap; 2 example tiles HDRI-only flat vs HDRI+manual controlled; Variant-B dense footer). **#19 `th`** standalone `images/lesson_17_19_three_point_hdri_diagram.svg` **5,029 B** (viewBox 800×680 top-down: dashed orange HDRI ring + subject center + RIM top/behind #FF6B9D, KEY lower-left 45° #667eea solid cone, FILL lower-right #6BCF7F dashed cone, camera bottom-center with sightline; D1 three-light coding). Both root `color="#222"` KEPT in standalone for `<img>` fallback; both cairosvg light+dark render-checked clean (footer overflow + camera/legend collision fixed pre-write).

**Byte trail:** 225,661 (P0) → #19 +6,416 → 232,077 → #18 +1,893 → 233,970 → #17 +6,699 → **240,669 B** (each get_file_info-confirmed). **POST-COMMIT INTEGRITY SWEEP CLEAN (fresh 240,669 B):** em-dash **19 HELD**, `&mdash;` 0, color222 0, figure/figcaption 22→**25** balanced, img 18→**19** (#18 only; #17/#19 inline add 0), literal `<svg`/`</svg>` 22→**25** (#17 + #19 inline + c18 overlay), `<marker` **4** (P6 adds none), wrapper-divs 18→**19** (#18 only), `hc-` 4, `th-` 4, `ff-` 4, c{N}- ids **0**, 11 section ids no dups, 0 live mermaid, `&#183;` 38→**53** (#17 7 + #19 4 + #18 overlay 2 + figcaptions), `&#176;` 7→**9** (#19 45° svg + figcaption), `&#215;` 1 / `&#8594;` 2 unchanged, `&#8211;` 2 (footer-tip ranges in #17/#19), literal middot/en-dash/minus 0, #18 PNG referenced once. Pre-existing intro em-dash ("both worlds—realistic base lighting") preserved verbatim above the #17 anchor. **BLENDER TORN DOWN CLEAN (D-E):** P6 legitimately added a manual Area key + env chain — all removed; 0/0/0/0/0 + EEVEE + AgX + look None + exposure 0 + grey-0.05 Background→World Output, no env-texture/HDRI datablocks, orphans purged. Temps purged + ENOENT-verified: Windows `C:\Users\Public\L17P6` isdir False, addon tmp*.hdr 0, WSL `_tmp_l17p6_*` glob empty, no `_overlay_VERIFIED.svg`. No BlenderMCP debt. Persisted HDRIs retained (belfast + brown_photostudio_02 2k/4k/8k + 6 new 2k). Registry rows #17/#18/#19 → INTEGRATED. **P6 combining-lights FULLY CLOSED, 3/3 figs. L17 25/30 produced / 25/30 integrated.**

### P5 — hdri-types (#13 + #14 + #15 + #16, P5 CLOSED 2026-06-15) · 215,086 → 225,661 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 13 | hdri_types_grid | PNG-overlay | `c13` (6 centered pills, no ids) | `<h3>Outdoor/Natural HDRIs</h3>` (after intro `<p>`) | +3,578 |
| 14 | outdoor_examples | PNG-overlay | `c14` (4 centered pills, no ids) | `<h3>Studio HDRIs</h3>` | +2,625 |
| 15 | studio_examples | PNG-overlay | `c15` (3 centered pills, no ids) | `<h3>Interior HDRIs</h3>` | +2,191 |
| 16 | interior_examples | PNG-overlay | `c16` (3 centered pills, no ids) | `<h3>Creative/Artistic HDRIs</h3>` | +2,181 |

Doc order in `hdri-types` now: intro `<p>` → **#13** → Outdoor/Natural HDRIs → **#14** → Studio HDRIs → **#15** → Interior HDRIs → **#16** → Creative/Artistic HDRIs. Inserted bottom-up #16 → #15 → #14 → #13, each anchor count=1 on a fresh copy by unique multi-line TEXT (#14/#15/#16 by their bare `<h3>` heading text; #13 by the intro-`<p>`+Outdoor-`<h3>` pair). All default-depth (figure col20 / div col24 / img+svg col28 / svg-children col32 / figcaption col20), LD10 wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=PNG px preserveAspectRatio xMidYMid meet, figcaption OUTSIDE. Overlays all centered-pill (`#1a1a1a` 0.82 + white 600-wt text, NO `c{N}-*` ids), pills near panel bottom y=810 (top row) / y=1712 (#13 bottom row), cairosvg-pixel-verified 1:1 over the real PNGs + visually reviewed before commit (LD19): c13 6 pills Clear sky/Cloudy/Sunset (row1) · Studio/Interior/Night (row2) at panel centers x=450/1352/2254 y=810 & y=1712 fs40 (bbox 316..2352 × 778..1744); c14 4 pills Clear sky/Cloudy/Sunset/Night x=450/1352/2254/3506 y=810 fs40 (bbox 316..3592 × 778..842); c15 3 pills Three-point/Softbox grid/Single key + ambient x=450/1352/2254 y=810 fs40 (bbox 292..2520 × 778..842); c16 3 pills Window/Artificial/Mixed x=450/1352/2254 y=810 fs40 (bbox 352..2340 × 778..842). All decode-verified RGB (#13 2704×1802, #14 3606×900, #15 2704×900, #16 2704×900). Post-commit integrity sweep CLEAN (fresh 225,661 B): em-dash **19 held**, `&mdash;` 0, color222 0, figure/figcaption 18→**22** balanced, img 14→**18**, literal `<svg`/`</svg>` 18→**22**, wrapper-divs 14→**18**, loading=lazy 18, `<marker` **4** (overlays add none), c13/c14/c15/c16 PNGs once each, c{N}- ids **0**, ff- 4, 11 section ids no dups, 0 live mermaid, `&#183;` 34→**38** (+4 figcaptions; pills carry plain text, no entities), `&#176;` 7 / `&#215;` 1 / `&#8594;` 2 unchanged, no en-dash/minus/literal-middot leak. No new em-dashes (figcaptions use `&#183;` separators + `&rsquo;` apostrophe). No Blender teardown owed (INTEGRATION-only; scene torn down clean at P5 PRODUCTION close — 0/0/0/0/0 + EEVEE + AgX + look None + grey-0.05 world, no HDRI datablocks). No temps/artifacts this chat (overlays built Claude-side only, no `_overlay_VERIFIED.svg`). Persisted HDRIs retained (belfast + brown_photostudio_02 2k/4k/8k + 6 new 2k). Registry rows #13/#14/#15/#16 → INTEGRATED. **P5 hdri-types FULLY CLOSED, 4/4 figs. L17 22/30 produced / 22/30 integrated.**

### P4 — adjusting-hdri (#11 + #9 + #12 + #23, P4 CLOSED 2026-06-15) · 204,841 → 215,086 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 11 | strength_comparison | PNG-overlay | `c11` (3 pills, no ids) | `<h3>Rotation</h3>` | +2,637 |
| 9 | rotation_node_setup | PNG-overlay (UI-chrome) | `c9` (highlight + leader + orange-stroked pill, no ids) | `<h3>Color Adjustment</h3>` (above #12) | +2,069 |
| 12 | rotation_effect | PNG-overlay | `c12` (4 angle pills, no ids) | `<h3>Color Adjustment</h3>` (below #9) | +3,019 |
| 23 | color_grading_nodes | PNG-overlay (UI-chrome) | `c23` (3 node pills, no ids) | `<h3>Background Visibility Control</h3>` | +2,520 |

Doc order in `adjusting-hdri` now (Strength prose) → #11 → Rotation → #9 → #12 → Color Adjustment → #23 → Background Visibility Control. Inserted bottom-up #23 → #12 → #9 → #11; within the shared Color-Adjustment anchor #12 inserted first so #9 lands above it (doc order #9>#12). Each anchor count=1 on a fresh copy by unique bare-`<h3>` TEXT. All default-depth (figure col20 / div col24 / img+svg col28 / svg-children col32 / figcaption col20), LD10 wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=PNG px preserveAspectRatio xMidYMid meet, figcaption OUTSIDE. Overlays REBUILT from recorded coords (container reset wiped the prior SVGs), cairosvg-rendered at exact PNG px + composited over the real PNGs + visually re-confirmed 1:1 before commit (LD19): c11 3 "Strength &#183; N" pills x=450/1352/2254 y=812 fs40; c12 4 angle pills 0&#176;/90&#176;/180&#176;/270&#176; at the 2×2 quadrant centers fs46; c9 `#FF6B00` highlight rect on the Mapping Rotation-Z=45&#176; row + leader to an orange-stroked "Z &#183; rotates HDRI" pill (orange stroke REQUIRED to read on the dark node canvas); c23 3 node pills HSV/ColorRamp/RGB Curves x=630/1892/3153 y=382 fs24 (y≤403 for the 411px image). All composite/centered-pill brand `#1a1a1a`, white text, NO `c{N}-*` ids. Pre-existing closing-checklist em-dash ("strength and rotation—these have the biggest impact", below the #23 anchor) preserved verbatim. Post-commit integrity CLEAN (em-dash 19 held; figure/figcaption 14→18; img 10→14; literal `<svg`/`</svg>` 18/18; wrapper-divs 10→14; marker 4; 11 ids no dups; c11/c9/c12/c23 PNGs once each; c{N}- ids 0; ff- ids 4; `&#176;` 3→7; `&#183;` 30→34). No Blender teardown owed (INTEGRATION-only; scene clean at P4 PRODUCTION close). **P4 adjusting-hdri FULLY CLOSED, 4/4 figs. L17 18/30 produced / 18/30 integrated.**

### P3 (final) — setting-up-hdri (#26 + #24, P3 CLOSED 2026-06-15) · 199,636 → 204,841 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 26 | viewport_mode_comparison | PNG-overlay | `c26` (centered-pill, no ids) | existing #27 figure (after Testing-Your-HDRI-Setup card) | +2,597 |
| 24 | quality_comparison | PNG-overlay | `c24` (centered-pill, no ids) | Setup-Philosophy blockquote (after #28 `ff` figure) | +2,608 |

Doc order in `setting-up-hdri` now #7 → #8 → #10 → #26 → #27 → #28 → #24. Inserted bottom-up #24 then #26, each anchor count=1 on a fresh copy by unique multi-line TEXT. Both default-depth (figure col20 / div col24 / img+svg col28 / svg-children col32 / figcaption col20), LD10 wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=2404×800 preserveAspectRatio xMidYMid meet, figcaption OUTSIDE. c24 = 2K·2048px / 4K·4096px / 8K·8192px; c26 = Solid / Material Preview / Rendered; both centered composite-pill (`#1a1a1a` 0.82, `&#183;` middots, NO c{N}-* ids), cairosvg-pixel-verified 1:1 over the real PNGs before commit (LD19). #24 re-rendered against the detail-rich `brown_photostudio_02` HDRI per D-I (separation Laplacian-variance 2K 74.8 / 4K 95.1 / 8K 111.8). Pre-existing blockquote em-dash ("intentionally simple—add one node") preserved through the #24 anchor. Post-commit integrity CLEAN (em-dash 19 held; figure/figcaption 14/14; img 10; literal `<svg`/`</svg>` 14/14; wrapper-divs 10; marker 4; 11 ids no dups; c24/c26 PNGs once each; c{N}- ids 0; ff- ids 4; `&#183;` 27→30). **P3 setting-up-hdri FULLY CLOSED, 14/14 figs. L17 14/30 produced / 14/30 integrated.**

### P3 (partial) — setting-up-hdri (5 of 7 figures, DONE 2026-06-15) · 182,621 → 199,636 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 28 | file_formats_guide | inline SVG | `ff` | Setup-Philosophy blockquote (after Supported-Formats card) | +7,713 |
| 27 | node_connection_closeup | PNG-overlay (UI-chrome) | `c27` (ring + centered-pill, no ids) | `<h3>HDRI File Formats</h3>` | +1,799 |
| 10 | viewport_shading_icons | PNG-overlay (UI-chrome) | `c10` (ring + pill, no ids) | `<h3>Testing Your HDRI Setup</h3>` | +2,097 |
| 8 | complete_node_network | PNG-overlay (UI-chrome) | `c8` (centered-pill, no ids) | green Quick-Setup-Summary box (block with #7, lower) | (block +5,406) |
| 7 | hdri_setup_workflow | PNG-overlay | `c7` (4 step-pills, no ids) | green Quick-Setup-Summary box (block with #8, upper) | (block +5,406) |

Visual doc order in `setting-up-hdri`: #7 → #8 (after Step-by-Step Loading card) → #10 (after Quick-Setup-Summary box) → #27 (after Verifying-It-Works card) → #28 (after Supported-Formats card). Inserted bottom-up #28 → #27 → #10 → (#7+#8 block). All default-depth (figure col20 / div col24 / img+svg col28 / svg-children col32 / figcaption col20; #28 inline figure col20 / svg col24 / children col28). c7/c8/c10/c27 cairosvg-pixel-verified 1:1 over the real PNGs before commit (LD19), content bbox in-bounds, visually reviewed. #28 `ff` root color222 stripped on inline, NO defs/markers, footer `<text>` collapsed. Pre-existing blockquote em-dash ("intentionally simple—add one node") preserved through the #28 anchor. Post-commit integrity CLEAN (em-dash 19 held; figure/figcaption 12/12; img 8; literal `<svg`/`</svg>` 12/12; wrapper-divs 8; marker 4; 11 ids no dups; c7/c8/c10/c27 PNGs once each; c{N}- ids 0; ff- ids 4). **P3 still OPEN: #26 viewport_mode_comparison + #24 quality_comparison remain to produce + integrate.**

### P2 — world-lighting-basics (2 figures, DONE 2026-06-15) · 175,358 → 182,621 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 6 | default_world_shader | PNG-overlay (UI-chrome) | `c6` (composite-pill, no ids) | world-lighting-basics section anchor (lower) | +3,005 |
| 5 | world_properties_panel | PNG-overlay (UI-chrome) | `c5` (composite-pill, no ids) | world-lighting-basics section anchor (upper) | +4,258 |

Two UI-chrome PNGs (no Blender renders this phase — screen-captured panels). #6 `lesson_17_06_default_world_shader.png` = 44,015 B, 1273×312 (clean 2-node shader-editor reference: Background + green noodle + World Output, even ~25px margins, no clipping, NO baked labels; recropped FINAL after x990-estimate crops failed — true node-body right edge is x1276, source full2 1920×1046, prior bad 20,304 B crop overwritten). #5 `lesson_17_05_world_properties_panel.png` = 33,629 B, 584×426 (Properties → World tab, unchanged). Inserted bottom-up #6 then #5, each anchor count=1, dryRun→commit→get_file_info. Both default-depth (figure col20 / img+overlay-svg col24 / figcaption col20), LD10 wrapper-div + img margin:0 loading=lazy + inlined overlay-svg viewBox=PNG px, figcaption OUTSIDE. c5/c6 = centered/composite-pill (`#1a1a1a` 0.82 + `#FF6B00`, NO `c{N}-*` ids), cairosvg-pixel-verified 1:1 over the real PNGs before commit (LD19). Post-commit sweep CLEAN: em-dash 19 held, figure 5→7, figcaption 5→7, img 2→4, literal `<svg` 5→7, wrapper-divs 2→4, marker 4 (overlays add none), c5/c6 PNGs once each, c5-/c6- ids 0, 11 ids no dups, 0 live mermaid. Blender torn down clean (NODE_EDITOR→VIEW_3D + chrome, stray temp IMAGE_EDITOR window closed, Properties→SCENE, AgX confirmed, 0 obj/0 mat + default grey world); temps purged + ENOENT-verified (Windows `C:\Users\Public\L17P2` + WSL `_tmp_l17p2_*` 5 files), no `_overlay_VERIFIED.svg` artifacts.

### P1 — what-is-hdri (5 figures, DONE 2026-06-15) · 139,279 → 175,358 B

| # | slug | type | id / c{N} | anchor (before) | bytes added |
|---|------|------|-----------|-----------------|-------------|
| 1 | hdr_ldr_comparison | PNG-overlay | `c1` (composite-pill, no ids) | `<h3>HDR vs. LDR Images</h3>` (block with #2) | (block +14,876) |
| 2 | dynamic_range_spectrum | inline SVG | `dr` | `<h3>HDR vs. LDR Images</h3>` (block with #1) | (block +14,876) |
| 30 | before_after_comparison | PNG-overlay | `c30` (composite-pill, no ids) | `<h3>How HDRIs Work as Lighting</h3>` | +2,300 |
| 3 | hdri_sphere_concept | inline SVG | `sp` | `<h3>Types of HDRI Maps</h3>` (after blockquote) | +9,135 |
| 4 | equirectangular_projection | inline SVG | `eq` | `<h3>When to Use HDRI</h3>` | +9,768 |

Visual doc order in `what-is-hdri`: #1 → #2 (after Understanding-HDRI card) → #30 (after HDR-vs-LDR card) → #3 (after HDRI-Revolution blockquote) → #4 (after Types-of-HDRI-Maps card). Inserted bottom-up #4→#3→#30→(#1+#2 block). All default-depth (figure col20 / svg+img col24 / svg children col28 / figcaption col20). c1/c30 cairosvg-pixel-verified 1:1 over the real 2002×1000 PNGs before commit. #4 `eq` kept both `<defs>` (forward-ref `eq-arrow` marker); #3 `sp` kept 3 markers; all 3 SVGs root color222 stripped on inline. Two pre-existing em-dashes preserved through anchors. Post-commit integrity CLEAN (em-dash 19 held; figure/figcaption 5/5; img 2; literal `<svg`/`</svg>` 5/5; wrapper-divs 2; marker 4; 11 ids no dups; c1/c30 PNGs once each; c1-/c30- ids 0).

