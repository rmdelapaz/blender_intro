# Lesson 07: Modeling Your First Object · Integration Progress

**Lesson HTML:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_07_modeling_your_first_object.html` (114.7 KB at planning chat close per status.md)
**Current state:** L07 INTEGRATION COMPLETE · 30 of 30 integrated · browser-QA PASS 2026-06-13 (8-img locked-style defect fixed; #20/#21 held flags RESOLVED) · L07 image work + browser-QA complete · HTML 221,053 B
**Last updated:** 2026-06-13 (L07 browser-QA PASS · full Playwright + real-Chromium sweep dark+light × 1920/1024/414 · 8 overlay imgs #17/#18/#19/#20/#21/#22/#24/#25 carried bare `<img>` tags missing the locked `display:block; max-width:100%; height:auto; margin:0` style → constant dy−16/dh+32 misregistration, FIXED · #20/#21 held flags RESOLVED · HTML 220,405 B to 221,053 B · the 8 overlay-wrapper imgs now carry the locked style, overlay-wrapper imgs 18/18 with margin:0 · em-dash 62 held · QA Status bullet below + `lesson_07_browser_qa_progress.md`)
**Next chat:** standing browser-QA queue · L05 / L06 browser-QA OR L11 1024-width sweep (Ray's call)

---

## SVG-vs-PNG split (locked 2026-05-26)

5 SVG / 25 PNG.

| # | File | Type | Prefix | Rationale |
|---|---|---|---|---|
| 1 | `lesson_07_01_learning_progression_diagram.svg` | SVG | `lp` | Flowchart with arrows + icons + labels; locked diagram precedent across L04 / L05 / L06 |
| 26 | `lesson_07_26_file_organization_tree.svg` | SVG | `fo` | Hierarchical tree with folder icons; locked tree-diagram pattern |
| 28 | `lesson_07_28_topology_comparison.svg` | SVG | `ef` | Paired-panel comparison with semantic-colored headers; matches L05 #18 good-vs-bad-topology + L06 #21 dd precedents. **Filename diverges from requirements doc naming-convention table** (originally listed `.png`) per locked SVG decision |
| 29 | `lesson_07_29_project_checklist_infographic.svg` | SVG | `pc` | Structured-layout infographic |
| 30 | `lesson_07_30_keyboard_shortcuts_reference.svg` | SVG | `ks` | Reference card; matches L04 #14 / L06 #44 sr filled-orange-badge precedent |

Other 25 figures: PNG. Requirements doc recommended only 4 SVGs (#1, #26, #29, #30); #28 added per the locked paired-panel-comparison pattern.

---

## Phase plan (locked 2026-05-26)

6 phases · 4 / 3 / 5 / 4 / 7 / 7. Section-aligned.

| Phase | Section(s) | Figures (placement order) | Count | Sub-chat split | INTEGRATION plan-lock as own chat |
|---|---|---|---|---|---|
| 1 | `#why-coffee-mug` + `#creating-body` | #1 SVG `lp` · #2 · #3 · #4 | 4 | no | no |
| 2 | `#hollowing-interior` | #5 · #6 · #7 | 3 | no | no |
| 3 | `#adding-handle` | #8 · #9 · #10 · #11 · #12 | 5 | yes (5 = threshold) | no |
| 4 | `#refining-shape` | #13 · #14 · #15 · #16 | 4 | no | no |
| 5 | `#final-details` | #17 · #18 · #19 · #20 · #21 · #22 · #23 | 7 | yes | yes (7 ≥ 6) |
| 6 | `#good-practices` + `#project` | #26 SVG `fo` · #27 · #28 SVG `ef` · #29 SVG `pc` · #24 · #25 · #30 SVG `ks` | 7 | yes | yes (7 ≥ 6) |

Per-figure section placement comes from the requirements doc "Image Placement Suggestions in HTML" section. Phase 6 figure order is the placement-order across `#good-practices` then `#project`, not the numeric file order.

---

## ID prefix registry (locked 2026-05-26)

**SVGs (5):** `lp` (#1) · `fo` (#26) · `ef` (#28) · `pc` (#29) · `ks` (#30)

**PNG overlay namespaces reserved (18):** `c5` · `c6` · `c8` · `c10` · `c11` · `c12` · `c13` · `c14` · `c15` · `c17` · `c18` · `c19` · `c20` · `c21` · `c22` · `c24` · `c25` · `c27`

Conservative reservation: any PNG where the requirements doc says "annotated," "labels," "callout," or multi-panel with panel labels gets a `c{N}` slot. Exact overlay variant per figure (full-callout · multi-callout · composite-overlay · decorative-shape-only) decided at each INTEGRATION plan-lock per L06 cadence.

**PNGs without overlays (7):** #2 · #3 · #4 · #7 · #9 · #16 · #23

No collisions with prior lessons since each lesson HTML is its own page scope.

---

## Placement table

Four-state legend: `not produced; not integrated` · `produced; not integrated` · `produced; handoff_written; not integrated` · `integrated`

| # | File | Phase | Section | Overlay | Status |
|---|---|---|---|---|---|
| 1 | `lesson_07_01_learning_progression_diagram.svg` | 1 | `#why-coffee-mug` | SVG `lp` | integrated |
| 2 | `lesson_07_02_initial_cylinder_setup.png` | 1 | `#creating-body` | — | integrated |
| 3 | `lesson_07_03_cylinder_loop_cuts.png` | 1 | `#creating-body` | — | integrated |
| 4 | `lesson_07_04_top_face_removed.png` | 1 | `#creating-body` | — | integrated |
| 5 | `lesson_07_05_inset_rim_operation.png` | 2 | `#hollowing-interior` | `c5` | integrated |
| 6 | `lesson_07_06_interior_extrusion_progress.png` | 2 | `#hollowing-interior` | `c6` | integrated |
| 7 | `lesson_07_07_interior_xray_view.png` | 2 | `#hollowing-interior` | — | integrated |
| 8 | `lesson_07_08_handle_attachment_points.png` | 3 | `#adding-handle` | `c8` | integrated |
| 9 | `lesson_07_09_handle_stubs.png` | 3 | `#adding-handle` | — | integrated |
| 10 | `lesson_07_10_handle_arc_progression.png` | 3 | `#adding-handle` | `c10` | integrated |
| 11 | `lesson_07_11_handle_bridge_connection.png` | 3 | `#adding-handle` | `c11` | integrated |
| 12 | `lesson_07_12_complete_handle_multiview.png` | 3 | `#adding-handle` | `c12` | integrated |
| 13 | `lesson_07_13_body_taper_comparison.png` | 4 | `#refining-shape` | `c13` | integrated |
| 14 | `lesson_07_14_rim_bevel_detail.png` | 4 | `#refining-shape` | `c14` | integrated |
| 15 | `lesson_07_15_proportional_editing_viz.png` | 4 | `#refining-shape` | `c15` | integrated |
| 16 | `lesson_07_16_base_foot_detail.png` | 4 | `#refining-shape` | — | integrated |
| 17 | `lesson_07_17_shading_comparison.png` | 5 | `#final-details` | `c17` reserved | integrated |
| 18 | `lesson_07_18_auto_smooth_panel.png` | 5 | `#final-details` | `c18` reserved | integrated |
| 19 | `lesson_07_19_sharp_edge_marking.png` | 5 | `#final-details` | `c19` reserved | integrated |
| 20 | `lesson_07_20_merge_by_distance.png` | 5 | `#final-details` | `c20` reserved | integrated |
| 21 | `lesson_07_21_nonmanifold_check.png` | 5 | `#final-details` | `c21` reserved | integrated |
| 22 | `lesson_07_22_origin_point_placement.png` | 5 | `#final-details` | `c22` reserved | integrated |
| 23 | `lesson_07_23_finished_mug_render.jpg` | 5 | `#final-details` | — | integrated |
| 24 | `lesson_07_24_mug_variations.png` | 6 | `#project` | `c24` | integrated |
| 25 | `lesson_07_25_handle_troubleshooting.png` | 6 | `#project` | `c25` | integrated |
| 26 | `lesson_07_26_file_organization_tree.svg` | 6 | `#good-practices` | SVG `fo` | integrated |
| 27 | `lesson_07_27_scale_reference.png` | 6 | `#good-practices` | `c27` | integrated |
| 28 | `lesson_07_28_topology_comparison.svg` | 6 | `#good-practices` | SVG `ef` | integrated |
| 29 | `lesson_07_29_project_checklist_infographic.svg` | 6 | `#project` | SVG `pc` | integrated |
| 30 | `lesson_07_30_keyboard_shortcuts_reference.svg` | 6 | `#project` | SVG `ks` | integrated |

---

## Locked rules

Full discussion in `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_06_completion_handoff.md`. Carrying forward unchanged into L07. Highlights:

**Path and edit conventions:**
- WSL `\\wsl$\Ubuntu\` paths only (never `\\wsl.localhost\`)
- `Filesystem:edit_file` `dryRun=true` first → commit `dryRun=false` → verify with `Filesystem:get_file_info` per edit
- `Filesystem:write_file` for new files and small full-doc overwrites; NEVER Claude's built-in `create_file`
- `tool_search` for deferred tools; never inherit "tool X is unavailable" assumptions from a compaction summary
- No new em-dashes anywhere in new content; middle-dot (`·`) for separation; pre-existing en-dashes and em-dashes in source HTML preserved verbatim through edit anchors; structural em-dash after a date in Status bullets grandfathered as list-item separator

**BlenderMCP rules (Blender 5.1.1):**
- No `bpy.ops.wm.read_factory_settings(use_empty=False)` calls anywhere
- BLENDER_EEVEE_NEXT → BLENDER_EEVEE fallback
- Op-name corrections: `mesh.inset` not `mesh.inset_faces`; `mesh.delete(type='FACE')` singular not `'FACES'`; `bmesh.ops.bisect_plane` over `bpy.ops.mesh.bisect`; `bmesh.ops.subdivide_edges(cuts=N)` for equal-spaced cuts only
- 10 L06 Phase 5 / 6 PRODUCTION discoveries: Screw modifier `transform_apply(rotation=True)` before add; `mesh.subdivide` needs VIEW_3D `temp_override`; multi-object Edit Mode breaks `knife_project` (use single-object edit + `select_set` for cutter); `plane.show_wire=True` for knife_project SOLID-shading visibility; `view3d.view_axis` operator over direct `region_data.view_rotation` assignment; knife modal A/C/Z bindings (5.1.1); Alt+Z X-ray ON before Cut Through; `subdivide(number_cuts=N)` is quadratic not exponential ((N+1)² faces); `bpy.ops.screen.screenshot` silent-truncates to 0 bytes on WSL UNC paths (capture to local Windows tempdir, Pillow-relay to WSL); bevel modal readout splits top-floating-text + bottom-hints-bar (not consolidated)
- Modal-status-bar-as-truth criterion for manual modal captures
- Win+Shift+S overlay-freezes-screen popover capture path (Windows 11)
- Pillow 12.2.0 at `C:\Users\pract\AppData\Roaming\Blender Foundation\Blender\5.1\scripts\addons\modules\PIL` for composites, dimension measurement, and pre-integration crop+resize
- Save Win+Shift+S snips direct to `\\wsl$\Ubuntu\...\images\` (skip Claude UI upload roundtrip)

**Image patterns:**
- Inline-stacked PNG overlay pattern with four variants: full-callout (ellipse + leader + marker + badge), multi-callout (2 / 3 / 4 callouts; pattern is count-agnostic), composite-overlay (centered bottom-of-panel rect+text on multi-panel composites, no highlight; geometry reusable across same-dimension composites), decorative-shape-only (shape itself carries message, no ellipse / leader / marker / badge)
- Warning-vs-positive overlay colorway split: `#E63946` warning + `#ffffff` text · `#4ADE80` positive + `#0f1a14` text
- Inline SVG re-indent to 20-space content depth; strip `color="#222"` from root `<svg>` when inlining (standalone file keeps it for `<img>` fallback)
- Deeper-indent variant for figures inside cards (col 20 figure / col 24 children)
- 3-5 line `oldText` anchors for `Filesystem:edit_file` per figure (extend to 6 lines if shorter anchors hit duplicate matches)
- Bottom-up execution order within each sub-chat so earlier inserts do not shift later anchors

**Workflow:**
- Cross-sub-chat composite pattern (named `_temp_*` handoff; sub-chat B opens with `Filesystem:get_file_info` pre-flight on the temps)
- Resume-chat workflow precedent (handoff doc at interruption-time drives resume; sub-chat envelope stays single unless section boundary requires split)
- MCP server hang recovery: 4-min timeout boundary → `Filesystem:get_file_info` size check before retry
- Pillow crop+resize pre-integration step for oversize manual captures (3838x2160 → 1920x1080 nominal; precedents: L04 #45, L06 #39)
- Pre-emptively plan sub-chat splits for any phase with 5+ figures; pre-emptively split INTEGRATION planning into its own chat for any phase with 6+ figures
- Warn Ray before context tightens rather than letting automatic compaction happen
- Propose plan + anchor choices + grouping decisions before integration; lock plan in writing first, then edit

**Brand palette (unchanged from L05 close):** `#FF6B00` Blender orange · `#4A9EFF` blue · `#B47EE8` purple · `#FF7EA8` pink · `#4ADE80` green · `#F5C242` yellow · `#E63946` X-axis red · `#0f1a14` near-black · `#ffffff` white.

**Style guide canonical:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\images\IMAGE_STYLE_GUIDE.md` v5 (49,085 B). v6 revision deferred; not blocking L07.

---

## Status

- **2026-05-26** · Planning chat closed. Section IDs in `lesson_07_image_requirements.md` confirmed to match `lesson_07_modeling_your_first_object.html` (all 8: `#why-coffee-mug`, `#creating-body`, `#hollowing-interior`, `#adding-handle`, `#refining-shape`, `#final-details`, `#good-practices`, `#project`). SVG-vs-PNG split locked (5 SVG · 25 PNG; #28 flipped PNG → SVG vs requirements doc naming-convention table per locked paired-panel-comparison pattern). Phase plan locked (6 phases at 4 / 3 / 5 / 4 / 7 / 7). ID prefix registry locked (5 SVG prefixes: `lp` #1, `fo` #26, `ef` #28, `pc` #29, `ks` #30; 18 PNG overlay namespaces reserved: `c5`, `c6`, `c8`, `c10`, `c11`, `c12`, `c13`, `c14`, `c15`, `c17`, `c18`, `c19`, `c20`, `c21`, `c22`, `c24`, `c25`, `c27`; 7 PNGs without overlays: #2, #3, #4, #7, #9, #16, #23). Next chat: Phase 1 PRODUCTION.
- **2026-05-26** · Phase 1 PRODUCTION closed. 4 of 4 figures produced for `#why-coffee-mug` + `#creating-body`. **#1** `lesson_07_01_learning_progression_diagram.svg` (3,852 B; SVG `lp`; hub-and-spoke radial flowchart, 5 spokes at 72° spacing, coffee-mug hub stroked in brand orange `#FF6B00`, spoke icons in palette purple / blue / yellow / green / pink for vase / bowl / bucket / water bottle / stool, arrow labels `Hollow interior` / `Curved surfaces` / `Handle creation` / `Tapered profile` / `Smooth shading`; canvas 800x560; standalone file keeps `color="#222"` on root for `<img>` fallback per locked inline pattern). **#2** `lesson_07_02_initial_cylinder_setup.png` (1,462,282 B; BlenderMCP scripted scene build, default UV cylinder at `location=(0,0,1)` so base rests on grid floor, front orthographic, solid shading, 3D cursor visible at world origin, no other objects, no selection outline). **#3** `lesson_07_03_cylinder_loop_cuts.png` (1,807,489 B; BlenderMCP Edit Mode + `bmesh.ops.subdivide_edges(cuts=4)` on the 32 vertical side edges, all geometry selected, three-quarter perspective at 30° azimuth + 15° elevation). **#4** `lesson_07_04_top_face_removed.png` (1,796,278 B; BlenderMCP top-face delete via `bpy.ops.mesh.delete(type='FACE')` singular per locked L05 rule, angled top-down perspective at 30° azimuth + 60° elevation, hollow interior with intact bottom face visible at base of cavity, loop cuts preserved on side walls). Engine BLENDER_EEVEE (BLENDER_EEVEE_NEXT fallback per locked rule). All 4 figures rendered at 1920x1080. Locked alt text + figcaption per figure captured in the PRODUCTION proposal exchange. **NEW L07 PRODUCTION discovery (1):** `bpy.ops.wm.redraw_timer(type='DRAW_WIN_SWAP', iterations=1)` is the reliable matrix-sync path after `region_3d.view_perspective` enum changes; `tag_redraw()` alone and `view_layer.update()` alone are insufficient · the cached `is_perspective` flag and `perspective_matrix` only sync after a real swap-redraw, which the downstream `render.opengl(view_context=True)` reader needs. Extends L06 P5 sub-chat A finding #5 (direct `region_data.view_rotation` assignment same desync mechanism). Locked rule for L07 carry-forward; complements `view3d.view_axis` and `view3d.view_orbit` operators by covering the persp/ortho enum-set case those operators don't reach. **PNG file-size observation (non-blocking):** #2 / #3 / #4 came in at 1.4 - 1.8 MB each, larger than the typical L06 single-pose viewport band of 500-800 KB. Likely cause: visible grid floor + Edit Mode wireframe overlay across most of the frame. Optional Pillow recompress deferred; total Phase 1 PNG payload 5.1 MB, well within the lesson budget. Placement-table rows #1 / #2 / #3 / #4 flipped to `produced; not integrated`. Next chat: Phase 1 INTEGRATION.
- **2026-05-26** · Phase 1 INTEGRATION closed. 4 of 4 Phase 1 figures integrated into `lesson_07_modeling_your_first_object.html` (`#why-coffee-mug` + `#creating-body`). HTML size 117,481 B → 124,924 B (+7,443 B / +7.3 KB across 4 figures). Per-figure deltas: **#4** plain PNG between "Removing the Top Face" card `</div>` and "Perfect! Now you have an opening at the top" para (+604 B); **#3** plain PNG between "Adding Edge Loops" card `</div>` and "🎯 Why add loop cuts?" blockquote (+597 B); **#2** plain PNG between "Adding the Cylinder" card `</div>` and "💡 Why this location?" blockquote, pre-existing em-dash in blockquote preserved verbatim through exact-match anchor (+607 B); **#1** inline SVG `lp` between the "A coffee mug combines several modeling challenges" para and the "What Makes a Coffee Mug Ideal" card, `color="#222"` stripped from root, all 8 structural comments preserved (+5,635 B). Bottom-up execution order #4 → #3 → #2 → #1 held; no anchor shifts. Dry-run-then-commit-then-verify per figure. All 4 placement-table rows flipped to `integrated`. **NEW L07 INTEGRATION discovery (1):** L06's locked inline-SVG indent pattern (figure col 16 / svg col 20 / children col 22-24 / deeper 28/32) is section-content-depth-specific to L06. L07 sections nest one level deeper (section content at col 20 vs L06's col 16) so the pattern shifts +4 across the board (figure col 20 / svg col 24 / children col 26 / nested col 28 / deepest col 30). The principle is "figure at sibling-content depth, +2 per nesting level inside svg"; the L06-specific column numbers are derivative. Future lessons should derive depths from each file's section-content depth rather than treating the L06 numbers as universal. Next chat: Phase 2 PRODUCTION (`#hollowing-interior`; 3 figures · #5 / #6 / #7).
- **2026-05-26** · Phase 2 PRODUCTION closed. 3 of 3 figures produced for `#hollowing-interior`. **#5** `lesson_07_05_inset_rim_operation.png` (546,955 B; 3844×720; 3-panel composite at top-down orthographic view; selected outer rim BEFORE · partial inset PREVIEW with new inner edge loop · completed inset AFTER showing two concentric edge loops at the rim; Pillow composite with `#FF6B00` 2-px dividers; engine BLENDER_EEVEE). **Alt:** "3-panel composite showing the inset operation creating mug rim thickness. Panel 1: top-down orthographic view of the cylinder with its top rim edge loop selected in orange. Panel 2: same view with a partial inset preview showing the new inner edge loop. Panel 3: same view with the completed inset producing two concentric edge loops at the rim." **Figcaption:** "Inset operation progression for creating rim thickness · selected edge loop · inset preview · completed rim with two concentric edge loops". **#6** `lesson_07_06_interior_extrusion_progress.png` (1,213,797 B; 3844×720; 3-panel composite at angled side perspective with `space.shading.show_xray = True` and `xray_alpha = 0.5`; inner rim edge loop selected at local z=+1 RIM COMPLETE · inner ring extruded down to local z=0 MID-EXTRUSION · inner ring extruded down to local z=-0.85 INTERIOR COMPLETE; Pillow composite with `#FF6B00` 2-px dividers; engine BLENDER_EEVEE). Initial render rejected (no X-ray, all 3 panels read identically because the exterior wall occluded the inner ring); re-rendered this chat with X-ray ON and EDIT → OBJECT → EDIT cache flush between consecutive panel renders. **Alt:** "3-panel composite showing interior wall creation by extruding the rim downward. Panel 1: angled side view of the inset-complete mug with the inner rim edge loop selected in orange. Panel 2: same view mid-extrusion with the inner surface partially extruded downward. Panel 3: same view with the interior walls fully extruded to near the cylinder base." **Figcaption:** "Interior wall creation by progressive downward extrusion of the inset rim · rim complete · mid-extrusion · interior complete". **#7** `lesson_07_07_interior_xray_view.png` (1,415,689 B; 1920×1080; single-pose angled perspective with X-ray transparency enabled at `space.shading.show_xray = True` and `xray_alpha = 0.4`; exterior and interior surfaces both visible through the semi-transparent shading; interior bottom cap visible inside the cavity; no selection; engine BLENDER_EEVEE). **Alt:** "Angled perspective view of the completed hollow mug interior with X-Ray transparency enabled. Both exterior and interior surfaces are visible through the semi-transparent shading; the interior bottom cap is visible inside the cavity." **Figcaption:** "Completed hollow interior shown through X-Ray transparency · exterior surface · interior walls · capped interior bottom". **NEW L07 P2 PRODUCTION discovery (1):** OpenGL viewport draw cache holds stale geometry across vert-position-only bmesh edits within a single Edit Mode session; force EDIT → OBJECT → EDIT toggle between consecutive renders to flush. Complements L07 P1 `DRAW_WIN_SWAP` matrix-sync discovery: P1 covers persp/ortho enum-set matrix cache after `region_3d.view_perspective` changes; P2 covers vert-position-only bmesh edit draw cache within a single Edit Mode session. Together the two L07 PRODUCTION discoveries cover the two distinct viewport-cache desync mechanisms encountered when scripting multi-panel renders that vary either view or geometry between panels. Locked rule for L07 carry-forward. Placement-table rows #5 / #6 / #7 flipped to `produced; not integrated`. Next chat: Phase 3 PRODUCTION (`#adding-handle`; 5 figures · #8 / #9 / #10 / #11 / #12; sub-chat split required per ≥5 threshold).
- **2026-05-26** · Phase 3 PRODUCTION sub-chat A closed (Phase 3 PRODUCTION sub-chat A complete · #8 / #9 / #10 produced for `#adding-handle`; this close-out Status bullet was deferred at sub-chat A close per OPEN ITEM in `session.md` `## L07 P3 sub-chat A close` and back-filled here at Phase 3 INTEGRATION kickoff per path (a)). 3 of 3 sub-chat A figures produced. **#8** `lesson_07_08_handle_attachment_points.png` (1,532,099 B; single-pose; engine BLENDER_EEVEE; side orthographic view of the coffee mug in Edit Mode with two body-wall faces selected in orange marking the upper and lower handle attachment points, vertically aligned along the body wall; `c8` overlay reserved for INTEGRATION-time callouts). **Alt:** "Side orthographic view of the coffee mug in Edit Mode with two faces selected in orange on the body wall. The two selected faces are vertically aligned to mark the upper and lower handle attachment points." **Figcaption:** "Two faces selected on the mug body wall to mark handle attachment points · upper attachment face · lower attachment face". **#9** `lesson_07_09_handle_stubs.png` (1,537,715 B; single-pose; engine BLENDER_EEVEE; angled side perspective of the coffee mug in Edit Mode with two short stub extrusions protruding outward from the body wall at the previously selected attachment faces; the two stub outer faces selected in orange; stubs vertically aligned and matched in size; no overlay reserved). **Alt:** "Angled side perspective of the coffee mug in Edit Mode with two short stub extrusions protruding outward from the body wall at the previously selected attachment faces. The two stub outer faces are selected in orange. The stubs are vertically aligned and matched in size, ready to anchor the top and bottom of the handle arc." **Figcaption:** "Initial handle stubs extruded from the two attachment faces · upper stub · lower stub · arc construction begins next". **#10** `lesson_07_10_handle_arc_progression.png` (504,611 B; 5126×720 4-panel composite; engine BLENDER_EEVEE; per-panel resolution 1280×720; viewport FRONT persp +8° elevation, dist 6.0, MEDIAN_POINT pivot; per-segment recipe `bmesh.ops.extrude_face_region` on selected tip face → translate new verts along pre-rotation tip normal × 0.28 → rotate +30° around global Y at new-vert median (pre-rotation median = post-rotation median) → EDIT → OBJECT → EDIT cache flush + `DRAW_WIN_SWAP` redraw before render; Pillow composite with `#FF6B00` 2-px dividers between panels via Pillow at `C:\Users\pract\AppData\Roaming\Blender Foundation\Blender\5.1\scripts\addons\modules\PIL` per locked rule; final scene state after segment 4: 252 faces; scene custom prop `L07_arc_tip_idx` = 247; selected tip face verts [276, 277, 278, 279]; final tip median (1.8985, -0.2318, -0.2993); 120° total arc rotation from segment-1 stub direction; `c10` overlay reserved for INTEGRATION-time composite-overlay panel labels). **Alt:** "4-panel composite showing the handle arc being built segment by segment from the upper stub. Panel 1: first segment extruded outward from the upper stub and rotated 30° at the new-vert median. Panel 2: second segment continues the arc with another 30° rotation. Panel 3: third segment continues the arc with another 30° rotation. Panel 4: fourth segment brings the arc nearly to vertical at 120° total rotation. Each panel shows the active tip face selected in orange after the segment's extrude-translate-rotate operation. Front perspective view consistent across all four panels." **Figcaption:** "Handle arc constructed by repeating the extrude-translate-rotate pattern · 30° per segment · 120° total arc rotation across four segments". **NEW L07 P3 sub-chat A observation (1, locked observation-only):** Cached BMFace `.normal` attributes report the pre-rotation face normal even after the vert positions have been rotated around the face median. `bmesh.update_edit_mesh(me)` does not invalidate the cache; only a fresh `bm = bmesh.from_edit_mesh(me)` or an explicit `bm.normal_update()` returns the post-rotation normal. Geometry on disk and at render is correct; only the in-memory cached `.normal` of an already-handled BMFace is stale. Complements L07 P1 PRODUCTION matrix-sync discovery (persp/ortho enum-set matrix cache after `region_3d.view_perspective` changes) and L07 P2 PRODUCTION viewport-cache-flush discovery (OpenGL viewport draw cache across vert-position-only bmesh edits) as the third distinct post-edit cache desync mechanism: BMFace `.normal` attribute cache across in-place vert rotations within a single bmesh handle. Locked observation-only; promote to formal carry-forward rule only if it surfaces a real production defect; so far it has only mis-reported a printout, with renders themselves correct because they read vert positions, not cached normals. Placement-table rows #8 / #9 / #10 were flipped to `produced; not integrated` at sub-chat A close (recorded in `session.md` `## L07 P3 sub-chat A close`); header line **Current state** was updated to `Phase 3 PRODUCTION sub-chat A complete · 10 of 30 produced · 4 of 30 integrated` and then re-updated at sub-chat B close to `Phase 3 PRODUCTION complete · 12 of 30 produced · 4 of 30 integrated`. Next chat at sub-chat A close: Phase 3 PRODUCTION sub-chat B (`#adding-handle`; 2 figures · #11 / #12).
- **2026-05-26** · Phase 3 PRODUCTION sub-chat B closed (Phase 3 PRODUCTION complete · #11 / #12 produced for `#adding-handle`; sub-chat A Status bullet for #8 / #9 / #10 remains pending per OPEN ITEM in `session.md`, to be resolved at Phase 3 INTEGRATION kickoff). 2 of 2 sub-chat B figures produced. **#11** `lesson_07_11_handle_bridge_connection.png` (1,448,734 B; 1920×1080; engine BLENDER_EEVEE; 2 more arc segments produced at the locked recipe from sub-chat A's #10 (252 → 262 faces; segment 5 tip face index 252; segment 6 tip face index 257, median (+1.5178, -0.2867, -0.6799), normal (-0.9952, -0.0980, 0.0000); 180° total arc rotation from segment-1 stub direction); manual tip-vert translate delta (-0.1294, +0.1500, +0.3199) applied to land the arc terminal edge loop with a 0.1500-unit gap above the bottom stub face 223; Edge mode with 8 edges selected [480-483, 536-539] covering the arc terminal edge loop and the bottom stub outer edge loop together; view RIGHT + ORBITLEFT 25° + ORBITUP 12°, view_location (1.0, -0.05, 1.1), view_distance 4.0). **Alt:** "Angled side perspective of the coffee mug in Edit Mode with the handle arc extended to six segments reaching near the bottom stub. The arc terminal edge loop and the bottom stub outer edge loop are both selected in orange, ready to be bridged. A small visible gap remains between the two selected edge loops." **Figcaption:** "Edge loops selected for bridging the handle arc to the bottom stub · arc terminal edge loop · bottom stub outer edge loop · ready to bridge with F". **#12** `lesson_07_12_complete_handle_multiview.png` (826,933 B; 3844×720; 3-panel composite at orthographic views; `bmesh.ops.bridge_edge_loops` returned {FINISHED} consuming the two selected edge loops to add 4 quads and 2 cap faces (262 → 264 faces; 288 verts; the 2 caps consumed in the bridge); Object Mode `shade_smooth` applied; 3 ortho panels each 1280×720 rendered with `view_selected`, floor on, axes and cursor off; Pillow composite with `#FF6B00` 2-px dividers → 3844×720; engine BLENDER_EEVEE). **#12 panel-order discovery:** composite panel order is Blender FRONT → RIGHT → TOP, which corresponds to printed panel labels Side Profile / Front View / Top View. The Blender FRONT orthographic view shows the classic arc silhouette (handle arc curving smoothly from top attachment to bottom attachment); the Blender RIGHT orthographic view shows the handle face-on protruding from the right side with the body behind. The "right orthographic" / "front orthographic" wording in the locked Alt string reflects the printed panel labels, not the Blender source view names; user-confirmed at proof. **Alt:** "3-panel composite showing the completed coffee mug with bridged handle from three orthographic views. Panel 1: right orthographic side profile showing the classic mug-and-handle silhouette with the handle arc curving smoothly from the top attachment to the bottom attachment. Panel 2: front orthographic view showing the handle protruding from the right side of the mug body with proper depth. Panel 3: top orthographic bird's-eye view showing the handle curvature and the two attachment points on the mug body." **Figcaption:** "Completed handle shown from three orthographic views after bridging and smooth shading · side profile · front view · top view". Final scene state after #12 render: 264 faces, 288 verts, scene custom prop `L07_arc_tip_idx` = 257. Placement-table rows #11 / #12 flipped to `produced; not integrated`. Next chat: Phase 3 INTEGRATION (`#adding-handle`; 5 figures · #8 / #9 / #10 / #11 / #12; single sub-chat).
- **2026-05-26** · Phase 3 INTEGRATION closed. 5 of 5 Phase 3 figures integrated into `lesson_07_modeling_your_first_object.html` (`#adding-handle`). HTML size 124,924 B → 136,424 B (+11,500 B / +11.2 KB across 5 figures). Per-figure deltas (bottom-up execution order): **#12** composite-overlay 3-panel positive-green between "✅ Handle Complete!" celebration card `</div>` and `</section>`; panel labels "Side profile" / "Front view" / "Top view"; viewBox 3844×720; font-size 44 scaled from style-guide baseline 22 (+2,489 B). **#11** multi-callout 2 positive-green between "📝 Bridging the Gap" instruction card `</div>` and "⚠️ Common Handle Issues" warning card; callouts "Arc terminal edge loop" (upper) / "Bottom stub outer edge loop" (lower); badges in top clear strip y=20-80; viewBox 1920×1080; font-size 22 (+2,749 B). **#10** composite-overlay 4-panel positive-green between "📝 Creating the Handle Curve (Bottom Part)" card `</div>` and `<h3>Connecting the Handle</h3>`; panel labels "Segment 1" through "Segment 4" (plain numerics chosen over cumulative-angle annotations because body text specifies 15°-per-segment while figure was produced at 30°-per-segment; plain labels accurate to both); viewBox 5126×720; font-size 60 scaled from baseline (+2,931 B). **#9** plain PNG no overlay between "📝 Creating Handle Stubs" card `</div>` and "Good! Now you have two little protrusions" para (+707 B). **#8** multi-callout 2 positive-green between "📝 Selecting Handle Attachment Points" card `</div>` and "⚠️ Handle Placement Tips" warning card; callouts "Top attachment face" (upper) / "Bottom attachment face" (lower); badges on right side at x=1480, y=280 and y=560; viewBox 1920×1080; font-size 22 (+2,624 B). Bottom-up execution order #12 → #11 → #10 → #9 → #8 held; no anchor shifts. Dry-run-then-commit-then-verify per figure. All 5 placement-table rows flipped to `integrated`. **NEW L07 P3 INTEGRATION discoveries (2):** (1) **Composite-overlay font-size scales linearly with viewBox width from style-guide baseline 22 at 1920 wide.** For high-resolution multi-panel composites the locked composite-overlay variant's badge font must grow proportionally to maintain ~10pt effective rendered size at typical HTML display width ~900px. Formula: `fs_composite = round(22 × viewBox_width / 1920)`. Verified at L07 #10 (5126 wide → fs 60) and #12 (3844 wide → fs 44). Locked rule for L07 carry-forward; recommended for IMAGE_STYLE_GUIDE.md v6. (2) **Composite-overlay colorway is NOT neutral; the positive-vs-warning split applies to all four overlay variants.** Proposal-time misread of style guide v5 corrected at INTEGRATION; L05 #29 SELECT CAP/INSET/EXTRUDE and #30 CUP/CHAIR/BUST precedents confirm composite-overlay panel labels default to positive-green `#4ADE80` rect + `#0f1a14` text. Locked rule for L07 carry-forward; style guide v5 wording already covers it, just clearer here. **Advisory observation (non-blocking):** locked #8 Alt says "Side orthographic view" but the actual rendered PNG shows mild perspective view of an early-state cylinder (no rim/inset from Phase 2), suggesting production used a fresh cylinder scene for visual focus on attachment-point selection. Locked Alt preserved verbatim through INTEGRATION; flagged for optional QA-pass revision to "Side angled-perspective view" if exact image-to-Alt match desired. Does not affect figure correctness or learner understanding. Next chat: Phase 4 PRODUCTION (`#refining-shape`; 4 figures · #13 / #14 / #15 / #16).
- **2026-05-26** · Phase 4 PRODUCTION closed. 4 of 4 figures produced for `#refining-shape`. Production order #13 → #14 → #16 → #15 (placement-order reordered so #15's proportional-edit vert distortion lands last and does not carry forward into #14 / #16). Engine BLENDER_EEVEE throughout (BLENDER_EEVEE_NEXT fallback per locked rule). Each render preceded by EDIT → OBJECT → EDIT cache flush + `DRAW_WIN_SWAP` redraw per locked L07 P1 / P2 discoveries. Scene-state chain: post-Phase-3 mug (264 faces, 288 verts) → taper (288 verts, top rim verts at z=1.0 scaled 1.1x at MEDIAN_POINT, outer r 1.0→1.1 / inner r 0.849→0.934, intermediate body loop cuts unchanged) → bevel (352 verts, 328 faces) → foot (416 verts, 393 faces) → proportional-edit simulation (416 verts unchanged; 22 verts displaced +z by smoothstep falloff). **#13** `lesson_07_13_body_taper_comparison.png` (888,049 B; 3842×900; 2-panel FRONT-orthographic composite; per-panel 1920×900; BEFORE panel = current cylindrical body; AFTER panel = 64 rim verts at local z=1.0 selected and scaled 1.1x at MEDIAN_POINT (median computed at world (0,0,1) center of selected ring); Pillow composite with `#FF6B00` 2-px divider at column 1920; both panels keep handle for reference per requirements doc; `c13` composite-overlay reserved for INTEGRATION-time panel labels with positive-green default colorway per locked L07 P3 INTEGRATION clarification). **Alt:** "2-panel side orthographic composite comparing the mug body before and after applying a subtle taper. Panel 1: cylindrical body with uniform diameter from top to bottom, handle visible for reference. Panel 2: same mug with the top edge loop scaled outward by 1.1x at MEDIAN_POINT pivot, producing a slight outward flare at the rim, handle still visible for reference." **Figcaption:** "Body taper comparison · cylindrical body before taper · 1.1x scaled top edge loop producing rim flare after taper · handle present in both panels for reference". **#14** `lesson_07_14_rim_bevel_detail.png` (920,276 B; 1920×1080; single-pose angled perspective = FRONT persp + ORBITLEFT 25° + ORBITUP 15°, view_distance 5.0, view_location (0,0,1); `bmesh.ops.bevel` on the 32 top outer rim edges with offset=0.05 segments=2 profile=0.5 affect='EDGES' clamp_overlap=True (creating 64 new faces → 328 total and 64 new verts → 352 total); smooth shading reapplied across all polygons; Pillow magnified detail inset 480×360 at upper-right (margin 30 px; source crop 240×180 auto-detected via numpy brightness-threshold scan finding rim center at render pixel (959, 223) where threshold = bg_brightness+20 with bg sampled from upper-left 50×50 corner; 2x LANCZOS resize) on `#1a1a1a` 506×386 underlay with `#ffffff` 3-px border; `c14` full-callout reserved for INTEGRATION-time single ellipse + leader + label into the rim). **Alt:** "Angled perspective view of the coffee mug with a beveled top rim. A magnified detail inset in the upper-right shows the rim cross-section close-up, revealing the additional edge loops that produce the rounded profile from the bevel operation. The main image shows the complete mug body with the beveled rim and the previously applied subtle top taper." **Figcaption:** "Beveled rim detail · main view of the complete mug with beveled rim · magnified cross-section inset showing the added edge loops from the bevel operation". **#15** `lesson_07_15_proportional_editing_viz.png` (962,057 B; 1920×1080; produced last in execution order so the distortion does not affect #14 / #16; scene `tool_settings.use_proportional_edit=True`, `proportional_edit_falloff='SMOOTH'`, `proportional_size=0.5` for scene-state authenticity; selected vert idx 96 at local (1.0, 0.0, 0.6) = body-side upper-right corner of upper stub neck quad at body wall, centered on handle axis (y=0); simulated proportional edit translates selected vert +z 0.15 then translates all verts within world radius 0.5 by +z 0.15 × smoothstep(1 - d/0.5); 22 verts affected; chosen vert post-distortion at local (1.0, 0.0, 0.75) = world (1.0, 0.0, 1.75); rendered in EDIT mode with VERT select mode for orange highlight on chosen vert; view RIGHT persp + ORBITLEFT 35° + ORBITUP -10°, view_distance 4.0, view_location (0.6, 0.0, 1.4); Pillow radial gradient overlay at projected viewport position (1043.2, 403.7) with radius 124.1 px (computed from screen-space delta between projected chosen vert and projected +0.5x offset vert, scaled from region 1572×899 to render 1920×1080 with Pillow y-axis flip); gradient color `#FFEB3B` with peak alpha 153/255 (60%) at center, smoothstep falloff to 0 at radius; `c15` multi-callout 3 reserved for INTEGRATION-time "Selected vertex" / "Influence circle" / "Affected area"). **Alt:** "Angled perspective view of the coffee mug handle-body connection in Edit Mode with proportional editing active. A single vertex at the upper handle attachment is selected in orange and has been translated upward; nearby vertices show smooth falloff distortion from the simulated proportional edit. A yellow-orange gradient circle is overlaid on the viewport at the selected vertex's screen position, indicating the proportional editing influence radius." **Figcaption:** "Proportional editing influence visualization · selected vertex at handle-body connection translated upward · gradient circle indicating the influence radius · smooth falloff distortion visible in nearby geometry". **#16** `lesson_07_16_base_foot_detail.png` (1,320,681 B; 1920×1080; single-pose bottom-up angled view = BOTTOM persp + ORBITUP 60° + ORBITLEFT -20°, view_distance 5.5, view_location (0.0, 0.0, 0.4); `bmesh.ops.inset_individual` on the bottom n-gon (32 verts at local z=-1.0; thickness 0.1, use_even_offset=True) creating 32 ring quads and reshaping the original face into a smaller inner n-gon at avg_r 0.8995; `bmesh.ops.extrude_face_region` on the new inner n-gon then translate +z 0.05 raising the inner floor (+65 faces → 393 total, +64 verts → 416 total); outer ring stays at z=-1.0 forming the foot that touches the floor; inner face recessed upward to z=-0.95 visible as the recessed center from below; smooth shading reapplied; no overlay per locked placement table). **Alt:** "Bottom-up angled view of the coffee mug showing the optional base foot detail. The mug bottom features a slightly recessed inner center face surrounded by a raised outer ring forming the foot, created by inset and upward-extrude operations on the original bottom face. The height difference is subtle but visible from this angle. Grid floor visible below for spatial reference." **Figcaption:** "Optional base foot detail viewed from below · recessed inner center face · raised outer foot ring created by inset and upward extrude on the original bottom face". Final scene state after Phase 4 PRODUCTION: 393 faces, 416 verts; scene custom props `L07_arc_tip_idx`=257, `L07_15_chosen_vert`=96, `L07_15_chosen_world`=[1.0, 0.0, 1.75], `L07_15_chosen_local`=[1.0, 0.0, 0.75], `L07_15_proj_center`=[854.15, 562.96], `L07_15_proj_off`=[950.88, 594.50], `L07_15_region_dims`=[1572, 899]; scene `tool_settings.use_proportional_edit=True`, `proportional_edit_falloff='SMOOTH'`, `proportional_size=0.5`. No new L07 P4 PRODUCTION discoveries: the phase reused the three already-locked L07 viewport-cache mechanisms (P1 matrix sync via `DRAW_WIN_SWAP`, P2 EDIT-OBJECT-EDIT toggle for draw-cache flush, P3 BMFace `.normal` cache observation-only) without surfacing new desync mechanisms. Placement-table rows #13 / #14 / #15 / #16 flipped to `produced; not integrated`. Next chat: Phase 4 INTEGRATION (`#refining-shape`; 4 figures · #13 / #14 / #15 / #16; single sub-chat).
- **2026-05-28** · Phase 4 INTEGRATION closed. 4 of 4 Phase 4 figures integrated into `lesson_07_modeling_your_first_object.html` (`#refining-shape`). HTML size 136,424 B to 144,978 B (+8,554 B / +8.4 KB across 4 figures). Per-figure deltas (bottom-up execution order): **#16** plain PNG no overlay between "📝 Creating a Mug Foot" card `</div>` and the "Now your mug has a raised outer edge" para (+807 B). **#15** multi-callout 3 positive-green between "💡 Proportional Editing Explained" yellow tip card `</div>` and `<h3>Adding a Base/Foot (Optional)</h3>`; callouts "Selected vertex" (chosen vert at projected pixel (1043, 404)) / "Influence circle" (gradient perimeter section, lower-right) / "Affected area" (upper-right quadrant of the gradient); right-stacked badges at x=1500, y=80 / 180 / 280; viewBox 1920×1080; font-size 22; pre-existing em-dash in the "falloff ... how far the influence spreads" sentence preserved verbatim through the exact-match anchor (+3,723 B). **#14** full-callout single positive-green between "📝 Beveling the Rim" card `</div>` and the "🎯 Understanding Bevels" blockquote; label "Beveled rim"; ellipse on the front-center rim section (cx=820 cy=235 rx=160 ry=22); badge upper-left at x=110 y=60, clear of the upper-right magnified detail inset (x=1384 to 1890, y=30 to 416); viewBox 1920×1080; font-size 22; 6-line anchor (extended past the standard 5 to clear 5 duplicate "Click to confirm" matches via the Beveling-specific "Scroll mouse wheel up once" line) (+2,009 B). **#13** composite-overlay 2-panel positive-green between "📝 Adding a Subtle Taper" card `</div>` and the "See how that tiny change" para; panel labels "BEFORE TAPER" / "AFTER TAPER"; viewBox 3842×900; font-size 44 per `fs = round(22 × 3842 / 1920)`; pre-existing em-dash in the "boring cylinder ... it has a shape" sentence preserved verbatim through the exact-match anchor (+2,015 B). Bottom-up execution order #16 → #15 → #14 → #13 held; no anchor shifts. Dry-run-then-commit-then-verify per figure. All 4 placement-table rows flipped to `integrated`. **Overlay-convention reuse note:** all three overlay variants matched the L07 P3 INTEGRATION markup verbatim rather than the L06 baseline numbers · multi-callout / full-callout badges at rect h=60 with text at rect center via `dominant-baseline="central"`, no letter-spacing, per-callout id naming `c{N}-highlight` / `-leader` / `-marker` / `-badge` (c8 / c11 precedent); composite-overlay badges at rect 440×100 with text at rect center and font-size 44 (c12 precedent, bottom margin scaled 40 to 50 for the 900-tall vs 720-tall panel). The proposal's independently-derived #13 geometry (rect 560×84 at y=773 / text y=835) was superseded at execution by the c12-precedent geometry (rect 440×100 at y=750 / text y=800) for cross-figure consistency. **No new L07 P4 INTEGRATION discoveries:** the phase reused the locked inline-stacked overlay pattern, the indent-depth-derivation rule (section-content depth col 20 · figure col 20 / div col 24 / img + svg col 28 / callout elements col 32 / figcaption col 24), the composite-overlay font-size formula, and the positive-green-default colorway without surfacing new mechanisms. **Standing gap surfaced:** Phase 2 INTEGRATION (#5 / #6 / #7, `#hollowing-interior`) was skipped in the phase sequence (Phase 2 PRODUCTION went straight to Phase 3 PRODUCTION); those 3 figures remain `produced; not integrated`. Next chat: Phase 2 INTEGRATION (`#hollowing-interior`; 3 figures · #5 / #6 / #7) to clear the oldest pending batch, OR Phase 5 PRODUCTION (`#final-details`; 7 figures · #17 / #18 / #19 / #20 / #21 / #22 / #23; sub-chat split required per ≥5 threshold; INTEGRATION plan-lock as own chat per ≥6 threshold) to continue the numeric sequence · Ray's call.
- **2026-05-28** · Phase 2 INTEGRATION closed (the skipped-phase gap). 3 of 3 Phase 2 figures integrated into `lesson_07_modeling_your_first_object.html` (`#hollowing-interior`). HTML size 144,978 B to 150,281 B (+5,303 B / +5.2 KB across 3 figures). Per-figure deltas (bottom-up execution order): **#7** plain PNG no overlay between "📝 Closing the Interior Bottom" card `</div>` and the "✅ What You've Accomplished" card (+624 B). **#6** composite-overlay 3-panel positive-green between "📝 Creating the Interior Surface" card `</div>` and the "🎯 Pro Tip" blockquote; panel labels "Rim complete" / "Mid-extrusion" / "Interior complete"; viewBox 3844×720; font-size 44 per `fs = round(22 × 3844 / 1920)`; c12-precedent geometry (rect 440×100 at y=580 / text y=630 / centers 640·1922·3204); pre-existing em-dash in the Pro Tip "interior bottom—it looks more realistic" sentence preserved verbatim through the exact-match anchor (+2,338 B). **#5** composite-overlay 3-panel positive-green between "📝 Step-by-Step: Insetting the Top" card `</div>` and the "Look at what you've created!" para; panel labels "Selected edge loop" / "Inset preview" / "Completed rim"; viewBox 3844×720; font-size 44; c12-precedent geometry (+2,341 B). Bottom-up execution order #7 → #6 → #5 held; no anchor shifts. Dry-run-then-commit-then-verify per figure. All 3 placement-table rows flipped to `integrated`. **No new L07 P2 INTEGRATION discoveries:** reused the locked inline-stacked overlay pattern, the indent-depth-derivation rule (figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24), the composite-overlay font-size formula, and the positive-green-default colorway without surfacing new mechanisms. **Skipped-phase gap now closed:** Phases 1 to 4 are all fully integrated; remaining work is Phase 5 (`#final-details`, #17 to #23) and Phase 6 (`#good-practices` + `#project`, 7 figures). Next chat: Phase 5 PRODUCTION (`#final-details`; 7 figures · #17 / #18 / #19 / #20 / #21 / #22 / #23; sub-chat split required per ≥5 threshold; INTEGRATION plan-lock as own chat per ≥6 threshold).
- **2026-05-28** · Phase 5 PRODUCTION sub-chat A closed (capture-method split locked: sub-chat A = render-path figures #17 / #22 / #23; sub-chat B = UI-chrome menu/panel captures #18 / #19 / #20 / #21, next chat). 3 of 3 sub-chat A figures produced for `#final-details`. **Clean finished-mug base re-established:** no `.blend` checkpoint existed, so the Phase-4-close #15 proportional-edit distortion (baked live in the 393 f / 416 v mesh) was reversed analytically by per-vert fixed-point solve of the recorded +z smoothstep move (Δz = 0.15 × smoothstep(1 − d/0.5) about local influence center (1.0, 0.0, 0.6), iterated over all verts so d ≥ 0.5 self-zero); 23 verts restored, chosen vert idx 96 back to local z 0.60 exactly, counts unchanged 416 v / 393 f; `tool_settings.use_proportional_edit` reset True → False. **#17** `lesson_07_17_shading_comparison.png` (405,125 B; 1920×900; 2-panel solid-shading composite at one shared 3/4 FRONT-persp view · ORBITLEFT 25° + ORBITUP 15°, dist 5.0, loc (0,0,1); left `shade_flat` / right `shade_smooth`; Pillow `#FF6B00` 2-px divider at x=959; per-panel 959×900; `c17` composite-overlay reserved for INTEGRATION panel labels, positive-green default). **Alt:** "2-panel side-by-side comparison of the same coffee mug with flat shading and smooth shading at an identical 3/4 view. Panel 1 (left): flat shading showing visible polygon facets across the mug body. Panel 2 (right): smooth shading showing a rounded, continuous surface from the same geometry." **Figcaption:** "Flat versus smooth shading on the same mug geometry · flat reveals individual polygon facets · smooth interpolates normals for a rounded surface · identical view and lighting". **#22** `lesson_07_22_origin_point_placement.png` (755,266 B; 1920×1080; single-pose Object Mode; origin set to bottom-center via cursor→(0,0,0) + `origin_set(ORIGIN_CURSOR)` so obj.location now (0,0,0); overlays ON · grid floor + origin dot + X/Y axes; 3D cursor hidden so it does not overlap the origin dot; view FRONT persp + ORBITLEFT 20° + ORBITUP 10°, dist 5.5, loc (0,0,0.85); `c22` full-callout reserved for INTEGRATION ellipse + leader + badge on the origin dot, label "Origin point: bottom center"). **Alt:** "Slightly angled perspective view of the finished coffee mug in Object Mode resting on the grid floor, with a small origin marker at the bottom-center where the red X and green Y axes cross." **Figcaption:** "Object origin placed at the mug base center · marker at the floor-level bottom center where the X and Y axes cross · ideal placement for predictable transforms". **#23** `lesson_07_23_finished_mug_render.jpg` (52,597 B; 1920×1080; **JPG quality 90, `.jpg` extension · diverges from the placement-table `.png` name per requirements-doc Technical Specifications "JPG for photorealistic renders (only #23)"; placement-table row updated to `.jpg`**; full EEVEE `render.render` hero, NOT opengl; matte-ceramic Principled material · base (0.82,0.78,0.71), roughness 0.55; screen-space vertical gradient world via TexCoord Window.Y → ColorRamp dark→light; 20×20 grey floor for soft contact shadow; three-point tracked AREA lights · key 600 @ size 4 upper-left, fill 150 @ 5 right, rim 450 @ 3 behind; 55 mm tracked camera at (5.5,-6.5,3.6) aimed (0,0,1.0); taa_render_samples 64; no overlay). First render rejected (70 mm @ dist 6.5 + key 1400 cropped + blew out the ceramic); reframed to 55 mm pulled back + key 600. **Alt:** "High-quality 3/4 product render of the finished coffee mug with smooth ceramic shading on a soft studio backdrop, resting on a neutral surface with a soft contact shadow and a light-to-dark grey gradient background." **Figcaption:** "The finished coffee mug · smooth ceramic shading, subtle rim taper, and curved handle presented as a studio product render with soft three-point lighting and a gentle contact shadow". **NEW L07 P5 PRODUCTION discoveries (2, locked observation):** (1) **Analytic reversal of a proportional-edit displacement** without a checkpoint · a recorded +z smoothstep proportional move is exactly invertible per-vert by fixed-point iteration on Δz (only z changed, falloff monotonic), restoring clean geometry while preserving all vert indices · cheaper and lower-risk than a from-scratch rebuild. (2) **EEVEE studio backdrop without modeling a cyclorama** · a world-shader TexCoord `Window` → SeparateXYZ.Y → ColorRamp → Background gives a clean screen-space vertical gradient behind the subject; pair with one grey floor plane for the contact shadow · reusable for future hero/product renders. No new viewport-cache mechanisms (reused P1 `DRAW_WIN_SWAP`). **Hero advisory (non-blocking):** the low-segment Phase-3 handle shows facets under smooth shading and a small interior is visible through the rim opening at the 3/4 angle · both honest to the produced geometry, not defects. **Scene state at sub-chat A close:** single object `Mug`, 416 v / 393 f, smooth-shaded, origin at world (0,0,0) bottom-center, no materials, world reset to flat 0.05 grey, no camera/lights/floor, engine BLENDER_EEVEE, proportional edit OFF. Placement-table rows #17 / #22 / #23 flipped to `produced; not integrated`. Next chat: Phase 5 PRODUCTION sub-chat B (`#final-details`; 4 figures · #18 / #19 / #20 / #21; UI-chrome menu/panel captures via Win+Shift+S after scripted state setup).

- **2026-05-28** · Phase 5 PRODUCTION sub-chat B closed (capture-method split per session.md Scope: Claude scripted each figure's scene/mode/selection/view state via BlenderMCP and self-verified with viewport screenshots; Ray took the Win+Shift+S / PrtSc snips of the UI chrome; Claude cropped each via Pillow at the locked addon-modules path). 4 of 4 sub-chat B figures produced for `#final-details`; Phase 5 PRODUCTION now complete (7 of 7). **#18** `lesson_07_18_auto_smooth_panel.png` (281,598 B; 1905×933; Modifier Properties MODIFIER tab with the `Smooth by Angle` NODES modifier expanded, Angle 30°, mug visible in Edit Mode wireframe behind; `c18` overlay reserved for INTEGRATION). **Alt:** "Blender Modifier Properties panel showing the Smooth by Angle modifier on the mug, expanded to reveal its Angle field set to 30 degrees. The mug is visible in the viewport behind the panel." **Figcaption:** "The Smooth by Angle modifier in Modifier Properties · Blender 4.1+ replaces the old Auto Smooth checkbox with this modifier · the Angle value (30°) is the threshold below which neighboring faces are smoothed". **Requirements-doc divergence:** AI-prompt #18 describes the pre-4.1 Object Data Properties → Normals → Auto Smooth checkbox; documented here as the 5.1.1 Smooth by Angle modifier panel per the locked figure-list correction (parallels the #12 / #23 divergences). **#19** `lesson_07_19_sharp_edge_marking.png` (323,540 B; 1560×941; Edit Mode, EDGE select; upper handle-to-body junction seam loop selected (4 edges shared between a handle face and a body face, upper half by z, centroid ≈ (1.24, -0.12, 1.36)) shown as the orange edge at the handle's top connection; Ctrl+E Edge menu open with Mark Sharp listed; FRONT-based 3/4 view, handle on the right; `c19` overlay reserved). **Alt:** "Coffee mug in Edit Mode with an edge at the handle-to-body junction selected in orange and the Edge menu (Ctrl+E) open, showing the Mark Sharp option. Marking the edge sharp gives explicit control over shading at the connection." **Figcaption:** "Marking a sharp edge at the handle junction · select the edge, open the Edge menu with Ctrl+E, then choose Mark Sharp · keeps a corner crisp where smooth shading would otherwise round it". **#20** `lesson_07_20_merge_by_distance.png` (325,656 B; 1850×1294; Edit Mode, all 416 verts selected (orange); Mesh → Clean Up → Merge by Distance cascade open (confirms Merge by Distance lives in Clean Up in 5.1.1); `c20` overlay reserved). **Alt:** "Coffee mug in Edit Mode with all geometry selected and the Mesh menu open, showing the Clean Up submenu with Merge by Distance highlighted. The operation welds vertices that share the same location." **Figcaption:** "Mesh cleanup with Merge by Distance · select all, then Mesh → Clean Up → Merge by Distance · removes doubled vertices left behind by extrude and bridge steps". **#21** `lesson_07_21_nonmanifold_check.png` (350,499 B; 1850×1300; Edit Mode, all deselected (grey mug); Select → Select All by Trait → Non Manifold cascade open; produced as an honest ACCESS-PATH figure per Ray's Option-A call, NOT a clean-result figure, because the mug is non-manifold (see finding below); `c21` overlay reserved). **Alt (revised at production):** "Coffee mug in Edit Mode with the Select menu open, showing Select All by Trait with the Non-Manifold option highlighted. This command selects non-manifold geometry such as holes, internal faces, and edges shared by more than two faces." **Figcaption (revised):** "Finding non-manifold geometry · Select → Select All by Trait → Non-Manifold · highlights holes and internal edges to review and clean up before finishing the model". The originally-locked #21 Alt ("selects no geometry, confirming watertight") was discarded as false. **NEW L07 P5 sub-chat B discovery (1, locked carry-forward):** shade_auto_smooth vs shade_smooth_by_angle operator split (5.1.1). `bpy.ops.object.shade_auto_smooth(angle=radians(30))` attaches the "Smooth by Angle" NODES modifier (a geometry-nodes group from the essentials datafiles) · this is the #18 target. `bpy.ops.object.shade_smooth_by_angle()` does NOT add a modifier · it marks sharp edges directly on the mesh. The MODIFIER Properties tab only appears in the editor context enum once the object is active. Locked for L07 carry-forward. **NEW finding (non-manifold mug, flagged for QA):** the produced 416 v / 393 f mug is non-manifold · Select Non-Manifold selects 100 edges / 84 verts: 32 boundary edges forming an open ring (radius ≈ 0.84) at the interior floor (interior bottom not fully closed) + 52 edges shared by 3+ faces (24 out at the handle attachment x>0.8, the rest near the inner-rim structure · consistent with the handle bridged over intact body-wall faces leaving internal faces). Hidden interior geometry · renders #17 / #22 / #23 are visually unaffected. NOT cleaned: fixing it would alter the locked 416 v / 393 f base already used by produced figures. Bears on the #28 topology-comparison figure and any lesson-body "should be clean" wording near the `#final-details` non-manifold card → standing QA follow-up. **Production incident (reverted, for traceability):** during #20 staging an accidental Duplicate (likely a stray Shift+D during manual menu navigation) doubled the mesh to 832 v / 1608 e / 786 f / 2 islands; detected via an island + count check; reverted by deleting the appended duplicate island (verts idx ≥ 416); counts verified back to 416 v / 804 e / 393 f / 1 island before proceeding. **Scene state at sub-chat B close:** single object `Mug`, 416 v / 393 f, smooth-shaded with the `Smooth by Angle` NODES modifier present (angle 30°), origin at world (0,0,0) bottom-center, no materials assigned to the Mug (orphan `CeramicMug` / `FloorMat` datablocks linger from #23), proportional edit OFF, engine BLENDER_EEVEE; a few handle-junction edges marked sharp from #19 (cosmetic); mode at close Edit Mode, deselected. Still NO `.blend` on disk · if Blender is closed before INTEGRATION the scene is gone, but all Phase 5 figures are captured so a rebuild is only needed if a future figure requires the live mesh. Placement-table rows #18 / #19 / #20 / #21 flipped to `produced; not integrated`. Next chat: Phase 5 INTEGRATION plan-lock chat (`#final-details`; 7 figures · #17 to #23).

- **2026-05-28** · Phase 5 INTEGRATION closed. 7 of 7 Phase 5 figures integrated into `lesson_07_modeling_your_first_object.html` (`#final-details`) plus 1 body-wording edit for the #21 carry-in flag. HTML size 150,281 B to 164,653 B (+14,372 B / +14.0 KB across 7 figures + 1 wording edit). Per-step deltas (bottom-up execution order): **#23** plain JPG no overlay between "📝 Getting a Good View" card `</div>` and the "Congratulations!" para (+624 B). **#22** full-callout 1 positive-green between "📝 Setting Origin to Bottom Center" card `</div>` and the "Now your origin point" para; label "Origin point: bottom center"; ellipse on origin dot (cx=960 cy=750 rx=40 ry=22); badge upper-left x=160 y=80 w=395 h=60; viewBox 1920×1080; fs 22; pre-existing em-dash in "makes sense—that's where" preserved verbatim through anchor (+1,729 B). **Body-wording edit** at "For our simple mug" para (carry-in flag for #21 ACCESS-PATH framing · "neither claims watertight"): old "For our simple mug, you probably won't have any non-manifold geometry. But it's a good habit to check, especially as you create more complex models." replaced with new "On your first beginner mug it's normal to find some non-manifold edges left from the inset, extrude, and bridge steps · review and clean them up before treating the model as production-ready. It's a good habit to check on any model you build." (+95 B). **#21** multi-callout 2 positive-green between "📝 Checking for Issues" card `</div>` and the revised "On your first beginner mug" para; ACCESS-PATH framing per carry-in; labels "Select All by Trait" / "Non-Manifold" (path-pointing only, no clean-result claim); ellipse 1 cx=350 cy=560 rx=140 ry=22 + ellipse 2 cx=720 cy=600 rx=130 ry=22; badges right-side stack x=1500 y=80 w=280 / y=240 w=200; viewBox 1850×1300; fs 22 (+2,541 B). **#20** multi-callout 2 positive-green between "📝 Mesh Cleanup Checklist" card `</div>` and the "This cleanup operation finds vertices" para; labels "Clean Up" / "Merge by Distance"; ellipse 1 cx=350 cy=620 rx=110 ry=22 + ellipse 2 cx=720 cy=660 rx=180 ry=22; badges right-side stack x=1500 y=80 w=160 / y=240 w=260; viewBox 1850×1294; fs 22; pre-existing em-dash in "decluttering your model's geometry—getting rid" preserved verbatim through anchor (+2,492 B). **#19** multi-callout 2 positive-green between "📝 Marking Sharp Edges" card `</div>` and the "This is especially useful at the base of the handle" para; labels "Selected edge" / "Mark Sharp"; ellipse 1 cx=820 cy=420 rx=80 ry=22 + ellipse 2 cx=400 cy=620 rx=100 ry=22; badges right-side stack x=1200 y=80 w=200 / y=200 w=160; viewBox 1560×941; fs 22 (+2,544 B). **#18** multi-callout 2 positive-green between "📝 Enabling Auto Smooth" card `</div>` and the "Now your mug should look even better!" para; labels "Smooth by Angle modifier" / "Angle: 30°" (5.1.1 modifier panel per carry-in flag, not the pre-4.1 Auto Smooth checkbox); ellipse 1 cx=1600 cy=120 rx=250 ry=30 (modifier header) + ellipse 2 cx=1500 cy=280 rx=180 ry=28 (Angle field); badges left-side stack x=80 y=80 w=340 / y=240 w=160 (positioned in viewport area clear of right-side Properties panel content); viewBox 1905×933; fs 22 (+2,537 B). **#17** composite-overlay 2-panel positive-green between "📝 Applying Smooth Shading" card `</div>` and the "That's it! Your angular cylinder" para; panel labels "Flat shading" / "Smooth shading" (sentence-case, c5 / c6 / c12 precedent); viewBox 1920×900; fs 22 per `fs = round(22 × 1920 / 1920)`; c12-precedent rect geometry adapted for 1920×900 · rect 440×100 at y=750 / text y=800 / bottom margin 50 (matches #13 900-tall precedent); panel centers x=480 / x=1440; pre-existing em-dash in "Don't worry—that's normal" preserved verbatim through anchor (+1,810 B). Bottom-up execution order #23 → #22 → body-wording → #21 → #20 → #19 → #18 → #17 held; no anchor shifts. Dry-run-then-commit-then-verify per step (8 verify checkpoints incl. the body-wording edit). All 7 placement-table rows flipped to `integrated`. **No new L07 P5 INTEGRATION discoveries:** reused the locked inline-stacked overlay pattern, the indent-depth-derivation rule (section-content depth col 20 · figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24), the composite-overlay font-size formula `fs = round(22 × viewBox_w / 1920)`, the positive-green-default colorway, and the per-callout id naming `c{N}-highlight[-K]` / `-leader[-K]` / `-marker[-K]` / `-badge[-K]` without surfacing new mechanisms. **Standing QA additions (estimated-position visual-proof flags, new):** #18 c18 modifier-header + Angle-field ellipses · #19 c19 selected-edge + Mark-Sharp ellipses · #20 c20 Clean-Up + Merge-by-Distance ellipses · #21 c21 Select-All-by-Trait + Non-Manifold ellipses · #22 c22 origin-dot ellipse · join existing standing #14 + #15 estimated-position QA flags. #17 c17 uses c12-precedent deterministic geometry so no estimate to proof. **Carry-in flag closure:** #21 ACCESS-PATH framing executed (callouts point at menu items, not at any clean-result claim); lesson-body wording at "For our simple mug" para replaced with honest non-manifold-likely wording. #28 topology-figure standing follow-up unchanged (separate from this batch). **Out-of-scope flag noted, not actioned:** #18 pre-4.1 Auto Smooth card body wording (Object Data Properties → Normals → Auto Smooth instructions) left intact this chat · figcaption documents the 4.1+ shift verbatim · optional future QA touch-up. **L07 progress:** 5 of 6 phases integrated; 23 of 30 figures integrated; remaining work is Phase 6 (`#good-practices` + `#project`; 7 figures · #24 / #25 / #26 SVG `fo` / #27 / #28 SVG `ef` / #29 SVG `pc` / #30 SVG `ks`; none produced; sub-chat split required per ≥5 threshold; INTEGRATION plan-lock as own chat per ≥6 threshold). Next chat: Phase 6 PRODUCTION.

- **2026-05-28** · Phase 6 PRODUCTION sub-chat A closed (capture-method split locked: sub-chat A = 3 PNGs via BlenderMCP · #24 / #25 / #27; sub-chat B = 4 SVGs hand-coded · #26 `fo` / #28 `ef` / #29 `pc` / #30 `ks`, next chat). 3 of 3 sub-chat A figures produced for `#good-practices` + `#project`. **Scene state at start matched Phase 5 sub-chat B close exactly** (`Mug` 416 v / 804 e / 393 f, smooth-shaded with `Smooth by Angle` NODES modifier at angle 30°, origin at world (0,0,0), no materials on object, orphan `CeramicMug` / `FloorMat` data blocks lingering, EDIT mode at start). Switched to OBJECT mode, deselected, BLENDER_EEVEE engine confirmed. Production order #27 → #24 → #25 (props-only-add first · then mesh-transform variants with save/restore between). **#27** `lesson_07_27_scale_reference.png` (641,388 B; 1920×1080; opengl viewport SOLID FRONT-orthographic via `view3d.view_axis(type='FRONT')` + `view_selected` on all 3 objects; UV sphere r=1.5 at (-5, 0, 1.5) smooth-shaded + Mug at origin + default 2u cube at (5, 0, 1); viewport overlays: grid floor ON, X-axis red line ON, Y-axis ON, Z-axis OFF, cursor / origins / extras OFF; shading SOLID + STUDIO Default; world background light grey 0.88; `c27` overlay reserved for INTEGRATION 3-callout labels). **Alt:** "Front orthographic view comparing the coffee mug to two reference objects on the grid floor. From left to right: a smooth grey sphere as a head-size reference, the finished coffee mug with its handle extending to the right, and a default Blender cube. The red X-axis line runs along the floor through the base of all three objects." **Figcaption:** "Scale reference comparison · UV sphere as head-size reference (left) · finished coffee mug (center) · default 2-unit Blender cube (right) · all resting on the grid floor for visual scale comparison". **#24** `lesson_07_24_mug_variations.png` (395,298 B; 3848×800 = 5 panels × 768 wide + 4 × 2-px `#FF6B00` vertical dividers; per-panel 768×800; opengl viewport SOLID + STUDIO Default with `color_type='SINGLE'` and `single_color=(0.92, 0.89, 0.84)` warm cream ceramic + viewport background light grey 0.88; locked 3/4 perspective via FRONT + ORBITLEFT ≈25° + ORBITUP ≈10°; per-variant `view_selected` with locked rotation restored + `view_distance *= 1.15` margin so each variant fills its panel uniformly; v1 first-pass rejected as wasted-headroom · v2 with per-variant view_selected locked; `c24` composite-overlay reserved for INTEGRATION panel labels, positive-green default colorway). **Per-variant per-vert transforms** (all applied to a saved-coords backup at start of block; restored after every render): **Tall Latte** `Z×2.0`; **Espresso Cup** `XY×1.15, Z×0.7`; **Curved Body** smoothstep belly bulge · `factor = 1 + smoothstep(|z-1|) × 0.25` outward in XY, body-only via `r < 1.5` (excludes handle); **Decorative Rim** smoothstep rim flare · `factor = 1 + smoothstep((z-1.6)/0.4) × 0.30` outward in XY for z > 1.6, body-only; **Bowl Style** `XY×1.4, Z×0.5`. Pillow composite at locked addon-modules PIL path. **Alt:** "5-panel horizontal composite showing five mug variations modelled from the same base coffee mug. Panel 1: tall latte mug, twice the height of the base. Panel 2: short and wide espresso cup. Panel 3: curved body mug with a subtle belly bulge at the middle. Panel 4: mug with a pronounced decorative rim flare at the top. Panel 5: low wide bowl-style mug. Each panel uses the same 3/4 perspective angle on a light grey background." **Figcaption:** "Five mug variations from the same base techniques · tall latte · espresso cup · curved body · decorative rim · bowl style · each shows how scaling and selective vertex editing adapt the mug for a new purpose". **#25** `lesson_07_25_handle_troubleshooting.png` (341,430 B; 1804×1202 = 3 cols × 600 + 2 × 2-px wide × 2 rows × 600 + 1 × 2-px tall, all `#FF6B00` dividers; per-panel 600×600; opengl viewport SOLID + STUDIO Default with cream `single_color` carried over from #24; handle-focused view at locked `view_location=(1.4, 0.0, 1.0)`, `view_distance=4.5`, 3/4 rotation preserved; 4 unique renders · base solution reused for all 3 bottom-row panels). **Handle-vert threshold `x > 1.05`** excludes body wall (x≈1.0) and bridge verts at the junction. **Per-problem per-vert transforms** (save/restore between): **Twisted** Y-shear `dy += (x-1.0) × (z-1.0) × 0.7` (tilts cross-section as you move along arc); **Thin** `Y × 0.35` (reduces handle depth perpendicular to arc plane, preserves arc shape and attachment); **Thick** `Y × 1.9` (bulges handle depth, same axis). Bottom row 3 base panels identical → differentiated by `c25` overlay at INTEGRATION (problem X / solution ✓ icons + labels per requirements doc "Twisted Geometry" / "Too Thin/Weak" / "Too Bulky" on top, fix indicators on bottom; `c25` overlay reserved). **Alt:** "2-by-3 grid composite showing common handle problems on the top row and their cleaned-up base versions on the bottom row. Top row left to right: handle with twisted cross-section, handle that is too thin, handle that is too thick. Bottom row left to right: three views of the clean base mug with a correctly proportioned handle for comparison." **Figcaption:** "Common handle problems and their fixes · top row shows three handle defects (twisted, too thin, too thick) · bottom row shows the corrected handle proportions". **No new L07 P6 PRODUCTION discoveries this sub-chat:** reused the locked L07 P1 `DRAW_WIN_SWAP` matrix-sync rule at every view change; per-variant save/restore via vert-coord backup at start of block + restore-and-`mesh.update()` after each render proven clean across 8 variant renders (5 for #24 + 3 modified for #25). Mug mesh fully restored at sub-chat close · final verification: zmin=0.000, zmax=2.000, xmax=2.078, vert count=416, polys=393 (identical to Phase 5 sub-chat B close-state base). Pillow composite Pillow-relayed to WSL UNC per locked rule. All temp files cleaned. Placement-table rows #24 / #25 / #27 flipped to `produced; not integrated`. Header **Current state** updated to `Phase 6 PRODUCTION sub-chat A complete · 26 of 30 produced · 23 of 30 integrated`. **Scene state at sub-chat A close:** single `Mug` object, 416 v / 393 f at world origin, smooth-shaded with `Smooth by Angle` NODES modifier (angle 30°), no materials on object, orphan `CeramicMug` / `FloorMat` data blocks linger, viewport SOLID + STUDIO Default + cream-tinted `single_color`, world background light grey, OBJECT mode, deselected, view at handle-focused `view_location=(1.4, 0, 1.0)` / `view_distance=4.5` / locked 3/4 rotation. Still no `.blend` on disk · sub-chat B is pure SVG hand-coding so does not need the live Blender mesh; if Blender closes between chats, no rebuild needed. Next chat: Phase 6 PRODUCTION sub-chat B (`#good-practices` + `#project`; 4 SVGs · #26 `fo` file-organization tree, #28 `ef` topology comparison paired-panel good-vs-bad, #29 `pc` project checklist infographic, #30 `ks` keyboard shortcuts reference card; hand-coded SVG construction at L06 #44 `sr` filled-orange-badge precedent + L05 #18 / L06 #21 `dd` paired-panel semantic-header precedent + L04 #14 keyboard-shortcuts precedent; standalone `.svg` files keep `color="#222"` on root for `<img>` fallback per locked inline pattern; INTEGRATION plan-lock as own chat per ≥6 threshold runs as the chat after sub-chat B).

- **2026-05-28** · Phase 6 PRODUCTION sub-chat B closed (Phase 6 PRODUCTION complete · #26 `fo` / #28 `ef` / #29 `pc` / #30 `ks` hand-coded for `#good-practices` + `#project`; L07 PRODUCTION now complete at 30 of 30). 4 of 4 SVG files produced. Production order #26 → #28 → #29 → #30 (top-down placement order). **#26** `lesson_07_26_file_organization_tree.svg` (6,577 B; SVG `fo` prefix; hierarchical folder-tree pattern per L01 #8 precedent; root `Blender_Assets/` (brand orange `#FF6B00`) → 4 top-level subfolders · Props (blue `#4A9EFF`) with nested Kitchen / Furniture (darker blue `#3a7fd1`) and 3 `.blend` files (purple `#B47EE8` with corner-fold path + small `#FF6B00` Blender-dot), Characters (pink `#FF7EA8`), Environments (green `#4ADE80`), Materials (yellow `#F5C242`); tree content matches the lesson body's `<pre>` block verbatim · diverges from the requirements-doc AI prompt which listed a `References/` folder; tree-connector group at trunk x=64 (root) / sub-trunk x=112 (Props) / sub-sub-trunks x=156 (Kitchen, Furniture); 10 tree rows total at y=140 / 184 / 220 / 250 / 280 / 316 / 346 / 386 / 420 / 454; standalone file keeps `color="#222"` on root for `<img>` fallback per locked inline pattern, will be stripped at INTEGRATION). **#28** `lesson_07_28_topology_comparison.svg` (9,759 B; SVG `ef` prefix; paired-panel pattern per L05 #18 + L06 #21 `dd` precedent; left GOOD positive-green `#4ADE80` header bar with `#0f1a14` text · right BAD warning-red `#E63946` header bar with `#ffffff` text; both panels share the same curved-top outline `M 62,220 Q 210,195 358,220 L 358,410 L 62,410 Z` (left) / `M 442,220 Q 590,195 738,220 L 738,410 L 442,410 Z` (right) representing a flared mug-rim cross-section · differentiator from L05 #18 generic flat-grid via the curved top boundary; LEFT panel · 4 horizontal edge loops following the top curve with diminishing arc depth (δ=38 / 76 / 114 / 152, arc 20 / 15 / 10 / 5 px) + 5 straight vertical columns at x=111 / 161 / 210 / 259 / 309 from y_top(x) to y=410; RIGHT panel · 6-pole at (590, 305) with 3-layer concentric glyph (r=12 red 0.20 opacity outer + r=6 solid red mid + r=2 white inner) and 6 red spoke edges to (590,250) / (635,280) / (635,330) / (590,360) / (545,330) / (545,280) + 6 gray ring edges between adjacent tips + extending edges to boundary + 2 red-outlined tris in lower-left + red-outlined hexagonal n-gon in lower-right; 3 property pills below panels (QUADS · EVEN SPACING · LOOPS FOLLOW FORM) at y=455; italic takeaway at y=438; Variant-B footer Tip rail at y=506 with `&gt;`-escaped "Mesh > Clean Up > Tris to Quads" path; size class matches L05 #18 9,810 B + L06 #21 9,690 B as pattern-adherence proxy). **#29** `lesson_07_29_project_checklist_infographic.svg` (5,720 B; SVG `pc` prefix; 5 vertical phase rows at y=130 / 201 / 272 / 343 / 414 (height 65, gap 6) per L06 #44 `sr` row-structure adapted for phases; each row has a filled-orange numbered badge (circle r=24 fill `#FF6B00` with white "1"-"5" text) on left at cx=78, phase title + time-estimate stacked at x=120, and 3 condensed key-task highlights with `&#9744;` empty-checkbox glyphs at x=370 with dy=20 per line; phases match lesson body verbatim · Phase 1 Basic Form 15 min · Phase 2 Interior and Walls 15 min · Phase 3 Handle Creation 20 min · Phase 4 Refinement 10 min · Phase 5 Final Polish 5 to 10 min; 3-task-per-phase condensation diverges from the requirements-doc AI prompt's 7 to 8 checklist items per phase to avoid duplicating the lesson body card content one paragraph up; orange-tinted footer Tip callout at y=506). **#30** `lesson_07_30_keyboard_shortcuts_reference.svg` (10,687 B; SVG `ks` prefix; 2-column layout per L04 #14 precedent with L06 #44 `sr` filled-orange-badge styling (rect h=22 rx=4 fill `#FF6B00` + white text) at 30-px row spacing; 4 category sections × 5 rows = 20 shortcuts; LEFT column · MODES & SELECTION (Tab, 1 / 2 / 3, A, Alt + Click, B) + MODELING (E, Ctrl + R, I, F, Ctrl + B); RIGHT column · TRANSFORM (G / R / S, + X / Y / Z, O, Shift + D, Ctrl + Z) + VIEW & FILE (Z, Np 1 / 3 / 7, Np 5, Ctrl + S, Shift + Ctrl + S); category headers fill `#FF6B00` weight 700 letter-spacing 1.5 at x=40 (left) / x=420 (right); plus-sign separators between multi-key combo badges at opacity 0.5; descriptions at x=200 (left) / x=580 (right); `&amp;` HTML-entity for `&` in MODES & SELECTION / VIEW & FILE category headers per locked-rule XML escaping; orange-tinted footer Tip callout at y=506 with "G Z 0.1" chained-transform tip). **No new L07 P6 sub-chat B PRODUCTION discoveries:** SVG hand-coding reused all locked patterns and added no new MCP / tool / cache mechanisms. **Standing follow-up clarification (#28 vs lesson body non-manifold mug):** SVG content is generic curved-surface topology (flared-rim cross-section), not mug-specific, so the L07 P5 sub-chat B non-manifold-mug discovery does not constrain or contradict the figure. Standing follow-up carried forward unchanged for INTEGRATION QA verification only. Placement-table rows #26 / #28 / #29 / #30 flipped to `produced; not integrated`. Header **Current state** updated to `Phase 6 PRODUCTION complete · 30 of 30 produced · 23 of 30 integrated`. L07 PRODUCTION now complete (5 SVGs · 25 PNGs · 7 SVG + 18 PNG callouts reserved). Next chat: Phase 6 INTEGRATION plan-lock chat (`#good-practices` + `#project`; 7 figures · #26 SVG `fo` / #27 / #28 SVG `ef` / #29 SVG `pc` / #24 / #25 / #30 SVG `ks`; own chat per ≥6 threshold; per-figure section placement and overlay variants derived at plan-lock time from the requirements-doc Image Placement Suggestions section; INTEGRATION-time tasks include · strip `color="#222"` from each SVG root when inlining · re-indent SVG to col 28 content depth per L07 convention · design `c24` composite-overlay 5-panel labels · `c25` 6-panel problem/fix labels · `c27` 3-callout for scale-reference · `c28` / `c29` / `c30` are inline SVGs without `c{N}` PNG overlays needed).

- **2026-05-28** · Phase 6 INTEGRATION plan-lock closed. 7-figure plan locked for `#good-practices` + `#project`. **Sub-chat split locked (option a, by-section):** sub-chat A = `#good-practices` 3 figures bottom-up #28 → #27 → #26; sub-chat B = `#project` 4 figures bottom-up #30 → #25 → #24 → #29. Each sub-chat executes dry-run → commit → verify per figure per locked cadence; bottom-up order so earlier inserts do not shift later anchors. **Per-figure anchor placements locked** (5-line `oldText` window built at sub-chat execution time, each anchored on the unique marker line below; pre-existing em-dashes preserved verbatim through exact-match): **#26** SVG `fo` inline at section-level placement between "Organizing Your Assets" card `</div>` and `</section>` (end of `#good-practices`; section-level locked over inside-card alternative which would have required the +4 deeper-indent variant figure col 24 / div col 28 / svg col 32). Unique marker line: `<p>Future-you will thank present-you for this organization!</p>`. **#27** PNG + c27 multi-callout between "Working with Scale" card `</div>` and "Quick Scale Check" card. Unique marker line: the `<li>` containing `<strong>Apply scale if needed:</strong>` + `<code>Ctrl+A</code>` + ` → Scale (converts visual scale to actual geometry)`. **#28** SVG `ef` inline between "Signs of Good Topology" card `</div>` and `<div class="mermaid">` (mermaid div lives at col 0 source-HTML quirk; figure inserts at col 20). Unique marker line: the `<li>` containing `<strong>Logical edge flow:</strong> Edges should make sense when you look at them`. **#29** SVG `pc` inline between "Step-by-Step Project Checklist" intro para and first checklist card. Unique marker line: `<p>Follow this checklist to ensure you don't miss any important steps. Check off each item as you complete it!</p>`. **#24** PNG + c24 composite-overlay between "💡 Creative Encouragement" `</blockquote>` and "Troubleshooting Common Issues" `<h3>`. Unique marker line: the `<p>` containing `<strong>💡 Creative Encouragement:</strong>`. **#25** PNG + c25 6-panel between first troubleshooting card ("twisted/weird handle") `</div>` and second card ("smooth shading weird/blotchy"). Unique marker line: the `<li>` containing `If it's really messed up, delete the handle` + `start over` + em-dash + `it's faster than trying to fix a badly twisted handle` (pre-existing em-dash in HTML; preserved verbatim through exact-match anchor; `start over[U+2014]it's faster` in source). **#30** SVG `ks` inline between "🎉 Celebration Time" `</blockquote>` and `</section>` (end of `#project`). Unique marker line: the `<p>` containing `<strong>🎉 Celebration Time:</strong>`. **`c24` composite-overlay 5-panel design locked** (viewBox 3848×800 per #24 PRODUCTION · positive-green default · fs 44 per `fs = round(22 × 3848 / 1920)` · sentence-case labels per c12 / c5 / c6 / c17 precedent): bottom margin 44 (linear interp 720→40, 900→50); rect 440×100 rx=6 fill `#4ADE80` at panel_center−220 / y=656; text at panel_center / y=706 / text-anchor middle / dominant-baseline central / system-ui stack / weight 700 / fill `#0f1a14` / no letter-spacing. Panel centers x=384, 1154, 1924, 2694, 3464 (5 × 768 wide + 4 × 2-px dividers per PRODUCTION). IDs `c24-badge-1` .. `c24-badge-5`; text elements without ids per c12 precedent. Labels (sentence-case): "Tall latte" / "Espresso cup" / "Curved body" / "Decorative rim" / "Bowl style". **`c25` composite-overlay 6-panel mixed-colorway design locked** (viewBox 1804×1202 per #25 PRODUCTION · 3×2 grid · top-row warning-red + bottom-row positive-green per warning-vs-positive colorway split applied panel-wise · fs 22 baseline since viewBox ≈ 1920): rect 480×80 rx=6 at panel_center−240; top-row y_top=480 / text_y=520 (bottom margin 40 within row-1 600-tall); bottom-row y_top=1082 / text_y=1122 (bottom margin 40 within row-2 600-tall); panel centers TL / TM / TR at (300 / 902 / 1504, 300), BL / BM / BR at (300 / 902 / 1504, 902). Top-row rect fill `#E63946` + text fill `#ffffff`; bottom-row rect fill `#4ADE80` + text fill `#0f1a14`. IDs `c25-badge-1` .. `c25-badge-6` (1–3 top L→R, 4–6 bottom L→R). Labels (sentence-case): top "Twisted geometry" / "Too thin / weak" / "Too bulky"; bottom "Rotate to untwist" / "Scale Y to thicken" / "Scale Y to thin" (action-oriented; maps 1:1 to PRODUCTION transforms Y-shear / Y×0.35 / Y×1.9). **New pattern observation:** mixed-colorway composite-overlay (3 warning + 3 positive in one figure) is a first for L07 INTEGRATION; warning-vs-positive split is already in carry-forward, this just combines them in one SVG · flag for IMAGE_STYLE_GUIDE v6. **`c27` multi-callout 3-callout design locked** (viewBox 1920×1080 per #27 PRODUCTION · positive-green · top-of-frame badge stack one per object; ellipse + leader + marker + badge per locked multi-callout pattern; fs 22 / weight 700 / system-ui / fill `#0f1a14` / no letter-spacing; ellipse stroke `#4ADE80` width 4 fill none; leader stroke `#4ADE80` width 3 stroke-linecap round; marker circle r=6 fill `#4ADE80`; rect h=60 rx=6 fill `#4ADE80`). Labels (sentence-case): K=1 "Head-size reference" / K=2 "Coffee mug" / K=3 "Blender 2-unit cube". Estimated positions per K (joins standing QA flag list at sub-chat A close, derived from FRONT-ortho 3-obj view_selected bbox assumption): K=1 badge x=90 y=80 w=320 h=60 / text (250, 110) / ellipse (250, 480, 170, 170) / marker (250, 310) / leader (250, 140)→(250, 310); K=2 badge x=800 y=80 w=320 h=60 / text (960, 110) / ellipse (960, 700, 130, 180) / marker (960, 520) / leader (960, 140)→(960, 520); K=3 badge x=1510 y=80 w=320 h=60 / text (1670, 110) / ellipse (1670, 700, 145, 145) / marker (1670, 555) / leader (1670, 140)→(1670, 555). IDs `c27-highlight-K` / `c27-leader-K` / `c27-marker-K` / `c27-badge-K` for K=1..3 per locked multi-callout convention. **SVG inline conventions for #26 `fo` / #28 `ef` / #29 `pc` / #30 `ks`** (plain inline SVG, no PNG, no `<div>` wrapper): `<figure>` col 20 / `<svg>` col 24 / svg children col 28 / nested g children col 32 / `<figcaption>` col 24. At inline time per file: (1) strip `color="#222"` attribute from root `<svg>` element (standalone `.svg` file keeps it on disk for `<img>` fallback); (2) re-indent all content from standalone-file indent to col 28; (3) preserve all structural comments verbatim (L07 #1 precedent: 8 comments preserved); (4) wrap with `<figure>` + `<figcaption>` at col 20 / col 24. Standalone file sizes verified at P6 PRODUCTION sub-chat B close: #26 6,577 B · #28 9,759 B · #29 5,720 B · #30 10,687 B. **Decisions locked at plan-lock:** (1) sub-chat split = option (a) by-section over option (b) by-overlay-complexity (section milestones cleaner; design load balanced across both sub-chats with c27 in A and c24 + c25 in B); (2) #26 placement = section-level (after "Organizing Your Assets" card `</div>`, before `</section>`) over inside-card alternative (would have required +4 deeper-indent variant); (3) c24 label case = sentence-case over Title Case / ALL CAPS (c12 / c5 / c6 / c17 precedent dominant); (4) c25 bottom-row fix labels = "Rotate to untwist" / "Scale Y to thicken" / "Scale Y to thin" (action-oriented; maps to PRODUCTION transforms). **No new INTEGRATION discoveries at plan-lock:** the plan reuses the locked overlay-convention block verbatim (figure col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24), the composite-overlay font-size formula `fs = round(22 × viewBox_w / 1920)`, the c12-precedent rect geometry, the multi-callout pattern, and the indent-depth-derivation rule. Plan-lock chat is a planning-only operation; no HTML edits this chat. HTML size unchanged at 164,653 B (post-Phase-5-INTEGRATION). Header **Current state** updated to `Phase 6 INTEGRATION plan-lock complete · 30 of 30 produced · 23 of 30 integrated · 7-figure plan locked`. Next chat: Phase 6 INTEGRATION sub-chat A (`#good-practices`; 3 figures · #28 → #27 → #26 bottom-up; execute per anchors and design specs above; on close write per-figure delta Status bullet + flip placement-table rows #26 / #27 / #28 to `integrated` + update **Current state** header + add #27 c27 estimated-position visual-proof flag to the standing QA list in `session.md`).
- **2026-05-28** · Phase 6 INTEGRATION sub-chat A closed. 3 of 3 `#good-practices` figures integrated into `lesson_07_modeling_your_first_object.html`. HTML size 164,653 B to 191,103 B (+26,450 B / +25.8 KB across 3 figures). Per-figure deltas (bottom-up execution order #28 → #27 → #26 per locked plan): **#28** inline SVG `ef` (topology good-vs-bad paired-panel) between "Signs of Good Topology" card `</div>` and `<div class="mermaid">`; `color="#222"` stripped from root + `preserveAspectRatio="xMidYMid meet"` added; re-indented to `<figure>` col 20 / `<svg>` col 24 / svg children col 28 / `<g>` children col 32 / `<figcaption>` col 24; all 17 structural comments preserved verbatim; middle-dot separators in figcaption (+12,945 B). **#27** PNG + c27 multi-callout 3-callout positive-green between "Working with Scale" card `</div>` and "Quick Scale Check" card; inline-stacked pattern (`<figure>` col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24); viewBox 1920×1080; locked estimated c27 geometry executed verbatim (K=1 "Head-size reference" / K=2 "Coffee mug" / K=3 "Blender 2-unit cube"; top-of-frame badge stack); pre-existing right-arrow `→` in the "Apply scale if needed" anchor `<li>` preserved verbatim through exact-match (+4,209 B). **#26** inline SVG `fo` (file-organization tree) at section-level between "Organizing Your Assets" card `</div>` and `</section>`; `color="#222"` stripped + `preserveAspectRatio` added; re-indented to the same col 20/24/28/32 depths; all structural comments preserved; middle-dot separators in figcaption (+9,296 B). Bottom-up order held; no anchor shifts. Dry-run-then-commit-then-verify per figure (3 verify checkpoints via `Filesystem:get_file_info`: 177,598 → 181,807 → 191,103 B). All 3 placement-table rows (#26 / #27 / #28) flipped to `integrated`; c27 row overlay column flipped from `c27 reserved` to `c27`. **No new INTEGRATION discoveries:** reused the locked inline-stacked overlay pattern, the SVG inline conventions (strip `color="#222"` / add `preserveAspectRatio` / re-indent / preserve comments), the multi-callout pattern + per-callout id naming `c27-highlight-K` / `-leader-K` / `-marker-K` / `-badge-K`, and the indent-depth-derivation rule without surfacing new mechanisms. **Standing QA addition:** #27 c27 3-callout ellipse / leader / badge positions are estimated from the FRONT-ortho 3-object `view_selected` bbox assumption · joins the standing estimated-position visual-proof flag list (added to `session.md` standing deferred follow-ups this chat). **L07 progress:** 26 of 30 integrated; remaining work is Phase 6 INTEGRATION sub-chat B (`#project`; 4 figures · #30 SVG `ks` → #25 PNG + c25 6-panel mixed-colorway → #24 PNG + c24 5-panel composite-overlay → #29 SVG `pc`; bottom-up; execute per plan-lock Status bullet 2026-05-28 anchors + design specs). Next chat: Phase 6 INTEGRATION sub-chat B.

- **2026-05-28** · Phase 6 INTEGRATION sub-chat B closed. **L07 INTEGRATION COMPLETE (30 of 30) · L07 image work complete.** 4 of 4 `#project` figures integrated into `lesson_07_modeling_your_first_object.html`. HTML size 191,103 B to 220,345 B (+29,242 B / +28.6 KB across 4 figures). Per-figure deltas (bottom-up execution order #30 → #25 → #24 → #29 per locked plan): **#30** inline SVG `ks` (keyboard-shortcuts reference) between "🎉 Celebration Time" `</blockquote>` and `</section>` (end of `#project`); `color="#222"` stripped from root + `preserveAspectRatio="xMidYMid meet"` added; re-indented to `<figure>` col 20 / `<svg>` col 24 / svg children col 28 / nested `<text>`-in-footer at col 32 (only the footer `<text>`/`<tspan>` block nests); all structural comments preserved verbatim (left/right column banners + per-row comments); middle-dot figcaption (+14,576 B). **#25** PNG + c25 6-panel mixed-colorway composite-overlay between first troubleshooting card ("handle looks twisted") `</div>` and second card ("smooth shading weird/blotchy"); inline-stacked pattern (`<figure>` col 20 / div col 24 / img + svg col 28 / callout col 32 / figcaption col 24); viewBox 1804×1202; top-row warning-red `#E63946`+`#ffffff` ("Twisted geometry" / "Too thin / weak" / "Too bulky"), bottom-row positive-green `#4ADE80`+`#0f1a14` ("Rotate to untwist" / "Scale Y to thicken" / "Scale Y to thin"); rect 480×80 at panel_center−240; top y=480/text 520, bottom y=1082/text 1122; fs 22; c25-badge-1..6 (1–3 top L→R, 4–6 bottom L→R); pre-existing em-dash in the "start over[U+2014]it's faster" anchor `<li>` preserved verbatim through exact-match (+3,313 B). **#24** PNG + c24 5-panel composite-overlay positive-green between "💡 Creative Encouragement" `</blockquote>` and "Troubleshooting Common Issues" `<h3>`; inline-stacked pattern; viewBox 3848×800; fs 44 per `fs = round(22 × 3848 / 1920)`; rect 440×100 at panel_center−220 / y=656 / text y=706; panel centers 384 / 1154 / 1924 / 2694 / 3464; sentence-case labels "Tall latte" / "Espresso cup" / "Curved body" / "Decorative rim" / "Bowl style"; c24-badge-1..5 (+3,064 B). **#29** inline SVG `pc` (project checklist) between the "Step-by-Step Project Checklist" intro para and first checklist card ("Phase 1: Basic Form"); `color="#222"` stripped + `preserveAspectRatio` added; re-indented to the same col 20/24/28/32 depths; all 5 `pc-phase-N` `<g>` groups + nested task `<g>` + comments preserved; `&#9744;` checkbox glyphs and `&#183;`-equivalent middle-dots intact; middle-dot figcaption (+8,289 B). Bottom-up order held; no anchor shifts. Dry-run-then-commit-then-verify per figure (4 verify checkpoints via `Filesystem:get_file_info`: 205,679 → 208,992 → 212,056 → 220,345 B). All 4 placement-table rows (#24 / #25 / #29 / #30) flipped to `integrated`; c24 / c25 overlay columns flipped from `reserved` to `c24` / `c25`. **Current state** header updated to L07 INTEGRATION COMPLETE. **No new INTEGRATION discoveries:** reused the locked inline-stacked overlay pattern, the SVG inline conventions (strip `color="#222"` / add `preserveAspectRatio` / re-indent to col 24/28/32 / preserve comments), the composite-overlay font-size formula `fs = round(22 × viewBox_w / 1920)`, the c12-precedent rect geometry, the positive-green-default colorway, the mixed-colorway composite-overlay (first realized here from the plan-lock design · #25 confirms 3-warning + 3-positive in one SVG renders cleanly · flag for IMAGE_STYLE_GUIDE v6), and the per-callout / per-badge id naming conventions without surfacing new mechanisms. **Standing QA addition:** #25 c25 badge positions and #24 c24 badge positions are deterministic composite-overlay geometry (panel-center-derived, not pixel-scanned estimates) so add NO new estimated-position visual-proof flags; #30 `ks` and #29 `pc` are inline SVGs with no callout overlays so likewise add no flags. The standing estimated-position QA list (#14 / #15 / #18 / #19 / #20 / #21 / #22 / #27) is unchanged by this sub-chat. **L07 image work complete:** all 6 phases integrated; 30 of 30 figures integrated (5 SVG inline · 25 PNG/JPG · 18 PNG callout overlays + 5 inline-SVG figures). Remaining L07 items are the standing deferred follow-ups in `session.md` (browser QA passes, IMAGE_STYLE_GUIDE v6 revision, #28 non-manifold-vs-lesson-body verification, optional Pillow recompress, #8 / #18 advisory wording touch-ups) · none blocking. Next chat: L07 browser QA pass OR Lesson 08 image pipeline kickoff · Ray's call.

---

- **2026-05-28** · L07 browser QA pass executed (live render via `http://localhost:5504/lesson_07_modeling_your_first_object.html` from WSL; capture method = canvas pixel-sampling at overlay coords against decoded PNGs, since the Chrome screenshot compositor returned blank frames this session · Ray approved measurement-only proof). HTML 220,345 B to 220,485 B (+140 B). **Broken-path check PASS:** all 25 raster assets return HTTP 200 with byte sizes matching the production log (24 PNG + 1 JPG); all 5 inline-SVG figures render at correct 800×560 aspect, non-zero; all 18 callout `c{N}` groups present (c5/6/8/10/11/12/13/14/15/17/18/19/20/21/22/24/25/27). **Estimated-position flags proofed (plausibility bar per Ray):** **#14** rim-bevel ellipse was high · pixel scan put the mug rim at viewBox y≈274 across the ellipse x-span, ellipse sat at cy=235 (band 213–257, ~37 px above rim) · CORRECTED `c14-highlight` cy 235→272, `c14-leader` y1 218→255 (ellipse rx/ry unchanged; badge + marker unchanged). **#19** sharp-edge: orange selected edge measured at (978,469) (tight bounds x972–984 y440–496), ellipse sat at (820,420) · 158 px off · CORRECTED `c19-highlight-1` (820,420)→(978,469), `c19-leader-1` object-end (890,410)→(955,452), `c19-marker-1` (890,410)→(955,452); c19-2 Mark-Sharp menu callout left as-is. **#27** scale-reference: objects measured at sphere x≈672 cy540, mug x≈980 cy568, cube x≈1296 cy572 (grey-on-grey, found via per-column min/max brightness deviation) · callouts were spread 250/960/1670 (bbox assumption too wide) · CORRECTED all 3 callouts: c1 ellipse (250,480,r170)→(672,540,r95) + leader (250,140→250,310)→(660,140→672,440) + marker→(672,440) + badge x90→500 + text x250→660; c2 ellipse (960,700,130×180)→(980,568,120×150) + leader→(985,140→980,415) + marker→(980,415) + badge x800→825 + text x960→980; c3 ellipse (1670,700,r145)→(1296,572,r95) + leader→(1300,140→1296,474) + marker→(1296,474) + badge x1510→1140 + text x1670→1296. **Measured plausible, NO edit:** **#15** c15-1 "Selected vertex" dead-on the gradient/vertex center (~1026–1045, 400–410); c15-2 / c15-3 ~35 px right of the gradient perimeter (marginal, within plausibility band). **#22** origin dot measured at (960,736); ellipse (960,750) ry22 · dot inside band, marker (945,735) on dot · on target. **#18** Smooth-by-Angle panel: both ellipse vertical rows align to panel rows (Angle field row confirmed by a bright value-spike at y≈280 inside the Properties panel at x>1587); horizontal column targets not resolvable from brightness alone but plausible · no edit. **Held open (could NOT safely proof · see flags):** **#20** Merge-by-Distance and **#21** Non-Manifold · both callouts measured sitting over the bright viewport / orange-selected mug rather than on the dark menu-cascade items they label; exact menu-item pixel targets are not resolvable by brightness sampling, and the screenshot compositor is broken this session, so any coordinate edit could not be verify-proofed per the locked cadence · left unchanged, flags retained in `session.md`. **Priority-2 #28 PASS:** the topology figure is a generic good-vs-bad edge-flow schematic (QUADS / EVEN SPACING / LOOPS FOLLOW FORM vs TRIS/N-GON/POLE on a curved patch) and makes no manifold/watertight claim, so the non-manifold-mug finding does not contradict it; the lesson body (and #29 checklist task "Merge by Distance and check non-manifold") already frames non-manifold geometry as expected-and-cleanable, which is aligned, not contradicted · no edit. **Advisory touch-ups (Ray approved both):** **#8** Alt "Side orthographic view" → "Side angled-perspective view" (+6 B); **#18** "📝 Enabling Auto Smooth" card body rewritten from the pre-4.1 Object Data Properties → Normals → Auto Smooth checkbox steps to the 5.1.1 Shade Auto Smooth → Modifier Properties → Smooth by Angle modifier path, aligning the card with the figure + figcaption (+137 B). **Verify cadence:** every edit dry-run → commit → `get_file_info` (220,345 → 220,345 → 220,345 → 220,342 → 220,485 B across the 5 edits); corrected coords re-confirmed live in the reloaded page (c14 820,272 · c19-1 978,469 · c27 672,540 / 980,568 / 1296,572). **Remaining:** held #20 / #21 menu-cascade overlays (need a working screenshot path or finer UI-element detection); resolved flags (#14 / #15 / #18 / #19 / #22 / #27) cleared from `session.md` standing follow-ups this chat; #28 standing follow-up cleared (verified PASS); #8 + #18 advisory items cleared (fixed).

- **2026-06-13** · L07 browser-QA PASS (full Playwright + real-Chromium sweep, dark+light × 1920/1024/414 — supersedes the 2026-05-28 canvas-pixel-sampling pass that ran with a broken screenshot compositor and left #20/#21 unproofed). ENTRY-STATE RECONCILED: live P0 read **220,405 B** but the prior-QA close logged 220,485 B; the −80 B delta carries a Jun-12 mtime = the same benign batch-touch seen on L09/L12/L13 this period, body verified sound → **220,405 B authoritative P0**. em-dash baseline **62** (whole-file, Python `read().count('\u2014')`-authoritative), held through the fix. **ONE FIGURE DEFECT FOUND + FIXED (F1):** figures **#17 / #18 / #19 / #20 / #21 / #22 / #24 / #25** (all of Phase 5 `#final-details` + the two Phase 6 `#project` troubleshooting PNGs) carried bare `<img>` tags WITHOUT the locked `display:block; max-width:100%; height:auto; margin:0` overlay-img style; the inline-block baseline gap shifted each overlay svg by a constant **dy=−16px / dh=+32px** vs its img box — the same defect class as the L13 #18/#19 fix, but here crossing into visible misregistration so it was fixed (unlike L09-F1, left benign). FIX: added `loading="lazy"` + the locked img style to all 8 imgs via `Filesystem:edit_file` dryRun→commit→`get_file_info`, one count=1 text anchor each (alt-tail + `>`). HTML **220,405 → 221,053 B (+648)**. Re-ran the harness: worst per-overlay box-delta **0.08px** across all 18 overlays × 6 combos (was up to 32px on the 8), mermaid 8/8, 0 blank imgs, 0 console errors, 414 overflow 0px. The 8 overlay-wrapper imgs now carry the locked style → **overlay-wrapper imgs 18/18 with margin:0**. **#20 / #21 HELD FLAGS RESOLVED:** the real-Chromium run captured live frames cleanly (no broken compositor this session), so the Merge-by-Distance (#20) and Non-Manifold (#21) menu-cascade overlays the 2026-05-28 pass couldn't proof now measure within tolerance (0.08px post-fix), callouts on their menu-cascade items as authored. Both cleared from the standing deferred queue; L07 browser-QA now complete. No lesson-body defects surfaced this pass. Full method + result table in `lesson_07_browser_qa_progress.md`. **Remaining L07 items:** none blocking; next standing-queue item is L05 / L06 browser-QA or L11 1024-width sweep (Ray's call).

---
