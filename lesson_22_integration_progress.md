# Lesson 22 &#183; Depth of Field and Focus &#183; Integration Progress

**Source of truth for L22 production + integration.** Mirrors the L21 progress-doc structure. Read order in a fresh chat: `session.md` &#8594; this doc &#167;6 to END &#8594; `images/IMAGE_STYLE_GUIDE.md` v6.

**Lesson file:** `lesson_22_depth_of_field_and_focus.html`
**Requirements doc:** `lesson_22_image_requirements.md` (generic AI-prompt spec &#8212; use ONLY for figure list, purpose, section placement, naming convention. Its palette `#2b2b2b`/`#ff8c00`/`#00bcd4` and "AI Image Generation Prompts" do NOT apply. IMAGE_STYLE_GUIDE v6 + locked render recipes + `#FF6B00` brand palette OVERRIDE it.)

**STATUS: ROSTER PLAN-LOCKED (2026-06-21). ALL 9 INLINE SVG BUILT: pilot #6 + B1 (#3/#5/#26) + B2 (#14/#15/#16) + B3 (#13/#18) done and signed off.** Q1&#8211;Q4 resolved. NOT yet integrated (live HTML still figure 0 / svg 0). PNG production IN PROGRESS: #2 + #19 DELIVERED. NEXT: #28 wrong_focus_examples (reuse staged DoF scene), then #9 bokeh + #22 sample-count (Cycles hi-samples), then UI-chrome #7/#11/#12/#23/#24/#29.

---

## &#167;1 &#183; Invariant baseline (LIVE HTML, UTF-8-aware Python count, NOT shell grep &#8212; locale unset so grep miscounts)

Captured at L22 OPEN against `lesson_22_depth_of_field_and_focus.html` as it exists pre-integration. Re-verify at EVERY checkpoint; integration is HTML-only (Blender stays pristine).

| Invariant | Baseline (pre-integration) | Notes |
|-----------|----------------------------|-------|
| bytes (raw) | **184,777** | `len(file.read())` bytes |
| chars (UTF-8) | 183,959 | |
| figure | **0** | none present pre-integration |
| figcaption | **0** | |
| img | **0** | |
| svg | **0** | |
| mermaid | **1** | ONE `<div class="mermaid">` diagram. The other 4 raw "mermaid" string hits are the loader `<script>` (import + `mermaid.initialize`). PRESERVE the 1 block; never touch. |
| em-dash U+2014 | **74** | ALL literal chars. PRESERVE verbatim, ADD NONE. Use `&#183;` / `.` / `,` / `( )` / `:`. Re-verify count at every checkpoint. |
| color222 | **0** | `#222` and `color="#222"` both 0. Req-doc `#2b2b2b` also 0 &#8212; confirms locked pipeline already governs the file. |

**Section IDs (document order, 11 total):**
`dof-fundamentals` &#183; `aperture-fstop` &#183; `focus-distance` &#183; `bokeh` &#183; `rack-focus` &#183; `practical-dof-scenarios` &#183; `troubleshooting-dof` &#183; `project-dof-mastery` &#183; `summary` &#183; `whats-next` &#183; `final-encouragement`

- The 9 section IDs in the requirements doc all EXIST and match in order against the live HTML.
- Two extra tail sections in live HTML not in req-doc: `whats-next`, `final-encouragement` (standard lesson tail; no figures proposed for them).

**TARGET invariants at integration close (LOCKED at roster plan-lock):**
figure = **20** / figcaption = **20** / img = **11** / svg = **20** / mermaid = **1** (preserved) / em-dash U+2014 = **74** (preserved) / color222 = 0.

> **svg-count derivation:** the `<svg>` tag count at close = inline-diagram SVGs (**9**) + PNG-overlay SVGs (one per PNG = **11**) = **20**. (Equals the figure count here only by coincidence: 9 SVG figures + 11 PNG figures = 20 figures, and 9 inline + 11 overlay = 20 `<svg>` tags.) img = 11 (one per PNG figure). figcaption = 20 (one per figure). Always re-derive from actuals at each integration checkpoint; never trust a single carried number.

---

## &#167;2 &#183; Locked roster (PLAN-LOCKED 2026-06-21)

**Tier source:** requirements doc Production Priority (High 9 / Medium 11 / Lower 10).
**Precedent:** L20 + L21 built **High + Medium**, deferred Lower. Same here.

**ROSTER = High + Medium = 20 figures &#8212; 9 inline SVG + 11 PNG. DEFER Lower = 10.**

### Roster (20) &#8212; FINAL types

| # | slug | tier | type | section |
|---|------|------|------|---------|
| 2 | shallow_deep_comparison | High | PNG (DoF render) | dof-fundamentals |
| 3 | fstop_scale_reference | High | **SVG** (reassigned Q2) | aperture-fstop |
| 5 | fstop_dof_chart | High | **SVG** (reassigned Q2) | aperture-fstop |
| 6 | focal_plane_diagram | High | SVG | focus-distance |
| 7 | focus_object_setup | High | PNG (UI-chrome) | focus-distance |
| 9 | bokeh_blade_examples | Medium | PNG (Cycles render) | bokeh |
| 11 | camera_dof_settings | Medium | PNG (UI-chrome) | bokeh |
| 12 | rack_focus_timeline | Medium | PNG (UI-chrome) | rack-focus |
| 13 | rack_focus_setup | Medium | SVG | rack-focus |
| 14 | portrait_dof_setup | High | SVG | practical-dof-scenarios |
| 15 | product_dof_setup | High | SVG | practical-dof-scenarios |
| 16 | architectural_dof_setup | Medium | SVG | practical-dof-scenarios |
| 18 | dof_troubleshooting_flowchart | High | SVG | troubleshooting-dof |
| 19 | cycles_eevee_comparison | Medium | PNG (DoF render) | troubleshooting-dof |
| 22 | sample_count_comparison | Medium | PNG (Cycles render) | troubleshooting-dof |
| 23 | focus_methods_comparison | Medium | PNG (UI-chrome) | focus-distance |
| 24 | rack_focus_curve | Medium | PNG (UI-chrome) | rack-focus |
| 26 | aperture_reference_card | High | **SVG** (reassigned Q2) | practical-dof-scenarios |
| 28 | wrong_focus_examples | Medium | PNG (DoF render composite) | troubleshooting-dof |
| 29 | render_samples_location | Medium | PNG (UI-chrome) | troubleshooting-dof |

**Final split:**
- **9 inline SVG:** #3, #5, #6, #13, #14, #15, #16, #18, #26
- **11 PNG:** #2, #7, #9, #11, #12, #19, #22, #23, #24, #28, #29

### Deferred (Lower 10): #1, #4, #8, #10, #17, #20, #21, #25, #27, #30
(req-doc SVG of these: #1, #4, #10, #17, #20, #27; PNG: #8, #21, #25, #30.)
**#30 gallery stays DEFERRED (Q3 resolved):** it is portfolio-inspiration, not a teaching figure, and is the most expensive to produce (4 full DoF project renders). The 5 in-roster DoF renders (#2/#9/#19/#22/#28) carry all the blur/bokeh/sample teaching. Clean late-stage add if Ray later wants an inspiration piece (matches L21's deferral of gallery-adjacent Lower figures).

### DoF-render figures &#8212; recipe SIGNED OFF (Q4 resolved)
Real camera DoF + high-frequency-detail FG/BG. Smooth spheres do NOT show blur (L21 #17 LD, took 4 iterations); use cube clusters / detailed geo. Enable camera DoF during render, restore DoF OFF at close.
- #2 shallow vs deep (f/1.8 vs f/16) &#8212; DoF render, both panels; same scene, aperture is the only change.
- #9 bokeh blade examples (5/7/9/12 blades) &#8212; **Cycles + high samples**; background point-light array for bokeh circles; blade-count is the variable.
- #19 cycles vs eevee &#8212; same DoF scene, two engines side by side.
- #22 sample-count progression (64/128/256/512) &#8212; **Cycles**; noise in the blur zone is the teaching point.
- #28 wrong-focus composite &#8212; DoF render, problem/solution pairs.

---

## &#167;3 &#183; id-prefix registry (one unique prefix per inline SVG)

One unique `{id}` prefix per inline SVG so all 9 coexist on the page without id collisions (IMAGE_STYLE_GUIDE header convention). Prefixes need only be unique WITHIN this page. LOCKED:

| # | slug | id-prefix | viewBox (provisional, confirm at production) |
|---|------|-----------|----------------------------------------------|
| 3 | fstop_scale_reference | `fs` | 800 560 |
| 5 | fstop_dof_chart | `fc` | 800 680 (table depth) |
| 6 | focal_plane_diagram | `fp` | 800 560 &#183; BUILT |
| 13 | rack_focus_setup | `rf` | 800 560 |
| 14 | portrait_dof_setup | `pd` | 800 560 |
| 15 | product_dof_setup | `pr` | 800 560 |
| 16 | architectural_dof_setup | `ar` | 800 560 |
| 18 | dof_troubleshooting_flowchart | `tf` | 800 680 (flowchart depth) |
| 26 | aperture_reference_card | `rc` | 800 680 (table depth) |

All 9 prefixes distinct: `fs` `fc` `fp` `rf` `pd` `pr` `ar` `tf` `rc`. (`fs` reused from the L01 folder-structure pilot &#8212; safe; uniqueness is per-page.)

---

## &#167;4 &#183; c{N} overlay registry (PNG callout overlays, authored at INTEGRATION)

`c{N}-*` namespaced overlay ids per PNG. No baked text on PNGs &#8212; all labels/markers/curves land as overlay SVG at integration. Colorway: positive green `#4ADE80`/text `#0f1a14`, warning red `#E63946`/text `#ffffff`. Composite PNGs with baked dividers get one centered badge per panel (no highlight/leader). 11 PNGs &#8594; 11 overlay slots:

| c{N} | # | PNG | overlay content (provisional) |
|------|---|-----|-------------------------------|
| c2 | 2 | shallow_deep_comparison | panel labels "Shallow f/1.4" / "Deep f/16"; sharp-subject marker |
| c7 | 7 | focus_object_setup | UI callouts: Enable DoF, Focus Object field, Empty link (pixel-verify vs cropped PNG) |
| c9 | 9 | bokeh_blade_examples | per-panel blade-count labels 5/7/9/12 |
| c11 | 11 | camera_dof_settings | numbered DoF-control callouts (F-Stop, Focus Object, Distance, Blades, Ratio) |
| c12 | 12 | rack_focus_timeline | start/end keyframe markers, transition label |
| c19 | 19 | cycles_eevee_comparison | panel labels "Cycles" / "Eevee" |
| c22 | 22 | sample_count_comparison | per-panel sample labels 16/64/256/1024 (ladder revised at #22 delivery; see &#167;6) |
| c23 | 23 | focus_methods_comparison | method labels + pro/con check/X marks |
| c24 | 24 | rack_focus_curve | ease-in / ease-out annotations on curve |
| c28 | 28 | wrong_focus_examples | per-pair problem (red X) / solution (green check) badges |
| c29 | 29 | render_samples_location | Render vs Viewport sample-field callouts |

(c3 / c5 / c26 dropped &#8212; those figures are now inline SVG, not PNG overlays.) Final solo-vs-overlay decided at integration (L20 precedent: some PNGs land bare).

---

## &#167;5 &#183; Production cadence

- **Plan-lock DONE.** Roster + type split + render recipe locked above. Build order below.
- **SVG: 1 pilot, then batches of 3** (IMAGE_STYLE_GUIDE workflow). Pilot = **#6 focal_plane_diagram** &#8212; BUILT + signed off (see &#167;6). cairosvg render-check light + dark before sign-off. 0 em-dash; color222 kept on standalone `.svg` for `<img>` fallback, STRIP on inline.
  - SVG batches after pilot: **B1** = #3, #5, #26 (the three reassigned infographics, shared table/chart idiom); **B2** = #14, #15, #16 (the three practical-scenario setup diagrams, shared side-profile idiom); **B3** = #13, #18 (rack-focus setup + troubleshooting flowchart). (9 SVG = pilot 1 + 3 + 3 + 2.)
- **PNG render routing** (IMAGE_STYLE_GUIDE v6 Locked decision 17 + 20):
  - color-dependent &#8594; F12 `render.render` Standard/None (NOT opengl).
  - grey-clay / arrangement &#8594; SUN-recipe (SUN rx66&#176;/en1.75/angle10&#176;, world grey 0.039/1.0).
  - **DoF renders &#8594; real camera DoF + high-frequency-detail FG/BG** (cube clusters, NOT smooth spheres). Bokeh (#9) + sample-count (#22) &#8594; Cycles + high samples. (#2/#19/#28 per &#167;2 sign-off.)
  - UI-chrome (#7/#11/#12/#23/#24/#29) &#8594; full-window local-tempdir `screen.screenshot` + Pillow crop in place (bridge-side PIL).
  - Composites: L20 convention (LANCZOS panels, (36,36,38) label band, 3px `#FF6B00` strip, 2px orange dividers, NO baked text; each grid cell its own top label band).
- **cairosvg pixel-verify Claude-side before EVERY PNG-overlay commit** (LD19): composite overlay over cropped PNG at viewBox=PNG-px, view to confirm 1:1.
- **Integration:** `Filesystem:edit_file` dryRun&#8594;commit&#8594;`get_file_info` when available; else read-full &#8594; edit-in-context &#8594; `write_file` overwrite, showing diffs first. Inline SVGs strip `color="#222"`, re-indent, collapse multi-line attrs, prune ASCII-divider comments, preserve role/aria/title/desc/id. Re-count invariants after every commit.
- **Browser QA at close:** fresh wide-desktop session via `http://localhost:5504/lesson_22_depth_of_field_and_focus.html`. Verify overlay alignment at narrow AND wide widths. High-DPR capture-timeout &#8594; isolate figure in short standalone clone doc.

---

## &#167;6 &#183; Status log

- **2026-06-21 &#183; L22 OPENED.** Read order complete (session.md, IMAGE_STYLE_GUIDE v6, lesson_22_image_requirements.md). Captured live-HTML invariant baseline (UTF-8-aware Python, not shell grep): bytes **184,777**; figure 0 / figcaption 0 / img 0 / svg 0; **mermaid 1** (one diagram; 4 other hits = loader script); **em-dash U+2014 74** (all literal &#8212; preserve, add none); color222 0. Section IDs verified against live HTML (11 in doc order; req-doc's 9 all present + match; 2 extra tail sections `whats-next` / `final-encouragement`). Created this progress doc as source-of-truth. Updated session.md (Active = L22). Blender untouched (true-pristine carried from L21 close).
- **2026-06-21 &#183; L22 ROSTER PLAN-LOCKED.** Q1&#8211;Q4 resolved with Ray: **Q1** roster = High+Medium 20, defer Lower 10 (confirmed). **Q2** #3/#5/#26 reassigned PNG&#8594;inline SVG (infographic tables/charts; L21 SVG-infographic precedent) &#8594; final split **9 inline SVG + 11 PNG**. **Q3** #30 gallery stays DEFERRED (portfolio inspiration, not teaching; most expensive; in-roster DoF renders carry the teaching). **Q4** DoF-render recipe signed off (#2/#9/#19/#22/#28 real camera DoF + high-freq FG/BG; #9 bokeh + #22 samples on Cycles high samples). Locked &#167;2 roster table, &#167;3 id-prefix registry (9 distinct prefixes), &#167;4 c{N} registry (11 overlays; c3/c5/c26 dropped), &#167;1 close-target invariants (figure 20 / figcaption 20 / img 11 / svg 20 / mermaid 1 / em-dash 74 / color222 0).
- **2026-06-21 &#183; L22 SVG PILOT #6 DONE (focal_plane_diagram).** Hand-written to v6 spec: 800x560, `fp` prefix (4 ids), 3px `#FF6B00` top strip, `currentColor` theming, role/aria/`<title>`/`<desc>`, Variant-B footer tip. Top-down idiom: camera at left, blue focus-distance dimension arrow to the orange focal-plane line, green DoF band (1/3 front / 2/3 behind asymmetry shown), 3 sample objects (orange solid dot = in focus; two soft red rings = too near / too far, blurred). Discipline checks PASS: em-dash U+2014 **0**; a11y chain present; `color="#222"` fallback kept (standalone, STRIP on inline at integration). cairosvg render-check PASS light + dark (currentColor flips correctly; brand colors hold on dark). Saved standalone to `images/lesson_22_06_focal_plane_diagram.svg` (5,862 B). NOT yet integrated into HTML (integration is a later phase; live-HTML invariants unchanged at figure 0 / svg 0). **NEXT: SVG batch B1 = #3 (fstop_scale_reference, `fs`), #5 (fstop_dof_chart, `fc`), #26 (aperture_reference_card, `rc`).**

- **2026-06-21 &#183; L22 SVG BATCH B1 DONE (#3, #5, #26).** The three Q2-reassigned infographics, hand-written to v6 spec. **#3 fstop_scale_reference** (`fs`, 800x560): row of 6 aperture irises f/1.4 to f/16, opening shrinks as f-number rises, openings color-keyed green&#8594;yellow&#8594;orange&#8594;red; gradient blur-direction bar (More blur / Less blur); full-stop takeaway. 5,438 B. **#5 fstop_dof_chart** (`fc`, 800x680 dense): 7-row table (f/1.4 to f/16) with aperture glyph (shrinking), DoF-depth bar (growing, color-keyed), typical use, look/feel; zebra rows; header divider. 6,592 B. **#26 aperture_reference_card** (`rc`, 800x680 dense): 5-scenario cheat-sheet (Portrait / Product / Architecture / Landscape / Action) with muted-orange f-stop badge, focal length, focus target, look; zebra rows. 6,739 B. All content sourced from req-doc purpose/rows; req-doc palette (`#ff8c00`/`#00bcd4`) NOT used. v6 brand palette throughout. Discipline checks PASS on all 3: em-dash U+2014 **0**; well-formed XML; a11y chain (role/aria/`<title>`/`<desc>`) present; 3px `#FF6B00` top strip; `color="#222"` standalone fallback kept (STRIP on inline at integration); Variant-B left-rail footer; `&#183;` separators. cairosvg render-check PASS light + dark for all 3 (currentColor flips; brand + semantic colors hold; muted badges legible on dark). Footer tips shortened to clear the x=760 content margin (measured rightmost ink x=724/757/687); #5 f/16 use-label trimmed to clear the LOOK column. Saved standalone to `images/lesson_22_03_fstop_scale_reference.svg`, `images/lesson_22_05_fstop_dof_chart.svg`, `images/lesson_22_26_aperture_reference_card.svg`; on-disk sizes verified via `get_file_info` (5,438 / 6,592 / 6,739 B). NOT yet integrated (integration is a later phase; live-HTML invariants unchanged at figure 0 / svg 0). SVG progress: pilot #6 + B1 (#3/#5/#26) = 4 of 9 inline SVG built. (NEXT after B1 was SVG batch B2 = #14/#15/#16, now done below.)

- **2026-06-21 &#183; L22 SVG BATCH B2 DONE (#14, #15, #16).** The three practical-scenario setup diagrams, hand-written to v6 spec, shared side-profile idiom (800x560 each, per &#167;3): camera glyph at left on a dashed optical axis, orange `#FF6B00` focal-plane line at the focus target, cyan `#4A9EFF` acceptable-sharpness band (1/3 front : 2/3 behind), cyan dimension arrows, muted-orange specs card, Variant-B left-rail footer. Band WIDTH is the teaching variable across the set: narrow (portrait, shallow) &#8594; moderate (product) &#8594; wide spanning all depth markers (architectural, deep). **#14 portrait_dof_setup** (`pd`, 800x560): head/shoulders bust at the plane (eyes marked orange), narrow band, heavy background bokeh; specs 85mm &#183; f/2.8 &#183; focus eyes &#183; BG 8+ units. 5,037 B. **#15 product_dof_setup** (`pr`, 800x560): product on platform (feature dot orange) at the plane, moderate band, soft backdrop; specs 50mm &#183; f/4 &#183; focus key feature &#183; BG 4 to 5 units. (Side-profile not isometric, to keep the three a matched set per &#167;3.) 4,801 B. **#16 architectural_dof_setup** (`ar`, 800x560): foreground 2u / midground 6u / background 12u markers (opacity steps with distance), wide band spanning near to far, focal plane at 1/3 (4u); specs card moved lower-left (y=446) to clear the wide band; specs 24mm &#183; f/11 &#183; focus 1/3 in &#183; max front to back sharp. 4,367 B. All content sourced from req-doc prompts #14/#15/#16 ONLY; req-doc palette (`#2b2b2b`/`#ff8c00`/`#00bcd4`) NOT used (verified 0 hits) &#8212; v6 + `#FF6B00` brand palette throughout. Discipline checks PASS on all 3: em-dash U+2014 **0**; well-formed XML; a11y chain (role/aria/`<title>`/`<desc>`) present; 3px `#FF6B00` top strip; `color="#222"` standalone fallback kept (STRIP on inline at integration); no `--` in XML comments. cairosvg render-check PASS light + dark for all 3 (currentColor flips; orange + cyan hold on dark; specs cards legible). Footer-tip rightmost ink measured x=550 / 549 / 564 (all clear the x=760 margin). Saved standalone to `images/lesson_22_14_portrait_dof_setup.svg`, `images/lesson_22_15_product_dof_setup.svg`, `images/lesson_22_16_architectural_dof_setup.svg`; on-disk sizes verified via `get_file_info` (5,037 / 4,801 / 4,367 B). NOT yet integrated (integration is a later phase; live-HTML invariants unchanged at figure 0 / svg 0). SVG progress: pilot #6 + B1 + B2 = **7 of 9 inline SVG built**. **NEXT: SVG batch B3 = #13 (rack_focus_setup, `rf`), #18 (dof_troubleshooting_flowchart, `tf`) &#8212; then PNG production.**

- **2026-06-21 &#183; L22 SVG BATCH B3 DONE (#13, #18) &#8212; closes the 9 inline SVG.** Hand-written to v6 spec. Content sourced from req-doc prompts #13/#18 ONLY; req-doc palette (`#2b2b2b`/`#ff8c00`/`#00bcd4`) NOT used (verified 0 hits) &#8212; v6 + `#FF6B00` brand palette throughout. **#13 rack_focus_setup** (`rf`, 800x560, top-down idiom per #6 precedent): camera glyph at left on a dashed `currentColor` optical axis; foreground subject at 2u (solid block, Start focus) and background subject at 10u (lighter block, End focus); orange `#FF6B00` dashed Focus-Empty path arrow sweeping foreground&#8594;background (the rack) with Focus-Empty plus-glyph and "animates near to far (the rack)" label (orange = tie-break highlight for the animated path); faint orange focal-plane ticks at each subject; cyan `#4A9EFF` 2u / 10u dimension arrows with drop-lines; Variant-B footer tip. "optical axis" label moved to x=540 (clear gap) to clear the background-subject block. 5,152 B. **#18 dof_troubleshooting_flowchart** (`tf`, 800x680 dense decision tree): red `#E63946` "DoF not working?" problem entry; 4 `currentColor` check nodes down the left spine (DoF enabled? &#183; Focus distance correct? &#183; Aperture wide enough? &#183; Enough render samples?) each with a sub-line; Yes continues down the spine, No branches right to a green `#4ADE80` solution node (Enable F-Stop or Focus Object &#183; Use Limits display to check &#183; Try f/2.8 or wider &#183; Increase to 256+ samples); green terminal "DoF renders correctly"; Problem/Check/Solution legend; Yes/No edge labels; connectors + arrowheads in `currentColor` opacity 0.45; Variant-B dense footer tip (y=641). 6,849 B. Per the locked spec: warning-red `#E63946` problem node, positive-green `#4ADE80` solution nodes, `currentColor` question nodes/connectors. Discipline checks PASS on both: em-dash U+2014 **0**; well-formed XML; a11y chain (role/aria/`<title>`/`<desc>`) present; 3px `#FF6B00` top strip; `color="#222"` standalone fallback kept (STRIP on inline at integration); no `--` in XML comments. cairosvg render-check PASS light + dark for both (currentColor flips: titles, camera glyph, subjects, axis, check-node borders, Yes/No labels all flip; orange + cyan + green + red hold on dark; solution-node dark badge text legible). Footer tips clear the x=760 content margin (#13 ends ~x=680, #18 ~x=665). Saved standalone to `images/lesson_22_13_rack_focus_setup.svg`, `images/lesson_22_18_dof_troubleshooting_flowchart.svg`; on-disk sizes verified via `get_file_info` (5,152 / 6,849 B, byte-exact vs Claude-side). NOT yet integrated (integration is a later phase; live-HTML invariants unchanged at figure 0 / svg 0). **SVG progress: pilot #6 + B1 + B2 + B3 = 9 of 9 inline SVG built &#183; SVG production COMPLETE.** **NEXT: PNG production (11 PNG per &#167;5 routing: DoF renders #2/#9/#19/#22/#28 real camera DoF + high-freq FG/BG, #9 bokeh + #22 samples on Cycles high samples; UI-chrome #7/#11/#12/#23/#24/#29 full-window local-tempdir screenshot + bridge-side Pillow crop). Confirm Blender get_scene_info true-pristine BEFORE and AFTER any bridge work; restore at close.**

- **2026-06-21 &#183; L22 PNG #2 DoF STAGING (DoF-render set OPENED, NOT yet delivered).** Confirmed Blender TRUE-PRISTINE before and after (Camera+Cube+Light, 0 mats/0 ngroups, EEVEE/AgX/None/exp0, res 1920/1080/100, world 0.88/1.0, DoF OFF, POINT 1000, lens 50). Built shared DoF scene per Q4 sign-off: three greebled cube-cluster subjects (high-freq edges, NOT smooth spheres) on a ground plane, spread near-left / center / far-right (Ray-picked classic-DoF-demo composition), focus Empty on center subject. **KEY FINDING (carry forward, saves the next chat the iteration):** at these subject distances (subjects 5 to 15 m from camera, 50 mm lens, 800x450 to 1600x900 render) the camera-DoF blur is sub-pixel and does NOT read at figure scale &#8212; verified numerically (f/1.8 vs f/16 mean abs diff 0.05 to 0.09, frac pixels differing 0.0005 to 0.0018) and visually (aperture ladder f/0.5 | f/1.4 | f/4 | f/16 composited side by side ALL look identical). EEVEE DoF does not respond to f-stop at this scale even with bokeh_max_size 200 / overblur 10 (made it WORSE); Cycles (OPTIX GPU, 96 to 128 samples + denoise) responds correctly but the blur is still inherently subtle because the geometry is in the DEEP-DoF regime (distant subjects). A single f/0.5 frame viewed in isolation looked soft but that was small-distant-subject softness, not a DoF gradient. **RESOLUTION FOR NEXT CHAT (Ray-endorsed direction):** RE-STAGE CLOSE-UP &#8212; put the subjects at ~1.5 / 2.5 / 6 m from the camera (portrait/macro geometry) where a wide aperture produces physically LARGE, obvious blur, then shallow vs deep reads dramatically. Keep greebled cube clusters + ground plane + three-subject near/center/far spread + Cycles render. Likely f/2.8 (or wider) shallow vs f/16 deep once close-up; VALIDATE the pair by compositing a small aperture ladder and viewing before committing the full #2/#19/#28 set. All temps + the REJECTED lesson_22_02_shallow_deep_comparison.png removed via bridge os.remove; NOTHING delivered to disk this chat. Composite recipe still locked (L20 convention: LANCZOS panels, (36,36,38) label band per cell, 3px #FF6B00 top strip, 2px orange divider, NO baked text; labels are c{N} overlays at integration). DoF-render frame aspect LOCKED 16:9 1600x900 per-panel (Ray pick). Live-HTML invariants UNCHANGED (figure 0 / svg 0; integration is still a later phase). **NEXT: re-stage close-up DoF scene, deliver #2, then #19 / #28 and #9 / #22 (Cycles hi-samples), then UI-chrome set #7/#11/#12/#23/#24/#29.**

- **2026-06-21 &#183; L22 PNG #2 DELIVERED (shallow_deep_comparison).** Re-staged CLOSE-UP per the prior bullet and delivered #2. **Root-cause found (carry forward, supersedes the prior "just go close-up" note):** close-up alone did NOT fix it. Three coupled problems had to be solved together: (1) **DoF depth math** &#8212; at 50 mm / focus 4 m even f/1.4 keeps a 0.58 m DoF and f/16 covers 16+ m, so subjects 2.8 to 7.8 m apart barely differ shallow-vs-deep. Verified via the lens DoF formula in-bridge; (2) **exposure washout** &#8212; AgX + world 0.88 str 1.0 + POINT light 1000 blew every mid-grey to near-white, so even real blur shifted pixels only ~0.1 to 0.2 MAD and read as identical panels. The fix is **LD17 recipe: view_transform Standard / look None + dark world (str 0.11, color 0.05) + POINT light retuned to 420 (was 1000, double-lit with the world)**; (3) **framing** &#8212; changing focal length re-scattered subjects every time; locked composition ONCE at 50 mm. Final locked scene: 50 mm, subjects DOF_Near 1.95 m (scale 0.8) / DOF_Center 3.35 m (scale 1.0, FOCUS target) / DOF_Far 6.33 m (scale 1.25), mid-grey contrast mats (0.55 / 0.80 / 0.45) on a dark 0.20 ground, POINT light 420 soft 1.5 at (3.5,-2.5,4.0). Aperture ladder f/1.4 | f/2.8 | f/5.6 | f/16 rendered + composited + viewed: clean monotonic blur spread (MAD 0.294 / 0.121 / 0.047 / 0; f/1.4 frac>12 = 0.0043, ~10x any prior attempt). **Picked f/1.4 shallow vs f/16 deep** (most dramatic at figure scale; within the Q4 "f/2.8 or wider" sign-off). Final panels Cycles OPTIX GPU 192 samples + denoise, 1600x900 each. Composited L20 recipe (LANCZOS, (36,36,38) 64px label band per cell, 3px #FF6B00 top strip, 2px orange divider, NO baked text). Saved `images/lesson_22_02_shallow_deep_comparison.png` (1,350,569 B, 3202x967) &#8212; `get_file_info` verified. Viewed final: left f/1.4 center-sharp with soft far cluster + foreground, right f/16 sharp front-to-back; aperture the only change. All 34 temps removed via bridge os.remove. **Blender scene KEPT STAGED (not torn down)** as the shared asset for #19/#28/#9/#22 per the cross-sub-chat retention pattern; full reproduce + pristine-restore spec saved to `images/_l22_dof_scene_recipe.json` (restores AgX + world 0.88/1.0 + light 1000 + cam pristine loc/rot + DoF OFF, deletes DOF_* objs/mats, unhides Cube render). Live-HTML invariants UNCHANGED (figure 0 / svg 0; integration still a later phase). **NEXT: reuse the staged scene (or rebuild from `_l22_dof_scene_recipe.json`) for #19 cycles_eevee_comparison + #28 wrong_focus_examples, then #9 bokeh_blade_examples + #22 sample_count_comparison (Cycles hi-samples; #9 needs a bg point-light/emissive-dot array for bokeh discs, #22's 64-sample panel SHOULD be noisy = the teaching). Then UI-chrome #7/#11/#12/#23/#24/#29. At DoF-BATCH close, restore true-pristine from the recipe JSON and re-verify.**

- **2026-06-21 &#183; L22 PNG #19 DELIVERED (cycles_eevee_comparison).** Reused the staged DoF scene (verified intact via get_scene_info + render-config check: CYCLES/GPU, Standard/None, world 0.11/(0.05,0.05,0.06), POINT 420, 50mm, DoF ON focus=DOF_Focus, res 1600x900, samples 192, Cube hidden). Same scene, same focus, same aperture; ONLY the engine changes (the teaching). Picked **f/1.4 for BOTH panels** (most dramatic engine gap at figure scale; matches the #2 aperture call; f/16 would make both engines near-identical). Engine id on this 5.1.1 build is **`BLENDER_EEVEE`** (not `BLENDER_EEVEE_NEXT`). Panel L Cycles OPTIX/GPU 192 + denoise (identical to #2 settings so engine is the only cross-figure variable); Panel R EEVEE with camera DoF read directly, bokeh params left at DEFAULTS (cranking bokeh_max_size/overblur made it worse per the #2-staging finding). Engine RESTORED to CYCLES immediately after the EEVEE render. Difference verified numerically (Cycles vs EEVEE MAD 0.0183, frac>12 = 0.0626, well above the ~0.0005 to 0.0018 no-read floor) and visually: Cycles gives smooth optical falloff; EEVEE screen-space DoF reads thinner/harder with edge-bite artifacts on the far cluster (= why production DoF goes to Cycles). Composited L20 convention (LANCZOS panels, (36,36,38) 64px label band per cell, 3px #FF6B00 top strip, single 2px orange divider, NO baked text; labels are the c19 overlay at integration). Saved `images/lesson_22_19_cycles_eevee_comparison.png` (1,618,492 B, 3202x967) &#8212; get_file_info-comparable size confirmed at save. All 4 temps removed via bridge os.remove. **Blender scene KEPT STAGED on CYCLES** (8 objs, DoF on, samples 192) for #28/#9/#22. Live-HTML invariants UNCHANGED (figure 0 / svg 0; integration still a later phase). **NEXT: #28 wrong_focus_examples (reuse staged scene, problem/solution focus pairs, DoF-render composite), then #9 bokeh_blade_examples + #22 sample_count_comparison (Cycles hi-samples; #9 needs a bg point-light/emissive-dot array for bokeh discs, #22's 64-sample panel SHOULD be noisy = the teaching). Then UI-chrome #7/#11/#12/#23/#24/#29. At DoF-BATCH close, restore true-pristine from `_l22_dof_scene_recipe.json` and re-verify.**

- **2026-06-21 &#183; L22 PNG #28 DELIVERED (wrong_focus_examples).** Reused the staged DoF scene (verified intact via get_scene_info + render-config check before start: 8 objs / 4 mats, CYCLES/GPU, Standard/None, world 0.11, POINT 420, 50mm, DoF ON focus=DOF_Focus, 192 samples, 1600x900, Cube hidden). Figure is a 2x2 problem/solution composite, pairs side-by-side per row (Ray pick): **top row Pair A** (problem: focus on DOF_Far so the intended NEAR subject is blurred = wrong | solution: focus on DOF_Near = right); **bottom row Pair B** (problem: focus on DOF_Near so the intended FAR subject is blurred = wrong | solution: focus on DOF_Far = right). Two opposite failure modes (focus-too-far ruins foreground subject; focus-too-near ruins background subject), more teaching than two variants of one error. f/1.4 (Ray pick, hardest-reading error). **KEY FINDING (carry forward):** the staged #2/#19 composition does NOT work for #28 unchanged. That framing centers DOF_Center as a dominant bright slab (NOT a focus target in any pair, so soft in all four panels) and projects DOF_Near off-frame left (world_to_camera_view x=-0.194), so the near-subject wrong/right flip had almost nothing visible to read (NEAR-region MAD 0.0026, barely above the ~0.002 floor; whole-frame MAD 0.0041, far below #2's 0.294). #28 is its OWN teaching figure (which subject is sharp), not an aperture/engine A/B against #2/#19, so a per-figure camera re-stage is correct. **Re-staged camera for #28 ONLY:** 40mm, cam loc (-0.9,-3.1,1.35) aimed at a near-weighted midpoint, DOF_Center temporarily scaled 0.6 so it stops blocking/dominating; this frames all three subjects on a clean diagonal (near depth 3.31m bottom-left foreground / center 4.97m / far 7.9m upper-right) and the foreground subject is the clear sharp-vs-blurred tell between panels. Verified by view BEFORE full render (modest region MAD 0.0064 near / 0.0036 far is background-and-ground dominated; subject EDGES flip clearly = reads at figure scale, per the L21 LD that good/bad comparisons can be pixel-correct yet read weakly &#8212; checked visually not just numerically). Final 4 panels Cycles OPTIX/GPU 192 + denoise, 1600x900 each. Composited L20 convention (LANCZOS panels, (36,36,38) 64px label band per cell, 3px #FF6B00 top strip, 2px orange dividers BOTH axes for the 2x2, NO baked text; per-pair problem red-X / solution green-check badges are the c28 overlay at integration). Saved `images/lesson_22_28_wrong_focus_examples.png` (2,567,185 B, 3202x1933) &#8212; get_file_info verified. **Staged scene RESTORED EXACTLY after #28** (recorded staged cam loc/rot/lens/fstop + focus-on-Center + center scale 1.0 before re-staging, restored from that record; the #28 re-stage was transient and left no trace) and re-verified via get_scene_info (8 objs / 4 mats, cam 50mm at (-0.15,-1.6,0.97), focus on DOF_Center, center scale 1.0, CYCLES/Standard, f/1.4). All 15 temps (Windows tempdir renders + WSL preview temps) removed via bridge os.remove. Live-HTML invariants UNCHANGED (figure 0 / svg 0; integration still a later phase). **NEXT: #9 bokeh_blade_examples + #22 sample_count_comparison (Cycles hi-samples; #9 needs a bg point-light/emissive-dot array behind the BG cluster for bokeh discs, blade-count 5/7/9/12 the variable; #22's 64-sample panel SHOULD be noisy in the blur zone = the teaching). Then UI-chrome #7/#11/#12/#23/#24/#29. At DoF-BATCH close, restore true-pristine from `_l22_dof_scene_recipe.json` and re-verify.**

- **2026-06-21 &#183; L22 PNG #9 DELIVERED (bokeh_blade_examples).** Reused the staged DoF scene + ADDED a background emissive-dot array (the staged scene has no bright highlights, so nothing to form bokeh discs). 2x2 composite, blade count the only variable: **TL 5 / TR 7 / BL 9 / BR 12** blades. **KEY FINDINGS (carry forward):** (1) bokeh discs only take the aperture-polygon SHAPE when strongly defocused AND large enough on frame; small or near-focus highlights render as plain round blobs and teach nothing. (2) **`cam.dof.aperture_blades=0` is the default CIRCULAR aperture; you MUST set blades >= 3 to get polygonal bokeh.** (3) **When `cam.dof.focus_object` is assigned, `cam.dof.focus_distance` is IGNORED** &#8212; the early tests looked round because focus sat on the dot grid via the focus object (focus_distance read 10.0 but was inert); fix = detach focus_object and set focus_distance directly (used 0.6m) so the 9m dot grid is massively defocused into large discs. (4) emission ~80 blows discs to flat white and softens the polygon corners; dropped to ~22 so edges stay crisp. Final recipe: 15 emissive spheres (radius 0.12, scale 1.5, DOF_Bokeh_M emission 22) on a staggered grid at Y~9m behind the subjects; camera 50mm at (-0.15,-2.2,1.1) aimed at the dot field; focus_distance 0.6m (focus_object DETACHED for the render); f/1.4; Cycles OPTIX/GPU **256 samples** + denoise; 1600x900 per panel. Progression reads: 5 = clearly polygonal (pentagon edges), 7 softer, 9 nearly round, 12 essentially circular (physically correct; 9-vs-12 is subtle by nature, c9 labels disambiguate at integration). Composited L20 convention (LANCZOS, (36,36,38) 64px band per cell, 3px #FF6B00 strip, 2px orange dividers both axes, NO baked text; c9 per-panel 5/7/9/12 labels are the integration overlay). Saved `images/lesson_22_09_bokeh_blade_examples.png` (2,861,536 B, 3202x1933) &#8212; get_file_info verified. **Staged scene RESTORED after #9** (bokeh dots + DOF_Bokeh_M removed; camera/focus_object/blades/samples returned to staged recipe) and re-verified get_scene_info (8 objs / 4 mats). Live-HTML invariants UNCHANGED.

- **2026-06-21 &#183; L22 PNG #22 BLOCKED (sample_count_comparison) &#8212; needs a Ray decision before delivery.** Attempted the locked recipe (same DoF scene, focus DOF_Center, f/1.4, four panels at 64/128/256/512 samples, denoise OFF so noise shows). **The teaching does NOT render:** all four panels look identical at figure scale because this clean clay scene under a single bright direct POINT light (energy 420) converges almost immediately in Cycles/OPTIX &#8212; 64 spp is already visually clean. Verified numerically (64-vs-512 MAD only 0.0022, ~the no-read floor) and visually (contact sheet: 64/128/256/512 indistinguishable). Tried two noise-raising levers, both insufficient at figure scale: (a) dimming the light to 40 (MAD 0.0015 &#8212; direct light is low-variance regardless of brightness, converges fast); (b) big soft-light radius 6.0 for noisy penumbra (MAD 0.0035, still not figure-legible). **Diagnosis:** Cycles render noise lives in INDIRECT light (multi-bounce GI, large soft shadows, volumetrics, caustics); a direct-lit clay scene won't produce figure-scale grain at any reasonable sample split. **OPTIONS for Ray (pick one next chat):** (1) RELIGHT #22 only as an indirect-dominated scene (kill the direct lamp; light by a bright emissive panel or strong world bounce) so low spp yields obvious grain/fireflies, restore staged scene after; (2) add a volumetric (thin world/object volume) which is notoriously noisy at low spp; (3) drop the sample ladder much lower (e.g. 4/16/64/256) AND relight, since 64 is already clean here; (4) re-scope #22 to a different teaching framing (e.g. denoiser ON vs OFF at fixed low spp, which reads more reliably than a sample ladder on an easy scene). Staged scene was RESTORED untouched after the attempts (light 420/soft 1.5, samples 192, denoise ON, blades 0, focus_object DOF_Focus) and get_scene_info re-verified (8 objs / 4 mats). NOTHING delivered to disk for #22. Live-HTML invariants UNCHANGED.

- **2026-06-21 &#183; L22 PNG #22 DELIVERED (sample_count_comparison) &#8212; UNBLOCKED via indirect relight + lowered ladder (Ray-picked hybrid a+c).** Root fix per the BLOCKED bullet diagnosis (Cycles noise needs INDIRECT light): for #22 ONLY, hid the direct POINT lamp from render (`light.hide_render = True`) and lit the scene by a single large out-of-frame emissive panel (`DOF_EmitPanel`, 6m x 4m grid at loc (-4.5,2.3,4.8) rot (0.95,-0.7,0), material `DOF_Emit_M` emission color (1.0,0.97,0.92) strength **12**, validated out-of-frame via world_to_camera_view uv (-1.47,3.81)). Emission tuned by exposure checks (str 5 too dark p50=43; str 11 p50=64; str 13 clipped max255 frac>250=0.006; locked **12**, p50~66 max<255 no clip). Ladder lowered to **16 / 64 / 256 / 1024 spp** (Ray-picked), **denoise OFF** all four, same staged camera/focus/aperture (50mm, focus DOF_Center, f/1.4) so the defocused far cluster is the blur zone where grain reads. Indirect lighting produced a clean monotonic noise spread: MAD-vs-1024 = 0.0152 / 0.0074 / 0.0048 / 0 (16-vs-1024 **0.0152**, ~7x the ~0.0022 no-read floor that BLOCKED the direct-lit attempt); local-grain proxy fell monotonically 5.52 / 2.79 / 1.80 / 1.64. Contact sheet viewed: 16 visibly grainy across clay faces + ground + blur zone, 64 noticeably cleaner, 256 nearly smooth, 1024 fully clean &#8212; reads at figure scale. Composited L20 convention (LANCZOS panels, (36,36,38) 64px label band per cell, 3px #FF6B00 top strip, 2px orange dividers BOTH axes for the 2x2, NO baked text; c22 per-panel labels now **16/64/256/1024** are the integration overlay). Saved `images/lesson_22_22_sample_count_comparison.png` (6,062,251 B, 3202x1933) &#8212; `get_file_info` verified. **c22 overlay registry (&#167;4) ladder label updated 64/128/256/512 &#8594; 16/64/256/1024** to match; caption/overlay at integration must use the new ladder. All 6 temps (4 Windows-tempdir ladder renders + 2 WSL preview temps) removed via bridge os.remove. **Staged scene RESTORED EXACTLY** (DOF_EmitPanel + DOF_Emit_M deleted, lamp unhidden energy 420 soft 1.5, samples 192, denoise ON) and get_scene_info re-verified true to recipe (8 objs / 4 mats, CYCLES/Standard/None, DoF on focus=DOF_Focus f/1.4 blades 0, Cube hidden). Live-HTML invariants UNCHANGED (figure 0 / svg 0; integration still a later phase). **DoF-render batch now COMPLETE (#2/#19/#28/#9/#22 all delivered, 5 of 5).** **NEXT: apply the PRISTINE_RESTORE block from `images/_l22_dof_scene_recipe.json` and re-verify get_scene_info true-pristine, then UI-chrome PNGs #7/#11/#12/#23/#24/#29 (full-window local-tempdir screenshot + bridge-side Pillow crop). Then INTEGRATION (inline 9 SVG + place 11 PNG with c{N} overlays), then browser QA.**
- **2026-06-21 &#183; L22 UI-CHROME PNG BATCH DELIVERED (6/6: #7/#11/#12/#23/#24/#29) &#8212; closes ALL 11 PNG.** Confirmed Blender true-pristine at open (3 objs, EEVEE/AgX/None, world 0.88/1.0, POINT 1000, DoF OFF, lens 50; corrected two stale DoF-batch leftovers on the Light: loc (3.5,-2.5,4.0)&#8594;factory (4.076,1.005,5.904) and shadow_soft_size 1.5&#8594;0.25, everything else already pristine). Captured via Locked-decision-20 path: full-window `screen.screenshot` to Windows tempdir &#8594; bridge-side Pillow crop in place using area.x/y/w/h with bottom-left&#8594;top-left origin flip (WIN 1920x1046; Properties area x1299 y35 w618 h792). Panel expand/collapse + Graph-Editor setup is NOT bridge-drivable (no RNA for panel show_expanded); Ray expanded panels, Claude ran all screenshot+crop+composite+overlay framing. **Base A (Camera Data &#183; Depth of Field panel, scene-configured DoF on / Empty_Focus assigned / f2.8 / 7 blades / ratio 1.0):** **#7 focus_object_setup** crop of DoF header + Focus-on-Object (Empty_Focus) + greyed Focus Distance rows &#8212; `images/lesson_22_07_focus_object_setup.png` (558x108, 12,628 B). **#11 camera_dof_settings_panel** full DoF panel (F-Stop 2.8, Focus Object, Focus Distance 5.25m, Aperture: Blades 7 / Rotation 0 / Ratio 1.000) &#8212; `images/lesson_22_11_camera_dof_settings_panel.png` (558x296, 24,619 B). **#23 focus_distance_methods_comparison** L20 composite (LANCZOS, 3px #FF6B00 top strip, (36,36,38) 64px label band per cell, 2px orange divider, NO baked text): LEFT manual-Distance method (focus_object cleared &#8594; Distance field active/white) vs RIGHT Focus-Object method (Empty_Focus assigned &#8594; Distance greyed) &#8212; `images/lesson_22_23_focus_distance_methods_comparison.png` (1118x179, 19,408 B). **Base B (Render Properties &#183; Sampling, engine&#8594;CYCLES, samples 256 / preview 64 / denoise on):** **#29 render_samples_settings_location** Sampling block crop (Viewport Max Samples 64 / Render Max Samples 256 / Denoise checked) &#8212; `images/lesson_22_29_render_samples_settings_location.png` (558x442, 34,942 B). **Base C (Graph Editor &#183; rack-focus fcurve, bridge-built):** keyframed `camera.data.dof.focus_distance` frame 1 = 2.0 (FG) / frame 60 = 9.0 (BG), BEZIER + AUTO_CLAMPED handles &#8594; ease-in/out S-curve (5.1 slotted Action: fcurve under action.layers[0].strips[0].channelbags[0].fcurves; switched VIEW_3D area to GRAPH_EDITOR via area.type, graph.select_all + view_all, show_handles on, reverted to VIEW_3D after). **#12 rack_focus_keyframe_timeline** curve + both keyframes + handles + playhead at frame 30 + frame-number ruler (menu strip trimmed, sidebar cropped out) &#8212; `images/lesson_22_12_rack_focus_keyframe_timeline.png` (555x869, 20,353 B). **#24 rack_focus_animation_curve** clean S-curve shape (playhead parked at frame -20 off-curve; sidebar cropped) for the ease-in/fast-middle/ease-out teaching &#8212; `images/lesson_22_24_rack_focus_animation_curve.png` (555x869, 20,349 B). All deliverables `get_file_info`-verified. NO baked text on any (all numbered callouts / method labels / pros-cons check-X / easing labels are the c{N} INTEGRATION overlays per Locked decision 10). All Windows-tempdir + WSL preview temps removed via bridge os.remove. **Blender FULL-RESTORED to true-pristine** (cleared camera animation + 0 orphan actions, deleted Empty_Focus, DoF OFF / blades 0 / lens 50, engine&#8594;EEVEE, AgX/None/exp0, world 0.88/1.0, light 1000/soft 0.25/factory loc, cam factory loc-rot, Cube unhidden, res 1920x1080@100, frame range reset, Cycles samples reset, Properties tab&#8594;RENDER) and get_scene_info re-verified (3 objs / 1 mesh / 0 mats / 0 ngroups / 0 actions / 0 orphans / 0 empties). Live-HTML invariants UNCHANGED (figure 0 / svg 0; integration is the next phase). **PNG PRODUCTION COMPLETE (11/11: DoF renders #2/#9/#19/#22/#28 + UI-chrome #7/#11/#12/#23/#24/#29). SVG COMPLETE (9/9). NEXT: INTEGRATION &#8212; inline 9 SVG + place 11 PNG with c{N} overlays into lesson_22_depth_of_field_and_focus.html (pixel-verify each overlay before commit; c22 ladder label = 16/64/256/1024), then browser QA at http://localhost:5504/lesson_22_depth_of_field_and_focus.html.**

- **2026-06-21 &#183; L22 UI-CHROME PNG BATCH COMPLETE (#7/#11/#23/#29 produced this chat; #12/#24 confirmed on disk) &#8212; ALL 11 PNG NOW DELIVERED.** Confirmed Blender true-pristine at chat open (corrected two stale DoF-batch leftovers on the default Light that the prior restore missed: location (3.5,-2.5,4.0) &#8594; factory (4.076,1.005,5.904); shadow_soft_size 1.5 &#8594; 0.25; all else already pristine). Capture mechanic per IMAGE_STYLE_GUIDE Locked decision 20 + L08 local-tempdir note: full-window `screen.screenshot` to Windows tempdir (carries docked-panel overlays) &#8594; Pillow crop in place to deliverable via `area.x/y/width/height` with bottom-left&#8594;top-left origin flip (left=ax, top=WIN_H-(ay+ah)); NO baked text (labels are c{N} overlays at integration). DRAW_WIN_SWAP x3 flush after every tab/area change; Properties `space.context` set via the bridge after a redraw flush (the context enum is stale until the first DRAW_WIN_SWAP, then exposes DATA/RENDER/etc). **KEY FINDING (carry forward):** panel expand/collapse and editor-area framing CANNOT be driven from the bridge (no RNA for panel `show_expanded`; no label-based UI op) &#8212; Ray expands panels / sets up editors manually, Claude does all screenshot+crop+composite+verify. **Base A** (Camera-Data tab, DoF panel expanded: Focus on Object=Empty_Focus, Focus Distance 5.25, Aperture F-Stop 2.8 / Blades 7 / Rotation 0 / Ratio 1.0) served #7, #11, #23. **#7 focus_object_setup** crop = DoF header + Focus subsection (558x108). **#11 camera_dof_settings_panel** crop = full DoF panel header-through-Ratio (558x296). **#23 focus_distance_methods_comparison** = L20 composite (LANCZOS, (36,36,38) 64px band per cell, 3px #FF6B00 top strip, 2px orange divider, NO baked text): LEFT manual-distance (focus_object cleared &#8594; Focus Distance field active/white) | RIGHT focus-object (Empty assigned &#8594; Distance greyed); 1118x179. **Base B** (Render tab, engine CYCLES, Sampling panel expanded) served **#29 render_samples_settings_location** = Sampling block, Viewport Max Samples 64 + Render Max Samples 256 + Denoise (558x442). **Base C** (big area switched to GRAPH_EDITOR via the bridge; rack-focus fcurve keyframed on `dof.focus_distance` frame 1=2.0 / frame 60=9.0, BEZIER AUTO_CLAMPED handles = ease-in/out S-curve; bridge `graph.select_all`+`graph.view_all` framed it cleanly, no manual step) produced **#12 rack_focus_keyframe_timeline** (playhead frame 30, mid-transition) + **#24 rack_focus_animation_curve** (full-curve easing view); both sidebar-cropped (Ray pick), ~20.3 KB each, verified visually. **NOTE on #12/#24 provenance:** a transient bridge `Communication error` mid-Base-C triggered a multi-step Blender UNDO that reverted the SCENE state (engine&#8594;EEVEE, Empty_Focus removed, DoF off, keyframes gone, big area&#8594;VIEW_3D) but did NOT undo the already-written #12/#24 PNG files on disk (file writes are not undone by Blender undo). Confirmed via get_file_info timestamps (17:22/17:23 this session) + visual check &#8594; accepted as-is (Ray-endorsed). Delivered files #7/#11/#23/#29 (written 17:11&#8211;17:18) were likewise unaffected. All Windows-tempdir + WSL preview temps removed via bridge os.remove (the rollback also discarded the Base C temps). c22 caption note unchanged. **Blender RESTORED to true-pristine and re-verified via get_scene_info** (3 objs Camera+Cube+Light, Light at factory (4.08,1.01,5.9) energy 1000 soft 0.25, 1 mesh / 0 mats / 0 node_groups / 0 orphans, EEVEE/AgX/None/exp0, res 1920x1080@100, world 0.88/1.0, DoF OFF, lens 50, no cam animation, Cube unhidden, frame range 1&#8211;250). Live-HTML invariants UNCHANGED (figure 0 / svg 0; integration still a later phase). **PNG production COMPLETE: all 11 delivered (DoF renders #2/#9/#19/#22/#28 + UI-chrome #7/#11/#12/#23/#24/#29).** **NEXT: INTEGRATION (separate phase) &#8212; inline the 9 SVG + place the 11 PNG with c{N} overlays into `lesson_22_depth_of_field_and_focus.html`; pixel-verify each c{N} overlay (cairosvg overlay-over-PNG at viewBox=crop-px, 1:1) BEFORE each commit; edit via dryRun&#8594;commit&#8594;get_file_info when available else read-full&#8594;edit-in-context&#8594;write_file overwrite showing diffs; preserve all 74 literal U+2014 + mermaid 1; target figure 20 / figcaption 20 / img 11 / svg 20 / mermaid 1 / em-dash 74 / color222 0. Then browser QA at http://localhost:5504/lesson_22_depth_of_field_and_focus.html.**

- **2026-06-22 &#183; L22 INTEGRATION COMPLETE (Phase A 9 SVG + Phase B 11 PNG + Phase C browser QA) &#8212; LESSON CLOSED.** All 9 inline SVG (Phase A) and all 11 PNG overlays (Phase B) integrated into `lesson_22_depth_of_field_and_focus.html`, one commit per figure, each PNG overlay cairosvg pixel-verified (overlay-over-PNG at viewBox=PNG-px, 1:1) BEFORE commit, invariants re-counted after every commit. **Final invariants (verified UTF-8-aware Python): bytes 280,696 &#183; figure 20 / figcaption 20 &#183; img 11 &#183; svg 20 (9 inline + 11 overlay) &#183; mermaid 1 &#183; em-dash U+2014 74 &#183; color222 0** &#8212; matches the &#167;1 close-target exactly. All 11 PNG `src` present exactly once; all 20 `<figure>`/`</figure>`, `<figcaption>`, `<svg>`/`</svg>` balanced; structural lint 0 malformed (every overlay has substantive alt + figcaption). **PNG overlay placements (section / anchor):** #2 dof-fundamentals (after Shallow-vs-Deep card, before Why DoF Matters); #19 troubleshooting-dof (after Render Engine Behavior card, before Troubleshooting Checklist); #28 troubleshooting-dof (after Wrong-Part-Sharp card, before Bokeh Artifacts); #9 bokeh (after Bokeh-in-Blender card, before Creating Bokeh Highlights); #22 troubleshooting-dof (after Grainy/Noisy card, before Subject Out of Focus); #7 focus-distance (after Setting-Focus-Distance card, before Focus and Subject Placement); #23 focus-distance (immediately after #7); #11 bokeh (in-card, after "Camera Properties → Depth of Field:" p, before the controls ul); #12 rack-focus (after Setting-Up-Rack-Focus card, before Timing and Pacing) &#8212; tall portrait, wrapper `max-width:560px; margin:0 auto`; #24 rack-focus (after Timing-and-Pacing card, before Rack Focus and Story) &#8212; tall portrait, same 560px wrapper; #29 troubleshooting-dof (after DoF-Not-Visible card, before DoF Too Strong/Weak). c{N} overlays per &#167;4 (green #4ADE80 / red #E63946 / orange #FF6B00; c22 ladder 16/64/256/1024). **Phase C browser QA: PASS** &#8212; rendered the 11 overlay figures with the real PNGs in headless Chromium (Playwright) at 380px (mobile) and 900px (desktop) columns; every overlay tracks its image 1:1 at both widths (guaranteed by viewBox=PNG-dims + position:relative wrapper + width/height:100% overlay; all 11 PNG on-disk dims confirmed === their overlay viewBoxes). Tall #12/#24 fill-and-center correctly (render larger/crisper at narrow than wide, ideal mobile behavior). No misalignment, clipping, or overflow at either width. Blender remained true-pristine throughout (integration is HTML-only; no bridge work). **L22 fully closed: SVG 9/9 + PNG 11/11 produced + integrated + QA passed.**

---

## Resolved decisions record (Q1&#8211;Q4, 2026-06-21)

- **Q1 &#183; Roster size / tiers** &#8594; RESOLVED: High+Medium 20, defer Lower 10. Matches L20/L21.
- **Q2 &#183; Infographic type reassignment** &#8594; RESOLVED YES: #3 (f-stop scale), #5 (f-stop&#8594;DoF chart), #26 (aperture reference card) move PNG&#8594;inline SVG. Roster split becomes 9 inline SVG + 11 PNG; c3/c5/c26 overlay rows dropped.
- **Q3 &#183; #30 gallery** &#8594; RESOLVED: leave DEFERRED. Portfolio inspiration, not teaching; most expensive (4 DoF project renders); teaching covered by in-roster DoF renders. Promote later only if Ray asks.
- **Q4 &#183; Per-PNG DoF render recipe** &#8594; RESOLVED CONFIRMED: #2/#9/#19/#22/#28 use real camera DoF + high-frequency-detail FG/BG (cube clusters, not smooth spheres); #9 bokeh and #22 sample-count on Cycles + high samples.
