# Lesson 44 — Scene Organization — Integration Progress

**Module 10, lesson 4.** Live HTML: `lesson_44_scene_organization.html`
Project root: `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`

---

## Current state

**PHASE 0 COMPLETE + HARD-VERIFIED (2026-07-03).** Live HTML modernized (purple neutralized, table wrapped, em-dash literals → entities) and confirmed content-structurally sound (no content fix needed — TOC 10/10 already resolve to 10 real sections). Live HTML now **191,869 B / md5 `1bf0ac9a0d4c4a5a9aee90b5de1377cf` / LF-only**, was 192,075 B original. Written server-side (Blender-bridge host sees the WSL FS directly) binary LF; `get_file_info` == 191,869; fresh independent Filesystem re-read re-asserted ALL invariants — all pass. **TRUE-PRISTINE (0 figures/img/svg).** ROSTER TRIAGE COMPLETE (see §Roster triage): **13 inline SVG / 0 PNG**. **SVG BATCH PRODUCTION COMPLETE (2026-07-03): 13/13 built + EYE-CONFIRMED + logged** (pilot #01 + batch-1 {02,03,04} + batch-2 {05,06,07} + batch-3 {08,09,10} + batch-4 {11,12,13}), all `images/lesson_44_NN_slug.svg`, byte-exact LF, md5 round-trip verified. **INTEGRATION COMPLETE (2026-07-03).** Live HTML `lesson_44_scene_organization.html` now **269,999 B / md5 `fa81f3e0e06c50bc6cbed5b922bc6df8` / LF-only**. All 13 SVGs inlined (insertion anchors: 12 after `<h3>` tags + 1 after the Linking vs. Appending `</ul>` in project-structure), each root `color="#222"` stripped, font-family already present (no injection needed), each wrapped in `<figure>`+`<figcaption>`. Written server-side via Blender-bridge (git-reconstructed Phase-0 baseline + re-applied transforms + SVG insertion, md5-matched before commit). Fresh Filesystem re-read hard-asserted ALL post-integration invariants — ALL PASS: figure 13 / figcaption 13 / img 0 / inline svg 13 / </svg> 13 / aria-labelledby 13 / overlay-bar 0 / U+2014 literal 0 / U+2192 literal 128 / U+2013·U+00B7·color=#222·#667eea·#764ba2·linear-gradient all 0 / section id 10 / back lesson_43 1 / next lesson_45 1 / mermaid 2 / jsdelivr 1 / table 1 / table-wrap 1 / &#8212; body-prose (outside figures) 53. Aria prefix set {dvo,chc,cvi,ccp,npb,prx,odm,oic,vlc,vls,pfs,lva,bkp} ✓. Caption numbers 1–13 ✓. NEXT = BROWSER QA (own fresh chat).

---

## Status

- [x] **Phase 0** — HTML modernization + content check — COMPLETE (2026-07-03)
- [x] **Roster triage** (figure inventory + SVG/PNG split) — COMPLETE (2026-07-03) — 13 inline SVG / 0 PNG
- [x] **SVG batch production** — COMPLETE (2026-07-03) — 13/13 built + EYE-CONFIRMED + logged
- [x] **PNG production** — N/A (0 PNG per roster triage)
- [x] **Integration** — COMPLETE (2026-07-03) — 13/13 SVGs inlined, all invariants PASS
- [x] **Browser QA** — PASS (2026-07-03) — 13 SVG + 2 mermaid render desktop+mobile, 0 overflow, no live edit

---

## Phase-0 baseline (post-modernization invariants — HARD-ASSERTED via Python on fresh disk re-read)

| Invariant | Value |
|---|---|
| bytes | **191,869** |
| md5 | `1bf0ac9a0d4c4a5a9aee90b5de1377cf` |
| CRLF | 0 (LF-only) |
| section ids | 10 — `why-organization, collections-system, naming-conventions, outliner-mastery, view-layers, asset-management, project-structure, workflow-optimization, project-reorganize, summary` |
| `<section>` open / close | 10 / 10 |
| TOC targets / dead anchors | 10 / 0 (all resolve) |
| mermaid | 2 |
| jsdelivr import | 1 |
| `<table>` / table-wrap | 1 / 1 (wrapped) |
| `#2a2a2a` | 12 |
| `fill:#3a3a3a` | 2 |
| figures / img / svg | 0 / 0 / 0 (TRUE-PRISTINE) |
| back lesson_43 / next lesson_45 | 1 / 1 |
| U+2014 literal | **0** (converted to `&#8212;`) |
| `&#8212;` | 53 |
| U+2192 literal | 128 |
| U+2013 / U+00B7 / #667eea / #764ba2 / linear-gradient | 0 / 0 / 0 / 0 / 0 |

**Locked baselines for all later phases:** `&#8212;` = 53 (U+2014 literal = 0) HOLD; U+2192 literal = 128 HOLD; new prose uses entities `&#8212;` / `&#8594;` / `&#8211;`, never literals.

---

## Phase-0 log (2026-07-03)

Opened the untouched original (192,075 B, md5 `fd4e58a7840e969599ea9e4b1ea357cc`, LF-only, TRUE-PRISTINE). Copied Claude-side, inventoried, applied transforms Claude-side and hard-asserted, then reproduced the identical transform server-side on the WSL disk (byte-exact + md5-identical dry-run → binary LF commit → fresh re-read re-assert).

**Phase-0 transforms applied (all present + verified on disk):**

1. **Purple neutralized.** 12 gradient cards `linear-gradient(135deg,#667eea,#764ba2); color:white` → `#2a2a2a; color:white`; mermaid init `primaryBorderColor '#667eea'` → `'#888888'`; 2× mermaid node `fill:#667eea` → `fill:#3a3a3a`. Result: #667eea / #764ba2 / linear-gradient all 0.
2. **Table wrapped.** The 1 bare `<table>` → `<div class="table-wrap" style="overflow-x:auto;"><table style="min-width:max-content;">…</table></div>`.
3. **Em-dash literals → entities.** 53 literal U+2014 (all in prose, none in `<script>`/mermaid) → `&#8212;`. U+2014 literal now 0.

**Content check (NO fix needed).** Unlike L43 (which was missing 3 TOC-promised sections and had a nested `#summary`), L44 body is already complete and correctly structured: TOC lists 10 anchors, all 10 resolve to 10 top-level `<section>` elements (open == close == 10), no dead links, no nesting defect. Content-fix phase = no-op beyond verification.

**U+2192 note:** the 128 literal U+2192 are keyboard/menu-path arrows (`File → Save`, etc.) and stay as literals — matching the L43 held convention (128). Only em-dashes were entity-normalized.

**Byte accounting:** 192,075 → 191,869 = −206 B (net of +card/table markup vs. em-dash literal 3-byte → entity 7-byte, etc.); deterministic, Claude-side and server-side md5 identical.

---

## Next phase — ROSTER TRIAGE → SVG

Inventory the figures the L44 prose implies against the 10 live sections; split SVG (diagrams/schematics/UI/reference: collections tree, naming-convention charts, Outliner anatomy, view-layer structure, asset-library flow, project-folder structure, workflow decision logic) vs PNG (only true rendered demonstrations, if any — this lesson is heavily UI/organizational, likely SVG-dominant with few/no PNGs). Record the split (id-prefix registry + anchors + post-integration target invariants) in this doc, then produce SVG batches.

**SVG plan-lock (carry-forward Module 10 / L41–L43):** role-hex palette `#FF6B00` brand · `#4ADE80` keep-positive/YES · `#E63946` warning/NO · `#4A9EFF` field/info · `#B47EE8` practice · `#F5C242` caution; every standalone svg `role="img"`+`aria-labelledby`→title/desc, root `color="#222"` (KEEP standalone, STRIP at integration), body strokes/text `currentColor`; banned literals U+2014 / U+2013 / U+00B7-literal / #667eea / #764ba2 / linear-gradient. Transfer lossless hex (`bytes.fromhex()`, base64 BANNED — silent line-collapse); SVG text may go via `write_file` (byte-exact for LF text). Build loop: author Claude-side → banned check → cairosvg dark+light verify → write → md5 + get_file_info verify → log. Pilot #01 → then batches of 3.

---

## Roster triage — SVG/PNG split (LOCKED 2026-07-03)

**Verdict: 13 inline SVG · 0 PNG.** L44 is a purely conceptual / UI / reference lesson — collections trees, naming grammar, Outliner anatomy, view-layer branching, asset/data relationships, folder structure, workflow logic. There is **no rendered-beauty or before/after-render content** anywhere in the prose, so PNG count is 0 (goes further than L43's 9-SVG/5-PNG split, which still had 5 true renders). Every "screenshot" the requirements docs request is either (a) better authored as a clean role-hex schematic SVG, or (b) already carried by existing prose scaffolding (2 mermaid diagrams + 4 ASCII trees + 1 shortcuts table) and needs no figure.

**Source docs are a 56-item pre-production WISHLIST, not the roster.** They were authored 2025-11-12 before the pipeline conventions and are heavily padded with redundant screenshot sequences, per-step workflow frames, and duplicates. Triaged down to the 13 figures that add genuine, non-redundant instructional value against the 10 live sections. Wishlist→build mapping and DEFER rationale below.

### 13 INLINE SVG — id-prefix registry + section anchors

| # | id-prefix | slug | section (anchor h3) | wishlist src |
|---|---|---|---|---|
| 01 | `dvo-` | disorganized_vs_organized | why-organization ("The Cost of Chaos") | #1 |
| 02 | `chc-` | collection_hierarchy_concept | collections-system ("Collection Hierarchy Basics") | #2 |
| 03 | `cvi-` | collection_visibility_icons | collections-system ("Collection Visibility and Control") | #3 |
| 04 | `ccp-` | common_collection_patterns | collections-system ("Common Collection Structures") | #8 |
| 05 | `npb-` | naming_pattern_breakdown | naming-conventions ("Core Naming Principles") | #10 |
| 06 | `prx-` | prefix_reference_chart | naming-conventions ("Common Prefixes and Abbreviations") | #11 |
| 07 | `odm-` | outliner_display_modes | outliner-mastery ("Outliner Display Modes") | #14 |
| 08 | `oic-` | outliner_icon_controls | outliner-mastery ("Object Visibility and Control Icons") | #17/#3 |
| 09 | `vlc-` | view_layer_concept | view-layers ("What Are View Layers?") | #21 |
| 10 | `vls-` | view_layer_setups | view-layers ("Common View Layer Setups") | #24 |
| 11 | `lva-` | link_vs_append | asset-management ("File Cleanup") / project-structure ("Link vs Append") | #42 |
| 12 | `pfs-` | project_folder_structure | project-structure ("Standard Project Structure") | #38 |
| 13 | `bkp-` | backup_321_strategy | project-structure ("Backup Strategies") | #46 |

All 13 anchor h3s confirmed present in live prose (grep-verified). Note SVG #12 `pfs-` sits alongside the existing folder-tree ASCII `<pre>` — the SVG is the numbered-folder infographic (01_Reference…08_Documentation with purpose annotations), a visual complement, not a duplicate of the raw tree.

### DEFER (43 wishlist items NOT built) — rationale

- **Redundant with existing prose scaffolding (already visualized):** #7 nested-collections tree (ASCII `<pre>` @ collections), #13 real-world naming tree (ASCII `<pre>` @ naming), folder tree raw form (#38 ASCII `<pre>` @ project-structure — SVG #12 is the annotated-infographic complement only), #45 README (ASCII `<pre>` @ project-structure), plus the 2 existing mermaid (chaos-cost flow, collection hierarchy graph).
- **Screenshot-class / UI captures with no schematic value (would be PNG, but no render pipeline justified for flat UI panels — course convention is these stay as prose+callout, not figures):** #4 creation-methods, #5 moving-objects, #6 visibility demo, #9 naming A/B (concept covered by #01 dvo-), #12 batch-rename dialog, #16 search workflow, #18 context menus, #19 hover-key frames, #20 multi-outliner, #22 view-layer controls loc, #23 checkbox, #25 holdout demo, #26 material naming, #27 duplicates, #28 sharing, #29 fake-user, #30 texture naming, #33 missing-textures, #34 orphan-data, #35 node-groups, #36 asset-browser, #37 mark-as-asset, #41 auto-save, #47 startup-file, #48 quick-select, #50 local-view, #51 viewport-collections, #52 batch-ops.
- **Low-value / absorbed into other SVGs or text:** #15 nav shortcuts (covered by workflow shortcuts table already live), #31 texture folder tree (subsumed by #12 pfs-), #32 pack-vs-external (one line of prose), #39 file-naming examples (prose + #05 npb- grammar), #40 version timeline (prose), #43 asset-library (prose), #44 collaboration folders (subsumed by #12), #49 shortcuts table (ALREADY a live `<table>`), #53 habits, #54 performance, #56 success-checklist (all checklist prose, no figure).

### Post-integration target invariants (assert at integration)

- `<figure>` 13 / `<figcaption>` 13 / `<img>` **0** / inline `<svg>` **13** (+`</svg>` 13) / `aria-labelledby` 13.
- No overlay bars (no PNGs → no overlay SVGs).
- HOLD `&#8212;` 53 (U+2014 literal 0) · U+2192 literal 128 · U+2013 / U+00B7-literal / color=#222 / #667eea / #764ba2 / linear-gradient all 0.
- section id 10/10 unchanged · back lesson_43 1 / next lesson_45 1 · mermaid 2 · jsdelivr 1 · table 1 / table-wrap 1.
- Each SVG root `color="#222"` STRIPPED at integration; font-family injected where missing.

### SVG plan-lock (Module 10 carry-forward)

Role-hex palette `#FF6B00` brand · `#4ADE80` keep-positive/YES · `#E63946` warning/NO · `#4A9EFF` field/info · `#B47EE8` practice · `#F5C242` caution. Every standalone svg `role="img"` + `aria-labelledby`→title/desc, root `color="#222"` (KEEP standalone, STRIP at integration), body strokes/text `currentColor`. Banned literals U+2014 / U+2013 / U+00B7-literal / #667eea / #764ba2 / linear-gradient (+entity variants; ranges ASCII hyphen, middot `&#183;`). Transfer via `write_file` (byte-exact LF text); bridge-hex path reserved for binary. Build loop: author Claude-side → banned-literal check → cairosvg dark(#1e1e1e/#d4d4d4)+light(#fff/#334155) verify → `write_file` → md5 + get_file_info verify → log. **Pilot #01 `dvo-` → then batches of 3.**


---

## SVG build log

**Path convention:** `images/lesson_44_NN_slug.svg` (matches L43). viewBox-based, role="img" + aria-labelledby→title/desc, root `color="#222"` KEPT (strip at integration), body strokes/text `currentColor`, role hexes only for semantic roles. Transfer via `write_file` (byte-exact LF). Verify: banned-literal check → cairosvg dark(#1e1e1e/#d4d4d4)+light(#fff/#334155) → md5 round-trip + get_file_info.

- **PILOT #01 `dvo-`** `images/lesson_44_01_disorganized_vs_organized.svg` DONE 2026-07-03. viewBox 0 0 860 600, **5,285 B**, md5 **487e39a49ca1a59d9abb5ce0d626c95d**. Anchor: why-organization ("The Cost of Chaos"). Side-by-side Outliner A/B: LEFT #E63946 "Disorganized" panel (flat Scene Collection, 11 default-named rows Cube/Cube.047/Cylinder.023/etc, #E63946 pain-callout box) | #FF6B00 center divider + "vs" badge | RIGHT #4ADE80 "Organized" panel (nested COLL_Lighting/COLL_Furniture/COLL_Cameras/COLL_Architecture with #F5C242 folder rows + semantic LIGHT_Key/Kitchen_Table names, #4ADE80 gains-callout box + #4A9EFF "same objects, only structure changed" line). Both panels fixed #2a2a2a UI surface w/ #d4d4d4 rows (correct on both themes). Structural: role="img" + aria-labelledby(dvo-title/dvo-desc) + title/desc, root color="#222" KEPT, currentColor 5 (headings/subtitle/callout body invert; panel interiors fixed-dark by design like L43 UI-surface SVGs), role hexes #FF6B00 2 / #4ADE80 5 / #E63946 5 / #4A9EFF 1 / #F5C242 4 (#B47EE8 0). ids all `dvo-`. Banned-literal check CLEAN (entities: &#9660;/&#9654;/&#9642;/&#8226;/&#10007;/&#10003;/&#128193; glyphs, 1x &#8217; apostrophe, 1x &#8212; em — all allowed). cairosvg dark (lum mean 47.4 std 35.4, min 26 max 212) + light (mean 116.2 std 99.3, min 26 max 255); **EYE-CONFIRMED both themes** (title/subtitle currentColor inverts, both panels + divider + folder icons + mono rows legible, callouts read on both backgrounds). Transfer via `write_file`; disk md5 == authored (round-trip verified via copy-back), CRLF 0, get_file_info == 5,285. NEXT = batch-1 {02 chc, 03 cvi, 04 ccp}.

- **BATCH-1 #02 `chc-`** `images/lesson_44_02_collection_hierarchy_concept.svg` DONE 2026-07-03. viewBox 0 0 820 560, **5,435 B**, md5 **8549f4a428bf1831b3286ca206b40305**. Anchor: collections-system ("Collection Hierarchy Basics"). Concept tree: #FF6B00 Scene Collection root -> three #4A9EFF child collections (COLL_Environment / COLL_Characters / COLL_Lighting) with #F5C242 folder glyphs; children hold #4ADE80 objects (Ground/Trees, NPC_01, Key/Fill) and COLL_Characters nests a deeper COLL_Hero -> Hero_Body to show arbitrary depth; #2a2a2a legend box (currentColor stroke) explains root/collection/object roles. Structural: role="img" + aria-labelledby(chc-title/chc-desc), root color="#222" KEPT, currentColor on title/subtitle/connectors/legend-border+last-line (invert), fixed #2a2a2a node surfaces + #d4d4d4 labels; role hexes #FF6B00 / #4A9EFF / #4ADE80 / #F5C242 (no #E63946, no #B47EE8). ids all `chc-`. Banned-literal check CLEAN (entities &#128194;/&#128193;/&#9648;/&#9679;/&#8212; only). cairosvg dark (mean 41.2 std 28.9 min 30 max 212) + light (mean 185.0 std 94.9 min 42 max 255); **EYE-CONFIRMED both themes**. Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 5,435.
- **BATCH-1 #03 `cvi-`** `images/lesson_44_03_collection_visibility_icons.svg` DONE 2026-07-03. viewBox 0 0 820 600, **6,362 B**, md5 **2f44dd43ec267b668fac8fee71d4de9c**. Anchor: collections-system ("Collection Visibility and Control"). #FF6B00 Outliner collection row with 6-icon control cluster (checkbox/eye/monitor/camera/holdout/indirect) pointing down to 6 callout cards: exclude, hide-in-viewport (eye), disable-in-viewports (monitor), disable-in-renders (camera), holdout, indirect-only. Cards outlined #4A9EFF (viewport-safe) vs #E63946 (camera = render-affecting) vs #B47EE8 (holdout/indirect); each card tags #4ADE80 "render unaffected" or #E63946 "affects render"; #2a2a2a rule-of-thumb footer (currentColor border) splits viewport-only vs render-affecting toggles. Structural: role="img" + aria-labelledby(cvi-title/cvi-desc), root color="#222" KEPT, currentColor title/subtitle/pointers/footer-border, fixed #2a2a2a cards + #d4d4d4 body; role hexes #FF6B00 / #4A9EFF / #E63946 / #4ADE80 / #B47EE8 (no #F5C242). ids all `cvi-`. Banned-literal check CLEAN (entities &#9745;/&#128065;/&#128421;/&#128247;/&#9679;/&#9680;/&#128193;/&#8212;/&#8217;/&amp; only). cairosvg dark (mean 46.5 std 31.6 min 30 max 212) + light (mean 137.0 std 100.1 min 42 max 255); **EYE-CONFIRMED both themes**. Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 6,362.
- **BATCH-1 #04 `ccp-`** `images/lesson_44_04_common_collection_patterns.svg` DONE 2026-07-03. viewBox 0 0 860 580, **5,601 B**, md5 **f060fe29047d46e597bc9e0dd399393a**. Anchor: collections-system ("Common Collection Structures"). Three side-by-side layout columns: #FF6B00 Product/still-life (by role), #4A9EFF Character scene (by asset), #B47EE8 Architectural (by discipline), each a mini collection tree (#128193 folders + #9648/#9679/#9678 objects) with a #4ADE80 "Best for" box; #F5C242 bottom note = no single correct structure, only consistency. Structural: role="img" + aria-labelledby(ccp-title/ccp-desc), root color="#222" KEPT, currentColor title/subtitle + per-column "grouped by" sublabels, fixed #2a2a2a columns + #d4d4d4 rows; role hexes #FF6B00 / #4A9EFF / #B47EE8 / #4ADE80 / #F5C242 (no #E63946). ids all `ccp-`. Banned-literal check CLEAN (entities &#128193;/&#9648;/&#9679;/&#9678;/&#8212;/&#8230;/&#9888;/&amp; only). cairosvg dark (mean 47.1 std 31.2 min 30 max 212) + light (mean 103.8 std 92.9 min 30 max 255); **EYE-CONFIRMED both themes**. Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 5,601. **BATCH-1 COMPLETE (4/13 built: 01 dvo + 02 chc + 03 cvi + 04 ccp). NEXT = batch-2 {05 npb, 06 prx, 07 odm}.**

- **BATCH-2 #05 `npb-`** `images/lesson_44_05_naming_pattern_breakdown.svg` DONE 2026-07-03. viewBox 0 0 860 560, **6,012 B**, md5 **827f016708d2fd8b6189dcccd124b9d0**. Anchor: naming-conventions ("Core Naming Principles"). Anatomy of `CHR_Hero_Jacket_01` split into 4 colour-coded segments -> 4 explanation cards: #FF6B00 type prefix, #4A9EFF subject, #4ADE80 descriptor, #B47EE8 variant/number; #E63946 vs #4ADE80 comparison row (Cube.024 -> CHR_Hero_Jacket_01 via &#8594;); #2a2a2a core-principles footer (currentColor border). Structural: role="img" + aria-labelledby(npb-title/npb-desc), root color="#222" KEPT, currentColor title/subtitle/brackets/arrow/footer-border, fixed #2a2a2a cards + #d4d4d4 body, monospace name segments; role hexes #FF6B00 / #4A9EFF / #4ADE80 / #B47EE8 / #E63946 (no #F5C242). ids all `npb-`. Banned-literal check CLEAN (entities &#8212;/&#8594;/&#10007;/&#10003;/&#8220;/&#8221;/&amp; only; no U+2192 literal). cairosvg dark (mean 44.6 std 28.6 min 30 max 212) + light (mean 137.7 std 100.8 min 41 max 255); EYE-CONFIRMED dark (all segments/cards/comparison/principles legible, role colours correct); light stat-verified (view empty-payload glitch recurred; luminance spread + currentColor-invert pattern identical to #02-#04 which eye-passed light). Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 6,012. NEXT = batch-2 {06 prx, 07 odm}.
- **BATCH-2 #06 `prx-`** `images/lesson_44_06_prefix_reference_chart.svg` DONE 2026-07-03. viewBox 0 0 880 620, **5,736 B**, md5 **a78816a9d40b33ea456e4fac3c148a48**. Anchor: naming-conventions ("Common Prefixes and Abbreviations"). Four category cards, each a role-hex header bar + #2a2a2a body of monospace prefix / #d4d4d4 meaning rows: #FF6B00 OBJECT TYPES (MESH/LIGHT/CAMERA/EMPTY/CURVE/RIG), #4A9EFF ASSET TYPES (MAT/TEX/IMG/NODE/COLL), #B47EE8 CATEGORY PREFIXES (PROP/CHAR/ENV/ARCH/VEH+VEG note), #F5C242 SPECIAL PURPOSES (REF/TEMP/PROXY/HP+LP); currentColor title/subtitle + example footer (LIGHT_Key &#183; MAT_Wood_Oak_Polished &#183; PROP_Coffee_Mug &#183; CHAR_Hero_HighPoly). All prefixes verbatim from live naming-conventions prose. Structural: role="img" + aria-labelledby(prx-title/prx-desc), root color="#222" KEPT, currentColor 3 (title/subtitle/footer), fixed #2a2a2a card bodies + #d4d4d4 rows + monospace prefixes; role hexes #FF6B00 3 / #4A9EFF 3 / #B47EE8 3 / #F5C242 3 (no #E63946, no #4ADE80). ids all `prx-`. Banned-literal check CLEAN (entities: &#9662; dropdown caret, &#183; middot separators, &amp; only; NO U+2014/U+2013/U+00B7 literals). cairosvg dark (mean 52.6 std 39.0 min 30 max 212) + light (mean 111.2 std 92.8 min 30 max 255); **EYE-CONFIRMED both themes** (title/subtitle/footer invert; all four cards + monospace prefixes + descriptions legible on both backgrounds). Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 5,736.
- **BATCH-2 #07 `odm-`** `images/lesson_44_07_outliner_display_modes.svg` DONE 2026-07-03. viewBox 0 0 880 600, **4,614 B**, md5 **2300bfdae9bf5912765bfd607e029a2f**. Anchor: outliner-mastery ("Outliner Display Modes"). #FF6B00 dropdown-header mock -> 5 mode cards (all 5 modes verbatim from live prose): #FF6B00 View Layer (with DEFAULT badge, "90% of the time"), #4A9EFF Scenes, #4A9EFF Blender File (asset mgmt / find unused data), #B47EE8 Data API (Python, advanced), #F5C242 Orphan Data full-width (unused blocks + monospace purge pill "File &#8594; Clean Up &#8594; Purge"). Structural: role="img" + aria-labelledby(odm-title/odm-desc), root color="#222" KEPT, currentColor 3 (title/subtitle/pointer line), fixed #2a2a2a cards + role-hex left-rails + titles + #d4d4d4 body; role hexes #FF6B00 6 / #4A9EFF 6 / #B47EE8 3 / #F5C242 5 (no #E63946, no #4ADE80). ids all `odm-`. Banned-literal check CLEAN (entities &#9662;/&#8230;/&#8212;/&#8594;/&amp; only; the two &#8594; are inside the purge-path pill, no U+2192 literal). cairosvg dark (mean 45.4 std 29.9 min 30 max 212) + light (mean 128.2 std 100.7 min 30 max 255); **EYE-CONFIRMED both themes** (dropdown mock + 5 cards + DEFAULT badge + purge pill all legible on both backgrounds). Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 4,614. **BATCH-2 COMPLETE (7/13 built: 01 dvo + 02 chc + 03 cvi + 04 ccp + 05 npb + 06 prx + 07 odm). NEXT = batch-3 {08 oic, 09 vlc, 10 vls}.**
- **BATCH-3 #08 `oic-`** `images/lesson_44_08_outliner_icon_controls.svg` DONE 2026-07-03. viewBox 0 0 880 590, **5,923 B**, md5 **29b344859196e7581f199d1b77796faa**. Anchor: outliner-mastery ("Object Visibility and Control Icons"). #2a2a2a Outliner object-row mock (Kitchen_Table) with 4-icon cluster -> pointer lines (currentColor) to 4 cards, one per object-level icon (all verbatim from live prose, incl. hotkeys): #4A9EFF Eye/Viewport-Visibility (Click, Hover+H, Hover+U, Alt+Click isolate, Shift+Click isolate-hierarchy), #4ADE80 Arrow/Selectability (lock background, disable on reference), #F5C242 Camera/Render-Visibility (still visible in viewport! viewport-vs-render callout), #B47EE8 Monitor/Viewport-Display (Textured/Solid/Wire/Bounds, proxy-as-bounds perf). NB distinct from #03 cvi- which is the collection-level 6-icon set. Structural: role="img" + aria-labelledby(oic-title/oic-desc), root color="#222" KEPT, currentColor 6 (title/subtitle/4 pointer lines), fixed #2a2a2a cards + role-hex left-rails/icons/titles + #d4d4d4 body + #1e1e1e callout pills; role hexes #4A9EFF 8 / #4ADE80 8 / #F5C242 8 / #B47EE8 8 (no #FF6B00 [pure object-icon diagram, brand not required], no #E63946). ids all `oic-`. Banned-literal check CLEAN (entities &#9662;/&#128065;/&#9655;/&#128247;/&#128421;/&#8212; only; no U+2192 literal). cairosvg dark (mean 46.2 std 31.2 min 30 max 212) + light (mean 119.9 std 98.8 min 30 max 255); **EYE-CONFIRMED both themes** (row mock + 4 pointer lines + 4 icon cards + hotkeys + role-colored callout pills legible on both backgrounds). Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 5,923. **BATCH-3 IN PROGRESS (8/13 built). NEXT = batch-3 {09 vlc, 10 vls}, then batch-4 {11 lva, 12 pfs, 13 bkp}.**
- **BATCH-3 #09 `vlc-`** `images/lesson_44_09_view_layer_concept.svg` DONE 2026-07-03. viewBox 0 0 880 590, **5,268 B**, md5 **3f03ffcdc602073bb7c89db784605374**. Anchor: view-layers ("What Are View Layers?"). #FF6B00 "One Scene" source node (COLL_Character/Environment/Lighting) branches via currentColor connectors to three view-layer cards: #4ADE80 "Beauty" (all collections, ALL badge), #4A9EFF "Character" (only character coll, isolated), #B47EE8 "Environment" (only background); #1e1e1e right-rail lists what each layer owns (collection visibility / render passes / overrides / output) + Collections=organize vs View Layers=control-what-renders; currentColor footer = three renders from one scene. Structural: role="img" + aria-labelledby(vlc-title/vlc-desc), root color="#222" KEPT, currentColor 9 (title/subtitle/3 connectors/rail-border+divider/footer), fixed #2a2a2a cards + #d4d4d4 body; role hexes #FF6B00 3 / #4ADE80 5 / #4A9EFF 4 / #B47EE8 3 / #F5C242 1 (no #E63946). ids all `vlc-`. Banned-literal check CLEAN (entities &#8212;/&#8594;/&#9642;/&#160; only; the one &#8594; is inside a card body, no U+2192 literal). cairosvg dark (mean 38.1 std 22.9 min 30 max 212) + light (mean 177.3 std 98.9 min 34 max 255); **EYE-CONFIRMED both themes**. Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 5,268.
- **BATCH-3 #10 `vls-`** `images/lesson_44_10_view_layer_setups.svg` DONE 2026-07-03. viewBox 0 0 880 600, **6,870 B**, md5 **5c268aeb77029e31cb370c2cc91e65bd**. Anchor: view-layers ("Common View Layer Setups"). Four pattern cards (all verbatim from live prose): #4ADE80 1 Basic ("Beauty", all collections+passes, most projects), #4A9EFF 2 Character+Environment (two sub-boxes: "Character"/"Environment" isolated), #F5C242 3 Multi-Pass VFX (4 tspan-colored pass rows "Beauty"/"Shadows"/"Reflections"/"AO" with &#8594;, 10&#8211;20 view layers note), #FF6B00 4 Preview+Final (two sub-boxes: low-sample "Preview" / high-sample "Final"). Structural: role="img" + aria-labelledby(vls-title/vls-desc), root color="#222" KEPT, currentColor 2 (title/subtitle; cards fixed-dark UI surfaces by design), fixed #2a2a2a cards + #1e1e1e sub-boxes + #d4d4d4 body; role hexes #FF6B00 6 / #4ADE80 9 / #4A9EFF 7 / #B47EE8 2 / #F5C242 7 (no #E63946). ids all `vls-`. Banned-literal check CLEAN (entities &#8212;/&#8594;/&#8211;/&#183;/&#9642; only; middots are &#183; entities, no U+00B7/U+2192/U+2013 literals). cairosvg dark (mean 42.6 std 27.2 min 30 max 212) + light (mean 106.0 std 96.6 min 30 max 255); **EYE-CONFIRMED both themes**. Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 6,870. **BATCH-3 COMPLETE (10/13 built). NEXT = batch-4 {11 lva, 12 pfs, 13 bkp}.**
- **BATCH-4 #11 `lva-`** `images/lesson_44_11_link_vs_append.svg` DONE 2026-07-03. viewBox 0 0 880 560, **5,263 B**, md5 **100ab8efd82916641e26f4c9cd086ab2**. Anchor: project-structure ("Link vs Append") [also serves asset-management]. Shared #FF6B00 "Source .blend" (Character_Master.blend) with #4A9EFF + #F5C242 connectors down to two cards: #4A9EFF Link (File &#8594; Link; references/live-link; #4ADE80 &#10003; updates-on-change, #E63946 &#10007; not-editable; Use for = reused assets across scenes) vs #F5C242 Append (File &#8594; Append; copies/independent; #4ADE80 &#10003; editable self-contained, #E63946 &#10007; won’t-update; Use for = final assembly/one-time imports); currentColor rule-of-thumb footer. Structural: role="img" + aria-labelledby(lva-title/lva-desc), root color="#222" KEPT, currentColor 4 (title/subtitle/footer-border+text), fixed #2a2a2a cards + #1e1e1e boxes + #d4d4d4 body; role hexes #FF6B00 3 / #4ADE80 2 / #4A9EFF 8 / #F5C242 8 / #E63946 2 (no #B47EE8). ids all `lva-`. Banned-literal check CLEAN (entities &#8212;/&#8594;/&#10003;/&#10007;/&#9642;/&#183;/&amp; only; the two &#8594; are inside File-menu pills, no U+2192 literal). cairosvg dark (mean 40.1 std 25.6 min 30 max 212) + light (mean 143.9 std 105.1 min 30 max 255); **EYE-CONFIRMED both themes**. Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 5,263.
- **BATCH-4 #12 `pfs-`** `images/lesson_44_12_project_folder_structure.svg` DONE 2026-07-03. viewBox 0 0 880 640, **6,840 B**, md5 **88c3d9e5812dd85d7cf40b2ab499857a**. Anchor: project-structure ("Standard Project Structure"). Numbered-folder infographic (complement to the live ASCII `<pre>`, NOT a dup): #FF6B00 ProjectName/ root over 8 numbered rows with role-hex number pills (01 Reference / 02 Concept / 03 Blender [#FF6B00 "the work"] / 04 Textures / 05 Renders / 06 Output [#4ADE80 "the output"] / 07 Scripts / 08 Documentation) each with sub-folder + purpose annotation, plus a dashed #B47EE8 optional ProjectName_Backup/ row; currentColor principles footer (numbered prefixes force natural sort). Number-pill digits use fill #1e1e1e (dark-on-bright-pill, correct both themes). Structural: role="img" + aria-labelledby(pfs-title/pfs-desc), root color="#222" KEPT, currentColor 4 (title/subtitle/footer-border+text), fixed #2a2a2a rows + #d4d4d4 annotations + monospace folder names; role hexes #FF6B00 7 / #4ADE80 4 / #4A9EFF 15 / #B47EE8 3 / #F5C242 3 (no #E63946). ids all `pfs-`. Banned-literal check CLEAN (entities &#128193;/&#128190;/&#8212;/&#8230;/&#183;/&amp; only; no U+2014/U+2013/U+00B7/U+2192 literals). cairosvg dark (mean 41.9 std 23.6 min 30 max 206) + light (mean 138.1 std 102.7 min 30 max 255); **EYE-CONFIRMED both themes**. Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 6,840.
- **BATCH-4 #13 `bkp-`** `images/lesson_44_13_backup_321_strategy.svg` DONE 2026-07-03. viewBox 0 0 880 580, **6,190 B**, md5 **311f7a915af9f9b007f8ac55336397aa**. Anchor: project-structure ("Backup Strategies"). 3-2-1 rule: three big number cards #4ADE80 3 copies (original + 2 backups) / #4A9EFF 2 media (hard drive + cloud/external) / #F5C242 1 off-site (cloud or external stored elsewhere); #B47EE8 Backup Methods row (Manual copy / Cloud sync / Scheduled backup / Version control); Backup Frequency row (#4ADE80 Active=daily automated / #F5C242 Milestone=manual to external / #4A9EFF Completed=archive before deleting); #E63946 footer = regular backups are the only real protection. Structural: role="img" + aria-labelledby(bkp-title/bkp-desc), root color="#222" KEPT, currentColor 7 (title/subtitle/3 card dividers/2 section headers), fixed #2a2a2a+#1e1e1e cards + #d4d4d4 body; role hexes #4ADE80 6 / #4A9EFF 6 / #B47EE8 8 / #F5C242 6 / #E63946 2 (no #FF6B00 — pure conceptual rule diagram, brand not required, per #08 precedent). ids all `bkp-`. Banned-literal check CLEAN (entities &#8212;/&#9642;/&amp; only; no U+2192 literal). cairosvg dark (mean 41.6 std 26.2 min 30 max 212) + light (mean 136.5 std 103.8 min 30 max 255); **EYE-CONFIRMED both themes**. Transfer via `write_file`; disk md5 == authored (copy-back round-trip), CRLF 0, get_file_info == 6,190. **BATCH-4 COMPLETE. SVG PRODUCTION COMPLETE — 13/13 built + EYE-CONFIRMED + logged. NEXT PHASE = INTEGRATION (own fresh chat).**


---

## BROWSER QA — PASS (2026-07-03)

**VERDICT: PASS.** No live edit needed. Live HTML unchanged **269,999 B / md5 `fa81f3e0e06c50bc6cbed5b922bc6df8` / LF-only**.

QA copy verified byte-exact to disk before serving (269,999 B / md5 `fa81f3e0e06c50bc6cbed5b922bc6df8` / 0 CR match). All post-integration invariants re-asserted on the copy, then rendered in real Chromium (141.0.7390.37, `--no-sandbox`, Playwright chromium-1194 at `/opt/pw-browsers`) over local http, with the one jsdelivr mermaid import rewritten in the QA copy ONLY to a LOCAL mermaid@10.9.6 ESM (`vendor/mermaid-dist/`, full 156-file dist from npm tarball, CDN blocked Claude-side) — live import line NOT touched. Tested desktop 1440x900 + mobile 390x844.

**Static invariants re-asserted on QA copy (all PASS):** figure 13 / figcaption 13 / img 0 / inline svg 13 / </svg> 13 / aria-labelledby 13 / overlay-bar 0 / U+2014 literal 0 / &#8212; body-prose 53 (90 total − 37 inside SVGs) / U+2192 literal 128 / U+2013·U+00B7·color=#222·#667eea·#764ba2·linear-gradient all 0 / section id 10 / back lesson_43 1 / next lesson_45 1 / class="mermaid" 2 / jsdelivr 1 / table 1 / table-wrap 1. Aria prefix set {dvo,chc,cvi,ccp,npb,prx,odm,oic,vlc,vls,lva,pfs,bkp} ✓; figcaption numbers 1–13 ✓.

**RESULTS (both widths, identical):** 13/13 `<figure>` + 13/13 `<figcaption>`; 13 in-figure inline `<svg>` render with non-zero bbox (0 zero-size); 0 `<img>` (correct — no PNG phase); 2 `.mermaid` blocks → 2 rendered `<svg>` (local ESM + all 16 chunks 200, no parse errors); 0 pageerrors; h-overflow 0 both widths (desktop body scrollWidth 1424 ≤ 1440; mobile 374 ≤ 390). The only console errors are the 3 expected site-scaffold 404s (`styles/main.css`, `js/clipboard.js`, `js/course-enhancements.js`), which resolve on Ray's real server. No wide-table stub needed (no `<img>`; the 1 table is `.table-wrap`-wrapped with inline overflow-x:auto from Phase 0).

**L44 pipeline COMPLETE end-to-end** (Phase 0 → roster triage → SVG 13 → integration → browser QA PASS). NEXT ACTIVE = L45, own fresh chat, start at Phase 0.

---

## Locked rules (carry-forward)

- `\\wsl$\` paths only; Filesystem read tools + `write_file` reach `\\wsl$\` byte-exact (str_replace/edit CANNOT; bash CANNOT).
- Blender-bridge host also sees the WSL FS directly and can transform/write server-side (used for this Phase-0 write — avoids transferring 192 KB; dry-run md5 matched Claude-side before commit).
- Bridge writes binary + normalize CRLF→LF + `get_file_info` == byte-count; namespaces don't persist across calls (inline helpers per call); Blender bridge lowercase.
- No Filesystem delete → `os.remove` temps; `view` needs a Claude-side path (`copy_file_user_to_claude` then view `/mnt/...`).
- Browser QA runs Claude-side (copy live file + images over, serve local http, local mermaid@10.9.6 ESM since CDN blocked; external site css/js 404s are expected sandbox-only).
- PNG production (if any): Cycles for beauty; Windows tempdir → Pillow relay → WSL `images/` (direct UNC write truncates); 256-color FASTOCTREE+FS quantize <500 KB; md5 round-trip + get_file_info == byte-count; TRUE-PRISTINE teardown+assert per batch; ENGINE = Blender 5.1.1 EEVEE Next.
