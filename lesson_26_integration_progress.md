# Lesson 26 &#183; Graph Editor Essentials &#8212; Integration Progress

**Source of truth for L26.** Module 6 (Introduction to Animation), lesson 3 of 4. Follows L25 (The Timeline and Keyframes, COMPLETE). Read order at every chat start: `session.md` &#8594; `curriculum_overview.md` (confirm title) &#8594; `images/IMAGE_STYLE_GUIDE.md` v6 &#8594; this doc.

**Lesson file:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_26_graph_editor_essentials.html`
**Requirements doc:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_26_image_requirements.md` (40-image catalog, AI prompts, placement map, SVG-recommendation list, priority tiers)
**Project root:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`
**Image output:** `images/`, naming `lesson_26_{II}_{slug}.{svg|png}`

---

## Current state

**CLOSED &#183; INTEGRATION COMPLETE (2026-06-24). 25 / 25 figures placed, all 15 commits DONE, browser QA PASSED.** All 25 figures (19 inline SVG + 6 PNG) integrated into the live HTML and verified on disk. Live HTML final (get_file_info + UTF-8 re-count): **291,776 bytes / figure 25 / figcaption 25 / img 6 / svg 25 (19 inline + 6 overlay) / mermaid 4 (CSS/JS harness noise, 0 rendered diagrams) / em-dash U+2014 = 34 / color222 0** &#8212; matches the projected close target exactly. Browser QA (Playwright/Chromium, file:// with the 6 PNGs + main.css staged) PASSED at BOTH 380px and 900px: 0 horizontal overflow; all 25 figures non-zero size; 6 overlays detected, 0 mistrack (overlay svg box matches img box); Figure 1 visually verified (4 callouts track regions, &#183;-separated caption). All 6 PNG c{N} overlays built + pixel-verified. Blender TRUE-PRISTINE (no bridge work in INTEGRATION). **L26 CLOSED in session.md.**

### Read-order optimization (locked 2026-06-24, applies from batch 6 onward)

Reduces per-chat reading. Per-batch read order is now: (1) `session.md`; (2) THIS doc (always &#8212; cheap, authoritative); (3) `images/IMAGE_STYLE_GUIDE.md` v6 &#8212; full guide for the PNG phase, but during the remaining SVG batches only the SVG-relevant parts are needed (palette hexes + axis colors, curve-chassis precedent from a sibling SVG e.g. #24, the dense-680 note, em-dash discipline, footer-tip variants, a11y attrs, file/naming conventions); the capture-workflow + color-dependent-render + Blender-5.1.1-op-name sections are PNG-phase only and can be skipped while producing SVGs; (4) `lesson_26_image_requirements.md` &#8212; do NOT read in full; read only the 2&#8211;3 target figures' content blocks (their numbered entry + matching prompt for content/sequence only), since the prompts are 4.0-era content specs and v6 governs style. (5) `curriculum_overview.md` &#8212; do NOT re-read; the title "Graph Editor Essentials" is already confirmed (Module 6, lesson 3 of 4). Batch-6 figure content is pre-glossed in the batch-6 status note below so the requirements doc may not need opening at all.

### Integration commit ledger (2026-06-24)

Bottom-up insertion order (so earlier line numbers never shift). Each figure block is inserted IMMEDIATELY BEFORE the unique anchor line shown (places it right after the preceding card's `</div>`, as a section-level sibling at default depth: `<figure>` col20 / children col24 / svg-inner col28). All 15 anchors confirmed UNIQUE 1x against the live file at plan-lock. The 4 PNG project figures place via wrapper-div + base img + overlay-svg (viewBox === PNG px, preserveAspectRatio xMidYMid meet, inset:0, pointer-events:none); c{N} overlay coords were Pillow-scanned from the orange keyframe dots and cairosvg pixel-verified 1:1 Claude-side before commit (LD19). Inline SVGs: strip root `color="#222"`, re-indent to col24/28, collapse multi-line attrs, prune `====` ASCII-divider comments (keep short structural / `<!-- Cell: Name -->`), preserve all role/aria/title/desc/id. 0 new em-dashes (`&#183;`).

| Commit | Figs | Section | Insert BEFORE this unique anchor | Status |
|---|---|---|---|---|
| 1 | #36 | summary | `<h3>Next Steps</h3>` | DONE |
| 2 | #32, #33, #34 | project | `<h3>Phase 4: Refine Scale Curves (10 min)</h3>` | DONE |
| 3 | #31 | project | `<h3>Phase 3: Refine Location Z Curve (15 min)</h3>` | DONE |
| 4 | #16 | handle-types | `<h3>The Six Handle Types</h3>` | DONE |
| 5 | #18, #19 | curve-handles | `<blockquote>` line containing `Handles Are Your Paint Brushes` | DONE |
| 6 | #14 | curve-handles | `<h3>Handle Angles and Direction</h3>` | DONE |
| 7 | #13 | curve-handles | `<h3>Handle Manipulation Basics</h3>` | DONE |
| 8 | #24, #25, #26 | reading-curves | `<h3>Comparing Multiple Curves</h3>` | DONE |
| 9 | #10, #11, #12 | reading-curves | `<h3>Diagnostic Reading</h3>` | DONE |
| 10 | #5, #6, #7, #8 | reading-curves | `<h3>Advanced Curve Reading</h3>` | DONE |
| 11 | #4 | interface | `<h3>Graph Customization</h3>` | DONE |
| 12 | #3 | interface | `<h3>Channel List Organization</h3>` | DONE |
| 13 | #2 (PNG, 1600x2146) | interface | `<h3>Interface Layout</h3>` | DONE |
| 14 | #1 (PNG, 1920x976) | interface | `<h3>Opening the Graph Editor</h3>` | DONE |
| 15 | #37 | what-is-graph-editor | `<h3>F-Curves Explained</h3>` | DONE |

Fig-order WITHIN a multi-fig commit = top-to-bottom reading order in the card region. #5/#6/#7/#8 map to the four `<pre>` blocks in the Basic Curve Interpretation card (linear / S-curve / sharp peak / flat plateau). #6 is placed ONCE here (reading-curves); the what-is-graph-editor "F-Curves Explained" reference stays prose (placement-map note). #2/#1 still need their c{N} overlays BUILT + pixel-verified at INTEGRATION (no baked labels): #1 annotate the four UI regions (Channel List / Graph View / header menu bar / native-blue Z-curve), positive-green badges; #2 is the 3-panel vertical stack &#8212; use the COMPOSITE-OVERLAY variant (one centered method badge per panel, no highlight ellipses; the baked `#FF6B00` 2px dividers carry panel separation).

### c{N} overlay id-registry (PNG overlays, per-PNG namespaced)

| PNG | callouts placed | notes |
|---|---|---|
| c31 (1572x307) | 1 warning-red | rings rounded auto valley f12 @ (616,279); badge "Default Auto handles &#183; soft, floaty valleys" |
| c32 (1572x307) | 2 green | Vector valley f12 @ (617,279); Horizontal apex f24 @ (813,166) |
| c33 (1572x307) | 1 green | Vector impact corner f36 @ (1026,279) |
| c34 (1572x307) | 1 green | Horizontal apex plateau f24 @ (963,82) |
| c1 (1920x976) | 4 green region callouts | ring+line+badge style. Each = circle r18 fill=none stroke #4ADE80 sw4 (ring on region) + line sw3 #4ADE80 + dot circle r5 #4ADE80 (connector) + rect h40 rx6 fill #4ADE80 + white text 22px/700 system-ui centered. Badges: "Channel List" (rect x40 y712 w240, ring 150,600), "Header Menu Bar" (rect x80 y162 w260, ring 200,20), "Graph View" (rect x560 y200 w200, ring 960,250), "F-Curve" (rect x1150 y160 w200, ring 470,126 on the blue Z-curve). No ellipses, no element ids (positional). Figcaption PREFIXED "Figure 1:" |
| c2 (1600x2146) | 3 green Method badges | composite-overlay variant. 3 green "Method 1/2/3" badges, rect x640 w320 h40 rx6 fill #4ADE80, white 22px/700 centered at y=367/1289/1700. No ellipses, no ids (baked `#FF6B00` 2px dividers carry panel separation). Figcaption PREFIXED "Figure 2:" |


---

## Invariant baseline (live HTML, pre-integration, UTF-8-aware)

Captured 2026-06-24 by copying the HTML Claude-side and counting with Python (UTF-8).

| Metric | Baseline (now) | Close target (projected) |
|---|---|---|
| bytes | 125,105 | 291,776 (FINAL, on disk) |
| figure (`<figure`) | 0 | 25 |
| figcaption | 0 | 25 |
| img | 0 | 6 (PNG count) |
| svg (`<svg`) | 0 | 25 (19 inline SVG + 6 PNG overlays) |
| mermaid (`class="mermaid"` diagrams) | 0 | 0 |
| em-dash U+2014 | 34 | 34 (preserve verbatim, re-verify every checkpoint) |
| color222 (`color="#222"`) | 0 | 0 |

Note: the live HTML has 4 literal `mermaid` substring hits, all QA-harness script/CSS loader references, NOT rendered `<div class="mermaid">` diagrams &#8212; diagram count is 0 and the close target stays 0. The 34 em-dashes are pre-existing prose, preserved verbatim; 0 new em-dashes added.

Close target counts derive from the build-now roster split below (25 figures: 19 inline SVG + 6 PNG; each PNG carries one `c{N}` overlay SVG, so svg total = 19 + 6 = 25; img = 6).

---

## Section IDs (confirmed against live HTML, document order)

All 9 placement-target sections from the requirements doc placement map exist verbatim:

1. `what-is-graph-editor` &#8212; What is the Graph Editor
2. `interface` &#8212; Graph Editor Interface
3. `reading-curves` &#8212; Reading F-Curves
4. `curve-handles` &#8212; Understanding Handles
5. `editing-curves` &#8212; Editing Curves and Keyframes
6. `handle-types` &#8212; Handle Types Mastery
7. `curve-modifiers` &#8212; F-Curve Modifiers
8. `project` &#8212; Project: Perfect Bounce
9. `summary` &#8212; Lesson Summary

(`final-encouragement` also present; no figures placed there.)

**Indentation depth (carry forward from L25, against L24/L25 live convention): ALL figures sit at the SAME depth &#8212; `<figure>` at col20, direct children (svg / wrapper-div / figcaption) at col24, overlay-svg inner content at col28.** Every figure is inserted AFTER the relevant card's closing `</div>` (a section-level sibling), never nested inside a card &#8212; even the ones the placement map describes as "within a card". Confirm exact anchors against the LIVE file at INTEGRATION before each commit (card subheadings must be unique 1x; extend non-unique `</div>`+`<blockquote>`/`<h3>` anchors up to the preceding unique line as in L25).

---

## Roster (resolved 2026-06-24)

Roster mirrors the L24 / L25 decision: **build High + Medium tiers now; defer Lower tier** unless reopened. Tiers taken verbatim from the requirements-doc Production Priority section.

**BUILD-NOW: 25** (High 11 + Medium 14).
- **High 11:** #1, #5, #6, #7, #8, #13, #14, #16, #31, #32, #36.
- **Medium 14:** #2, #3, #4, #10, #11, #12, #18, #19, #24, #25, #26, #33, #34, #37.

**DEFER: 15 Lower** &#8212; #9, #15, #17, #20, #21, #22, #23, #27, #28, #29, #30, #35, #38, #39, #40 (out of scope unless reopened).

### Format split (build-now 25)

Format assignment = requirements-doc file-naming-convention block (`.svg` vs `.png`) intersected with build-now tiers. This lesson is SVG-heavy &#8212; Graph Editor teaching is mostly synthetic curve / handle / easing diagrams, not UI captures.

**Inline SVG &#8212; 19:** #3, #4, #5, #6, #7, #8, #10, #11, #12, #13, #14, #16, #18, #19, #24, #25, #26, #36, #37.
- Navigation infographic (#3), channel-hierarchy tree (#4), the four basic-curve diagrams (#5 linear / #6 S-curve / #7 sharp peak / #8 flat plateau), the three real-world curve examples (#10 bouncing ball / #11 door opening / #12 pulsing glow), handle anatomy (#13), handle-length comparison (#14), handle-type comparison chart (#16), the two common-handle-pattern diagrams (#18 ease in/out / #19 bounce), the three problem-diagnosis composites (#24 robotic / #25 floating / #26 overshoot), the shortcuts reference card (#36), the professional-workflow flowchart (#37).

**PNG &#8212; 6:** #1, #2, #31, #32, #33, #34.
- Real Graph Editor UI captures: #1 interface overview (annotated), #2 opening-methods composite (3-panel). Project curve screenshots (Graph Editor F-curve captures): #31 Z-curve before refinement, #32 Z-curve after refinement, #33 impact-keyframe detail (zoom), #34 apex-keyframe detail (zoom).
- Each PNG gets one `c{N}` overlay SVG at INTEGRATION (no baked-in callouts; overlay viewBox === PNG px). Per-PNG `c{N}-*` id registry maintained at INTEGRATION.

### Style note (IMPORTANT &#8212; same as L25)

The requirements doc + AI prompts were written against **Blender 4.0** with a **literal dark-panel color scheme** (`#2b2b2b` backgrounds, `#00ffff` cyan curves, `#ff8c00` orange, `#ffff00` yellow handles, `#33ff33`/`#3333ff`/`#ff3333` channel colors). Those prompts are a CONTENT spec, not a STYLE spec. **All SVGs follow `IMAGE_STYLE_GUIDE.md` v6:** transparent / page-inheriting via `currentColor`, brand orange `#FF6B00` top accent, the locked palette (axis X=`#E63946` red / Y=`#4ADE80` green / Z=`#4A9EFF` blue &#8212; which conveniently matches Blender's own gizmo colors the prompts intend), positive-green / warning-red overlay badge split, `&#183;` separators, zero new em-dashes. Curve color uses brand orange or a palette role, NOT `#00ffff` cyan. This mirrors how L25's prompts were treated. Target Blender for any capture = 5.1.1 (course-wide), NOT 4.0.

### Open roster Q's &#8212; RESOLVED

- **Q: build-now boundary?** A: High + Medium (25), defer Lower (15). Locked, mirrors L24/L25.
- **Q: format of the diagnosis composites #24/#25/#26?** A: Inline SVG (problem/solution curve pairs are synthetic diagrams, `.svg` in the naming block). Locked.
- **Q: do the 4.0 prompt hexes govern style?** A: No &#8212; v6 governs. Prompts are content-only. Locked (see Style note).
- **Q: shared project scene for #31/#32/#33/#34?** A: One staged bouncing-ball scene (the L25 #27/#28 bounce ladder is a natural reuse: Ball Z-Location f1=8/f12=1/f24=5/f36=1/f44=3/f52=1/f58=2/f64=1, 24 fps) drives all four Graph Editor F-curve captures (#31 default Auto handles = before; #32 refined handles = after; #33 impact-valley zoom; #34 apex-plateau zoom). Stage once, verify Blender TRUE-PRISTINE before + after. Detailed keyframe + handle-refinement spec deferred to PNG-production plan-lock.

---

## Per-file id-prefix registry (inline SVG)

Assign at SVG-production time (unique 2&#8211;3 char prefix per file to avoid cross-SVG id collisions on the page). To be filled in as each SVG is built:

| # | slug | id-prefix |
|---|---|---|
| 03 | navigation_controls | nc |
| 04 | channel_hierarchy | ch |
| 05 | curve_linear | cl |
| 06 | curve_s_shape | cs |
| 07 | curve_sharp_peak | cp |
| 08 | curve_flat_plateau | cf |
| 10 | example_bouncing_ball | bb |
| 11 | example_door_opening | dr |
| 12 | example_pulsing_glow | pg |
| 13 | handle_anatomy | ha |
| 14 | handle_length_comparison | hl |
| 16 | handle_types_chart | ht |
| 18 | pattern_ease_in_out | ei |
| 19 | pattern_bounce | bn |
| 24 | diagnosis_robotic_motion | dx |
| 25 | diagnosis_floating_motion | df |
| 26 | diagnosis_overshoot | do |
| 36 | shortcuts_reference | sr |
| 37 | workflow_diagram | wf |

## PNG-production plan (PLAN-LOCKED + DONE 2026-06-24)

Capture-path per PNG (v6 decision 20 routing). No baked labels on any PNG; every callout is the `c{N}` overlay SVG added at INTEGRATION (overlay viewBox === PNG px, 1:1). The four project curves (#31&#8211;34) rode ONE staged bouncing-ball scene (TRUE-PRISTINE verified before + after &#8212; both passes recorded in the status log).

| # | slug | placement | capture path (as built) | PNG px | status |
|---|---|---|---|---|---|
| 01 | interface_overview | interface | docked Graph Editor maximized to full window, full-window `screen.screenshot` to local tempdir + deterministic area crop; native-blue Z-curve, 8 keys + handles, channel region + header | 1920x976 | DONE |
| 02 | opening_methods | interface | 3-panel composite, all Win+Shift+S (Ray): editor-type dropdown popover (Graph Editor highlighted) + Animation-workspace tab/layout + split-window (Resize Region). Common-width (1600) VERTICAL stack, `#FF6B00` 2px dividers, Pillow composite. Panel temps removed via `os.remove` | 1600x2146 | DONE |
| 31 | project_curve_before | project | docked Graph Editor F-curve, default AUTO_CLAMPED handles (before); `graph.view_all` frame, full-window screenshot + crop (3,704,1575,1011) | 1572x307 | DONE |
| 32 | project_curve_after | project | docked Graph Editor F-curve, refined handles (after): VECTOR valleys + horizontal ALIGNED apexes, final key FREE-flat at rest; `graph.view_all` frame + crop | 1572x307 | DONE |
| 33 | project_impact_detail | project | docked Graph Editor zoom on f=36 impact valley (VECTOR sharp corner) flanked by f=24/f=44 flat apexes; `graph.view_selected(include_handles)` on f=24/36/44 + crop | 1572x307 | DONE |
| 34 | project_apex_detail | project | docked Graph Editor zoom on f=24 apex plateau (horizontal hang) flanked by f=12/f=36 sharp valleys; `graph.view_selected(include_handles)` on f=12/24/36 + crop | 1572x307 | DONE |

**Routing summary (as built):** Win+Shift+S (Ray drove): #02 all three panels (editor-type popover + Animation workspace + split-window). Docked-panel local-tempdir + crop (Claude scripted scene + framing via `graph.view_all`/`view_selected`, captured, cropped): #01, #31, #32, #33, #34. Composite: #02 (3 panels, vertical stack). **NOTE: the bridge-driven `screen.screen_full_area` maximize/restore STALLED the bridge once on the #01 restore step (required a Blender restart); avoided thereafter by capturing the DOCKED editor + cropping to the area rect &#8212; no maximize needed for #31&#8211;34.** `graph.view_all` and `graph.view_selected(include_handles=True)` ARE bridge-drivable for framing/zoom (the L22 "framing not bridge-drivable" caveat did not hold here for view-all / view-selected). Per-PNG c{N} overlay viewBox === the PNG px in the table above.

---

## Placement map (from requirements doc, build-now figures only)

- **what-is-graph-editor:** #37 (after "Why the Graph Editor Matters"), #6 (F-Curves Explained card), [#9 steepness deferred].
- **interface:** #1 (section opening), #2 (Opening the Graph Editor card), #3 (Navigation Controls card), #4 (Channel List Organization card).
- **reading-curves:** #5, #6, #7, #8 (Basic Curve Interpretation card &#8212; note #6 also referenced in what-is; place the single #6 figure once, in reading-curves, and route the what-is reference to text), #10, #11, #12 (Real-World Curve Examples card), #24, #25, #26 (Diagnostic Reading card). [#9 steepness deferred.]
- **curve-handles:** #13 (What Are Handles? opening), #14 (Handle Length and Influence card), #18, #19 (Common Handle Patterns card). [#15 angle effects deferred.]
- **editing-curves:** [all Lower &#8212; #20/#21/#22/#23 deferred; no build-now figures here.]
- **handle-types:** #16 (section opening). [#17 manipulation, #40 in-action deferred.]
- **curve-modifiers:** [all Lower &#8212; #27/#28/#29 deferred; no build-now figures here.]
- **project:** #31 (Phase 2 Analyze), #32 + #33 + #34 (Phase 3 Refine Location Z). [#30 setup, #35 scale-sync, #39 quality deferred.]
- **summary:** #36 (Essential Keyboard Shortcuts card). [#38 multi-channel deferred.]

NOTE: #6 appears in two placement suggestions (what-is-graph-editor and reading-curves). One physical figure; place in reading-curves (its primary home as a basic-curve type) and leave the what-is reference as prose. Confirm at INTEGRATION. Likewise #5&#8211;8 are a 4-figure cluster in the Basic Curve Interpretation card.

---

## Locked rules (cross-reference, do not restate specs)

All standing rules carry from `session.md` RULES block and `IMAGE_STYLE_GUIDE.md` v6 Locked decisions (1&#8211;21). Not restated here. Key reminders specific to this lesson's surfaces:

- **Style: v6 governs, NOT the 4.0-era prompt hexes.** Transparent/`currentColor`; brand orange `#FF6B00`; axis X=`#E63946`/Y=`#4ADE80`/Z=`#4A9EFF`; curve lines in orange or palette role, never `#00ffff`. Target Blender 5.1.1 for captures.
- Graph Editor UI captures (#1, #31&#8211;34) use docked-panel full-window local-tempdir `screen.screenshot` + deterministic area crop (v6 decision 20); maximize-isolate per the L18 maximize-stall fix. #02's editor-type dropdown is a transient popover &#8594; Win+Shift+S overlay-freeze.
- Pixel-verify every PNG overlay (cairosvg Claude-side) before `edit_file` commit (v6 decision 19).
- Blender TRUE-PRISTINE verified before + after any bridge work (#31&#8211;34 shared scene staging).
- 0 new em-dashes; preserve the 34 pre-existing verbatim; re-verify at every checkpoint.
- Edit cadence: `Filesystem:edit_file` dryRun &#8594; commit &#8594; `get_file_info`; bottom-up insertion order at INTEGRATION.
- Batching cadence: 1 pilot SVG, then batches of 3 (v6 Workflow conventions). INTEGRATION in its own chat (&#8805;6 figures &#8212; this lesson has 25). Sub-chat splits at &#8805;5 figures per production phase. Warn Ray before context gets long rather than letting auto-compaction occur.

---

## Status log

- **2026-06-24 &#8212; INTEGRATION COMPLETE &#183; L26 CLOSED.** All 15 commits DONE (bottom-up), 25 / 25 figures placed, browser QA PASSED. Re-confirmed live invariants as first action (get_file_info + UTF-8 re-count Claude-side): **291,776 bytes / figure 25 / figcaption 25 / img 6 / svg 25 (19 inline + 6 overlay) / mermaid 4 (CSS/JS harness noise, 0 rendered diagrams) / em-dash U+2014 = 34 / color222 0** &#8212; matches the projected close target exactly; no HTML edits needed. Browser QA (prior session, Playwright/Chromium, file:// with the 6 PNGs + main.css staged Claude-side) PASSED at BOTH 380px and 900px: 0 horizontal overflow; all 25 figures non-zero size; 6 overlays detected, 0 mistrack (overlay svg box matches img box); Figure 1 visually verified (4 callouts &#8212; Channel List / Header Menu Bar / Graph View / F-Curve &#8212; track regions, &#183;-separated caption renders). Filled the c1 + c2 overlay-registry rows (c1: 4 green ring+line+badge region callouts on #01 1920x976, Figure 1 caption; c2: 3 green Method 1/2/3 composite-overlay badges on #02 1600x2146, Figure 2 caption). Recorded final invariants. Updated session.md: L26 closed, handoff = thin pointer to this doc. Blender TRUE-PRISTINE (no bridge work in INTEGRATION).

- **2026-06-24 &#8212; PNG PRODUCTION COMPLETE &#8212; all 6 PNG DONE (#01, #02, #31, #32, #33, #34); ALL 25 / 25 L26 figures now produced.** Switched to the FULL v6 guide for the capture sections this phase. Blender verified TRUE-PRISTINE before AND after the bridge work (both passes match the L22-close baseline line-by-line). Captured each PNG with NO baked labels (all callouts deferred to c{N} INTEGRATION overlays; overlay viewBox === PNG px). Files + dims: `lesson_26_01_interface_overview.png` 1920x976 (78,622 B), `lesson_26_02_opening_methods.png` 1600x2146 (741,872 B), `lesson_26_31_project_curve_before.png` 1572x307 (50,204 B), `lesson_26_32_project_curve_after.png` 1572x307 (49,083 B), `lesson_26_33_project_impact_detail.png` 1572x307 (45,102 B), `lesson_26_34_project_apex_detail.png` 1572x307 (50,517 B).
  - **Shared bounce scene (#31&#8211;34):** staged a `Ball` UV-sphere with a single Z-Location F-curve, keys f1=8/f12=1/f24=5/f36=1/f44=3/f52=1/f58=2/f64=1 at 24 fps (frame range bounded 1&#8211;64 for clean framing). **#31 before** = the default insert state (5.1.1 inserts `AUTO_CLAMPED`, not raw `AUTO`; kept as-is &#8212; it reads as the soft auto-smoothed before-curve and is the truer 5.1.1 default a learner sees). **#32/#33/#34 after** = refined handles: the four ground-contact valleys (z=1 at f12/36/52/64) set to **VECTOR** (sharp pointed corners), the apexes + start (f1/24/44/58) set to **ALIGNED** flattened **horizontal** (rounded hang); the final rest key (f64) right handle set **FREE** + dead-flat so the tail settles at rest instead of diving below baseline (caught + fixed at QA &#8212; first after-capture had the VECTOR right-handle pulling the tail to z=0). #33 zooms the f=36 impact valley (flanked by f=24/f=44 apexes), #34 zooms the f=24 apex plateau (flanked by f=12/f=36 valleys), both via `graph.view_selected(include_handles=True)` on the 3-key selection. The Z F-curve renders in Blender's native blue (matches the locked Z=`#4A9EFF`; no cyan). Teardown after #34: `animation_data_clear`, removed Ball + orphaned mesh, purged actions, reset frame range to 1&#8211;250 &#8212; re-verified TRUE-PRISTINE.
  - **5.1.1 layered-action API (carry-forward, same as L25):** `action.fcurves` is GONE; F-curves live under `act.layers[].strips[].channelbags[].fcurves`. Iterate that path to read/edit keyframe handles.
  - **Capture craft this phase:** the docked Graph Editor at its normal Animation-workspace geometry (area x=3 y=35 w=1572 h=307 in a 1920x1046 window) was captured via full-window `screen.screenshot` to the Windows tempdir, then bridge-side Pillow crop (3,704,1575,1011) with the bottom-left&#8594;top-left flip, saved straight to WSL `images/`. **`graph.view_all()` and `graph.view_selected(include_handles=True)` ARE bridge-drivable** for framing/zoom (under a VIEW_3D-style `temp_override` on the GRAPH_EDITOR area's WINDOW region) &#8212; the L22 "Graph-Editor framing is NOT bridge-drivable" caveat did not hold here; manual framing was not needed for #01/#31&#8211;34. **BRIDGE-STALL WARNING:** the bridge-driven `bpy.ops.screen.screen_full_area()` maximize/restore sequence (L18 isolated-step pattern) **STALLED the bridge on the #01 RESTORE call** (4-min timeout, required a full Blender restart). The #01 raw screenshot had already landed on the Windows tempdir before the stall and SURVIVED the restart, so #01 was not lost; re-staged the scene and captured #31&#8211;34 from the DOCKED editor (cropping to the area rect) with NO maximize, sidestepping the stall path entirely. **Lesson: for a docked editor whose full extent already fits the area, prefer full-window-screenshot + area-crop over bridge-maximize &#8212; the maximize/restore round-trip is the stall risk, and it is unnecessary when the docked area is large enough.** #01 used the one banked maximized raw (editor filled the 1920x976 window); #31&#8211;34 used the docked 1572x307 crop.
  - **#02 opening_methods composite:** all three panels are Win+Shift+S manual snips (Ray drove) &#8212; none reachable by the bridge (transient editor-type popover, workspace-tab state, split-window affordance). Panel 1 = editor-type dropdown popover with the **Graph Editor** entry (Shift F6) highlighted under Animation; panel 2 = the **Animation** workspace tab active + its full layout (3D viewport over docked Graph Editor); panel 3 = the split Graph Editor showing the **Resize Region** border affordance. p3 first failed to save to the expected path (ENOENT on first pre-flight) &#8212; Ray re-saved and it landed. The three panels had mismatched aspect ratios (1678x607, 2409x1638, 2409x715), so chose a **common-width (1600) VERTICAL stack** with `#FF6B00` 2px dividers over the plan's nominal "3 side-by-side" &#8212; side-by-side at common height would have crushed the tall middle panel. Built via Pillow (orange canvas showing through as the 2px divider gaps), saved to `images/`, removed all three `_temp_l26_02_p{1,2,3}.png` via bridge `os.remove`.

- **2026-06-24 &#8212; FINAL BATCH 6 of SVG production DONE (#03, #04, #36) &#8212; non-curve infographic + cheat-sheet set complete; ALL 19 / 19 inline SVG now done.** All three built Claude-side and cairosvg-verified light + dark before disk-write (0 U+2014, 0 cyan, `color="#222"` fallback retained, full a11y attrs `role`/`aria-labelledby`/`<title>`/`<desc>`). All three are the 800x680 dense variant, non-curve, and use the **FILLED-orange cheat-sheet badge variant** (solid `#FF6B00` rect / `#ffffff` text), chosen once and applied consistently across #03 and #36 (matches #16's V-key badge precedent). #03 `navigation_controls` (id `nc`, 6,422 bytes): two-column infographic &#8212; MOUSE column (MMB drag = Pan View, Scroll = Zoom In/Out, Shift+MMB = Pan Vertically, Ctrl+MMB = Drag to Zoom) and KEYBOARD column (Home = Frame All Curves, Numpad . = Frame Selected, Spacebar = Play Animation), each row a filled-orange key badge + bold action label + muted description; plus a `currentColor`-0.04 bottom panel "Lost in the curves? Frame your way back" reinforcing Home (frames everything) + Numpad . (frames just the selection) with two more filled badges. Footer tip: Emulate 3 Button Mouse / Alt+Left drag fallback. Caught + removed one dangling `&#183;` separator after "frames everything" at QA. #04 `channel_hierarchy` (id `ch`, 7,998 bytes): channel-list tree diagram on the folder-pilot tree-connector chassis (straight lines only, `currentColor` 0.3, object trunk + per-group sub-trunks + horizontal branch stubs). Root `Cube` &#8594; three transform groups (Location / Rotation Euler / Scale), each with X/Y/Z leaf rows; **every leaf color-coded by axis** (X=`#E63946` red / Y=`#4ADE80` green / Z=`#4A9EFF` blue) via a rounded swatch, with a tiny **sample curve in the matching axis color** beside each channel (flat-ish for Location/Rotation, near-flat lines for Scale). A right-side `currentColor`-0.04 legend panel maps each axis swatch to its motion (left/right, forward/back, up/down) with the locked note "Same colors as the move gizmo"; group rows carry a muted `&#9662;` expand triangle. Footer tip: collapse-group / hide-unused-channels. #36 `shortcuts_reference` (id `sr`, 8,609 bytes): keyboard cheat-sheet, **2x2 grid of four grouped `currentColor`-0.04 cards** with color-coded top accent bars + headers &#8212; NAVIGATION (blue `#4A9EFF`: Home / Numpad . / MMB drag / Scroll / Spacebar), SELECTION (green `#4ADE80`: A / Alt+A / B / C / Shift+Click), EDITING (orange `#FF6B00`: G / G&#183;X/Y / S / R / V / Shift+D / X), INTERFACE (purple `#B47EE8`: N / T / Shift / Ctrl). Group-header colors reuse the workflow-flowchart (#37) step-accent palette for cross-figure consistency. **Layout fix at QA:** the EDITING card first drafted as two 5+3 columns at 40px pitch &#8212; the second column's badges (R/Shift+D/X) overlapped the first column's description text; rebuilt EDITING as a single compact 24px-pitch column of 7 entries (Shift+D Duplicate and X Delete share the final row with clear horizontal separation), re-verified clear. The other three cards were clean first pass. All three: 3px orange top accent, transparent/`currentColor`, `color="#222"` fallback retained, 0 em-dashes (`&#183;` separators; `&#9662;` triangle glyph in #04). Registry rows filled (nc/ch/sr). **Progress: ALL 19 / 19 inline SVG DONE** = {#03,#04,#05,#06,#07,#08,#10,#11,#12,#13,#14,#16,#18,#19,#24,#25,#26,#36,#37}. **SVG PRODUCTION PHASE COMPLETE.** **NEXT PHASE: PNG production** &#8212; 6 PNG (#1 interface_overview, #2 opening_methods, #31 project_curve_before, #32 project_curve_after, #33 project_impact_detail, #34 project_apex_detail); switch to the FULL v6 style guide for the capture-workflow / color-dependent-render / Blender-5.1.1-op sections (skipped during the SVG batches). The four project curves (#31&#8211;34) ride ONE staged bouncing-ball scene (verify Blender TRUE-PRISTINE before + after). Then INTEGRATION in its own chat, then browser QA at 380px + 900px. Blender still TRUE-PRISTINE (no bridge work this phase).

- **2026-06-24 &#8212; BATCH 5 of SVG production DONE (#25, #26, #37) &#8212; diagnosis trio closed + first flowchart.** All three built Claude-side and cairosvg-verified light + dark before disk-write (0 U+2014, 0 cyan, `color="#222"` fallback retained, full a11y attrs). #25 `diagnosis_floating_motion` (id `df`, 5,591 bytes): closes the diagnosis trio on #24's chassis &#8212; a bouncing-ball Z-curve between shared apex/impact/apex orange diamonds. PROBLEM = symmetrical mirror-parabola arc in warning-red `#E63946` dashed (`stroke-dasharray="2 9"`); FIX = asymmetric green `#4ADE80` arc (short hang then steep late fall into impact, then brisk rise easing into a long apex hang). Red "Problem: same rate up and down" badge (`#E63946`/`#ffffff`) + two green annotation badges (Fall: steep / Rise: gentle, `#4ADE80`/`#0f1a14`). Took two reshape passes: first draft's red+green tracked nearly identically (asymmetry didn't read at thumbnail scale) and the problem badge text overflowed; widened badge + redrew both curves for strong visible divergence, then refined the green rise to ease into the apex rather than snap up. #26 `diagnosis_overshoot` (id `do`, 5,651 bytes): rotation curve 0&#176;&#8594;90&#176; between shared start/end diamonds, with a brand-orange dashed `target value (90&#176;)` reference line. PROBLEM = red dashed curve that humps ABOVE the target near the end then settles back to it (`M 110 460 C 340 460 470 176 560 176 C 620 176 660 210 720 210`), with a dashed-red highlight ellipse ringing the hump; FIX = solid green curve that eases up and lands exactly on the target (flat green clamp handle + square handle point at the end). Red "Problem: passes the target" badge + green "Fix: Auto Clamped handles" badge. Footer-tip uses `&#8250;` chevron (`V &#8250; Auto Clamped`); `&#176;` degree glyph used. Trimmed target label + footer tip to clear the right edge. #37 `workflow_diagram` (id `wf`, 7,393 bytes): **first flowchart of the lesson, NON-curve.** Horizontal 4-step left-to-right pipeline of `currentColor`-tinted rounded node cards (fill `currentColor` opacity 0.04, top accent bar per step): STEP 1 BLOCK &#183; in the Timeline (blue `#4A9EFF`) &#8594; STEP 2 TIME &#183; in the Dope Sheet (green `#4ADE80`) &#8594; STEP 3 REFINE &#183; in the Graph Editor (orange `#FF6B00`) &#8594; STEP 4 POLISH &#183; in the Graph Editor (purple `#B47EE8`). Straight connectors only at `currentColor` opacity 0.3 with solid orange `#FF6B00` arrowhead triangles (no curves). Two phase brackets beneath: a neutral `currentColor`-0.3 bracket under steps 1&#8211;2 labelled "80% of the time / blocking and timing the action", and an orange-0.6 bracket under steps 3&#8211;4 labelled "20% of the time / but it makes all the difference". Node sequence + 80/20 split taken from the requirements-doc #37 content. All three: 3px orange top accent, transparent/`currentColor`, full a11y attrs, `color="#222"` fallback retained, 0 em-dashes. Registry rows filled (df/do/wf). **Progress: 16 / 19 inline SVG done** = {#05,#06,#07,#08,#10,#11,#12,#13,#14,#16,#18,#19,#24,#25,#26,#37}. **REMAINING 3 inline SVG = FINAL batch 6: #03 navigation_controls, #04 channel_hierarchy, #36 shortcuts_reference** (non-curve infographic + cheat-sheet set). Then 6 PNG (#1,#2,#31,#32,#33,#34). Blender still TRUE-PRISTINE.

  **Batch-6 content gloss (so the requirements doc need not be reopened):**
  - **#03 `navigation_controls`** (id tbd) &#8212; navigation infographic. Mouse/keyboard controls with icons + descriptions, organized as a grid/list: Middle Mouse drag = Pan View; Scroll Wheel = Zoom In/Out; Home = Frame All Curves; Numpad . = Frame Selected; Spacebar = Play Animation; Shift+Middle Mouse = Pan Vertically; Ctrl+Middle Mouse = Zoom. Non-curve; key glyphs use the orange accent. Likely the 800x680 dense variant if it crowds at 560. Cheat-sheet badge variant: pick one (filled or muted) and stay consistent with #36.
  - **#04 `channel_hierarchy`** (id tbd) &#8212; channel-list tree diagram, straight tree connectors only (no curves, `currentColor` 0.3 like the folder-structure pilot). Expandable hierarchy: Object &#8594; Location (X/Y/Z), Rotation Euler (X/Y/Z), Scale (X/Y/Z), with each leaf color-coded by axis (X=`#E63946` red / Y=`#4ADE80` green / Z=`#4A9EFF` blue) and a tiny sample curve swatch in its axis color beside each channel. Footer note: color coding matches the transform gizmo colors. Likely 800x680 dense (9 leaf rows + 3 group rows).
  - **#36 `shortcuts_reference`** (id tbd) &#8212; keyboard-shortcut cheat-sheet card, 4 grouped sections: NAVIGATION (Home / Numpad . / MMB drag / Scroll / Spacebar), SELECTION (A / Alt+A / B / C / Shift+Click), EDITING (G / G+X G+Y / S / R / V / Shift+D / X), INTERFACE (N / Shift / Ctrl / T). Key badges in the chosen cheat-sheet variant (filled orange or muted orange-border); group headers; non-curve. Almost certainly the 800x680 dense variant.

- **2026-06-24 &#8212; BATCH 4 of SVG production DONE (#18, #19, #24) &#8212; pattern pair + diagnosis trio opener.** All three built Claude-side and cairosvg-verified light + dark before disk-write (0 U+2014, 0 cyan, `color="#222"` fallback retained, full a11y attrs). #18 `pattern_ease_in_out` (id `ei`, 5,232 bytes): curve-chassis single monotonic ease-in/out cubic `M 110 460 C 360 460 470 170 720 170` (reuses #06's shape) with horizontal flat handle arms + square handle points at both ends, hero orange diamonds, three leader annotations (ease in / steep middle / ease out), green positive pill "Smooth &#183; the default for natural motion"; footer-tip em-dash caught at write and replaced with a sentence break (0 new em-dashes). #19 `pattern_bounce` (id `bn`, 5,292 bytes): chassis bounce-settle in brand orange &#8212; sharp pointed valleys at ground contact (short handle V-stubs drawn at the first valley to show the pointed-corner cause) alternating with rounded `Q`-arc peaks of decreasing height (4.7&#8594;3.3&#8594;2.0&#8594;1.0) settling to rest at frame 24; annotations for sharp valley / rounded peak / comes to rest. First "Sharp valley" annotation was left-anchored at x194 and clipped off the canvas left edge; re-anchored to x276 in open lower-middle space, re-verified clear of the peak label. Distinct from #10 (full Z-location bounce curve) by focusing on the handle pattern at the contact/apex points. #24 `diagnosis_robotic_motion` (id `dx`, 5,199 bytes): **opens the diagnosis trio &#8212; first figure to use the WARNING-RED `#E63946` problem/solution colorway.** Two-curve before/after on the chassis between the SAME two keyframes (110,460)&#8594;(720,170): the PROBLEM curve is a dashed straight diagonal `L`-line in warning-red `#E63946` (linear/vector handle arms, `stroke-dasharray="2 9"`) reading as "wrong"; the FIX curve is the solid green `#4ADE80` eased S (flat end handles + square points), shared orange diamonds on top. Red problem badge (`#E63946` rect / `#ffffff` text "Problem: straight line") + green fix badge (`#4ADE80` rect / `#0f1a14` text "Fix: ease the handles") per the v6 overlay-colorway split. Footer-tip menu chevron uses `&#8250;` (`T &#8250; Bezier`), matching #16's chevron precedent; 0 em-dashes. **Progress: 13 / 19 inline SVG done** = {#05,#06,#07,#08,#10,#11,#12,#13,#14,#16,#18,#19,#24}. **REMAINING 6 inline SVG:** #25 diagnosis_floating_motion + #26 diagnosis_overshoot (the other two diagnosis composites, warning-red colorway, build on #24's two-curve before/after pattern), #37 workflow_diagram (flowchart), and the non-curve set #03 navigation_controls + #04 channel_hierarchy + #36 shortcuts_reference. **NEXT: batch 5 of 3 = #25 `diagnosis_floating_motion`, #26 `diagnosis_overshoot`, #37 `workflow_diagram`** (closes the diagnosis trio + the flowchart). **FINAL batch 6 = #03 `navigation_controls`, #04 `channel_hierarchy`, #36 `shortcuts_reference`** (non-curve infographic + cheat-sheet set). 6 PNG (#1,#2,#31,#32,#33,#34) still not started. Blender still TRUE-PRISTINE.

- **2026-06-24 &#8212; BATCH 3 of SVG production DONE (#13, #14, #16) &#8212; handles cluster complete.** All three built Claude-side and cairosvg-verified light + dark before disk-write. #13 `handle_anatomy` (id `ha`, 5,129 bytes): curve-chassis variant on a single central keyframe (415,315) with the Bezier tangent drawn as a horizontal arm through the control point, square handle points at each arm end, hero orange diamond, three leader annotations (control point / handle point / handle tangent arm); an ease-in/out orange curve passes through at the tangent slope. #14 `handle_length_comparison` (id `hl`, 5,349 bytes): TWO curves between the SAME shared keyframes (150,430)&#8594;(680,200) &#8212; long-handle GREEN `#4ADE80` broad flat-start/flat-end S vs short-handle ORANGE `#FF6B00` near-straight steep diagonal; faint handle arms + square points in each curve's color, shared orange diamonds drawn on top; green/orange labelled annotations. First short-handle pass read too similar to the long curve; reshaped to a steep near-linear diagonal so the snappy-vs-smooth contrast reads at thumbnail scale; footer tip trimmed to fit plot width. #16 `handle_types_chart` (id `ht`, 9,658 bytes): **800x680 dense variant**, 2x3 cell grid of the five Blender 5.1.1 handle types + a how-to-set legend cell. Cells: AUTOMATIC (green band, smooth sloped tangent), AUTO CLAMPED (green band, flat peak / no overshoot), VECTOR (blue band, linear V), ALIGNED (yellow band, collinear tilted arms smooth pass), FREE (warning-red band + white text, broken-tangent sharp corner), Setting-a-type (orange left-rail legend with a `V` key badge). Free corner first read too smooth; reshaped to a two-segment angular kink so it reads distinctly from ALIGNED's smooth collinear pass; footer tip trimmed. Cell band colorways follow the palette / positive-green / warning-red split (per v6); cell tints use `currentColor` at 0.04 / 0.06 so they cascade in dark mode. All three: 3px top accent, transparent/`currentColor`, full a11y attrs (`role`/`aria-labelledby`/`<title>`/`<desc>`), `color="#222"` fallback retained, 0 em-dashes (`&#183;`; `&#8250;` used as the menu chevron in #16). Registry rows filled (ha/hl/ht). **Progress: 10 / 19 inline SVG done** = {#05,#06,#07,#08,#10,#11,#12,#13,#14,#16}. **REMAINING 9 inline SVG:** #03 navigation_controls + #04 channel_hierarchy (the two non-curve infographics), #18 pattern_ease_in_out + #19 pattern_bounce (curve-chassis pattern pair), #24 diagnosis_robotic_motion + #25 diagnosis_floating_motion + #26 diagnosis_overshoot (problem/solution curve composites &#8212; warning-red reserved here), #36 shortcuts_reference (cheat-sheet card), #37 workflow_diagram (flowchart). **NEXT: batch 4 of 3 = #18 `pattern_ease_in_out`, #19 `pattern_bounce`, #24 `diagnosis_robotic_motion`** (#18/#19 inherit the curve chassis; #24 opens the diagnosis trio using the warning-red problem/solution colorway). Then a closing batch for #25/#26/#37 and a batch for the #03/#04/#36 non-curve set &#8212; final sequence to confirm at next chat start. 6 PNG (#1,#2,#31,#32,#33,#34) still not started. Blender still TRUE-PRISTINE.

- **2026-06-24 &#8212; BATCH 2 of SVG production DONE (#10, #11, #12) &#8212; Real-World Curve Examples cluster complete.** All three built Claude-side on the curve-graph chassis and cairosvg-verified light + dark before disk-write. Axis-role curve colors applied per content (per the v6 axis convention): #10 `example_bouncing_ball` (id `bb`, 5,177 bytes) Z-Location curve in **Z-blue `#4A9EFF`** &#8212; multi-bounce parabola via `Q` arcs, sharp valleys at frames 10/20(approx)/25/33, rounded peaks at decreasing heights (5&#8594;4&#8594;2.5&#8594;1), frame axis 0&#8211;40, annotations for hang-time peak / impact valley / energy loss. #11 `example_door_opening` (id `dr`, 4,951 bytes) rotation curve in **X-red `#E63946`** &#8212; reuses the monotonic ease-in/out cubic from #06, degree axis 0&#176;/30&#176;/60&#176;/90&#176;, slow-start / fast-swing / slow-settle labels, green positive pill. #12 `example_pulsing_glow` (id `pg`, 4,967 bytes) emission curve in **brand orange `#FF6B00`** (emission has no axis role) &#8212; 3-cycle cubic-bezier sine (period 10 frames, valleys value 1 / peaks value 5), keyframe diamonds on every extremum, muted-orange Cycles-modifier hint pill. All: 3px curves, transparent/`currentColor`, 3px top accent, full a11y attrs, `color="#222"` fallback retained, 0 em-dashes (`&#183;`; degree glyph `&#176;` used in #11). **Progress: 7 / 19 inline SVG done (#05&#8211;08, #10&#8211;12).** **NEXT: batch 3 of 3 = #13 `handle_anatomy`, #14 `handle_length_comparison`, #16 `handle_types_chart`** (handles cluster &#8212; #13/#14 are curve-chassis variants with visible Bezier handles; #16 is a 2x3 reference grid, likely the 800x680 dense variant). Blender still TRUE-PRISTINE.
- **2026-06-24 &#8212; BATCH 1 of SVG production DONE (#06, #07, #08) &#8212; basic-curve cluster complete (#05&#8211;08).** All three built Claude-side on the locked curve-graph chassis and cairosvg-verified light + dark before disk-write. #06 `curve_s_shape` (id-prefix `cs`, 4,488 bytes): first cubic path produced a non-monotonic wave (reversed direction mid-curve); replaced with a single monotonic ease-in/out cubic `M 110 460 C 400 460 430 170 720 170` (flat tangents both ends, steep middle), re-verified correct; positive-green pill "Natural, organic motion". #07 `curve_sharp_peak` (id-prefix `cp`, 4,419 bytes): line-to-apex with a small `Q` rounding at frame 12, caution-yellow pill "Bounce &#183; impact behavior". #08 `curve_flat_plateau` (id-prefix `cf`, 4,648 bytes): flat orange line held at value 5 (y=215, tick relabeled to match), horizontal handle stubs, double-headed no-movement arrow, course-blue info pill "Stationary &#183; paused / held pose". All: brand-orange `#FF6B00` 3px curves (no cyan), transparent/`currentColor`, 3px top accent, full a11y attrs, `color="#222"` fallback retained, 0 em-dashes (`&#183;`). Pill colorway convention now exercised across the cluster: positive=green (#06), caution=yellow (#05/#07), info=blue (#08); warning-red reserved for the true problem/solution figures #24/#25/#26. Registry rows filled. **Progress: 4 / 19 inline SVG done (#05, #06, #07, #08).** **NEXT: batch 2 of 3 = #10 `example_bouncing_ball`, #11 `example_door_opening`, #12 `example_pulsing_glow`** (Real-World Curve Examples cluster &#8212; same chassis, axis-role curve colors per content: bounce Z=blue, door rotation=red, glow=palette). Blender still TRUE-PRISTINE.
- **2026-06-24 &#8212; SVG PRODUCTION started; PILOT #05 `curve_linear` DONE.** Pilot SVG built Claude-side, cairosvg-verified on both light and dark backgrounds (curve in brand orange `#FF6B00` 3px NOT cyan; transparent/`currentColor` cascades correctly to light text on dark bg; 3px orange top accent; `role`/`aria-labelledby`/`<title>`/`<desc>` present; `color="#222"` fallback retained for standalone file; 0 em-dashes, `&#183;` separators used; caution pill widened to fit text after first render showed overflow). Written to `images/lesson_26_05_curve_linear.svg` (4,414 bytes on disk, verified via `get_file_info`). id-prefix `cl` registered. This locks the reusable curve-graph chassis (header + plot frame x110..720/y170..460 + frame/value axes + ticks 1/12/24 & 0/5 + orange keyframe diamonds + leader-line annotations + Variant-B footer tip) that #06/#07/#08 and downstream curve diagrams (#10/#11/#12, #18/#19, #24/#25/#26) inherit. **NEXT: batch 1 of 3 = #06 `curve_s_shape`, #07 `curve_sharp_peak`, #08 `curve_flat_plateau`** (completes the basic-curve cluster, same chassis). Then continue batches of 3. Blender still TRUE-PRISTINE (no bridge work this phase).
- **2026-06-24 &#8212; L26 OPENED + PLAN-LOCKED.** L25 closed this chat (both optional cosmetic tidies applied to the live file: figure #13 wrapper `max-width:560px`&#8594;`width:546px` shrink-wrap, figure #26 double-blank-line collapsed; re-verified on disk 256,978 bytes / figure 27 / figcaption 27 / img 13 / svg 27 / mermaid 0 / em-dash 60 / color222 0 &#8212; all invariants pass). Created this progress doc. Confirmed next-lesson title "Graph Editor Essentials" via `curriculum_overview.md` (Module 6, lesson 26, lesson 3 of 4). Captured invariant baseline against live HTML (UTF-8-aware Python count): bytes 125,105 / figure 0 / figcaption 0 / img 0 / svg 0 / mermaid-diagram 0 / em-dash 34 / color222 0. Confirmed all 9 placement-target section IDs present verbatim. Resolved roster: 25 build-now (High 11 + Medium 14), 15 Lower deferred (#9,#15,#17,#20,#21,#22,#23,#27,#28,#29,#30,#35,#38,#39,#40). Locked format split: 19 inline SVG (#3,#4,#5,#6,#7,#8,#10,#11,#12,#13,#14,#16,#18,#19,#24,#25,#26,#36,#37) + 6 PNG (#1,#2,#31,#32,#33,#34). Flagged the style note: 4.0-era prompt hexes are content-only; v6 palette governs (curve lines orange/palette not cyan; capture target Blender 5.1.1). Set projected close target: figure 25 / figcaption 25 / img 6 / svg 25 / mermaid 0 / em-dash 34 / color222 0. Blender TRUE-PRISTINE. Next: SVG production (pilot first, then batches of 3); recommend a fresh chat for the production run.
