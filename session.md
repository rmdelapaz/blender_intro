# Blender Course · **L49 (Architectural Visualization) INTEGRATION COMPLETE + HARD-VERIFIED (2026-07-04). Module 11, lesson 3. 14/14 figures live (14 inline SVG, 0 PNG). NEXT = BROWSER QA (own fresh chat).**
Live HTML `lesson_49_architectural_visualization.html` (project root `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`) now **288,624 B / md5 `5257df57c463c645fb8ce6063f661c78` / LF-only / 0 CR** (Phase-0 baseline 232,555 -> 288,624 = +56,069 B). All 14 SVGs inlined at their §Section anchors (immediately after each h3, before its first prose child), each root ` color="#222"` STRIPPED, each wrapped `<figure>`+`<figcaption>` (figcaption == that SVG's `<title>`). **0 PNG** (roster verdict, L45/L47-parallel). Write path = carry-forward: Claude-side transform hard-asserted (30/30), reproduced SERVER-SIDE via Blender bridge reading source HTML + 14 SVGs from WSL disk (source re-verified pristine 1ab3e179 at commit -> dry-run md5 == 5257df57 byte-exact BEFORE commit -> binary LF write -> get_file_info==288,624 -> fresh independent Filesystem re-read re-assert ALL 30 PASS); no `read_factory_settings`. POST-INTEGRATION INVARIANTS (30/30 PASS on fresh disk re-read): figure 14 / figcaption 14 / inline svg 14 / img 0 / aria-labelledby 14 / role="img" 14; color="#222" 0; U+2014-literal 0 / U+2013 0 / U+00B7 0 / #667eea 0 / #764ba2 0 / linear-gradient 0; mermaid 7 / jsdelivr 1 / `<table>` 8 / table-wrap 8 / overflow-x:auto 8 unchanged; U+2192 1 (nav arrow) held; section id 10 order==TOC unchanged; back lesson_48 1 / next lesson_50 1 unchanged; all 14 inline svg XML well-formed; all 14 figcaptions == their SVG `<title>`; each figure inserted once at its registry anchor. Full integration record + anchor map in `lesson_49_integration_progress.md` (§INTEGRATION COMPLETE). **NEXT PHASE = BROWSER QA (own fresh chat):** copy live HTML Claude-side byte-exact (md5 + 288,624 B + 0 CR match disk), serve local http, local mermaid@10.9.6 ESM (CDN blocked), Chromium 1440x900 + 390x844; re-assert the 30 invariants on the fresh copy before serving; no image staging (0 PNG); inline SVGs carry `width="760"` no responsive clamp -> mobile fit main.css-dependent (L45/L47/L48 finding); external site css/js 404s expected sandbox-only. RULES (carry-forward): `\\wsl$\` paths only; Filesystem read tools + `write_file` reach `\\wsl$\` byte-exact (str_replace/edit + bash CANNOT reach UNC); Blender-bridge host also sees the WSL FS via UNC (server-side transform/write, dry-run md5 match before commit, binary + normalize CRLF->LF + get_file_info==byte-count; do NOT call `read_factory_settings`); browser QA Claude-side (local mermaid ESM, CDN blocked). Warn Ray to start a fresh chat when the conversation gets long.

---

## L48 QA-PASS ARCHIVE HEAD (superseded 2026-07-04 by L49 integration)
BROWSER QA PASSED, no live edit needed. LIVE HTML unchanged **325,411 B / md5 `bdb56cdfb703a5bc593ff7c67c5f42f8` / LF-only / 0 CR**. QA harness = real Chromium (Playwright chromium-1194 at `/opt/pw-browsers`, `--no-sandbox`, `--proxy-server=direct://`) over local http; live file copied Claude-side byte-exact (md5 + 325,411 B + 0 CR match disk); the 4 PNGs staged + all 4 md5s/dims re-verified against §PNG build log (P1 d928ef4c 900x900 / P2 88acb956 900x900 / P3 204e52a6 1824x956 / P4 2261b02f 1280x1280); all 34 post-integration invariants re-asserted on the fresh copy BEFORE serving (figure 18/18, figcaption 18/18, img 4, inline svg 14, aria-labelledby 14, role="img" 14, color=#222/#667eea/#764ba2/purple-gradient/U+2014-literal/U+2013/U+00B7 all 0, U+2192 166, &#8212; 126, mermaid 8, jsdelivr 1, table/table-wrap 7/7, linear-gradient 1 green #4CAF50, back L47 1/next L49 1, section id 16 order==TOC, all 14 inline svg XML well-formed, each PNG referenced once) — all pass. QA-only render copy rewrote the one jsdelivr mermaid import to LOCAL mermaid@10.9.6 ESM (`vendor/mermaid-dist/`, npm dist, CDN blocked) — live import line NOT touched. Tested desktop 1440x900 + mobile 390x844. RESULTS (both widths): 18/18 `<figure>` + 18/18 `<figcaption>`; 14 in-figure inline `<svg>` render non-zero bbox (0 zero-size); 4 `<img>` served 200 with natural dims matching disk (900x900 / 900x900 / 1824x956 / 1280x1280); 8 `.mermaid` → 8 rendered `<svg>` (local ESM, all chunks 200, no parse errors); 0 pageerrors; desktop h-overflow 0 (scrollW 1440). Only console errors = 4 expected site-scaffold 404s (styles/main.css, js/clipboard.js, js/course-enhancements.js, favicon.png), resolve on Ray's real server. **MOBILE FINDING (sandbox-only, not a live defect) — L47-parallel:** at 390px raw h-overflow was 422 (scrollW 812) because L48's 14 inline SVGs carry fixed `width="760"` and the document's own `<style>` has NO `figure svg{max-width:100%}` clamp — mobile SVG clamping depends entirely on the site `styles/main.css` (404 in sandbox). Offenders confirmed = the inline `<svg>/<rect>/<text>/<g>` at width 760. Verified: injecting the real `figure svg{max-width:100%;height:auto}` rule drops mobile h-overflow to 0 with all 8 mermaid still rendering. The 4 PNG `<img>` carry inline max-width:100% so they already clamp without main.css. On Ray's real server (main.css served) the overflow does not occur — flagged as a live main.css dependency (same class as L45/L47), NOT a QA failure. **VERDICT: PASS.** Full QA record in `lesson_48_integration_progress.md` §BROWSER QA. **L48 pipeline COMPLETE end-to-end (Phase 0 → roster triage → SVG 14 + PNG 4 → integration → browser QA PASS). NEXT = L49, own fresh chat, start at Phase 0.** RULES (carry-forward): `\\wsl$\` paths only; Filesystem read tools + `write_file` reach `\\wsl$\` byte-exact (str_replace/edit + bash CANNOT reach UNC); Blender-bridge host sees WSL FS via UNC (server-side transform/write, dry-run md5 match before commit, binary + normalize CRLF→LF + get_file_info==byte-count; do NOT call `read_factory_settings` — tears down addon socket); browser QA runs Claude-side (copy live file + PNGs, serve local http, local mermaid@10.9.6 ESM since CDN blocked, external site css/js 404s expected sandbox-only). Warn Ray to start a fresh chat when the conversation gets long.

---

## L48 INTEGRATION ARCHIVE HEAD (superseded 2026-07-04 by L48 QA PASS)

# Blender Course · **L48 (Product Visualization Project) INTEGRATION COMPLETE + HARD-VERIFIED (2026-07-04). Module 11, lesson 2. 18/18 figures live (14 inline SVG + 4 PNG). NEXT = BROWSER QA (own fresh chat).**
Live HTML `lesson_48_product_visualization_project.html` (project root `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`) now **325,411 B / md5 `bdb56cdfb703a5bc593ff7c67c5f42f8` / LF-only / 0 CR** (Phase-0 baseline 258,778 -> 325,411 = +66,633 B). All 14 SVGs inlined at their §SVG-registry section anchors, each root ` color="#222"` STRIPPED, each wrapped `<figure>`+`<figcaption>` (figcaption == SVG `<title>`); all 4 PNGs inserted as `<figure><img src="images/lesson_48_PN_slug.png" ... style="max-width:100%;height:auto;"/>`+`<figcaption>` at their §PNG-registry anchors (P1/P2 creating-materials, P3 lighting-setup, P4 final-render). Write path = carry-forward: Claude-side transform hard-asserted (34/34), reproduced SERVER-SIDE via Blender bridge reading source + 14 SVGs from WSL disk (source re-verified pristine eb6c10d3 at commit -> dry-run md5 == bdb56cdf byte-exact BEFORE commit -> binary LF write -> get_file_info==325,411 -> fresh independent Filesystem re-read re-assert ALL 34 PASS); bridge spec = compact ASCII-JSON string arg (1,844 B), figure blocks rebuilt host-side from on-disk SVGs (not streamed); no `read_factory_settings` (tears down addon socket). POST-INTEGRATION INVARIANTS (34/34 PASS on fresh disk re-read): figure 18 / figcaption 18 / img 4 / inline svg 14 / aria-labelledby 14 / role="img" 14; color="#222" 0 / #667eea 0 / #764ba2 0 / purple-gradient 0; **U+2014 literal 0** / U+2192 166 HOLD / U+2013 0 / U+00B7 0; mermaid 8; jsdelivr 1; `<table>` 7 / table-wrap 7; linear-gradient 1 (green #4CAF50 KEPT); back lesson_47 1 / next lesson_49 1; section id 16 (order==TOC); each PNG referenced once at its anchor; all 14 inline svg XML well-formed; all 14 figcaptions == their SVG `<title>`. **NOTE** `&#8212;` total 82 -> 126 (+44 inside SVG figure content + 2 PNG captions; body-prose 82 HELD; real invariant U+2014-literal-0 holds — L44/L45/L47-parallel split). Full integration record + anchor map in `lesson_48_integration_progress.md` (§INTEGRATION COMPLETE). **NEXT PHASE = BROWSER QA (own fresh chat):** copy live HTML Claude-side byte-exact (md5 + 325,411 B + 0 CR match disk) + stage the 4 PNGs (md5s per §PNG build log), serve local http, local mermaid@10.9.6 ESM (CDN blocked), Chromium 1440x900 + 390x844; re-assert the 34 invariants on the fresh copy before serving; external site css/js 404s expected sandbox-only; inline SVGs carry no responsive clamp -> mobile fit main.css-dependent (L47 finding), PNG `<img>` carry inline max-width:100% (clamp without main.css). RULES (carry-forward): `\\wsl$\` paths only; Filesystem read tools + `write_file` reach `\\wsl$\` byte-exact (str_replace/edit + bash CANNOT reach UNC); Blender-bridge host also sees WSL FS via UNC (server-side transform/write, dry-run md5 match before commit, binary + normalize CRLF->LF + get_file_info==byte-count; do NOT call `read_factory_settings`); browser QA Claude-side (local mermaid ESM, CDN blocked). Warn Ray to start a fresh chat when the conversation gets long.

---

## L48 PHASE-0 ARCHIVE HEAD (superseded 2026-07-04 by L48 integration)

# Blender Course · **L48 (Product Visualization Project) PHASE 0 COMPLETE + HARD-VERIFIED (2026-07-04). Module 11, lesson 2. Live HTML modernized, TRUE-PRISTINE (0 figures). NEXT = ROSTER TRIAGE → SVG/PNG (own fresh chat).**
Live HTML = `lesson_48_product_visualization_project.html` (project root `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`). Opened untouched original (257,073 B / md5 `7797e563ff56d0f05be86d37304bf534` / LF-only / 0 CR / TRUE-PRISTINE, 0 figures/img/svg). Applied Phase-0 transforms Claude-side + hard-asserted, reproduced identical transform SERVER-SIDE on WSL disk via Blender bridge (input re-verified pristine 7797e563 at commit → dry-run md5 byte-exact to Claude-side BEFORE commit → binary LF write → get_file_info==258,778 → fresh independent Filesystem re-read re-assert ALL 30 PASS). Live HTML now **258,778 B / md5 `eb6c10d3d45bee6bb5a843ed5c09b07a` / LF-only**. Byte delta 257,073→258,778 = **+1,705 B** (deterministic; Claude-side and server-side md5 identical). **CONTENT FIX (L43/L46-parallel):** TOC listed only **6** anchors but body has **16** top-level `<section>`; rebuilt TOC 6→16 to match body section order/ids [project-overview, product-selection, reference-gathering, modeling-strategy, scene-planning, project-setup, modeling-workflow, materials-overview, creating-materials, lighting-setup, camera-composition, final-render, variations, project-wrap, summary, whats-next]. TOC labels = emoji-stripped h3 titles; all 16 resolve, 0 dead, section open==close==16. POST-PHASE-0 INVARIANTS (30/30 PASS on fresh disk re-read): bytes 258,778; md5 eb6c10d3; CR 0; U+2014 literal 0 → &#8212; 82; U+2192 166 HOLD; U+2013/U+00B7 0; #667eea 0 / #764ba2 0 / purple-gradient 0; total linear-gradient 1 (green #4CAF50 KEPT); #2a2a2a cards 6; mermaid fill:#3a3a3a 8 / fill:#667eea 0; primaryBorderColor 888888 1 / 667eea 0; color="#222" 0; figure/img/svg 0/0/0; `<table>` 7 / table-wrap 7 (all 7 carry inline overflow-x:auto); TOC 16/16 (order==body); dead anchor 0; jsdelivr 1; back lesson_47 1 / next lesson_49 1. Phase-0 transforms: 6 gradient cards `linear-gradient(135deg,#667eea,#764ba2);color:white` → `#2a2a2a;color:white`; 8× mermaid node `fill:#667eea` → `fill:#3a3a3a`; 1× mermaid `primaryBorderColor '#667eea'` → `'#888888'`; wrapped **6 bare `<table>`** in `.table-wrap` + injected inline `style="overflow-x:auto;"` on all 7 `.table-wrap`; 82 literal U+2014 → `&#8212;`. **CARRY-FORWARD:** overflow-x:auto on all 7 `.table-wrap` (restores L43/L44/L46/L47 pattern; mobile-table clamp without external main.css). Full baseline + log in `lesson_48_integration_progress.md` (§Phase-0 baseline, §Content check, §Phase-0 transforms, §Carry-forward). **NEXT PHASE = ROSTER TRIAGE → SVG/PNG (own fresh chat):** product-visualization *project* lesson — inventory figures the content needs against the 16 sections (planning charts, reference-analysis, modeling strategy, scene/camera composition, studio lighting diagrams likely SVG; final-render / before-after possible PNG candidates), split SVG vs PNG, record split in progress doc, then produce SVG batches. HOLD &#8212; 82 / U+2192 166 through all later phases; new prose uses entities never literals. RULES (carry-forward): `\\wsl$\` paths only; Filesystem read tools + `write_file` reach `\\wsl$\` byte-exact (str_replace/edit + bash CANNOT reach UNC); Blender-bridge host also sees the WSL FS directly via UNC and can transform/write server-side (dry-run md5 matched Claude-side before commit; bridge writes binary + normalize CRLF→LF + get_file_info==byte-count); bridge string-arg payload caps ~6 KB so large content transformed server-side from disk, NOT streamed as literals; Module SVG plan = role-hex palette (#FF6B00 / #4ADE80 / #E63946 / #4A9EFF / #B47EE8 / #F5C242) + banned-literal list (U+2014, U+2013, U+00B7 literal, #667eea, #764ba2, purple linear-gradient); SVG text via write_file (base64 BANNED — silent line-collapse; hex reserved for binary); cairosvg dark+light verify before each SVG write; browser QA Claude-side (local mermaid@10.9.6 ESM, CDN blocked). Warn Ray to start a fresh chat when the conversation gets long.

---

## L47 QA-PASS ARCHIVE HEAD (superseded 2026-07-04 by L48 Phase 0)

# Blender Course · **L47 (Post-Processing) FULLY CLOSED — BROWSER QA PASS (2026-07-04). Module 11, lesson 1. 16/16 figures live (16 inline SVG, 0 PNG). NEXT ACTIVE = L48 (own fresh chat, Phase 0).**
BROWSER QA PASSED, no live edit needed. LIVE HTML unchanged **344,941 B / md5 `e7ed707d8b903820b75cfb6471ea0902` / LF-only**. QA harness = real Chromium (Playwright chromium-1194 at `/opt/pw-browsers`, `--no-sandbox`) over local http; live file copied Claude-side byte-exact (md5 + 344,941 B + 0 CR match disk); all 23 post-integration invariants re-asserted on the fresh copy before serving (figure 16/16, figcaption 16/16, img 0, inline svg 16/16, aria-labelledby 16, role="img" 16, color=#222/#667eea/#764ba2/linear-gradient/U+2013/U+00B7/U+2014-literal all 0, U+2192 124, &#8212; 77, section id 10/10 order==TOC, back L46 1/next L48 1, mermaid 6, jsdelivr 1, table/table-wrap 7/7, CR 0, all 16 inline svg XML well-formed) — all pass. QA-only render copy rewrote the one jsdelivr mermaid import to LOCAL mermaid@10.9.6 ESM (`vendor/mermaid-dist/`, 156-file dist from npm, CDN blocked) — live import line NOT touched. Tested desktop 1440x900 + mobile 390x844. RESULTS (both widths): 16/16 `<figure>` + 16/16 `<figcaption>`; 16 in-figure inline `<svg>` render (non-zero bbox 760xNN, 0 zero-size); 0 `<img>` (no PNG phase); 6 `.mermaid` → 6 rendered `<svg>` (local ESM, all chunks 200, no parse errors); 0 pageerrors; desktop h-overflow 0 (scrollW 1440). Only console errors = 4 expected site-scaffold 404s (styles/main.css, js/clipboard.js, js/course-enhancements.js, favicon.png), resolve on Ray's real server. **MOBILE FINDING (sandbox-only, not a live defect) — L45-parallel:** at 390px raw h-overflow was 418 (scrollW 808) because L47's 16 inline SVGs carry fixed `width="760"` and the document's own `<style>` has NO `figure svg{max-width:100%}` clamp — mobile SVG clamping depends entirely on the site `styles/main.css` (404 in sandbox). Verified: injecting the real `figure svg{max-width:100%;height:auto}` rule drops mobile h-overflow to 0 with all 6 mermaid still rendering. On Ray's real server (main.css served) the overflow does not occur — flagged as a live main.css dependency (same class as L45's mobile-table finding), NOT a QA failure. NOTE for future lessons: unlike the tables (inline overflow-x:auto from Phase 0), the inline SVGs have no inline responsive clamp, so their mobile fit is main.css-dependent. **VERDICT: PASS.** Full QA record in `lesson_47_integration_progress.md` §BROWSER QA. **L47 pipeline COMPLETE end-to-end (Phase 0 → roster triage → SVG 16 → integration → browser QA PASS). NEXT = L48, own fresh chat, start at Phase 0.** RULES (carry-forward): `\wsl$\` paths only; Filesystem read tools + `write_file` reach `\wsl$\` byte-exact (str_replace/edit + bash CANNOT reach UNC); bridge writes binary + normalize CRLF→LF + get_file_info==byte-count; browser QA runs Claude-side (copy live file, serve local http, local mermaid ESM since CDN blocked, external site css/js 404s expected sandbox-only). Warn Ray to start a fresh chat when the conversation gets long.

---

## L47 INTEGRATION ARCHIVE HEAD (superseded 2026-07-04 by L47 QA PASS)

# Blender Course · **L47 (Post-Processing) INTEGRATION COMPLETE + HARD-VERIFIED (2026-07-04). Module 11, lesson 1. 16/16 figures live (16 inline SVG, 0 PNG). NEXT = BROWSER QA (own fresh chat).**
Live HTML `lesson_47_post_processing.html` (project root `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`) now **344,941 B / md5 `e7ed707d8b903820b75cfb6471ea0902` / LF-only** (Phase-0 baseline 261,132 &#8594; 344,941 = +83,809 B). All 16 SVGs inlined at their ��Roster-triage `<h3>` section anchors, each root ` color="#222"` STRIPPED, each wrapped `<figure>`+`<figcaption>` (figcaption = SVG `<title>`). Write path = Phase-0 pattern: Claude-side transform hard-asserted, reproduced SERVER-SIDE via Blender bridge (input re-verified pristine bf66fb37 at commit &#8594; dry-run md5 == e7ed707d byte-exact BEFORE commit &#8594; binary LF write &#8594; `get_file_info`==344,941 &#8594; fresh independent Filesystem re-read re-assert ALL PASS). POST-INTEGRATION INVARIANTS (ALL PASS on fresh disk re-read): `<figure>` 16 / `<figcaption>` 16 / `<img>` 0 / inline `<svg>` 16 / `aria-labelledby` 16 / `role="img"` 16; NO overlay bars; color="#222" 0 / #667eea 0 / #764ba2 0 / linear-gradient 0 / U+2013 0 / U+00B7 0; **U+2014 literal 0** / **U+2192 literal 124 HELD**; &#8212; total 77 (57 body-prose HELD + 20 inside SVG figure content &#8212; L44/L45-parallel split; real invariant U+2014-literal-0 holds); section ids 10 (order == TOC) [intro-post-processing, compositor-basics, render-passes, color-correction, atmospheric-effects, common-fixes, external-software, final-output, project, summary]; back lesson_46 1 / next lesson_48 1; mermaid 6; jsdelivr 1; `<table>` 7 / table-wrap 7 (all carry inline overflow-x:auto); CR 0. All 16 inlined SVGs XML well-formed; all 16 figcaptions == their SVG `<title>`. **0 PNG** (roster verdict). Full integration record + anchor map in `lesson_47_integration_progress.md` (§Integration COMPLETE, §Status). **NEXT PHASE = BROWSER QA (own fresh chat):** copy live HTML Claude-side byte-exact (md5 + 344,941 B + 0 CR match disk), serve local http, local mermaid@10.9.6 ESM (CDN blocked), Chromium 1440x900 + 390x844; re-assert the post-integration invariants on the fresh copy before serving; no image staging (0 PNG); external site css/js 404s expected sandbox-only. RULES (carry-forward): `\\wsl$\` paths only; Filesystem read tools + `write_file` reach `\\wsl$\` byte-exact (str_replace/edit + bash CANNOT reach UNC); Blender-bridge host also sees the WSL FS directly via UNC (server-side transform/write, dry-run md5 match before commit, binary + normalize CRLF&#8594;LF + get_file_info==byte-count); browser QA Claude-side (local mermaid ESM, CDN blocked). Warn Ray to start a fresh chat when the conversation gets long.

---

## L47 PHASE-0 ARCHIVE HEAD (superseded 2026-07-04 by L47 integration)

# Blender Course · **L47 (Post-Processing) PHASE 0 COMPLETE + HARD-VERIFIED (2026-07-04). Module 11, lesson 1. Live HTML modernized, TRUE-PRISTINE (0 figures). NEXT = ROSTER TRIAGE → SVG (own fresh chat).**
Live HTML = `lesson_47_post_processing.html` (project root `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`). Opened untouched original (260,687 B / md5 `8eccd5e1ee7590454e4f9aacd2cf5154` / LF-only / 0 CR / TRUE-PRISTINE, 0 figures/img/svg). Applied Phase-0 transforms Claude-side + hard-asserted, reproduced identical transform SERVER-SIDE on WSL disk via Blender bridge (input re-verified pristine at commit → dry-run md5 byte-exact to Claude-side BEFORE commit → binary LF write → get_file_info==261,132 → fresh independent Filesystem re-read re-assert ALL 31 PASS). Live HTML now **261,132 B / md5 `bf66fb37c647882d055fb36e0acb8338` / LF-only**. Byte delta 260,687→261,132 = **+445 B** (deterministic; Claude-side and server-side md5 identical). **CONTENT CHECK: NO fix needed** (like L44/L46, unlike L43): TOC lists 10 anchors, all 10 resolve to 10 top-level `<section>` (open==close==10), 0 dead links, 0 missing sections — content already complete + correctly structured. POST-PHASE-0 INVARIANTS (31/31 PASS on fresh disk re-read): bytes 261,132; md5 bf66fb37; CR 0; LF 4385; U+2014 literal 0 → &#8212; 57; U+2192 124 HOLD; #667eea 0 / #764ba2 0 / linear-gradient 0; gradient cards → #2a2a2a 4; mermaid fill:#3a3a3a 6; primaryBorderColor 667eea 0 / 888888 1; `<table>` 7 / table-wrap 7 (all 7 carry inline overflow-x:auto) / bare-table 0; color="#222" 0; figure/img/svg 0/0/0; jsdelivr 1; section ids 10 (order matches TOC); dead anchor 0; U+2013/U+00B7 0; back lesson_46 1 / next lesson_48 1. Phase-0 transforms: 4 gradient cards `linear-gradient(#667eea,#764ba2);color:white` → `#2a2a2a;color:white`; 6× mermaid node `fill:#667eea` → `fill:#3a3a3a`; 1× mermaid `primaryBorderColor '#667eea'` → `'#888888'`; wrapped **all 7 bare `<table>`** in `.table-wrap` + injected inline `style="overflow-x:auto;"` on all 7; 57 literal U+2014 → `&#8212;`. **CARRY-FORWARD:** overflow-x:auto on all 7 `.table-wrap` (restores L43/L44/L46 pattern; fixes the L45 mobile-table main.css dependency — L47 wraps clamp without external css). Full baseline + log in `lesson_47_integration_progress.md` (§Phase-0 baseline, §Content check, §Phase-0 log, §Carry-forward). **NEXT PHASE = ROSTER TRIAGE → SVG (own fresh chat):** inventory figures the content needs against the 10 sections (post-processing lesson — compositor node graphs, render-pass breakdowns, color-correction wheels, atmospheric/glare effects, before/after fixes, external-software comparison → likely SVG-dominant with some before/after PNG candidates), split SVG vs PNG, record split in progress doc, then produce SVG batches. HOLD &#8212; 57 / U+2192 124 through all later phases; new prose uses entities never literals. RULES (carry-forward): `\\wsl$\` paths only; Filesystem read tools + `write_file` reach `\\wsl$\` byte-exact (str_replace/edit + bash CANNOT reach UNC); Blender-bridge host also sees the WSL FS directly via UNC and can transform/write server-side (used for this Phase-0 write, avoids large transfer; dry-run md5 matched Claude-side before commit; bridge writes binary + normalize CRLF→LF + get_file_info==byte-count); bridge string-arg payload caps ~6 KB so large content must be transformed server-side from disk, NOT streamed as literals; Module SVG plan = role-hex palette (#FF6B00 / #4ADE80 / #E63946 / #4A9EFF / #B47EE8 / #F5C242) + banned-literal list (U+2014, U+2013, U+00B7 literal, #667eea, #764ba2, linear-gradient); SVG text via write_file (base64 BANNED — silent line-collapse; hex reserved for binary); cairosvg dark+light verify before each SVG write; browser QA Claude-side (local mermaid@10.9.6 ESM, CDN blocked). Warn Ray to start a fresh chat when the conversation gets long.

---

## L46 ARCHIVE HEAD (superseded 2026-07-04 by L47)

# Blender Course · **L46 (Rendering for Production) PHASE 0 COMPLETE + HARD-VERIFIED (2026-07-04). Module 10, lesson 6. Live HTML modernized. NEXT = ROSTER TRIAGE → SVG (own fresh chat).**
Live HTML = `lesson_46_rendering_for_production.html` (project root `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`). **RECOVERY NOTE:** a prior bad write had truncated the live file to 45 B; Ray restored the pristine original from backup this session (verified md5 `fc5a5098a929a0a31a4f5d53022bd310` / 368,720 B / LF-only / 0 CR before use). Phase-0 then re-applied deterministically. Applied Phase-0 transforms Claude-side + hard-asserted, reproduced identical transform SERVER-SIDE on WSL disk via Blender bridge (dry-run md5 byte-exact to Claude-side BEFORE commit → binary LF write → get_file_info==369,625 → fresh independent Filesystem re-read re-assert ALL 32 PASS). Live HTML now **369,625 B / md5 `4973ef40d54f73f7ec981a8e4873c150` / LF-only**. Byte delta 368,720→369,625 = **+905 B** (deterministic). **TARGET md5 RE-BASELINE (Ray sign-off):** Phase-0 was originally derived to target md5 `405b9a7160f1fc762e65d02956b76eb2`, which assumed a specific `final-project` TOC label not on hand this session. The 369,625-B size target pinned the label to 35 chars but not its wording; per Ray, target md5 re-baselined to current deterministic output `4973ef40…` with `final-project` label = **"Project: Production Render Pipeline"** (35 chars, concise "Project: …" convention). If the old `405b9a71` label wording later surfaces and differs, that one TOC field is the reconcile point. **CONTENT FIX:** TOC rebuilt 10 → 13 entries to match body section order/ids [production-mindset, output-formats, render-passes, file-organization, batch-rendering, network-rendering, quality-control, delivery-workflow, backup-recovery, render-farms, legal-copyright, final-project, summary] — fixes dead `#project-production-pipeline` anchor + 4 missing sections (backup-recovery, render-farms, legal-copyright, final-project). TOC labels = emoji-stripped h2 titles except summary ("Summary and Best Practices") + final-project ("Project: Production Render Pipeline"). Phase-0 transforms: 4 gradient cards `linear-gradient(#667eea,#764ba2);color:white` → `#2a2a2a;color:white`; 4× mermaid node `fill:#667eea` → `fill:#3a3a3a`; 1× mermaid `primaryBorderColor '#667eea'` → `'#888888'`; wrapped 4 bare `<table>` in `.table-wrap` + injected inline `style="overflow-x:auto;"` on **all 9** `.table-wrap` divs; 59 literal U+2014 → `&#8212;`. **U+2192 literal HELD at 131.** POST-PHASE-0 INVARIANTS (32/32 PASS on fresh disk re-read): bytes 369,625; md5 4973ef40; CR 0; LF 6302; U+2014 literal 0 → &#8212; 59; U+2192 131 HOLD; gradient-667eea 0 / #2a2a2a 4; mermaid-667eea 0 / fill:#3a3a3a 4; primaryBorderColor 667eea 0 / 888888 1; table-wrap 9 (all 9 carry inline overflow-x:auto) / `<table>` 9 / bare-wrap 0; TOC 13/13 (order==body order); dead anchor 0; section ids 13 (order matches); nonascii preserved 923; U+2013/U+00B7/linear-gradient 0. **CARRY-FORWARD:** overflow-x:auto now on all 9 `.table-wrap` (restores L43/L44 pattern; fixes the L45 mobile-table main.css dependency — L46 wraps clamp without external css). Full baseline + log in `lesson_46_integration_progress.md` (§Phase-0 baseline, §Phase-0 log, §Carry-forward). Staging temps deleted (`_l46_p00.hex`, `_l46_stage.hex`, `_l46_probe.txt` + interim b64s; no `_l46_*` remain). **NEXT PHASE = ROSTER TRIAGE → SVG (own fresh chat):** inventory figures the content needs against the 13 sections, split SVG vs PNG, record split in progress doc, then produce SVG batches. HOLD &#8212; 59 / U+2192 131 through all later phases; new prose uses entities never literals. RULES (carry-forward): `\\wsl$\` paths only; Filesystem read tools + `write_file` reach `\\wsl$\` byte-exact (str_replace/edit + bash CANNOT reach UNC); Blender-bridge host also sees the WSL FS directly via UNC and can transform/write server-side (used for this Phase-0 write, avoids large transfer; dry-run md5 matched Claude-side before commit; bridge writes binary + normalize CRLF→LF + get_file_info==byte-count); bridge string-arg payload caps ~6 KB so large content must be transformed server-side from disk, NOT streamed as literals; Module 10 SVG plan = role-hex palette (#FF6B00 / #4ADE80 / #E63946 / #4A9EFF / #B47EE8 / #F5C242) + banned-literal list (U+2014, U+2013, U+00B7 literal, #667eea, #764ba2, linear-gradient); SVG text via write_file (base64 BANNED — silent line-collapse; hex reserved for binary); cairosvg dark+light verify before each SVG write; browser QA Claude-side (local mermaid@10.9.6 ESM, CDN blocked). Warn Ray to start a fresh chat when the conversation gets long.

---

## L45 QA-PASS ARCHIVE HEAD (superseded 2026-07-04 by L46)

# Blender Course · **L45 (Optimization Techniques) FULLY CLOSED — BROWSER QA PASS (2026-07-04). Module 10, lesson 5. 16/16 figures live (16 inline SVG, 0 PNG). NEXT ACTIVE = L46 (own fresh chat, Phase 0).**
BROWSER QA PASSED, no live edit needed. LIVE HTML unchanged **339,586 B / md5 `7b8840ea2697676e7c35b1ab95bf2ce6` / LF-only**. QA harness = real Chromium (Playwright chromium-1194 at `/opt/pw-browsers`, `--no-sandbox`) over local http; live file copied Claude-side byte-exact (md5 + 339,586 B + 0 CR match disk); all 25 post-integration invariants re-asserted on the fresh copy before serving (figure 16/16, figcaption 16/16, img 0, inline svg 16/16, aria-labelledby 16, color=#222/#667eea/#764ba2/linear-gradient/U+2013/U+00B7/U+2014-literal all 0, &#8212; entity 53, U+2192 258, section id 10/10, back L44 1/next L46 1, mermaid 4, jsdelivr 1, table/table-wrap 6/6, `<pre>` 1) — all pass. QA-only render copy rewrote the one jsdelivr mermaid import to LOCAL mermaid@10.9.6 ESM (`vendor/mermaid-dist/`, 359-file dist from npm, CDN blocked) — live import line NOT touched. Tested desktop 1440x900 + mobile 390x844. RESULTS (both widths): 16/16 `<figure>` + 16/16 `<figcaption>`; 16 in-figure inline `<svg>` render (non-zero bbox, 0 zero-size); 0 `<img>` (no PNG phase); 4 `.mermaid` → 4 rendered `<svg>` (local ESM, no parse errors); 0 pageerrors; desktop h-overflow 0 (body 1424≤1440). Only console errors = 3 expected site-scaffold 404s (styles/main.css, js/course-enhancements.js, js/clipboard.js), resolve on Ray's real server. **MOBILE TABLE FINDING (sandbox-only, not a live defect):** at 390px one render-optimization table gave raw h-overflow 50 (scrollW 440) because — unlike L43/L44 — L45's 6 `.table-wrap` divs carry NO inline `overflow-x:auto`; mobile table clamping depends entirely on the site `styles/main.css` (404s in sandbox). Verified: injecting the real `.table-wrap{overflow-x:auto}` rule drops mobile h-overflow to 0; all 6 tables are `.table-wrap`-wrapped (0 bare). On Ray's real server (main.css served) the overflow does not occur — flagged as a live main.css dependency, not a QA failure. **VERDICT: PASS.** Full QA record in `lesson_45_integration_progress.md` §BROWSER QA. **L45 pipeline COMPLETE end-to-end (Phase 0 → roster triage → SVG 16 → integration → browser QA PASS). NEXT = L46, own fresh chat, start at Phase 0.** RULES (carry-forward): `\wsl$\` paths only; Filesystem read tools + `write_file` reach `\wsl$\` byte-exact (str_replace/edit + bash CANNOT); bridge writes binary + normalize CRLF→LF + get_file_info==byte-count; browser QA runs Claude-side (copy live file, serve local http, local mermaid ESM since CDN blocked, external site css/js 404s expected sandbox-only). Warn Ray to start a fresh chat when the conversation gets long.

## L45 ARCHIVE HEAD (superseded 2026-07-04)

# Blender Course · **L45 (Optimization Techniques) INTEGRATION COMPLETE (2026-07-04). Module 10, lesson 5. 16/16 figures live (16 inline SVG, 0 PNG). NEXT = BROWSER QA (own fresh chat).**
Live HTML `lesson_45_optimization_techniques.html` **339,586 B / md5 `7b8840ea2697676e7c35b1ab95bf2ce6` / LF-only** (baseline 277,920 &#8594; 339,586 = +61,666 B). All 16 SVGs inlined at their unique `<h3>` subsection anchors (bottom-up), each root ` color="#222"` STRIPPED, each wrapped `<figure>`+`<figcaption>` (figcaption = SVG `<title>`). Write path = Phase-0 pattern: Claude-side transform hard-asserted, reproduced SERVER-SIDE via Blender bridge (dry-run md5 byte-exact BEFORE commit &#8594; temp sidecar verify &#8594; binary LF write &#8594; `get_file_info`==339,586 &#8594; fresh Filesystem re-read re-assert ALL PASS). POST-INTEGRATION INVARIANTS (25/25 PASS on fresh disk re-read): figure 16 / figcaption 16 / img 0 / inline svg 16 / aria-labelledby 16; color="#222" 0 / #667eea 0 / #764ba2 0 / linear-gradient 0 / U+2013 0 / U+00B7 0; **U+2014 literal 0**; &#8212; entity 53 (body-prose 44 HELD + 9 inside SVG figure content #04/05/06/14/15/16 &#8212; L44-parallel split; real invariant U+2014-literal-0 holds); U+2192 258; section id 10/10; back lesson_44 1 / next lesson_46 1; mermaid 4; jsdelivr 1; table 6 / table-wrap 6; `<pre>` 1. **0 PNG** (roster verdict). NEXT = BROWSER QA (own fresh chat): copy live HTML Claude-side byte-exact, serve local http, local mermaid@10.9.6 ESM (CDN blocked), Chromium 1440x900 + 390x844; no image staging (0 PNG). Full record in `lesson_45_integration_progress.md` §Integration COMPLETE. RULES (carry-forward): `\wsl$\` paths only; Filesystem read tools + `write_file` byte-exact (str_replace/edit + bash CANNOT reach UNC); Blender bridge host sees WSL FS via same UNC (server-side transform/write, dry-run md5 match before commit, binary + normalize CRLF&#8594;LF + get_file_info==byte-count); browser QA Claude-side. Warn Ray to start a fresh chat when the conversation gets long.

---

## L44 ARCHIVE HEAD (superseded 2026-07-04)

# Blender Course · **L44 (Scene Organization) FULLY CLOSED — BROWSER QA PASS (2026-07-03). Module 10, lesson 4. 13/13 figures live (13 inline SVG, 0 PNG). NEXT ACTIVE = L45 (own fresh chat, Phase 0).**
BROWSER QA PASSED, no live edit needed. LIVE HTML unchanged **269,999 B / md5 `fa81f3e0e06c50bc6cbed5b922bc6df8` / LF-only**. QA harness = real Chromium (141.0.7390.37, Playwright chromium-1194 at `/opt/pw-browsers`, `--no-sandbox`) over local http; live file copied Claude-side byte-exact (md5 + 269,999 B + 0 CR match disk); all post-integration invariants re-asserted on the fresh copy before serving (figure 13/13, figcaption 13/13, img 0, inline svg 13/13, aria-labelledby 13, overlay-bar 0, U+2014 literal 0, &#8212; body-prose 53, U+2192 128, U+2013·U+00B7·color=#222·#667eea·#764ba2·linear-gradient all 0, section id 10/10, back L43 1/next L45 1, class="mermaid" 2, jsdelivr 1, table/table-wrap 1/1) — all pass. QA-only render copy rewrote the one jsdelivr mermaid import to LOCAL mermaid@10.9.6 ESM (`vendor/mermaid-dist/`, 156-file dist from npm, CDN blocked) — live import line NOT touched. Tested desktop 1440x900 + mobile 390x844. RESULTS (both widths, identical): 13/13 `<figure>` + 13/13 `<figcaption>`; 13 in-figure inline `<svg>` render (non-zero bbox, 0 zero-size); 0 `<img>` (no PNG phase); 2 `.mermaid` → 2 rendered `<svg>` (local ESM, all 16 chunks 200, no parse errors); 0 pageerrors; h-overflow 0 both widths (desktop body 1424≤1440, mobile 374≤390). Only console errors = 3 expected site-scaffold 404s (styles/main.css, js/clipboard.js, js/course-enhancements.js), resolve on Ray's real server. No wide-table stub needed (0 img; the 1 table is `.table-wrap`-wrapped w/ inline overflow-x:auto from Phase 0). **VERDICT: PASS.** Full QA record in `lesson_44_integration_progress.md` §BROWSER QA. **L44 pipeline COMPLETE end-to-end (Phase 0 → roster triage → SVG 13 → integration → browser QA PASS). NEXT = L45, own fresh chat, start at Phase 0.** RULES (carry-forward): `\\wsl$\` paths only; Filesystem read tools + `write_file` reach `\\wsl$\` byte-exact (str_replace/edit + bash CANNOT); bridge writes binary + normalize CRLF→LF + get_file_info==byte-count; browser QA runs Claude-side (copy live file + images over, serve local http, local mermaid ESM since CDN blocked, external site css/js 404s are expected sandbox-only). Warn Ray to start a fresh chat when the conversation gets long.

---

## L44 ARCHIVE HEADS (superseded 2026-07-03)

# Blender Course · **L44 (Scene Organization) INTEGRATION COMPLETE (2026-07-03). Module 10, lesson 4. 13/13 SVGs live. NEXT = BROWSER QA (own fresh chat).**
Live HTML `lesson_44_scene_organization.html` **269,999 B / md5 `fa81f3e0e06c50bc6cbed5b922bc6df8` / LF-only**. All post-integration invariants HARD-ASSERTED PASS (figure 13/13, figcaption 13/13, img 0, inline svg 13, aria-labelledby 13, overlay-bar 0, U+2014 literal 0, U+2192 128, U+2013·U+00B7·color=#222·#667eea·#764ba2·linear-gradient all 0, section id 10/10, back L43 1/next L45 1, mermaid 2, jsdelivr 1, table/table-wrap 1/1, &#8212; body-prose 53). No PNG phase. Browser QA = own fresh chat (copy live HTML + 13 SVGs Claude-side, serve local http, local mermaid@10.9.6 ESM, Chromium 1440x900 + 390x844).
Read `lesson_44_integration_progress.md` (§Current state, §SVG build log [all 13], §Roster triage registry for the 13-SVG id-prefix map + anchors + post-integration target invariants, §Locked rules). **13/13 inline SVG built + EYE-CONFIRMED + logged** (all `images/lesson_44_NN_slug.svg`, byte-exact LF, md5 round-trip + get_file_info verified): 01 dvo (5,285) / 02 chc (5,435) / 03 cvi (6,362) / 04 ccp (5,601) / 05 npb (6,012) / 06 prx (5,736) / 07 odm (4,614) / 08 oic (5,923) / 09 vlc (5,268) / 10 vls (6,870) / 11 lva (5,263) / 12 pfs (6,840) / 13 bkp (6,190). **0 PNG** (roster triage verdict — pure conceptual/UI/reference lesson). LIVE HTML UNTOUCHED **191,869 B / md5 1bf0ac9a0d4c4a5a9aee90b5de1377cf / LF**. **NEXT PHASE = INTEGRATION (own fresh chat):** insert the 13 inline SVGs into their §Roster-triage section anchors (bottom-up), STRIP each root `color="#222"`, inject font-family where missing, wrap each in `<figure>`+`<figcaption>`; then assert §Roster triage post-integration target invariants (figure 13 / figcaption 13 / img 0 / inline svg 13 / aria-labelledby 13; NO overlay bars; HOLD &#8212; 53 / U+2192 128 / U+2013·U+00B7·color=#222·#667eea·#764ba2·linear-gradient all 0; section id 10/10, back L43 1/next L45 1, mermaid 2, jsdelivr 1, table/table-wrap 1/1). RULES: `\\wsl$\` paths only; Filesystem read tools + `write_file` byte-exact (str_replace/edit + bash CANNOT reach UNC); Blender-bridge host also sees WSL FS directly (server-side transform/write, dry-run md5 match before commit); role-hex #FF6B00/#4ADE80/#E63946/#4A9EFF/#B47EE8/#F5C242; banned literals U+2014/U+2013/U+00B7-literal/#667eea/#764ba2/linear-gradient; browser QA Claude-side (local mermaid@10.9.6 ESM, CDN blocked). Warn Ray to start a fresh chat when the conversation gets long.

---

# Blender Course · **L44 (Scene Organization) SVG 8/13 DONE (2026-07-03). Module 10, lesson 4. Batch-2 {05,06,07} + batch-3 #08 oic all built + EYE-CONFIRMED + logged. 5 SVG remain: batch-3 {09 vlc, 10 vls}, batch-4 {11 lva, 12 pfs, 13 bkp}. NEXT = CONTINUE SVG BATCHES (own fresh chat).**
Read `lesson_44_integration_progress.md` (§Current state, §SVG build log, §Roster triage registry for the 13-SVG id-prefix map + anchors + post-integration target invariants, §SVG plan-lock, §Locked rules). Specs = 56-item WISHLIST in `lesson_44_image_requirements_part1/2.md` + `lesson_44_quick_reference.md` — build ONLY the 13 locked in §Roster triage. **8/13 inline SVG built + EYE-CONFIRMED + logged:** 01 dvo / 02 chc / 03 cvi / 04 ccp / 05 npb / 06 prx / 07 odm / 08 oic (all `images/lesson_44_NN_slug.svg`). **5 remain:** 09 vlc- view_layer_concept (view-layers "What Are View Layers?") / 10 vls- view_layer_setups (view-layers "Common View Layer Setups") / 11 lva- link_vs_append (project-structure "Link vs Append") / 12 pfs- project_folder_structure (project-structure "Standard Project Structure", numbered-folder infographic 01_Reference–08_Documentation — complement to the existing ASCII pre, NOT a dup) / 13 bkp- backup_321_strategy (project-structure "Backup Strategies", 3-2-1 rule). LIVE HTML UNTOUCHED **191,869 B / md5 1bf0ac9a0d4c4a5a9aee90b5de1377cf / LF**. Per-SVG loop: author Claude-side → banned-literal check → cairosvg dark(#1e1e1e/#d4d4d4)+light(#fff/#334155) verify (pip install cairosvg --break-system-packages if missing) → `write_file` to `images/lesson_44_NN_slug.svg` (byte-exact LF, TEXT not bridge-hex) → copy_file_user_to_claude back + md5 round-trip + get_file_info → append §SVG build log. Integration = NEXT phase (own fresh chat) once 13/13 built. RULES: role-hex #FF6B00/#4ADE80/#E63946/#4A9EFF/#B47EE8/#F5C242; banned literals U+2014/U+2013/U+00B7-literal/#667eea/#764ba2/linear-gradient; standalone svg root color="#222" KEEP now / STRIP at integration; every svg role="img"+aria-labelledby→title/desc; `\\wsl$\` paths only, `write_file` byte-exact (str_replace/edit + bash CANNOT reach UNC); view-tool empty-payload glitch recurs — if a light render won't display, stat-verify (cairosvg luminance spread) + rely on dark eye-confirm (as done for #05). Warn Ray to start a fresh chat when the conversation gets long.

---

# Blender Course · **L44 (Scene Organization) ROSTER TRIAGE COMPLETE + PILOT SVG #01 (2026-07-03). Module 10, lesson 4. Split LOCKED = 13 inline SVG / 0 PNG. NEXT = finish SVG batches (pilot #01 dvo- built; 10 remain, batches of 3).**
Roster triage done + locked into `lesson_44_integration_progress.md` §Roster triage (13-SVG id-prefix registry + section anchors + wishlist→build map + 43-item DEFER rationale + post-integration target invariants + SVG plan-lock). **Verdict 13 SVG / 0 PNG** — L44 is pure conceptual/UI/reference (collections trees, naming grammar, Outliner anatomy, view-layer branching, asset/data relations, folder structure, workflow logic); NO rendered-beauty or before/after content anywhere → 0 PNG (goes past L43's 9+5). The requirements docs (`lesson_44_image_requirements_part1/2.md`, `lesson_44_quick_reference.md`) are a **56-item pre-production WISHLIST authored 2025-11-12, NOT the roster** — padded with redundant screenshot sequences + duplicates; triaged to the 13 that add non-redundant value vs the 10 live sections; existing scaffolding (2 mermaid + 4 ASCII trees + 1 shortcuts table) absorbs the rest. **13 SVG registry:** 01 dvo- disorganized_vs_organized (why-org) / 02 chc- collection_hierarchy_concept (collections) / 03 cvi- collection_visibility_icons (collections) / 04 ccp- common_collection_patterns (collections) / 05 npb- naming_pattern_breakdown (naming) / 06 prx- prefix_reference_chart (naming) / 07 odm- outliner_display_modes (outliner) / 08 oic- outliner_icon_controls (outliner) / 09 vlc- view_layer_concept (view-layers) / 10 vls- view_layer_setups (view-layers) / 11 lva- link_vs_append (asset-mgmt/project-struct) / 12 pfs- project_folder_structure (project-struct) / 13 bkp- backup_321_strategy (project-struct). Post-integration targets: figure 13 / figcaption 13 / img 0 / inline svg 13 / aria-labelledby 13; NO overlay bars; HOLD &#8212; 53 / U+2192 128 / U+2013·U+00B7·color=#222·#667eea·#764ba2·linear-gradient all 0; section id 10/10, back L43 1/next L45 1, mermaid 2, jsdelivr 1, table/table-wrap 1/1. **Pilot #01 dvo- built + cairosvg dark+light verified this chat; files land in `svg/` (per prior lessons).** LIVE HTML UNTOUCHED 191,869 B / md5 1bf0ac9a0d4c4a5a9aee90b5de1377cf. NEXT = author SVG #02-#13 in batches of 3 (build loop: author → banned-literal check → cairosvg dark(#1e1e1e/#d4d4d4)+light(#fff/#334155) → write_file → md5+get_file_info → log to progress doc §SVG build log). RULES: `\\wsl$\` paths only; Filesystem read tools + write_file byte-exact (str_replace/edit + bash CANNOT); role-hex palette #FF6B00/#4ADE80/#E63946/#4A9EFF/#B47EE8/#F5C242; banned literals U+2014/U+2013/U+00B7-literal/#667eea/#764ba2/linear-gradient; SVG text via write_file (bytes.fromhex reserved for binary/PNG, base64 BANNED); standalone svg root color="#222" KEEP now / STRIP at integration; browser QA Claude-side (local mermaid ESM, CDN blocked). Warn Ray to start a fresh chat when the conversation gets long.

---

# Blender Course · **L44 (Scene Organization) PHASE 0 COMPLETE + HARD-VERIFIED (2026-07-03). Module 10, lesson 4. Live HTML modernized, TRUE-PRISTINE (0 figures). NEXT = ROSTER TRIAGE → SVG (own fresh chat).**
Live HTML = `lesson_44_scene_organization.html` (project root `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`). Opened untouched original (192,075 B / md5 fd4e58a7840e969599ea9e4b1ea357cc / LF-only / TRUE-PRISTINE). Applied Phase-0 transforms Claude-side + hard-asserted, then reproduced the identical transform SERVER-SIDE on the WSL disk via the Blender bridge (dry-run md5 byte-exact to Claude-side BEFORE commit → binary LF write → get_file_info==191,869 → fresh independent Filesystem re-read re-assert ALL PASS). Live HTML now **191,869 B / md5 1bf0ac9a0d4c4a5a9aee90b5de1377cf / LF-only**. **CONTENT CHECK: NO fix needed** (unlike L43): TOC lists 10 anchors, all 10 resolve to 10 top-level `<section>` (open==close==10), 0 dead links, no nesting defect — content already complete + correctly structured. POST-PHASE-0 INVARIANTS (all verified on fresh disk re-read): bytes 191,869; CRLF 0; 10 section ids [why-organization, collections-system, naming-conventions, outliner-mastery, view-layers, asset-management, project-structure, workflow-optimization, project-reorganize, summary]; section open==close==10; TOC 10/10 resolve (0 dead); mermaid 2; jsdelivr 1; table 1 / table-wrap 1 (wrapped); #2a2a2a 12; fill:#3a3a3a 2; figures/img/svg 0/0/0 (TRUE-PRISTINE); back lesson_43 1 / next lesson_45 1; **U+2014 literal 0 → &#8212; 53** (all 53 prose em-dashes entity-normalized); U+2192 literal 128 HOLD; U+2013/U+00B7/#667eea/#764ba2/linear-gradient all 0. Phase-0 transforms: 12 gradient cards `linear-gradient(#667eea,#764ba2);color:white` → `#2a2a2a;color:white`; mermaid `primaryBorderColor '#667eea'` → `'#888888'`; 2× mermaid node `fill:#667eea` → `fill:#3a3a3a`; 1 bare `<table>` → `.table-wrap` wrapped; 53 literal U+2014 → `&#8212;`. Byte delta 192,075→191,869 = −206 B (deterministic; Claude-side and server-side md5 identical). Full baseline + log in `lesson_44_integration_progress.md` (§Phase-0 baseline, §Phase-0 log). **NEXT PHASE = ROSTER TRIAGE → SVG:** inventory figures the content needs against the 10 sections (lesson is heavily UI/organizational — collections tree, naming charts, Outliner anatomy, view-layer structure, asset-library flow, folder structure, workflow logic → likely SVG-dominant, few/no PNGs), split SVG vs PNG, record split in progress doc, then produce SVG batches. HOLD &#8212; 53 / U+2192 128 through all later phases; new prose uses entities `&#8212;`/`&#8594;`/`&#8211;` never literals. RULES: `\\wsl$\` paths only; Filesystem read tools + write_file reach `\\wsl$\` byte-exact (str_replace/edit + bash CANNOT); Blender-bridge host also sees the WSL FS directly and can transform/write server-side (used for this Phase-0 write, avoids 192 KB transfer; dry-run md5 matched Claude-side before commit); bridge writes binary + normalize CRLF→LF + get_file_info==byte-count; namespaces don't persist (inline helpers per call); Blender bridge lowercase; no Filesystem delete → os.remove temps; Module 10 SVG plan = role-hex palette (#FF6B00 brand / #4ADE80 keep-positive / #E63946 warning / #4A9EFF field-info / #B47EE8 practice / #F5C242 caution) + banned-literal list (U+2014, U+2013, U+00B7 literal, #667eea, #764ba2, linear-gradient) + transfer via bytes.fromhex() (base64 BANNED — silent line-collapse); browser QA Claude-side (local mermaid ESM, CDN blocked). Warn Ray to start a fresh chat when the conversation gets long.
