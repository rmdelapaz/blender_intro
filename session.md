# Blender Course &middot; **L21 (Composition Principles) &#8212; COMPLETE (2026-06-21): 26/26 figures placed, committed &amp; QA-PASS (11 inline SVG + 15 PNG w/ c{N} overlays). Browser QA cleared narrow + wide desktop; #29/#30 visually verified. NO ACTIVE LESSON &#8212; next lesson per Ray.**

**Active lesson: NONE** (L21 closed). **Next lesson = Ray's pick.** When a new lesson opens, create its `lesson_NN_integration_progress.md` and resume the standard read order (`session.md` &#8594; that doc &#167;6 to END &#8594; `images/IMAGE_STYLE_GUIDE.md` v6).

**L21 INVARIANT BASELINE (live HTML, UTF-8-aware Python count, NOT shell grep &#8212; locale unset so grep miscounts):** HTML **242,879 B**; figure 0 / figcaption 0 / img 0 / svg 0; **mermaid 2** (TWO blocks &#8212; both preserve, never touch); **em-dash U+2014 71** (all literal chars &#8212; preserve verbatim, ADD NONE, use `&#183;`); color222 0. Section IDs (doc order): composition-fundamentals &#183; rule-of-thirds &#183; golden-ratio &#183; visual-weight &#183; leading-lines &#183; framing &#183; symmetry &#183; depth-layers &#183; color-composition &#183; breaking-rules &#183; project &#183; summary.

**ROSTER LOCKED: High+Medium 26** (11 SVG + 15 PNG). Q1&#8211;Q4 resolved. Deferred (4 Lower): #23, #24, #25, #28.

**L21 INTEGRATION DONE (P1&#8211;P12 complete &amp; committed):** all 26 figures placed in `lesson_21_composition_principles.html`. Final live-HTML recount (UTF-8-aware Python, NOT shell grep): bytes **346,385**; figure **26** / figcaption **26** / img **15** / svg **26** (11 inline + 15 overlay) / **mermaid 2** (preserved) / **em-dash U+2014 71** (preserved) / color222 0 &#183; ALL INVARIANT TARGETS MET. Integration is HTML-only; Blender untouched (true-pristine). **Remaining = browser QA only.**

**ALL 26 FIGURES PRODUCED.** Files in `images/lesson_21_NN_slug.{svg,png}`.
- **11 SVGs** (cairosvg-checked light+dark, 0 em-dash, color222 kept for `<img>` fallback &#8212; STRIP on inline): #1 `vh`, #2 `rt`, #5 `pt`, #6 `vw` (680), #9 `ll` (680), #13 `ff`, #14 `sy`, #16 `dl`, #19 `ov`, #20 `cw` (680), #26 `tl`.
- **15 PNGs** (all QA-reviewed, Ray-approved, promoted; NO baked text &#8212; labels/curves land as `c{N}` overlays at integration): #3 thirds_good_vs_bad (1802x964, Option A: `c3` thirds-grid+center-cross overlay carries the read), #4 golden_spiral (1200x800, Option A hero-at-eye host; spiral curve = `c4` overlay), #7 symmetry_vs_asymmetry (1802x964), #8 balance_seesaw_principle (1200x800), #10 s_curve_example (1200x800; S-curve = `c10` overlay), #11 lines_entry_exit_flow (1802x964, 2-panel), #12 framing_examples (1802x1930, 2x2), #15 centered_symmetry_example (1200x900), #17 dof_layer_separation (1200x800), #18 atmospheric_perspective (1200x900), #21 complementary_contrast (1200x900), #22 warm_cool_temperature (1802x964, 2-panel), #27 focal_length_comparison (2704x964, 3-panel), #29 blender_composition_ui (1920x1046, full Blender UI camera-view w/ thirds+center+golden guides), #30 project_gallery_template (1804x1298, 3x2 six-principle gallery).

**`c{N}` overlay registry (authored at INTEGRATION, all assigned in progress-doc &#167;4):** c3 (thirds-grid GOOD + center-cross BAD), c4 (golden-spiral curve into eye), c7 (SYM/ASYM labels), c8 (weight/distance markers), c10 (S-curve line), c11 (GOOD/BAD + flow arrows), c12 (4 panel labels), c15 (center-axis), c17 (FG/MG/BG + "in focus"), c18 (near/far), c21 (complementary pair), c22 (WARM/COOL), c27 (24/50/135mm labels), c29 (callout at guide overlay), c30 (6 principle labels). Final solo-vs-overlay decided at integration (L20 precedent: some land bare).

**BROWSER QA: PASS (2026-06-21).** Verified all 26 figures render; the 15 PNG `c{N}` overlays align over their images at narrow AND wide desktop widths (0.01px worst-case at 1128px content); 11 inline SVGs theme correctly light+dark; both mermaid diagrams intact; no table/nav layout overflow. #29 (c29 ring on the dashed guide intersection just left of the sphere, badge legible/not clipped) and #30 (6 labels centered in dark bands) visually confirmed via live rendered screenshots in a fresh wide-layout session. **FINAL INVARIANTS (met):** figure 26 / figcaption 26 / img 15 / svg 26 (11 inline + 15 overlay) / **mermaid 2** / **em-dash U+2014 71** / color222 0; bytes 346,385.

**TOOLING NOTE:** if the integration chat has `Filesystem:edit_file`, use the standard dryRun&#8594;commit&#8594;`get_file_info` flow. If NOT (as in the PNG chat), use read-full &#8594; edit-in-context &#8594; `Filesystem:write_file` full overwrite &#8594; `get_file_info` + UTF-8-aware re-count, showing diff blocks before each overwrite.

**KEY PRODUCTION LESSONS (logged progress-doc &#167;5):** (1) good-vs-bad placement comparisons (#3) read weakly at composite scale even when pixel-correct &#8212; route teaching to the `c{N}` overlay. (2) path/curve figures (#4 spiral, #10 S-curve) can't be staged as scene geometry that reads as the curve &#8212; stage a host with elements along the implied path, draw the curve as overlay; exponential spirals defeat node sampling (clump at eye) &#8212; use hero-at-eye + outer-arc host. (3) DoF (#17) needs high-frequency-detail FG/BG objects (cube clusters, NOT smooth spheres) for blur to read &#8212; took 4 iterations. (4) 2x2/3x2 grids (#12/#30): each cell gets its own top label band so `c{N}` labels every cell.

**Blender: TRUE-PRISTINE** (restored + verified `get_scene_info` at PNG-chat close: Camera+Cube+Light, 0 mats / 0 node_groups, EEVEE/AgX/None/exp0, res 1920/1080/100, world grey 0.88 str 1.0, Light POINT 1000; composition guides OFF, DoF OFF, viewport out of camera view). Integration is HTML-only &#8212; no bridge needed; if any bridge work is added, confirm `get_scene_info` before/after and restore pristine at close.

**RULES:** `\\wsl$\` paths only (never `\\wsl.localhost\`; Chrome file: URLs display-only &#8212; view QA via `http://localhost:5504/lesson_21_composition_principles.html`); 0 new em-dashes (`&#183;`); `Filesystem:write_file` for new files/full overwrites (never `create_file`); `edit_file` dryRun&#8594;commit&#8594;`get_file_info` when available; `blender:execute_blender_code` lowercase only; cairosvg pixel-verify Claude-side before EVERY PNG-overlay commit (LD19); no Filesystem delete tool &#8212; delete temps via Blender bridge `os.remove`.

**Standing browser-QA queue EMPTY (L05&#8211;L21 all cleared).** L21 fully COMPLETE (26/26 + QA PASS). L20 fully COMPLETE (17/17 + QA PASS). L19 done (35/35 + QA PASS).

**Project root:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`
