# Session Resume Pointer

Single-block snapshot per `INTERRUPTION_RECOVERY.md` pillar 2.2. Overwrite-every-update semantics. First file to read in every new chat, before `status.md` and before any per-lesson progress doc.

---

## Chat / Date / Status

**Lesson 06 Phase 3 INTEGRATION sub-chat 2 CLOSED 2026-05-23 (resumed after interruption); Phase 3 INTEGRATION COMPLETE: 28 of 50 lesson images integrated overall; all 8 Phase 3 images integrated into `<section id="inset-outset">` (3 images at sub-chat 1) and `<section id="mesh-cleanup">` (5 images this sub-chat). Next chat: Phase 4 PRODUCTION plan-lock for `#extrude-operations` (6 images, no SVG; c45 and c49 overlays planned for INTEGRATION not PRODUCTION).** 2026-05-23. Sub-chat 2 execution was interrupted partway through close-out; resume diagnosis confirmed all 5 HTML inserts landed in the prior session against the locked Phase 3 INTEGRATION plan specs (c20 / c23 overlay pixel geometry exact, #21 SVG inlining checklist satisfied with all 10 `dd-*` ids preserved and root `color="#222"` stripped, em-dash-free figcaptions with middle-dot separators, pre-existing right-arrow `→` in #20 anchor preserved verbatim) and only close-out doc work was outstanding. Resume close-out executed cleanly: 3-edit `Filesystem:edit_file` commit on `lesson_06_integration_progress.md` (Status bullet append + 5-row placement-table flip block + bottom-of-doc Next chat rewrite, dry-run-then-commit clean) plus this `session.md` rewrite via `Filesystem:write_file`. Per-figure post-resume block measurements: #20 at 2,196 bytes (c20 full callout, positive-green `BY DISTANCE`), #21 at 10,773 bytes (inline SVG with `dd` prefix, heaviest insert), #22 at 1,043 bytes (plain figure), #50 at 1,073 bytes (plain figure), #23 at 2,248 bytes (c23 full callout, warning-red `FLIPPED`; first warning-red PNG overlay in Lesson 06); total 17,333 bytes of figure-block content matching the HTML net delta of 185,725 → 203,063 bytes (+17,338 net within 5 bytes of indentation noise). Progress doc grew 226,760 → 230,526 bytes (+3,766 net). No MCP server hangs encountered during resume close-out.

**Locked work products carry forward into Phase 4 PRODUCTION plan-lock (all in `lesson_06_integration_progress.md` and Process expectations section below):**

- **Phase 4 scope:** `#extrude-operations` section. 6 images: #24 extrude operation sequence (plain) · #45 transform constraint indicators (`c45` overlay planned for INTEGRATION) · #25 extrude normals vs regular (plain) · #26 extrude individual faces (plain) · #27 extrude scale technique (plain) · #49 extrude menu Alt+E (`c49` overlay planned for INTEGRATION). No SVGs in Phase 4 per the locked Option L phase plan.
- **Fresh scene reset required at Phase 4 PRODUCTION execution start:** delete L06_NormalsMonkey and any other leftovers from Phase 3 PRODUCTION sub-chats A / B / C. L06_Cube primitive likely reused across most Phase 4 captures since the Extrude family operations are well-demonstrated on a basic cube.
- **Plan-lock scope is read-pass plus plan-write only:** no HTML edits, no image production this chat. Read this `session.md`, then `lesson_06_integration_progress.md` (Status section through 2026-05-23 sub-chat 2 CLOSED bullet; Phase 1 / Phase 2 / Phase 3 PRODUCTION plan sections as structural model; placement-table rows #20 / #21 / #22 / #23 / #50 now show `integrated`), then `lesson_06_image_requirements.md` entries #24 / #25 / #26 / #27 / #45 / #49 for placement intent and capture prompts, then the PART 06 slice of `lesson_06_edit_mode_essentials.html` (203,063 bytes after Phase 3 INTEGRATION close) covering `<section id="extrude-operations">` to confirm section structure, indentation depth (expected 0/4/8 carry-forward from PART 03 / 04 / 05), and identify 6 per-image insertion anchors via h3 / h4 / blockquote / mermaid / table landmark scan.
- **Plan-lock close criterion:** new `Phase 4 PRODUCTION plan` section written into the progress doc between the existing `Phase 3 INTEGRATION plan` section and the `Locked rules` section, covering per-image capture specs (scene setup, Edit Mode state, view config, overlay config, capture path, output dimensions, file size budget, post-capture Pillow processing if any), sub-chat split decision (single sub-chat or A/B split based on scene rebuild needs), ID prefix registry update registering `c45` and `c49` as planned PNG annotation overlay prefixes; Status bullet appended; bottom-of-doc Next chat repointed at Phase 4 PRODUCTION sub-chat A (or single sub-chat) execution; this `session.md` rewritten to point at Phase 4 PRODUCTION sub-chat A (or single sub-chat) execution.

**Retroactive archive sweep for Lessons 02-04 EXPLICITLY OUT OF SCOPE per Ray's direction.** Forward-only work from here.

**Retroactive Lesson 05 browser QA pass still pending** (long-deferred QA debt for all 30 Lesson 05 figures). Optional alternative to Phase 4 PRODUCTION plan-lock if Ray wants to clear backlog first.

**Lesson 06 mid-lesson browser QA pass also available** (verify all 28 integrated figures render correctly in browser, now including all 5 Phase 3 sub-chat 2 mesh-cleanup figures just integrated: #20 with c20 BY DISTANCE positive-green overlay, #21 dissolve vs delete inline SVG with `dd` prefix, #22 limited dissolve before-and-after, #23 with c23 FLIPPED warning-red overlay, #50 fill operations comparison; plus the 3 Phase 3 sub-chat 1 inset figures #17 / #18 / #19; plus all 20 Phase 1 + Phase 2 figures). Optional sanity check before starting Phase 4 PRODUCTION plan-lock.

---

## Current step (next chat)

**Recommended next step: Lesson 06 Phase 4 PRODUCTION plan-lock chat.** Read this `session.md`, then `lesson_06_integration_progress.md` (Status section now has the 2026-05-23 Phase 3 INTEGRATION sub-chat 2 CLOSED bullet through `session.md rewritten in same close-out turn via Filesystem:write_file to point at Phase 4 PRODUCTION plan-lock`; **Phase 3 INTEGRATION COMPLETE** with 8 of 8 Phase 3 images integrated and 28 of 50 lesson images integrated overall; placement-table rows #20 / #21 / #22 / #23 / #50 now show `integrated`; bottom-of-doc Next chat section points at Phase 4 PRODUCTION plan-lock), then `lesson_06_image_requirements.md` entries #24 / #25 / #26 / #27 / #45 / #49 for Phase 4 placement intent and capture prompts.

Then read the PART 06 slice of `lesson_06_edit_mode_essentials.html` covering `<section id="extrude-operations">` to confirm section structure, verify indentation depth (expected 0/4/8 carry-forward from PART 03 / 04 / 05), and identify 6 per-image insertion anchors via h3 / h4 / blockquote / mermaid / table landmark scan. Lock per-image capture specs and sub-chat split decision in a new `Phase 4 PRODUCTION plan` section inserted into the progress doc between the existing `Phase 3 INTEGRATION plan` section and the `Locked rules` section, following the structural model of the Phase 1 / Phase 2 / Phase 3 PRODUCTION plan sections.

Plan-lock close criterion: new plan section written; Status bullet appended for plan-lock close; bottom-of-doc Next chat repointed at Phase 4 PRODUCTION sub-chat A (or single sub-chat) execution; this `session.md` rewritten to point at Phase 4 PRODUCTION sub-chat A (or single sub-chat) execution. Plan-lock chat scope: no HTML edits, no image production.

**Alternative next steps if Ray wants something other than Phase 4 PRODUCTION plan-lock:**

- **Lesson 06 mid-lesson browser QA pass** (verify all 28 integrated figures render correctly in browser, now including the 5 sub-chat 2 mesh-cleanup figures just integrated). Optional sanity check before starting Phase 4 PRODUCTION plan-lock.
- **Retroactive Lesson 05 browser QA pass** (closes long-deferred QA debt for all 30 Lesson 05 figures).
- **Non-Blender-course work.** Phase 3 INTEGRATION is cleanly closed with all 8 Phase 3 images integrated; Phase 4 PRODUCTION plan-lock can pick up any future chat without losing context.

---

## Process expectations (carry forward; all locked rules; see `images/IMAGE_STYLE_GUIDE.md` v4 and `lesson_06_integration_progress.md` for full discussion)

**Path discipline:**
- `\\wsl$\Ubuntu\` paths only, never `\\wsl.localhost\`

**File operations:**
- `Filesystem:edit_file` with `dryRun=true` first, then commit with `dryRun=false`, then verify with `Filesystem:get_file_info` after every commit
- `Filesystem:write_file` for new files and small full-doc overwrites (e.g., this `session.md`); NEVER Claude's built-in `create_file`
- Tools are deferred; call `tool_search` before invoking; do not inherit "tool X is unavailable" assumptions from a compaction summary
- `Filesystem:copy_file_user_to_claude` for one-way copy from user filesystem to Claude's container; useful for bash-grep section mapping on large docs before edit work, and for PNG pixel inspection ahead of overlay coordinate resolution
- `Filesystem:edit_file` matches `oldText` exactly including newlines but may normalize trailing whitespace at end of file; if a multi-paragraph oldText that includes a trailing blank line fails, retry with the trailing blank line excluded from oldText

**MCP server hang recovery (locked from Phase 1 INTEGRATION sub-chat 1):**
- If `Filesystem:edit_file` (especially with a long `newText` containing inline SVG callout blocks) times out at the 4-minute timeout boundary, the commit may or may not have landed; do not assume failure
- Recovery procedure: wait briefly for the MCP server to recover, then call `Filesystem:get_file_info` to check file size; if size matches pre-edit baseline the commit did not land and a clean retry is safe; if size matches post-edit expectation the commit landed during the timeout window and a retry would create a second insertion
- Phases 1-3 PRODUCTION and INTEGRATION all completed without MCP hangs (excluding the c2 multi-callout commit in Phase 1 INTEGRATION sub-chat 1 which hit this exact path); the recovery path remains armed for any future heavy-content commit

**Em-dash discipline (style guide v4, locked decision 9):**
- No new em-dashes anywhere in new content (figcaptions, alt text, status blocks, markdown notes, handoff docs, archive doc headers, pointer blocks, SVG label / heading / subhead / pill / footer / takeaway text, overlay badge text, overlay title / desc text, style-guide prose)
- Middle-dot (`·`, U+00B7) for dash-style separation in SVG labels and overlay badge text per the locked precedents
- Pre-existing em-dashes and en-dashes in source content preserved verbatim through edits (including inside `oldText` anchors); right-arrows `→` similarly preserved
- Quoting pre-existing em-dashes inside backtick code spans in a status bullet is OK
- Structural em-dash after a date in Status bullets (the date-then-separator-then-description pattern) is grandfathered as a list-item separator, not new prose

**Inline image pattern (locked from Phase 1 Lesson 01, confirmed across all subsequent phases including Lesson 06 Phase 1, Phase 2, and Phase 3 INTEGRATION sub-chats 1 and 2):**
- Plain `<figure>` / `<figcaption>` wrapper, no class
- PNGs: `<img loading="lazy">` with descriptive alt text. Figure at col 4, img and figcaption at col 8 (default depth) for figures at the 0/4/8-space indentation tier
- PNGs with overlays: `<figure>` wraps a `<div style="position: relative; display: inline-block; max-width: 100%;">` which holds the `<img loading="lazy" style="display: block; width: 100%; height: auto;">` plus the overlay `<svg>`; figcaption immediately after the closing `</div>`
- Inline SVGs: figure at col 4, svg root at col 8, svg children at col 12, deeper nesting at 16 / 20, figcaption at col 8; `color="#222"` stripped from inlined root `<svg>` (breaks dark mode); standalone `.svg` files in `images/` retain `color="#222"` for `<img>` fallback
- Multi-line attribute continuations on `<text>`, `<line>`, `<polygon>`, `<rect>`, `<polyline>`, `<desc>`, `<tspan>` collapsed onto single lines during inlining
- Decorative ASCII-divider author comments pruned during inlining; short structural comments kept
- Deeper-indent variant (figure col 8, contents at col 12/16/20, figcaption col 12, figure close col 8) for figures placed inside cards that open at col 4
- 2-to-3-line `oldText` anchors are the default for unique insertion points; extend to 4-8 lines when needed
- **Lesson 06 indentation discontinuity:** PART 2 sections (`#enter-edit-mode`, `#selection-modes`) at 12/16/20-space depth; PART 03 `#proportional-editing` at 0/4/8-space depth; PART 04 sections (`#loop-tools`, `#advanced-selection`, `#selection-tools`) at 0/4/8-space depth; PART 05 sections (`#inset-outset`, `#mesh-cleanup`) at 0/4/8-space depth; PART 06 `#extrude-operations` expected at 0/4/8-space depth (to be verified at Phase 4 PRODUCTION plan-lock)

**PNG annotation overlay pattern (style guide v4 component snippet, confirmed Phases 3-6 Lesson 05 INTEGRATION and Lesson 06 Phase 1, Phase 2, Phase 3 INTEGRATION sub-chats 1 and 2):**
- Wrapper div `position: relative; display: inline-block; max-width: 100%`
- Overlay SVG viewBox matches PNG pixel dimensions; style `position: absolute; inset: 0; width: 100%; height: 100%; pointer-events: none` plus `preserveAspectRatio="xMidYMid meet"`
- Img inline style `display: block; width: 100%; height: auto`
- No baked-in callouts on PNGs going forward
- Overlay ids namespaced per PNG with a `c{N}-*` prefix (Phase 4 will register `c45-*` and `c49-*` at plan-lock)
- Highlight shape (ellipse or circle) fill `none`, stroke matches colorway, width 4 (thin-strip variant: width 2)
- Leader line stroke matching colorway, stroke-width 3, stroke-linecap `round` (thin-strip variant: stroke-width 2)
- Marker circle r=6 at badge-side end of leader (7 px from badge edge per c28 convention) when a highlight is present (thin-strip variant: r=4, 3 px from badge edge)
- Badge rect rx=6, fill `#4ADE80` (positive) or `#E63946` (warning); text font-family `system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif`, font-size 22, font-weight 700, text-anchor middle, fill `#0f1a14` on green or `#ffffff` on red, letter-spacing 2 (thin-strip variant: rx=4, font-size 14, badge height 22, no letter-spacing)
- Composite-overlay variant for horizontally-divided composite PNGs with baked-in 2px `#FF6B00` vertical dividers: no highlight ellipses, no leader lines, just one centered badge per panel near the bottom edge
- **Decorative-shape-only variant** (Phase 2 c13 / c14 / c15): stroke `#ffffff` width 2 directly on the target region without highlight ellipse, leader, marker, or badge; suitable for tool-indicator overlays where the shape IS the message
- **c3 thin-strip variant** locked for sub-720px-wide image strips (~0.6x scale on all callout geometry)

**Overlay badge colorway split (style guide v4 locked decision 8):**
- Positive labels: `#4ADE80` green badge with `#0f1a14` dark text
- Warning labels: `#E63946` red badge with `#ffffff` white text
- Pick from the label's semantic role; mixed colorways on a single PNG are expected
- Lesson 06 Phase 3 introduced the first warning-red PNG overlay (c23 `FLIPPED` badge integrated this sub-chat); future Phase 4 c45 (transform constraint indicators) likely positive-green since axis constraints are affirmative; c49 (Extrude menu Alt+E options) colorway TBD at Phase 4 PRODUCTION plan-lock per semantic role of the highlighted option(s)

**Overlay coordinate resolution (locked path; same path applies to any future PNG overlay coordinate work):**
- For PNG-overlay coordinate work: `Filesystem:copy_file_user_to_claude` the source PNG into Claude's container, then run Pillow pixel scans to detect button bounds, leader paths, and clearance from nearby UI elements
- Two-scan pattern: first scan locates target region by distinctive color (blue/orange/highlighted); second scan locates nearby elements that the leader must clear to verify leader-path clearance before locking pixel coords
- For decorative-shape-only overlays: orange-selected-vertex bbox scan (c13), centroid+max-radius scan (c14), or concave-hull / outline-trace scan with RDP simplification (c15) per the shape variant

**BlenderMCP capture (locked, confirmed Phases 2-6 Lesson 05 plus Lesson 06 Phase 1, Phase 2 PRODUCTION, Phase 3 PRODUCTION sub-chats A / B / C):**
- Lowercase `blender:execute_blender_code` (uppercase hangs)
- NO `bpy.ops.wm.read_factory_settings(use_empty=False)` calls anywhere (kills BlenderMCP addon mid-execution; recovery requires restarting Blender)
- Clean Blender state manually instead (`bpy.data.objects.remove(obj, do_unlink=True)`, etc.)
- Render engine fallback path: try `BLENDER_EEVEE_NEXT` first; on `TypeError` or enum error, fall back to `BLENDER_EEVEE` (Phase 3 PRODUCTION sub-chat A confirmed `BLENDER_EEVEE_NEXT` not present in this build)
- Pie menus and other transient UI Blender's render API cannot reach: manual Win+Shift+S capture into `images/` directly (Phase 3 sub-chat B #20 precedent; expect Phase 4 #49 Extrude menu Alt+E to use this path)
- **`bpy.ops.render.opengl(write_still=True, view_context=True)` does NOT include overlay layers** (Face Orientation, Wireframe, Statistics, etc.); use it for clean shaded-viewport renders WITHOUT overlay content
- **`bpy.ops.screen.screenshot_area` with `temp_override(window=window, screen=screen, area=area_3d, region=region_3d)` DOES include overlay layers** (sub-chat C confirmed for Face Orientation overlay); use for any overlay-dependent imagery; crop to visual area via Pillow at the appropriate offset to exclude VIEW_3D area chrome
- `bpy.ops.screen.screenshot` (no-suffix full-window variant) works cleanly with `temp_override` for capturing the entire Blender app window (Phase 1 sub-chat B' precedent for c2 1920x1046 chrome-included capture)
- Re-find the VIEW_3D area and region in each `blender:execute_blender_code` block since Python local variables do not persist
- UNC path `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\images\...` works cleanly from Blender Python on Windows
- **`bpy.ops.ed.undo()` inside `temp_override` blocks is a known connection-drop trigger** (Phase 3 sub-chat A discovery); prefer `or recreate` clauses over `ed.undo()` paths for state rewind in multi-panel sub-chats
- Default user-perspective view rotation: `Euler((radians(63.6), 0, radians(-46.7)), 'XYZ').to_quaternion()`. View distance scales with subject size
- Pillow 12.2.0 installed at `C:\Users\pract\AppData\Roaming\Blender Foundation\Blender\5.1\scripts\addons\modules\PIL`; survives Blender restarts

**Face Orientation overlay capture recipe (locked from Phase 3 sub-chat C; not applicable to Phase 4 since Extrude family does not involve Face Orientation overlay):**
- Theme alpha check FIRST: `bpy.context.preferences.themes[0].view_3d.face_front` and `face_back` default RGBA alpha may be 0.0 in Blender 5.1.1 which silently suppresses the overlay tint; always read `preferences.themes[0].view_3d.face_front[3]` and `face_back[3]` before any Face Orientation capture and bump to ≥0.4 if zero
- Capture via `bpy.ops.screen.screenshot_area` with `temp_override` (NOT `bpy.ops.render.opengl` which excludes overlay layers)
- Solid shading mode required: Face Orientation overlay only renders in `space.shading.type = 'SOLID'`
- For view-aligned face selection on Suzanne (e.g., right-cheek faces), add front-facing dot-product check (`face_normal · view_dir < -0.15`) to the BFS scan filter, not just a bbox check
- Chrome-aware Pillow crop: screenshot_area output includes VIEW_3D area chrome; crop to visual area (e.g., 1572x900 raw → 1280x720 cropped at offset CROP_X=72 / CROP_Y=88) before sanity-check pixel scans

**Blender 5.1.1 op-name and add-on discoveries (locked through Lesson 05 Phase 6 plus Lesson 06 Phase 1, Phase 2, and Phase 3 PRODUCTION):**
- `bpy.ops.mesh.inset_faces` does NOT exist; use `bpy.ops.mesh.inset` instead
- `bpy.ops.mesh.delete(type='FACES')` (plural) does NOT exist; valid enum values are `('VERT', 'EDGE', 'FACE', 'EDGE_FACE', 'ONLY_FACE')`; use `type='FACE'` (singular)
- `bpy.ops.mesh.select_non_manifold()` works in 5.1.1 without bmesh fallback
- Prefer `bmesh.ops.bisect_plane` over `bpy.ops.mesh.bisect` for unambiguous local-mesh-space `plane_co` semantics
- `bmesh.ops.subdivide_edges` with cuts=N produces ONLY equal-spaced cuts; for unequal-spaced cuts, use `bmesh.ops.bisect_plane` per cut plane
- `bpy.ops.mesh.select_nth(offset=0, skip=1)` (Checker Deselect) requires loop-selection-history metadata that `bm.edges[i].select = True` does NOT produce; manual alternating-ring bmesh selection is the working scripted equivalent
- `bpy.ops.object.join` bakes source-object transforms into the joined verts' local coords; use `obj.matrix_world @ v.co` world-coord filtering for vertex selection after a join
- **`bpy.ops.mesh.beautify_fill()` is a no-op on 2-triangle topology** (Phase 3 sub-chat A #50 discovery); deviate to `bpy.ops.mesh.poke()` for visually-distinct triangulation outcome
- **Loop Tools is on the Extensions Platform in Blender 4.2+/5.x**, NOT in the bundled Add-ons tab. Scripted install path: `bpy.ops.extensions.repo_sync_all()` then `bpy.ops.extensions.package_install(repo_index=0, pkg_id='looptools', enable_on_install=True)`
- Phase 4 PRODUCTION may surface additional Extrude family op-name quirks (`bpy.ops.mesh.extrude_region_move`, `bpy.ops.mesh.extrude_faces_indiv`, `bpy.ops.mesh.extrude_repeat`, etc.); log any deviations at Phase 4 PRODUCTION sub-chat close

**Brand palette (style guide v4 current):**
- Blender brand / SVG accent rail: `#FF6B00`
- Course / structure / Z-axis blue: `#4A9EFF` (darker variant `#3a7fd1`)
- Practice / process: `#B47EE8`
- Reference / inspire: `#FF7EA8`
- Resources / Y-axis green / Normals / positive overlay badges: `#4ADE80`
- Portfolio / outcome / Caution / warning glyph: `#F5C242`
- X-axis red / Warning / negative glyph / warning overlay badges: `#E63946`
- Badge text dark on green: `#0f1a14`; badge text light on red: `#ffffff`
- Decorative-shape-only overlay stroke: `#ffffff` width 2 (c13 / c14 / c15)

**SVG house style (style guide v4):**
- Default viewBox `0 0 800 560`; dense-content variant `0 0 800 680`
- Top 3px `#FF6B00` accent rail at (0, 0)
- System-ui font stack
- `color="#222"` retained on standalone files for `<img>` fallback; strip on inline
- Heading at y=56 size 24 weight 700; subtitle at y=84 size 14 opacity 0.65; divider at y=108
- First body subhead at y=138 size 18 weight 600 (or row content from y=138 on cheat-sheet layouts per #44 5-row precedent; or paired-panel content from y=138 per #21 precedent with two 350px-wide panels at x=40..390 and x=410..760, 32px header bars, 3x3 face-grid mesh diagrams centered at y=307.5 within each panel body, captions at y=460)
- Footer tip card y=506-540 on 560 canvas (y=641-675 on 680 variant); Variant B (4px left rail) is matured default; Two-line wrapped footer prose via single `<text>` with inner `<tspan x="58" dy="15">` per #21 precedent
- Per-file id prefix prevents collisions across multiple inline SVGs on one HTML page (Phase 1 `pf` for #8; Phase 2 `sr` for #44; Phase 3 `dd` for #21; no SVG in Phase 4 per locked phase plan)

**Documentation rhythm:**
- Per-lesson `lesson_NN_integration_progress.md` is the running narrative log (live; lean after archive sweep at lesson close)
- Per-lesson `lesson_NN_integration_progress_archive.md` is the close-out archive of locked PRODUCTION specs and INTEGRATION plans (created at close-out chat)
- Per-lesson `lesson_NN_completion_handoff.md` is the close-out summary written at lesson close
- `status.md` is the project-wide snapshot, rolled at every lesson close
- `status_archive.md` is the project-wide archive
- This `session.md` is the resume pointer, overwritten every milestone

**Cadence:**
- One production chat plus one integration chat per phase by default
- Sub-chats expected for any phase of 6+ images
- Lesson 06 Phase 3 PRODUCTION at 8 images used 4 chats (1 plan-lock + sub-chat A + sub-chat B + sub-chat C recapture)
- Lesson 06 Phase 3 INTEGRATION at 8 images used 4 chats (1 plan-lock RESUMED + sub-chat 1 + sub-chat 2 [resumed close-out]; the sub-chat 2 execution was interrupted partway through but resumed cleanly with all 5 HTML inserts intact from the prior session, only the close-out doc updates remaining)
- Lesson 06 Phase 4 at 6 images projected at 2-3 chats (1 plan-lock + 1-2 production sub-chats + 1 integration plan-lock + 1-2 integration sub-chats; matches Lesson 05 Phase 6 precedent for 6-image phases)
- Warn Ray before context gets tight rather than letting automatic compaction happen
- Propose spec plus design plan before generating new images; lock plan in writing first, then build
- Propose plan plus anchor choices plus grouping decisions before integration; lock plan in writing first, then edit

**Placement-table state legend:**
- `not captured; not integrated` (or `not produced; not integrated`)
- `produced; not integrated` (or `captured; not integrated`)
- `produced; handoff_written; not integrated`
- `integrated`

**Style guide v4 addenda pending at Lesson 06 close:**
- Fold c3 thin-strip variant into style guide v4 as documented deviation for sub-720px-wide image strips
- Fold the overlay coordinate resolution workflow (Filesystem:copy_file_user_to_claude + Pillow two-scan pattern) into style guide v4 as the locked path for PNG-overlay pixel work
- Fold the decorative-shape-only overlay variant (c13 / c14 / c15) into style guide v4 as documented variant for tool-indicator captures
- Fold the paired-panel SVG layout variant (#21 precedent) into style guide v4 as documented variant for comparison-style SVGs
- Fold the two-line footer tip pattern into style guide v4 as documented variant for verbose footer tips
- Note the Loop Tools Extensions Platform install path in any future BlenderMCP capture recipe involving add-ons
- Note the `Filesystem:edit_file` trailing-blank-line normalization workaround as a documented edge case
- Note the `bpy.ops.ed.undo()` inside `temp_override` connection-drop pattern in the BlenderMCP capture recipe
- Fold the Face Orientation overlay capture recipe (Phase 3 sub-chat C) into style guide v4 BlenderMCP capture recipe section: theme `face_front` / `face_back` alpha non-zero requirement; `screen.screenshot_area` includes overlays vs `render.opengl` does not; Suzanne -Y orientation and view-aligned face selection with front-facing dot-product check; chrome-aware Pillow crop for screenshot_area output
- Note the resumed-execution close-out pattern (interrupted sub-chat where HTML inserts landed but doc updates pending; resume diagnosis via Filesystem:get_file_info on HTML and progress doc; post-resume block measurement instead of per-commit byte tracking) as a documented workflow precedent

---

## Specifics (per next-chat plan)

If next chat is **Lesson 06 Phase 4 PRODUCTION plan-lock** (recommended default):

- Project root: `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`
- Source docs to consult (in order): this `session.md`, then `lesson_06_integration_progress.md` (Status section through 2026-05-23 Phase 3 INTEGRATION sub-chat 2 CLOSED bullet; placement-table rows for all 28 integrated images now show `integrated`; rows for Phase 4 images #24 / #25 / #26 / #27 / #45 / #49 show `not captured; not integrated`; Phase 1 / Phase 2 / Phase 3 PRODUCTION plan sections carry historical specs as structural model for the new Phase 4 PRODUCTION plan section to be written), then `lesson_06_image_requirements.md` entries #24 / #25 / #26 / #27 / #45 / #49 for placement intent and capture prompts, then the PART 06 slice of `lesson_06_edit_mode_essentials.html` (203,063 bytes after Phase 3 INTEGRATION close) covering `<section id="extrude-operations">` to confirm section structure, indentation depth, and per-image insertion anchors via h3 / h4 / blockquote / mermaid / table landmark scan
- Phase 4 PRODUCTION plan-lock scope: read-pass plus plan-write; no HTML edits, no image production this chat. Lock per-image capture specs for all 6 images (scene setup, Edit Mode state, view configuration, overlay configuration, capture path, output dimensions, file size budget, post-capture Pillow processing if any), sub-chat split decision (single sub-chat or A/B split based on scene rebuild needs across the 6 captures), ID prefix registry update (register `c45` and `c49` as planned PNG annotation overlay prefixes; no inline SVG in Phase 4 per the locked phase plan so no new SVG id prefix needed), and Phase 4 PRODUCTION close criterion
- Fresh scene reset required at Phase 4 PRODUCTION execution start (delete L06_NormalsMonkey and any other leftovers from Phase 3 PRODUCTION sub-chats A / B / C); L06_Cube primitive likely reused across most Phase 4 captures since the Extrude family operations are well-demonstrated on a basic cube
- Plan-lock close criterion: new `Phase 4 PRODUCTION plan` section written into the progress doc between the existing `Phase 3 INTEGRATION plan` section and the `Locked rules` section; Status bullet appended for plan-lock close; bottom-of-doc Next chat repointed at Phase 4 PRODUCTION sub-chat A (or single sub-chat) execution; this `session.md` rewritten to point at Phase 4 PRODUCTION sub-chat A (or single sub-chat) execution. Doc-update commits can be combined into one `Filesystem:edit_file` dry-run-then-commit pair per the locked execution cadence
- Locked rules carry forward unchanged (see Process expectations section above)
- Budget note: plan-lock is a read-pass plus plan-write chat with no HTML edits or image production; main substantive work is the PART 06 slice read, per-image spec lockdown, and the plan section insert into the progress doc (~250-350 lines of new plan content following the Phase 3 PRODUCTION plan section's structural model). Comfortable single-chat scope per the Phase 1 / Phase 2 / Phase 3 PRODUCTION plan-lock precedents

If next chat is **Lesson 06 mid-lesson browser QA pass** (alternative; sanity check before starting Phase 4 PRODUCTION plan-lock):

- Open `lesson_06_edit_mode_essentials.html` in a browser, walk all 28 integrated figures, verify each renders correctly (c2 / c3 / c46 / c10 / c16 / c20 / c23 PNG overlays positioned correctly; c13 / c14 / c15 decorative-shape-only overlays trace their intended regions; #8 `pf`, #44 `sr`, and #21 `dd` inline SVGs render in both light and dark mode; no broken image refs; sub-chat 1 inset PNGs and sub-chat 2 mesh-cleanup PNGs render at full width with crisp dividers and overlays positioned correctly over PNG content)
- Flag any rendering issues for follow-up fixes; if no issues found, append a Status section bullet noting mid-lesson QA pass complete
- Phase 4 PRODUCTION plan-lock can pick up after the QA pass closes

If next chat is **Lesson 05 retroactive browser QA pass** (alternative; closes long-deferred QA debt):

- Open `lesson_05_organic_modeling_techniques.html` in a browser, walk all 30 figures across Phases 1-6 of Lesson 05, verify each renders correctly
- Flag any rendering issues for follow-up fixes; if no issues found, append a Status section bullet noting QA pass complete
- Phase 4 PRODUCTION plan-lock can pick up after the QA pass closes

If next chat is **non-Blender-course work** (alternative; this `session.md` cleanly closes Phase 3 INTEGRATION sub-chat 2 with the resumed close-out):

- Pick up wherever Ray directs. Phase 4 PRODUCTION plan-lock can start any future chat without losing context; Phase 3 INTEGRATION is fully closed with all 8 Phase 3 images integrated (28 of 50 lesson images integrated overall) and all locked specs for Phase 4 already documented in summary form in the Status block above
- If non-Blender-course work runs for multiple chats, leave this `session.md` alone (it will continue pointing at Lesson 06 Phase 4 PRODUCTION plan-lock whenever Ray returns to the Blender course)

---


---
