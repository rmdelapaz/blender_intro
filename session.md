# Session pointer (blender_course)

Resume-pointer file per `INTERRUPTION_RECOVERY.md` pillar 2.2. Single-block snapshot of the current project state. Overwrite at every Phase boundary and at every chat close. First file every new chat reads, before `status.md` and before any per-lesson progress doc.

**Chat / Date / Status:** Phase 5 INTEGRATION chat for Lesson 04 (resumed mid-chat at M5 after an interruption that landed M1 through M4 cleanly: the three Phase 5 figure inserts brought the HTML from 192.9 KB to 215.6 KB, and the Phase 5 integration Status bullet was already in place at the top of `lesson_04_integration_progress.md`; the resume took the HTML and the progress doc as ground truth, ran no further integration work, and committed the M5 `status.md` rollover and the M6 `session.md` overwrite to close the phase cleanly). 2026-05-19. CLOSED CLEAN.

**Last completed:** Phase 5 INTEGRATION for Lesson 04, closed 2026-05-19. Inlined all three Phase 5 assets into `lesson_04_basic_object_manipulation.html`. SVG #10 `lesson_04_10_duplication_comparison.svg` (id prefix `dc`) inserted in the `#duplication` section between the Linked Duplicate card's closing `</div>` and the When to Use Linked Duplication info card; SVG #11 `lesson_04_11_origin_point.svg` (id prefix `op`) inserted in the `#origin-transform` section between the Origin Point Functions card's closing `</div>` and the Why Origins Matter info card; PNG #12 `lesson_04_12_transform_properties.png` inserted in the `#origin-transform` section between the Transform Properties card's closing `</div>` and the Try It Now: Precise Positioning card, with an inline-stacked `c12-` namespaced annotation overlay carrying a single dashed orange `#FF6B00` rectangle at `x=915 y=80 w=290 h=460` `stroke-width=6` `stroke-dasharray="24 14"` tracing the N-panel Item tab Transform section, plus a filled orange tab-style pill at `x=915 y=24 w=480 h=50` with white 30px bold "N-Panel: Transform Section" text sitting above the rectangle's top edge (the minimal #9-style overlay pattern rather than the multi-callout #8-style, since the N-panel itself already labels each field clearly at lesson display width). All three inserts via `Filesystem:edit_file` with `dryRun=true` first then commit. For #10 and #11, the locked inline-SVG pattern: `color="#222"` attribute stripped from each inlined root `<svg>` tag (the standalone `.svg` files in `images/` retain that attribute for `<img>` fallback), content re-indented to 20-space content depth (figure and /figure at column 16, svg open/close plus figcaption at column 20, svg-attribute continuations at column 25, SVG body at column 22, `<g>` children at column 24, rect and path attribute continuations inside `<g>` at column 30), every `role`/`aria-labelledby`/`<title>`/`<desc>`/`id` preserved verbatim with `dc-` and `op-` prefixes carried through unchanged so no collision with the existing `ss/st/tg/am/gl/ra/so/c8/c9` id prefixes on the page. For #12, the locked Phase 4 inline-stacked overlay pattern: `<figure>` wraps a `position: relative; display: inline-block; max-width: 100%` div containing the `<img>` and an absolutely-positioned overlay `<svg>` with `pointer-events: none`, viewBox `0 0 1920 1046` matching the PNG pixel dimensions exactly, namespaced `c12-` id prefix for title and desc, img element carries inline `style="display: block; max-width: 100%; height: auto; margin: 0;"` to override the global `img { margin: var(--spacing-md) auto; }` rule so the wrapper div sizes cleanly to the image content. Overlay geometry previewed with PIL on the actual 1920x1046 PNG and on a downscaled 800px lesson-display-width version to confirm legibility before HTML commit per the Phase 4 cadence. Alt text on the #12 `<img>` describes the underlying screenshot content (Blender 5.1 viewport in Object Mode, default cube with transformed Location 2.5/-1/1.5 m, Rotation 30/0/45 degrees XYZ Euler, Scale 1.500/1.000/0.750, Dimensions 3/2/1.5 m, navigation gizmo and sidebar tabs visible); the overlay SVG `<title>` and `<desc>` cover the annotation specifically so assistive tech receives both. All three figcaptions em-dash-free, conversational, pointing the reader at what to notice. SVG #10: "Two side by side panels. On the left, Shift + D copies the cube wholesale: two separate meshes, edit one and the other stays untouched. On the right, Alt + D shares one mesh between both cubes; the small chain link between them is the visual cue that an Edit Mode change to either will ripple through to its partner." SVG #11: "A single cube with the small orange dot at its center marking the object origin. The three labeled cards around it spell out what that one point does: the pivot for R, the center for S, and the X, Y, Z values the N-panel reports for Location. Move the origin and every transform reroutes through the new point." PNG #12: "The N-panel Item tab with the default cube's Location set to (2.5, -1.0, 1.5), Rotation to thirty degrees on X and forty-five on Z, and Scale stretched to 1.5 on X and squashed to 0.75 on Z. Dimensions report the actual size the cube takes up in the scene after those scales apply, in Blender units." No new em-dashes anywhere in any new content. Source-prose em-dashes inside the M2 anchor for #11 (the Why Origins Matter paragraph reads "hinge is in the middle—it would swing all wrong! Origins are like hinges—they determine") preserved verbatim through the edit. HTML size deltas: M1 #10 added 11091 bytes (197567 to 208658); M2 #11 added 9596 bytes (208658 to 218254); M3 #12 with overlay added 2539 bytes (218254 to 220793). Total Phase 5 integration growth 23226 bytes (22.7 KB). Final HTML 220793 bytes (215.6 KB), up from 197567 bytes (192.9 KB) at Phase 5 integration chat start. `lesson_04_integration_progress.md` rolled forward in M4 to 60593 bytes (59.2 KB): new Phase 5 integration Status bullet inserted at top of the Status list, placement-table rows for #10, #11, #12 flipped from `produced; not integrated`, `produced; not integrated`, and `captured; not integrated` respectively to `integrated`, phase plan Phase 5 row flipped to "3 of 3 produced or captured, 3 of 3 integrated". `status.md` rolled forward in M5 to 31429 bytes: Site-Wide Feature row Lesson 04 entry from "12 of 14 produced, 9 of 14 integrated" to "12 of 14 produced, 12 of 14 integrated"; Image Integration Status row Lesson 04 Images Integrated cell from 9 of 14 to 12 of 14 and a Phase 5 integration paragraph appended to the notes cell; Lesson 04 file size in the Total Size header and Module 1 Lesson File Status row refreshed from 192.9 KB to 215.6 KB; Other Documentation entry for `lesson_04_integration_progress.md` refreshed (state description from "Phases 1, 2, 3, and 4 integrated plus Phase 5 produced/captured but not yet integrated; 12 of 14 images produced or captured, 9 of 14 integrated; HTML at 192.9 KB" to "Phases 1, 2, 3, 4, and 5 integrated; 12 of 14 images produced or captured, 12 of 14 integrated; HTML at 215.6 KB"). All three doc artifacts verified on disk via `Filesystem:get_file_info` after each commit. LOCKED RULE from 2026-05-19 carried through: no `bpy.ops.wm.read_factory_settings(use_empty=False)` calls anywhere; applies to all future PNG capture chats.

**Current step (next chat):** Lesson 04 Phase 6 PRODUCTION. Capture PNG #13 `lesson_04_13_completed_scene.png` and author SVG #14 `lesson_04_14_keyboard_shortcuts.svg`. PNG #13: finished table-scene Eevee Next render via the BlenderMCP bridge, scene built programmatically using the addon-safe pattern. Capture recipe: delete the default cube via `bpy.data.objects.remove(bpy.data.objects['Cube'], do_unlink=True)`; add a ground plane sized to the camera framing (6m square at z=0 is the planning-doc default); add a 2m square table top via cube primitive scaled to (1.0, 1.0, 0.05) raised to z=0.75; add four cylindrical legs at the table-top corners scaled to (0.05, 0.05, 0.35) at z=0.35; add the four target objects on the table top spaced to read clearly (UV sphere with radius around 0.2 at one corner, cone with radius around 0.2 at another, torus with major radius around 0.2 at the third, cylinder with radius around 0.15 at the fourth, each raised so its base sits on the table top); add a sun light or area light overhead; position the camera at perspective view three-quarter angle on the table; set Eevee Next as the render engine; render via `bpy.ops.render.render(write_still=True)` to `images/lesson_04_13_completed_scene.png`. Target file size 200 KB to 1 MB. NEVER call `bpy.ops.wm.read_factory_settings(use_empty=False)`; operate on the existing default scene. SVG #14: two-column cheat-sheet card adapting the Lesson 02 #18 (`lesson_02_18_keyboard_shortcuts_reference.svg`) plus Lesson 03 #18 (`lesson_03_18_navigation_shortcuts_reference.svg`) reference-card pattern verbatim, id prefix `ks`, four category groupings to mirror the existing tabular cheat sheet in the summary section: Selection (A, Alt + A, B, C, Ctrl + drag, Click, Shift + Click), Transformations (G, R, S plus G/R/S + X/Y/Z axis constraints plus G/R/S + X + X / Y + Y / Z + Z local constraints plus numeric typing for exact values), Duplication and Cursor (Shift + D, Alt + D, Shift + S, Shift + Right Click), Advanced (Ctrl + A apply scale, Alt + G/R/S reset transforms, N to toggle sidebar). 800x560 viewBox, 3px Blender-orange top strip at y=0, `currentColor` labels and dividers, key-combo badges in Blender-orange `#FF6B00` rounded rects with white bold text, footer tip at y=506 in `#FF6B00`, full accessibility tags (`role="img"` plus `aria-labelledby` pointing at matching `<title id="ks-title">` and `<desc id="ks-desc">`), `color="#222"` retained on the standalone root for `<img>` fallback. Both #13 and #14 verified on disk via `Filesystem:get_file_info` after write. After production: update `lesson_04_integration_progress.md` with new Phase 6 production Status bullet at top of the Status list, Available section advanced from 12 of 14 to 14 of 14 (full set on disk), Missing section reduced to 0 of 14, placement-table row #13 flipped from `not captured; not integrated` to `captured; not integrated`, placement-table row #14 stays at `produced; not integrated` (it was already at `produced; not integrated` from the planning chat's anticipatory population, but #14 is not actually on disk yet; that inconsistency resolves naturally when Phase 6 production lands the SVG), phase plan Phase 6 row flipped to `2 of 2 produced or captured, 0 of 2 integrated`. Update `status.md`: Site-Wide Feature row Lesson 04 entry to "14 of 14 produced, 12 of 14 integrated"; Image Integration Status row Lesson 04 Images Produced cell to 14 of 14, Images Integrated stays at 12 of 14, appended Phase 6 production paragraph in the notes cell. Lesson 04 HTML size in `status.md` unchanged at 215.6 KB (no HTML edits this pass). Other Documentation entry for `lesson_04_integration_progress.md` refreshed. Overwrite `session.md` to CLOSED CLEAN pointing at Phase 6 INTEGRATION.

**Process expectations:** All Lesson 04 locked rules carry forward. `\\wsl$\Ubuntu\` paths only, never `\\wsl.localhost\`. No new em-dashes anywhere in new content; pre-existing source em-dashes preserved verbatim. `Filesystem:edit_file` for existing files with `dryRun=true` first then commit; `Filesystem:write_file` for new files or full-file overwrites of small docs like this `session.md`; never the built-in `create_file`. **LOCKED RULE from 2026-05-19: NEVER call `bpy.ops.wm.read_factory_settings(use_empty=False)` from MCP-executed Blender code.** That call reloads Blender's startup file and unregisters all addons including BlenderMCP, killing the bridge socket mid-execution. Use only addon-safe operations: direct cursor and object manipulation, primitive add, transforms, view positioning via `temp_override`, render config, OpenGL render, screen.screenshot. If a true factory-clean baseline is required, Ray restarts Blender manually before the capture chat begins. This rule is documented in `lesson_04_image_prompts.md` Addon safety note and applies to all future PNG capture chats in this project. **LOCKED PATTERN from Phase 4 integration: inline-stacked SVG annotation overlays for PNG figures.** Structure: `<figure>` at column 16; inside that a `<div style="position: relative; display: inline-block; max-width: 100%;">` at column 20; inside the div an `<img ... style="display: block; max-width: 100%; height: auto; margin: 0;">` and an overlay `<svg style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none;" ...>` both at column 24; SVG body at column 26; nested defs and markers at column 28 and 30. Overlay SVG viewBox matches the PNG pixel dimensions exactly. Overlay SVG id prefix is namespaced (`c8`, `c9` used in Phase 4; `c12` used in Phase 5 #12). Marker definitions inside `<defs>` get the same prefix. Title and desc cover the annotations specifically; the `<img>`'s alt covers the underlying screenshot content. The img inline `style` overrides the global `img { margin: var(--spacing-md) auto; }` rule from `styles/main.css` so the wrapper div sizes cleanly to the image content; the wrapper div's `display: inline-block` combined with the figure's existing `text-align: center` keeps the figure centered horizontally. For SVG-only figures (no PNG underneath) the existing locked pattern still applies: plain `<figure>` with `<svg>` directly inside, 20-space content depth, strip `color="#222"` from the root, preserve all ids and aria-labelledby. Axis palette X=`#E63946`, Y=`#4ADE80`, Z=`#4A9EFF` (locked); `images/IMAGE_STYLE_GUIDE.md` palette revision to fold `#E63946` in is still deferred to a dedicated chat. `INTERRUPTION_RECOVERY.md` minimum viable subset (pillar 2.2 resume pointer, pillar 2.1 save-at-milestone-boundaries, pillar 2.4 intermediate states) remains in force. This `session.md` is the kickoff read in every new chat, before `status.md` and before any per-lesson progress doc. `tool_search` before invoking unfamiliar tools. Warn before context gets tight.

**Specifics:** Phase 6 covers the manipulation-project and summary sections, the final two sections of the lesson. PNG #13 placement (from the placement table): manipulation-project section, after the "Project Goal" info card's closing `</div>` (which lists the four scene elements), before the `<h3>Step 1: Setup and Cleanup</h3>`. The figure sits as the visual target reference before the step-by-step build begins. SVG #14 placement: summary section, "Essential Manipulation Shortcuts Reference" subsection, after the "Your Manipulation Cheat Sheet" card's closing `</div>` (which holds the four reference tables: Selection, Transformations, Duplication and Cursor, Advanced), before the `<h3>Common Questions at This Stage</h3>`. Mirrors the Lesson 02 #18 and Lesson 03 #18 placement pattern: tabular cheat sheet first, visual reference card after. PNG #13 capture recipe lives in `lesson_04_image_prompts.md` (addon-safe pattern, no `read_factory_settings` calls). Acceptance gate: the four target objects (sphere, cone, torus, cylinder) sit on the table top with the ground plane visible underneath, lit cleanly enough for the render to read as the visual target reference for the project section; cropping or recompositing is fine if the first render frames awkwardly. SVG #14 design (from the planning chat's notes plus the Lesson 02 #18 and Lesson 03 #18 reference-card pattern): 800x560 viewBox, two-column layout with four section headers stacked top-to-bottom (Selection, Transformations, Duplication and Cursor, Advanced), each section listing its shortcuts with key-combo badges in Blender-orange `#FF6B00` rounded rects (white bold 13pt text, rect dimensions roughly 80x22 matching the operation-badge pattern from #10 and #5) alongside short prose descriptions in `currentColor`. Footer tip at y=506 in `#FF6B00`. Note: placement table row for #14 currently reads `produced; not integrated` from the planning chat's anticipatory population, but #14 is not actually on disk yet; that inconsistency resolves naturally when Phase 6 production lands the SVG. Phase 6 INTEGRATION (a separate chat after production closes) inlines both: PNG #13 as a plain `<figure>`/`<img>`/`<figcaption>` insert (no overlay needed; the rendered scene reads as the project target on its own), SVG #14 as an inline SVG insert with `color="#222"` stripped and 20-space content depth per the locked pattern. After Phase 6 INTEGRATION, Lesson 04 image production and integration are complete (14 of 14 produced, 14 of 14 integrated); Lesson 04 close-out handoff written to `lesson_04_completion_handoff.md` mirroring the Lesson 03 close-out file at `lesson_03_completion_handoff.md`. Full per-phase plan with placement anchors, design notes, and id-prefix registry in `lesson_04_integration_progress.md`.


---
