# Blender Course &middot; Next chat: **RESUME Lesson 11 Phase 6 INTEGRATION** (`#albedo-values`: commit #19, #20, #18 &mdash; #21 already in). **L11 P6 PRODUCTION COMPLETE + #21 INTEGRATED &mdash; 24/30 produced / 21/30 integrated &middot; HTML 323,139 B.**

**Last updated:** 2026-06-04 (**LESSON 11 PHASE 6 PRODUCTION COMPLETE + #21 INTEGRATED; #19/#20/#18 PRODUCED, NOT YET INTEGRATED &mdash; 24/30 produced / 21/30 integrated &middot; HTML 323,139 B.** Single chat, INTEGRATION inline. Cleanup: obsolete scratch retired to `lesson_11_p5_inserts_READY.txt.deleted` (toolset has no hard-delete; rename-out is the inert retirement). PRODUCTION (all 4 verified on disk): #18 `lesson_11_18_albedo_range_chart.svg` (6,633 B, `ar`, 800x680; black&rarr;white albedo rail + 6 material zones + sRGB 0-255 row + red avoid end-caps + green 0.30-0.80 most-common band; cairosvg light+dark render-checked, footer-tip overflow shortened); #21 `lesson_11_21_srgb_linear_diagram.svg` (6,833 B, `sl`, 800x680; sRGB vs linear gradient bars + gamma curve corrected to true s^2.2 points + 2 worked-value boxes; midpoint labels moved onto dark halves for orange contrast); #19 `lesson_11_19_material_albedo_library.png` (112,852 B, 2000x2000, 4x4 self-labeling swatch grid, snow 0.85&rarr;charcoal 0.04, baked name+albedo+sRGB per cell &mdash; **c19 SKIPPED**, self-labeling, L10 `c22` precedent); #20 `lesson_11_20_saturation_albedo.png` (50,528 B, 1800x1000, 3 red swatches high/med/low sat with baked albedo 0.30/0.50/0.78 &mdash; **c20 SKIPPED**, self-labeling, default). Both PNGs Pillow-host built DIRECTLY on the bridge + written straight to WSL (no base64); NO BlenderMCP scene build (pure Pillow), scene untouched, nothing to RESTORE. Both inline SVGs hand-written per v6, standalone keeps `color="#222"`. INTEGRATION (bottom-up #21&rarr;#19&rarr;#20&rarr;#18): **#21 COMMITTED** before `<h3>Testing Albedo Accuracy</h3>` (314,433&rarr;323,139, +8,706), inlined (root `color="#222"` stripped, re-indented figure col20 / svg col24 / children col28, decorative `=====` comments pruned, svg-root continuation attrs aligned col29, gradient comment collapsed to 1 line, role/aria/title/desc/id preserved). **#19 dryRun verified clean, NOT committed** (chat hit tool-use limit). #20 + #18 pending. No new em-dashes; pre-existing body em-dashes near anchors untouched.)
**Project root:** `\\wsl$\Ubuntu\home\practicalace\projects\blender_course`

---

## Next step: RESUME Lesson 11 Phase 6 INTEGRATION (`#albedo-values`) &mdash; finish #19, #20, #18

P6 PRODUCTION is DONE (all 4 figures on disk, verified) and **#21 is already INTEGRATED**. HTML is at **323,139 B**. Three figures remain to integrate, all inline, no overlays to build (`c19`/`c20` both SKIPPED &mdash; self-labeling). This is HTML-edit-only: NO BlenderMCP, scene already clean, nothing to RESTORE, no figures to re-produce.

Files on disk (do NOT rebuild): `images/lesson_11_18_albedo_range_chart.svg` (`ar`), `images/lesson_11_19_material_albedo_library.png`, `images/lesson_11_20_saturation_albedo.png`, `images/lesson_11_21_srgb_linear_diagram.svg` (`sl`, already in HTML).

Procedure: `copy_file_user_to_claude` to refresh the live HTML, confirm it is at **323,139 B**, and re-verify the three remaining anchors GLOBALLY UNIQUE on the fresh copy (each count = 1). Then insert **bottom-up #19 &rarr; #20 &rarr; #18** (`edit_file` dryRun &rarr; commit &rarr; `get_file_info` per insert), `oldText` = the col-20 h3 anchor, `newText` = figure block + blank line + the same h3 verbatim:

- **#19** PNG, plain `<figure>` (no overlay) before `<h3>Albedo for Non-Dielectric Materials</h3>`. Alt: "A 16-cell reference grid of common materials ordered from light to dark, each cell showing its name, measured albedo value, and sRGB base color: fresh snow 0.85 down to charcoal 0.04." Figcaption: "Material albedo reference library. Sixteen common materials with their measured albedo and sRGB base color, ordered from snow at 0.85 down to charcoal at 0.04."
- **#20** PNG, plain `<figure>` (no overlay) before `<h3>Common Material Albedo Values</h3>`. Alt: "Three red swatches of the same hue at high, medium, and low saturation, labeled with effective albedo 0.30, 0.50, and 0.78. More saturation means lower effective albedo." Figcaption: "Saturation and albedo. The same red hue at three saturations: the pure-saturated swatch has the lowest effective albedo, the pale tint the highest."
- **#18** inline SVG `ar` before `<h3>Why Albedo Ranges Matter</h3>`. Inline per v6: strip root `color="#222"`, re-indent figure col20 / svg col24 / children col28, svg-root continuation attrs aligned col29, prune decorative `=====` comments, keep structural comments, preserve role/aria/title/desc/id. The standalone disk `.svg` keeps `color="#222"` &mdash; only the inlined HTML copy drops it. (#21 `sl` was inlined exactly this way; mirror it.)

Figures sit at default section depth (`<figure>` col20, contents col24) &mdash; confirmed against existing `#albedo-values` markup. Pre-existing body em-dashes near the anchors (lead "pretty colors&mdash;it's", "measurable property&mdash;not") must be preserved verbatim; edits touch only the h3 lines. No new em-dashes in figcaptions/alt/SVG text.

Final QA on a refreshed copy after all three commits (expected end state **24/30 integrated**): `<figure>` 24 (+4 from P5's 20), `<svg>` 22 (+2: #18 + #21 inline; #19/#20 are PNG-no-overlay), `<figcaption>` 24; 0 `color="#222"`; 0 `colour`; em-dash U+2014 = 77 unchanged whole-file; 0 en-dash; 0 literal U+00B7; new ids `ar-title`/`ar-desc` + `sl-title`/`sl-desc` unique; `sl-srgb`/`sl-lin` gradient ids unique; only intentional dupes remain D1 `le-title`/`le-desc` &times;2; both new SVGs parse + re-rasterize dark-mode-safe from the live HTML. **`c19`/`c20` SKIPPED** (self-labeling, L10 `c22` precedent). Then update `lesson_11_integration_progress.md` (Status bullet + header to PHASE 6 COMPLETE / 24-30 / 24-30) and repoint this `session.md` to **P7 `#advanced-pbr-parameters`**.

After P6: **P7 `#advanced-pbr-parameters`** (#22/#25 SVG + #23/#24/#27 PNG + #26 reuse by D2 &mdash; the &ge;5 phase: PRODUCTION 2 sub-chats, INTEGRATION borderline-inline) &middot; P8 `#project` (#28/#29). `#summary` carries no figures &mdash; no phase.

**Carry forward all locked rules (below + v6 LD1&ndash;21).**

---

## Read at chat start

1. `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\session.md` (this file)
2. `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_11_integration_progress.md` (the L11 plan + Status log; P1 + P2 + P3 + P4 logged COMPLETE, header at PHASE 4 INTEGRATION COMPLETE / 17-30 produced / 16-30 integrated; the figure inventory, 14/16 split, 8-phase plan, D1/D2, the #14 produce-in-P4-A / integrate-in-P5 split, and the id-prefix + `c{N}` registries all locked)
3. `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_11_image_requirements.md` (the 30-figure source spec + AI prompts + placement-in-HTML map per section id)
4. `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\images\IMAGE_STYLE_GUIDE.md` (v6 &mdash; current; locked conventions LD1&ndash;21)
5. `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_06_completion_handoff.md` (locked inline pattern + all carry-forward rules)

---

## L11 outcome so far

P1 `#what-is-pbr` (3) &check; COMPLETE &mdash; #1 PNG + `c1`, #2 inline SVG `tp`, #3 inline SVG `le` (D1 produce-once). P2 `#energy-conservation` (3 + #3 reuse) &check; COMPLETE &mdash; #4 PNG + `c4`, #5 PNG color-dependent + `c5`, #30 PNG color-dependent + `c30`, + #3 `le` reused-by-ref (D1, 2nd-ref figcaption, ids not renamed). P3 `#fresnel-effect` (3) &check; COMPLETE &mdash; #6 PNG host (Route 1 Pillow-vignette) + `c6`, #7 inline SVG `fa`, #8 inline SVG `fc` (Schlick-faithful curves). P4 `#microfacet-theory` (6, own INTEGRATION chat by the &ge;6 rule) &check; COMPLETE &mdash; P4-A produced #9 `mf` / #10 `ms` / #11 `mk` + #14 `ca`; P4-B produced #12 (`c12`) / #13 (`c13`) / #26 (`c26`); P4 INTEGRATION placed the six `#microfacet-theory` figures (#9/#10/#11 inline SVG + #12/#13/#26 PNG) and built + realized `c12`/`c13`/`c26`. #14 `ca` is produced but integrates in P5. **17/30 produced / 16/30 integrated &middot; HTML 294,731 B.** Craft notes: #1 + #4 + #6 + #12 + #26 sourced via the Pillow-vignette overlay-host route, built DIRECTLY on the bridge (the reliable Claude&rarr;WSL PNG route &mdash; do not hand-transcribe base64, it silently corrupts). #5 + #30 = color-dependent renders (v6 LD17, Standard view transform); #30 used a white room (documented deviation). #13 = a STANDARD Cycles render (highlight SHAPE, not marker color, carries the message; AgX kept). The P4-A/P4-INTEGRATION SVGs are hand-written per v6 house style, cairosvg 1:1 light+dark render-checked + XML-well-formed before write/inlining. All `c{N}` overlays pixel-verified 1:1 over their PNGs via cairosvg composite before commit; `c4`/`c6`/`c12`/`c13`/`c26` realized (were reserved). At P4 INTEGRATION the #13 pill centers were corrected from the plan estimate to the measured sphere centers after eyeballing the composite. No type-flips. Remaining: P5&ndash;P8 (13 figures), per the phase plan.

---

## L09 outcome (all 10 phases COMPLETE)

P1 `#snapping-system` (3) &check; &middot; P2 `#grid-snapping` (4) &check; &middot; P3 `#vertex-snapping` (5) &check; &middot; P4 `#numerical-input` (7) &check; &middot; P5 `#measurement-tools` (5) &check; &middot; P6 `#unit-systems` (3) &check; &middot; P7 `#symmetry-workflows` (8) &check; &middot; P8 `#professional-tips` (11) &check; &middot; P9 `#project` (5) &check; &middot; **P10 `#summary` (4) &check; COMPLETE**. **55/55 produced / 55/55 integrated &middot; HTML 502,906 B.**

---

## Standing deferred follow-ups (NOT part of the doc-maintenance sweep)

- **Browser-QA pass for Lesson 11** (the 16 P1&ndash;P4 figures so far. P1: #1 `c1` overlay alignment over the host PNG, #2 `tp` + #3 `le` inline SVGs. P2: #4 `c4` overlay alignment over the energy-budget host PNG (title / material names / % labels / legend land inside their bands), #5 `c5` caption pills + centered `Same Total Energy` band over the glow render, #30 `c30` CORRECT/INCORRECT pills + red-X over the white-room render, #3 `le` 2nd-ref inline SVG. P3: #6 `c6` two caption pills + `0&deg;`/`85&deg;` angle pills land inside the two water-panel clear bands, #7 `fa` angle diagram (rays / energy-weighted arrows / legend), #8 `fc` dielectric-vs-conductor curve graph. P4 (NEW): #9 `mf` 4-panel microfacet cross-section, #10 `ms` multiple-scattering profile, #11 `mk` masking/shadowing pair (all three inline SVG &mdash; confirm `currentColor` in dark mode + footer/legend `&#183;` separators render); #12 `c12` over the roughness reference chart (title + 6 zone labels land in the top/bottom clear bands, 0.0/1.0 pills sit on the rail ends), #13 `c13` over the anisotropic-highlights render (the two pills sit under the correct spheres at x440/x1478, brush-direction arrow lands on the right highlight band), #26 `c26` over the 2&times;3 anisotropic grid (6 cell labels + arrows land in their cells). Verify dark + light at 1920/1024/414: overlay text stays inside clear bands at all widths, pills clear the panels/spheres, inline SVGs inherit `currentColor` in dark mode, and the `le` reuse renders identically to the 1st ref. Accumulate with later L11 phases.)
- **Browser-QA pass for Lesson 09** (all 55 integrated figures &middot; dark + light, 1920/1024/414 widths). Newest = #52&ndash;#55 (`#summary`): the 3 inline SVGs #52 `tk` / #53 `sr` / #55 `qc` + the #54 `c54` warning-vignette overlay. Plus #47&ndash;#51 (`#project`) and the earlier-phase figures.
- Browser QA pass for Lessons 05 / 06 / 07 / 08 (accumulated; L08 image work COMPLETE 40/40, PARKED).
- Browser QA pass for Lesson 07 &middot; HELD-OPEN flags only (#20 / #21 menu-cascade overlays).
- Optional progress-doc archive sweeps for Lessons 05 / 06 / 07 / 08.
- Lesson body Knife Tool Modes table edit in `lesson_06_edit_mode_essentials.html` (low-priority).
- **Browser-QA pass for Lesson 10** &mdash; DONE / CLEARED 2026-06-03 (COMPLETE / CLEAN, recorded in `lesson_10_integration_progress.md`).

---

## Locked rules carry forward

Full list in `lesson_06_completion_handoff.md` and the carry-forward blocks in `lesson_07/08/09/10_integration_progress.md` + the L11 progress-doc Locked-rules block. All UNCHANGED: WSL `\\wsl$\Ubuntu\` paths only (never `\\wsl.localhost\`); `Filesystem:edit_file` dry-run &rarr; commit &rarr; `get_file_info` verify; `Filesystem:write_file` for new files / full-doc overwrites (never `create_file`); `copy_file_user_to_claude` to refresh the working HTML copy before grep/sed + verify anchor uniqueness first; no new em-dashes (middle-dot `&#183;` entities; pre-existing em/en-dashes preserved verbatim through exact anchors; content entities `&#215;`/`&#8211;`/`&#8594;`/`&#183;` etc are fine; NOTE the L11 body carries pre-existing em-dashes &mdash; preserve every one); `tool_search` for deferred tools; brand palette governs all overlays/SVGs; inline-SVG conventions (strip root `color="#222"` on inlining, re-indent, collapse multi-line attrs, prune decorative comments, keep structural comments, preserve role/aria/title/desc/id); figure indentation depth from parent container (section-level 20-space; card-internal deeper-indent 24-space); PNG overlay = wrapper-div + overlay-SVG, viewBox = PNG pixels, NO baked labels; pixel-verify every PNG overlay via cairosvg-composite-over-PNG (Claude-side) or PIL-composite (bridge-side) BEFORE commit; color-dependent render recipe v6 LD17 (F12 `render.render` not `render.opengl`; AgX&rarr;Standard view transform; 0.05 world; locked L09Orange/L09Cyan marker values; RESTORE AgX + base values at close) for #5 / #15 / #30; theory-heavy sourcing (Pillow-vignette host OR Canva/Adobe raster) is a PRODUCTION-time call per figure; BlenderMCP lowercase `blender:execute_blender_code` + clean-scene-manually via `bpy.data.objects.remove` + BLENDER_EEVEE (EEVEE_NEXT&rarr;EEVEE fallback) + Cycles for off-screen reflection/refraction; NEVER `read_factory_settings`; inline all render helpers per call (namespaces do not persist); scene state between PRODUCTION chats is always a clean base (RESTORE at close; STOP if a leftover `_WORK` collection or hidden base objects are found); pre-plan sub-chat splits for &ge;5-figure phases (P4, P7) + INTEGRATION-own-chat for &ge;6 (P4); plan-lock before build; warn Ray before context tightens rather than letting automatic compaction happen.

---
