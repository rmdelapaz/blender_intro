Blender Mastery Course · Lesson 09 ("Precision Modeling Techniques") · start the L09 image pipeline (planning + Phase 1 setup). Mirrors the L07/L08 pipeline.

Read first, in order:
1. `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\session.md`
2. `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_06_completion_handoff.md` (locked inline pattern + all carry-forward rules)
3. `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_08_integration_progress.md` (the most recent completed pipeline you're mirroring — placement table, prefix registry, overlay conventions, composite-overlay font-size formula `fs = round(22 × viewBox_w / 1920)`, small-frame fs-floor, axis-tinted/synthetic-overlay/warning-vs-positive variants, indent-depth-derivation rule, all in the Status bullets)
4. `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_09_image_requirements.md` (already written — 55 figures; 12 SVG recommendations: #9 snap menu · #18 rotation angles · #15 math expressions · #23 measurement overlays · #26 real-world dimensions · #36 project-setup workflow · #37 snapping decision tree · #38 duplication-methods comparison · #47 T-connector spec · #52 toolkit summary · #53 shortcuts reference · #55 quality checklist; AI prompts, SVG recs, placement suggestions, naming convention, priority tiers)

L09 source files (both already on disk):
- Lesson HTML: `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_09_precision_modeling_techniques.html` (224,251 B; 11 sections, ids: `#why-precision` · `#snapping-system` · `#grid-snapping` · `#vertex-snapping` · `#numerical-input` · `#measurement-tools` · `#unit-systems` · `#symmetry-workflows` · `#professional-tips` · `#project` · `#summary`)
- Image requirements: the doc in read-item 4

Status: L08 is image-complete at 40/40 (produced + integrated); only standing item is an optional L08 browser-QA pass on the accumulated estimated-position flags, parked — NOT part of L09. L09 has its requirements doc but no production/integration work yet.

Scope this chat (propose-then-lock per decision, in writing, before executing):
1. Verify the requirements doc against the live HTML — the doc names sections by DESCRIPTIVE NAME ("### Section: Grid Snapping and Alignment"), not `#id`; reconcile each to a real HTML section id (mapping looks clean: Why Precision Matters→`#why-precision`, Grid Snapping and Alignment→`#grid-snapping`, etc.) and confirm every placement anchor card/heading resolves. Confirm the 55-figure count and naming-convention table.
2. SVG-vs-PNG split (start from the doc's 12 SVG recs; reconcile to the house palette + hand-coded inline-SVG conventions; flag any infographic-class figure that's a Canva-raster-fallback candidate vs hand-coded).
3. Phase plan, section-aligned (55 figures across 11 sections → expect more/larger phases than L08's 9; pre-plan sub-chat splits for any phase ≥5 figures and own-chat INTEGRATION plan-locks for any phase ≥6).
4. ID prefix registry (SVG prefixes + reserved `c{N}` PNG-overlay namespaces).
5. Write `\\wsl$\Ubuntu\home\practicalace\projects\blender_course\lesson_09_integration_progress.md` (placement table, prefix registry, locked rules carry-forward, decisions-pending block).
6. Repoint `session.md` at L09 Phase 1 PRODUCTION.

All locked rules carry forward unchanged (WSL `\\wsl$\Ubuntu\` paths only; `edit_file` dry-run→commit→`get_file_info` verify; `Filesystem:write_file` for new files, never `create_file`; no new em-dashes, middle-dot `·` separators; `tool_search` for deferred tools; brand palette governs all overlays/SVGs; BlenderMCP clean-scene-manually + BLENDER_EEVEE fallback). Warn me before context tightens rather than letting compaction hit.
