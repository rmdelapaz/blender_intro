Open L23 of the Blender Mastery Course. L22 (Depth of Field and Focus) is fully COMPLETE and closed — do not touch it.

Project root: \\wsl$\Ubuntu\home\practicalace\projects\blender_course

Read order before doing anything:
1. session.md  (top block + L22 archive)
2. images/IMAGE_STYLE_GUIDE.md  (v6 — all asset conventions)

Then to open L23:
- Confirm the L23 lesson HTML filename and its section IDs by reading the live HTML (UTF-8-aware Python count, not shell grep).
- Capture the L23 invariant baseline: bytes, figure/figcaption/img/svg counts, mermaid count, em-dash U+2014 count, color="#222" count.
- Create lesson_23_integration_progress.md as the source of truth (mirror the L22 doc's structure: §1 close-target invariants, §2 roster, §3 id-prefix registry, §4 c{N} overlay registry, §5 production cadence, §6 status log).
- Then plan the roster with me (tiers, SVG-vs-PNG split, render recipes) before producing anything.

Standing rules (unchanged, in session.md):
- \\wsl$\ paths only; 0 new em-dashes (use &#183;), preserve all pre-existing literal U+2014 verbatim and re-verify count at every checkpoint.
- write_file for new files/full overwrites (never create_file); edit_file dryRun→commit→re-copy+recount per edit.
- cairosvg pixel-verify Claude-side before EVERY PNG-overlay commit.
- Blender is true-pristine; verify get_scene_info before and after any bridge work, restore at close.
- Warn me to start a fresh chat when this one gets long rather than letting it auto-compact.

Start by reading session.md and the style guide, then confirm the L23 filename + baseline and report back before planning.
